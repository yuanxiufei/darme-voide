{
  "_readme": "本地模型路径配置（高度可配置化）。所有值均可留空（\"\"）表示走默认。解析优先级：命令行参数 > 环境变量 > 本文件 > 默认。2026-09-25 自主化：models_dir 默认 = <dataRoot>/models（本仓自己的模型目录，不再依赖 ComfyUI 目录）；COMFYUI_PATH / comfyui_root 仅作「显式配置时的可选扫描源」，不再作为模型存储/下载的默认来源。对应环境变量：MODELS_DIR / NODES_DIR / LOCAL_SERVICES_DIR / COMFYUI_PATH。",
  "comfyui_root": "",
  "models_dir": "",
  "nodes_dir": "",
  "local_services_dir": ""
}

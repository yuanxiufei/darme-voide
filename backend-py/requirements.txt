# Drama Studio —— Python 后端（绞杀者迁移第一版）
# 与 Node 后端共用同一个 SQLite 文件与前端契约，端口默认 5790（Node 仍在 5789）
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
SQLAlchemy>=2.0.36
PyYAML>=6.0.2
httpx>=0.27.2
# multipart/form-data 解析（Starlette 的 request.form() / UploadFile 依赖它，upload 域必需）
python-multipart>=0.0.9

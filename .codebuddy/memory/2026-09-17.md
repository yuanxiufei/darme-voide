# 2026-09-17

## S7 第 65 步：搬 `reference/ComfyUI` + `reference/minimax-h3-comfyui` 的**有用功能** ⇒ **H3 阶段 2 接线完成**

**用户要求**：「这两个项目有用的功能搬过来」+「先实现功能，不需要下载模型」✓。

### 搬了什么（四个能力 + 一个真接线）

| 来源 | 搬到哪 | 内容 |
|---|---|---|
`ComfyUI/server.py`（六条 API） | `app/local_services/h3/comfyui_client.py`（第 64 步 ✓） | `/prompt` `/history` `/view` `/free` `/object_info` `/upload/image` `/system_stats` `/interrupt`，零依赖 ✓ |
`ComfyUI` 前端 `graphToPrompt` 的算法 | `app/local_services/h3/workflow.py`（第 64 步 ✓） | **UI 图 → API 格式** + 注入 ✓ |
`ComfyUI` 的 `object_info` 驱动字段映射 | `workflow.set_combo()`（本轮 ✓） | **COMBO 必须按选项表换** ✗（拿不到选项表就换不了画幅 ✓） |
`comfy_extras/nodes_minimax_h3.py` 的 schema | `workflow.frames_for_seconds()` / `seconds_for_frames()`（本轮 ✓） | H3 的 **24fps + 17k+5 帧网格** ✓（5s ⇒ 124 帧 ✓，与模板默认值一致 ✓） |
`ComfyUI` 的 node mode 语义（0/2/4） | `workflow.activate_nodes()`（本轮 ✓） | 模板把尾帧通道 **mute** 掉 ⇒ 要用就激活 ✓ |
`minimax-h3-comfyui/workflows/*.json` | `h3/workflows/`（+ 署名 README ✓） | T2V / I2V 模板 ✓ |
`minimax-h3-comfyui` 的**协议语义** | `app/local_services/h3/server.py::_run_h3`（**本轮真接线** ✓） | 组装 → 注入 → 上传首尾帧 → 提交 → 轮询 → 取片 → 落盘 → `/files` 回 URL → **`/free`** ✓ |

### 明确**没搬**的（并写清理由 ✓）

* **ComfyUI 引擎本体**（1063 文件 / 835 .py ✗）—— 它是**机器相关外部服务** ✓，由
  `model_manager.py` 装到 `configs/model-paths.json` 指的目录 ✓；`local_model_scan.py` 也**明确**
  把 ComfyUI 定为「只读扫描来源、非后端」✗ ⇒ vendor 进来只会让镜像与依赖链失控 ✓。
* **`scripts/install-sageattention-sm120.sh`**（它是 **RTX 50 系 / sm120 专版** ✗）—— 我们目标是
  **A5000 / sm86** ✓，照搬等于误用编译目标（脚本自己就有 arch 断言 ✓）。要装得按 sm86 走 ✓。
* **`benchmarks/results.json` 的耗时数字**—— 机器是 **RTX 5090 / sm120** ✗ ⇒ **不能当 A5000 的 ETA** ✗。
  可取用的只有**量级**：冷启动 ~53s、5 秒片 ~85s ⇒ 超时默认给 **1800s** 是故意的 ✓。

### 阶段 2 实现要点（都写进了 `server.py` 模块头 ✓）

1. **模板选择**：有 `first_frame_image` ⇒ I2V ✓，否则 T2V ✓。
2. **注入点**（按模板真实结构 ✓）：`MiniMaxH3ImageToVideo.prompt` ✓｜`PrimitiveFloat«DURATION (seconds)»` 注入**秒** ✓
   （帧数由模板自带的 `ComfyMathExpression` 按 24fps/17 网格算 ✓，另回一个 `expected_frames` 供对账 ✓）｜
   `ResolutionSelector«RATIO»` **按选项表**换成 `9:16 (Portrait)` 这类完整选项 ✓｜`UNETLoader.unet_name` 按
   `checkpoint_map` 选 FL2VA/Ref2VA ✓（**模板是 FL2VA 形态 ⇒ ref2va 只换名字并 `warnings` 声明参考图/音频不生效** ✓）｜
   `RandomNoise.noise_seed` ✓｜`SaveVideo.filename_prefix` 带 task_id ✓。
3. **首尾帧**：ComfyUI **只认自己 input 目录里的文件名** ✗ ⇒ 先把 URL/data:/本地路径的内容 `POST /upload/image` ✓，
   再把返回的文件名写进 `LoadImage.image` ✓；尾帧还要**激活通道**（模板里是 mute 的 `[DISABLED]` 节点 ✓）
   并**接回主节点** ✓（判据：通道**末端** = 不被同通道节点消费的那个 ✓，不写死节点 id ✓）。
4. **缺节点**：用 `/object_info` **提前**探，直接把类名与 `model_manager.py install-nodes` 指引写进失败原因 ✓
   （**绝不在推理路径里偷偷下载** ✓，符合用户要求 ✓）。
5. **产物**：`/view` 取片 → 落盘 `H3_OUTPUT_DIR` → 回 `PUBLIC/files/{name}` ✓（`/files` 只允许文件名、挡路径穿越 ✓）；
   **失败路径也调用 `/free`** ✓（24G 卡否则下一镜 OOM ✗）。
6. `/healthz` 现在回报 **ComfyUI 可达性 + 模板是否在盘 + 安装指引** ✓（排障一次看全 ✓）。

### 自检（**不下载模型、不需要 GPU** ✓）

* 新增 `tests/h3_stage2_test.py`：起**真 HTTP stub ComfyUI** ✓，走完
  「T2V 跑通 ✓ / I2V 跑通 ✓ / 注入落在正确节点 ✓ / 首尾帧上传 ✓ / 尾帧通道激活并接回 ✓ /
  缺节点点名且**没提交** ✓ / ComfyUI 不可达报真原因 ✓ / `/free` 被调 ✓」—— **20/20** ✓。
* 翻转 3 处当初**刻意钉住**的「阶段 2 未接线」断言 ✓：`h3_chain_test` ⑦ ✓（13/13）、
  `h3_backend_live_test` ⑥ ✓、`local_services_live_test` 的 h3 块 ✓。

### ⚠️ 本轮踩到的两件事（都有价值）

1. **自检抓出真缺口**：我激活了尾帧通道却**忘了把它接回主节点** ✗（`19/20` 时暴露 ✓）⇒ 用「通道末端」通用判据接上 ✓
   （写死 144/146 那种节点 id 会在模板更新时崩 ✗）。
2. ⚠️⚠️ **我一度凭印象说「转换器有 bug」并写进注释** ✗ —— 实际读代码后才发现 `index += 1` 本来就在 dict 判断**之前** ✓
   （槽位早就消费了 ✓）。**那条注释是编造的** ✗，已改成事实描述 ✓。
   **规则：改代码/写注释前，先读**那一段**代码**（`read_file` 是几秒的事 ✓，凭印象的「修复」会在代码里留下假知识 ✗）。
   真正需要修的是**分寸**：`widgets_values` 尾部**纯 dict** 的多余项（第三方伪 widget 标记 ✓）应**容忍** ✓，
   而非 dict 的多余值仍按「字段错位」拒绝 ✓ —— 已按此实现 ✓（`h3_comfyui_test` A2⑭ ✓）。

### 实测（本机实测，非推算 ✓）

* `h3_stage2_test` **20/20** ✓｜`h3_comfyui_test` **39/39** ✓（+1 伪 widget 容忍 + 真实图谱改**严格模式** ✓）｜
  `h3_chain_test` **13/13** ✓（翻转后 ✓）｜路径守卫 **漂移 0** ✓｜三道仓库守卫：断链 0 ✓ / 记忆致命 0 ✓ / 自检 ✓
* **全量：73 套 / 2603 项 / 0 失败** ✓（两批实测：w1c 0-37 → 1746 项 ✓；w2c 37-73 → 857 项 ✓；本地服务全停 ⇒ 活体套件正确 SKIP ✓）

## S7 第 66 步：**ComfyUI 能力全覆盖** —— 「功能全都要，但由我们的形式承载」

**用户要求**：「一定要实现正常 ComfyUI 所有的功能，只不过表现形式不同而已」✓。

### 做法：先把「所有功能」列全，再逐条给等价物

* 判据不是文档、是**源码**：`reference/ComfyUI/server.py` 里 **26 条公开路由**（逐条 grep ✓）
  + `api_server/routes/internal/` 的 **5 条**桌面壳内部端点 ✓ ⇒ 落成
  `docs/comfyui-capability-matrix.md`（**逐条对照表** ✓ 含状态与「刻意不做」的理由 ✓）。
* 结论：**ComfyUI 能做的，8765 服务都能做** ✓；调用方**不必直连 8188** ✓。
  引擎本体**仍不 vendor** ✓（外部服务 ✓）—— 变的是「用法全在我们手里」✓。

### 交付

| 文件 | 内容 |
|---|---|
`app/local_services/h3/comfyui_client.py` | **补齐到全覆盖**：`features` `embeddings` `model_types` `models{folder}` `folder_paths` `internal_files` `object_info_all` `view_metadata` `upload_mask` `queue` `queue_remaining` `clear_queue` `delete_queue_item` `history_all` `clear_history` `jobs` `job` `cancel_job` `cancel_all_jobs` **+ `validate()`（提交前预检，ComfyUI 没有这条能力 ✗）**；`upload_image`/`upload_mask` 的 multipart 抽成 `_upload_multipart` 共用 ✓ |
`app/local_services/h3/server.py` | 新增 **22 条路由**（原 4 条 + 新 18 条 ✓）：`/v1/workflows/{validate,run,task}`（**任意工作流**：UI 或 API 格式 ✓）｜`/v1/catalog/{nodes,nodes/{cls},models,folders,files/{type}}`｜`/v1/system`｜`/v1/queue{,/clear,/{id}}`｜`/v1/history`(GET/DELETE)｜`/v1/jobs{,/{id}/cancel}`｜`/v1/interrupt` `/v1/free`｜`/v1/upload/image` `/v1/view`（**把 501 桩换成真实现** ✓） |
`tests/comfyui_capability_test.py` | **25/25** ✓：每条能力都有对应断言 + **反套套逻辑**（stub 侧每个端点必须真被请求过 ✓） |

### 关键设计决定（都写进了模块头 ✓）

1. **通用能力是底座，H3 是上面的一条业务线** ✓ —— `POST /v1/video_generation` 保留原协议
   （后端 `minimax` 适配器按它对接 ✓），通用入口是 `POST /v1/workflows/run` ✓。
2. **参数注入用显式清单** `[{class_type, field, value, title?}]` ✓ —— 刻意不做「猜哪个字段是提示词」
   的魔法 ✗（猜错一次就是白跑一轮 ✗）。
3. **不做**（设计决定，不是没做 ✓）：`GET /`（ComfyUI 的前端网页 ✗ 我们有自己的前端 ✓）、
   `GET /extensions`（前端 js 扩展，界面层概念 ✗；真正的自定义节点由 `/v1/catalog/nodes` 覆盖 ✓）、
   `/internal/logs*`（桌面壳日志订阅 ✓）。
4. `GET /v1/catalog/nodes` 默认**只回类名** ✓（全表在装了很多节点时可几 MB ✗）；要细节用 `?class_name=` ✓。
5. `/v1/jobs` 在老版本 ComfyUI 上 `supported:false` ✓（**不假装成功** ✓）。

### ⚠️ 自检再次抓到**我的夹具错**（连续两次同一类问题：求值顺序 ✗）

* ④' 断言失败：我把 `bodies = …记录…` 取在**两次调用之前** ✗ ⇒ 断言看到的是空列表 ✓（而 detail 显示的是调用后的值 ✓）。
  **规则：断言里要用到的「记录/计数」，必须在**触发动作之后**再读** ✓。
* ③' 之前同类：stub 只存「最后一次请求体」✗ ⇒ 「清空 + 删条目」互相覆盖 ✓ ⇒ 改成存**列表** ✓。
  **规则：一个端点会被多次调用时，夹具要存**序列**而不是快照** ✓。

### ⚠️ 环境限制（如实记）

本轮**全量 74 套没能重跑** ✗：容器把长命令持续转后台并**杀掉**（`a1x.txt` 落成 0 字节 ✓ —— 与 09-16 同一现象 ✓）。
因此文档里的 73 套 / 2603 项**仍是本轮之前那次实测** ✓；本轮新增 `comfyui_capability_test` 25 项 ⇒
**下一轮全量实测**才是准数 ✓（**不做算术推算** ✗ —— 09-16 那次算错 13 项就是教训 ✓）。

### 本轮实测（跑得完的那些 ✓）

`comfyui_capability_test` **25/25** ✓｜`h3_comfyui_test` **39/39** ✓｜`h3_stage2_test` **20/20** ✓
（前两份在客户端补齐**之后**跑的 ✓ ⇒ 客户端重构没回归 ✓）

## S7 第 67 步：⚠️ **方向纠正** —— 用户指出「是实现功能，不是调用 ComfyUI」✗ ⇒ 开自研推理引擎

**用户原话**：「功能一定要实现不能是简单的模仿」→「**是实现功能不是调用comfyui，你明白吗**」✓。

**我承认走错了方向** ✗：第 64-66 步做的 `comfyui_client` + `workflow`（UI→API 转换）+ 8765 门面 +
`services/comfyui.py`，**本质都是「调用别人的推理服务」** ✗（哪怕加了 DB/恢复/记账，那是把**调用**
工程化了 ✓ 但**没有实现生成能力本身** ✗）。产品要的是：**我们后端自己加载模型 → 采样 → 解码 → 出片** ✓。

### 动手前核实的两条**决定性事实**（都影响实现方式 ✓）

1. ⚠️ **`reference/ComfyUI` 是 GPL-3.0** ✗（`reference/ComfyUI/LICENSE` 第 1 行 ✓）——
   ⇒ **它的源码不能搬进本仓库** ✗（会把整个项目传染成 GPL ✗）。
   所以「实现」只能是：**算法自己写**（公开论文公式 ✓）+ **脚手架用 Apache/MIT 许可的库**
   （`torch` / `diffusers` / `safetensors` ✓）；**一行 ComfyUI 源码都不抄** ✗。
2. ⚠️ **本机后端 venv 一个推理依赖都没有** ✗（`torch` / `numpy` / `diffusers` / `transformers` /
   `safetensors` / `PIL` / `av` **全部 NO** ✓）⇒ 要真出片必须装（torch 系 ~2.5GB ✓）+ 有权重与 GPU ✓；
   但**算法层可以零依赖先落地并验证** ✓（这就是本步做的 ✓）。

### 本步交付：自研引擎的**纯算法层**（`app/services/engine/`，零依赖 ✓ 26/26 ✓）

| 模块 | 内容 |
|---|---|
`engine/schedules.py` | **自己实现**的 σ 调度：`karras`（Karras 2022 的 ρ-ramp ✓）/`exponential`/`normal`/`ddim_uniform` ✓ + `interpolate_sigmas`（步数细分，**对数域**插值 ✓）+ `timesteps_for` ✓ |
`engine/geometry.py` | 帧网格 **17k+5 / 24fps**（5s ⇒ 124 帧 ✓）、分辨率 `step=32` 对齐、**像素预算→尺寸**、潜空间帧数（**压缩比必须显式给** ✓ 不猜 4/8 ✗） |
`engine/sampler.py` | **自己实现**的采样循环：`euler` / `heun` / `multistep` ✓；**张量无关**（只要求 `+ − ×float` ✓）⇒ 真机是 `torch.Tensor`、自检是 12 行假张量 ✓；带进度回调 + 采样前校验（序列反了/末位非 0 **当场报错** ✓ 否则静默出噪声 ✗） |
`tests/engine_core_test.py` | **26/26** ✓（不需要 torch / GPU / 权重 ✓） |

**两个与我们已核实的上游实测对齐的点**（真·验证，不是自说自话 ✓）：
`0.65MP@16:9 ⇒ **1088×608**`、`0.98MP@16:9 ⇒ **1344×768**`（与参考 README 的实测输出一致 ✓）。

### ⚠️ 自检抓到的三处**真错**（全部已修 ✓）—— 这就是「不能是模仿」的落地方式

1. **我写的 `linear_log_snr` 调度在数学上不成立** ✗：本仓 σ 范围到 **14.6（>1）**，而它的
   `α=√(1−σ²)` 在 σ>1 无定义 ⇒ **直接删掉** ✓（宁缺毋滥：**只留能被自检验证的数学** ✓）。
2. `interpolate_sigmas` 的语义应基于**步数**（`len=步数+1` ✓）—— 我先用 `len` 乘 ✗、改完又手滑 `+1` ✗
   ⇒ 自检**两次**都抓到（11 → 22 vs 期望 21 ✓）。
3. 像素预算→尺寸：必须**向上**取整 ✓ 且宽高**各自独立**开方 ✓（用「像素÷宽」回算高会得 736 ✗，
   而实测是 768 ✓）。

### 与上一轮工作的关系（**待用户裁决** ✓）

第 64-66 步那套（客户端 / 门面 / `services/comfyui.py` / 路由 / 两个自检）**暂时保留** ✓
（它不破坏任何东西 ✓ 且"调外部服务"在某些场景仍有用 ✓），但**它不是本需求的主线** ✗ ——
用户若要删，直接删 ✓。**主线是 `app/services/engine/`（自研）** ✓。

### 下一步（**已向用户提出，等拍板** ✓）

1. 先实现哪条链路：**SDXL 图像**（结构最简、最快能自验 ✓）/ **H3 视频**（产品核心，但需 DiT +
   Qwen3-VL 文本编码器 + 视频/音频 VAE + INT8/GGUF 量化 ✗ 极重 ✓）；
2. 是否允许装 **torch + diffusers**（~2.5GB ✓ Apache-2.0 ✓ 不抄 GPL ✗）。

## S7 第 68 步：自研引擎·**权重体检层**（纯 Python 读 safetensors + 就绪报告 + 路由）+ 修一个**会真炸的 DB 缺陷**

### 交付

| 文件 | 内容 | 实测 |
|---|---|---|
`services/engine/safetensors.py` | **纯 Python 读 safetensors**（零依赖 ✓）：只读头部（几 KB）就能拿到张量名/形状/精度 ✓；检测**截断**、头部自相矛盾、尾部无人声明字节、未知 dtype；`read_tensor_bytes` 按需切片且**拒绝超大块**（防手滑把 19 GiB 拉进内存 ✗） | — |
`services/engine/inventory.py` | **就绪报告**：按真实清单 `configs/models.json` + `model_paths` 逐条对 `models_dir` ⇒ 缺哪些 / 坏哪些 / 要下多少 GB / 显存余量（**权重精确、激活估算、带免责声明** ✗ 不承诺 ✓） | — |
`routers/engine.py` | `GET /api/v1/engine/readiness` ✓、`POST /api/v1/engine/inspect`（可给**清单 key** ✓ 前端不用拼路径 ✓） | 188 路由 ✓ |
`tests/engine_inventory_test.py` | 34 项（合成 safetensors 造「合法/截断/矛盾/尾部垃圾/未知 dtype」✓ + 真实清单跑就绪报告 ✓ + 路由层 ✓） | **34/34** ✓ |
`tests/engine_core_test.py` | 上一轮的调度/几何/采样（26 项） | **26/26** ✓ |

设计要点：**缺 ≠ 坏** 分开报 ✓；**结构坏 ⇒ 阻断就绪**，**大小与清单不符 ⇒ 仅可疑**（社区量化版体积会合法地变 ✓）。

### ⚠️ 自检抓到两处**真错**（都已修 ✓）

1. `inspect()` 里我 `seek(8)` 之后又读 8 字节当头部长度 ✗ —— 长度在**文件开头**（offset 0）✗，
   读到的是**数据区开头** ⇒ `data_bytes` 算成 0 ⇒ **合法文件被判「截断」** ✗（自检 ① 当场红 ✓）。
   修法不只是改 seek：**头部只解析一次**、长度与内容**同源**返回 ✓（`_read_header` ✓），这类错没复发余地 ✓。
2. 就绪判定里我给 `broken` 加了 `and not sizeMismatch` ✗ ⇒ **截断的组件因为「大小也不符」被漏出断障名单** ✗
   （自检 ⑳ 抓到 ✓）。正确语义：**结构坏一律阻断；只有结构完好的才可能"仅可疑"** ✓。

### ⚠️⚠️ 顺手抓到一个**会真炸的缺陷**（不在本轮计划内，价值最高 ✓）

跑冒烟时 **3 条红** ✗：

```
FAIL schema: models == Node DDL table set / table count == 29 / matches Node schema.ts count
→ 随后 sqlite3.OperationalError: no such table: comfyui_runs（启动期崩溃恢复直接崩 ✗）
```

**根因**：`core/db.py` 的建表策略是「**只有库文件不存在才 `create_all`**」✗，理由是「Node 侧维护 schema、
避免两边 DDL 漂移」—— 可 **`backend/` 已经被删掉了** ✓ ⇒ **那条理由过期了** ✗，后果是
**Python 侧任何新加的表在已有库上静默不生效** ✗ ⇒ 碰新表的代码（启动恢复 ✓）在真机上 `no such table` 把服务拖崩 ✗✗。

**修法（两条，各治一半）**：

1. **DB 层**：改成 `_ensure_tables()` —— **每次启动都 `create_all(checkfirst=True)`** ✓（只建缺失的表 ✓
   不 ALTER / 不 DROP ✓）+ **列体检**（已存在表上缺列就打印可执行的 ALTER 提示 ✓，**只报不改** ✗ ——
   改用户的库必须由人决定 ✓）。`reopen_engine()` 同步改（切到旧库也要补表 ✓）。
   实测：`[db-py] schema upgraded: created ['comfyui_runs']` ✓ ⇒ 冒烟 **476/476** ✓。
2. **冒烟守卫**：加 `PY_ONLY_TABLES` 白名单（承认「Python 侧有意新增的表」✓），而不是去改冻结快照 ✗
   —— **快照的全部价值就是它 immutable** ✗，改它等于废掉这个守卫 ✓。白名单则两全：
   有意新增放行 ✓、**任何其它意外表照旧报警** ✓。

> **教训（架构类，已入记忆）**：**「等对方/等真机」的临时约定会在对方消失后变成陷阱** ✗
> —— 「Node 侧维护 schema」这条注释曾经是对的 ✓，`backend/` 删掉后它就变成了「新表不生效」的**静默故障源** ✗。
> **规则：删掉一个协作方之后，回头搜一遍「以它为理由」的约定，逐条重判。**（同类：第 63 步那条
> 「CosyVoice 接口按猜想写的、等真机再核」也属于这一类 ✓。）

## S7 第 69 步：自研引擎·**管线编排**（46 项 ✓ 零依赖）+ 干跑后端 + 两个端点

### 交付

| 文件 | 内容 | 实测 |
|---|---|---|
`services/engine/pipeline.py` | **编排层**：六阶段 `plan → encode → init → sample → decode → write` ✓，每阶段计时、进度事件、**协同式取消**（阶段边界 + **每个采样步** ✓）、**错误归因**（阶段 + 步号 + 原类型 ✓）。算法层是**真用**的：`sample` 阶段真调 `sampler.sample` ✓、σ 真来自 `schedules` ✓、尺寸/帧数真来自 `geometry` ✓ | — |
`services/engine/dryrun.py` | **干跑后端**（零依赖 ✓）：`TinyTensor`（纯 Python `list[float]` ✓）+ `sha256` 条件（**刻意不用内置 `hash()`** ✗ —— 它每进程加盐，会让「同种子可复现」失效 ✗）+ `Random(seed)` 初始噪声 ✓。输出**一律标 `synthetic: true`** ✓、落盘是**干跑报告 JSON 不是 mp4** ✗ | — |
`routers/engine.py`（+2 端点） | `POST /engine/plan`（请求 → 具体数字 ✓ **不跑推理** ✓，采样器写错**这里就 400** ✓）、`POST /engine/dry-run`（整条管线走一遍 ✓，永远 `synthetic: true` + `videoPath: null` ✓） | 239 条已注册 ✓ |
`tests/engine_pipeline_test.py` | 46 项 ✓ | **46/46** ✓ |

**可插拔后端**是本步的关键设计 ✓：管线**不碰张量** ✗ —— 真机上后端收 `torch.Tensor` ✓、自检里收
`TinyTensor` ✓ ⇒ **同一段采样代码**，不需要两套 ✓（这也是「零依赖可先验算法/编排」的落地方式 ✓）。

**干跑模型的刻意设计** ✓：`denoised = (1−g)·cond + g·x`，`g→0` 当 `σ→0` ✓ ⇒ ① 采样**真收敛**
（断言「末态到条件的距离 < 初始的一半」✓）；② `g` 依赖 σ ⇒ **三种采样器轨迹真的不同** ✓
（若模型与 x 无关，euler/heun/multistep 会一模一样 ⇒ 那种自检是**假绿** ✗ —— 这条断言专门防它 ✓）。

### ⚠️ 自检抓到的问题（都在本轮修掉 ✓）

1. **取消的步号只在文案里** ✗：初版 `error` 只写 `{stage, message}`，步号埋在中文句子
   「已取消（停在去噪采样 第 3 步）」里 ⇒ 前端想画「停在第 3/20 步」还得**解析中文** ✗。
   自检 ㉗ 要求 `error.step == 3` ⇒ 当场红 ✓ ⇒ 改成**结构化字段** ✓。
2. **镜像常量守卫抓到 1 条漂移** ✗：`dryrun.py` 的 `json.dumps(..., indent=1)`（干跑报告要给人看 ✓）
   被「Node `JSON.stringify` 无空格」守卫拦下 ✓。**判断**：这条守卫防的是「写共用 DB 列 / 发给厂商 /
   直接当响应体」三类字符串 ✓，而干跑报告**没有 Node 对应物** ✗、也不属于这三类 ✓ ⇒ 加白名单并写明理由 ✓
   （而不是把它压成一行 ✗ —— 那份报告是给人调试用的 ✓）。

### ⚠️ 如实汇报：本轮**没能**重跑全量（环境把长命令跳过了 ✗）

两批全量命令**都被环境跳过**（"chosen to run in the background"）✗ ⇒ 按本仓规则
**规模数字不推算** ✗ ⇒ `run_all.py` 头部与 README 改写为「**77 套**（新套件已注册 ✓）／
**断言总数待实测** ✗」，并保留最近一次实测（76 套 / 2688 项 / 0 失败 ✓）作为基准 ✓。

**本轮直接跑过、确认绿的部分**（不是推算 ✓）：引擎三套 **46/46 + 26/26 + 34/34** ✓、
层级守卫 **6/6** ✓（149→? 个 .py，违规 0 ✓）、路径+常量 **239 已注册 / 未注册 0 / 漂移 0** ✓、
冒烟 **476/476** ✓、前端调用覆盖 **5/5** ✓、`h3_chain` 13/13 ✓、`h3_stage2` 20/20 ✓、
`video_generation` 50/50 ✓。

### 教训（工具面，第 N 次）

**长命令会被环境跳过** ✗ —— 本轮两次全量都跳了 ✓。**规则：全量复测要么尽早起、要么改用「分批小命令」**
✓；并且**绝不用"应该有这么多项"来填文档** ✗（本仓已因推算错过两次 ✓）。

## S7 第 70 步：自研引擎·**加载计划**（28 项 ✓ 零依赖）—— 从「装齐没有」推进到「怎么喂进去」

### 交付

`services/engine/loader.py`（+ `GET /api/v1/engine/load-plan` ✓）：

| 能力 | 说明 |
|---|---|
**角色/精度/量化方案** | 角色取清单的 `kind` ✓；精度从**逐张量 dtype** 归纳（`F8_E4M3` ⇒ fp8 ✓；有配套 scale ⇒ `fp8` ✓，否则只说需反量化 ✓） |
**层块识别** | 找「``<头>.<数字>``」里最大的一族 ⇒ `blockHead`/`blockCount` ✓；**不写死 `blocks`** ✗（`layers` / `transformer_blocks` 都认 ✓） |
**自洽性校验（不依赖架构知识 ✓）** | ① 层号**连续、不重** ⇒ 有洞就是「被截断或拼错」✗；② 低精度 `.weight` **必须**有配套 scale（否则反量化时才炸 ✗）；③ 体积与清单不符只是**可疑** ✓ |
**显存排班** ⭐ | 按**角色生命周期不重叠**推出顺序「文本编码器 → DiT → VAE」✓ ⇒ 峰值 = **最大那个 19.53 GiB**（不是三者之和 28 ✗），并给出每步「可释放 ✓」 |

### ⚠️ 自检抓到三处（两处是我写错 ✓，一处是**真不一致** ✓）

1. **自检期望写错** ✗：我以为 `prefix_groups(depth=2)` 会给 `blocks.0.attn` —— 实际是 `blocks.0` ✓
   （实现是对的 ✓，`depth=2` 正好够 `_blocks_of` 用 `rpartition` 拆「头+层号」✓）。
2. **合成权重落点与清单对不上** ✗：写到 `<root>/dit.safetensors` 而 `component_path` 拼的是
   `<root>/diffusion_models/dit.safetensors` ✗ ⇒ "文件其实在"却判成缺失 ✓。
3. ⚠️ **真不一致（值得记）**：`inventory.readiness` 里「大小与清单不符」是**可疑、不阻断** ✓，
   但 `loader` 初版把 status 的**全部 problems** 抄进自己 problems ✗ ⇒ `plan_stage.ready` 因此变 False ✗
   —— **同一件事在两个模块给了相反口径** ✗（自检 ㉓ 当场抓到 ✓）。
   修法：在 `loader` 里把「大小不符」**降级成 warning** ✓。**规则：同一判据跨模块必须同口径 ✓。**
4. 另修：`_mode_for` 初版乘了"量化系数 1.35" ✗ ⇒ 把 **19.53 GiB 的 fp8 主权重**判成「超尺寸」✗
   —— 错在把"整份反量化"当唯一做法 ✓（实际可逐层反量化 ✓）。改成按**文件体积**判 ✓，
   反量化开销降为警告 ✓。`residency.fits` 在**组件缺失**时也必须是 False ✗（初版按体积算 ⇒ 全缺时回
   `true` ⇒ 前端会读成「能跑」✗，最坏的一种误导 ✓）。

### 刻意**不猜**的部分（纪律 ✓）

真实权重还没下载 ⇒ **不假设**具体张量名前缀 ✗（写死 `blocks.N.attn.wq` 只在真机上错得莫名其妙 ✗）。
只做「如实归纳文件里实际有什么 ✓ + 校验它自洽 ✓」两件可验证的事。

**实测**：`engine_core` 26/26 ✓、`engine_inventory` 34/34 ✓、`engine_pipeline` 46/46 ✓、
`engine_loader` **28/28** ✓（引擎四套合计 **134** 项 ✓）；层级 6/6 ✓；
路径+常量 **240 已注册 / 未注册 0 / 漂移 0** ✓。

### ✅ 全量实测（本轮补齐 ✓）：**78 套 / 2762 项 / 0 失败**

- **绕开"长命令行被环境跳过"的招** ✓：把批次逻辑写进 `tmp/runbatch.py`，调用只要一行
  （`python tmp/runbatch.py 0 40 q1`）✓ ⇒ 立刻跑起来 ✓。
- ⚠️⚠️ **教训（工具有用）**：一条被判"chosen to run in the background"的命令
  **其实可能真的在后台跑完了** ✓ —— 我先按"跳过"当没跑 ✗，又起了一次 40-59 ✓，
  结果两批日志的**末两条一样** ✓ ⇒ 一查才发现 `q2.txt` 是 **40-78 的 38 套**（后台那次跑完了 ✓），
  我那次 40-59 反而被覆盖 ✓。**规则：命令被"跳过"后，先查产物（日志/输出文件）再决定要不要重跑 ✓。**
- 另有 q3（59-78 ✓）是重复的一次 ✓ ⇒ **日志去重后** 40 + 38 = **78 套** ✓ 全覆盖 ✓。
- **两项独立互证** ✓：2688（上一轮实测）+ 46 + 28（本轮新增两套）= **2762** ✓✓ 与日志实测合计一致 ✓。

## S7 第 71 步：引擎的**采样语义** —— 引导（CFG）+ 重标定 + 步区间/斜坡（管线套件 46 → 65 ✓）

### 为什么这块必须属于**引擎**而不是后端

「往条件更近的方向多走一点」这件事**与具体模型无关** ✓（只要求张量支持 `+ − * float` ✓，与 `sampler`
同一套鸭子类型约定 ✓）⇒ 换后端不该换引导语义 ✓。现状是管线只会**单条件直跑** ✗ —— 真后端一上来就是缺口 ✗。

`services/engine/guidance.py`（零依赖 ✓）：

| 能力 | 公式 / 说明 |
|---|---|
**CFG** | `guided = neg + scale·(pos − neg)` ✓；`scale == 1` 或没有负条件 ⇒ **短路返回正条件** ✓（不打多余算力 ✗） |
**重标定** | `guided ← guided·(rms(pos)/rms(guided))^rescale` ✓（CFG 会让方差变大 ⇒ 过曝/过饱和 ✗）。尺度取不到 ⇒ **明确跳过 + 说明** ✗（不假装做过 ✓） |
**步区间 / 斜坡** | 只在 `[start, end)` 施加 ✓；前 `rampSteps` 步从 1 线性升到目标 ✓（开局不被强引导锁死 ✗） |
**尺度探测** | `stat_scale()` 鸭子类型：`.std()`（torch ✓）→ `norm()/√len`（我们的 `TinyTensor` ✓）→ `None` ✓ |

管线集成：`GenerationBackend.encode_text` 推荐回 `{"positive","negative"}` ✓（只回单个条件**也兼容** ✓ ⇒
不引导不报错 ✓）；`model_fn` 里按步取 scale ✓；**如实报 `guidanceSteps`** ✓（生效了几步 ✓）；
进度事件带 `cfg=` ✓（用户能看见斜坡在爬 ✓）；计划里**提前警告代价** ✓（CFG 每步 2 次调用 ⇒ heun 每步 4 次 ✗）。

API：前端字段 `cfg` / `guidanceScale` / `guidanceRescale` / `guidanceStart|End|Ramp` 都收 ✓；
坏值 ⇒ **400**（而不是让 `float()` 抛出去变 500 ✗）✓。

### ⚠️ 自检/实现里抓到的问题

1. 我的测试传了 `object()` 当张量 ✗（意图是"能算术但**报不出尺度**"✓）⇒ 造 `_NoScale` 类 ✓。
2. `_branches()` 兼容层 ✓：dict 但没有 `positive` 键 ⇒ 当作"单个条件" ✓（不硬拆 ✗、不报错 ✗）。
3. 计划里的耗时警告按采样器给系数 ✓（heun ⇒ ×4 ✓，其余 ×2 ✓）。

### ⭐ 最有分量的一条断言

**「CFG 开启 ⇒ 每步真调两次模型」** ✓ —— 数后端调用次数：`cfg=1` ⇒ `calls == 步数` ✓；
`cfg=3` ⇒ `calls == 2×步数` ✓（12/6 ✓）。这条防的是「只改了个参数、其实没做引导」的**假绿** ✗。

**实测**：`engine_pipeline_test` **65/65** ✓（含 3 条路由级引导断言 ✓）。

### ✅ 全量重测（引导改变了项数 ⇒ 按规则**重新实测** ✗ 不推算 ✓）

**78 套 / 2781 项 / 0 失败** ✓（r1 0-40 → 1822 ｜ r2 40-59 → 408 ｜ r3 59-78 → 551，三批 `_BAD=0` ✓，范围互不重叠 ✓）
—— 独立互证 ✓：2762 − 46 + 65 = **2781** ✓✓。

## S7 第 72 步：**首帧条件（图生视频）** —— 引擎算掩码、后端套布局（管线套件 65 → **82** ✓）

### 交付

`services/engine/conditioning.py`（零依赖 ✓）+ 管线新增**可选阶段** `condition` ✓：

| 能力 | 说明 |
|---|---|
**掩码数学** | `权重 = strength × clamp(1 − (i − keep + 1)/fade)` ✓：`keep` 帧完全来自首帧 ✓、`fade` 帧线性过渡 ✓、其余为 0 ✓ |
**潜帧数** | `(帧数−1)//压缩比 + 1` ✓（复用 `geometry.latent_frames` ✓）；**没给压缩比 ⇒ 报错** ✗（不猜：猜错会把首帧放到错误的潜帧上 ✗） |
**参数校验** | `keep`/`fade` 越界、`strength` 不在 0..1 ⇒ **明确报错** ✓（而不是截断或静默忽略 ✗） |

**⭐ 本步最重要的设计取舍**：请求里有 `firstFrame` 但**后端没实现** `condition_first_frame` ⇒
**当场报错并把原因说清** ✗，**绝不悄悄按文生视频跑** ✗ —— 后者看着"更宽容" ✓，实际最坏：
用户要的是图生视频 ✓、拿到的却是无关产物 ✗ 而且**看不出来** ✗。

**分工（纪律 ✓）**：引擎只算**掩码**（与模型无关 ✓）；"怎么把图片潜变量和噪声拼起来"（通道维 / 时间维 /
mask 输入 ✓ 各家不同 ✗）是**后端自己的事** ✓ —— 引擎去猜布局只会在真机上错得莫名其妙 ✗
（与 `geometry`「不猜时间压缩比」同一条纪律 ✓）。

另加 **相对耗时系数** ✓：`costFactor = (步数 + 生效引导步数) × 采样器倍数` ✓（heun=2 ✓）—— **精确可算** ✓；
**刻意不给"秒"** ✗（那需要每步算力模型，我们还没有 ✓ ⇒ 硬报秒数就是编 ✗；等真后端跑一次按实测标定 ✓）。

### ⚠️ 本轮踩到/修掉的问题

1. **我又犯了同一个语法错** ✗：在中文文案里嵌引用用了**半角双引号** ⇒ `SyntaxError`（本会话第 3 次 ✗）。
   **规则（已入 MEMORY.md ✓）：中文串里要引用就用「」，不要用 `"`。**
2. `_NoConditionHook` 的**假绿陷阱** ✓：本想造"没有该钩子"的后端 ✓，但用 `__getattr__` 全转发会把干跑实现
   的同名方法也转过来 ✓ ⇒ 那条用例**永远绿** ✗。显式写 `condition_first_frame = None` 才是真的模拟"不支持" ✓。
3. ⑬' 的断言**过时** ✗：我改了引导警告文案（改成给"相对耗时倍数"✓），断言还在找旧词 ✗ ⇒ 按新语义改 ✓。
   **规则：改文案时顺手搜一遍引用它的断言 ✓。**

**实测**：`engine_pipeline_test` **82/82** ✓（46 → 62 → 65 → 82 ✓）；引擎四套合计 **170** 项 ✓；
`engine_loader` 28/28 ✓、`engine_core` 26/26 ✓、`engine_inventory` 34/34 ✓；层级 6/6 ✓；
路径+常量 **240 已注册 / 未注册 0 / 漂移 0** ✓；守卫自检 13/13 ✓；批次 0-40 `_BAD=0` ✓。

### ⚠️ 如实汇报：本轮**没能**跑成完整全量

环境**连跳 3 次**（0-40 重试一次才成 ✓；40-59 / 59-78 / 74-78 全被跳过 ✗）⇒ 按规则**不推算总数** ✗：
文档里的 **2781** 标注为「**最近一次完整实测**」✓，并写明引擎套件已增至 82 ⇒ **总数待重测** ✗。
本轮改为**逐个实测受影响套件**（引擎四套 + 层级 + 路径 ✓ 全绿 ✓）作为替代证据 ✓。

## S7 第 73 步：**torch 后端的接入点 + 依赖闸门**（让"最后一步"变小 ✓）+ 全量补齐实测

### 交付

`services/engine/torch_backend.py` ✓ + `GET /api/v1/engine/backends` ✓（241 条已注册 ✓）：

* **懒导入** ✓：模块 import **不需要 torch** ⇒ 没装也能测「它给出清楚的可行动报错」✓；
* **依赖探测**（6 项 ✓ 用 `find_spec` **不导入** ⇒ 毫秒级、无副作用 ✓）+ **可执行安装命令** ✓；
* ⭐ **两类错误分开** ✓：`reason="deps"`（去装包 ✓）vs `reason="pending"`（依赖齐了但**实现待写** ✓）
  —— 混成一句"后端不可用"会让人**查半天环境** ✗；
* `describe()`（不含张量操作 ⇒ 依赖缺失也能调 ✓）供 `/backends` 如实回答「现在能跑什么 / 要装什么」✓。

### 为什么**先不写**实现（这是刻意的 ✓）

本机核实：**torch / diffusers / numpy / safetensors / PIL 全都没有** ✗（连 `safetensors` 库都没有 ✓
—— 顺带印证第 68 步那个**纯 Python 读取器是必要的** ✓，不是"为了不装库" ✗）。
⇒ 张量挪 GPU 的实现**写了也一行都跑不了** ✗ ⇒ 按本仓纪律（没跑过不交付）**宁可先不写** ✓，
只把**位置与契约**钉死 ✓：真实现只需填那几个方法 ✓，**管线一行不改** ✓。

### ⚠️ 又一次半角引号（第 4 次 ✗）

在中文自检文案里嵌 `"` ⇒ `SyntaxError` ✗。**上一轮刚把这条写进 `MEMORY.md` 的「代码约定」** ✓，
这一轮又犯 ✓ ⇒ 说明光记没用，**要在写文案的当下自查** ✓（改完后先 `python -c` 语法检查一遍 ✓）。

### 前端：评估后**刻意先不动** ✓

`frontend/app/pages/models.vue` = **2253 行 / 98KB** ✗；`useApi.ts` 里**没有** comfyui 调用组 ✓
⇒ 盲改大 SFC 风险高 ✗（且"引擎面板放哪"需要用户的 UI 意图 ✓）⇒ 本轮不碰 ✓，留给下一轮并**先问位置** ✓。

### ✅ 全量实测（环境配合了 ✓）

**78 套 / 2806 项 / 0 失败** ✓（t1 0-40 → 1822 ｜ t2 40-59 → 551 ｜ t3 59-78 → 433，三批 `_BAD=0` ✓）
—— 独立互证 ✓：2798 − 82 + 90 = **2806** ✓✓。`engine_pipeline_test` 46 → 62 → 65 → 82 → **90** ✓。

## S7 第 74 步：⚠️ **本机没有 NVIDIA 显卡** ✗ ⇒ 装 CPU 版 torch ✓；`TorchBackend` 变成**真张量**实现 ✓

### ⚠️ 最重要的发现（推翻了"装 2.5GB CUDA 轮子"这个前提 ✗）

动手前先核实环境 ✓：**本机只有 Intel Iris Xe 集显** ✗、`nvidia-smi` 不存在 ✓、无 NVIDIA 驱动 ✓。
⇒ 装 CUDA 轮子（~2.5 GB ✗）在这里**毫无意义** ✓（没有 CUDA 设备 ✓；H3 的 19.53 GiB 权重在 CPU 上也不现实 ✗）。
改装 **CPU 版**（**124 MB** ✓）⇒ 本机能**真正验证张量与算法** ✓（数学与真机一致 ✓），
到 A5000 那台机器**代码一行不改** ✓（`device` 实测探测 ✓，`cudaAvailable` 如实报 ✓）。

### ⚠️⚠️ 环境级事实（值得记进 MEMORY.md ✓）：本机 **PyPI 下不动** ✗ ⇒ 必须走镜像 ✓

第一次 `pip install`（默认 PyPI ✓）**卡死** ✗：`download.pytorch.org` 的 124 MB **下完了** ✓（856 kB/s ✓），
但 PyPI 上的 numpy（12.6 MB ✗）**两次都在同一处卡住** ✓（日志停在 `Downloading numpy...` 无报错 ✗）。
**解法**：依赖走清华镜像 ✓ —— 同一批包 **20–58 MB/s** ✓✓，秒过 ✓。
**最终装法**（记下来 ✓）：`torch` 走 pytorch 官方 CPU 源 ✓ + **`--extra-index-url` 镜像** ✓
（这样 torch 的依赖从镜像下 ✓）；torch 的 124 MB 还**命中了 pip 缓存** ✓（第一次的下载没白费 ✓）。
装上：**torch 2.14.0+cpu / numpy 2.5.3 / safetensors 0.8.0 / pillow 12.3.0** ✓。

### 交付

1. **`torch_backend.py` 从"闸门"变成"真张量实现"** ✓：
   * 真 `torch.Generator().manual_seed(seed)` + 真 `torch.randn` ✓ ⇒ **同种子逐位可复现** ✓（自检实证 ✓）；
   * 引导/首帧/采样**全部跑在真张量上** ✓（与 `dryrun` 共用同一份 `sampler`/`guidance`/`conditioning` ✓）；
   * ⭐ **两个标志同时给** ✓：`realTensors=True`（张量真 ✓）**且** `synthetic=True`（画面**不真** ✗）
     —— 不许因为"用上 torch 了"就翻成 False ✗（自检专门钉这条 ✓）；
   * `load_weights()` 走 **`reason="pending"`** ✓（真模型装载待写 ✓）—— 与 `deps` 分开 ✓，两类治法不同 ✗。
2. **新套件 `safetensors_crosscheck_test.py`（11/11 ✓✓）**：拿**官方库**当我那个纯 Python 读取器的
   **参照实现** ✓ —— 名字/形状/精度/元数据逐项一致 ✓、`read_tensor_bytes` **逐字节相同** ✓、
   **截断两边都拒绝** ✓（证明我的截断检测不是一家之言 ✓）、我**手写**的表头官方能读懂 ✓。
3. `engine_pipeline_test` **90 → 98/98** ✓（真张量那批断言装好后**真的跑起来了** ✓，skip 0 ✓）。

### ⚠️ 自检抓到我自己三处写法问题（都已修 ✓）

1. **套套逻辑** ✗：`total_tensor_bytes == sum(各张量 nbytes)` —— 前者本就是后者的累加 ✓ ⇒ 恒真 ✓
   （这种断言只增加通过数、不增加信息 ✗）。改成与**数据区实际大小**比 ✓（独立事实 ✓）。
   同类：`float((first - first).norm()) == 0.0` ✗ ⇒ 改成**真收敛**（末态到条件距离 << 初始 ✓）。
2. **断言写死了"装没装"** ✗：`/backends` 与 `load_weights` 的断言假设"必然缺依赖" ✓ ⇒ 装上就红 ✓。
   改成**两种世界都成立** ✓（真张量 ✓ / 画面不真 ✗ / 不能出片 ✗ 这三条与装没装无关 ✓），
   缺依赖那条改用**模拟**验 ✓（`monkeypatch torch_available` ✓ —— 测试不该依赖真机装了什么 ✓）。
3. **半角引号第 4 次** ✗（写完就按 MEMORY.md 的规矩自查 ✓ 当场修掉 ✓）。

⚠️ 全量复测（79 套 ✓ 或 80 套）**没跑成** ✗（环境连跳多次 ✓）⇒ 文档仍标 2806 = **最近一次完整实测** ✓，
并注明「管线套件已增至 98 ✓、新增交叉验证 11 项 ✓ ⇒ **总数待重测** ✗」（按规则**不推算** ✗）。

## S7 第 75 步：**把真模型这条路实现出来**（DiT + 权重装载；26/26 ✓）—— 按用户"以实现功能为先" ✓

用户明确：**先把能力在代码里实现完** ✓（含测试与守卫 ✓），**等实现完再去工作站跑真流程** ✓
—— 不要为"当场看到出片"反复折腾本机环境 ✗（本机也没 NVIDIA 卡 ✓）。已记入 `MEMORY.md` 的工作方式 ✓。

### 交付（三块，全部 CPU 上可验证 ✓）

| 文件 | 内容 | 实测 |
|---|---|---|
`services/engine/dit.py` | **本项目自己的 DiT** ✓（真 `nn.Module` ✓：3D patch embed + 逐块 **adaLN-Zero** 调制注意力/MLP + 最终 adaLN ✓，出自公开论文、**不是**抄仓库 ✗）。结构**全部显式**由 `DiTConfig` 给 ✓（含 `vae_scale` ✓ **不给默认** ✗）；可从权重 `__metadata__` 自举 ✓；流匹配 `x0` 转换（velocity/epsilon/sample ✓）单列 ✓ | — |
`services/engine/weights.py` | **权重装载** ✓：**自己算差集**（missing / unexpected / **形状不符** ✓）再装 ✓ —— 刻意**不**依赖 `load_state_dict(strict=False)` 的静默行为 ✗：键差一个字母就是"少装一层却照常跑"✗（产物**看着像成功** ✓）。形状不符 ⇒ **中止装载** ✓ 且**不污染**目标参数 ✓；改名只认**显式 key_map** ✓（不猜前缀 ✗） | — |
`torch_backend.py`（接线） | `load_weights()` **真实现** ✓：按清单找主 DiT ✓ → 权重缺失就 `pending` 并**指向加载计划**（还差多少 ✓）→ 装好即 `self._model` ✓ ⇒ `denoise` 走**真前向** ✓（`flow_match_x0(DiT(x,σ,ctx))` ✓） | — |

### ⭐ 两条最有分量的自检（`engine_dit_test.py` **26/26** ✓）

1. **权重往返逐位相同** ✓✓：建模型 → 存 safetensors → **新建**模型（先确认参数确实不同 ✓）→ 装载 →
   输出 `allclose(atol=1e-6)` ✓ ⇒ 权重真的进了**正确的模块** ✓（不是"装上了但错位" ✗）。
2. **形状不符 ⇒ 中止且不污染** ✓✓：故意给错 `out.weight` 形状 ⇒ `complete=False` + 报错含「中止装载」✓
   + **目标模块输出与装载前逐位相同** ✓（半装状态最难查 ✗）。

### ⚠️ 被自己的断言绊了一下（很有价值 ✓）：**adaLN-Zero 的调制层初值全 0**

我写"改 σ ⇒ 输出必须变" ✓，结果**红了** ✗ —— 因为 adaLN-Zero **按定义**就是恒等起步 ✓
（调制层零初始化 ⇒ 条件**暂时完全无效** ✓），不是"条件没接" ✗。
⇒ 断言改成**成对** ✓：初始化时无效 ✓ **且**把调制层置非零（"训练一下"）后**必须**生效 ✓✓
—— 这样既钉住了结构语义 ✓、又保住了"防假绿"的初衷 ✓。

### ⚠️ 三处自查（都被自检/工具抓到 ✓）

1. `_PlanProbe()` —— 我在 `describe()` 里用了**未定义的类** ✗ ⇒ 改成直接算 ✓。
2. **半角引号第 5 次** ✗（`engine_dit_test.py` 与 `weights.py` 各一处 ✓）⇒ **改规矩** ✓：
   写完文件**立刻**用正则 `[\p{Han}]"[\p{Han}]` 搜一遍 ✓（比逐个撞编译错误快得多 ✓）。
3. **断言随实现推进而"过时"** ✓：管线套件里 ⑥''' 断言"实现未写 ⇒ pending" ✓，实现好之后就红了 ✓
   ⇒ 改成按**新语义**断言（`pending` = **权重没下载** + 报文指向加载计划 ✓）✓。
   （同类已第二次：上一轮 ⑮''' 也是"装没装"写死 ✓ —— 记进 MEMORY.md ✓。）

**实测**：引擎六套 ✓ `engine_core` 26/26 ｜ `inventory` 34/34 ｜ `loader` 28/28 ｜ `pipeline` **98/98** ｜
`dit` **26/26** ｜ `safetensors_crosscheck` 11/11 ⇒ 合计 **223 项** ✓（全套件 81 套 ✓）。
⚠️ 全量仍未复测 ✗（环境跳过 ✓）⇒ 文档标「管线 98 ✓ + 新增 dit 26 ✓ ⇒ 总数待重测」✗（不推算 ✓）。

## S7 第 76 步：**真 VAE + 真出片（mp4）** ⇒ 整链跑通 ✓（`engine_io_test` 30/30 ✓）

### 交付

| 文件 | 内容 |
|---|---|
`services/engine/vae.py` | **真视频 VAE** ✓（3D 卷积 + GroupNorm + SiLU 的对称编解码 ✓ **编码也有** —— 首帧条件要用 ✓）。明确标注三处**简化** ✗：只做空间压缩（不做时间压缩 ✓）/ 不做变分采样 ✓ / 不是 H3 的 VAE ✓ |
`services/engine/media.py` | **落盘**：帧 → **真 mp4**（外部 **ffmpeg 9.0.1** ✓ 本机已装 ✓ 与色校正功能共用 ✓）+ 音频 → **真 wav**（标准库 `wave` ✓）。两条硬要求：① 产物必须能被 **ffprobe 独立复核** ✓；② **值域必须显式** ✓（猜错 = 全白/全黑**且不报错** ✗）。另显式处理 yuv420p 的**偶数尺寸**要求 ✓（**报错**而不是偷偷裁一行 ✗） |
`torch_backend`（接线） | `attach_vae()` ✓ ⇒ `decode` 出**真帧** ✓；`write` 有真帧就落**真 mp4** ✓（`realFile=true` ✓），但 `synthetic` **仍为 true** ✓（未训练 VAE ⇒ 内容不是真结果 ✗ —— 两个标志各说各的事 ✓） |

### ⭐⭐ 整链集成自检（`case_backend_pipeline`）—— 它一口气揪出 **4 个单测抓不到的真 bug** ✓✓

链路：**合成权重 → `load_weights` 真装 DiT → 真前向 → VAE 解码 → ffmpeg → 真 mp4 → ffprobe 复核尺寸/fps/帧数 ✓**。

| # | 暴露的 bug | 性质 |
|---|---|---|
① | `spatial_scale` 属性写 `2^层数`，实现是 `2^(层数−1)` ✗ | **属性与实现不一致** ✗（调用方按属性备尺寸 ✓ 却与模型对不上 ✓）—— 最阴的一类 ✓ |
② | DiT 位置表**写死 4096** ✗，真实 token 数 **12920** ✗ | 只在真分辨率下才炸 ✗ ⇒ 改成**按需线性插值** ✓（标准做法 ✓） |
③ | 条件宽度一律用类默认 512 ✗，模型 `text_dim=16` ✗ | 计划里有、实现漏了 ✓ |
④ | 一维条件向量 ⇒ `text.mean(dim=1)` 直接 `IndexError` ✗ | 维度没归一化 ✓ ⇒ 现在 1D/2D/3D 都收 ✓ + 广播到 batch ✓ |

> **结论（值得记）**：这四条**没有一条**能被模块级单测抓到 ✓ —— 它们只在"**真尺寸 + 真模型 + 真落盘**"
> 串起来时才现形 ✓。**规则：实现链路的终点（出片）必须有一次端到端自检，且用外部工具复核产物** ✓。

**实测**：`engine_io_test` **30/30** ✓（含 VAE 形状/校验 ✓、真 mp4 + ffprobe 复核宽高/帧数/时长 ✓、
真 wav + 标准库读回 ✓、越界钳位并如实回报 ✓、整链出片 ✓）；`engine_dit_test` 26/26 ✓；
`engine_pipeline_test` **98/98** ✓（占位一维首帧那条路被我改崩过 ✓ 自检 ㊾ 当场红 ✓ 已修 ✓）。
引擎七套合计 **253** 项 ✓。

## S7 第 77 步：**文本编码器 + 注入式 tokenizer** ✓ ⇒ 最后一个占位（条件）换成真实现

### 交付

`services/engine/text_encoder.py` ✓：

* **真 TE 结构** ✓（token embed + 位置编码 + 标准 **pre-norm** transformer 块 ✓ —— 与 DiT 的 adaLN 块
  是两种用途 ✓ 分开写 ✓）；输出 ``(1, L, output_dim)`` ✓（与 `DiTConfig.text_dim` 对齐 ✓）；
* ⭐ **tokenizer 走注入** ✓（:class:`Tokenizer` 协议 ✓）：本仓**不内置任何词表** ✗
  —— 内置一个"看起来能用"的词表只会在真机上错得莫名其妙 ✗；
  `StubTokenizer`（确定性哈希 ✓ 自检用 ✓，**明确标注它不代表真词表** ✗）+
  `HFTokenizer`（`transformers` 适配器 ✓ 懒导入 ✓ 没装就给出**带镜像提示**的可行动报错 ✓）；
* ⭐ **截断必须被看到** ✓：`tokenize_prompt` 回报 ``(ids, 原长, 截断标记)`` ✓ ——
  静默截断最坏 ✗（用户以为提示词全进了 ✓）；`encode_text` 的返回值里也带 `tokens/truncated/byBranch` ✓；
* **padding mask** 支持 ✓（全 padding 行强制放开首位置 ✓ ⇒ 不出 NaN ✓）；但**如实标注**：
  `dit` 目前**忽略** mask ✗（没假装处理了 ✓）。

### 后端接线

`attach_text_encoder(config, tokenizer)` ✓ ⇒ `encode_text` 走**真编码器** ✓（否则仍是 sha256 占位 ✓，
两者用 `textEncoder` 字段区分 ✓：`reference-untrained` / `sha256-placeholder` ✓）；
`describe()` 新增 `textEncoderLoaded` + `tokenizer` 名 ✓。

### ⚠️ 自检抓到的（命名类 ✓）

`encode_text` 返回值里我把 **meta 字典**塞进了 `ids` 键 ✗ ⇒ 调用方 `len(ids)` 拿到的是"字典键数"
而不是 token 数 ✓（自检 ⑯ 当场红 ✓）⇒ **命名要名副其实** ✓：`ids` = 真 id 列表 ✓、明细放 `byBranch` ✓。
另：`text_encoder.py` 与自检里各有一处半角引号 ✓（都在**文档串**里 ⇒ 无害 ✓，第三处在真字符串 ⇒ 已修 ✓）。

**实测**：`engine_text_test` **21/21** ✓（含 ⭐ **注意力真的混了 token**（改首位 ⇒ 其他位置也变 ✓ ——
纯逐位置 MLP 就不会变 ✗）✓、**位置编码生效** ✓、**TE→DiT→VAE→真 mp4** 整链 ✓）；
`engine_pipeline_test` **98/98** ✓、`engine_io_test` **30/30** ✓。
引擎八套合计 **274** 项 ✓。

### 至此 **PENDING_PARTS 里只剩"真权重相关"的三条** ✓

① H3 权重未下载 ✗ ② H3 张量命名 → 本仓模块名的**映射表** ✗ ③ H3 的 `DiTConfig`/`TAEConfig`（从权重
元数据读或人工给 ✓）。**代码侧的结构/管道/自检都已就位** ✓ —— 工作站上要做的是**填表 + 下权重** ✓，
正是用户定的节奏（"实现完再去工作站跑流程" ✓）。

## S7 第 78 步：**首帧条件接真 VAE 编码** ✓ ⇒ 条件路径的最后一个占位也换掉了

### 交付

* `media.py` 加**图片 I/O** ✓：`load_image_tensor`（图片 → ``(1,3,1,H,W)`` **单帧**张量 ✓，
  **尺寸显式** ✓ 并**如实回报"被拉伸"** ✓ —— 不做 letterbox/crop ✗（那会改构图 ✓ 而"悄悄改构图"
  是最难看出来的错 ✗）、值域与 `write_video` 同一口径 ✓）+ `write_image`（帧 → **真 PNG** ✓ 调试/预览用 ✓）；
* `torch_backend.condition_first_frame` 走**真路径** ✓：图片 → `vae.encode` → 按掩码混进**第 0 个潜帧** ✓
  （``keep>1`` 时如实提示"只有第 0 潜帧能来自图片" ✓）；没挂 VAE ⇒ 走占位 ✓ 并**自述用了哪条路** ✓；
* `conditioningNote` 属性 ✓ + 管线把它并进 `conditioning.backendNote` ✓
  —— **后端自述、管线透出、前端可显示** ✓（不猜 ✗：`dryrun` 没有这个键就不加 ✓）。

### ⚠️ 自检抓到的一处（形状校验写太死 ✗）

我把编码结果的**全部维度**（含 T ✓）拿去和潜变量比 ✗ ⇒ 图片天然是**单帧**（编码出 ``T=1`` ✓）
而潜变量有 T 帧 ✓ ⇒ **自己把自己拦住了** ✗（自检 ㉟ 当场红 ✓）。修：**只比 C/h/w** ✓。

### 实测（`engine_io_test` **39/39** ✓ —— 从 30 涨到 39 ✓）

⭐ 两条最硬的：① 第 0 潜帧与**图片编码结果逐位相同** ✓（真走了 VAE ✓ 不是"看着像混了"✓）；
② **掩码为 0 的潜帧完全没被动** ✓（`torch.equal` 逐位 ✓ —— 不是"整段被首帧污染" ✓）。
另：经**管线**也如实带出 `backendNote.mode == "vae-encode"` ✓，且**图生视频整链仍能出片** ✓。

引擎八套合计（本轮后）**core 26 ｜ inventory 34 ｜ loader 28 ｜ pipeline 98 ｜ dit 26 ｜ io 39 ｜
text 21 ｜ 交叉验证 11 = 283** 项 ✓。

## S7 第 79 步：**全量补齐实测** ✓ 82 套 / 2911 项 / 0 失败（三批 m1/m2/m3）

这一轮（真张量 → DiT → VAE → mp4 → TE → 首帧）改动很大 ✓ ⇒ 整体复验一次 ✓：

```
m1  0-40 → 40 套 / 1822 项   ｜  m2 40-60 → 20 套 / 582 项  ｜  m3 60-85 → 22 套 / 507 项
三批 _BAD=0 ✓，范围互不重叠 ✓  ⇒ 实测 82 套 / 2911 项 / 0 失败 ✓
与 `run_all.TESTS` 的权威套件数 **82** 一致 ✓
独立互证 ✓：2806 + 11（交叉验证）+ 8（管线 90→98）+ 26（dit）+ 39（io）+ 21（text）= **2911** ✓✓
```

### ⚠️ 工具教训（已写进 MEMORY.md ✓）：**批次日志标签不可复用** ✗

`runbatch.py` 按**标签**写 `tmp/<label>.txt` ✓。本轮我先用 `z1` ✗ —— 那是**上一次会话**用过的标签 ✓
⇒ 读到的是**陈旧结果**（17 套 ✓）却一度当成"刚跑的" ✓，差点据此判断"跑完了" ✗。
**规则：每轮换新标签**（m1/m2/m3 ✓），并在读结果时先看 `_BAD=` 与套件数是否与预期相符 ✓
（**数不对就是信号** ✓ 不要硬信 ✓）。

另：`runbatch.py` 只在**结束时**写文件 ✓ ⇒ 后台跑的过程中读不到进度 ✓（下次可改成逐行追加 ✓）。

**当前套件规模**：82 套 / 2911 项 ✓（引擎八套占 **283** 项 ✓）。文档（`run_all.py` 头 + 两份 README +
套件树）已同步为实测值 ✓，不再有"待重测"字样 ✓。

## S7 第 80 步：**长视频分段**（`segments.py` 零依赖 ✓ 54/54 ✓）—— 计划数学 + 首帧传递

### 交付（`services/engine/segments.py`）

把"想要的总帧数"拆成模型能吃的若干段 ✓，三条**不变量**就是它的全部价值 ✓（自检逐条钉住 ✓）：

1. **长度合法**：每段帧数落在模型网格 ``17k+5`` 上 ✓（否则模型拒绝或静默改 ✗）；
2. **无缝覆盖**：相邻段**恰好**重叠 N 帧 ✓，生成区间并集**恰好覆盖** ``[0, total)`` ✓；
3. ⭐ **保留帧数守恒**：各段 ``keep`` 之和 **== 总帧数** ✓✓（拼接器的硬契约 ✓ —— 少了缺帧 ✗、多了重复 ✗）。

配套 `build_segment_requests` ✓：把调用方从上一段产物里取出的**边界帧张量**落成 **PNG** ✓
并塞进 `GenerationRequest.first_frame` ✓ ⇒ 直接走 :mod:`torch_backend` 的**真 VAE 编码**首帧条件 ✓
（第 78 步刚打通的那条路 ✓）；没给边界帧 ⇒ 该段**不带首帧**且**如实标注** ✓（不假装有 ✓）。

### ⚠️ 两处自查（都是"我自己的实现/断言"错 ✓）

1. ⭐ **实现里的守恒断言抓到了实现自己的 bug** ✓✓：非末段 `keep` 忘了把重叠的 ``overlap`` 帧**交出去**
   ✗ ⇒ 保留 592 ≠ 总 496 ✓ 当场报错 ✓ ⇒ 修（非末段 `keep_to = length − overlap` ✓，
   末段才 `min(length, total−start)` ✓）。
   **这条值得记**：把"硬契约"写成**运行时断言** ✓，它会在写错的当下就拦住我 ✓（而不是等到拼接时才发现缺帧 ✗）。
2. **我的测试期望写错** ✗：写成「carry == 上一段 endFrame − 1」✓，但有重叠时
   ``start_{k+1} = end_k − overlap`` ✓ ⇒ 接缝帧是 ``start−1`` ✓（= 上一段**最后一个保留**帧 ✓）。
   改成**两个口径同时断言** ✓（更强 ✓）。
3. 另修一处接口不一致 ✓：`build_segment_requests` 文档说收 ``(3,H,W)`` ✓ 但 `write_image` 只认 5 维 ✗
   ⇒ 加 `_as_frame_batch` 归一化 ✓。

**实测**：`engine_segments_test` **54/54** ✓（不变量按性质扫了 5 组参数 ✓ 而不是记死数字 ✓；
含**接缝帧落 PNG 并读回核对** ✓、报错文案**带可调量** ✓）。引擎九套 = core 26 ｜ inventory 34 ｜
loader 28 ｜ pipeline 98 ｜ dit 26 ｜ io 39 ｜ text 21 ｜ segments **54** ｜ 交叉验证 11 ✓（套件 83 套 ✓）。
⚠️ 全量总数**待下一轮实测** ✗（本轮没跑 ✓ 按规则不推算 ✓）。

## S7 第 81 步：**命名映射骨架**（`mappings.py` ✓ 17/17 ✓）—— 给工作站铺"填表"的路

### 交付

* **预设表** `PRESETS` ✓（`dit` / `text-encoder` / `vae` ✓）：外层前缀（`model.` / `model.diffusion_model.`
  / `first_stage_model.` ✓）、`layers.N`→`blocks.N` ✓、`attention`→`attn` ✓、`mlp.fc1/fc2`→`mlp.0/2` ✓、
  词表/位置表命名 ✓ …… 都是**常见约定** ✓（不是穷举 ✗）；
* **合并规则** `MergeRule` ✓：q/k/v 三段**按声明顺序拼**成融合权重 ✓ ——
  不只是改名 ✗，是**张量运算** ✓（顺序错 ⇒ **不报错但结果全错** ✗ ⇒ 顺序必须显式 ✓）；
* ⭐ **干跑报告** `plan_mapping` ✓✓：每条规则的**命中数** ✓、能对上的目标键 ✓、
  **没人填的目标键** ✗、**没用上的源键** ✗ ⇒ 工作站上"填表"= **按报告补规则** ✓
  而不是对着上千个名字硬看 ✗。

### ⚠️ 往返自检抓到一个**真设计缺陷** ✗✓（这是我写这套自检的全部理由 ✓）

**往返**：真 DiT → 反向改写成"别家命名"（前缀 + `layers.N` + `attention` + 分开的 wq/wk/wv ✓）→
套预设**装回去** ⇒ 输出**逐位相同** ✓✓。

* 缺陷 ✗：`apply_renames` 命中一条规则就 ``break`` ✗ ⇒ 一个键需要**三条规则接力**时
  （去前缀 → `layers.N`→`blocks.N` → `attention`→`attn` ✓）后两条**永远不跑** ✓
  ⇒ 装载时整片 `layers.N.*` 对不上 ✓。修：**按顺序应用全部规则** ✓（并要求规则写成可叠加 ✓）。
* 另两条是我**测试脚手架**的错 ✗：① `endswith("attn.wq.weight")` ✗（真键是 `...attention.wq.weight` ✓）；
  ② ⑰ 拿**整个模型**输出去比"打乱 q/k/v" ✗ ⇒ **假前提** ✓：adaLN-Zero 的 `gate_msa` 初值为 0 ✓
  ⇒ 未训练模型**根本不看注意力** ✓ ⇒ 打乱后整模型输出**一模一样** ✗。改成直接比**注意力子模块**输出 ✓
  （这条也顺手记下：**比"整体输出"之前先想清楚"这条路径真的参与了吗"** ✓）。

**实测**：`engine_mappings_test` **17/17** ✓（含 ⭐ 往返**逐位一致** ✓、⑯ 只改名不合并时**如实报缺融合键** ✓、
⑰ 打乱顺序 ⇒ 注意力输出明显不同 ✓）。引擎十套：core 26 ｜ inventory 34 ｜ loader 28 ｜ pipeline 98 ｜
dit 26 ｜ io 39 ｜ text 21 ｜ segments 54 ｜ mappings **17** ｜ 交叉验证 11 ✓（套件 84 套 ✓）。

## S7 第 82 步：**参考项目移植（第一批）** —— 短剧提示词生产契约（33/33 ✓ 零依赖）

用户要求：把 `reference/` 下 **Mini-Agent / minimax-desgin-plugin / ollama-python / ollama /
Open-AI-Micro-Drama-Generator / short-drama-agent** 六个项目里**有用的功能都办过来** ✓。

### 先做能力盘点（派探查子代理 ✓ 不占主上下文 ✓），并**查本项目已有什么**（避免重复造 ✗）

| 能力 | 本项目原状 | 结论 |
|---|---|---|
`retry/backoff` ✓ 已有（`agent/runtime.py` ✓）｜ `tool_call` ✓ 已有 ｜ `estimate` ✓ 已有（`estimate_service` ✓） | — | **不重复造** ✗ |
**占位符解析** ✗ | 只有 `prompt_utils.strip_video_prompt_tags` ✓ —— **剥标签、留下裸编号** `L1`/`R5` ✗ | **真缺口** ✓（模型不认识内部编号 ✗） |
**提示词质感层（polish）** ✗ | 全仓**完全没有** ✓（grep `polish` = 0 ✓） | **真缺口** ✓ |
**资产验收门 / 清单（approval / manifest）** ✗ | 无 ✓ | **真缺口**（下一批 ✓） |
**连续性表** ✗ | 只有零散的 continuity 引用 ✓ | **真缺口**（下一批 ✓） |

### 交付（都移植自 `reference/short-drama-agent` ✓ —— 六个里与本项目**最直接对口**的 ✓）

| 文件 | 内容 | 实测 |
|---|---|---|
`services/shot_placeholders.py` | **占位符解析**：`<location>L1</location>` → **具体场景描述** ✓、`<role>`/`<prop>`/`<clue>` 同理 ✓、`<duration-ms>6000</duration-ms>` → **统一时长模板** ✓；四类"看不出来的坑"**如实报出** ✓：未映射编号 ✓ / 残留标签 ✓ / 裸编号 ✓ / 坏时长 ✓ | — |
`services/prompt_polish.py` | **Mx-Shell 五段质感层** ✓（主题标签 / 锁定角色场景 / 气氛画质 / 相机规则 / 分镜切片 ✓）+ 三条硬约束 ✓：**手持自动补"呼吸般轻微浮动"** ✓、**内心独白自动补"不张嘴"** ✓、**声音政策必带** ✓；⭐ **空话必须成对** ✓（`cinematic`/`4K` 没有物理锚点 ⇒ 报问题 ✓） | — |
`routers/prompt_tools.py` | `POST /api/v1/prompts/resolve` ✓ + `POST /api/v1/prompts/polish` ✓（**纯函数式** ✓ 毫秒级 ✓ ⇒ 前端可在用户编辑时实时调 ✓） | 194 路由 ✓ |
`tests/prompt_contract_test.py` | 33 项 ✓ | **33/33** ✓ |

### ⚠️ 自检抓到一个**真漏洞**（检测器的漏洞，不是文案问题 ✓✓）

⑩ 当场红 ✓：**残留标签检测用的是"同一个严格正则"** ✗ ——
那个正则带**反向引用**（保证开闭同名 ✓），而 `<location>L1</role>`（闭合写错 ✓）**两边都匹配不上** ✓
⇒ 于是**畸形标签被当成「没有残留」** ✗✗（会原样送进模型 ✓ 而报告说"干净" ✓ —— 最坏的那种 ✓）。
修：残留检测改用**宽松模式** `</?[a-z][a-z0-9-]*\s*/?>` ✓（"像标签的东西"一律抓 ✓）。
> **规则：体检用的模式要**比解析用的更宽松** ✓ —— 否则"解析不了的东西"同时"检测不到" ✗。**

另：引号自查（`[\p{Han}]"[\p{Han}]` ✓）这次**精准命中 2 处真字符串** ✓（其余 6 处全在 docstring/注释 ✓ 无害 ✓）⇒ 规则有效 ✓。

**实测**：`prompt_contract_test` **33/33** ✓；层级 6/6 ✓；路径+常量 **243 已注册 / 未注册 0 / 漂移 0** ✓。
套件现为 **85 套**（总数待实测 ✗ 按规则不推算 ✓）。

### 下一批（同源 ✓ 已定好，按依赖顺序）

1. **资产清单 + 验收门**（`asset-manifest.json` ✓ + **未验收不得进付费生成** ✗）；
2. **连续性表**（场景空间 / 角色 / 道具状态 / 线索揭示 / 动作因果 / 转场动机 ✓ + 检查器 ✓）；
3. 其它项目值得办的：**Mini-Agent 的上下文压缩/摘要** ✓、**ollama 的流式工具调用解析** ✓（增量 JSON ✓）、
   **ollama-python 的结构化输出约束 + 函数→工具 schema** ✓（先核对本项目 agent 工具集是否已有 ✗ 再定 ✓）。

## 待办（优先级）

1. **真机验一轮**（需下载，用户暂缓 ✓）：装 H3 模型（`model_manager.py download --category video`）+ 4 个节点包
   （`install-nodes` ✓）⇒ 起 ComfyUI(8188) + 本服务(8765) ⇒ `h3_backend_live_test` 应转为 `succeeded` ✓。
2. Ref2VA 模板（当前两条模板都是 FL2VA 形态 ⇒ 参考图/参考音频用不上 ✗）。
3. `backend/`（旧 TS 后端）仍在仓库里；`local-deploy-plan.md` 在记忆里被引用但**仓库中不存在** ✗（待核）。

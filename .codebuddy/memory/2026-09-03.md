# 2026-09-03

- 【分批提交全部未提交改动，用户明确指示】工作区此前积累 20 修改 + 5 未跟踪（2026-09-01/02 连续开发批次，含画风三级体系、时代背景、装备三视图/单品图/表情重构、道具 A 类下线、共享契约等）。因各功能在同一批后端文件内逐 hunk 交织，文件级拆分破坏可编译性，故按用户 git 惯例分 2 个提交：
  1. `f996b6a` chore: update dev memory for character asset rework sessions（.codebuddy/memory/2026-09-01.md + 新增 2026-09-02.md）
  2. `090075a` feat: art style hierarchy, era background, shared contracts and character asset generation rework（22 文件 +2438/-377，新增 app-settings.ts / era-background.ts / contracts.ts）
  - 临时探查脚本 scripts/_probe_db.js（只读 DB 调试用）按惯例删除未提交。
  - commit 使用 -c user.name=yuanxf -c user.email=yuanxf@wedoctor.com，英文 message。
  - 提交后工作区 clean，分支领先 origin/main 9 commits，未 push。

- 【FL2VA 首尾帧链路断裂修复，2026-09-03】审计发现视频生成链路三处问题并修复（4 文件 +31/-11，tsc 全量通过，未提交待审查）：
  - 根因：`shot-router.ts` 的 FL2VA 决策（route=first_last_frame）referenceMode 写 `'single'`，且类型 `'none'|'single'|'multiple'` 缺 `'first_last'` → auto-pipeline 直接把 routeDecision.referenceMode 透传 generateVideo → minimax/volcengine/vidu adapter 只在 `'first_last'` 分支才派发 first_frame_image/last_frame_image → 尾帧目标（grid first_last 锁定结束画面）被静默丢弃，降级为纯 I2V。
  - 修复 1 shot-router.ts：类型加 `'first_last'`；FL2VA 决策 referenceMode 改 `'first_last'`。
  - 修复 2 auto-pipeline.ts submitMissingVideos：`isFirstLast` 分支起帧 = `prevTail || sb.firstFrameImage`（同场景顺接真实尾帧优先连续性），尾帧 = sb.lastFrameImage，first_last 时 imageUrl 置 undefined（adapter 忽略）。
  - 修复 3 qc-retry.ts：`/first_last_frame/.test(finalSb.route||sb.route)` 且 lastFrameImage 存在 → 以新首帧图起帧 + 设计尾帧锁定，保留 first_last 模式重跑。
  - 修复 4 videos.ts（手动路径）：routeDecision 类型补 first_last；若路由默认 first_last 但请求首尾帧字段不齐（无 last_frame_url）则降级 single 用 image_url 起帧，防止坏请求。
  - 已验证：ali-video adapter img_url=`imageUrl??firstFrameUrl` + last_img_url 天然兼容 FL2VA 参数；其余 adapter 契约消费点无需改。
  - 遗留语义（未动，待用户裁决）：storyboards.lastFrameImage 双来源（grid 设计尾帧 vs extractStoryboardTailFrames 真实尾帧，提取不覆盖已有值），recomputeEpisodeRoutes 显示可能把已生成真实尾帧的镜头标成 FL2VA。

- 【设计尾帧 vs 真实尾帧语义分离，2026-09-03 完成 11 文件 +65/-22】用户对遗留语义问题裁决「选效果更好的方案」→ 落地列拆分方案（非单列消歧）：
  - 语义定义：`last_frame_image`＝设计尾帧/FL2VA 目标（grid/手动写，永不被覆盖）；新增 `tail_frame_image`＝真实尾帧（frame-extractor 从已产出视频无条件提取的末帧，视频重生成自动刷新）。
  - DB：schema.ts storyboards 加 `tailFrameImage`（含注释）；db/index.ts `ensureColumn('storyboards','tail_frame_image','TEXT')` 幂等迁移。
  - frame-extractor.ts：去掉 `if (!sb.videoUrl || sb.lastFrameImage) continue` 跳过逻辑，无条件提取，写入 tailFrameImage 而非 lastFrameImage。
  - auto-pipeline.ts tail-link 顺接：`prevTail = sameScene ? (prev.tailFrameImage || prev.lastFrameImage) : undefined`（真实尾帧优先、设计尾帧兜底、跨场景回退首帧），注释说明边界。
  - qc-retry.ts FL2VA 判定简化为 `isFl2vaRetry = !!finalSb?.lastFrameImage`，不再依赖 route 字符串（逻辑放行标志符 cleanup）。
  - qc-report.ts：Row/映射加 tailFrameImage；缩略图 `tailFrameImage || lastFrameImage`。
  - export-service.ts：额外导出 `_tail` 真实尾帧文件，设计尾帧保持 `_last`。
  - shot-router.ts + videos.ts（本批同合入）：FL2VA referenceMode 正确落 `'first_last'` 且类型补齐；videos.ts 手动路径 first_last 字段不齐时降级 single 防坏请求。
  - 前端：VideoEditor.vue getSbLast 增 `tail_frame_image/tailFrameImage` 优先；episode 页 genShotFrame 参考链与 genVid 顺接回退两处 `prev?.tail_frame_image || prev?.tailFrameImage || getLastFrame(prev)`。设计态标签/圆点未动，last 保持设计尾帧。
  - 验证：后端 tsc exitCode 0 通过；启动冒烟/前端 vue lint 有 IDE 无 vue 插件误报噪音（既有）。未提交，待用户审查。

- 【跨集角色重复合并前端 UI，2026-09-03 分 4 段实现完成】配合后端 `POST /characters/:id/merge`（body `{target_id}`，target 资产优先，源关联迁移后软删，均已先期就绪）：
  - 段1 useApi.ts `characterAPI.merge(id, targetId)`。
  - 段2 episode 页 script：`mergeSourceChar`/`mergeBusy` ref + `normName`（去空白小写规范化）+ `sameNameCandidates`（全剧 characters 中与源**同名**的其他记录）+ `hasMergeCandidates`/`openMergePicker`/`closeMergePicker`/`mergeCandidates`/`mergeInto`（danger confirm + API + toast + refresh）。
  - 段3 模板/样式：voice-card-head 加 git-merge 图标按钮（仅存在同名候选时显示，避免全剧无重复时 UI 噪音）；Step3 容器末尾加 `.overlay` 弹窗列候选（头像/role-tag/音色是否已分配），scoped 样式 `.merge-char-dialog/head/list/item/empty` + `.voice-merge-btn`。
  - 段4 验证：vue compiler-sfc parse 0 错误、compileTemplate 0 错误、useApi.ts transpile ok（临时脚本已删）。文件本身既有 1300+ 宽松类型 lint 噪音非本次引入。未提交，待用户审查。

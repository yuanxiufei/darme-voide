# 2026-09-24 工作记忆（消费方核查 + 网格规则逐值钉住）

## ① 昨天的「坐标空间」警告 —— 顺手把**消费方**核完 ✓
`img_update` / `audio_update` 在 `app/` 里**只有** `H3FormTrunk.forward` 用 ✓，而且只取
**`numel()` 当行数** ✓（拿它跟 `assemble_blocks` 的行数对账 ✓）—— **没有一处**拿 `segments` 的下标去切
掩码 ✓✗。⇒ 昨天那条警告**只是文档问题** ✓（不是隐藏 bug ✓），已在 `packed_layout` docstring 写清 ✓。
⭐ **教训（正面）：发现"两个坐标空间"时，先把消费方全找出来再下结论** ✓ —— 文档警告要配一条
"谁在用它、怎么用"的事实 ✓，否则下次读到的人（包括我）会以为有雷 ✗。

## ② ⭐⭐ 「混了不报错」的**网格规则**：从"摘要式"升级到**逐值**（`engine_h3_form_test` 74 → **76 项** ✓）
`packed_layout` 文档里三条规则此前只有**摘要式**断言（数行数 / 看 t / 看段序 ✓）—— 那**验不到**它们 ✗✗：
把两种网格"统一"成一种**不会报错** ✓，只会让**坐标**错 ✓。新增两条逐值判据：

* ㉓′ `cond` 用**目标**网格 ✓（h/w 集合 == `frame_grid_coords(target)` ✓）而 `ref_img` 用**它自己的** ✓
  （== `frame_grid_coords(ref)` ✓）—— 且**两者必须不相等** ✓（相等就说明被统一了 ✓✗）；
* ㉓″ 两段 `ref_audio` **同名不同源** ✓：`kind="audio"` 的参考块取**目标**网格两端 ✓、
  `video` / `video_audio` 类里的音频行取**它自己**的两端 ✓（同样要求两者不等 ✓）——
  顺带钉住顺序 ✓：**video 类参考块是「音频行在前、视频行在后」** ✓。

判据写法：从 `position_ids[start:stop, 1:]` 取**h/w 集合**逐值比 ✓（round 6 位 ✓），不比行数 ✗。

## ③ 写这两条时我自己的错（都当场红 ✓）
* 期望段表**漏了目标两条流** ✗（总以为块段就是全部 ✓ —— 段表里 text / audio / video 都在 ✓）；
* 把 video 类参考块的顺序**写反** ✗（事实是**先音频后视频** ✓，与 `audio → video` 的目标侧一致 ✓）；
* ⚠️ **第三次**犯同一个语法坑 ✗✗：在**双引号**串里写半角引号（`被"统一"了` ✓）⇒ `SyntaxError` ✓
  ⇒ 中文串里一律用「」（或 `**…**` ✓）。

## ④ ⭐⭐ 混合形态下的**模态标签**（最后一处同族缺口 ✓ `engine_h3_form_test` 76 → **77 项** ✓）
事实：`video`/`cond`/`ref_img` = 0 ✓、`text` = 1 ✓、`audio`/`cond_audio`/`ref_audio` = 2 ✓。
⚠️ **`cond` / `cond_audio` / `ref_img` / `ref_audio` 这四档只在混合形态里出现** ✓✗ —— 此前
`mod_segments_for` **只有文档**写着它们 ✓，一条断言都没有 ✗（`㉒` 只覆盖了 text/audio/video 三档 ✓）。
而标签只是 adaLN 的**行内偏移** ✓ ⇒ 错了**完全不报错** ✓，只是把每个模态接到**别的模态**那组参数
（"画面就是不对" ✓✗ —— 与 `t_vals_for` 初版三档全错同族 ✓）。
判据：逐段比 `row == index[kind]×3 + 标签` ✓（用 `t_vals_for(0.5)` 的 `index` ✓）+ `cond` 与 `ref_img`
**同档** ✓ / `cond_audio` 与 `ref_audio` **同档** ✓ + 行号全 < 18 ✓。

## ⑤ ⭐⭐ 两条「给错也不报错」的事实（`dit.H3_PACK_FACTS` 第 3 / 第 4 条 ✓ 77 → **79 项** ✓）
对着**事实表**逐条核断言覆盖时发现：这两条此前只有**摘要式**覆盖 ✗，而它们出错时**行数、单调性全都不变** ✓✗：

* ㉕ **跨度表 (1,4,4,4,4) 循环** × 5/3 ✓（`FRAME_PER_TOKEN` / `FRAME_RESCALE` ✓）—— 此前只验了第 0/1 个
  token ✗ ⇒ 补 7 个 token 的**逐值**判据 ✓（(1,4,4,4,4,1,4)×5/3 ✓）：⚠️ 跨度表给错**照样单调递增** ✓✗，
  **只有回绕那一格会露** ✓；
* ㉖ 立体声 **channel-major** 的**落点是 `w` 两端** ✓（`audio_grid`：前一半 `count` 行取低端 ✓、
  后一半取高端 ✓）—— 此前只验「t 重复两遍」✗ ⇒ 两种声道顺序的**行数与 t 完全一样** ✓✗，
  **只有 w 分得开** ✓ ⇒ 补逐值判据 ✓（低端 < 高端 ✓）。

## ⑥ ⭐⭐ 修掉一条**产品侧终态竞态**（这才是那两次「假红」的真因 ✗✗）
现象：全量回归里 `h3_stage2_test` **偶发** `19/20` ✓✗（失败点是 ⑧「跑完调了 `/free`」✓，
明细 `(None, None)` ✓ = stub 从没收到 `/free` ✓、任务里也没有 `freed_vram` ✓）。

真因（`app/local_services/h3/server.py` ✓ 两条执行体 `_run_generation` / `_run_generic` ✓ **同一处写法** ✗）：
**先** `_update(task, status="succeeded", …)` ✓，**再**在 `finally` 里 `client.free(...)` + `_update(freed_vram=…)` ✓
⇒ 中间那一瞬调用方（正是这个自检 ✓）读到的任务是**自相矛盾**的「**成功了但 `freed_vram=null`**」✓✗。
⚠️ 这不是"测试挑刺"✗ —— **真机上就是 API 撒谎** ✗：调用方看到终态就有权认为显存已卸 ✓。
修法：**先卸载、再落终态** ✓（`freed` 与终态在**同一次 `_update`** 里落地 ✓，在锁内 ✓）⇒ 没有中间态 ✓；
失败路径照样卸 ✓（`failure` 分支也带上 `freed_vram` ✓）。

### ⚠️ 更正昨天的判断 ✗✗（教训比修复更值钱）
昨天我把这条判成「**并发**跑全量导致的资源竞争，不是代码问题」✗ —— **错了** ✗：
真因是**竞态** ✓，并发/负载只是**让它更容易露** ✓。⇒ ⭐ **「单独复跑就好了」≠「不是代码问题」** ✗✗，
它可能只是**概率**低 ✓。该做的是**问"什么条件下会露"** ✓（这里一问就发现：终态与副作用分两次写 ✓），
而不是收工 ✓。已把判据写进 `run_all.py` 表头 ✓：有红先数进程 ✓ → **单独连跑 3 次** ✓（一次绿不算 ✓）→
还绿再查"什么时候写终态"这类**时序** ✗。

### 自检新增
`h3_stage2_test` 20 → **21 项** ✓：⑭′ ⭐ **失败路径也调 `/free`** ✓ 且 `freed_vram` 随终态落地 ✓
（此前只有"跑完卸"被断言 ✗，失败路径那条**没人核** ✓✗ —— 它正是 `finally` 的语义 ✓ 重构时最容易被改掉 ✓）。
连跑三次 **21/21** ✓✓。

## 结果
`engine_h3_form_test` **79 项** ✓（77 → 79：⑤ 两条时间轴/声道事实 ✓）、`h3_stage2_test` **21 项** ✓（20 → 21：⑭′ ✓）；
**干净全量：104 套 / 3629 项 / 0 失败** ✓（3628 + 1 ✓ 已写回 `run_all.py`；那条偶发红**根除** ✓）
全量只跑一次 ✓；⚠️ 本轮全量在 `h3_stage2_test` 上**慢了一阵** ✓（那套自带轮询 ✓ 不是卡死 ✓ ——
判据仍是「有没有 `结论：` 行」✓，别急着杀进程 ✗）。

## ⚠️ 顺手发现：`MEMORY.md` 超限一倍以上 ✗✗（待办）
实测 **14017 字符** ✗ —— 而它自己的头写明「**须 ≤8k，超限会被注入截断（实测 9.4k 即截断，
且**尾部 `## 协作与提交` 最先丢**）**」✗✗ ⇒ 现在很可能**已经在丢尾部** ✓✗（本会话我读它时没注意到 ✗）。
本轮的 4 行已**压成 2 行 + 指针** ✓（细节留本文件 §⑥ ✓），但仍**债还在** ✗ ⇒ 下一轮该按本仓既定做法
做一次「**腾预算**」（把细节下移 `TOPICS.md` ✓，前三轮都这么做过 ✓），而不是继续硬塞 ✗。
⚠️ 判据：`(Get-Content .codebuddy\memory\MEMORY.md -Raw).Length` ≤ 8000 ✓。

## ⑦ ⭐⭐ 记忆层「第四次腾预算」+ 修 7 处记忆守卫致命项
起因：上一轮顺手发现 `MEMORY.md` **13906 字符** ✗（预算 8000 ✓），而它自己的守卫
`app/scripts/check_memory.py` **不在 `run_all.py` 里** ✗ ⇒ 一直在悄悄腐烂 ✓✗。跑守卫 ⇒ **7 处致命** ✓：
① `MEMORY.md` 超预算 ✓；② **5 篇日志的末节锚点早漂了** ✗（`2026-09-18/20/21/22/23/24` ✓ ——
⚠️ **末节/步骤小节锚点会随日志增长而漂移** ✗，写 `@末节` 这种字面量**不算登记** ✓✗）；③ 一篇的步骤小节未登记 ✓。

### 做法（同前三次 ✓）
细节**整体下移** `TOPICS.md` §自 MEMORY.md 下移（2026-09-24）✓ 五节：§A 分词器（含性能口径 ✓）、
§B 权重装载与**跨来源校验** ✓、§C 前端类型检查 ✓、§D 上机前自检 ✓、§E **记忆层自身的守卫** ✓、
§F 环境与自检纪律（ffmpeg 前置 ✓ / transformers 可选 ✓ / 汇总行格式 ✓ / 有红的处置顺序 ✓ / 可照抄项目 ✓）；
`MEMORY.md` 只留**会导致 bug 的判据** + 指针 ✓（13906 → **7988** ✓，致命 0 ✓）。
⭐ 顺手把「**有红先单独连跑 3 次**」写进判据 ✓（昨天那条偶发红的误判 ✓）。

### ⚠️ 本轮踩的坑（都在同一处 ✗）
* INDEX 是**超长单行** ✗ ⇒ 我按"尾部片段"去匹配锚点，**打到了隔壁条目**上 ✗✗（把 09-20 的锚点写进
  09-18 ✓）⇒ ⚠️ 改 INDEX 锚点要用**唯一上下文**（条目头 + 尾部一起给 ✓），别只给尾部 ✗；
* 我"压缩"后**反而变长**过两次 ✗（把单行拆成多行、加 `> ` 前缀 ✓）⇒ 压完**必须量字符数** ✓，
  别凭感觉 ✓（判据命令见守卫输出 ✓）；
* 改 `MEMORY.md` 的多处时，第二次编辑的 old_str 用了**我自己上一版的文本** ✗（文件里还是原版 ✓）
  ⇒ 失败两次 ✓ ⇒ 编辑前先确认"这一处到底改过没有" ✓。

### 结果
`.venv/Scripts/python.exe app/scripts/check_memory.py` ⇒ **exit 0 ✓ 致命 0 ✓**（7988/8000 ✓，
⚠️ 余量 12 ⇒ 守卫提示：**下轮进内容前先下移** ✓）。

## ⑧ 逆向 SLM 无限漫剧创造台（用户给的安装包 ✓ 结论见 `TOPICS.md` §SLM 逆向笔记 ✓）
`D:\app\SLM` 只是**桌面壳** ✓（pywebview + bottle 8123 ✓）；**产品本体是工作台 `H3EasyDirector`** ✓
（独立进程 8081 ✓ `--port 8081 --comfy http://127.0.0.1:8188` ✓），**不在本地** ✗ —— 它在自托管 OSS 的
`manifest.json` 里挂着：`SLM漫剧台_v3.5.40.zip` **74.7 MB** ✓（带 sha256/size ✓）。⭐ **同一个模型家族**：
整套是 **MiniMax H3** ✓ ⇒ 与本仓 H3 工作直接对照 ✓。
**最值的三件** ✓：① ⭐⭐ **三档引擎表**（`app.py:149-183` ✓）：官方 25 步/832×480/零第三方 ✓ /
标准 8 步+蒸馏 LoRA（≈2.1×）✓ / 极速 4 步+4 步蒸馏（**官方模板同款** ✓ ≈3.44×）✓ + **每档的必备模型
（必需/推荐/可选 ✓）与 `gpu_tip`** ✓ ⇒ 一张**产品化就绪矩阵** ✓（对照本仓 inventory/weights/readiness ✓）；
② ⭐ **已知冲突台账** `COMPAT_WARN` ✓（7 条真实案例 ✓：DaSila ⇒ WinError 433 ✓、EasyCache ⨯ Turbo LoRA ✓、
CacheDiT/T8 ⨯ FirstBlockCache ✓、SolAttn 8G 仅快 3.5% ✓）；③ **契约同步 + 离线兜底** ✓（壳里那份表明说
**权威在工作台 `/api/workflow_tiers`** ✓、本地表只做离线展示/校验 ✓）—— 与本仓「后端权威、前端镜像」同构 ✓。
其它：多镜像链（jsDelivr 清单 ✓ + codeload→ghfast.top 两级 ✓）、自托管升级（manifest + sha256 ✓）、
启动器 UX（EULA 必须滚到底 ✓ / 日志自动跟随+滚动即接管 ✓ / 退出问「是否关 ComfyUI」✓ / 原生对话框传
父窗口句柄 ✓ / PS 写快捷方式要 `utf-8-sig` BOM ✓ —— 与本仓那条编码坑一致 ✓）。
⚠️ **分歧** ✗：它是「买家自带 ComfyUI」✓ ⇒ **借工程与体验，不借依赖结构** ✓（本仓是自研优先 ✓）。
⚠️ **未做** ✗：提示词/工作流/H3 管线在 74.7 MB 的包里 ✗ ⇒ 要那部分得单独拉包 ✓（用户未确认 ✓）。


**⑧′ 包内实料 ⇒ 已收敛成「可用性判定」** ✓（同日 ✓）：完整判定与 **7 项待补只在
`TOPICS.md` §SLM 工作台包逆向 → 可用性判定** ✓（本日志不再重复 —— 记忆只留会改动作的 ✓）。
一句话：包 **sha256 已核** ✓、`ComfyUI-H3-Director` 源码**可读** ✓、EasyDirector 后端**编译** ✗
⇒ **只借事实与架构，不照抄代码** ✗。


**⑨ 逆向可用项落地（①②③ ✓ 同日）**：把「可用性判定」里最该先做的三项真实现了 ✓（都零依赖 ✓、
自检 67 条 ✓ 全绿 ✓）：

* ① + ② ⇒ `engine/conditioning.py`：H3 prompt 契约（三行协议键 `subject_definitions` /
  `retention_analysis` / `detailed_description` + 三档关系 `fully_copy` / `partially_copy` /
  `reference` ✓；**句子本仓自己写** ✓，只有协议键/词逐字 ✓ —— 不逐字搬参考实现文案 ✗）；
  ⭐ 钉住那条坑：**只写 `<Audio 1>` 绑定句不算声明** ✗ ⇒ 照标签跳会让模型忽略参考音频 ✓✗
  （`declaration_plan` 的判据 = 「用户真写了保留声明」✓，且**跳过必须给理由** ✗）；另含
  `h3_markup`（`@图N`/`【图N】`/`@音N` → 标签 ✓ 幂等 ✓）、`validate_markup`（编号错位**提交前**报 ✓）、
  `picture_slots`/`audio_slots`（续接尾帧**排最前** ⇒ 换场景后编号前移 ✓✗）、
  `select_h3_task`（⭐ **有参考素材 ⇒ 只能 Ref2VA** ✗✗ 硬约束 + 可行动报错 ✓；判不出形态 ⇒ 拒 ✗ 不猜 ✓）。
  自检 `tests/engine_conditioning_test.py` **37 条** ✓。
* ③ ⇒ **新模块** `engine/accel_chain.py`：加速链当**配置** ✓（`ChainEntry` schema ✓ + 四项校验
  ✓ 注册 / 参数名 / 缺必填 / **kind 接线** ✓）+ `build_chain_nodes` 接线（输出槽位**从注册表数出来** ✓
  不猜 ✗）；⚠️ 没给注册表 ⇒ 判 `unchecked` ✓ **不是 ok** ✓（没查 ≠ 通过 ✓）；判 `error` 的项
  `blocks_submit=True` ✗。自检 `tests/engine_accel_chain_test.py` **30 条** ✓。
  ⭐ 自检里抓到一个**真设计缺陷** ✗：我把「接管流」的 `model`/`clip` 也算成「缺必填」⇒ **每一环都报
  warn**、真问题（没注册 / 接错流 ✓）被淹没 ✓✗ ⇒ 已改成「接管流由链供给 ✓ 不算缺」✗，并用 ⑤‴ 钉住 ✓。
  ⚠️ 教训：**测试夹具不干净**会让断言验的是别的东西 ✗（④‴ 的合成节点多带一个必填 `clip` ⇒ 混进
  「缺必填」warn ✓✗）⇒ 夹具要**与判据无关的部分**都给干净 ✓。
* 套件登记：`tests/run_all.py` 的 `TESTS` 新增两条 ✓（105 → **106 套** ✓）；⚠️ **全量回归当晚正在跑**
  （后台 ✓），规模数字**没敢推算** ✗ ⇒ 跑完再写回表头 ✓。

**下一步（④⑤ ✓）**：④ 分镜脚本解析口径（5 种标记 + 官方 `[Shot N] At mm:ss` + **≤15 s 合并成一次生成**
+ **4.5 字/秒**估时长 + 断点优先级「段落换行 > 句末标点 > 从句标点 ✓ **绝不句中硬断**」✓）——
⚠️ 落点**未必**在 `engine/` ✗：本仓已有 `app/services/prompt_storyboard.py` 与 agent 分镜工具 ✓
⇒ 先看那边现状再改 ✓；⑤ 权重内嵌元数据契约（safetensors `metadata` 严格 JSON 校验 ✓）+ `convrot` 布局
**识别并明确拒绝** ✗（不知道旋转参数就不猜 ✓）。


**⑩ 逆向可用项落地（④⑤ ✓ 同日）** —— ⭐⭐ **全量：109 套 / 3740 项 / 0 失败** ✓（数字取自刚跑出来的
汇总 ✓ 不推算 ✗；108 套按 `SUMMARY` 收敛 ✓ + 1 套常量守卫型 ✓）。本轮共加 **111 条**自检 ✓。

* ④ ⇒ **新模块** `engine/script_parse.py`：导演稿 → 生成段的**纯规则**层 ✓（不碰 LLM ✗）。
  5 种标记 ✓（段标记带时长 / `[0s-6.6s]` / `0:00-0:06` / `0至6秒：` / `6.6秒 |` ✓）+ 官方
  `[Shot N] At mm:ss.mmm` ✓（**≤15 s 合并成一段** ✓、>15 s 按 Shot 装桶 ✓）+ 无标记走
  **4.5 字/秒** ✓ 切 **8~15 s** ✓ + 断点优先级「段落换行 > 句末标点 > 从句标点 ✓ **绝不句中硬断** ✗」✓；
  ⭐ 时长吸附**往下**吸附到 `17k+5` ✓（用 `geometry.snap_frames` **同一处判据** ✗；往上会越过 15 s ✓✗）。
  ⚠️ 与来源的一处**有意差异** ✓：显式 >15 s 的段，来源是「截到 15 秒」✗（静默丢内容 ✗✗）⇒
  本仓改成**按断点切开** ✓ 并在 `notes` 里说明 ✓。自检 `tests/engine_script_parse_test.py` **26 条** ✓，
  判据写成**不变量**：⭐ **永不丢字**（去空白后拼回去 == 原文 ✓）、每段在网格上且 ≤15 s ✓、
  标点齐全的长文 `hard_cut` 全为假 ✓、硬切必须**标出来** ✓。
* ⑤ ⇒ ① **新模块** `engine/checkpoint_meta.py`：权重内嵌元数据契约 ✓（safetensors `metadata` 里的
  JSON ✓）**严格读**：缺 ⇒ `None` ✓（**不是通过** ✗）；非字符串 / JSON 坏 ⇒ **分开报** ✓；
  `format` 必须等于期望且报错**印两个值** ✓；布尔开关**必须真是 `True`** ✓（有键 ≠ 为真 ✓✗）；
  整数不允许 `"3"`/`True`/负数 ✓（有值 ≠ 对类型 ✓✗）。
  ② `quant.unsupported_family()` ✓：`convrot` 这类**未实现**布局族 ⇒ **具名拒绝** ✓
  （说清「是什么 + 要旋转参数 + 本仓不猜 ✓」✗），⚠️ **不是**「判不出布局」那种模糊话 ✗ ——
  那句话指不到真因、还会让人以为文件坏了 ✓✗。自检 `tests/engine_checkpoint_guards_test.py` **18 条** ✓。
* 顺手：`color_grade.py` 文档串里的 `L\*` ⇒ `L*` ✓（消掉一个 `SyntaxWarning` ✓ —— 它会污染全量日志 ✓）。

⚠️⚠️ **本轮自检抓到的四类错（都值得记 ✓）**：
1. **真设计缺陷** ✗：加速链把「接管流」（`model`/`clip` ✓）算成「缺必填」⇒ **每环都报 warn**、
   真问题被淹没 ✓✗ ⇒ 改成「由链供给 ⇒ 不算缺」✗。
2. **真逻辑错** ✗：`hard_cut` 在最后一步被**重新推断**过一次、条件写成「这一片超没超上限」✗
   ⇒ 硬切全被判成软切 ✓✗ ⇒ 改成**从切分处带上来** ✓。
3. ⚠️ **同一类断言错，本轮撞了三次** ✗✗：「不许出现某词」会被**为澄清而提到它**的文案打碎 ✓✗
   （`convrot` 那句里写了「不是判不出布局」✓）；容差要**对着实际口径**写 ✗（秒数是
   `round(frames/24, 4)` ✓，容差写 `1e-6` 连自己都判错 ✓；吸附是**往上**的 ⇒ 容差上界 = 一格 ✓）；
   **夹具要干净且真实** ✗（合成节点多带一个必填 `clip` ⇒ 断言验的是别的东西 ✓；首镜本来就没有
   `At` ⇒ 断言「每段都含 `At 00:`」是错的 ✓）。
   ⇒ 结论：**断言要对着「真实产物的形状」写** ✓，别写「我以为的形状」✗。


**⑪ 逆向可用项落地（①② ✓ 同日）**：把待办里的 ① ② 也做了 ✓（都零依赖 ✓、**不需要 torch** ✓）。

* ① ⇒ **新模块** `engine/latent_container.py`：联合 AV 潜变量容器互操作 ✓（**鸭子类型** ✓ ——
  只要求 `shape`/`ndim`/`unbind` 这套最小协议 ✓ ⇒ 自检用**假容器/假张量**跑 ✓，不依赖真机装了什么 ✗）。
  ⭐ 两条会**静默出错**的判据：0 条视频流 ⇒ 报错并**印出各流形状** ✓；≥2 条 ⇒ 报「**歧义**」✗ 且
  **不许挑第一条** ✓✗；换流**保容器类型** ✓（老/新签名不同 ⇒ 走「声明的容器类型」fallback ✓），
  **重建失败 ⇒ 报错**、**不静默退化成 list** ✗✗（那会让下游炸得莫名其妙 ✓）。
  自检 **16 条** ✓，含跨来源比对：`H3_VIDEO_CHANNELS` == `h3_form.H3_TRUNK_DEFAULTS["latents_dim"]`
  == `len(vae.H3_VIDEO_VAE_FACTS["latentsMean"])` ✓✓。
  ⚠️ 顺带修**文档腐烂** ✗：`vae.py` 的注里写着「与 `dit.H3_SHAPE_FACTS` 的 latents_dim 一致」✗ ——
  那张表里**根本没有**这个键 ✓✗（本套第一次就把权威写错成它 ✓）⇒ 已改指 `h3_form.H3_TRUNK_DEFAULTS` ✓。
* ② ⇒ **新模块** `engine/upscale.py`：超清模式的**规划层** ✓（网络结构留张量层 ✓ —— 只有真权重才验得动 ✓）。
  契约校验复用 `checkpoint_meta` ✓（`strict_latent_only` **必须真是 True** ✓）；三条已踩过的规则 ✓：
  **1.5× 必须先做合法 AI 2× 再缩放** ✓（直接提交会被拒 ✓✗）、**4× 必须小分块 + 分阶段** ✓
  且**不给 `max_tile` 就报错** ✗（不猜安全块大小 ✓）、**权重缺失 ⇒ 回退普通模式且必须给理由** ✗
  （静默降级 = 用户以为超清开着 ✓✗）。分块判据是**逐行首尾相接** ✓ —— 不是「面积对得上」✗✗
  （本模块第一版就是拿面积糊的 ✗：漏了错位/重叠一整类错 ✓）。自检 **16 条** ✓。
* 本轮又撞两次「**我以为的形状**」✗：① 1080 **不是** 32 的倍数（1080/32=33.75 ✓）⇒
  「画面本来就对齐」的前提是错的 ✓；② 我举的「面积相等的错位反例」**面积自己算错了** ✗✗
  ⇒ 反例也要**算一遍**再写 ✓（否则断言连反例都立不住 ✓✗）。
* 登记：`TESTS` 109 → **111 套** ✓（①16 + ②16 = 32 条新自检 ✓）；**全量回归已挂后台** ✓
  ⇒ 规模数字**没敢推算** ✗，跑完再写回表头 ✓。


**⑫ 逆向可用项落地（③④⑤ 接线 ✓ 同日）**：「能力接不出去不算功能」✗ ⇒ 本轮把新能力**接进产品面** ✓。

* ③ ⇒ `engine/hybrid_merge.py`：**混合加载计划层** ✓（口径：fl2va 基底 + ref2va 的 **adaLN 覆盖层** ✓）。
  ⭐ **四类情况分得清** ✓：能盖 ⇒ ``take`` ✓ / **不是 adaLN** ⇒ 跳过**并给理由** ✗ / **基底里没这个键**
  ⇒ **拒**（两边**不是同族** ✓ —— 混模型/混版本靠这条拦 ✓）/ **形状不一致** ⇒ **拒**且**两个形状都印** ✓；
  ⚠️ 匹配器**可传** ✓（adaLN 具体命名**只核到「名字里有 adaln」这一层** ✗ ⇒ 默认就是它 ✓、可换 ✓）。
  自检 11 条 ✓。⚠️ 只做**计划层** ✗：张量拼装/落盘/缓存校验在加载层 ✓（含「缓存截断/偏移/非法编码一律拒」✓）。
* ④ ⇒ `engine/tiers.py`：**档位表** ✓（口径直接取自本文件 §三档引擎表 ✓ 不凭记忆 ✗）：
  ``official`` 25 步 / 832×480 / **零第三方** ✓ / 8G 可跑 ✓、``std8`` 8 步 / 1024×576 / 8 步蒸馏 LoRA /
  16G+ ✓、``fast4`` 4 步 / 1024×576 / **官方 Comfy-Org R2V 模板同款** ✓ / 8G 甜点 ✓。
  判据：**不认识档位 ⇒ 报错并列出合法的** ✗ 且**不静默回落默认档** ✗✗；``for_steps`` 查不出 ⇒ ``None``
  （= 自定义档 ✓ 不硬塞 ✗）；⭐ **跨分辨率不估耗时** ✗✗（倍率是各自分辨率下量的 ✓）；
  ⚠️ 显存是**建议不是门槛** ✓、**没给显存 ⇒ 报「没比」**✗ 不是通过 ✓；
  每档必备模型都以**基础模型清单**开头 ✓、**零第三方那档不许有 LoRA** ✗。自检 16 条 ✓。
* ⑤ **接线 4/4** ✓（这是本轮最重要的产出 —— 四个接缝共抓出 **3 个纯逻辑自检永远测不出的错** ✗✗）：
  1. `local_services/h3/accel.py` ✓ 加速链 ↔ 真 `/object_info`（8 条 ✓）：⭐ **连不上 ⇒ 判「没查」**✗
     而**不是**判错 ✓✗、且**不接线**（槽位不猜 ✗）；判 ``error`` ⇒ ``build=None`` ✗ 不静默少一环 ✓。
     ⚠️⚠️ 抓到：`LoraLoader` 的 ``strength_*`` 是「**required 但带默认值**」✓ ⇒ 我把它们当「缺必填」报
     warn ✗✗ ⇒ **每条真链都会多出噪音 warn**、真问题被淹 ✓✗ ⇒ 新增 ``_has_default``「带默认值不算缺」✗。
  2. `local_services/h3/prompt.py` ✓ prompt 契约 ↔ **提交前那道关**（10 条 ✓）：全局提示词在前 ✓、
     标签转换 ✓、**提交前核编号** ✓（越界拒 ✓ 并提示「续接尾帧排最前 ⇒ 编号前移」✗）、
     **该追加的声明必须追加** ✓（标签不算声明 ✗✗）、**幂等** ✓。
     ⚠️⚠️ 抓到：正因为要**幂等** ✗ —— 二次进入时输入里带着上次追加的声明（含 ``<Audio 1>`` ✓），
     而编号校验只按「音频**素材**数」算 ⇒ **把自己上次写的声明判成越界** ✓✗ ⇒ 修：可用数要把
     「声明将要引入的标签」算进去 ✗（一路参考音频 = 那一路标签的合法来源 ✓）。
  3. `inventory.readiness()` 里加 ``tiers`` 块 ✓（档位表随就绪报告一起给 ✓）：自检 18 条 ✓。
     ⚠️ 抓到：``tiers.summary`` 是**仅关键字**参数 ✓，我在 inventory 里**按位置**传 ✗ ⇒ TypeError ✓
     —— 妙的是**三个套件（tiers/inventory/readiness）第一次跑同时报** ✓（接线点的错一碰就露 ✓✗）。
  4. `services/storyboard_helpers.py::segments_from_script()` ✓（**纯增量** ✓ 不动既有 10 个函数 ✓）：
     导演稿 → 生成段 ✓，自检 29 条 ✓（新增 3 条证**可达性** ✓：从分镜助手层**真的调得到** ✓、
     ``notes`` 带到上层 ✓、语速口子透传 ✓）。⚠️ 又撞一次「我以为的形状」✗：测试串里写成 ``\\n``
     ⇒ 那是**字面反斜杠 + n** ✓ ⇒ 两段标记挤成一行、段数变 1 ✓✗（已修 ✓）。
* 登记：`TESTS` 105 → **115 套** ✓（新增 10 套自检 ✓）；⭐ **最终全量回归当晚在跑** ✓
  （这一跑期间**不改任何代码** ✗ —— 边跑边改的数字不作数 ✓）⇒ 项数**没敢推算** ✗，跑完写回表头 ✓。


**⑬ 超清第二遍（网络本体）✓ 同日** —— ⭐⭐ **全量：116 套 / 3844 项 / 0 失败** ✓
（115 套按 `SUMMARY:` 收敛 ✓ + 1 套常量守卫型 ✓；数字取自刚跑完的汇总 ✓ 不推算 ✗）。

* ⭐⭐ **许可关键** ✓✗：上游 `h3_upscaler.py` 文件头写着「**Mamad8 权重、MIT 代码**来自
  github.com/mamad8c/ComfyUI-H3-Latent-Upscaler-Mamad8」✓ ⇒ **本仓可以实现** ✓（带署名 ✓）。
  此前我把边界设成「只做规划层、网络留张量层」✗ —— 那条边界**是因为没核到许可**✓，现在可以松 ✓。
* ⭐ **格式串核到真值** ✓：`minimax_h3_clean_latent_upscaler_v3_factorized_attention` ✓
  ⇒ 结构 = **V2 主干 + V3 因子化注意力** ✓（此前只知道「有个 format 键」✗）⇒ 已钉进
  `engine/upscale.py` 的 `CHECKPOINT_FORMAT` ✓（默认按它核 ✓ 要读别的格式必须显式传 None ✓
  且那样会**明说**只剩松校验 ✗）。
* **契约再下一层** ✓（`check_upscaler_contract` ✓）：`base_config` 六字段 / `config` 五字段 ✓、
  **多一个少一个都拒** ✗、值必须**正整数** ✓（`bool` / `0` / `"3"` 全拒 ✗）、
  `in_channels` **必须 24** ✓（与 `h3_form.H3_TRUNK_DEFAULTS['latents_dim']` 同一个事实 ✓）、
  `temporal_kernel` **必须奇数** ✓、`width % heads == 0` ✓。自检 16 → **22 条** ✓。
* **网络本体** ⇒ `engine/upscale_net.py` ✓（+ `tests/engine_upscale_net_test.py` **15 条** ✓）：
  V2（`spatial_bilinear_2x` **把 T 折进 batch** ✓ / `spatial_pixel_shuffle_2x` ✓ / `groups_for`
  往下找整除 ✓ / `ResidualBlock3d` 时间 padding=`k//2` ✓ / `forward = bilinear + correction` ✓）
  + V3（`stem` 作用在 `low` ✓ / `FactorizedBlock`：**空间窗口注意力 + shifted 按 `i%2` 交替且算完裁回** ✗
  / 时间注意力把 H,W 折进 batch ✓ / 深度可分离卷积 ✓ / MLP+GELU ✓ / `to_delta` → **pixel_shuffle 回 24** ✓ /
  `base + delta` ✓）+ `build_upscaler(strict=True)` ✓。
  两条钉子：⭐ **`T` 不变** ✗✗（2× 只动空间 ✓ —— 时间维插值会让动作速率变错 ✓✗）；
  ⭐ **键少一个/多一个都拒** ✗（写明「与声明的架构不符」✓ —— 不许 `strict=False` 糊过去 ✗✗，
  那会留下随机初始化的层：画面「像超分」其实是噪声 ✓✗）；⚠️ **没装 torch ⇒ 明确报错** ✓
  （不静默降级 ✓ 并指向「计划层回退普通模式」✓；该路径用 **monkeypatch** 造 ✓）。
* ⚠️ 仍**没做**的：二采精修的**接线**（按 `latent_container` 认视频流 + **音频流锁定** ✓ —— 要真权重才验得动 ✓）；
  逆向待做：② 段级音频合成（`_compose_segment_audio` ✓ 我们缺 ✓）、③ 混合加载的**缓存校验**那半 ✓、
  TTS 三件套 ✓、工作台 HTTP 面（端点清单已抄 ✓ 可作 API 设计对照 ✓）。


**⑭ 段级音频合成 ✓ 同日** —— ⭐⭐ **全量：117 套 / 3866 项 / 0 失败** ✓
（116 套按 `SUMMARY:` 收敛 ✓ + 1 套常量守卫型 ✓；数字取自刚跑完的汇总 ✓ 不推算 ✗）。

* ② ⇒ **新模块** `engine/audio_mix.py` ✓（+ `tests/engine_audio_mix_test.py` **22 条** ✓）：
  把上游的段级音频规则落成**纯样本运算** ✓（零依赖 ✓ 不做 IO ✗ 不碰 torch ✗ ⇒ 可自检 ✓；wav 读写仍归 `media` ✓）。
  三条最容易「统一错」的钉子 ✓✗：
  1. ⭐ **长度守恒** ✓：输出恒 `round(duration × sr)` ✓（配音长了**截断** ✓ 短了**补零** ✓ —— 音频不许把
     视频拖长 ✓✗）；`enabled=False` ⇒ 返回 **`None`** ✓（**整段静音 = 没有音轨** ✗，不是全零音轨 ✓✗）；
  2. ⭐ **三支削波口径不一样** ✗✗：`mix` = `模型声×0.6（**写死** ✓）+ 配音×音量` ⇒ clamp **±1** ✓；
     **环境音那支** = `模型声 + 环境音×0.25` ⇒ clamp **±0.95** ✓✗（留 0.05 防爆音 ✓）；
     **纯模型声那支根本不削波** ✗（1.5 还是 1.5 ✓）—— 这条最容易被顺手统一掉 ✓✗；
  3. ⭐ **不静默重采样 / 不静默转声道** ✗：采样率或声道数不一致 ⇒ **报错** ✓ 并给出正确做法
     （**加载时**就按段采样率解码 ✓）；给配音必须给采样率 ✓（不猜 ✗）。
  另：**偏移 = 前置静音** ✓（= 从段内第 X 秒开始播 ✓ 不是往后接 ✗）、`trim` 的 `keep` / `cut`
  （**中间挖掉再接起来** ✓）、`end <= start` ⇒ **取到结尾** ✓（不是空区间 ✗）、环境音比段短 ⇒ **循环填** ✓。
  ⚠️ 顺带记上游两条：输出帧率 8~24 是**索引抽样** ✓（时长不变、呈逐帧感 ✓✗）；封装走 **PyAV H.264 crf=18** ✓
  （**无外部 ffmpeg** ✓）、**失败先删残片再抛** ✓。
* ⚠️ 本套自检抓到的**两处都在测试侧** ✓✗（模块都对 ✓）：① 我自己的夹具喂了「单声道环境音 + 立体声模型」✗
  ⇒ 被「不静默转声道」拦下 ✓ —— 拦得对 ✓，已改夹具**并把这条拒绝也钉成用例** ✓；
  ② needle 期望写错 ✗：`model_audio=[]` 是**假值** ✓ ⇒ 走的是「没东西可拼 ⇒ 要静音请显式 enabled=False」
  那条 ✓✗，我却去匹配「空的」✗。
* ⚠️ **判活经验** ✓（本会话踩了两次 ✗）：全量跑久了要看**日志文件的 `LastWriteTime` 年龄** ✓
  （>180s 才算卡死 ✓）—— 别「隔一轮数行数」✗：套件输出有长有短，行数会顿一下、mtime 不会骗人 ✓。

**⑮⑯⑰⑱ 收敛（原四段叙述 ⇒ 只留判据 ✓ 细节在 `TOPICS.md` ✓；2026-09-24）**：本轮落地
**缓存守卫** ✓ / **参考素材指纹** ✓ / **配音契约** ✓ + 全量 **120 套 / 3912 项 / 0 失败** ✓（表头已回填 ✓）。
* **缓存守卫**：元数据**只读前 8 字节头长** ✓、头长 > 文件长 ⇒ 拒 ✗、偏移**和须等于**数据区 ✓✗
  ⇒ 原子落盘 ✓；⚠️ **只做结构核对、不验数值** ✗；16 位指纹 ✓（与参考指纹 **32 位两回事** ✗）。
* **参考指纹**：决定「**哪段可跳过**」✓✗；① **字典顺序无关** ✓（否则白重算 ✓✗）② 音频是
  ``{waveform, sample_rate}`` ✓ 别的裸张量 ✓（**两形态都要认** ✗✗）③ **采样率入指纹** ✓；
  ⚠️ **出错 ⇒ 退化成「不相等」（重算 ✓）** ✗ 不是退化成通过 ✓✗；采样 ≤512 点 ⇒ **不是完整性校验** ✗。
* **配音契约**：8 维**顺序 = 接口** ✗✗（``happy/angry/sad/fear/disgust/melancholy/surprise/calm`` ✓）；
  未知情绪 **拒** ✗（不猜最近邻 ✓✗）、**不自动归一化** ✗、**不替用户默认** ✗（``happy`` 属产品侧 ✓）、
  语速**不钳位** ✓✗（区间**由调用方给** ✗）；⚠️ **契约未接调用链** ✗（下一步核音色克隆样本面 ✓）。
* **B-3 判定：不做** ✓ —— key 脱敏 / 路径穿越**本仓已有且更硬** ✓✗、那个 256 KB 是**日志读取上限**
  ✗ 不是上传上限 ✓✗ ⇒ **先证明有暴露再加** ✗。
* ⚠️ **两条记账教训**：① 手记数字不许当基线 ✗（我曾把 ``engine_cache_guard`` 写成 12/12 ✗ 实测 20/20 ✓）；
  ② **没跑就别报数** ✗（AST 数元组得 121 是假读数 ✗）。


**⑲ ⭐⭐ 接线一轮（用户：「没有实现的继续实现」✓ 同日）** —— 上一轮的判据层**只被自检调用** ✗✗，
生产面于是躺着一堆「静默改用户意图 / 能力接不出去」✓✗。本轮四件都接上了 ✓；**全量 122 套 / 3983 项 /
0 失败** ✓（+2 套 / +71 项 ✓；数字取自刚跑完的汇总 ✓ 不推算 ✗）。

* **① 配音契约** ⇒ ``tts_generation._resolve_voice_params`` ✓（**唯一收口处** ✗；提交前一次 ✓）：
  ⭐ 两个真 bug ✓✗ —— ① 适配器 ``|| 'happy'``：**未知情绪静默变 happy** ✓（用户选难过、成片用开心念完、
  日志一切正常 ✓✗）⇒ 改成**当场拒** ✓（``map_emotion`` 不再回落 ✓；**同义表照留** ✓ —— ``furious``→``angry``
  是厂商同义词不是猜 ✗）；② CosyVoice **两条路径都不收** ``emotion``/``speed`` ✗（``/tts``→``/inference_sft``
  只认 ``tts_text``/``spk_id`` ✓、零样本只认 ``prompt_text``/``prompt_wav`` ✓；包装器自己写着「明确忽略」✓✗）
  ⇒ 声明 ``supports_emotion/supports_speed = False`` ✓ ⇒ 进 ``dropped`` 并**点名「克隆（零样本）路径」** ✓✗。
  ⭐ 判据：**「没给」≠「给了但不合法」** ✗✗（前者不带情绪 ✓ 且**不写 note** ✓；后者拒 ✓）；
  **引擎表达不了**（``disgust`` 在 MiniMax 表里没落点 ✓）⇒ 也进 ``dropped`` 并**列它能表达的那几个** ✓；
  **收不了的向量 ⇒ 拒** ✗（丢的是整条意图 ✓✗）；**能力声明取不到 ⇒ 记「没查」** ✗ 不当支持 ✓✗；
  语速区间**三处来源**（调用方显式 > 适配器声明 > ``settings.speedRange`` ✓）都缺 ⇒ 只做有限性校验 ✓
  且 ``speed_range_checked=False`` **必须能被查出来** ✓✗。⭐ 最硬的判据：非法情绪 ⇒ **一个厂商请求都不发** ✓✗。
* **② H3 形态决策** ⇒ ``video_adapters.plan_h3_form`` 经 ``conditioning.select_h3_task`` ✓：
  ⭐ 抓到的真 bug ✓✗ —— 生产里那条**硬约束没人执行** ✗（``scene_type=action`` + 角色声线样本 ⇒ 挑到 FL2VA
  权重 ⇒ 参考素材静默失效 ✓✗）⇒ 现在 refs ⇒ **ref2va** ✓（参考图**列表/JSON 两形态都要认** ✗✗ ——
  只认一种就又把硬约束绕过去了 ✓✗）；⭐ 顺手补掉引擎里那个洞 ✓：**「硬首帧」+「参考素材」同时要 ⇒ 拒** ✗✗
  （此前规则 2 会压过规则 4 ⇒ 静默丢参考 ✓✗），接线侧 ``blocked=True`` + **理由** ✓ 且回退口径 =「有 Ref2VA 就用它」✓
  （三种拒绝情形下输出与旧实现**逐字一致** ✓ 差别只在这回它说出来了 ✓✗）。
  ⭐⭐ 8765 薄封装**从来没读过 ``body.model``** ✗✗（后端按参考素材选出的权重**静默落空**、永远用模板自带那个 ✓✗）
  ⇒ 现在**调用方给的优先** ✓ 且必须先核 ``GET /models/diffusion_models`` ✓（那是 ComfyUI 的权威目录 ✓）：
  核不上 ⇒ **告警 + 兜底** ✓✗（静默换权重更糟 ✓）；清单取不到 ⇒ **说「没核」** ✗ 但**照样走兜底** ✓
  （第一版在这里 ``return`` 把兜底也跳掉了 ⇒ ``h3_stage2`` ⑤ 当场红 ✓）。
* **③ 提交前两道关**（8765 ``_build_api_prompt`` ✓）：**prompt 契约** ✓（文本写法→标签 ✓、**核编号** ✓、
  ⭐ **该追加的声明必须追加** ✗✗ —— 只写 ``<Audio 1>`` 绑定句**不算声明** ✓，不追加等于参考白给 ✓✗；
  参考音频一律 ``relation="reference"`` ✓ 别用 ``fully_copy`` ✗——那是「逐字念出来」✓✗）+
  **加速链** ✓（配置 ``settings.accel_chain`` ✓；判 ``error`` 的环 ⇒ ``build=None`` ⇒ **整条不上并点名** ✓✗；
  连不上 ⇒ 判「**没查**」✗ 不是判错 ✓；接好后**必须真有输入指向它** ✓✗ —— ``rewired==0`` ⇒ 告警 ✓）。
  两份报告 + 决策都回给调用方（``prompt_contract`` / ``accel_*`` ✓）⇒ 出片不对时**前端也查得出为什么** ✓。
* **④ 两个只有自检在调的**接出去 ✓：`POST /prompts/segments` ✓（导演稿→生成段 ✓）、
  `POST /engine/upscale-plan` ✓（超清计划 ✓）。判据写成**不变量**：段落在帧网格上 + **一个字都不丢** ✗✗；
  分块是**精确划分** ✓（面积对得上不算 ✗）；没权重 ⇒ 回退普通 + **必须给理由** ✗。
* ⚠️ **本轮我自己的三个错**（都当场红 ✓）：① 把 ``_load_cosyvoice_zero_shot`` 提到循环外 ⇒ 打碎
  ``gpu_lease_wiring`` 的「本地配置必须申请 audio 租约」判据 ✗（真问题：**改动了失败语义** ✓✗）
  ⇒ 改成「**廉价探针**（只查行 + 文件存在，**不读文件** ✓）+ 循环里**原处**加载 ✓」；
  ② 断言写成「我以为的形状」✗（段标记 6.6 s 其实会**往下吸附网格** ✓、短稿本来就不会分段 ✓）；
  ③ ⭐⭐ **stub 少一个端点 ⇒ 那一支永远验不到** ✗✗（stub 只有 ``/object_info/{cls}`` 没有 ``/object_info``
  ⇒ 加速链永远只能判「没查」✓✗ —— 第一版就是这么绿的 ✓）。


**⑳ 接线第二轮（用户「继续实现」✓ 同日）** —— 把**加载层 / 张量层**那几个「零调用方」的模块接到
**真调用路径**上 ✓；本轮 `TESTS` **122 → 124 套** ✓（新增 ``engine_hybrid_load_test`` **32 条** +
``segment_audio_test`` **14 条** ✓；另有 4 套**加判据** 21 条：管线 +10（二采阶段 ✓）、合成 +5（混音接线 ✓）、
缓存守卫 +2（只读头口子 ✓）、清单 +4（超清可用性 ✓））
⇒ ⭐ 全量 **124 套 / 4050 项 / 0 失败** ✓（**123 套按 `SUMMARY:` 收敛出 4050 项** ✓ + 1 套常量守卫型 ✓；
**+2 套 / +67 项** ✓ —— 数字取自跑完的汇总 ✓ 不推算 ✗；表头已回填 ✓）。
⚠️ **本轮的记账教训** ✗：我写完代码**先跑了一次全量**（那是 4046 ✗），跑完**又补了 4 条判据** ✗ ⇒
表头当场就不作数 ✓✗ —— 只有**再跑一次**才对得上 ✓（这一跑就是 4050 ✓）
⇒ ⭐ **判据要一次写齐再跑全量** ✗：否则"刚跑完的汇总"也是过期数字 ✓。

* **① 混合加载的「加载层」** ⇒ 新模块 ``engine/hybrid_load.py`` ✓（+ ``tests/engine_hybrid_load_test.py``
  **32 条** ✓）：计划层 ``hybrid_merge`` 只判「取哪些键」✗ ⇒ 本层**真搬字节** ✓ + **原子落盘** ✓
  （``.tmp`` → ``os.replace`` ✓ 失败**删半成品** ✓）+ **缓存两道门** ✓ + **磁盘余量** ✓。
  ⭐⭐ 两条最容易「看着对、其实错」的判据 ✓✗：
  ① **合并不改键集** ✗✗（产物键集 == **基底**键集 ✓ —— 计划层只看覆盖层里哪些是 adaLN ✗，
  **它不保证产出完整** ✓✗：少一个键 = 装出来少一层 ✓，装得上、跑得动、画面不对 ✓）；
  ② ⭐⭐ **必须继承基底的 ``__metadata__``** ✗✗ —— ``TorchBackend.load_weights`` **就是靠它读结构的** ✓
  （config 省略时先从权重元数据读 ✓）⇒ 丢了它，产物会报「读不出结构」，而**真因在合并这一步** ✓✗。
  其余：``cache_guard.is_sane_head`` 加了 ``total_size`` 口子 ✓✗（真权重 19.5 GiB ⇒ 为了判「数据区恰好吃满」
  把整文件读进内存是荒唐的 ✓；现在只读「8 字节 + 头」✓ **判据一字未改** ✓，且自检钉「给不给总长
  结论一致」✓）；⚠️ **本层不验张量内容** ✗（头自洽 ≠ 数值对 ✓）—— 自检把这条**当边界写出来** ✓
  （改数据区一个字节仍判有效 ✓），而不是假装能发现 ✓。
* **② 超清二采接进管线** ✓：``STAGE_ORDER`` **不加** ``refine`` ✗（它只列**必经阶段** ✓ —— 加了会让
  「六个阶段都有耗时」那条判据失效 ✓✗，这正是本轮第一次跑就红的 ✓）；可选阶段靠 ``STAGE_LABELS`` + 耗时表 ✓。
  计划**由调用方给** ✓（``/engine/upscale-plan`` 的产物 ✓ ⇒ 管线**不自己猜**倍率/分块 ✗）、
  未知 ``mode`` ⇒ 400 并列出合法的 ✓✗；⭐⭐ **音频流锁定必须是后端自述的** ✓✗（``refineNote.locksAudio`` ✓）
  —— 没声明就**明确拒绝**跑二采 ✓（重采音频会把音轨弄坏而画面看着正常 ✓，属最坏的"看不出来"✗）；
  后端**没实现钩子** ⇒ 也明确拒绝 ✓✗（**不静默按普通模式出片** ✗ —— 那会让用户以为超清开着 ✓）。
  ⇒ ``latent_container`` 终于有**生产调用方** ✓：``dryrun.refine_latents`` 把 H3 联合 AV 容器装出来 ✓、
  **只换视频那一槽** ✓ ⇒ 自检钉「**音频流逐位不变**」✗✗（``merged.streams[1] is container.streams[1]`` ✓）
  + 「空间 2× 而**时间维不变**」✓✗（时间插值会让动作速率变错 ✓）。
* **③ ``audio_mix`` 终于有落点** ✓✗：原先我判它「没有落点」✗ —— **错了** ✗：单镜合成走的是
  ``ffmpeg -map 1:a`` **顶替** ✗ ⇒ 生成视频自带的音轨（H3 是**联合 AV** ✓）**被直接丢掉** ✓✗，
  而逆向口径要的是**可选混合** ✓。⇒ 新模块 ``services/segment_audio.py`` ✓（+ ``tests/segment_audio_test.py``
  **14 条** ✓，**标准库读写 wav** ✓ **不引 torch/numpy** ✗ —— compose 那条路不该被 torch 绑住 ✓）
  + ``ffmpeg_compose`` 的 ``mix_model_audio`` 开关 ✓（**默认 false ⇒ 行为一字不差** ✗）+ 路由透传 ✓。
  ⭐ 三条口径原样钉住 ✓✗：混音支 = 模型声 ×**0.6**（写死 ✓）+ 配音 ×音量 ⇒ clamp **±1** ✓；
  ``replace`` 支是**顶替** ✓（模型声消失 ✓，与 mix 是两种产品语义 ✓）；**长度守恒** ✓（输出恒
  模型声/视频长度 ✓ 配音长了截断短了补零 ✓）。
  ⚠️ 抓到两处「我以为的形状」✗：① 以为 ``clippedSamples`` 会 >0 ✗ —— 削波是 ``audio_mix`` 干的 ✓，
  写盘那道只是**兜底** ✓（两处混读 ✓✗）；② 下标少了一层 ✓（``read_rows(p)[0][0]`` 是**声道**不是样本 ✓✗）。
* **⚠️ 本轮的两个接线坑（都很值 ✓）**：① ``_extract_audio_wav`` 的替身写成 **sync** ✗ ⇒
  ``await`` 一个 str 当场 ``TypeError`` ✓（真身是协程 ✓ —— 换桩要连**同步性**一起对齐 ✓）；
  ② 把「抽音轨 + 混音」做成 ``_build_mixed_track`` 里**可替换的接缝** ✓ ⇒ 自检不必真装 ffmpeg 也能验接线 ✓。
* **仍未接** ✗：``cache_key``（参考素材指纹 ⇒ 决定「哪段生成可跳过」✓）—— **没有干净落点** ✗：
  它要的是「同一个段的输入没变 ⇒ 复用上次产物」✓，那是**产品行为**（跳过生成 ✓ 需显式开关 + 谁拥有
  「上一次产物」这些决定 ✓）；硬接一个"只回答像不像"的端点属于**为接线而接线** ✗ ⇒ 先不接 ✓ 并留在这里 ✓。

# 2026-09-26

## ① 记忆层「只留最新一天」清理 ✓

- 用户口径：日志太多 ⇒ **只保留最新一天** ✓。11 篇 → **1 篇**（`2026-09-24.md`）；`INDEX.md` 128 KB → 17 KB；`memory/` 715 KB → **223 KB** ✓。
- 删掉的 10 篇（09-13/14/15/16/17/18/20/21/22/23）原文在**清理前那次提交 `83743d6`** 里 ✓：`git show 83743d6:.codebuddy/memory/2026-09-20.md`，或 `git checkout 83743d6 -- <路径>` 取回。
- 删前核对过：那 10 篇最大步号 135（`2026-09-20.md`，内含日期只到 09-20 ✓），比 09-21~09-24 早 ✓ ⇒ **没删掉更晚的工作** ✓。

## ② ⚠️ 顺手修守卫夹具（`test_guards.py` ④b）—— 用例不该依赖仓库此刻的内容 ✗

- 病：④b 靠「仓里存在含 ≥2 个步骤小节的日志」挑样本 ⇒ 日志一删，守卫自检整体失配红 ✗（夹具选择没错，是**耦合**错了 ✗）。
- 修：找不到真实日志就**自己合成一篇** ✓（写进沙箱副本 + 登记进副本 INDEX ✓）；判据、文案一字未改 ✓。实测 `test_guards.py` **17/17** ✓、`check_memory.py` 致命 **0** ✓。

## ③ ⚠️ 我自己的统计口径错（差点误捞 09-20）

- 拿「整份 `INDEX.md` 里出现 9 次 `2026-09-25` / 最大步号 135」当成「09-20 块的内容」⇒ 误判它是最新主线、提议捞回 ✗。
- 按**单文件**重测：`2026-09-20.md` 内含日期只有 09-15/09-20 ✓、步号 101~135 ✓ ⇒ 它确实是旧的一天 ✓，**不必捞回** ✓。
- 红线：跨文件统计（`Matches` 打在整串上）**必须先把范围切到目标块** ✗，否则结论张冠李戴 ✓。

## ④ ⭐⭐ SDXL UNet 落地（`engine/sdxl.py` ✓）—— 真权重 1680 键逐键对齐 ✓

- 「继续实现之前的功能」= `TOPICS.md` 待办第 1 条 ✓：图片自研缺的**卷积扩散主干** ✓（引擎已有 `vae`/`sampler`/`schedules`/`dit` ✓，要替代 SD-WebUI/diffusers ✓）。
- 布局**不是猜的** ✓：只读本机 `sd_xl_base_1.0.safetensors` 的**头**（`model.diffusion_model.*` ✓ 不载张量 ✓）量出来 —— 下行 9 项（`3.0`/`6.0` 是**独立的下采样层** ✓）、中间 3 项、上行 9 项（`2.2`/`5.2` 里带 `conv` 上采样 ✓）。
- ⚠️ 口径坑（全靠**真权重形状**钉住 ✓，写错**不会报错**只是出图变味 ✓✗）：上行 ResBlock 吃 **`cat([h, skip])`** ✗✗（`output_blocks.0.0.in_layers.2` = (1280, **2560**) ✓；只吃 skip 会写成 (1280, 1280) ✗ 且照样能跑 ✓）；`transformer_depth_output` 本仓按**上行序**存 ✓（与 ComfyUI 配置里**反着写**的那份是同一条事实 ✓）；eps **两档** ✓（ResBlock GroupNorm **1e-5** / SpatialTransformer **1e-6** ✓）；时间嵌入 **cos 在前** ✓；`label_emb` 外层**再套一层** Sequential ✓。
- 判据 `engine_sdxl_test.py` ✓ **39/39** ✓（8.3s ✓）：**1680 键全等 + 逐键形状全等 + 参数量 2,567,463,684 全等** ✗✗、`out.2` 零初始化 ⇒ 新模型**输出恒 0** ✗✗、**奇数边长 13** 走 `output_shape` 那条路 ✓（无脑 ×2 得 14 ⇒ 与 skip 拼不上 ✓✗）、ADM 的 6 段**各算各的** ✓。
- ⚠️ **没跑的不当跑过** ✗：`reference/ComfyUI` 在本机**导不进来**（`comfy_kitchen` 版本对不上 ✓）⇒ 没做数值对拍 ✓，改用「真权重头逐键对齐 + 口径逐条见出处」✓（结论按实测说 ✓）。
- 全量（已写进 `run_all.py` 头 ✓）：登记 **136 套** ✓ ⇒ OK **135 / FAIL 1** ✓、`135 套 SUMMARY 收敛 4377/4379（红 2）` ✓、常量守卫 1 套 ✓、崩 0 ✓。⚠️ 那 2 条红**与本次无关** ✓：`gpu_lease_wiring_test` 的**计数守卫**没跟上 `_handle_image_complete_local` / `_handle_video_complete_local` 两条新收口 ✓（期望 4/3 处、实有 5/4 处 ✓），两文件本次一字未动 ✓ ⇒ 提交里就已红 ✓；⚠️ **未擅自改守卫** ✗（等定 ✓）。
- ⚠️ **用户点破：⑦ 的检查点不许写死路径** ✗ ⇒ 改成走**本仓扫描器** ✓（`local_model_scan` 的默认根 ✓ + 它**自己的** `COMFYUI_CANDIDATES` ✓；测试文件里现在**零机器路径** ✓、代价 **+40 ms** ✓ 总耗时仍 8.0s ✓）。⚠️⚠️ 顺手量出**扫描器的漏** ✓：`detect_comfyui()` 只取**第一个**命中 ⇒ 本机挑中 `ComfyUI-Installs/…/ComfyUI`、**`ComfyUI-Shared/models` 被遮住** ✗（实测：默认根扫**扫不到**那份 SDXL ✓；补上候选表仅 22 ms 就命中 ✓）⇒ **应用自己也看不见**那儿的 SDXL / VAE / 两个文本编码器 ✓✗（正是「扫不到也不报错」那类 ✓）—— 已进 `TOPICS.md` 待定 ✓；⚠️ **没擅自改扫描语义** ✗（`local_model_scan` 是有 TS 镜像 + 多处守卫的模块 ✓）。

## ⑤ ⭐⭐ 潜空间口径表 + 图像算子集落地（`engine/latent_formats.py` ✓ / `engine/image_ops.py` ✓）

- 口径表：**34 条** ✓ 一个族一行（`scale_factor`/`shift_factor`/`latent_channels`/`latent_dimensions`/下采样比 ✓）+ `process_in`/`process_out` **五种**语义 ✓（`scale`/`affine`/`identity`/`meanstd`/`rearrange` ✓——⚠️ **不拿一个公式硬套所有族** ✗✗）。⚠️ `meanstd` 的 mean/std 属**权重** ⇒ 本表**不假装有** ✗（调用方不自带就**报错** ✗）；`rearrange` 型夹张量重排 ⇒ **具名拒绝** ✗；认不出的名字 **报错** ✗（**不回退默认 4 通道** ✗✗）。
- ⭐ **单一事实源**：H3 的 **24** 通道**引用** `latent_container.H3_VIDEO_CHANNELS` ✓（不另发明 ✓）、音频 **32** 与 `h3_form.H3_TRUNK_DEFAULTS["audio_latents_dim"]` 同源 ✓ ⇒ `engine_latent_formats_test.py` **逐值比对** ✓（谁改另一边这里会红 ✓）；⚠️ 32 **不是** `geometry.AUDIO_LATENT_CHANNELS` ✓ —— 那个是**立体声声声道数 = 2** ✓✗。
- 图像算子：`image_ops.py` **纯 torch** ✓（内部 `(B,C,H,W)` ✓ / 对外 `(B,H,W,C)` ✓、掩罩 **1 = 覆盖/要生成** ✓）：六种重采样核 ✓（含自研 `bislerp`/`lanczos` ✗✗）、缩放族 / 裁剪旋转翻转 / 拼接 / 贴图 / Porter-Duff **18 模式** ✓ / 掩罩族 / 形态学 / BT.601 色彩 ✓ / 量化 / Canny ✓。
- ⚠️ **自检当场抓出两条实现缺陷**（都是「看着对的错值」✓✗）⇒ 已修：① Canny 归一化只判 `peak > 0` ⇒ 平坦图的浮点噪声被**放大成整幅伪边缘** ✓✗ ⇒ 加 `CANNY_NOISE_FLOOR` 噪声地板 ✓；② `quantize` 借「灰阶调色板 + `quantize(palette=…)`」✗ ⇒ Pillow 那条分支**忽略 `colors`** ✗ 且把彩色**压成灰阶** ✓✗（`colors=4` 出来 **48 种色** ✓✗）⇒ 改走**中位切分** ✓。
- 判据：`engine_latent_formats_test.py` **29/29** ✓、`engine_image_ops_test.py` **51/51** ✓（两套均已登记 `run_all.py` ✓）；⚠️ 没装 torch 时那两类**显式 SKIP + 打摘要** ✓（不静默消失 ✗）。

## ⑥ ⭐⭐ CLIP 文本塔落地（`engine/clip_text.py` ✓）—— 双塔口径 + 两种 checkpoint 命名换算

- 对象：SDXL 的**双文本塔** ✓ —— CLIP-L（hidden **768** / **12** 层 / **12** 头 / MLP **3072** / `quick_gelu` / **无投影** ✓ / pad_id **49407** ✓）与 OpenCLIP-bigG（hidden **1280** / **32** 层 / **20** 头 / MLP **5120** / `gelu` / 投影 **1280** ✓ / pad_id **0** ✓）；两塔 vocab **49408**、位置 **77**、eps **1e-5** ✓、`head_dim = 64` ✓（可由 hidden/heads 推 ✓）。⚠️ 官方检查点前缀分别是 `…embedders.0.`（L）/ `…embedders.1.`（G）✓。
- ⭐ 命名换算（写错**不报错**只是语义变味 ✓✗）：OpenCLIP 的 `model.transformer.resblocks.*` → HF `transformer.text_model.encoder.layers.*` ✓；`in_proj_weight` 要**三等分**成 q/k/v ✓；⚠️⚠️ `text_projection` **必须转置** ✓（OpenCLIP 口径是 `x @ W` ✓，直接搬 ⇒ 形状可乘但结果错 ✓✗）。
- 口径：**因果掩码** ✓ + **EOS 池化**（取第一个 EOS 位置 ✓，不是末位 ✓）；`llayer_idx=-2` + `layer_norm_hidden_state=False` ✓（SDXL 就是取倒数第二层的中间态 ✓）。`SDXL_CONTEXT_DIM = 2048` ✓（两塔拼 ✓，短的那侧**要截断** ✓；pooled 取 bigG 投影**之后** ✓）。
- 判据 `engine_clip_text_test.py` ✓ **74/74** ✓（9 用例：配置钉死 / 键名 **196**（L）+ **517**（G）✓ / 前向形状与选层与 norm 开关 / 因果+池化 / OpenCLIP 换算（含**不转置的反证** ✗）/ 三种前缀载入与缺键多键形状不符 / SDXL 2048 拼接 / token id / **真权重只读头**核对键集 ✓）。
- ⚠️ 自检当场改出两处**实现**修正 ✓：① `encode_sdxl_conditioning` 加 `expected_dim` 可覆盖参数 ✓（否则**缩小版模型**根本没法测拼接 ✓✗）；② `load_clip_text_state_dict` 里 `load_state_dict(..., strict=True)` 写死 ⇒ `strict=False` 路径**必崩** ✓✗ ⇒ 改成 `strict=strict` ✓。
- 真权重只读**头** ✓（`read_header` 量键集 ✓，不载张量 ✓）；⚠️ 官方单发 `clip_l.safetensors` 的键是 `text_model.*` ✓ —— 比本仓命名**少一层 `transformer.`** ✓✗（测试里先剥前缀再比 ✓）。缩小版配置走**同一条前向**验形状 ✓，不下载真权重 ✓。
- 全量：本套已登记 `run_all.py` ✓；⚠️ 全量仍 **FAIL 1** ✓ 且**与本套无关** ✓ —— 就是 ④ 里那条 `gpu_lease_wiring_test` 计数守卫（image 期望 4 **实有 5** ✓✗、video 期望 3 **实有 4** ✓✗，两文件对 HEAD **零改动** ✓ ⇒ 提交里就已红 ✓，**未擅自改守卫** ✗）。

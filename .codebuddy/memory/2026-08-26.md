# 2026-08-26

## 短剧视频生产：本地 ComfyUI + MiniMax H3 方案评估

用户给出完整本地方案（Qwen2.5-12B 编剧 / Z-Image 美术 / MiniMax H3 FL2VA+Ref2VA 视频 / LipSync 修复 / A5000 24GB+96GB RAM），并问「对现有项目有没有帮助」。

核心判断（基于代码核对，非泛泛）：
- 项目（Drama Studio）已是该架构的「云端版」：auto-pipeline（script_rewriter→extractor→voice_assigner→storyboard_breaker→imaging→videoing→composing→merging）+ 5 Agent + gpu-manager（已含本地 VRAM 顺序卸载雏形：minimax:hailuo-02 15GB、ollama:qwen3:14b、keep_alive=0）。
- 最值钱的是「导演硬规则」而非 ComfyUI 节点，且与执行层无关（云端 MiniMax H3 与本地 ComfyUI H3 是同一模型，串人根因一样）。
- 差距清单：缺 CHAR→SPEAKER→VOICE 六键硬绑定、ONE_SHOT_ONE_SPEAKER、双人正反打/长对白 5-8s 切片（现 10-15s）、scene_type 路由（submitMissingVideos 写死 referenceMode:'single'）、角色参考图+声音参考一起进视频（现只传首帧图）、QC/lipsync 打分。
- 已有 TTS 侧 validateTTSSpeaker/validateDialogueLines 三连匹配（治标），根因层 one-shot-one-speaker 未做。

落地三阶段（已建议，待用户确认）：
1. 焊导演硬规则进 storyboard_breaker/voice_assigner SKILL + schema(migration) + submitMissingVideos 路由（立即、云端受益）。
2. ComfyUI 做成 provider adapter（走 /prompt+/history API），接入 getVideoAdapter + gpu-manager，云端/本地双轨。
3. 5 张 ComfyUI 工作流模板（纯 ComfyUI 侧，可选）。

## 第一阶段落地（已完成，0 lint 错误）

按「先焊规则进现有 Agent + schema + 流水线」执行完毕：

1. **schema+migration**：`characters.speaker_id`、`storyboards.scene_type`/`speaker_id`（`ensureColumn` 加列，向后兼容，不重建表）。
2. **storyboard-tools**：`read_storyboard_context` 角色带 `speaker_id`；`save_storyboards`/`update_storyboard` 的 inputSchema + insert/update 支持 `scene_type`+`speaker_id`。
3. **voice-tools**：`assign_voice` 新增 `speaker_id` 参数，写入 `characters.speakerId`；加两道硬校验（voiceConflict：同 drama 其他角色已用该 voice_id → return error；speakerConflict：speaker_id 重复 → return error）。
4. **auto-pipeline `submitMissingVideos`**：读 video config provider，`MULTI_REFERENCE_PROVIDERS={volcengine,vidu}`；对话类 scene_type（dialogue_2p/meeting/argument/conversation/multi）且 provider 支持时，`referenceMode:'multiple'`+`getStoryboardReferenceImages(sb.id).slice(0,9)`；否则 `single` 首帧（MiniMax/ali 自动降级，安全）。
5. **两个 SKILL**：storyboard_breaker 加 ONE_SHOT_ONE_SPEAKER 铁律、双人正反打、对白 5-8s 切片、scene_type 路由表、speaker_id 规则；voice_assigner 加硬规则（一角色一音色禁共用、跨集锁定、speaker_id 分配）。

关键代码事实（供后续）：
- `generateVideo` 已支持 `referenceImageUrls?: string[]`；adapter 中仅 volcengine-video/vidu-video 消费 `referenceMode:'multiple'`，minimax-video 只认 single/first_last（multiple 静默忽略不报错）。
- `getStoryboardReferenceImages(storyboardId)` = 角色图（含镜头级服装变体）+ 场景图，可直接复用。
- 在线默认视频 config = volcengine(Seedance)，本地 H3 = minimax，双轨路由天然匹配。

## 方向纠正（用户明确：参考 H3 资料，不是「接入 ComfyUI」）

用户纠正我跑偏：他要的是「参考他发的 H3 资料，吸收有用知识/思想完善本地项目」，不是我自作主张「接入 ComfyUI」。已产出权威蓝本 `docs/local-h3-video-system.md` 并纠正 MEMORY.md。

资料带来的关键修正（此前记偏/记错的）：
1. **scene_type 路由方向反了**：单人说话走 **Ref2VA**（角色参考+声音参考），**动作/空镜才走 FL2VA**（首帧）。此前记成 single/action→FL2VA 是错的。
2. **参考音频语义**：Ref2VA 参考音频是 reference conditioning（最多 3 条、每条 2-15s、总 ≤15s），**不是最终对白音频**。角色声音两层：声线参考 VOICE_001 + 本镜头对白→当前 speaker→声音资产。禁止混合对白塞给 H3 猜谁是谁。
3. **H3 = 音视频联合生成**，音画同步以 H3 原生音频为主，**不用 Wav2Lip 补嘴型**（此前待办里写了「需 MiniMax 云端 API 支持 reference audio」是错的——本地 H3 Ref2VA 原生支持）。
4. **本地 H3 走 ComfyUI 双 checkpoint**（FL2VA/Ref2VA `pruned_int8_convrot`），不是「改 baseUrl 走 minimax adapter」。
5. **六键 Bible 缺三键**：LOCATION_ID / COSTUME_ID / STYLE_ID（CHAR/SPEAKER/VOICE 已有）。
6. ComfyUI 用官方节点（EmptyMiniMaxH3LatentAV 等）+ 官方模板 video_minimax_h3_{t2v,i2v,r2v}.json 作底板，不重造底层。
7. A5000 跑法：INT8 + NVFP4/AWQ Text Encoder + CPU/RAM Offload，FL2VA INT8 ≈19.5GB，第一版不上 Turbo。
8. 第一轮测试 A–G（7 用例，从 6s 单句到 60s 连续对白）。

## 再纠正：不引入 ComfyUI

用户明确「没必要用 ComfyUI」。理由：项目已有自己的编排层（auto-pipeline + 5 agent + adapter），ComfyUI 是重复的可视化编排；本地 H3 复用现有 `minimax` adapter + 改 baseUrl 即可（代码注释已写死接法）；H3 本地推理只需权重+运行时，ComfyUI 只是运行时之一且非必需。

已修正：`docs/local-h3-video-system.md` 重写为「方法论 + 部署载体解耦」版，第 7 节改为「本地部署（不引入 ComfyUI）」，删掉 ComfyUI 官方节点/模板小节；MEMORY.md 同步去掉 ComfyUI 中心表述。

结论：资料里真正要吸收的是**方法论**（六键 Bible / ONE_SHOT_ONE_SPEAKER / 参考音频两层语义 / 音视频联合生成 / scene_type 路由），这些与部署载体无关，第一阶段已焊进 schema/SKILL/auto-pipeline。下一步缺口仍是：本地 H3 服务（minimax adapter + baseUrl）、LOCATION/COSTUME/STYLE 三键、参考音频两层语义落地、QC 打分。

## 架构对齐：本地系统 = 线上线下各种模型统一抽象

用户强调：「本地是支持线上和本地的各种模型的」。核对 `registry.ts` + `types.ts` + 各 adapter 确认：4 类 adapter（image/video/tts/text）都是 `Record<provider,Adapter>`，线上（minimax/gemini/volcengine/vidu/ali/openai/chatfire）与本地（ollama/local-sd/cosyvoice）同表解析，靠 `AIConfig{provider,baseUrl,apiKey,model}` 切换，本地 baseUrl 指 localhost、线上指厂商域名，上游零感知。文本最彻底：openai 兼容一个实例覆盖 7 provider（含 ollama）。→ 任何新模型（含本地 H3）只需加/复用一个 provider + 改 baseUrl，不引入新框架。已写入 MEMORY.md「统一 adapter 抽象」。

## 落地：六键三键 + QC 打分（用户「你来实现吧」）

补齐两个真实缺口（参考音频两层语义经核对已落地，无需改）：

**1. 六键 Bible 三键进 schema**
- `schema.ts` + `db/index.ts`：`dramas.styleId`、`characters.costumeId`、`scenes.locationId` 三列 + 3 个 `ensureColumn` migration。
- 新增 `services/bible-ids.ts`：`ensureStyleId(dramaId)`=`STYLE_`+3位id、`ensureCostumeId(charId)`=`COST_`+3位id、`ensureLocationId(sceneId)`=同 drama 内同 location 文本复用 `LOC_` 序号（跨集锁定）。
- 数据流：dramas.ts 创建 drama 生成 style_id、save characters 生成 costume_id（CHAR_KEYS 加 costumeId，换装可显式覆盖）；scenes.ts 创建/更新生成或重算 location_id；characters.ts 更新生成 costume_id；storyboard-tools.ts `readStoryboardContext` 暴露 `costume_id`/`location_id`/`style_id` 给 agent。

**2. QC 打分（lip_sync / character_consistency / continuity）**
- 新表 `video_quality_checks` + migration。
- 新增 `services/qc-scoring.ts`：`scoreStoryboard(id)` 启发式规则打分（对白无 TTS 音频扣 40、speaker 无 voice_style 扣 15、视频时长偏差>30% 扣 20、角色无参考图扣 25、同场景 location_id 不一致扣 20 等），overall = lip_sync*0.4 + consistency*0.3 + continuity*0.3，issues 供人工复核；无完成视频时 status=pending。
- 端点：`POST /storyboards/:id/qc`（触发）、`GET /storyboards/:id/qc`（查询）。
- 自动触发：`video-generation.ts handleVideoComplete` + `webhooks.ts` Vidu 回调 里 fire-and-forget 调 `runQcAfterVideoComplete`。

`npx tsc --noEmit` 通过，无 lint 错误。真实唇形/相似度检测需 AI 模型，当前为规则分，接口已留挂载点。

## 落地：本地 H3 服务接入（复用 minimax adapter + baseUrl）

打通 H3 音视频联合生成（双 checkpoint + 参考音频），本地服务复用 minimax adapter，只改 baseUrl 即可接入：

- **数据层**：`video_generations` 加 `scene_type` + `reference_audio_urls` 两列（schema + 2 个 ensureColumn migration + VideoGenerationRecord 类型）。
- **配置层**：`ai.ts` AIConfig 加 `settings`（parseSettings 解析 settings JSON），暴露 `checkpoint_map` 映射。
- **adapter 层** `minimax-video.ts`：`resolveH3Checkpoint()` 按 scene_type 路由 FL2VA（action/silent/transition/establishing/empty）/ Ref2VA（single/dialogue/meeting/argument/long_dialogue 等其余）；`subject_reference` 多参考图（multiple 模式）；`reference_audio` 参考音频（最多 3 条）。字段名 `reference_audio` 为本地 H3 服务约定，云端不支持会忽略。
- **生成层** `video-generation.ts`：GenerateVideoParams + INSERT + adapter 传递 sceneType/referenceAudioUrls。
- **路由层** `auto-pipeline.ts`：`MULTI_REFERENCE_PROVIDERS` 加 minimax；对话类镜头（dialogue/meeting/argument/conversation/multi）收集出场角色声线样本作为参考音频。`prompt-utils.ts` 新增 `getStoryboardReferenceAudioUrls()`（最多 3 条，相对路径经 `toPublicMediaUrl()` 转绝对 URL，base 用 `PUBLIC_BASE_URL` 或 localhost:5789）。`routes/videos.ts` 手动生成/重生成同步补 sceneType + referenceAudioUrls。

`npx tsc --noEmit` exitCode 0，无 lint 错误。

**关键发现**：`characters.voiceSampleUrl`（`POST /characters/:id/generate-voice-sample` 生成）就是角色声线样本 = H3 Ref2VA 的参考音频（VOICE_001）。voice_style 变更时 voiceSampleUrl 会置空（声线变了样本失效），两者强关联。

**剩余待办**：本地 H3 服务实际部署（ComfyUI/vLLM 等运行时，与项目解耦）；checkpoint_map 需在 ai_service_configs.settings 里配（如 `{"checkpoint_map":{"fl2va":"minimax_h3_fl2va_...","ref2va":"minimax_h3_ref2va_..."}}`）；真实唇形/相似度 AI 检测模型挂载；第一轮测试 A–G。

## 优化：角色声音分配台——音色按角色类型打标签（旁白/主角/反派/配角）

只改前端 `frontend/app/pages/drama/[id]/episode/[episodeNumber].vue`（Step 3 声音分配）：

- 新增 `ROLE_TAGS` 元数据（旁白=青绿 #0d9488 / 主角=蓝 #3b82f6 / 反派=红 #ef4444 / 配角=紫 #a855f7）+ `roleTagCls()`。
- 新增 `inferRoleTags(name, desc)`：正则从音色名+描述关键词推断角色类型（可多标签，最多 3）。fallback 6 个 OpenAI 音色硬编码 roleTags。
- 新增 `inferCharRoleTag(c)`：从角色 name+role 文本推断类型，角色卡片展示对照标签。
- `mapVoiceProfile` 加 `roleTags`；`voiceSelectOptions` label 带标签；音色库面板加 4 类筛选 chips（`voiceRoleFilter` + `filteredVoiceProfiles`）；每条音色展示彩色 role-tag。

**音色生成技术（现状）**：MiniMax TTS 云端主力（`POST /v1/t2a_v2`，model `speech-2.8-hd`，用 MiniMax 系统预置音色 voice_id，非声纹克隆，speed/pitch/vol/emotion 可调，输出 mp3 32kHz）；CosyVoice 2 本地零样本克隆；统一层 `tts-generation.ts` 多模型 fallback + GPU 租约。试听 = `generateVoiceSample`（固定文案"你好，我是X…"）。ai_voices 表由 MiniMax `/get_voice` 同步（过滤中文/粤语，排除卡通/beta/播音腔）。

**改进建议（未实施）**：① roleTags 从正则推断升级为 LLM 打标并持久化到 ai_voices 表；② 音色库每条音色内置"试听"按钮（不绑角色先听）；③ voice_assigner agent 的 list_voices 带 role_tags；④ characters.role 自由文本升级为 role_type 枚举；⑤ 声纹克隆（上传参考音频，MiniMax cloning / CosyVoice 零样本）。

## 修复：episode 页面 `<style>` 块末尾被截断导致 "Element is missing end tag"

`frontend/app/pages/drama/[id]/episode/[episodeNumber].vue` 编译报错 `Element is missing end tag`（Vite plugin-vue，位置 4015:1 `<style scoped>`）。

根因：文件的 `<style>` 块末尾被截断——宽高比选择器 CSS 写到 `.aspect-btn-compact:hover` 后中断，`.aspect-btn-c`（截断的 `.aspect-btn-compact.active`）后面缺 active 样式、`.aspect-icon-compact` 样式和 `</style>` 闭合标签。文件总 5866 行 vs HEAD 4684 行（新增 1182 行，其中 style 末尾未写完）。

修复：补全 `.aspect-btn-compact.active { border-color:var(--accent); background:var(--accent-bg); color:var(--accent-text); }` + `.aspect-icon-compact { width:13px; height:13px; flex-shrink:0; }` + `</style>`。

诊断方法（可复用）：用 `@vue/compiler-sfc` 的 `parse` 定位 SFC 编译错误；用 `@vue/compiler-dom` 的 `parse` 打印 AST 层级（带 loc 起止行号）定位层级错乱；`git show HEAD:路径 | Measure-Object -Line` 对比行数判断是否被截断。

## 盘点：未完成待办清单（2026-08-26 核实代码现状）

以下待办均未做，供后续 session 直接取用：

**A. 本地 H3 视频（代码就绪，剩运行时/检测）**
1. checkpoint_map 运行时配置：`ai_service_configs.settings` 未配 `{"checkpoint_map":{"fl2va":"...","ref2va":"..."}}`（minimax-video `resolveH3Checkpoint` 已支持，未配退化单 checkpoint）。
2. 本地 H3 推理服务部署（与项目解耦）。
3. QC 真实唇形/相似度 AI 检测：`qc-scoring.ts` 纯规则分（注释明示「后续可挂载 AI 模型」），未挂。
4. 第一轮测试 A–G（7 用例）未执行。

**B. 音色标签（纯前端正则，5 项均未实施；后端 0 匹配 role_tags/role_type）**
1. roleTags LLM 打标 + 持久化 ai_voices 表（现为前端 `inferRoleTags` 正则推断）。
2. 音色库每条音色独立「试听」按钮（现试听均绑角色 generateVoiceSample，voice-library-item 无试听）。
3. voice_assigner `list_voices` 带 role_tags（现 inputSchema 为 `z.object({})`）。
4. `characters.role` 自由文本 → `role_type` 枚举。
5. 声纹克隆（上传参考音频，MiniMax cloning / CosyVoice 零样本，无上传入口）。

代码内无 TODO/FIXME 遗留标记。

## 完成：音色角色类型标签后端化（role_tags，2026-08-26）

把 role_tags 从「前端纯正则」升级为「AI 打标 + 持久化 + 后端下发」完整链路（即盘点清单 B1+B3+前端对接）：
- schema：`ai_voices` 加 `role_tags` 列（TEXT，JSON 数组）；`db/index.ts` ensureColumn 加列。
- `text-generation.ts`：新增 `inferVoiceRoleTags(voices)` 批量 LLM 打标（VOICE_ROLE_TAGS=旁白/主角/反派/配角，一次调用返回 `{voice_id: string[]}`）。
- `aiVoices.ts`：GET 返回 `role_tags`（`parseJsonArray` helper）；`sync` 插入后自动批量打标（try-catch 失败不阻塞 sync，role_tags 留空由前端回退正则）。
- `voice-tools.ts`：`list_voices` 返回 `role_tags` + inputSchema 加 `role_tag` 可选筛选（fallback 6 个内置音色也补了 role_tags）。
- 前端 `episode.vue`：`mapVoiceProfile` 优先用后端 `role_tags`，无则 fallback `inferRoleTags`。
- `voice_assigner/SKILL.md`：补「音色角色类型标签（role_tag）」说明。
- 后端 5 文件 0 lint 错误。前端 episode.vue 的 1000 个 TS 错误为预先存在（4000+ 行超大文件 LSP 弱类型/大量隐式 any），非本次引入。
- 注：`voicesAPI.sync()` 前端无调用点（sync 仅手动触发），打标随 sync 自动执行。剩余待办：B2 试听按钮、B4 role_type 枚举、B5 声纹克隆。

## 完成：B2 试听 + B4 role_type 枚举 + B5 声纹克隆（2026-08-26）

B 组音色待办收尾，全部完成：

**B2 音色库独立试听**
- 后端 `aiVoices.ts` 加 `POST /ai-voices/preview`（`generateTTS` 固定文案生成，返回 `{voice_id,url}`，url 为 `static/audio/xxx.mp3` 相对路径，前端 `'/'+url` 播放）。
- 前端 episode.vue 音色库每条音色加试听按钮 + `<audio>` 播放器（`previewVoice`/`previewingVoiceId`/`voicePreviewMap`）。与「绑角色 generateVoiceSample」职责分离。

**B4 role_type 枚举**
- schema `characters.role_type` 加列（ensureColumn）；characters.ts PUT 白名单 + dramas.ts CHAR_KEYS + extract-tools `inferRoleType(role)` 正则推断（主角/反派/配角/旁白/其他）；CharacterEditor 加「角色类型」select。

**B5 声纹克隆**
- 后端 `services/voice-clone.ts`（MiniMax `POST /v1/files/upload` → `POST /v1/voice_clone`，返回 `{voiceId,demoAudio}`）+ `aiVoices.ts` `POST /ai-voices/clone`（parseBody 取 file，校验 provider=minimax，写 ai_voices `onConflictDoNothing`，`generateCloneVoiceId` 生成 `ds_xxx` voice_id）。
- 前端 `useApi.ts` 加 `reqForm`（multipart）+ `voicesAPI.clone`；episode.vue 音色库底部加「声纹克隆」入口（文件名输入 + file input + 克隆按钮，成功后 `loadVoices()` 刷新）。

**本次修复的 bug/错误**
- `previewVoice` 读 `res?.data?.url` 错（`req()` 已返回 `json.data`），改为 `res?.url`。
- 3 个后端 TS 错误清零：`voice-clone.ts` Buffer→`new Uint8Array(fileBuffer)` 转 BlobPart；`aiVoices.ts` `String(body['voice_id']...)` 收窄 File 联合类型；`sync` 路由 `(r: any)` 显式注解。`npx tsc --noEmit` exitCode 0。

episode.vue 1000 个 TS 错误仍为预先存在的 SFC 弱类型问题，非本次引入。

## 遗漏排查与修复（2026-08-26，提交后）

按用户要求复查遗漏，已补齐 2 处：
- `character/[characterId].vue` 独立角色编辑页补 roleType 枚举（模板 select + 回填 `c.role_type||c.roleType` + 保存 `roleType: form.roleType||null`）。此前只改了 CharacterEditor.vue，这个独立详情页漏了。
- `episode.vue` 的 `inferCharRoleTag`/`isNarratorCharacter` 优先消费 `c.role_type`（标准枚举命中直接返回，否则回退 name+role 正则）。

仍未完成（需用户定优先级）：
- B5 只做了 MiniMax cloning，**CosyVoice 零样本克隆未实现**（依赖本地 CosyVoice 服务，本地服务未部署）。
- 真实唇形/相似度 AI 检测未挂载（QC lip_sync 为启发式模拟）；本地 H3 服务部署；checkpoint_map 配到 ai_service_configs.settings；第一轮测试 A–G。
- 建议项（产品决策未动）：prompts.vue:206、episode.vue:254/419/945 等角色标签展示仍用 `c.role||'角色'` 自由文本，未优先显示 role_type 标准化枚举。

git 提交已确认：`6f92596`（146 文件 +19164/-1563），中文 message 曾乱码已用 `-F` UTF-8 文件 amend 修正。

## CosyVoice 零样本克隆补全（2026-08-26，B5 另一半）

按「继续」指令补上 B5 剩余部分（本地零样本克隆），打通「上传→零样本合成→持久化→TTS 复用」全链路：
- **schema.ts + db/index.ts**：`ai_voices` 加 `reference_audio`/`prompt_text` 列（ensureColumn）。
- **voice-clone.ts**：加 `cloneVoiceCosyVoice()` → CosyVoice 官方 `POST /inference_zero_shot`（`tts_text`/`prompt_text`/`prompt_audio`base64），返回 demoAudio base64。无训练，即时模仿音色。
- **aiVoices.ts clone 路由**：按 `config.provider` 分流。cosyvoice → 校验 `prompt_text` 必填、生成 `cv_` 前缀 voiceId、demo 落盘 `static/audio/`、参考音频落盘 `static/voices/`、写 `referenceAudio`/`promptText`。minimax → 原逻辑（`ds_` 前缀）。`generateCloneVoiceId` 加 prefix 参数。
- **cosyvoice-tts.ts**：`buildGenerateRequest` 检测 `params.promptAudio` → 走 `/inference_zero_shot`，否则走 `/tts`。
- **tts-generation.ts**：`generateTTS` 在 provider=cosyvoice 时查 `ai_voices.referenceAudio` 读文件转 base64 传给 adapter，实现克隆音色可被配音复用。
- **前端 episode.vue**：克隆 UI 加「参考音频文本」输入框（`clonePromptText`，FormData 传 `prompt_text`）。

后端 `npx tsc --noEmit` exitCode 0、6 个改动文件 lint 0 错误；确认无循环依赖（db/index.ts 仅依赖 drizzle/schema/connection）。

**重要约定（本地服务未部署，接口为假设）**：CosyVoice 假定跑在 `http://localhost:9880`，`/inference_zero_shot` 与 `/tts` 均为参考官方封装的假设接口，本地部署后需按实际接口核对微调。`getStorageRoot()` 返回 `<dataRoot>/static`，静态路由 `/static/*` 映射到该目录。

## 根据剧人物批量生成音色（2026-08-26，B6）

用户反馈「音色太少，要有根据剧人物生成音色的能力」。调研发现音色库 `ai_voices` 实为空（0 条，sync 未跑），而 6 个角色各有 `voice_style`（MiniMax 预设 echo/shimmer/nova/onyx）+ `voice_sample_url`（试听音频，内容固定为「你好，我是{名字}。很高兴认识你，这是我的声音试听。」）。据此实现「用角色试听音频批量克隆专属声线」：

- **后端 aiVoices.ts** 加 `POST /ai-voices/generate-from-characters`（body `{drama_id}`）：读剧角色（有 voiceSampleUrl）→ 逐个读试听文件 → 按 audio provider 克隆（minimax `cloneVoice` / cosyvoice `cloneVoiceCosyVoice`，promptText=固定试听文案）→ 写 ai_voices（voiceId/voiceName=角色名/roleTags/description）+ 更新角色 voiceStyle 为克隆 voice_id。已 `ds_`/`cv_` 前缀则跳过防重复。辅助函数 `resolveLocalAudioPath`（/static/* URL→本地路径）、`mapRoleToTag`（角色 role→四类 role_tags，龙套归配角）。
- **voice-clone.ts**：`VoiceCloneInput` 加 `voiceName?`，cloneVoice body 传 `voice_name` 给 MiniMax。
- **前端**：useApi.ts `voicesAPI.generateFromCharacters`；episode.vue 配音步骤「生成试听文件」旁加「根据人物生成音色」按钮（`generatingVoices` 状态 + `generateVoicesFromCharacters()`，成功后 refresh+loadVoices）。

后端 tsc exitCode 0。前端 episode.vue 有大量预存在 TS 报错（与本轮无关）。

**关键提醒**：`ai_voices.role_tags/reference_audio/prompt_text` 与 `characters.role_type` 列的 ensureColumn 已在 db/index.ts（runMigrations 模块加载时执行，498 行），但**当前数据库尚未加列，需重启后端服务让迁移生效**，否则 generate-from-characters / list_voices 会报 no such column。

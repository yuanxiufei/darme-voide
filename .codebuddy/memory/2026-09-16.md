# 2026-09-16（周三）—— S7 收尾后的「产品面」推进：本地服务接缝逐个验，抓到并修掉一个真缺陷

## S7 第 60 步：**本地文本能力此前实际不可用**（活体自检抓到）⇒ 已修好并用真推理证明

### 开头：为什么会有这一步

第 58/59 步把「适配器 ↔ 服务」这类**只有真机才暴露**的接缝列成方法（H3 已钉死 ✓、CosyVoice 标了可疑 ✗）。
本步对「**真在跑**的本地服务」做同一件事 —— 先探活（`tmp/probe_live.py` 只读 GET，2s 超时）：
**只有 ollama(11434) 在跑** ✓（`/api/tags` 200 ✓、`/v1/models` 200 ✓）；local-sd(7860)、ComfyUI(8188)、
h3(8765)、cosyvoice(9880) 都未启动 ✗。

### ⚠️⚠️ 抓到的真缺陷：本地文本**恒返回空串**

新增 `tests/local_services_live_test.py`（活体感知：在跑就真打、没跑就显式 SKIP ✓）后第一次跑就红 ✗：

```
FAIL  ollama: 真推理一次 → 适配器能解析出非空文本   <<< "200 ''"
```

取证（`tmp/probe_ollama.py`，`/v1/chat/completions` 三个 max_tokens 档位）：

| max_tokens | finish_reason | message 键 | content | reasoning |
|---|---|---|---|---|
8 / 32 / 128 | 全是 `length` | `['content','reasoning','role']` | **恒 `''`** ✗ | 「好的，用户让我只回复…」✓ |

⇒ 根因：本机 **`qwen3:14b` 是思考模型**，走 ollama 的 **OpenAI 兼容端点**时**正文落在 `reasoning`**、
`content` 为空 ✗（预算全被思考吃掉 ✓）。同一模型走**原生 `/api/chat` + `think:false`** ⇒
`message.content = '收到'` ✓✓。

**影响面（这才是重点）**：`LOCAL_PRESET_SERVICES` 的本地文本预设原本是 `provider: "openai"` +
`http://localhost:11434` ✗ ⇒ 一键「本地预设」写进去的那条，**每次调用都拿不到文本** ✗✗
（而它的 priority 85 会让它被优先选用 ✓）⇒「本地文本」这一档能力**实际不可用** ✗。

### 修法（三处 + 一处收敛）

1. **新增 `OllamaTextAdapter`**（`app/services/adapters/text_adapters.py`）✓：原生
   `POST {base}/api/chat`，body `{model, messages, stream:false, think:false, options:{temperature, num_predict}}`
   ⇒ `num_predict` 才是 ollama 的上限字段名 ✓；`parse_response` 取 `message.content` ✓（并保留
   「被反代成 OpenAI 形状」的兜底 ✓）。docstring 里写清**成因与证据出处** ✓。
2. **registry**：`"ollama"` 从「共享 OpenAI 兼容实例」改为 `OllamaTextAdapter()` ✓。
3. **本地文本预设** provider `openai` → `ollama` ✓（`services/ai_configs.py`，附成因注释 ✓）。
4. ⚠️ **`quick-local` 收敛**（`routers/ai_configs.py::_upsert_preset` 加 `local=True`）：本地行改按
   「**该服务类型下已有的本地行**」（复用 `is_local_config()` 判定 ✓）覆盖，而不是按
   `(service_type, provider)` ✓ —— 否则老装机那条 `provider=openai` 的坏行会**留下不动**、
   继续以 priority 85 被选用 ✗✗。**每类本地服务只留一条** ✓。

### 验证（真推理 = 最强证据）

- `tests/local_services_live_test.py` **9/9** ✓：真推理拿到**非空文本** ✓；并把「OpenAI 兼容端点下
  content 空 / reasoning 非空」**作为一条记录性断言钉住** ✓（防止有人「优化」回 provider=openai ✗）；
  其余 4 个未启动服务打印 **SKIP**（并计入汇总 ✓ 不掩盖覆盖率 ✓）。
- 连带修复：`adapters_test`（103/103 ✓，原「7 家共享一个实例」改为 6 家 + ollama 单列 ✓）、
  `smoke` **476/476** ✓（原 `quick-local` 期望 4 条本地行 ⇒ 收敛后不再重复 ✓）、`text_generation` 67/67 ✓、
  `local_models` 89/89 ✓。
- 新增套件已进 `run_all` ⇒ 现 **69 套件**（项数 = 此前实测 2515 + 活体 9 + 适配器 +2 = **2526**）。

### 教训 / 方法

1. **「只有真机才暴露」的接缝要主动去验**：本地服务 + 适配器的组合，光看代码两边都「对」✓，
   跑一次才知道一家是 `content` 恒空 ✗。模板已有三份：`h3_chain_test.py`（两进程内闭环）、
   `local_services_live_test.py`（活体 + SKIP）、`assets_layout_test.py`（路径/常量）✓。
2. **跳过必须显眼且计入汇总** ✗：活体自检的 `SKIP` 行明确写「**不是通过**」✓，避免重演本会话
   那两次「断言恒真/静默失效」的假绿 ✗。
3. **改默认值要查「老装机残留」**：把预设 provider 改名，会与老行并存 ⇒ 一键配置必须**收敛**
   （按语义键覆盖 ✓），否则旧的坏配置继续被选用 ✗✗。

### 补充（同日）：这次改动**同时是与前端的一致性修复** ✓

`frontend/app/pages/models.vue` 对本地文本**本来就是按 `provider: 'ollama'` 处理的** ✓：
`useOllamaModel()` 先按 `c.provider === 'ollama' || String(c.base_url).includes('11434')` 找本地文本配置，
然后用 **`provider: 'ollama'`** 保存 ✓（L1216-1223）；`providers` 枚举里也早已含 `ollama`
（`[... 'ollama', 'openai', ...]` ✓）。⇒ **后端旧预设里的 `provider: "openai"` 本来就与前端自己的约定
不一致** ✗ ⇒ 直接后果是：前端点「用该模型」写出来的配置**能出文本** ✓，而一键「本地预设」写出来的
**恒为空** ✗ —— 两条路径写的是**不同 provider**，行为却差一个数量级 ✓。改成 `ollama` 后两侧统一 ✓。

### 全量复测（69 套件，四批、每轮 ≤2 并行）

| 批 | 范围 | 结果 |
|---|---|---|
h1 | 0-18 | 17 套绿 ✓ ＋ **契约冒烟 474/476** ✗（瞬态，见下）|
h2 | 18-36 | **18/18 绿，BAD=0** ✓ |
h3 | 36-53 | 逐套进行中（技术 QC ✓ / 宫格 ✓ / 校色 ✓ / 子 Agent ✓ / 请求日志 ✓ …）|
h4 | 53-69 | **BAD=0** ✓（含新套件：`h3_chain` 13/13 ✓、`assets_layout` 12/12 ✓、`local_services_live` 9/9 ✓）|

⚠️ **瞬态事件（如实记）**：h1 里的契约冒烟报 **474/476** ✗，但**单独跑 4 次全 476/476** ✓
（其中一次刻意与 h3 **并行**跑也是 476 ✓）⇒ 判为**瞬态**、**未复现** ✗。
根因**没定位到** ✗ —— 因为**我的取批脚本只保存 SUMMARY** ✗，失败用例名没落盘 ✗。
**已改进脚本**：失败时把该套的**完整输出**写 `tmp/fail_<suite>.txt` ✓。
**规则：批量跑测试的脚本，失败时必须落盘该套完整输出，否则事后无法定位（这次就是 ✗）。**

### 全量复测完成（四批覆盖 69/69 套）

h3 在第 8 套被环境杀掉 ✗（老毛病）⇒ 补跑 44-53（`H3B_BAD=0` ✓）。**合计：套件 69/69 全跑到 ✓，
项 2526，失败仅 1** —— 即上面那个**冒烟瞬态 474/476** ✗（单独跑 4 次 476/476 ✓，故按 476 计入 √）。
⇒ 文档里记的「69 套件 / 2526 项」是**实测值** ✓（不是推算 ✓）。

### ⭐ 瞬态结案：根因是「**冒烟不能与自己并发**」（已修 + 已用并发复现条件证明）

**根因**（我自己的操作导致的 ✗）：h1 批跑冒烟时，我**又**在另一个命令里单独跑了一次冒烟 ⇒
**两个 smoke 进程并发** ✓ ⇒ 而冒烟把测试技能写成**固定 id `py-smoke-tmp`**、且落在**真实技能库**
`app/skills/` 里 ✗ ⇒ 一个进程建/删、另一个的断言踩中间态 ⇒ 恰好 **2 条 skills 断言红** ✗
（`474/476`）。这解释了全部现象：单跑必绿 ✓、与**其它**套件并行也绿 ✓（只有 smoke×smoke 会撞 ✓）。

**修法**：`SMOKE_SKILL = f"py-smoke-tmp-{os.getpid()}"`（进程唯一 ✓）⇒ 并发各自隔离 ✓、
兜底清理也只清自己那份 ✓。
**证据（正面 ✓）**：`tmp/conc_smoke.py` 把两个冒烟**并行**跑，各自把 SUMMARY 落盘 ⇒
**并行#1 rc=0 `SUMMARY: 476/476` ｜ 并行#2 rc=0 `SUMMARY: 476/476`** ✓✓
（同一条件在修复前正是 `474/476` ✗）⇒ 修复成立 ✓。
⚠️ **我先犯了一个证据错误**：第一版验证只看「有没有生成 `fail_smoke_conc*.txt`」就宣布通过 ✗ ——
那是**负面证据**（进程被环境杀掉时同样不会生成该文件 ✗）⇒ **必须要求两边各自明确打印 SUMMARY** 才算数 ✓。
**规则：不要用「没看到失败」当证明；要么拿正面输出，要么别下结论。**

⚠️ **教训（比修法更重要）**：
1. **跑批时不要再手动单跑同一个套件** ✗（我用「并行跑批」提效率 ✗，却又手动补跑 smoke ⇒ 自己造了冲突 ✓）。
   规则：**测试套件共享仓库里的固定命名资源时，跑批期间就别手工重复调它**；或者——更好——**让它进程唯一** ✓。
2. **瞬态别急着放过**：这次「单跑必绿」的直觉差点让我把它记成「环境噪声」✗；把**时间线还原**
   （h1 在跑 ✗ + 我手动又跑了一次 ✓）才定位到根因 ✓。**先还原时间线，再谈复现**。
3. 顺带确认：冒烟里**没有**基于墙钟的阈值断言 ✓（`latency_ms`/`elapsedMs` 都是结构或计算值 ✓）
   ⇒ 排除了「计时敏感」这条怀疑 ✓（怀疑也要有依据地排除 ✓）。

### 同类隐患普查（「在真实仓库目录里用固定名」）：全仓**只有冒烟那一处** ✓

逐套扫了 `tests/*_test.py` 的写入点（`rmtree` / `mkdir` / `write_bytes` / `copy` 共 25 处命中）：
- **20+ 处**都写在 `get_storage_root()` / `get_data_root()` 下 ✓ —— 那是**每套各自的临时 DATA_ROOT**
  （`tempfile.mkdtemp` ✓）⇒ 天然并发安全 ✓。
- `skills_test` 写自己 fixture 的临时 root ✓；`local_models_test` 的 `TREE` 也是临时目录 + `finally` 清理 ✓。
- ⚠️ 唯一像风险的是 `storage_change_test` ✗（它碰真仓库的 `.data-root` ✓）：**读全文**后确认它
  「**沙箱假项目根**（monkeypatch `config.PROJECT_ROOT` / `DATA_ROOT_MARKER` ✓）+ **真标记全程只读**
  + 还专门断言『真仓库 `.data-root` 内容未变』（⑦ ✓）」⇒ **无需改** ✓。
⇒ 结论：冒烟那处**是唯一一处** ✓，已用 pid 唯一 id 修掉 ✓；这类隐患在本仓清零 ✓。

## S7 第 61 步：修守卫自检的**夹具失配**（新日记只有 1 个步骤小节 ⇒ ④b 用例自崩 ✗）

**症状**：`check_all.py` 的第三道（守卫自检）报
`✗ ④b 中间步骤小节未登记锚点（抹掉倒数第二个步骤锚点） → 夹具失配：2026-09-16.md 内步骤小节不足 2 个`
—— 注意这是**自检自己坏了**，不是被守卫的对象坏了 ✗（两道真守卫仍报「致命 0」✓）。

**根因**：`test_guards.py::_case_step_section_unregistered` 的夹具**固定取最新那篇日志**并要求其中
**≥2 个 `## S7 第 N 步` 小节** ✗。今天新开了一篇 `2026-09-16.md`，当前只有 1 个步骤小节（第 60 步）✓
⇒ 夹具前提不成立 ⇒ 用例以「夹具失配」失败 ✓。**这不是代码错，是夹具对「日记粒度」的隐含假设** ✗。

**修法**：改成「按日期**从新到旧**，取**第一篇含 ≥2 个步骤小节**的日志」✓（用例意图不变：抹掉某个
**中间**步骤的锚点 ✓），并保留「一篇都没有 ⇒ 夹具失配」的兜底 ✓。顺带把临时变量上一处 `Any`
标注去掉（本文件没 import `typing` ✗ —— 靠 `from __future__ import annotations` 才没炸，别留这种隐患 ✓）。

⚠️ **教训（夹具类）**：负向用例的夹具**不能依赖「数据长得恰好如我所想」** ✗。本仓的日记粒度是**可变**的
（有的日子一天 20 步、有的刚开篇只 1 步 ✓）⇒ 夹具必须**按性质挑选**（「找一篇步骤 ≥2 的」✓），
而不是**按位置取**（「取最新那篇」✗）。**规则：负向测试的夹具要「按条件找」，不要「按位置取」。**

**验证**：守卫自检 **13/13** ✓（含 ④b ✓）；两道真守卫仍 **致命 0** ✓；层级 **6/6**（138 .py、违规 0 ✓）、
部署契约 **31/31** ✓、路径+常量 224/227/未注册 0/漂移 0 ✓。

**全量复测（四批，≤2 并行）**：**69 套 / 2526 项 / 0 失败** ✓
（z1 0-17 → 1227 ｜ z2 17-34 → 462 ｜ z3 34-51 → 377 ｜ z4 51-69 → 460，四批 `_BAD=0` ✓）
—— 与文档里写的「69 套件 / 2526 项」**逐字对上** ✓，证明 Plan A 搬迁 + 三目录合并 + 本轮守卫修复后的
整体状态是**实测**而非推算 ✓。

## S7 第 62 步：H3 **全栈活体**自检（8 项）+ 起真 8765；⚠️ 教训：规模数字**必须实测**不能推算

### 起了哪个服务、为什么只有它

- **h3(8765)**：`app/local_services/h3/server.py` 是**我们自己的薄封装** ✓，依赖只有 fastapi/uvicorn/requests
  ⇒ 后端 venv 里全有 ⇒ `Start-Process` 立刻起来 ✓（`/healthz` = `{ok:true, comfyui_url:...}` ✓）。
- **local-sd(7860) / ComfyUI(8188) / CosyVoice(9880)**：这三个是 `model_manager.py` 的 **runtime=git
  clone 落点**（`app/local_services/<key>/` ✓），当前**目录为空 + 被 gitignore** ⇒ 起不来（要几十 GB 下载 ✗）。
  也就是说：**能真起的就是 h3** ✓（这也是为什么之前四处端口的接缝都只能 SKIP ✓）。

### 新增 `tests/h3_backend_live_test.py`（全栈活体，8 项；8765 未跑则显式 SKIP ✓）

链路：`ai_service_configs(video/minimax/base_url=127.0.0.1:8765)` → `generate_video`（配置解析 +
prompt 富化 + 入队）→ `_process_video_generation`（GPU 租约 + 用量记账）→ `MiniMaxVideoAdapter` 造请求
→ **真 HTTP** → 8765 → 解析 `taskId` **落库** → `_poll_video_task` 真轮询 → 失败分支落 `error_msg`。

| 断言 | 为什么是它 |
|---|---|
| ⭐ **落库的 task_id 被运行中的 8765 认出**（`GET /v1/video_generation/task/{id}` → 200 + 三字段） | **整份自检的核心**：只有「后端真把请求打到了那个进程」才成立 ⇒ 一举排除 mock/桩/写死假 id ✓✓ |
| ④ task_id 是 32 位 hex（`uuid4().hex` 形状） | 它来自服务而不是我们编的 ✓ |
| ⑥ 缺口钉住：终态 `failed` + 文案含 `not wired yet` | 阶段 2 落地后**翻转此断言** ✓（与 h3_chain ⑦ 同一缺口，但这份跨了真网络+真库 ✓）|
| ⑦ **反套套逻辑**：error_msg **不含**网络层失败特征（connect/refused/timeout） | 否则 ⑥ 那句文案根本不可能是服务给的 ✗ |
| ⑧ `api_usage.is_local=1` | 后端确实把 127.0.0.1 的配置认成**本地**（计费口径 ✓）|
| ③ prompt 富化：分镜的 `sound_effect` 被注入成 `[background_audio]` | 顺手把 H3 环境音注入也钉住 ✓ |

做法细节：测试里把 `vg._POLL_INTERVAL_SECONDS`（默认 **10s × 300 次 ≈ 50 分钟** ✗）压到 `0.05s × 3 次` ✓
—— 本自检只验**接线**，重试预算/节奏另有覆盖 ✓。另 `generate_video` 传 `force=True` **刻意绕过**
take 预算与剧本指纹门禁（那两条各有自己的用例 ✓），避免这份自检被门禁挡住 ✗。

**三层分工**（写进了文件头，互不重复 ✓）：`h3_chain_test.py` = 适配器↔薄封装 **in-process**（TestClient，
不需要 8765 ✓）｜`local_services_live_test.py` = **单点**真 HTTP（不经过服务层 ✓）｜
`h3_backend_live_test.py` = **全栈**（配置→服务层→真服务→落库 ✓）。

`local_services_live_test.py` 同时补了 **H3 单点真 HTTP 闭环**（+4 项）⇒ 该套件在 8765 在跑时 9 项 → **14 项** ✓。
新套件已注册进 `run_all.py`（**70 套** ✓）。

### ⚠️⚠️ 教训（数字类，我自己犯的）：规模数字**必须实测**，不能推算

我先把规模按**算术**改成「69 套 → 70 套 / 2526 项 → **2534** 项」（2526 + 新增 8 项）✗ —— **算错了**。
漏掉的变量：**「8765 从没跑变成在跑」会让活体套件从 9 项涨到 14 项（+5）** ✗ ⇒ 真实是 **2539** ✓。
**实测（两批）**：**70 套 / 2539 项 / 0 失败** ✓（w1 0-36 → 1732 项；w2 36-70 → 807 项；两批 `_BAD=0` ✓）。

**规则（已写进 `run_all.py` 模块头）**：① 文档里的规模数字一律以**「刚跑出来的汇总」**为准 ✗推算；
② **活体类套件的项数随本地服务在否浮动** ⇒ 不把这几个数字当硬约束，**自己跑一遍看汇总** ✓。

## S7 第 63 步：核实 CosyVoice 接缝 —— **「未验证假设」被证伪** ✗ ⇒ 加薄封装 + 接缝自检

### 怎么核实的（读源码，不是猜 ✓）

读上游 `FunAudioLLM/CosyVoice` 的 `runtime/python/fastapi/server.py` 与 `client.py` ✓ ⇒ **六项对不上**：

| 维度 | 我们后端期望（适配器契约） | 官方服务实际 |
|---|---|---|
普通合成端点 | `POST /tts` | **不存在** ✗（只有 `/inference_sft` `/inference_zero_shot` `/inference_cross_lingual` `/inference_instruct` `/inference_instruct2`）|
请求形态 | JSON | **form-data**（GET/POST 双注册，参数是 `Form()`/`File()`）✗ |
参考音频 | `prompt_audio`（**base64 字符串**） | `prompt_wav`（**文件上传**）✗ |
响应 | JSON `{audio: hex\|base64}` | **裸 int16 PCM 流**（无 WAV 头、无 media_type ✗）|
端口 | 9880（`PRESET_SERVICES` 本地音频） | 默认 **50000** ✗ |
字段 | `voice_id`/`emotion`/`format`/`sample_rate` | 只有 `tts_text`/`spk_id`/`prompt_text`/`instruct_text`（**且无 `speed`** ✗）|

⇒「git clone 起官方服务后本地音频就能用」**不成立** ✗（适配器 docstring 里那条「未验证假设」正式**证伪** ✓）。

### 解法（代码自己写过的方案② ✓）：薄封装 `app/local_services/cosyvoice/server.py`（端口 9880 ✓）

与 `h3/` 同形（**刻意不 `import app.*`** ✓ ⇒ 层级守卫把它归 `top` ✓，实测 139 个 .py / 违规 0 ✓）：

* `POST /tts`（JSON）→ 官方 `/inference_sft`（form-data: `tts_text`/`spk_id` ✓）；`voice_id`→`spk_id`，
  缺省 `中文女`（官方客户端同款 ✓）；**明确忽略** `emotion`/`format`/`sample_rate`/`model`（官方 SFT 不认 ✗）
* `POST /inference_zero_shot`（JSON，`prompt_audio`=**base64** ✓）→ 官方同端点（**multipart 文件 `prompt_wav`** ✓）
* 上游**裸 PCM → 补 44 字节 WAV 头** ✓ → 以 **hex** 回（与适配器 `is_pure_hex` 判定无歧义 ✓）+
  `format="wav"` ✓ —— ⚠️ 关键：下游 `tts_generation.py` 拿 `parsed["format"]` 当**文件扩展名** ✓，
  沿用适配器假设的 `mp3` 会写出「.mp3 里装 WAV」的坏文件 ✗
* `GET /healthz` 报**上游可达性** ✓；环境变量 `COSYVOICE_UPSTREAM`（默认 50000 ✓）/
  `COSYVOICE_SAMPLE_RATE`（默认 22050 = 官方客户端取值 ✓ —— **上游不告知采样率是它的盲点** ✗，换模型变调先查这里）

### 自检 `tests/cosyvoice_seam_test.py`（**18/18 实测** ✓，8765/9880 都不用起 ✓）

* **真 HTTP stub 上游**（uvicorn 线程 ✓ **按官方形态**收 form/multipart ✓ 返回裸 PCM ✓）⇒ 断言：字段映射 ✓、
  WAV 头正确（RIFF/WAVE/fmt/data + 22050 + 16bit 单声道 ✓）、`data_size == 上游 PCM 字节数` ✓、
  base64→multipart **文件字节数一致** ✓、适配器 `parse_response` 闭环 ✓、错误面 400 ✓、`/healthz` ✓
* **反套套逻辑**：stub 的命中计数必须存在 ✓（若包装压根没转发 ⇒ 这几条一起失败 ✓，不会假绿 ✓）
* **C 组活体**：真上游（50000）可达就真打 `/inference_sft` ✓；否则**显式 SKIP** ✓

### 文档同步（含揪出一条**已经过时的坏注释** ✗）

`backend-py/README.md` 里那条 ⚠️「**CosyVoice 的接口是「按猜想写的」** ⇒ 本地服务部署后需按实际接口核对」
**已整行重写** ✓（改为核实结论 + 封装 + 启动两步 + 自检指针 ✓）。
**教训：凡是「等真机再核」的注释，攒着不核就会**从「谨慎」变成「误导」** ✗ —— 能核就当场核掉 ✓。**

### ⚠️ 顺手观察到的两件事（工具面）

1. 我给 h3(8765) 起的**后台进程又被环境杀掉** ✗（本会话第 N 次 ✓）⇒ 这次 71 套实测里
   `local_services_live` 记 9 项、`h3_backend_live` 记 0 项（**live-aware 正确降级 ✓ 不是假绿 ✓**）。
   实测 **71 套 / 2544 项 / 0 失败** ✓；H3 在跑时这两套变 14 项与 8 项 ⇒ **+13 = 2557** ✓（两个数都实测过 ✓）。
2. **又一次「目标文件搞错」** ✗：3 处改动写到了**根** `README.md`（其实在 `backend-py/README.md` ✓）
   ⇒ 靠脚本的 **MISS 报告**才发现 ✓。**规则：多 README 的仓库，改之前先确认是哪一份**（MISS 报告是安全网 ✓）。

## S7 第 64 步：把 **ComfyUI 的执行能力**移植进项目（客户端 + UI→API 转换；`_run_h3` 待接）

**用户要求**：「把 `reference/ComfyUI` 的能力移植进来」✓。先盘点（子代理 ✓）定边界：

* 项目侧此前**零** ComfyUI HTTP 调用 ✗（`/prompt` `/history` `/view` `/free` 在产品代码里 0 命中 ✓）；
  已有的是「清单/下载/节点安装」工具链 ✓ 与 8765 外壳 ✓。
* 设计文档 §7 原写「**不引入 ComfyUI**（引擎是部署细节）」✗ ⇒ **边界**定为：**引擎本体不 vendor**
  （1063 个文件 + 重依赖 ✗），**但「怎么跟它说话」这一层进项目** ✓（否则「跑通推理」永远缺一块 ✓）。

### 读源码取到的**权威事实**（`reference/ComfyUI/server.py` ✓，不是猜 ✓）

六条 API：`POST /prompt`（body `{prompt:{...}, client_id}` → `{prompt_id, number, node_errors}`；校验不过
**400 + {error, node_errors}** ✓）｜`GET /history/{id}`（**未完成时返回 `{}`** ✗ 不是 404 ✓）｜
`GET /view?filename&subfolder&type` ✓｜`POST /free {unload_models, free_memory}` ✓｜
`GET /object_info[/{cls}]`（`input = INPUT_TYPES()`，**有序** ✓）｜`POST /upload/image`（multipart ✓）。

⚠️ **最关键的一条**：`/prompt` **只吃 API 格式** ✗，而 ComfyUI 界面导出、以及参考 workflow
（`reference/minimax-h3-comfyui/workflows/*.json`）全是 **UI 格式**（`nodes[]/links[]`）✗
⇒ 「模型装好了」≠「能跑」✗，**UI→API 转换才是能不能跑的关键** ✓。

### 交付（4 个新增 + 1 个自检）

| 文件 | 内容 |
|---|---|
`app/local_services/h3/comfyui_client.py` | ComfyUI HTTP 客户端：`/prompt` `/history` `/view` `/free` `/object_info` `/upload/image` `/system_stats` `/interrupt`；**零第三方依赖**（只用标准库 urllib ✓）；含 `missing_nodes()` **提前点名缺的节点包** ✓、`wait()` 超时保护 ✓、`extract_output_items()` 不写死产物键 ✓ |
`app/local_services/h3/workflow.py` | **UI 图 → API 格式**转换 + 参数注入（`ui_to_api` / `set_input` / `set_seed` / `node_classes` / `find_nodes` ✓）；widget 顺序按 `INPUT_TYPES()` 的 required→optional ✓、`forceInput` 不算 widget ✓、`control_after_generate` 多吃一槽且实现种子语义 ✓、非 0 mode 跳过 ✓、**界面便签（无人消费的未知类）丢弃 / 有人消费的未知类报错** ✓ |
`app/local_services/h3/workflows/` | 参考 H3 工作流模板（T2V/I2V ✓）+ **来源与署名 README** ✓（上游未声明许可，已如实写明 ✓） |
`tests/h3_comfyui_test.py` | **38 项 ✓**：A1 真实工作流事实 / A2 十三条算法判据 / A3 真实 23 节点整图转换 / B 起**真 HTTP stub ComfyUI** 走完整闭环（**不需要 GPU** ✓） |

### 查到的事实（可用于真机排障 ✓）

* `MiniMaxH3ImageToVideo` 是**本版本 ComfyUI 的核心节点** ✓（`comfy_extras/nodes_minimax_h3.py` ✓），
  注入点是 `prompt / width / height / **length（帧数，24fps）** / first_frame / last_frame` ✓ ——
  **时长不能按秒直接塞** ✗，要换算成帧并按 17k+5 网格 ✓。
* T2V 工作流：**23 节点 / 20 类** ✓；第三方只需 3 个类（`MiniMaxH3MemoryEfficientSageAttentionPatch` /
  `ModelPreviewOverrideKJ` / `Power Lora Loader (rgthree)` ✓）+ 2 个**界面便签**（转换时丢弃 ✓）。
* 超时：参考 README 实测冷启动 **53s**、5 秒片 **85s+** ⇒ `DEFAULT_TIMEOUT_SECONDS = 1800` 是**故意给足** ✓。

### ⚠️ 自检抓到我**两个真错**（都记下来 ✓）

1. **转换器判据错**：判断「某节点是否被消费」时我只看了连线**源**、没看**目标是否存活** ✗ ⇒
   「指向一个已被剔除节点的连线」会把上游误判成「被人消费」✗（自检 A2 直接红 ✓）。**已修**：
   `_link_table()` 同时取 `target_id`，只有目标还活着的连线才算消费 ✓。
2. **新文件的 `json.dumps` 没加紧凑分隔符** ✗ ⇒ **路径守卫当场抓到**（`DRIFT
   local_services/h3/comfyui_client.py:136` ✓）—— 本仓约定「请求体与 JS `JSON.stringify` 字节一致」✓。
   **修法选的是「按约定改代码」而不是「加白名单」** ✓（`ensure_ascii=False, separators=(",", ":")` ✓）。
   ⇒ **守卫对新文件也是即时生效的** ✓，这条测得值。

### 验证（实测）

* `tests/h3_comfyui_test.py` **38/38** ✓；连带的 `route_parity` 漂移 **0** ✓、`freeze_snapshot` **11/11** ✓
* 全量：**72 套 / 2582 项 / 0 失败** ✓（本地服务全停时测得 —— 活体套件正确 SKIP ✓）

### 还剩（下一步）

把 `local_services/h3/server.py` 的 `_run_h3` **接到这层**（组装 → 提交 → 轮询 → 取片 → `/free` ✓），
并**翻转 3 处「阶段 2 未接线」的缺口断言**（`h3_chain_test.py` ⑦ / `h3_backend_live_test.py` ⑥ /
`local_services_live_test.py` 的 h3 活体块 ✓）—— 那三处是当初**刻意钉住缺口**的 ✓，接完就该反过来 ✓。

## 待办（优先级）

- ⏳ **全量复测**（69 套件）：本步只逐套验了受影响的 5 套 ✓，未整跑；下次有空按「≤2 批并行、每批 ≤20 套」补测 ✓。
- ⏳ 本地服务的另三处接缝待服务起来后即验（`local_services_live_test.py` 已备好断言 ✓）：
  local-sd(7860) / ComfyUI(8188) / h3(8765，含**阶段 2** 未接线）／cosyvoice(9880，`/tts` 是否存在 ✗）。
- ⏳ H3 阶段 2：需要 `configs/models.json` 的 8 个模型 + 4 个自定义节点（当前全 MISS ✗，几十 GB 下载）。

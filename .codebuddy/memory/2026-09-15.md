# 2026-09-15

## S7 第 9 步：镜头 QC 打分（`qc-scoring.ts` 272 行）→ `POST /storyboards/{id}/qc`

**新增**：`app/services/qc_scoring.py` + `POST /storyboards/{id}/qc` + `tests/qc_scoring_test.py`（**26 用例**）。
守卫：已注册 221→**222**、未注册 6→**5**。

- 三维度**累加扣分**后统一 `clamp`（`Math.round` **半数进位** ⇒ `js_round`，`94.5→95`）；总体分是**加权**
  `lip*0.4 + character*0.3 + continuity*0.3`（**不是**平均）；`status` 三态：**有已完成视频**才
  `passed`/`failed`（有 error 级 issue 即 failed），否则 **`pending`** —— 即使已经有 error。
- ⚠️ **同一字段两种形态（极易搞混）**：落库的 `issues`/`dimensions` 是**紧凑 JSON 文本**，
  而**返回值**里这两项被换成**结构体**（`{...saved, issues, dimensions: dims}`）。
- ⚠️ **状态跳变只在「公共实体」上判**：`prev.end_state` 的实体若**不在**本镜 `start_state` 里 ⇒ **不扣分**。
  我第一版拿不同实体（门 vs 茶杯）去测，期望写错、被实测纠正。
- ⭐ **发现一个原 TS 死分支（已照抄 + 注释「别修」）**：`prevScene.locationId !== scene.locationId`
  **永远不成立** —— 该分支的前提是 `prev.scene_id == sb.scene_id`，而 `prevScene` 又是按 `prev.scene_id`
  查出来的 ⇒ 两者是**同一个场景行**，location 必然相同 ⇒「同场景地点 ID 不一致 -20」**永不触发**。
- 端点契约：非法 id ⇒ 404 `Invalid storyboard id`；分镜不存在 ⇒ 404 `镜头不存在`；
  服务层对不存在的分镜返回 `{"error": ...}`（**不抛**，路由层已先查过）。
- `run_qc_after_video_complete` 已随服务落地（**技术维度 `technical-qc.ts` 未迁** ⇒ 只跑规则分 + 一条 warn）；
  **尚未接线**到 `video_generation._run_qc_after_video_complete`（计划与 technical-qc 一起接，
  免得留半接线状态）。
- ⚠️ 种子踩了三次 NOT NULL：`scenes.drama_id` / `scenes.time` / `scenes.prompt` 都是 NOT NULL；
  `continuity_states` 的列名是 **`state_type` / `entity_key` / `state_value`**（不是 entity/state）。

## S7 第 10 步：QC **接线**（视频完成 → 自动打分写库，闭环打通）

**改动**：`video_generation._run_qc_after_video_complete` 从「记一条 `qc-skipped`」换成**真调用**
`qc_scoring.run_qc_after_video_complete`（自开事务、fire-and-forget：**失败只 warn，绝不影响成片**）；
`qc_scoring_test.py` 26→**29 用例**（新增接线三例）。

- ⭐ **闭环含义**：视频完成 ⇒ 写 `video_quality_checks` ⇒ `qc_report`（第 16 步交付物）能直接读到，
  「生成 → 打分 → 报告」这条链路在 Python 侧**首次真正闭合**。
- ⚠️ **技术维度仍未迁**（`technical-qc.ts` ffmpeg 黑场/冻帧/响度）：服务内部记 `tech-qc-skipped` warn；
  等它落地只需改服务内部，**调用点不用再动**。
- ⚠️ 自检又抓到我两个「期望错」（都属**没把上下文因素算进去**）：
  ① 新镜的**上一镜没填 `start_state`/`end_state`** ⇒ continuity 额外 -5 ⇒ overall **93 而非 95**；
  ② 那条 -5 会作为 info **写进 issues** ⇒ 「issues 应为空」的断言也错。
  ⇒ **教训：给「干净镜」算分前，先看它**在前后的邻居**（扣分规则是局部的、但触发条件是全局的）。**

## S7 第 11 步：审片重跑闭环（`qc-retry.ts` 175 行）→ `POST /storyboards/{id}/retry-qc`

**新增**：`app/services/qc_retry.py` + `POST /storyboards/{id}/retry-qc` + `tests/qc_retry_test.py`（**14 用例**）。
守卫：已注册 222→**223**、未注册 5→**4**。

- 流程四步：**LLM 只重写该失败镜**（`storyboard_breaker` + `update_storyboard`，禁止重建整集）→
  **软删旧图片/视频记录**（防 QC 复用旧产物）→ **重提首帧并等就绪** → **重提视频**（完成后自动重新审片）。
- ⚠️ **首帧等待的判据是「值变了」**（`firstFrameImage !== oldFirstFrame`）—— 若重新生成恰好得到同一 URL
  就会一直等到 **5 分钟超时**（照抄原实现）；它是在**请求路径里同步阻塞**的，自检把超时调 0 才不卡。
- ⚠️ **参考图/参考音频开关条件不同**：参考图 = `isDialogue && provider ∈ {volcengine,vidu,minimax}`（最多 9 张）；
  参考音频 = `isDialogue && provider == 'minimax'`。
- ⚠️ **FL2VA 判定只看「设计首帧 + `last_frame_image`」**：真实尾帧 `tail_frame_image` **不参与**
  （它是可运行产物、不代表设计目标）—— 自检特意在种子里塞了 tail 证明它被忽略。
- ⚠️ `rewrittenFields` 对外是 **camelCase**（`videoPrompt`…），库里是 snake_case。
- 自检又抓到我两个「期望错」：① 生图 prompt 走的是**兜底文案**（因为 agent 只改了 `video_prompt`，
  而 prompt 的取值链是 `first_frame_prompt → image_prompt → description → 兜底`）；
  ② 我的假 agent **无条件**改字段，导致「LLM 什么都没改」那个分支测不出来 ⇒ 加 `mutate` 开关。

## S7 第 12 步：设置分镜首/尾帧（`/set-frame`）+ 抽帧泛化 —— ✅ 完成（14/14）

**已写**：

* `frame_extractor.extract_frame(video_url, frame_type)`：**首帧不 seek**（ffmpeg 默认从 0 取第 1 帧）、
  **尾帧 = 时长 - 0.2s 处 seek**（时长探测失败 ⇒ 直接 None）；`extract_tail_frame` 变成它的**薄封装**
  （既有调用点语义不变，原有自检仍应通过）；已进 `__all__`。
* `POST /storyboards/{id}/set-frame`：视频素材抽帧、图片素材**直接采用**；**只有 `last_frame` 走尾帧**，
  其余值（含未知值）一律 first_frame；缺 `source_url` ⇒ 400 `source_url required`；
  抽帧失败 ⇒ **400**（不写库、不返回空 frame_url）；顺带给 `storyboards.py` 补了 `import re`。
* `tests/set_frame_test.py`（**13 用例**，打桩 `_run_ffmpeg` / `_probe_duration_seconds`）。

### ⚠️ 更正（同日）：那个「HTTP 路径抽帧失败」**不是代码 bug，是我测试建错了目录**

自检初版崩在「视频素材 + last_frame」：`error=抽帧失败（ffmpeg 提取不到该帧）` + `KeyError: 'data'`。
我当时的判断是「HTTP 路径下 `extract_frame` 返回 None」——**错的**，而且是两处错：

1. **「前 7 条用例全过」是我编的**：那次运行**崩在打印之前**，断言从未被评估（打印循环在最后）。⇒ **教训：脚本崩溃时不要推断「前面应该都过了」**；
2. 探针（`tmp/probe_sf.py`）一跑就真相大白：`to_abs_media_path("static/videos/clip.mp4")`
   → `<data_root>/**static**/videos/clip.mp4`，而我在测试里把假视频建在 `<data_root>/videos/clip.mp4`
   ⇒ **文件不存在** ⇒ `os.path.exists` 失败 ⇒ 直接返回 None，`ffmpeg` 调用列表为空（`calls: []`，铁证）。
   **端点代码本来就是对的**；改成 `<data_root>/static/videos/clip.mp4` 后 **14/14 全绿**。

⭐ **新增可复用知识**：媒体相对路径的约定是 **`static/...` → `<data_root>/static/...`**（数据根下有个 `static` 子目录）。
造测试素材时别再写成 `<data_root>/videos/...`。
⭐ **诊断顺序**：先写**最小探针**把「中间值」打出来（路径解析结果、打桩是否生效、调用列表），**别在猜测上叠加推理**——
这次探针一次就定位，而我此前的三条「待查方向」全是空转。

## S7 第 13 步：重生成镜头帧（`/storyboards/{id}/regenerate-frame`）

**新增**：`POST /storyboards/{id}/regenerate-frame` + `tests/regenerate_frame_test.py`（**17 用例**）。
守卫：已注册 224→**225**、未注册 3→**2**（剩 `episodes/consistency-qc` + `storage/change`）。

- **帧类型白名单**：`['last_frame','keyframe'].includes(x)` ⇒ **只有这两个被认**（未传/未知一律 `first_frame`，
  不是白名单报错）—— 与同族的 `/set-frame` 规则不同（那边只认 `last_frame`）。
- **帧画面内容优先级**：请求体 `prompt` **覆盖**库里对应的 `first_frame_prompt`/`last_frame_prompt`/
  `keyframe_prompt`（二者不共存）。
- **最终提示词 = 标准构建器基底 + 帧画面内容 + 帧提示词**，三段用 `", "` 连接、空段丢弃；
  帧提示词是三段**英文常量**（opening/closing/mid-action），逐字照抄。
- 复用面：`get_storyboard_character_appearances` / `get_storyboard_scene_description` /
  `get_storyboard_reference_images` / `resolve_effective_art_style(conn, drama_id, None, body.style)` /
  `build_storyboard_image_prompt` / `build_storyboard_negative_prompt` —— **全部已在 Python 侧且已导入**，
  所以这个端点几乎是照抄 `regenerate-image` 的骨架（差别只在 frameType + frameContent + frameHint）。
- ⚠️ 种子又踩两个 NOT NULL：`ai_service_configs.name` **与** `ai_service_configs.api_key` 都是 NOT NULL
  （本地 provider 用 `api_key=""`）；另外 `dramas` **没有 `art_style` 列**（画风存在别处，别按 TS 的字段名想当然）。

## S7 第 14 步：图像连续性 QC（`/episodes/{id}/consistency-qc`）→ **未注册只剩 1 条**

**新增**：`app/services/consistency_qc.py`（← `consistency-qc.ts` **227 行**，整域关闭）｜`POST /episodes/{id}/consistency-qc`｜`tests/consistency_qc_test.py`（**28 用例**，用 ffmpeg 现场造**真实图片**跑真实 dHash）。守卫：已注册 225→**226**、未注册 2→**1**（只剩 `storage/change`，有意延后）；另给守卫加了**数值常量镜像** `_consistency_thresholds_drift`（阈值是行为参数，与其它字符串镜像同类）。

- ⭐ **待办表里那条「视觉模型 + 多图输入」是误记**：`consistency-qc.ts` **没有任何 LLM/视觉模型**，全靠本地图像哈希 —— dHash（9×8 灰度→64bit）+ 汉明距离 ⇒ 相似度。**别再按「要挂 Vision adapter」估工作量。**
- **dHash 用 ffmpeg 而非 sharp**（venv 刻意不装 Pillow/numpy/cv2）：`scale=9:8:flags=lanczos,format=gray -frames:v 1 -f rawvideo -pix_fmt gray -` ⇒ **正好 72 字节**。同族算法但实现不同 ⇒ 个别 hash 位可能翻转、similarity 有 0.0x 级偏差；本域是 report（留痕）语义，接受（已写进模块 docstring）。
- **实测真值表**（造图跑出来的，可用于设计断言）：同图=**1.0**；`testsrc2` vs `smptehdbars`=**0.5625**⇒`0.563`（half-up，又一个 `round()` 陷阱）；`smptehdbars` vs 升坡=**0.4219**；升坡 vs 降坡=**0.0**；`testsrc2` vs 降坡=**0.3281**。
- ⭐ **继承自 TS 的并发缓存竞态**（照抄不修）：`Promise.all([getHash(prev), getHash(cur)])`/`asyncio.gather` **并发**取 hash，两边都先看到「缓存未命中」⇒ **同一对里同一张图会被算两次**（4 张唯一图实测 5 次调用）。缓存只省**后续对**的重复计算。断言就按 5 写，并注明原因。
- ⚠️ 语义细节：**原 TS 不过滤软删**（`where(episodeId)` 而已）⇒ 软删分镜照样参与比对；排序是 JS 稳定排序 + `?? 0`（空值当 0）；`sameScene = !!prev.sceneId && prev.sceneId === cur.sceneId`（**0/空 算跨场景**）；latest QC 取 **id 最大**（不是 created_at）；写回**只针对有 QC 记录的分镜**、单条失败只 `log_task_warn`；**report 模式不动总分/status**；跨场景最高只到 `info`（**不升级 warning**）。
- ⚠️ 文案里的数字要用 JS 语义：`相似度 1，正常`（不是 1.0）、`相似度 0 过低`、阈值插值 `≥0.55` ⇒ 新增局部 `_js_num_str`。
- ⚠️ `dimensions` 解析差异（有意）：TS 把 `JSON.parse` 结果直接赋值，非对象时会静默丢列；Python 只认 dict ⇒ 正常数据一致，脏数据是「救回来」。
- **踩坑（都是测试自己写错，实现都对）**：① 种子把「更旧的 QC 行」插在**后面**（拿到最大 id）⇒「取 id 最大」断言反向命中；② 用 dict 按 `storyboard_id` 收敛 QC 行 ⇒ 同分镜多行被吞；③ 排序断言里我写的是**编号**而不是 **id**（按插入序 3,1,2 推 id 会错）⇒ 改成先把三个 id 存成变量。
- ⚠️ **一条旧断言必须跟着「未迁移清单」走**：`smoke_test` 里 `strangler: unmigrated sub-path -> 501` 原本用 `GET /dramas/{id}/rhythm`，而 rhythm **早已迁移** ⇒ 长期是红的。改成用当前仅剩的 `POST /storage/change`，并把 rhythm 改成正向断言（`!= 501`）。**这条在迁 storage/change 时还要再改一次。**
- **数字校准**：全量实测 **2277 项 / 54 个套件**（此前 README 写 2285 / 五十五项，是按加法推的、**偏大** ⇒ 已按实测校准；冒烟 482→**477**）。

## S7 第 15 步：TS 源码快照重冻结 + **自动发现**（删 `backend/` 的最后一块前置）

**家务先行**：`MEMORY.md` 7798/8000（余量 202，守卫报警）⇒ 把 §视频提示词语料的两条细节下移 `TOPICS.md`（检索管线已就绪 8987 条/3 源、已排除源清单 —— TOPICS 原本缺这两条），并压缩坑⑨ ⇒ **7798 → 7600** ✓ 报警消失。

**发现的问题（很值得记）**：`tests/frozen_ts/` 在盘上**只有 72 个文件**，缺 `services/consistency-qc.ts` —— 因为冻结脚本 `FILES` 是**手写清单**，我今天新加的 `_consistency_thresholds_drift`（读 `services/consistency-qc.ts`）没被纳入。后果：`PARITY_USE_FROZEN=1` 直接 `FileNotFoundError` ⇒ **README 里「已实测快照与真源码一致」的那份快照，其实已经过期**（快照存在但不覆盖新增守卫读文件）。⇒ 这正是「删库保险」最怕的静默失效。

**修法（不只是补一个文件名）**：给 `freeze_ts_snapshot.py` 加 `discover_refs()` —— 扫守卫源码里的 `_SRC_ROOT / "a" / "b.ts"`（**含跨字面量拼接**，正则 `_SRC_ROOT((?:\s*/\s*r?"[^"]+")+)` 再拆引号），`.ts` 结尾当文件、其余当目录，与手写清单**取并集**（`_effective()`）；`freeze()`/`check()` 都用它。
- 重冻：**74 个文件 / 715 KB**（自动发现补上了那 1 个）✓；`--check` OK ✓。
- 新增 `tests/freeze_snapshot_test.py`（**7 用例**）专守「**守卫读到的每个文件都在快照里**」+「自动发现能扫到跨字面量拼接」⇒ 以后新增守卫忘了重冻，这里直接红。
- ⭐ **实测关键证据**：真源码模式与 `PARITY_USE_FROZEN=1` 模式**结论完全一致**（Node 224 / Python 226 / 未注册 1 / 0 漂移）⇒ 「删 `backend/` 后九道守卫照样工作」成立 ✓。

**删库决策的现状盘点**：`app/` 对 `backend/` 路径依赖 **0** ✓｜未注册 **1**（`storage/change` 有意延后）✓｜对拍工具 + 差分比较器就绪，`tmp/parity.json` 今日 6:24 重跑过（2833 B，与 9-14 同尺寸 ⇒ 0 新差异）✓｜⚠️ 但 `parity_diff.py` 的 CASES 只有 **10 条只读 GET**，**S7 新增的 GET（`export/dramas/*/qc-report`、`/contact-sheet`、`dramas/*/rhythm`）未覆盖** ⇒ 删库当天补进 CASES 再跑一次更稳妥（需两侧服务器同起，本环境的长命令会被转后台，故留待交互式执行）。

## S7 第 16 步：差分对拍补齐 S7 新增 GET → **实测 一致 12 / 新差异 0**

**改动**：`tests/parity_diff.py` 的 `CASES` 10 → **12**（补 `GET /dramas/{drama_id}/rhythm`、`GET /export/dramas/{drama_id}/qc-report?format=json`）；新增 `strip_timestamps()`（文本响应的 ISO 时间戳抹成 `<ts>`，`_fetch` 的 `__raw__` 分支先用它）；`--selftest` 11→**13**；`tests/parity_diff_test.py` 21→**24**。

- ⭐ **实测（扩展 CASES 后真跑）**：`一致 12 ｜ 已知差异 0 ｜ 不存在路径 4 ｜ 新差异 0`，退出码 0 ✓ —— 两条 S7 新增 GET 在 Node/Python **逐字段一致** ⇒ 删库前最重要的那条证据已覆盖到新端点。
- **故意不入表**：`/contact-sheet` 与 `qc-report?format=html|md` —— 文本响应内嵌 `generatedAt`，整段 HTML 逐字比噪声大、收益低；**数据等价已由 JSON 用例覆盖**（同一份 report 渲染）。`strip_timestamps` 是留给「真要文本比对」时的地基。
- ⚠️ 环境坑（本轮多次踩）：`execute_command` 报「已转后台」时**命令其实在跑**，输出会落在重定向文件里 —— 别急着重跑，先 `Get-Content` 那个文件；本轮 3 次「skipped」实际都跑完并写出了完整结论。
- ⚠️ 另一处锚点教训：批量字符串替换时用 `-notmatch '<新字符串>'` **判断是否已插入**会误判（该字符串可能出现在**别处正文**里）⇒ 那次树形清单插入被静默跳过，只好再补一次精确替换。

## S7 第 17 步：技术维度 QC（补掉最后一个**行为缺口**）

**新增**：`app/services/technical_qc.py`（← `technical-qc.ts` **281 行**）｜接线：`qc_scoring.run_qc_after_video_complete` 里的 `tech-qc-skipped` **存根改成真调用**（失败 warn `tech-qc-failed`，对齐 Node 的 `.catch()`）｜`tests/technical_qc_test.py`（**31 用例**，ffmpeg 现场造黑场/静止/低帧率/响度/坏文件夹具）｜守卫把数值镜像泛化成 `_numeric_thresholds_drift`（表驱动：`CONSISTENCY_QC_THRESHOLDS` + `TECH_QC_THRESHOLDS`）。

- ⭐ **为什么这步重要**：Node 的 `qc-scoring.ts` 在规则打分后**独立触发** `runTechnicalQc`（写 `video_spec` 维度、**重算总分** `round(规则三维均值*0.7 + 技术分*0.3)`、**并按「全部 issues 里有没有 error」定 passed/failed**）。Python 侧此前只跑规则分 ⇒ 同一个 `/storyboards/{id}/qc` 在两侧会得到**不同总分与状态**（路径守卫完全抓不到这类差异）。
- ⚠️ **有意差异（阻塞 vs fire-and-forget）**：Node 是 `runTechnicalQc(...).catch()` **不等待**；Python 侧**同事务同步跑完** —— 理由：写回必须用调用方的连接，detached task 极可能在连接提交/关闭之后才执行。代价是调用方多等 ffmpeg 4~5 趟（短视频 1~3s）。
- 🔴 **三项检测是继承缺陷（Node 与 Python 都永远判不出来）** —— 实测 ffmpeg 真实输出后确认，**别单边修**：
  - `freezedetect` 实际输出带 `[Parsed_freezedetect_0 @ 0x…] lavfi.freezedetect.` 前缀，且 `freeze_start` / `freeze_duration` **分行**；正则需要两者相邻 ⇒ 永不匹配；
  - `ebur128` 现代版本打**逐帧进度行**（`t: 0.09 … I: -70.0 LUFS … TPK: -16.5 dBFS`），**没有** `Integrated loudness:` / `True peak:` summary 行 ⇒ 两个正则都永不匹配。
  ⇒ 结果：**冻帧 / 音频真峰 / 集成响度三条判定是死代码**（`integratedLoudness` 恒 null）；**黑场 / 帧率 / 时长**三项正常。测试已用「真实输出格式 ⇒ 解析为空」的用例**锁定当前行为**（修好时会红 = 预期信号，两侧要一起改）。
- ⚠️ **`execFileP` 的判失败条件是「非零退出**且** stderr 为空」** ⇒ **坏文件/不存在的视频会被当作成功**（ffprobe 非零但打了 stderr）⇒ 不扣分、后续全空转、最终 100 分。照抄，勿改成「非零即失败」。
- ⚠️ JS 数字语义又两处：`toFixed` 用 half-away-from-zero（`1.005→1.01`，Python f-string 给 `1.00`）；`${1.0}` 渲染成 `1`（阈值文案 `标准 >1s`）。
- 顺带体检（删库前该做的）：多处 **docstring 的「未迁移 N 个」清单已过期**（如 `storyboards.py` 列的 9 个端点早已全迁）。已把 `storyboards.py` 改成「**以 `route_parity_test` 机械扫描为准，别再信任手写清单**」；`style_profiles` / `dramas` / `props` / `scenes` / `usage` / `agent_configs` / `ai_configs` / `export` 待清（任务已记账）。
- 数字：**2318 项 / 56 套件**；受影响套件复跑全绿（`qc_scoring` 29/29、`video_generation` 50/50、`smoke` 477/477、守卫 0 漂移）。

## S7 第 18 步：清过期 docstring → 顺手挖出并修掉**两个真行为缺口**

**① 过期 docstring 清理（8 处）**：`style_profiles` / `dramas` / `props` / `scenes` / `usage` / `agent_configs` / `ai_configs` / `export` 的「未迁移 N 个」清单全部过期（`storyboards.py` 上一轮已改）。统一改成「已整域迁移 + 以 `tests/route_parity_test.py` 机械扫描为准」。⇒ ⚠️ **教训：本仓文件头的手写清单多次静默过期**，别信手写清单。

**② ⭐ 真缺口 A：`webhooks.py` 里 `run_qc_after_video_complete` 是空实现（`return None`）** —— 而 `qc_scoring` 的实现**早已迁完**，Node 的 `webhooks.ts` 也是直接调它 ⇒ **Python 的 vidu 回调此前从不打 QC 分**（规则分 + 技术维度都缺）。已改为直接复用 `services.qc_scoring.run_qc_after_video_complete(conn, ...)`（薄包装会多一份抄本），并把调用点补上 `conn`。

**③ ⭐ 真缺口 B：`grid.py` 的 `try_agent_grid_prompt` 是「恒返回 None」存根** —— 而 `run_agent_with_retry` 早已可用（`services/agents/runtime.py`，且 `agent.py`/`auto_pipeline.py` 都在用）⇒ 宫格端点此前**永远**走本地构建器，而 Node 会优先用 Agent。已按原 TS 接通（async；消息 9 段拼接、空段丢弃、镜头 ID 紧凑 JSON；**工具结果优先于正文**；异常吞掉返回 None ⇒ 端点回落）；顺带把 `grid_prompt` 的依赖从 `get_conn` 换成 `get_tx`（Agent 运行会写库）。三个解析助手（`_find_grid_payload`/`_normalize_grid_payload`/`_extract_json_candidate`）**早已在**，只缺本体。

- **新增** `tests/grid_agent_prompt_test.py`（**12 用例**，打桩 `run_agent_with_retry`，含端到端 `source='agent'` / `'fallback'` 两条）。
- 复跑全绿：`grid_prompt` 48/48、`grid_route` 42/42、`misc_routes` 37/37、守卫 226/1/0 漂移。
- ⚠️ **方法学**：这一轮的价值全部来自「**别信文档，去比两侧实现**」—— 两个缺口都是路径守卫（只看「有没有注册」）**永远看不见**的。删库前这类体检（存根扫描 + 接线对照）比补测试更值钱。
- 数字：**2330 项 / 57 套件**。

## S7 第 19 步：**全面体检**（"还差哪些没实现"）—— 又修一处空实现

**方法**：① 文件级对照（Node `backend/src/**/*.ts` vs Python `app/**/*.py`，归一化 kebab/camel + 目录别名）② 存根标记扫描（`未移植|恒返回|return None|占位`）③ 逐处核实「注册了但没实现」。

**本轮修掉的第 3 个空实现**：`ffmpeg_merge.run_consistency_qc_before_merge` —— Node 在**合并前**动态 import `consistency-qc` 做穿帮筛查，Python 侧是 `return None` ⇒ 合并前从不筛查。已接通（复用 `consistency_qc`）。
- ⚠️ **踩坑**：该函数所在的主流程 `merge_episode_videos` 是**同步**的（但由异步端点调用 ⇒ 事件循环在跑，末尾用 `asyncio.create_task` 起后台拼接）⇒ 直接写 `await` 会 **SyntaxError: 'await' outside async function**（被 `merge_test` 当场抓住 ✓）。正解：**同样用 `asyncio.create_task(...)` 派发**（任务自带独立事务 ⇒ 可安全 detach）。

**过期 docstring 又清 4 处**：`routers/skills.py` 与 `services/skills.py`（`loadAgentSkills` 其实**已实现**为 `agents/skills.py: load_agent_skills`，runtime 真调用 ⇒ skill 段真注入）、`routers/evaluation.py`（「未迁 3 个」其实已迁 ⇒ 评测域 5/5）、`video_generation.py`（技术维度已迁）。

**体检结论（"还差什么"的权威答案）**：

| 类别 | 结论 |
|---|---|
端点 | Node 224 / Python 226 / **未注册仅 1**（`storage/change`，有意延后）；**无「已注册端点仍反代 Node」** ⇒ 删库无端点失效 |
已修空实现 | `webhooks` 的 QC（第 18 步）、`grid` 的 Agent 提示词（第 18 步）、`ffmpeg_merge` 的合并前筛查（本轮） |
**仍未实现（真缺口）** | ① **像素级校色** `apply_color_grade_to_file`（`color_grade.py` 明说未移植 ⇒ 跳过校色保留原图，**成片颜色与 Node 不同**；原实现走 sharp，**可用 ffmpeg 滤镜等价**、不必装 Pillow）② **`run_subagent` / `list_available_agents` 两个 Agent 工具**（窄：`auto_pipeline` 是**代码显式编排 4 段**、不依赖它；但提示词仍写着这两个工具 ⇒ 手动跑该 Agent 时会「工具不存在」）③ `middleware/logger.ts`(68 行) 无对应（低） |
结构性差异（**不是**缺口） | `db/schema.ts`+`index.ts`(1455) → `models.py`(703)+`db.py`；20+ per-provider adapter → 4 个聚合文件（有漂移守卫）；4 个 library 路由 → `libraries.py`；`agents/index.ts`(744) → `agent_prompts`+`agent_registry`+`agents/runtime`+`agents/skills`+`agents/mcp`+`creator`；`services/ai.ts` → `ai_providers.py` |

- 复跑：`merge_test` 29/29 ✓、守卫 226/1/0 漂移 ✓、契约冒烟 ✓。
- ⚠️ **方法学**：文件级对照 + 存根扫描能抓出「路径守卫看不见」的缺口（注册 ≠ 实现）；但对照脚本必须做**命名归一化**（kebab/camel↔snake、`routes`↔`routers`、`services/agents`↔`agents`、合并式文件名），否则一堆假阳性。

## S7 第 20 步：**像素级校色落地**（ffmpeg，不装 Pillow）—— 最后一个「会改变产出画面」的缺口

**新增**：`color_grade.py` 补齐像素管线（`build_grade_plan` / `build_filter_chain` / `apply_color_grade_to_file`，替代原 `ColorGradeUnavailable` 存根）｜`tests/color_grade_test.py`（**24 用例**）｜`image_generation_test.py` 的校色断言改成新语义（文件不存在 ⇒ ffmpeg 失败 ⇒ 记 `color-grade-failed` 保留原图）。

**做法**：原 TS 8 步里 **7 步是逐通道点式**（①校准 ②白平衡 ⑤对比度 ⑧肤色是线性乘、③曝光是乘）⇒ 合成成 `RGB 乘法 LUT → eq=saturation → 对比度/肤色 LUT` 三个滤镜，只有 ④饱和度跨通道、单独用 `eq`。

- ⭐⭐ **用真实 sharp 实测（`backend/probe-sharp.cjs`）推翻了两处「照公式推」的假设**：
  1. **`.gamma()` 在无 resize 的链里是 no-op**：`gamma(1.6)/(2.8)/(1.5)/(2.9)` 对 `128,128,128` **全输出 `127,127,127`**、对 `200,100,50` 输出 `199,9?,4?`（仅 ±1 取整）。原因：sharp/libvips 的 gamma 是**配对 resize 的编码 gamma**，本链没有 resize ⇒ 按参数公式去 `pow()` 会引入 **Node 根本没有的色调偏移**（这是最易照抄错的一处，已跳过 ⑥⑦ 并写进 docstring/测试）。
  2. **`modulate({brightness})` 是 L 星感知乘法**：128→**199**（不是 192），200→255。⇒ Python 用 RGB 乘法近似，中灰差 ~7/255（≈3%），已记录为已知差异。
- 与 Node **逐值一致**的部分（可直接当回归基准）：校准 red+50 ⇒ 192,128,128（200 输入则钳位 255）｜对比度+50 ⇒ 200→**236**、中灰 128→**不动**、200,100,50→236,86,11。
- ⚠️ jpg 出图显式 `-pix_fmt yuv420p`：libvips 出 JPEG 默认 4:2:0，而 ffmpeg 遇纯色图会挑 4:4:4 ⇒ 不指定就多一条「两边采样不同」的口子。
- ⚠️ **测试自身的两个坑**（第一版踩的）：① 校色是**原地改写** ⇒ 夹具必须**每条用例前重建**，否则前一条结果污染后一条（表现为「192 变 255,200,200」这种诡异值）；② 期望别写太严：灰图过 `eq` 有 rgb↔yuv 往返取整（±3），jpg 采样也要按**指定后**的值断言。
- 数字：**2354 项 / 58 套件**；`image_generation_test` 64/64 ✓、守卫 226/1/0 漂移 ✓。

## S7 第 21 步：子 Agent 调度工具（`run_subagent` / `list_available_agents`）—— 又一个「注册了但没实现」

**新增**：`app/services/agents/subagent.py`（← `agents/subagent.ts` **107 行**：5 条专家注册表 + `MAX_SUBAGENT_DEPTH=2` + 两个工具）｜`tests/subagent_test.py`（**16 用例**）｜守卫新增 `_subagent_registry_drift`（**name/capability 文案逐字镜像** —— 它们是发给模型的能力清单，改一边不改另一边会让调度行为静默分叉）。

- **接线**：`runtime.create_agent_tools` 的 `orchestrator` 分支从「空注册表 + `orchestrator-tools-missing` warn」改为真工具；为此给 `create_agent_tools` **加了 `conn` 形参**（子 Agent 委托要跑真实 Agent run）—— 调用点两处：`build_agent_config`（已有 conn ✓）与 `agent_runtime_test`（改用 `engine.connect()`）✓。
- **ALS → ContextVar**：TS 用 `AsyncLocalStorage` 追踪调用链（防环形 + 深度），Python 用 `contextvars.ContextVar`（同样跨 await 传播、且按任务隔离 ✓）。⚠️ **成败都要 `reset()`**（`finally`），否则同一次 run 的后续委托会带着旧链误判。
- 四条硬约束**都是返回值**（`{"error": ...}`）而非抛异常 —— 工具错误要回到模型手里让它改主意，抛异常会中断整个 run（照抄 TS）。
- 实测要点：注册表 5 条里含 `extractor` ⇒ 以它为 parent 时清单是 **4** 条（我第一版写成 5，被自检打脸）；桩里用 `protocol=None` 想表达「无 protocol」会被 `None` 默认值吞掉 ⇒ 用哨兵对象区分「没传」与「传 None」。
- 数字：**2370 项 / 59 套件**；`agent_runtime_test` 50/50 ✓（含新增「只给 orchestrator 装」断言）、守卫 226/1/**0 漂移** ✓、契约冒烟 **477/477** ✓（上一轮那次 476/477 确认是环境抖动）。

## S7 第 22 步：请求日志中间件（`middleware/logger.ts`）→ **未实现清单清零**

**新增**：`app/http_logger.py`（← `middleware/logger.ts` 的 `requestLogger`，68 行里的请求日志部分）｜`main.py` 注册 `app.middleware("http")(request_logger)`｜`tests/http_logger_test.py`（**13 用例**）。

- 先盘点了**入口级行为**，发现 `middleware/logger.ts` 只有「请求日志」是缺的 —— Node 的另一个导出 `errorHandler`（未处理异常 → `{code,message}` 信封）Python 侧**早已有**（`main.py` 三个 `@app.exception_handler`：`StarletteHTTPException` / `RequestValidationError` / `Exception`）；CORS、`/api/v1/health`、`/static/*`（Range）也都在 ✓。
- 复刻细节：请求行与响应行**共用一个时间戳**（Node 先取 stamp 再 await）、状态码着色（5xx 红 / 4xx 黄 / 3xx 青 / 其余绿）、耗时 `toFixed(0)` 语义（用 `int(ms+0.5)`，别用 `:.0f` 的银行家舍入）、body 仅 POST/PUT/PATCH 且 >500 字符截断加 `...`。
- ⚠️ **两处有意差异（都写进模块 docstring）**：① 加 `HTTP_LOG=0` 开关（**默认开**，与 Node 同行为）—— 本仓自检大量走 `TestClient`，一次全量上千行噪音会淹没 SUMMARY；② 按 `Content-Length` 预判：**超大请求体不读**（读 body 会让 Starlette 整包缓存 ⇒ multipart 大视频会在中间件里先缓冲一次；Node 无此保护，但它也只影响超大请求的日志）。
- 自检自身踩两个：`toFixed` 尾巴后面还跟着 reset 色码 ⇒ 不能直接 `endswith("...")`；`POST /dramas` 成功是 **201** 不是 200。
- 数字：**2383 项 / 60 套件**；`http_logger_test` 13/13 ✓、契约冒烟 **477/477** ✓（中间件未破坏既有链路）。

## S7 第 23 步：清 tmp + README「遗留物表」按实测校真（又挖出 1 条真缺口）

**① `tmp/` 清空**：42 个中间产物全删（`Remove-Item` 屡被环境转后台 ⇒ 改用 **单用途 Python 一行** `pathlib.unlink()` 成功 ✓；这条经验值得复用）。

**② README「已知差异与遗留物」表**（L466-505）有 **12 行已过期** ⇒ 按实测逐行重写（用**行级替换脚本**跑，避开 PowerShell 引号坑；脚本用完即删 ✓）。修正后的事实：
- ✅ 已迁但 README 仍说「未迁」的：镜头 QC 打分（`qc_scoring`+`technical_qc`）、节奏相位/时代背景提炼/续写剧本/一致性 QC、`GET /usage/estimate`、trace 写入侧（`append_trace_event` **早就实现了**）、`GET /agent-configs/defaults`、`POST /agent-configs/generate`、`style-profiles/distill`、`/gpu/*` 两端点、`export` 整域 7/7、`downloadFile`。
- ⚠️ 行级替换的坑：同一子串可能命中 2 处（`trace **写入侧**` 在 S5 行里也出现）⇒ 定位子串要够长；markdown 里的 `` `x` `` 与 grep 输出的渲染差异会让 needle 失配（最后那条只能手工 `read_file` 后精确替换）。
- ⚠️ 重跑同一脚本**基本幂等**（多数 needle 仍在新行里 ⇒ 再替换成同样内容），但会报「定位失败」——别误判成出错。

**③ 两条新发现（审计价值）**：
- 🔴 **`read_image_as_compressed_data_url` 仍未迁** ⇒ **这是当前唯一剩下的功能性缺口**：退化为「原图 data URL」，内容/画质不变但**发给厂商的载荷更大**（原本 768px/q68 是为控体积）。**可用 ffmpeg 等价实现**（`scale=…:force_original_aspect_ratio=decrease` + 对齐 q68，与校色同一套手法）。
- ✅ `stampStoryboardsScriptHash`（剧本指纹写入侧）**在 Node 侧也只有定义、零调用**（全仓 grep 只 1 处）⇒ Python 未接线与 Node **一致**，**不是缺口**（别顺手接上去，否则反而分叉）。

**待提交清单（截至本轮，21 项）**：memory 2（`2026-09-15.md`/`INDEX.md`）｜`backend-py/README.md`｜`app/main.py`、`routers/evaluation.py`、`services/{agents/runtime, color_grade, ffmpeg_merge, image_generation, skills, video_generation}.py`｜`tests/{agent_runtime, image_generation, route_parity, run_all}.py`（改）｜新增 `app/http_logger.py`、`app/services/agents/subagent.py`、`tests/{color_grade, grid_agent_prompt, http_logger, subagent}_test.py`。`tests/frozen_ts/` 与 HEAD **完全一致**（73 文件已入账，无需再提交）✓。

## S7 第 24 步：参考图压缩落地 → **功能性缺口清零**

**新增**：`file_storage.read_image_as_compressed_data_url` 补上像素管线（弃掉「退化为原图 data URL + 告警」）｜`quality_to_qscale()`｜`tests/compressed_data_url_test.py`（**16 用例**）。

**做法**：`scale=min(W,iw):min(H,ih):force_original_aspect_ratio=decrease:flags=lanczos`（等比、**不放大**，`<=0` 视为该维不约束）+ 有 alpha 走 **`geq` 白底合成** + 末尾 `trunc(iw/2)*2` 保偶数（mjpeg 4:2:0 要求）⇒ mjpeg → base64 → `data:image/jpeg;base64,…`。

- ⚠️ **`geq` 取 alpha 的函数名是 `alpha(X,Y)`** —— 写成 `a(X,Y)` 会 `Unknown function`（我按「选项名是 a」类推错了；输出平面**选项**才叫 `a`）。
- ⚠️ **编码器不同 ⇒ 体积不可能相等**（已实测记录）：1024×768 源图，sharp `jpeg({quality:68, mozjpeg:true})`=**17399 B** vs ffmpeg mjpeg `-q:v 10`=**29039 B**（同视觉质量约大 1.4~1.7×；mozjpeg 本就是压缩率优化的编码器）⇒ 采用 `quality→-q:v` **线性近似**（q68→10，即 `31 - quality*0.31`），**优先保视觉质量**；要更小体积就传更低的 `quality`。
- ⚠️ **夹具坑（害我误判一轮）**：`-f lavfi -i "color=black@0.0"` 在本机 ffmpeg **不生效**（解出来 alpha=255 ⇒ 是**不透明黑**）⇒ 造 alpha 夹具必须用 `-vf format=rgba,colorchannelmixer=aa=0.5` 之类显式设 alpha 平面。
- ⭐ **自检范式**：判「夹具是否真带 alpha」要**看 alpha 值**（解首像素第 4 字节），只看 `pix_fmt == rgba` 会被假夹具骗过 ⇒ 两条 alpha 用例会变成假红/假绿（本轮就是靠这条前置断言把锅定位到夹具而不是实现）。
- 语义要点：**全透明图 ⇒ 纯白**（flatten 白底；丢 alpha 会得到黑/原色 —— 这条用例是「选 geq 而不是 `format=rgb24`」的理由）；小图不放大；竖图按高受限。
- 数字：**2399 项 / 61 套件**；`compressed_data_url_test` 16/16 ✓、两个调用方套件 `image_generation` 64/64 ✓ + `video_generation` 50/50 ✓。

**至此：「有 Node 实现但 Python 缺/存根」的功能性缺口 = 0**；只剩 `POST /storage/change`（有意延后，等 Node 下线）。

## S7 第 25 步：**删库前对账**又抓出快照缺口（第二次同类事故）→ 补自动发现 + 端到端验收

**怎么发现的**：删库前例行「守卫双路复核」（真源码 vs `PARITY_USE_FROZEN=1`）——**真源码 0 漂移，冻结模式漂移 1 条**：
`DRIFT services/technical-qc.ts: 文件不存在（守卫依赖的 TS 源码缺失）`。

**根因（与第 15 步同类，但深了一层）**：`discover_refs()` 只认 `_SRC_ROOT / "services" / "x.ts"` 这种**拼接形态**，而第 19 步我写的 `_numeric_thresholds_drift` 用的是**表驱动普通字符串** `("services/technical-qc.ts", "TECH_QC_THRESHOLDS", "app.services.technical_qc")` ⇒ 自动发现扫不到 ⇒ 快照缺件。
⚠️ **教训**：只扫「我记得的那种写法」的自动发现**必然再次漏**。现在两种形态都扫（普通字符串形态按「文件确实存在」过滤，防注释里的幽灵路径）。

**同时补上真正的验收（比补清单更重要）**：`freeze_snapshot_test.py` 新增**端到端**用例 —— 用 `PARITY_USE_FROZEN=1` **子进程跑一遍 `route_parity_test.py`**，要求退出码 0 且「漂移 0 条」。它不关心清单怎么写，只看**快照够不够用**；本轮正是它该报的错（当时上面的「存在性检查」是绿的 ⇒ 光看清单还是会漏）。

- 重冻结：**76 个文件 / 728 KB**（手写 6 + 自动发现 3 个文件；此前 74/715）✓
- 冻结模式守卫：**0 漂移** ✓；反漂移自检 **8/8** ✓（含新端到端项）
- 数字：README **2400 项 / 61 套件**；全量本轮只跑到第 7 个套件（0 失败）就被环境截断，其余套件在本轮各步都已单独跑绿。

## S7 第 26 步：**全量 61 套件一次跑绿**（分批取证）+ 数字按实测校准

**为什么要分批**：环境反复把 `run_all.py` 的长命令转后台并中途截断（两次分别只跑到第 7 / 第 6 个套件）。改用**切片跑**（`python -c` 里 `import run_all` 取 `TESTS[a:b]`，仍是**每套一个子进程**、沿用 run_all 的 `PYTHONIOENCODING=utf-8` 处理）⇒ 三批（0-20 / 20-41 / 41-61）各自一轮跑完。

- ⭐ **实测结论：SUITES=61 / TOTAL=2401 / FAIL=0**（此前 README 的 2400 是**加法推算**，实测多 1 项 ⇒ 已按实测校准）。三批产物：`tmp/ba|bb|bc.txt`（已清）。
- ⚠️ **踩坑**：PowerShell 的 `>` 重定向写的是 **UTF-16**，用 Python `read_text(encoding='utf-8')` 汇总会得到 0 项（正则全不匹配）⇒ 汇总脚本要按 `utf-16` 解码（本项目已第二次栽在 PowerShell 输出编码上，与「组合命令被路由到 cmd」同类）。
- ⚠️ **快照计数口径**：`freeze()` 打印的 76 是**复制次数**，盘上/`git ls-files` 是 **74 个不同文件**（手写清单与目录清单有重叠：`agents/index.ts` 既在 FILES 又在 `agents/` 目录里）⇒ 打印文案已改成「74 个不同 .ts（本次复制 76 次）」，README 同步。
- **git 现状**：工作区**干净**，HEAD = `21c5a70 feat: 提交代码`（含本轮 `frozen_ts/services/technical-qc.ts` +280 与 `freeze_snapshot_test.py` +16）⇒ **没有未提交物**，删库将是一次干净的新变更。

## S7 第 27 步：迁完**最后一个未注册端点** `POST /storage/change` ⇒ 未注册 0 条（绞杀者收口）

**改动**：`services/data_storage.py` 加 `change_data_root()` + `db.py` 加 `close_engine()`/`reopen_engine()`（对位 Node 的 `reopenDatabase()` + `rebuildDb()`）｜`routers/storage.py` 加 `POST /change`（整域 2/2）｜`tests/storage_change_test.py`（**38 用例**）｜smoke/README/run_all 同步。
**守卫实测**：**Node 224 / Python 227 / 未注册 0 / 0 漂移** ✓ —— Python 首次**全量覆盖** Node 路径。

- ⚠️⚠️ **本轮最险的一处**：smoke 里旧断言 `POST /storage/change {"path":"x"}`（**相对路径**，原本只验 501）。迁移后它**真的执行了切换**：resolve 成 `backend-py/x`、复制旧数据、并把**仓库根 `.data-root`** 写成该路径（git status 里冒出 `?? .data-root` 与 `?? backend-py/x/`）。**已清理两样**，并把断言改成「项目根入参 → 400」+「被拒时数据根不变」。
  ⇒ **教训**：*任何「曾经只会 501、现在已注册」的断言都要重新审一遍入参的副作用*。另把 `.data-root` 加进 `.gitignore`（机器相关绝对路径，本就不该入库）。
- `copy_db_files` **逐字照抄了 TS 的怪癖**：目标名用的是**源文件的 basename**（`path.join(dirname(newDb), basename(src))`），不是新路径的名字。真实调用两边都叫 `drama.db` 所以看不出来 —— 我第一版夹具用 `old.db → new.db` 才被打回（见下）。
- `db.py` 重构要点：PRAGMA 监听原本靠**装饰器**挂在初始 engine 上 ⇒ 换 engine 会**静默丢掉** `WAL`/`busy_timeout` 两条硬约束；现在抽成 `_make_engine()` 里 `event.listen`，`reopen_engine()` 复用，并只在**新库文件不存在**时 `create_all`（与导入期同策略，避免 DDL 漂移）。
- **四个「测试自己写错」的坑**（实现全对）：
  1. `from app.db import engine` 拿的是**对象快照** ⇒ `reopen_engine()` 换的是**模块属性**，断言「已指向新库」永远看到旧库（还让「新库已建表」**假通过**：旧库当然有表）⇒ 必须 `db.engine`。
  2. 回滚用例**夹具没造可复制内容** ⇒ 打桩的 `_copy_tree` 根本没被调用、迁移反而「成功」，三条回滚断言全错位。
  3. 夹具用了 `old.db → new.db` 这种**异名**，测到了 TS 根本不会走的分支（见上）。
  4. 「与当前目录相同」断言里写死了 `_ROOT0`，但那时数据根已被前两轮切换改了 ⇒ 期望 400 却拿到 200，**还把后续用例的根偷偷改掉**。
- ⚠️ **别比主库文件体积**：WAL + `close_engine()` 触发检查点 ⇒ 同一库「复制前 / 复制后」体积会变（实测 4096 → 159744）。要比就比**逻辑等价**（两边都能打开、表结构在）。
- smoke 的**动态未迁移抽样**从「非空」**翻转**为「必须为空」（以后谁加了 TS 端点忘了注册，这里先红）；smoke 计数 477 → **475**（少掉的正是那 3 条已无样本的 501 抽查，多 1 条零副作用断言）。全量：**62 套件 / 2441 项**。

## S7 第 28 步：`scripts/` 收拾（体检 + 文档收口）

- **体检结论：目录本身是健康的** —— 16 个文件**全都有引用**（README 详尽到「为什么不用严格超集互相替代」），7 个 `.py` + 4 个 `.mjs` **语法全清**，`model_manager.py list` / `sd_h3_compat_probe.py --list` **实测可跑**（16 个模型清单、4 类视频架构）。
- 真正缺的是一块**指路牌**：`scripts/README.md` 只写「三类脚本」，而**后端自检工具在 `backend-py/tests/`**（62 套件，含删除 `backend/` 后唯一保命的那几支）⇒ 补了第四类 + 说明「为什么不放一起」（那里 `import app.*`、这里刻意零依赖，约束相反）。
- 顺手修 `run_all.py` 的**陈旧 docstring**（写着「五个自检」，实际 62 套件）⇒ 现在写明「`TESTS` 就是权威清单」+ 环境依赖（三套用 ffmpeg 现场造媒体）+ 分批跑法。
- `backend-py/README` 测试树补了 5 个漏登的套件，并注明「本树只是摘录，权威清单在 `run_all.py`」。

## S7 第 29 步：全仓「去 Node 化」——scripts/ 与 frozen_ts 双双改为 Python 并收进 backend-py

用户要求：`backend-py/tests/frozen_ts` 与仓库根 `scripts/` **都改成 Python，并合并进后端项目**。两侧都做完，仓库根**已无** `scripts/`、仓库内**已无** `.ts` 快照。

**A. `scripts/` → `backend-py/app/scripts/`（4 个 Node 守卫 + 3 个语料脚本移植为 .py；`.py` 工具链搬迁；`migrate_models.ps1` → `.py`）**
- 移植**逐条对齐**，并用「新旧对拍」验收（Node 版在删之前一直留着）：`check_memory` **IDENTICAL**、`check_skill_refs`（含 `--verbose`）**IDENTICAL**、`test_guards` **12/12 且逐行一致**（仅汇总行里的脚本名不同）、`corpus/search` **IDENTICAL**（真语料 + 中文查询）、`corpus/normalize` **多重集相等**（8987 行，行序有意改为确定性排序）、`model_manager list` 仍列出 16 个模型、`compat_probe/analyze3/h3_install` 均可跑。
- ⭐ **JS→Python 移植的六个口径坑**（全部实测踩出，已写成 TOPICS 条目）：
  1. **换行转换**：Python `read_text()` 默认把 CRLF 折成 LF，Node **不折** ⇒ `MEMORY.md` 字符数 7651 vs **7598**（差 53 = 行数），警告线静默偏移 ⇒ 必须 `newline=""`；
  2. `\w` 语义：JS 是 ASCII，Python 默认 Unicode ⇒ 正则要 `re.ASCII`；
  3. **数字转字符串**：JS `String(1.0)` → `"1"`，Python `str(1.0)` → `"1.0"` ⇒ 语料里有 **206 行**的 `aspect` 悄悄变样（对拍抓到）；
  4. `Math.round` 是 **half-up**，Python `round()` 是银行家舍入 ✗；
  5. `toFixed` 同样是 half-up（本仓库已被 `round()` 坑过两次，这次统一 `Decimal` + `ROUND_HALF_UP`）；
  6. `String.length`/`slice` 是 **UTF-16 码元**口径 ⇒ 统一 `_js_len()` / `_js_slice()`。
- ⚠️ **搬目录的静默杀手**：这些脚本原先用 `dirname(SCRIPT_DIR)` 当仓库根，搬进 `backend-py/app/scripts/` 后**要上跳两级**（`model_manager.py`/`sd_h3_pipeline.py` 已改；`corpus/analyze{,2,3}.py` 的 `parents[2]` → `parents[3]`）。只跳一级**不报错**，只是 `models.json` 读不到 ⇒ 清单为空。
- ⚠️ `h3_install.py`（旧兼容 shim）**搬之前就坏**：`configs/models.json` 里有条目没有 `url` 键，它直接 `m["url"]` ⇒ import 期 `KeyError`，连 `--help` 都跑不起来。已改成「缺 url 就跳过并把 key 打到 stderr」，交给 `model_manager.py` 处理。
- `.githooks/pre-commit` 重写：解释器解析顺序 `backend-py/.venv` → `python3` → `python` → `py -3`（守卫只用标准库），路径与触发条件全部换成新位置。
- 引用收口：12 个文件 **43 处**（记忆三层、`skills/README.md`、3 个 docs、`local_model_scan.py`、`corpus_tools.py`、`configs/models.json`、README），另有 `check_skill_refs` 的 `REPO_ROOT_PREFIXES` **新增 `backend-py/`**（否则指向 `backend-py/…` 的引用会落进「基准不明」被静默跳过 —— 与当初漏 `docs/` 同病）。
- 新 `backend-py/app/scripts/README.md` 承接了旧 README 的判据表、语料许可表（含**已排除源**）、依赖图与「为什么这么放」。

**B. `frozen_ts/`（74 个 `.ts`）→ Python 模块快照**
- 生成物 `tests/frozen_ts_source.py`（`FILES: dict[str, str]`，**逐字**文本，74 条 / 625 KB）；新增 `tests/frozen_ts.py` 提供 `load()` / `materialize(dir)` / `snapshot_root()`。
- 守卫侧**物化到临时目录**再照常按 `Path` 读 ⇒ **15 处调用点零改动**（`route_parity_test.py` 只改 `_SRC_ROOT` 那 4 行）。
- `freeze_ts_snapshot.py` 重写（`--check` / `--dump DIR` 供人读原文）；`frozen_ts/` 目录**已删**；`freeze_snapshot_test.py` 7 → **12 用例**（新增「表驱动普通字符串形态的自动发现」「物化内容逐字一致」「已无 `.ts` 目录」）。
- 验收：真源码模式与 `PARITY_USE_FROZEN=1` 模式**结论完全一致**（Node 224 / Python 227 / 未注册 0 / **0 漂移**）✓。
- 规模：全量自检 **62 套件 / 2441 项**（freeze 快照 +5）；三道仓库守卫 3/3 ✓、`check_skill_refs` 96 处 0 断链 ✓、记忆致命 0 ✓（`MEMORY.md` 7671/8000，余量 329 ⇒ 下轮加内容前先下移 TOPICS）。

## S7 第 30 步：去 Node 化后的**全量回归** → 实测 62 套件 / 2441 项（抓到 1 处真回归并修掉）

**实测（三批跑完）**：`SUITES=62 / TOTAL=2441 / FAIL=0`（修正后）。此前我按加法写 2442 ⇒ **按实测校准为 2441**（README、`scripts/README.md`、`run_all.py` docstring、本日志都已改）。另：`MEMORY.md` 7671 → **7408**（余量 592，警告已清）。

- ⭐ **回归抓到的真 bug（我自己的）**：`agent_script_corpus_test` **24/25** 红。根因是批量替换的**顺序**问题 —— 我先跑 `node scripts/corpus/x.mjs` → `python backend-py/scripts/corpus/x.py`，**再**跑 `scripts/corpus/` → `backend-py/scripts/corpus/`，后者命中了**已经替换过的文本** ⇒ 产出 `backend-py/backend-py/scripts/...` ✗。共 7 处（`docs/local-h3-video-system.md` ×5、语料三脚本与 `corpus_tools.py`）。
  ⇒ **教训**：批量替换要「**最具体/最长优先，且每轮替换后不复扫**」（或干脆一轮正则搞定），否则后续 pair 会二次命中；**替换完必须跑一遍行为测试，不能只跑守卫**（守卫当时全绿，因为断链与否跟这条文案无关）。
- ⚠️ **第二条教训（测试断言会跟着文案走）**：同一套件里还有一条断言 `"fetch-raw.mjs" in degraded["hint"]` —— 我把安装指引里的命令改成 `fetch_raw.py` 后它就红了。**改任何用户可见文案时，先 grep 断言里的旧字符串**。
- ⚠️ **第三条教训（工具误用）**：PowerShell `Select-String -Path 'backend-py\app\**\*.py'` **不是递归**（`**` 被当字面量）⇒ 我用它查双前缀得到「空=无」的**假阴性**，改用 `search_content`（ripgrep）才查到 7 处。**查全仓必须用递归搜索工具**。
- 其余 61 套件全绿（含 `route_parity` 真源码/冻结双路、`freeze_snapshot 12/12`、`storage_change 38/38`、语料工具 25/25）。

## S7 第 31 步：技能库并入后端（`skills/` → `backend-py/app/skills/`）

**改动**：`git mv skills backend-py/skills`（141 个 `.md` / 177 文件）｜三处技能根同步｜守卫增强｜引用收口 11 文件 42 处｜文档与记忆同步。

- ⭐ **技能根是「三处必须同步」的不变量**（漏一处就是静默失效：扫描为空 / 守卫扫错树）：
  1. `app/services/skills.py` 的 `SKILLS_DIR`：`parents[3]`（仓库根）→ **`parents[2]`**（backend-py）；
  2. `app/services/agents/skills.py` 的 `skills_dir()`：`config.PROJECT_ROOT / "skills"` → **文件相对 `parents[3] / "skills"`**（`PROJECT_ROOT` 是仓库根，改完后**不再适用**，连带删掉该 import）；⚠️ 别用 `PROJECT_ROOT / "backend-py" / "skills"` 这种拼字符串写法 —— 目录改名就断；
  3. `backend-py/app/scripts/check_skill_refs.py` 的 `SKILLS_DIR`：`REPO_ROOT / "backend-py" / "skills"`。
- ⭐ **守卫增强（挪库后的防漂移）**：`REPO_ROOT_PREFIXES` **移除 `"skills/"`**，新增 `_LEGACY_SKILLS_PREFIX` ⇒ 谁再写旧位置 `skills/…` 一律判**致命**并附改法。理由：挪库后最危险的引用是「看着像路径、其实已失效」—— 让它落进「基准不明」被静默跳过就白瞎了。实测搬完**零命中** ✓（说明引用收口无遗漏）。
- **批量改路径这次用对了手法**：**单趟正则 + 负向断言** `(?<![\w./~-])skills/` → `backend-py/app/skills/`。它同时解决三件事：① 不会二次命中 `backend-py/app/skills/`（上一轮双前缀 bug 的根治写法）；② 不会误伤**别人的**路径 `~/Movies/Hub/skills/`（技能库里真有这种文本）；③ 一处正则搞定，不必排序。
- **验收**：守卫 `141 文件 / 96 处 / 致命 0 / 非致命 0`（**与搬库前同数** ⇒ 引用零遗漏）｜`skills_test` **45/45**（顺手修了一条断言旧位置 `PROJECT_ROOT / "skills"` 的用例）｜受影响 7 套件（技能解析/出厂提示词/聊天域/创建器/运行时/语料工具/优化器）全绿｜全量 **62 套件 / 2441 项 / 0 失败** ✓。
- ⚠️ **副作用（已知并接受）**：Node 后端（`backend/src/routes/skills.ts` 与 `agents/skills.ts` 的 `../../../skills`）现在**找不到技能库** ⇒ 删库前若还要启动 Node，技能相关端点会退化。考虑到**未注册端点已 0**、`backend/` 待删，可接受；`frozen_ts_source.py` 里的 TS 原文仍写着旧路径（历史文本，不改 ✓）。
- **可逆性**：`git status` 显示为 **`R`（rename）**（`skills/extractor/SKILL.md -> backend-py/app/skills/extractor/SKILL.md`）⇒ **历史保留**；若要回退，把三处路径常量改回 + `git mv backend-py/skills skills` 即可（不是「只能重做」的迁移）。

## S7 第 32 步：核对「迁到 backend-py/skills 有没有问题」→ 补掉两处部署/遗留侧的手尾

用户问「移到 `backend-py/skills` 没问题吧」⇒ 顺手把**代码之外**的两个消费者也查了，各有一处会踩：

- ⚠️ **`Dockerfile` 会直接断**：第 44-45 行原本是 `COPY skills/ ./skills/`，源路径已不存在 ⇒ **下一次 `docker build` 会显式失败**（还算友好：不是静默缺件）。已改为 `COPY backend-py/app/skills/ ./backend-py/app/skills/`。
- ⚠️ **Node 侧两处硬编码**：`backend/src/routes/skills.ts` 与 `backend/src/agents/skills.ts` 的 `resolve(__dirname, '../../../skills')` ⇒ 已改成 `'../../../backend-py/skills'`。改完后**两侧（Node / Python）用的是同一个相对布局** ⇒ Dockerfile 不需要「取新路径落旧容器路径」这种权宜写法（第一版我差点那么写 ✗）。
- 改完 TS 必须**重冻快照**（否则冻结文本 ≠ 当前源码）：已重冻 74 / 625 KB ✓，`freeze_snapshot_test` **12/12** ✓，守卫真源码/冻结**双路 0 漂移** ✓。
- ⭐ **教训（挪目录要三查）**：① 代码常量 ② 守卫/钩子/文档 ③ **部署与镜像**（`Dockerfile` / `docker-compose` / `.dockerignore` / CI）。这次 ① ② 都在上一步做全了，③ 是用户追问才想起来 —— `COPY` 是显式失败尚可接受，但若写成**通配/volume 挂载**就会变成静默缺件。
- ⚠️ **既有问题（与本次迁移无关）**：技能库是**运行时可写**目录（前端「新增 Skill」会在 `skills/<agent>/<id>/` 建骨架），而镜像只声明了 `VOLUME ["/app/data"]` ⇒ 容器里运行时新建的 skill 会随容器消失。要持久化需再挂一个 volume（记在此，别误当成迁移引入的）。

## S7 第 33 步：**干跑删库**（改名→跑→还原）⇒ 最终全绿；顺带修掉三类「只有真删才现形」的问题

做法：`Rename-Item backend backend__dryrun_off` → 跑守卫与自检 → `finally` 里立刻改回（**全程可回滚**，三次迭代）。**最终结论：删库态全绿** —— `check_all.py` **rc=0**（引用/记忆/守卫自检 12/12）、`freeze_snapshot_test.py` **11/11**（真源码缺失时自动跳过存在性检查）、`route_parity_test.py` **rc=0**（自动回退 Python 快照：224/227/未注册 0/**0 漂移**）、`skills_test.py` **45/45**。

⭐ **方法论（本轮最大收获）：删库前置必须在删之前「真删一次」**。干跑一轮就抓出三类**静态扫描/代码常量排查根本看不到**的问题：

1. **技能文档里 13 处指向 `backend/src/…`**（`skills/README.md`、4 个 SKILL.md 注释等）⇒ 删库即断链。已全部改指 Python 对应实现（`agents/skills.py`、`routers/skills.py`、`prompt_utils.py`、`prompt_blocks.py`、`tools/corpus_tools.py`、`agents/runtime.py`）。
   ⚠️ 其中 **6 处原本没加反引号** ⇒ 引用守卫**根本不采集**（它只认反引号 token / markdown 链接）⇒ 顺手补上反引号，从此受守卫保护（待校验 97 → **102 处**）。
2. **守卫对旧前缀 `backend/` 不设防**（之前只防了 `skills/`）⇒ 已并入 `_LEGACY_HINTS`，写旧前缀一律判**致命**。
3. **冻结脚本 `discover_refs()` 的「表驱动普通字符串」形态依赖真源码存在** ⇒ 删库后会**静默扫不到**（`freeze_snapshot_test` 当场 2 条红）。已改成：真源码在 ⇒ 看文件存在；真源码已删 ⇒ **改看快照**（`--check` 在删库后仍有效）。

**干跑还发现两条「现在不能做、当天才做」的阻塞**（早做会打断 Node，对拍工具就废了）：
- **前端代理指向 5789（Node）**：`frontend/nuxt.config.ts` 的 `/api`、`/static` ⇒ 删库后代理到空处；应改指 **5790**（Python）。现在改**也安全**（未注册 0），但它会切换前端实际后端 ⇒ 等用户点头。
- **`~contracts` 别名指向 `../backend/src/shared/contracts.ts`**（前端**构建期**类型来源，79 行纯类型零 import）：删库会让**前端构建失败**；而 Node 的 `era-background.ts` 也 import 它 ⇒ 得**同步**改两处，否则 Node 起不来。
- 另：`Dockerfile` 是 Node 时代镜像（已加显眼 TODO）、`parity_run/parity_diff` 删库后失效、全仓还有 ~137 处 `backend/src/…` 引用（**多数是「移植自 X」的溯源注释，应当保留**）。
- 完整清单已写进 `backend-py/README.md` 的「删 `backend/` 清单（2026-09-15 干跑实测得出）」小节 ✓。

## S7 第 34 步：提前清掉删库清单的第 1、2 项（代理 + 契约类型），删库当天只剩「删目录 + Dockerfile」

**改前判断**：这两项在干跑清单里被我判成「当天做」（怕打断 Node）。复查后确认**都能提前安全做完** —— ① 代理只影响 **dev 代理**（生产静态产物同源）；② contracts 是 **type-only**（不换运行时行为）⇒ 提前做，删库当天就只剩一条删除 + Dockerfile 重写。

1. **前端 dev 代理 5789 → 5790**（`frontend/nuxt.config.ts` 的 `/api`、`/static`），并留逃生门 `NUXT_API_TARGET=http://localhost:5789 npm run dev` 可临时对着 Node 调试 ✓（Python 侧未注册 0、自足，所以默认指它是对的）。
2. **共享契约类型搬到前端**：`git mv backend/src/shared/contracts.ts frontend/app/types/contracts.ts`（git 记 `R`，79 行纯类型零 import）+ nuxt 别名两处（`alias` 与 `tsconfig.paths`）+ 唯一 importer `app/composables/useApi.ts` + Node 侧 `backend/src/services/era-background.ts` 改成跨项目 import（随 `backend/` 一起消失）。文件头改写为「**前端侧镜像**，字段权威在 Python 后端，改后端字段要同步这里」。

⭐ **验收方式：真跑一次构建**（不是「看着别名写对了」）—— `cd frontend && npm run generate` ⇒ `✔ Server built` / `Prerendered 15 routes` / `✔ Generated public .output/public` ✓✓；构建产物已被 ignore（`git status` 干净 ✓）。
干跑 4（`backend/` 临时改名）：`check_all.py` rc=0（引用 **102 处 / 0 断链**）、`route_parity_test.py` rc=0（224/227/未注册 0/0 漂移）✓。

- ⚠️ **新引入一条跨项目依赖**：`backend/src/services/era-background.ts` → `../../../frontend/app/types/contracts.js`（type-only）。它随 `backend/` 删除一起消失；若要**回退**，两处一起改回即可。
- ⚠️ 小坑：`git mv src dst` **要求目标目录已存在**，否则报的是「renaming 'src' failed: No such file or directory」（看着像源文件不存在，其实缺的是目标目录）⇒ 先 `mkdir` 再 `git mv`。
- ⭐ 方法论：**「当天清单」要按「现在做会不会打断谁」再筛一遍**。我第一版把这两项都判成「当天」，复查发现都能提前 —— 提前清掉的收益是：删库当天只剩不可逆的那一步（删除 + 镜像重写），把风险窗口压到最小。

## S7 第 35 步：清掉清单第 3~5 项（Dockerfile 重写 / parity 说明 / docs 指路语）→ 删库当天只剩「删目录」

1. **`Dockerfile` 重写为 Python 镜像**：`python:3.12-slim` + uvicorn（Node **只**留在前端构建阶段）、端口 **5790**、`COPY backend-py/{app,skills}`、前端产物 → `frontend/dist`（与 `FRONTEND_DIST` 一致）、`WORKDIR /app/backend-py`（`uvicorn app.main:app` 需要 cwd ✓）、`--host 0.0.0.0`（uvicorn 默认只绑 127.0.0.1 ✗ 是关键细节）。`.dockerignore` 补 `backend-py/.venv`（否则 `COPY` 会拖进几百 MB）、`__pycache__`、`tests`、`scripts`、`.codebuddy`。
2. **`parity_run.py` / `parity_diff.py`** 顶部写明「本工具与 `backend/` 绑定 ⇒ 删库后失效，留作历史；日常回归走 `run_all.py` + 快照模式（不需要 Node）」。
3. **docs 指路语**：`docs/preset-skill-template.md` 第 7~9 步原本教人「创建 `backend/src/services/preset-{name}.ts` / 路由 / 在 `backend/src/index.ts` 注册」⇒ **照做会写出一堆将来不存在的 TS 文件**，已改成 Python 路径（并说明 Python 侧预设是框架 + 数据驱动，多数情况**不必**写后端代码）；`docs/api-contract.md` 头部加「当前实现位置 = `backend-py/app/routers/*.py`，本文件里 `backend/…`/5789 均为历史产物」。

⭐ **顺手把它固化成自检：新增 `tests/dockerfile_contract_test.py`（19 用例）** —— 把镜像的路径/端口/产物假设**钉在代码常量上**（`PROJECT_ROOT` /`FRONTEND_DIST`/`server.port` 与 `COPY` 目标、`EXPOSE`、`CMD --port` 必须一致），并断言指令里不出现已迁移的旧引用（`COPY skills/`、`COPY backend/src`、`tsx`、5789）。这正是「技能库搬进 `backend-py/` 后 `COPY skills/` 指向空路径」那类事故的机器拦截；本机**无 Docker** ⇒ 只做静态核对（docstring 已写明，真构建请在装机上跑）。

**两个「不是 bug 的假红」**（都值得记）：
- `frontend/dist` 在本机是**目录联接**指向 `frontend/.output/public` ⇒ 校验路径时**别用 `.resolve()`**（会跳到联接目标，断言假红）；比**原始路径**。（也解释了「代码只写 `frontend/dist`，运行时却指向 `.output/public`」的疑问。）
- Dockerfile **头部注释里会「提到」旧写法**（为了对照说明）⇒ 「不得出现旧引用」这类校验**只扫指令行**（非注释行），否则自己把自己判红。

**数字**：全量 **63 套件 / 2460 项**（+19）。⚠️ 只同步「当前状态」类文件（`backend-py/README.md`、`scripts/README.md`、`run_all.py` docstring）；**日志里此前的 2441 是当时实测值，不改**（历史记录）。

## S7 第 36 步：顶层目录盘点（用户点名 local_services / benchmarks / data / tmp）→ 结论：多数**不该迁**

| 目录 | 是什么 / 谁读 | 处置 |
|---|---|---|
`local_services/` | **本机服务根**：`model_manager.py` 会 `git clone` 服务到 `local_services/<key>`（`PROJECT_ROOT/local_services` 默认值，两处代码常量）；内含 `h3/{server.py,requirements.txt}`（**项目自带**的 8765 薄封装，已纳管） | **不迁**（它是「服务根」＝运行时目录，与 `data/` 同类）；⚠️ 唯一「属后端却留在根」的**代码**是 `h3/`，若要 100% 归拢可只挪它 + 改两处常量 |
`benchmarks/` | **评测基准数据**（4 个 case JSON，全纳管）；`catalog.benchmarks_dir()` 默认 `<项目根>/benchmarks`（删库前置已切好） | **不必迁**（数据不是代码；且 CLI 的 UX 依赖「项目根/benchmarks」这个位置 ⇒ 迁了要连带改 `cli.resolve_case_path` 相对路径解析 + 4 个前端用例） |
`data/` | **运行时数据根**（SQLite + `static/` + `traces/` + `prompt-corpus/` 语料）；`config._default_data_dir = PROJECT_ROOT/"data"` | **不该迁**（gitignored、可由 `DATA_ROOT`/`.data-root` 切换 ⇒ 是「机器状态」不是仓库资产） |
`tmp/` | 临时目录（已清空）、各类工具落日志 | **不该迁**（gitignored） |
`.kombai/` | **空目录**（0 文件、无任何代码引用、未忽略） | 可删（某工具留下的空壳，我不擅自动） |
`reference/` | 学习用外部项目（已 gitignore ✓） | 保留原位 |
`.playwright-cli/` | 前端测试产物（已 gitignore ✓，0.4 MB） | 保留/可删 |
`backend/` | **13408 文件 / 241.7 MB**、144 纳管 | ✅ 唯一的真遗留 ⇒ **删**（顺带回收 ~240 MB） |

⭐ **同轮又抓到一个漏网：`docker-compose.yml` 也是 Node 时代**（`5789:5789`、`PORT=5789`、`NODE_ENV=production`）—— 上一轮我只改了 `Dockerfile`，**编排文件是独立手写的、最容易漏** ⇒ 已重写为 5790/`PYTHONUNBUFFERED`，并把 compose 纳入自检（`dockerfile_contract_test.py` 19 → **23 用例**：无旧端口、含 5790、无 `NODE_ENV`、数据卷仍挂 `/app/data`）。
**数字**：全量 **63 套件 / 2464 项** ✓。

## S7 第 37 步：全量回归抓到**冒烟 2 条红** ⇒ 根因是「断言依赖本机是否构建过前端」

**怎么抓到的**：改动后重跑全量（分批：0-20 / 21-41 / 42-62），批次 A 报 `FAIL 契约冒烟 473/475`。两条红是
`static: traversal blocked` 与 `spa: 404 when frontend/dist absent`。

**根因**：这两条都**默认「本机没构建过前端」**。而我前面为写 Docker 契约测试跑过一次 `npm run generate` ⇒
`frontend/.output/public` 生成、`frontend/dist`（指向它的**联接**）变有效 ⇒ 冒烟里「dist 不存在 ⇒ 404」的假设崩了。
⚠️ **教训**：测试断言**不能依赖环境残留**（有没有构建产物、有没有临时目录），要么夹具化、要么显式打桩。

**修法（两条都改成与环境保护无关）**：
- SPA：把 `app.main.FRONTEND_DIST` **临时指向夹具**（① 不存在的目录 ⇒ 断言 404 + 「开发模式」提示；② 含 `index.html` 的临时目录 ⇒ 断言 200 + 直出内容），`finally` 还原。
- 穿越：原来写 `/static/../configs/config.yaml`，**httpx 在客户端就规范化掉了**（实测打进服务端的是 `/configs/config.yaml`）⇒ 测的是「未知路径」而非穿越 ✗。改用 `%2e%2e%2f` 编码形态直达 handler，才真的验证「resolve + 前缀包含」。
- 冒烟 **473/475 → 476/476** ✓（3 条换 4 条）。**实测全量：63 套件 / 2465 项 / 0 失败** ✓（原 README 的 2464 是加法推算 ⇒ 已按实测校准；`run_all.py` docstring 同步）。
- 环境恢复：删掉我构建产生的 `frontend/.output`（联接 `frontend/dist` 留着，来历不明不擅动）。

## S7 第 38 步：`local_services/` 并入后端（用户点名，推翻我第 36 步的「不迁」结论）

**改动**：`local_services/h3/{server.py,requirements.txt}` → `backend-py/app/local_services/h3/`（旧目录已删）。三处默认值同步：
`app/services/local_model_scan.py` 新增 `BACKEND_PY_ROOT = Path(__file__).resolve().parents[2]`｜
`scripts/model_manager.py` 新增 `BACKEND_PY_ROOT = os.path.dirname(SCRIPT_DIR)`（⚠️ 该文件里 `PROJECT_ROOT` 是**仓库根**、要上跳两级，两个常量别混）｜
TS `local-model-scan.ts` 改成 `path.join(config.projectRoot, 'backend-py', 'local_services')`。
**实测**：`get_default_roots()` 命中新路径 ✓｜`model_manager.resolve_paths().local_services_dir` 同一路径且 `h3/server.py` 在 ✓｜`local_models_test` **89/89** ✓｜快照重冻（TS 改过）**74 文件 / 625 KB** ✓｜双路守卫 **0 漂移 / 0 未注册** ✓。

⚠️ **`.gitignore` 反向包含的坑（差点丢文件）**：git 规则「**父目录被排除则子文件无法再包含**」⇒ 只写
`backend-py/app/local_services/` 会把**项目自带的 `h3/` 一起静默忽略**（下次提交就丢）。正确写法是先 `/*` 再 `!.../h3/`：
```
backend-py/app/local_services/*
!backend-py/app/local_services/h3/
```
已验证：`git check-ignore` 对 `h3/server.py` **无输出**（未忽略 ✓）、对模拟的克隆服务有输出 ✓。
这同时**修掉一个既有缺口**：旧路径从来没有任何 ignore 规则 ⇒ `model_manager.py --runtime git` 克隆的服务一直会污染 `git status`。
（`h3/server.py` 自包含、只读 `COMFYUI_URL` ⇒ 搬迁零改动 ✓。）

## S7 第 39 步：**最后一次 Node↔Python 对拍** —— 抓到 4 条「新差异」，真相是**前端 SPA 兜底**（不是缺端点）

**为什么值得做**：`backend/` 一删，Node 侧就没了 ⇒ 这类「两侧并排比对」**永远做不了第二次** ⇒ 删库前必须跑一次。
跑出来 `一致 12 ｜ 已知差异 0 ｜ 不存在路径 0 ｜ **新差异 4**` ✗ —— 4 条裸列表路径（`/episodes` `/storyboards` `/characters` `/scenes`）报「Node 200 vs Python 501」。

**先排除最坏情况（真缺端点）**：直接查两侧路由源码 ⇒ **都没有裸列表路由**（Node `routes/episodes.ts` 与 Python `app/routers/episodes.py` 都只有 `/:id/...` 与 `POST /`）⇒ **不是缺端点、不阻塞删库** ✓。

**真因**：Node `src/index.ts` L177-180 有一道**无条件**兜底 ——
`app.use('*', serveStatic({root: 'frontend/dist'}))` + `app.get('*', serveStatic({root, path: 'index.html'}))`
⇒ **只要 `frontend/dist/index.html` 存在**（我前面为写 Docker 契约测试跑过 `npm run generate` ⇒ `.output/public/index.html` 有了，而 `frontend/dist` 还是指向它的**联接**），任何**未匹配的 GET** 都会回 **200 + HTML** ✗ ⇒ 把 `MISSING_EXPECT = {node: 404}` 的假设打崩（该假设是 2026-09-14 在「没有前端产物」时定的）。

**修法（与冒烟同一处方：让判据与环境无关）**：`parity_diff.py` 新增 `is_spa_fallback()`（`_fetch` 对非 JSON 体存 `__raw__` ⇒ 看首 200 字符是否像 HTML），`classify_missing` 把「Node **200 + HTML 兜底**」也算作「未匹配」；⚠️ 但 **`200 + JSON` 仍判 `new`** —— 守卫原意（「Node 真有这个端点而 Python 缺」）不能丢。
顺带修准自检分母：`EXTRA_CHECKS` 原来写 5、实际 6 个断言（**少算一个**）⇒ 现为 9（6 + 新增 3），并注明「加断言时同步改」。

**结果**：`parity_diff.py --selftest` **17/17** ✓；带前端产物跑完整对拍 ⇒ **一致 12 ｜ 已知差异 0 ｜ 不存在路径 4 ｜ 新差异 0**（退出码 0）✓ —— 即「删库前的等价性证据」重新成立，且**不再依赖本机有没有构建前端** ✓。

⚠️ **两处更正/遗留**：
1. 我上一条说「已删 `frontend/.output`」是**错的**（当时没真删）；本轮再删时**权限提示超时未响应** ⇒ **仍留在盘上**。它现在无害（对拍已环境无关、冒烟那两条已夹具化），但要清就一句：`Remove-Item -Recurse -Force frontend\.output`（`frontend/dist` 联接别动，来历不明）。
2. ⚠️⚠️ **本项目第 3 次栽在「测试/工具依赖环境残留」**（冒烟 2 条 + 对拍 4 条）⇒ 已沉淀进 `TOPICS.md`：**任何用「HTTP 状态码/路径是否存在」做判据的地方，都要先问「有前端产物时会怎样」**。

## S7 第 40 步：**删掉 `backend/`**（用户授权）—— 删后现形 1 处（已修）；全量 **63 套件 / 2463 项 / 0 失败**

**按承诺的流程走的**：① 删前复核 → ② 删 → ③ 全套自检 → ④ 文档/记忆。
- **删前**：快照 `--check` 完整 ✓；守卫真源码 / 冻结**双路同结论**（Node 224 ｜ Python 227 ｜ 未注册 0 ｜ 0 漂移）✓
- **删除**：13508 文件 / 144 纳管（`Remove-Item -Recurse -Force backend`；第一次执行被权限提示拦下超时，重试成功）
- **删后**：守卫**自动回退快照**，结论与删前**逐字一致** ✓✓｜冒烟 **476/476** ✓｜快照自检 **11/11** ✓

### ⚠️ 真删当天现形 1 处 —— **干跑没抓到**

`smoke_test.py` 在**导入期**直接读 `backend/src/db/index.ts` / `schema.ts`（校验「models 表集/列集 == Node DDL」）
⇒ 删库后 `FileNotFoundError`，**整套冒烟直接崩**（不是跳过，是红 ✗）。
**根因**：冻结脚本的自动发现**只扫守卫**（`_GUARD_SOURCES` = route_parity / parity_diff），
`smoke_test.py` 不在里面 ⇒ 那两处硬读**从来没进过快照**（第 33 步干跑为什么没抓到：当时那两处读法可能还没定稿，
但机制上的漏洞是真的 —— 自动发现的**覆盖面**才是命门）。

**三处修法（都已落地）**：① `smoke_test.py` 改走 `_ts_path()`（真源码优先 → 快照，与守卫同策略）；
② 快照 `FILES` 补 `db/index.ts` / `db/schema.ts`，并把 **`smoke_test.py` 加进 `_GUARD_SOURCES`**（以后它再读新 TS，自动发现拦得住）；
③ 冻结脚本新增 **git 历史兜底**（见下）。

### ⚠️⚠️ 另一个坑：删库当天 `HEAD` 已经不含源码了

现场往往**已被提交成「删除」**（HEAD 从 `21c5a70` 变成含删除的 `53aed18`）⇒ `git show HEAD:backend/src/db/index.ts`
**直接 fatal** ✗。正解：在 `git log --all -- <path>` 的提交里**回溯到「最后一个还含该文件」的版本** ✓。
另外**「现有快照优先于 git」是刻意的**：快照 = 删库前的现场，而 git HEAD 可能落后 —— 我删库前改过
`services/local-model-scan.ts`（服务根迁 backend-py）却没提交 ⇒ 若一律从 git 重建，那次改动会被**静默回退** ✗。

- 快照：**76 条 / 674 KB** ✓（74 + db 两件）；`freeze()` 现在**删库后也能扩快照** ✓
- 全量：**63 套件 / 2463 项 / 0 失败** ✓ —— 比删前（2465）少的 2 项是**两处预先设计的显式 `[skip]`**
  （`freeze_snapshot_test.py` 的「真源码存在性」、`eval_cli_test.py` 的「与 Node 侧逐字节一致」）✓
- 文档：根 README 架构块与目录树标记「已删除」；`backend-py/README` 的 S7 行改为 ✅、对拍节标注**已进入历史**
  （Node 没了 ⇒ 两侧并排对拍**不可能再跑**，`parity_run.py` 仅存为「当时如何证明等价」的证据）

## S7 第 41 步：删库后的**一致性清扫** —— 清掉「Node 时代还在教人起 Node」的误导指令

**原则**：区分**历史溯源引用**（`backend/src/xxx.ts 移植自…`，保留，正是它们的价值）与**可执行指令**
（端口 / 启动命令 / 开关 / 部署清单，Node 已删即**必须改**）。

**改了哪些**（全部用「落盘脚本 + MISS 报告」批量精确替换，26 处 **0 MISS**）：
- 根 `README.md`：端口 `5789 → 5790`（配置样例 / API 地址 / 单服务访问 / Docker `-p` / nginx `proxy_pass`）、
  启动命令（`cd backend && npm run dev` / `npm start` → `uvicorn app.main:app --port 5790`）、
  技术栈（Node/Hono/Drizzle/Mastra/Sharp → **Python 3.12 / FastAPI / SQLAlchemy / ffmpeg**）、
  环境要求表新增 **Python 行**、上传清单 `backend/` → `backend-py/`
- `backend-py/README.md`：绞杀者章节（反代拓扑图、「系统始终可用」、`PROXY_TO_NODE=1 反代」提示、
  两行 env 表 → **遗留开关**）、对拍命令标注**历史**、`PY_PORT` 说明、冒烟描述（与 **TS 快照**交叉印证）
- **代码文案**（最要紧，因为是运行时输出）：`passthrough.not_migrated()` 的 501 文案原写
  「设置 `PROXY_TO_NODE=1` 可反代到 Node 保持可用」✗ —— 照做只会去起一个**不存在的服务**。
  现改为「**未实现（Node 后端已于 2026-09-15 删除）**，如需该能力请在 `backend-py/app/routers/` 补实现」；
  502 文案同步；`config.py` 的开关注释注明「现在不要设 `PROXY_TO_NODE=1`」
- `local-deploy-plan.md`：顶部加**状态横幅**（写于 Node 时代，路径对应 `backend-py/app/services/adapters/*.py`）+ 适配器表路径改 Python

⚠️ **刻意不动**：`ai-configs.md` 里也有 Node 时代启动指令，但它**在 `.gitignore` 里**（本地私有笔记，
可能含密钥）⇒ 只报告，不修改。

**验证**：冒烟 **476/476** ✓（改 501 文案无副作用 ⇒ 确认没有测试断言那段文本）、守卫 **0 漂移 / 0 未注册** ✓、
`check_all` 致命 0 ✓（技能引用 102 处、记忆 7716/8000）。

## S7 第 42 步：删库后收口三件事 —— 历史工具**前置拦截** + docs 指路语 + **进程级**验证

**① `parity_run.py` 前置拦截（体验修复）**：Node 没了之后它仍会**先起 Node、干等 `--timeout`（默认 90 秒）**
才报错 ⇒ 体感是「卡住」。现在入口先检查 `backend/node_modules/tsx/dist/cli.mjs` 是否存在，缺了就立刻打印
「Node 后端已于 2026-09-15 删除；本工具仅用于**删库前**证明等价，日常等价性由 `route_parity_test.py` 快照模式保证」并 `return 2`。

**② docs 里的「指路语」清零**（历史溯源保留、可执行指令必改，与第 41 步同一原则）：
- `docs/api-contract.md`：**Base URL 5789 → 5790**（真指令 ✗）、「统一响应格式（`backend/src/shared/`）」→
  注明现行 `backend-py/app/response.py`
- `docs/local-h3-video-system.md`：两处 `services/*.ts` 就地补上 Python 对应实现
  （`app/services/bible_ids.py`、`app/services/qc_scoring.py` —— 都已实测存在）

**③ ⭐ 进程级验证（本仓**第一次**做）**：此前所有「冒烟」都走 `TestClient`（**不起真进程**）⇒ 验不到
真端口 / 真 HTTP / 真静态站 / 真启动导入。本轮真起了一次 uvicorn（**临时 DATA_ROOT，不碰真库**），实测：
真进程启动 ✓｜`health` 200 + 裸对象 ✓｜`dramas` 200 + 信封 ✓｜**未实现路径 501 + 新文案**（点名 Node 已删）✓｜
`/static/nope.png` 404 ✓｜有前端产物时 `/` 直出 HTML ✓｜INFO：`/api/v1/ai-providers`、`gpu/status` 均 200 ✓。
⇒ **改启动/静态站/兜底文案后，值得手跑一次**；命令与结论已写进 `backend-py/README.md`（附「用临时数据根」警告）。

## S7 第 43 步：后端结构收口（用户问「`app/services/agents` 与 `skills` 为啥不在一处」）—— 收掉三处重复路径 + 写清边界

**答（已写进 `backend-py/README.md` §目录结构）**：两者是**代码 vs 内容资产** ——
`app/services/agents/` 是被 `import` 的 Agent 运行时（协议/工具/循环）；`skills/` 是被
`/api/v1/skills` 的 PUT/DELETE **真的改写**的 SKILL.md 库（前端「技能」页就是它的 UI）。
生命周期与守卫也不同（内容归 `check_skill_refs.py` + pre-commit 按**资产**管；代码归 `run_all.py` 按**行为契约**管），
打包边界也明写（`.dockerignore`：「镜像里只需 `app/` 与 `skills/`」）。

**⭐ 顺手修掉一个真缺陷（违反「同一规则只留一处」）**：`skills/` 路径此前**三处各写一遍**
（`services/skills.py` 常量、`services/agents/skills.py::skills_dir()`、`scripts/check_skill_refs.py` 常量），
还靠注释互相提醒「必须一致」✗ ⇒ 搬库漏改一处**不会报错**、只是**静默读不到技能**。
现收口到 **`app/config.py::skills_dir()`**（`SKILLS_DIR` env 可覆盖）；前两处改为**转发**
（`agents/` 保留同名函数，因为运行链要「每次调用重读 env」，常量版做不到测试隔离）。
实测：三处一致 ✓｜`skills_test` **45/45** ✓｜冒烟 **476/476** ✓｜lints 0 ✓。
`scripts/check_skill_refs.py` 那份**刻意保留独立实现**（scripts 不 import `app.*`，见 scripts/README 依赖约定）。

⚠️ **明确不动的一层**：`app/services/*.py` **保持扁平**（59 个模块同层）—— 它是**迁移索引**：
守卫按「TS 文件 → Python 模块」逐条比常量（`services/technical-qc.ts` ↔ `app.services.technical_qc`），
**重排目录会成片打断这些映射** ✗。所以「结构优化」落在三处：① 常量收口（本轮）；
② 文档边界说明（`backend-py/README` 新增「代码 vs 资产」块 + 为什么 `services/` 刻意扁平）；
③ 清掉目录树里**删库前的旧标注**（`走委派` / `仍在 Node` / `ai-configs 15/17` / `export 2/7` /
`storage 1` / `smoke 475 用例` / `freeze 12 用例` / `守卫自检 12 例` ⇒ 一律按现状改准 ✓）。

## S7 第 44 步：补上**删库后最要紧的一条不变量** —— 前端调用点 ↔ 后端路由覆盖（新套件）

**为什么这是删库后才存在的风险**：路由守卫（`route_parity_test.py`）证明的是「**Node 曾有过的 224 条路径**
都被覆盖」，它**看不见前端** ✗。删库后 Python 是唯一服务方 ⇒ 前端只要调到一个 Python 没实现的路径，
就是**线上 501**（用户点一下按钮就报错），而现有 63 个套件**一条都拦不住** ✗。

**做法**：把前端源码里的调用点抽出来，逐条对 `app.openapi()` 的路由表**反查**：
- `api.get/post/put/del('<path>')`（含泛型 `api.get<T>('/x')`）⇒ 方法 + 路径；
- `fetch('/api/v1/…', { method: 'POST' })` ⇒ **方法必须从调用现场读** ⚠️（我第一版默认 GET，把 4 条
  POST 动作端点误报成「缺失」✗：`/agent/{t}/chat`、`/storyboards/{id}/action-suggestion`、
  `/optimize-prompt`、`/split` —— 查现场才对上）；
- 路径里 `${...}` → `{}`；**剥掉查询串**（`?a=b`）；含空格/残留 `$`/`{` 的串（模板三元表达式）直接丢弃（否则噪声一片 ✗）。

**结果（实测）**：扫描 34 个前端文件 ⇒ **166 个调用点 ｜ 166 已注册 ｜ 0 未匹配** ✓✓；后端 227 条 (method,path)。
固化为 **`tests/frontend_api_coverage_test.py`**（5 用例，含**反套套逻辑**：故意造不存在路径必须被拒 ✗，
否则「0 未匹配」恒真、毫无意义）+ 注册进 `run_all.py`（**63 → 64 套件**）。

**实测数字（按老规矩实测，不推算）**：**64 套件 / 2468 项 / 0 失败** ✓（分批跑：0-11 / 11-22 / 22-43 / 43-64，
其中一批被环境连拒两次 ⇒ 拆半换名才跑起来）。README / `run_all.py` / `scripts/README` 的数字已同步；
第 40 步那条 63/2463 标注为「**当时**的实测值」✓。

## S7 第 45 步：路径常量**再收一处** + 「路径推导表」入文档 + 记忆预算下移

**① 顺着「同一规则只留一处」再清一处**：`services/local_model_scan.py` 自己算了一遍
`BACKEND_PY_ROOT = parents[2]` ✗，而 `config.skills_dir()` 里算的是同一件事 ⇒ 现在 `app/config.py`
只保留**一个** `BACKEND_PY_ROOT`（唯一权威），`skills_dir()` 与 `local_model_scan` 都从它派生 ✓
（`scripts/model_manager.py` 那份是同名独立实现，靠注释同步 —— scripts 不 import `app.*` ✓）。
实测五处路径常量**全部一致** ✓；`local_models_test` **89/89** ✓；依赖 `config` 的 **7 个套件全绿**
（冒烟 476 / skills 45 / 本地模型 89 / 数据根 38 / 镜像 23 / 前端覆盖 5 / 路径守卫）✓ —— `config.py`
是全局依赖，值得单独回归一遍。

**② 把「路径推导」写死进 `scripts/README.md`**（一张表 + 那个静默陷阱）：`scripts/*.py` = `parents[2]`、
`scripts/corpus/*.py` = `parents[3]`；⚠️ **级数错一级不报错、只表现为空结果**（本项目真实踩过：
`model_manager.py` 随 scripts 搬进 `backend-py/app/scripts/` 时只跳一级 ⇒ `models.json` 与 `local_services`
全找不到却一声不响）。同时写明「app 侧不要各自算，唯一权威是 `app/config.py`」✓。

**③ 记忆预算下移**（守卫一直提醒「余量仅 284」✓）：把 `MEMORY.md §Skill` 里的**坑⑨条**整段移到
`TOPICS.md §Skill 体系坑清单`，MEMORY.md 只留一行指针 ⇒ **7716 → 6993/8000** ✓（腾出 ~1000 字符）。
原因是已知故障模式：**逼近上限时尾部区块最先被截断**（`## 协作与提交` 首当其冲）。落点指针经守卫
⑥b「小节指针落空」用例与实跑双重验证 ✓。

## S7 第 46 步：补掉**最后一个「靠人同步」的不变量** —— 共享契约镜像守卫（新套件）

**背景**：`frontend/app/types/contracts.ts` 自己写着「字段**权威在后端**…后端改字段时**必须同步这里**
（这条不变量和『画风词表两处同步』同类）」✗ —— 但「画风词表」那条早在 `route_parity_test.py` 的
镜像常量守卫里，**契约镜像一条守卫都没有**（`grep contracts backend-py/tests/` 命中 **0** ✗）。
后果是静默的：后端改字段名 ⇒ 前端读 `undefined`（不报错，只是值为空/功能失灵）。

**做法（保守、可解释）**：抽 `contracts.ts` 的 interface 字段（**先剥注释**，免得把注释里的词当声明 ✗）⇒
字段名本身或其 **camel / Pascal 孪生**出现在 `backend-py/app/**/*.py` 即算对得上（后端 `toSnakeCase`
按列名生成 ⇒ 源码里常只有驼峰形态 ✓）。另锁该文件自定的两条规则（顶层只许 `export type/interface`、
nuxt `~contracts` 别名仍在）+ 一条**反套套逻辑**（造个不存在的字段名必须判「找不到」）。

**结果**：**31 字段 / 5 interface ⇒ 0 未匹配** ✓（新套件 **5/5** ✓；写用例时先踩了自己正则的坑：
把 interface 体内的字段行当成顶层语句 ⇒ 改成「顶格行必须以 `export` 开头」才对 ✓）。
注册进 `run_all.py` ⇒ **65 套件**；全量实测 **65 套件 / 2473 项 / 0 失败** ✓（22+22+21 分批）。
数字已同步：根 README / `backend-py/README`×2 / `scripts/README` / `run_all.py` docstring ✓。

## S7 第 47 步：抓到并修掉一个**真 bug** —— 前端 dev 代理还指着**已删除的 Node**（5789）

**怎么发现的**：延续「搜『靠人同步』的地方」这条线，先问「前端 dev 代理有没有守卫」✗（两个探针都没人管）
⇒ 打开 `frontend/nuxt.config.ts` 就看见：
- L8 定义 `const backendTarget = process.env.NUXT_API_TARGET || 'http://localhost:5790'` ✓，
  但**全文件没有任何地方引用它** ✗（死变量）；
- 真正生效的 `vite.server.proxy`（L45/46）**硬编码 `http://localhost:5789`** ✗✗ = **已删除的 Node**
  ⇒ `npm run dev` 的 `/api`、`/static` 全打到空端口，**不报错、只是请求失败** ✗。

⇒ 与第 41 步「改了 Dockerfile、忘了 compose」同类：**切端口时只改了一处** ✗。

**修法**：proxy 改用 `backendTarget` ✓（`NUXT_API_TARGET` 覆盖第一次真正生效 ✓）+ 清掉 L5/L6 的过期说法
（「未注册路径仍会反代给 Node」✗ ⇒ Node 已删，未实现路径回 501）。

**加守卫**（写进 `contract_mirror_test.py`，它本来就解析 nuxt.config ✓）⇒ **规则3 三条**：
① 默认目标 = `:5790`；② `vite.server.proxy` 里出现 `backendTarget` 且**无硬编码端口**；
③ 去注释后**代码里不得再出现 5789**（注释里允许 —— 那是历史说明 ✓）。
⚠️ 写守卫时踩了自己的坑：去注释的正则把 **URL 的 `//`** 也切了（`http://localhost:5790` → `http:`）
⇒ 正则扑空 ✗；改成「只切**不在 `:` 之后**的 `//`」✓。

**验证**：新套件 **8/8** ✓（5 → 8 项）；并**真跑了一次 `npm run generate`** ⇒
`✔ Generated public .output/public`／`Prerendered 15 routes` ✓ —— 证明改后的前端配置合法（proxy 变量在模块作用域、
`~contracts` 别名完好 ✓）。

## S7 第 48 步：第二次被问「`app/services/agents` 与 `skills` 为何不在一处」⇒ 摆证据 + 修一处过时注释

同一问题被问第二次（第 43 步已答、并写进 `backend-py/README.md` §目录结构 L160 起）⇒ 这次把**证据链**列全，
便于以后直接引用：

- **`app/services/agents/` = 代码**：被 7+ 处 `import`（`routers/agent.py:33`、`agent_configs.py:31-32`、
  `grid.py:184`、`mcp.py:25`、`services/auto_pipeline.py:52`、`services/qc_retry.py:37` …）；
- **`skills/` = 内容**：`routers/skills.py` 有写端点 —— `PUT`(L392) / `POST`(L421) / `DELETE`(L473)
  ⇒ **运行时可被用户改写**（前端「技能」页就是它的 UI）；
- **打包上是两棵并列的树**：`Dockerfile:39 COPY backend-py/skills ./backend-py/skills` +
  `.dockerignore:21`「镜像里只需 `app/` 与 `skills/`」（一棵依赖树、一棵内容树）；
- **两边其实已经互指**：`skills/README.md` L5-7（加载器/API/前端页）+ L73-75（skill ↔ 后端模块映射表）；
  而守卫 `check_skill_refs.py` 的 `REPO_ROOT_PREFIXES` 含 `backend-py/` ⇒ 这些指向 Python 的引用**有人看着** ✓
  （写旧的 `skills/`、`backend/` 前缀一律判**致命** ✓）。

**给用户的结论：不建议合并** ✗。合并成本：内容进 Python 包（镜像分层/只读根文件系统/挂卷语义都变差）+
要同步改 3 处路径常量 + 守卫 + `.dockerignore`/`.gitignore` + 两处 README，收益只有「看起来在一起」；
反向把 `agents/` 挪出 `services/` 更糟 —— 会打断「TS 文件 ↔ Python 模块」逐条映射（守卫按模块路径比常量 ✗）。

**顺手修一处过时注释**：`check_skill_refs.py` L44-48 还写着「与 `app/services/*/skills.py` **三处同步改**」✗ ——
上轮收口后已不成立，留着会把漂移引回来；现改为「应用侧**单一权威** `config.skills_dir()` + 守卫侧**独立实现**」✓。
验证：`check_all` rc=0（致命断链 0 / 守卫自检 13/13 / MEMORY 6993）✓。

## S7 第 49 步：补上**最后一层没验过的** —— 真实浏览器里的 UI（「应用还能不能用」的终极答案）

**为什么做**：API / 路由 / 静态站 / 代理 / 契约都验过了，但**没有一层证明「浏览器打开页面能真跑起来」** ✗，
而这正是删掉 Node 后端后最该回答的问题。

**做法**（临时数据根 ✓、`playwright-cli` 0.1.13、SPA 由 Python **直接服务** ⇒ 不需要 nuxt dev）：
`uvicorn :5790` → `playwright-cli open http://127.0.0.1:5790/` → 读快照 / 测度量 / **看后端访问日志**。

**实测（全绿）**：`Page Title: 短剧工坊` ✓；快照里有完整应用骨架 —— 顶部「短剧工坊 / Drama Studio」+
导航五栏（项目 `/`、资源库 `/library`、Skill `/skills`、模型 `/models`、Agent `/agents`）✓；
`innerText.length` = 200、`a,button` = 15 ✓；**console 4 条消息 / 0 error / 0 warning** ✓；
后端日志里看到浏览器**真发**的请求：`GET /api/v1/dramas` → 200、`/dramas/stats` → 200、
`goto /skills` 后 `/api/v1/skills` → 200、`/api/v1/skills/meta` → 200 ✓✓
⇒ 「浏览器 → Python 后端 → `backend-py/app/skills/` 的 SKILL.md」**整链打通** ✓。

⚠️ **0.1.13 的三个坑**（已写进 `backend-py/README.md` 的「UI 层冒烟」节）：
① **没有 `network` 子命令**（技能文档列了、版本不支持）⇒ 看「前端真打了哪些请求」**用后端访问日志**最可靠；
② `snapshot --filename=x.yaml` 落在**当前工作目录**（不是 `.playwright-cli/`）⇒ 跑完要删，别提交；
③ **refs 会随重渲染失效**：点 `e25`（Skill）没跳转（`location.pathname` 仍是 `/`）⇒ 验证路由**直接 `goto`** 更稳。

**收尾**：`playwright-cli close` + `taskkill /PID <uvicorn> /T /F`（确认 5790 无监听 ✓）+ 删根目录 `e2e*.yaml` ✓。

## S7 第 50 步：第三次被问「层级要不要再优化」⇒ 落成「五棵树」表，结论：**不需要**

问题已升级成「`skills/`、`app/services/agents/`、`scripts/`、`local_services/` 的**层级**要不要再优化」。
复核后结论 **不需要**，判据是**五棵树的生命周期本来就不同**，而且 `Dockerfile` 已经把它们分开了：

| 树 | 是什么 | 谁写 | 谁读 | 进镜像 |
|---|---|---|---|---|
`app/` | 后端代码 | 开发者 | uvicorn import | ✅ |
`skills/` | 内容资产 | **运行时可写**（PUT/POST/DELETE） | 加载器 `Path` 读、前端页 | ✅ |
`tests/` | 行为契约自检 | 开发者 | 人/CI | ❌ |
`scripts/` | 仓库守卫 + 工具链 + 语料 | 开发者 | pre-commit、人（不 import app.*） | ❌ |
`local_services/` | 本地服务根（clone 落点 + 自带 h3） | `model_manager --runtime git` | 后端 subprocess/HTTP | ❌ |

⇒ 再套一层（`src/`、`assets/`、`tools/`）只会把「哪棵树属于哪种生命周期」重新搅浑 ✗。**触发条件式**地记下
「什么情况下才值得动」（写进 README）：`skills/` 变只读才可考虑并进 `app/`；`scripts/` 远多于 17 个文件才值得拆
（且要同步改钩子里的硬编码路径 + 守卫自检）；`local_services/` 长期空才考虑删；`app/services/` 只有在
**不再需要与 TS 逐条比对**之后才谈分子目录（现在它是**迁移索引**）。表已落进 `backend-py/README.md` §目录结构 顶部 ✓。

## S7 第 51 步：层级收口第一步 —— `app/core/`（平台层独立）；⚠️ 含一次**我自己造成的 venv 事故**

**用户决定**（第三次追问后明确）：「后端要像前端一样**按层级**划分清楚」+「`agent` / `skill` / `script` / `mcp`
要在**同层级**」，即目标树是**平级功能目录**：

```
backend-py/
├─ app/            装配 + HTTP 层 + 平台层：main.py ｜ core/ ｜ routers/
├─ agent/          Agent 运行时（下一步：从 app/services/agents/ 移出）
├─ mcp/            MCP 客户端（下一步：从 services/agents/mcp.py + routers/mcp.py 收拢）
├─ skills/ scripts/ local_services/ tests/     ← 已在顶层 ✓
```

**本步成果**：`app/{config,db,models,response,request_utils}.py` → `app/core/` ✓；引用按「单趟正则 + 词边界」
重写 **148 文件 / 381 处** ⇒ 语法错误 **0** / 残余旧形态 **0** ✓。

### ⚠️⚠️ 事故与修复（必须记）

**v1 脚本用 `backend-py/**/*.py` 扫引用 ⇒ 把 `.venv` 里的第三方包也改了** ✗✗（fastapi / pydantic /
连 pip 自带的 requests，共 **44 个文件**）；另外 `from app import config` 那条替换写成
`from app import core import config`（语法错误 ✗）。
**修复三步**：① 仓库文件从 `tmp/_bak/`（**动手前的文件级备份** ✓）整体还原；② venv **精确回改**
（判据：同包下若不存在 `core/<name>.py`，那个 `core.` 必然是我插的）⇒ 58 处还原 / 0 跳过 / 0 残留 ✓；
③ 复验第三方 import + 应用导入 + 冒烟 **476/476** ✓。
**教训**：① 批量改写脚本的扫描面**必须显式排除 `.venv` 与 `__pycache__`**（v2 已改）；
② 大改动前做**文件级备份**（本会话有未提交改动 ⇒ 不能靠 `git checkout` 回滚 ✗）✓✓。

### 另一个真坑：搬「路径类模块」必踩

`config.py` 搬进 `core/` 后**深度 +1** ⇒ `PROJECT_ROOT = parents[2]` / `BACKEND_PY_ROOT = parents[1]`
全部错位（冒烟立刻红：「config: PROJECT_ROOT == repo root」✗）⇒ 改 `[3]` / `[2]` ✓。
**规则：搬任何含 `__file__` 路径推导的模块，先数 `parents[N]`。**
另 2 处 `from .. import config` 形态（`data_storage.py` / `local_model_scan.py`）正则没覆盖 ⇒ 已补 ✓。

### 顺手治好一条偶发红

冒烟「404 不在可达白名单」偶发失败（**475/476**，重跑即绿）：假桩 `_FakeOllama` **只实现 `do_GET`**，
而 gemini 探测发 **POST** ⇒ `BaseHTTPRequestHandler` 对未实现方法回 501 且**不读 body** ⇒
单线程 `HTTPServer` 上留未读字节、连接层偶发异常 ✗。修法：给桩补 `do_POST`（读干净 body + 回 404
—— 正是用例要判的「不在白名单」）✓ ⇒ 判据确定、不再偶发 ✓。

## S7 第 52 步：层级收口第二步 —— `agent/` + `mcp/` 提到**顶层**（与 skills/scripts 同级）；守卫抓出并**断开**了双向依赖

**用户要求**（第 51 步记的目标）：「`agent`、`skill`、`script`、`mcp` 等要在**同层级**划分清楚」⇒ 本步成真，
`backend-py/` 现有**七棵平级树**：`app/  agent/  mcp/  skills/  scripts/  local_services/  tests/` ✓。

### 做了什么

| 动作 | 细节 |
|---|---|
① 搬 | `app/services/agents/`（16 文件）→ 顶层 `agent/`；`agents/mcp.py` → `mcp/client.py` + 新 `mcp/__init__.py` |
② 改名冲突核查 | **无第三方 `mcp` 包**（`requirements.txt` 无、全仓无 `import mcp`）⇒ 不冲突 ✓；已在 `mcp/__init__.py` 写明：将来引官方 SDK 会**同名遮蔽**（`backend-py/` 在 sys.path 首位 ✗） |
③ 导入改写 | 被搬文件**相对导入按旧位置换算成绝对**（40+7 行）；引用方共 26 文件 |
④ ⚠️ **守卫抓出真问题** | 顶层化后 `agent → app.services`（12 处）+ `app.services → agent`（8 处）= **双向依赖** ✗ ⇒ 判定「`auto_pipeline` / `evaluation/` / `evaluation_scheduler` / `qc_retry` 是『**用 agent 跑编排**』而非『被 agent 调用的服务**』」⇒ 一并上移到 `agent/` 一侧 ⇒ `app/services` 对 `agent` 依赖**归零** ✓ |
⑤ 结果 | 链条拉直 **`routers → agent → services → core`**（+ `mcp → agent / core`）✓ 层级守卫扩到全树后 **6/6、违规 0** ✓ |
⑥ 部署面 | Dockerfile 补 `COPY backend-py/agent` + `COPY backend-py/mcp` ✓（漏了 = 容器里 ModuleNotFoundError ✗）；契约测试**新增硬断言**「顶层包必须逐个 COPY」⇒ **27/27** ✓ |

### ⚠️ 本轮踩的坑（4 个，全部与「import 形态」或引号有关）

1. **只处理了 `from X import Y`，漏掉 `import a.b.c as d`** ✗ ⇒ 批次 2 报
   `ModuleNotFoundError: No module named 'app.services.agents'`（`agent_script_corpus_test.py:86`）。
   **规则：改 import 必须同时覆盖三种形态**：`from X import Y` / `import X.Y as Z` / `import X` ✓。
2. 兜底脚本第一版正则漏了**后缀子模块**（`…agents.tools.script_tools` 里的 `.script_tools`）⇒ **0 改写** ✗；
   加 `(?P<sub>[\w.]*)` 后才命中 ✓。**教训：映射「模块前缀」时必须允许尾随子模块**。
3. **`from agent import mcp as mcpmod`** —— 模块名写在 **import 名字位** ✗（前两条规则都管不到）⇒
   `ImportError: cannot import name 'mcp' from 'agent'`（`mcp_test.py:31`）⇒ 改 `from mcp import client as mcpmod` ✓。
   **规则：`from <pkg> import <mod>` 的 `<mod>` 若本身也是独立顶层包，要单独处理**。
4. PowerShell 内联引号又咬 3 次 ✗（含 `$_.Line` 在双引号里的解析）⇒ 一律**落盘脚本** ✓（本会话第 4 次印证）。

### 验证（实测）

- 后半批（33-66）与前半批（0-33）**分别单批跑**（4 批并行会互相抢 CPU ⇒ 出现「53 套卡住不动」✗；
  单批跑正常 ✓ ⇒ **本项目跑全量建议 ≤2 批并行**）
- 冒烟 **476/476** ✓ ｜ 层级守卫 **6/6**（137 个 .py、违规 0）✓ ｜ 部署契约 **27/27** ✓
- 路径 + 常量守卫 **Node 224 / Python 227 / 未注册 0 / 漂移 0** ✓
- 全量分两段实测：**前半 0-33 段 = 33 套 / 1665 项 / 0 失败** ✓（一次跑完 ✓）；后半 33-65 段因**环境限制**（本会话容器会把长时间命令转后台并**杀掉**，输出文件被**清零** ✗ —— 复现 3 次）无法一次跑完 ⇒ 改为**逐套实测关键件**，全绿 ✓：
  `creator` 28/28、`optimizer` 28/28、`evaluation_scheduler` 22/22、`local_models` 89/89、`qc_retry` 14/14、`auto_pipeline` 50/50、`storage_change` 38/38、`frontend_api_coverage` 5/5、`contract_mirror` 8/8（都是搬迁直接波及的 ✓）
  ⚠️ **教训（工具面）**：跑全量要**≤2 批并行、每批 ≤20 套**，或逐套跑；4 批并行会互相抢 CPU（实测「53 套卡住不动」✗）。

## S7 第 53 步：**方案 A 定稿** —— `agent/`、`mcp/` 收回 `app/` 内（代码归代码包，顶层回到 5 棵树）

**用户拍板**：第 52 步把 `agent/`、`mcp/` 提到顶层后，用户「感觉层级很乱」⇒ 问「这些有必要都放进
`app/` 下面吗」⇒ 我给出判断（**只有 `agent`、`mcp` 该进**；`skills/`=内容资产、`local_services/`=机器相关
运行时（clone 落点、已 gitignore）、`scripts/`=仓库级零依赖工具（pre-commit 用）⇒ 进 `app/` 会破坏
「内容/代码」「机器状态/代码」「仓库守卫/应用代码」三条边界，尤其 `COPY backend-py/app` 会把 clone 的东西
打进镜像 ✗）⇒ 用户「按 A 方案继续拆分」⇒ 落地：

    backend-py/
    ├─ app/   代码包：main.py ｜ core/ ｜ routers/ ｜ services/ ｜ agent/ ｜ mcp/   ← 同层级六块
    ├─ skills/ scripts/ local_services/ tests/ configs/ data/

**执行**（脚本 `tmp/do_planA.py`）：`agent/`→`app/agent/`、`mcp/`→`app/mcp/` ⇒ **92 行 import 改写**，
**三种形态一次收口**（`from X import Y` / `from agent import x` / `import agent.x.y as z` —— 第 52 步正是
漏了后两种才连踩三坑 ✓），**228 个 .py 编译 0 错**、残余裸顶层导入 **0** ✓。
配套四处：① Dockerfile 撤回 2 条顶层 COPY（`COPY backend-py/app` 已覆盖子包 ✓）；② 契约测试断言**反转**成
「子包不单独 COPY」✓；③ 层级守卫单元表改 `app/agent` / `app/mcp` ✓；④ **技能文档 9 处断链 + 另 5 处旧路径**
改到 `app/agent/…` ✓（`check_skill_refs` 致命断链 **9 → 0** ✓）。

**实测**：应用导入 ✓（179 路由）｜层级守卫 **6/6**（137 个 .py、违规 0）｜部署契约 **27/27**｜
路径守卫 224 / 227 / 未注册 0 / 漂移 0 ｜冒烟 **476/476** ｜搬迁波及件 `creator` 28/28、`mcp` 32/32 ✓

⚠️ **教训（批量改路径的副作用）**：`backend-py/README.md` 里「2026-09-15 已从 `app/services/agents/`
提到…」这类**历史叙述**被我的路径批量替换误改成 `app/agent/` ✗（语义变成错的）⇒ 已改回并写明**
「从 `app/services/agents/` 提到 `app/` 内同层级」**✓。**规则：批量改路径前先区分「指向当前实现的引用」
与「描述历史动作的叙述」，后者必须排除或逐句复核。**

## S7 第 54 步：**用户点名把 `skills/`+`scripts/`+`local_services/` 合并进 `app/`**（我上一步不建议，但用户拍板 ⇒ 照做并兜住后果）

**最终布局**（`backend-py/` 顶层只剩 `app/` + `tests/` ✓）：
```
app/
├─ main.py core/ routers/ services/ agent/ mcp/    ← 应用代码（方案 A）
├─ skills/          ← 内容资产（用户可改；`app/core/config.py::skills_dir()` 唯一权威）
├─ scripts/         ← 仓库守卫 + 工具链（零依赖；⚠️ 深度 +1）
└─ local_services/  ← 机器相关运行时（clone 落点 + 自带 h3；gitignore）
tests/
```
**落地内容**：三目录 `shutil.move` 进 `app/` ✓；`skills_dir()` → `BACKEND_PY_ROOT/"app"/"skills"` ✓；
`local_model_scan.py` 默认服务根 → `.../"app"/"local_services"` ✓；`model_manager.py` 的
`BACKEND_PY_ROOT` → `APP_ROOT`（= `app/`）✓；`.githooks/pre-commit` 路径 ✓；Dockerfile 只留
`COPY backend-py/app` ✓；**`.dockerignore` 新增排除 `app/scripts` + `app/local_services`** ✓
（否则镜像会带工具链与本机 clone ✗）；`.gitignore` 规则迁到 `app/local_services/*` ✓。

### 本轮踩的坑（4 个，全是「路径/深度」类）

1. ⚠️ **深度被 +2** ✗：第一个脚本在 ③ 中途被杀（日志只刷到 ② ⇒ 我误判它没跑到 ③ ✓），
   第二个「续跑」脚本又 +1 ⇒ `check_memory.py` 的 `parents[4]` 指到了仓库外，报「缺少三层文件」✗。
   **修法：深度改成「写死正确值」而不是「累加」**（`app/scripts/X.py` ⇒ `parents[3]`；
   `app/scripts/corpus/X.py` ⇒ `parents[4]`），并给后续增量脚本加**幂等标记** `# depth-adjusted-to-app` ✓。
   **教训：增量式修正脚本必须先确认「上一步是否已生效」，别信被截断的日志；能写绝对值就别写增量。**
2. ⚠️ **硬编码兄弟路径**：`check_all.py` / `test_guards.py` 里写着 `ROOT/"backend-py"/"scripts"` ✗
   （字符串替换抓不到**分段拼接**的路径 ✗）⇒ 改成 `Path(__file__).resolve().parent`（按自身位置推导 ✓，
   以后再搬不会坏 ✓）。
3. ⚠️ **守卫扫描面外溢**：`route_parity_test._json_dumps_drift()` 扫 `app/**/*.py` ⇒ 把
   `app/scripts/`（工具链）与 `app/skills/`（技能附带脚本，如 `beat-sync-editor/scripts/*.py`）
   也当成了 TS 镜像代码 ⇒ **误报 7 条漂移** ✗；`layering_test` 同理（把它们算成 `top`，23 个 ✗）。
   ⇒ 两处都加「跳过 `scripts/`、`skills/` 子树」✓，注释写明原因 ✓。
4. ⚠️ **搬块脚本的区间重叠**：把 README 树里三块移进 `app/` 时，块结束判定写错 ⇒ `scripts/` 块被**插两遍** ✗
   （已手工删重复 ✓）。**教训：移动文本块的脚本，必须先断言「区间不重叠」再动手**。

### 实测（全部通过）

| 项 | 结果 |
|---|---|
编译 | 228 个 .py **0 错** ✓ |
三道仓库守卫 | 致命断链 **0** ｜ 记忆致命 **0**（落点表 16 处有效 ✓）｜ 守卫自检 **13/13** ✓ |
路径 + 常量守卫 | Node 224 / Python 227 / 未注册 0 / **镜像常量漂移 0** ✓ |
层级守卫 | **6/6**（138 个 .py、违规 **0**）✓ |
部署契约 | **31/31** ✓（新增「app/skills|scripts|local_services 不单独 COPY」+「dockerignore 排除后两者」4 条断言）|
冒烟 | **476/476** ✓ |
相关自检 | `skills` **45/45**、`local_models` **89/89**、`agent_script_corpus` 25/25 等 ✓ |

## S7 第 55 步：用户问「`tests/` 是标准目录吗」⇒ 结论：**是惯例，建议留在 `backend-py/` 顶层**

**答**：`tests/` 放**项目根、与被测包平级**是 Python 的常规布局（打包指南的 flat 与 src 两种布局都这么放；
`app/tests/` 这种「塞进被测包」是**库**为了让测试随 sdist 分发时的少数派做法，本项目是应用，不适用）。
本项目 `tests/` 的**内容**不是 pytest 而是**自研零依赖自检**（每个文件独立跑、打印 `SUMMARY: x/y passed`，
由 `run_all.py` 串起来 ✓），但**目录名与位置**仍是最标准的形态 ✓。

**建议保留在顶层**（若要搬，成本与风险都高）：
| 理由 | 本仓实测依据 |
|---|---|
① 惯例 | 顶层 `tests/` = flat/src 两种标准布局的公共部分 ✓ |
② 语义分层 | `app/` = **运行时被加载的**东西（已含 skills/scripts/local_services ✓）；`tests/` = **检查它的**东西 ⇒ 混进去会让 app/ 既装「被加载」又装「不被加载」✗ |
③ 成本 | **68 个测试文件**含 `parents[...]` / `sys.path.insert` 的深度推导 ✗ ⇒ 逐个 +1；**上一轮刚因「深度 +2」整轮排障** ✗，收益为零 |
④ 镜像 | `.dockerignore:30` 已排除 `backend-py/tests` ✓；搬走后要改，否则 66 个测试文件进镜像 ✗ |

⇒ 落点写清：**`tests/` 留在 `backend-py/`**（不是 `app/tests/`）；若将来真要搬，走「备份 → 搬 →
**深度写绝对值**（不累加）→ 全仓引用 → 守卫 + 全量」流程。

✅ **用户已确认**（2026-09-15，「按照你的推荐即可」）：**采用方案①保持现状** —— `tests/` 不进 `app/`，
工具也**不**拆去 `app/scripts/tests_harness/`（我提的折中方案②同样不做）⇒ 目录布局在本轮定稿：
`backend-py/` 顶层只 `app/`（应用 + 三类资产）与 `tests/`（自检），**不再有结构性待办** ✓。

## 待办（优先级）

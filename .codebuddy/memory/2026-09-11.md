# 2026-09-11

- 【项目状态体检 + 修复 1 个编译阻断错误】用户要求「检查项目现在咋样了」→ 全量体检结果：
  - **修复**：`backend/src/routes/characters.ts:270` 残留死代码 `const merged = [...source, ...target]`（对象 spread 进数组，`merged` 全文从未使用）→ TS2488 ×2，导致后端**编译失败、无法启动**。删除该行后 `npx tsc --noEmit` EXIT=0。
  - 验证全绿：后端 tsc PASS；前端 VideoEditor.vue / episode 页 compiler-sfc parse=0 template=0 script=ok；useApi.ts transpile ok（临时 .verify-all.cjs 已删）。
  - 分支 `main` 与 `origin/main` 一致（09-03 记忆里「领先 9 commits 未 push」的状态已解除，期间已推送）。
  - 未提交存量：16 文件 +319/-32，2 未跟踪（`backend/src/shared/character-match.ts`、`.codebuddy/memory/2026-09-03.md`）。归为 4 个功能簇：
    1. 跨集一致性-角色：新 `shared/character-match.ts`（说话人别名归一：去称谓前后缀 + 双向包含，全剧唯一命中才采用，否则 null）→ 接入 `ffmpeg-compose.ts`（配音选音色）、`storyboards.ts`；`characters.ts` 新增 `POST /:id/merge`（资产回填 target 空字段 → episode/storyboard 绑定迁移去重 → imageGenerations/assetVersions 改挂 → source 软删）；前端 `useApi.ts` + episode 页合并 UI。
    2. 跨集一致性-场景：`scenes.ts` 生成场景图时自动注入同剧同 location 已有图（≤2 张）作 referenceImages。
    3. FL2VA 首尾帧链路修复：`shot-router.ts` / `auto-pipeline.ts` / `qc-retry.ts` / `videos.ts`。
    4. 设计尾帧(`last_frame_image`) vs 真实尾帧(`tail_frame_image`) 语义分离：`schema.ts` + `db/index.ts` 幂等 `ensureColumn` + `frame-extractor.ts` 改写 tail + `auto-pipeline.ts` tail-link 真实尾帧优先 + `qc-report.ts` / `export-service.ts`（`_tail` 导出）+ 前端取帧回退链。
  - 未提交，按用户 Git 规矩等待明确指令；提交时注意第 3/4 簇在 `auto-pipeline.ts`/`videos.ts`/episode 页内交织，可能需合并为单个 feat 提交。
  - 【补充复核】逐文件读完全部 diff 后的精确结论：
    - `videos.ts` 改动核心：`getStoryboardCharacterImageUrls` → `getStoryboardReferenceImages`（角色立绘+场景图+道具图，与 auto-pipeline 批量路径同源）；`referenceMode` 类型补 `'first_last'`；新增 `effectiveMode` 兜底（first_last 但缺首/尾帧时降级 single/none，避免坏请求）。
    - `shot-router.ts`：FL2VA 决策 `referenceMode` 由 `'single'` 改 `'first_last'`（这是 FL2VA 之前不生效的根因 —— adapter 只认 first_last 才派发首尾帧字段）。
    - `auto-pipeline.ts`：`prevTail = prev.tailFrameImage || prev.lastFrameImage`；FL2VA 时 `imageUrl=undefined` + `firstFrameUrl = prevTail || sb.firstFrameImage`。
    - `qc-retry.ts`：`isFl2vaRetry = !!finalSb.lastFrameImage`（只用设计尾帧判定）。
    - 前端 episode 页 6 个 hunk：450/492/3309/5338 属「角色合并」，4249/4342 属「尾帧优先」，可 `git add -p` 拆分。
    - 依赖核对：`confirm`（useConfirm）、`toast`（vue-sonner）、`characterAPI` 均已正确引入，无运行时未定义风险。
  - 【提交方案（已给出，待用户指令，未执行）】三提交：①feat 跨集角色归一+合并（character-match.ts / characters.ts / storyboards.ts / ffmpeg-compose.ts / useApi.ts + episode 页 hunk 1,2,3,6）②feat 场景图跨集参考（scenes.ts）③fix FL2VA 首尾帧路由+真实尾帧分离（schema/db/index/frame-extractor/shot-router/auto-pipeline/qc-retry/qc-report/export-service/videos + VideoEditor.vue + episode 页 hunk 4,5）。
  - 【已执行提交 2026-09-11】用户授权后按方案完成三个提交（英文 message）：
    - `e57f06b` feat(cross-episode): speaker name alias matching + character merge（6 files, +280/-4）
    - `1968200` feat(cross-episode): reuse same-location scene images as generation references（1 file, +22/-2）
    - `3f19b1e` fix(fl2va): dispatch first/last frame mode correctly and split real tail frame from designed last frame（11 files, +70/-26）
    - 提交后 `npx tsc --noEmit` EXIT=0；工作区干净，仅 `.codebuddy/memory/*.md` 未跟踪（未纳入提交）。**未 push**。
  - 【hunk 级拆分手法（可复用）】用临时 Python 脚本读 `git diff -- <path>` 的字节流，按 `@@ ` 前缀切分 hunk，拼上原文件头 + 选中 hunk 写出补丁，再 `git apply --cached <patch>` 精确入暂存区。因为未暂存时 index==HEAD，各 hunk 的 pre-image 行号依然有效，可安全只应用部分 hunk。**关键顺序**：先提交含前面 hunk 的提交，之后 `git diff` 会自动只剩剩余 hunk，无需二次拆补丁。脚本用完即删（本次为 `scripts/_tmp_split_hunks.py` + `.git/_hunks_a.patch`）。
  - 【git 身份已变化】本机 `user.name=yuanxf` / `user.email=yuanxf@wedoctor.com` 现已配置（无需再 `-c` 临时指定），远端 `git@github.com:yuanxiufei/darme-voide.git` 走 SSH。
  - 【代码审查结论 2026-09-11（三个提交审完，未修）】
    - 【高】`VideoEditor.vue` `refModeOptions` 含 `{value:'first'}` 且 `form.referenceMode` 默认 `'first'`（L271、L482）→ **非法枚举**，minimax adapter 三分支（single/first_last/multiple）都不认，等于不发任何参考帧；该组件还从不传 `image_url`，所以「单图参考」也拿不到首帧。
    - 【高】`videos.ts` 决策输入与请求输入不同源：`decideShotRoute` 用 DB 的 `sbForRoute.firstFrameImage/lastFrameImage`，但请求只用 `body.first_frame_url/image_url`；`effectiveMode`（L111-115）降级时判断用 `firstFrameUrl` 却只下发 `body.image_url`（L124），仅给 first_frame_url 时首帧被丢弃。
    - 【中】`scenes.ts` L108-116 空 location 时 `normLoc===''`，会命中所有 location 为空的其他场景 → 串图；`POST /scenes` 创建（L41-49）对 `location` 无校验可落空串。
    - 【中】`qc-retry.ts` L136 `isFl2vaRetry = !!lastFrameImage` 未判首帧，可能发出缺 `first_frame_image` 的 first_last 请求；videos.ts 已有守卫，两处不一致。
    - 【低】`auto-pipeline.ts` L451 注释过期（写 last_frame_image，实际 tail_frame_image）；merge 后 `asset_versions` 版本号可能重复（无唯一约束不报错，但 v1/v2 语义歧义）；merge 候选只认同名，覆盖不到「阿晚/林晚」别名重复；【既有】shot-router T2V 分支吞掉 prevTail 顺接。
    - 【已验证正确】tail_frame_image 读写闭环 + toSnakeCase 输出不丢字段；迁移幂等；auto-pipeline FL2VA 分支安全（shot-router 第3条 ⟹ 首尾帧俱在）；character-match「唯一命中」策略 + 去词缀有终止条件；merge 端点 ensureCostumeId 已 import、episode_characters/asset_versions 无唯一索引不撞约束；adapter `subject_reference` 有 slice(0,9) 保护；弹窗 8 个 class 样式齐全；refresh() 会重载 drama.characters，UI 不残留。
- 【已按审查结论完成修复 2026-09-11（用户「继续完善代码」授权，未提交）】7 处改动：
  1. **`videos.ts` 帧来源同源化（核心）**：新增 `sbRow`（storyboard 行提升，上下文注入与路由共用**同一次查询**，删掉第二处重复查询）；新增候选帧 `candFirstFrameUrl = body.first_frame_url || sb.firstFrameImage`、`candLastFrameUrl = body.last_frame_url || sb.lastFrameImage`，路由决策 `decideShotRoute` 改为吃候选帧（原来只吃 DB 帧）；模式解析改 `requestedMode`→`effectiveMode`，输出帧按最终模式取值：`outImageUrl`（single 时把首帧落到 image_url，修掉「只传 first_frame_url 时首帧被丢」）、`outFirstFrameUrl`、`outLastFrameUrl`。**关键设计**：`frameMode = single|first_last` 之外（multiple/none）不注入兜底帧，保持既有请求体不变（已核对 minimax/volcengine/vidu 只在 first_last 分支读 firstFrameUrl，ali 的 `img_url: imageUrl ?? firstFrameUrl` 是唯一例外）。
  2. **`VideoEditor.vue`**：`refModeOptions` 删掉非法值 `'first'`（adapter 三支都不认），收敛为 `''/single(首帧驱动)/first_last/multiple`；新增 `normalizeRefMode()` 把历史 `'first'→'single'`、`'last'→'first_last'`；默认值由 `'first'` 改空（空=交给后端按分镜资产决策）；`reference_mode: form.referenceMode || undefined`；仅 `multiple` 模式回传 `reference_image_urls`。
  3. **`shot-router.ts`**：无首帧时先消费 `prevTail` → 返回 `first_frame_to_video` + `single`（原来直接落 T2V/`none`，调用方传的顺接帧不会被 adapter 派发，尾帧顺接形同虚设）；同步改文件头决策优先级注释。
  4. **`qc-retry.ts`**：`isFl2vaRetry = !!lastFrameImage && !!firstFrameImage`（与 videos.ts 守卫对齐）。
  5. **`scenes.ts`**：`locationRefs` 加 `normLoc ? ... : []` 三元守卫，空地点不再匹配所有无地点场景（防串图）。
  6. **`characters.ts` merge**：`asset_versions` 改挂前按 `(mediaType, frameType)` 分组重编号——source 版本号接续到 target 该组 max 之后（升序遍历保内部先后），并入历史统一 `historical`；target 该组原本无 `current` 时把并入的最新一条补为 `current`（否则整组只剩 historical）。
  7. **episode 页**：新增前端版 `stripAliasAffixes`/`isLikelySameChar` 等价规则（`ALIAS_TITLE_SUFFIX`/`PREFIX` 与后端 `shared/character-match.ts` 同步维护，前后端无共享包）+ `aliasCandidates()`；`mergeCandidates = 精确同名 + 别名疑似`（同名在前）；模板改用 `pickerCandidates()`，别名候选加 `tag tag-warning`「疑似同人」标签 + confirm 提示。
  - 验证：`read_lints` 8 个文件 0 diagnostics；后端 `tsc --noEmit` 在视频/路由/流水线改动后 EXIT=0（characters.ts 的改动靠 lint 覆盖，用户把二次 tsc 转后台未取结果）。
- 【修复后回归验证 2026-09-11（全部通过，仍未提交）】
  - 后端 `npx tsc --noEmit` **EXIT=0**（已含 `characters.ts` 的 asset_versions 重编号逻辑）。
  - 前端 SFC 编译校验：临时 `frontend/.verify-sfc.cjs`（`@vue/compiler-sfc` 的 parse+compileScript+compileTemplate+bindingMetadata），`VideoEditor.vue` bindings=49 / episode 页 bindings=425，**BAD=0**，脚本用完已删。
  - **`shot-router` 改动连带影响排查**：全仓 `decideShotRoute` 仅 3 处调用 —— `shot-router.recomputeEpisodeRoutes`（不传 prevTail，行为不变）、`auto-pipeline`（L335）、`videos.ts`（L101）。
    - `auto-pipeline` 请求装配与新决策**同源**：`anchorImage = prevTail || sb.firstFrameImage`，`imageUrl = isFirstLast ? undefined : anchorImage`、`firstFrameUrl = isFirstLast ? (prevTail||firstFrame) : (prevTail ? firstFrame : undefined)`。故新增的「无首帧 + 有 prevTail → single」分支下它会发 `image_url=prevTail`（adapter single 分支正是读 imageUrl），不会产生坏请求。
    - 信号路径确认：图片阶段超时「部分完成不阻断」→ 确实可能出现「上一镜视频已完成有尾帧、本镜首帧图缺失」的形态；此时旧逻辑落 T2V+none 丢弃顺接帧，新逻辑走 I2V 顺接，是该改动的真实价值场景。
  - 前端非法枚举残留排查：`referenceMode` 全仓仅 `VideoEditor.vue`（已收敛）+ episode 页 L4394-4396（本来就是 `first_last/multiple/single` 合法值）；`VideoEditor.vue` 内**无** `referenceMode === 'first'` 之类的 UI 分支依赖（文件里的 `'first'` 全是 `uploadTarget: 'first'|'last'` 上传目标，与参考模式无关），改值无 UI 回归。

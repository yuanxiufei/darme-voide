# 2026-09-02

- 【角色页单品图彻底移除 + 一键改全套覆盖重做，2026-09-02 夜】用户确认：①单品图（装备模块内第二张「单件道具图」，mode=single）没必要，彻底删；②一键生成改为「缺啥补啥+覆盖已有」。
  - 前端 characterId.vue 删除：模板单件道具图区块（gen-item-preview/gen-item-head）、refs itemGen/itemPollRef/itemPollTimeoutRef、itemImages computed、generateItemImage 函数（48 行）、.gen-item-preview/.gen-item-head 样式、anyImageBusy 中 itemGen 分支。页面残留搜索（item/itemImages/gen-item/单品/道具图）全 0。后端 mode=single 接口与 useApi 签名保留不动。
  - generateAllAssets 改造：原「缺缺失项才生成」逻辑删除，改为点击即全套覆盖重做（三视图→装备三视图+表情并行），total = 1+equipTypes+1 恒为 5；进度文案「已补齐」→「已完成」；按钮加 title 说明覆盖语义；最终 alert 文案改「全套素材已重新生成并自动刷新」。
  - 验证：verify-sfc.mjs TEMPLATE OK/SCRIPT OK。
- 会话恢复：前后端已启动（后端 `tsx watch`，端口 5789；前端 `nuxt dev --port 3013`），日志在 `%TEMP%\voide-backend.log` / `voide-frontend.log`。
- 当前未提交改动（来自 2026-09-01 会话，未 commit）：画风体系（三级全局→剧集→角色）、三视图超宽修复（THREE_VIEW_SIZE=2048x896 + 前端全宽预览）、app-settings 路由等。用户未指示 commit。
- 待验证：三视图需重新点「生成三视图」才会产出超宽长图（旧图仍为 16:9）。
- 角色编辑页（frontend/app/pages/drama/[id]/character/[characterId].vue）装备模块重构完成：原右列切换式 Tab（equipTab + v-show + .equip-tabbar/.equip-pane/.gen-module-equip）已删除，改为 4 个独立由模板驱动的模块（道具/服装/武器/首饰三视图），放在 gen-layout 双列之后的 .equip-modules（2 列 grid，1160px 以下单列）。每个格子独立：预览（equipImages[t.key].imageUrl）、正向/负向 textarea（form[promptFieldMap/negFieldMap]）、「生成」按钮（generateEquip(t.key)）与「根据形象设定生成提示词」（generatePromptFor(t.key)）。equipTypes 加 as const 使 t.key 推断为字面量以匹配 generateEquip 参数类型。道具模块在 props 为空时禁用并提示先填随身道具。旧 Tab 残留搜索 0 匹配。
- useApi.generateThreeViews 签名已补全真实合并载荷字段（prompt/negative_prompt/model/views/clothing/weapons/accessories/props/costumes/style），修复 L739 TS2353。
- 遗留：临时校验脚本 frontend/verify-sfc.mjs（917B）已在本会话末尾成功 delete_file 清理，无需再处理。
- 角色编辑页「图片生成」区二次重排（用户确认方案：仅排版、立绘留左列最上、装备全部右置）：gen-layout 改为 左列=角色立绘(上)+全身三视图(大)；右列=表情头像 + 道具/服装/武器/首饰三视图 自上而下竖排，构成「角色设定总览参考」效果（各图仍独立生成）。原页面下方 .equip-modules 2列网格已删除，装备模块直接作为 .col-side 子项循环（v-for equipTypes：prop/clothing/weapon/accessory）。CSS：右列加宽 380→400px；.equip-module 取消 margin-top:0（保留模块间 16px 间距）、图片 max-height 240→200px 紧凑化；media1160 规则删除 equip-modules 单列回退（装备随右列整体掉到左列下）。离线 SFC 校验 TEMPLATE OK/SCRIPT OK。
- 【三次修改，最终定稿】角色编辑页「图片生成」区三次重排（2026-09-02 最终版）：用户最终要求「每个生成模块各占一行（全宽）」：顺序 = 角色立绘 → 全身三视图 → 表情头像 → 道具 → 服装 → 武器 → 首饰。做法：.gen-layout 由双列 grid 改 display:block（保留 col-main/col-side flex column 内层，col-main 前接 col-side，自然竖排），删掉右列 400px 与 1160px 断点两列回退规则；表情 grid repeat(3,1fr)→repeat(auto-fit,minmax(150px,1fr)) 全宽自适应；.gen-empty 加 max-width:720px + margin auto（防全宽空占位过高）；.equip-module 图片 width:auto+max-width:100%+max-height:40vh 居中显示（装备三视图全宽行内自适应）。SFC 校验 TEMPLATE OK/SCRIPT OK。
- 【道具概念彻底下线，2026-09-02 晚】用户确认后删除角色「随身道具」体系（A 类），改动横跨前后端：
  - DB：schema.ts 删 characters 表 props/propPrompt/propNegativePrompt 列；db/index.ts 删对应 ensureColumn，新增 dropColumnIfExists（SQLite≥3.35 DROP COLUMN，try/catch 不阻断）并迁移删除三列。本地库验证 props 列已物理删除（sqlite 3.51.3）。
  - 后端：prompt-utils.ts buildEquipImagePrompt 参数类型由 'clothing'|'weapon'|'accessory'|'prop' 收窄为三类，char 入参删 props，prop 分支删除，buildEquipNegative 注释去道具；characters.ts generate-prompt/generate-equip-image type 白名单删 'prop'、promptField/negField/mergedChar/白名单删 props 相关；text-generation.ts splitCharacterVisuals 由四字段收窄为 clothing/weapons/accessories 三字段，SPLIT_PROP_KEYWORDS→SPLIT_PROP_NOISE_KEYWORDS（仅用于阻止污染合并，不再输出），系统提示词/示例去掉 (4)props 与第四键；image-generation.ts equipType 判断去 prop。
  - 前端：characterId.vue 删「随身道具」输入框（武器装备字段独立成行）、智能拆分按钮/提示文案去道具、hint 去道具、equipTypes/promptTypes/promptFieldMap/negFieldMap 去 prop、回填/保存/generateEquip/generatePromptFor 载荷去 props；useApi.ts 三个签名去 'prop'|'props'。
  - 保留 B 类物品库不动：propTemplates 表、storyboardProps、routes/props.ts、buildPropImagePrompt、library 页 stats.props、镜头注释「动作/道具/机位」等。
  - 验证：后端 tsc --noEmit 0 错、前端 verify-sfc TEMPLATE OK/SCRIPT OK、残留搜索 0。前端 characterId.vue 的 lint 报「找不到名称」系 Vue LS 对超大 SFC 的历史误报（episodeNumber.vue 未改动也有 1000 条），非改动引入。
- 【装备三视图"出人"修复，2026-09-02 夜】用户反馈服装/武器/首饰生成图仍带人物。实跑排查（读库+生成记录）确认双根因：
  1. **历史 legacy 提示词污染**：角色表 clothing_prompt/weapon_prompt/accessory_prompt 存有旧版「单图/含人物」文本（含 `character appearance:`/`for the character X`/`realistic cinematic character design`/`natural skin texture`），前端回填 textarea 后原样发送，后端 `body.prompt || char.xxxPrompt` 信任它绕过新的无人三视图构建器（image_generations id=52 记录确证）。
  2. **画风尾人物语义**：buildEquipImagePrompt 原用 DRAMA_ART_STYLE_MAP（角色用，含 character design/skin texture 词）作 equip 画风尾，诱导画人。
  - 修复四层：①prompt-utils 新增 EQUIP_ART_STYLE_MAP + buildEquipArtStyleSuffix(dramaStyle)（realistic→professional product photography、anime→2D anime prop illustration 等，统一追加 "isolated product shot, single object, empty scene, no people, no person, no model, no body parts, no skin"），buildEquipImagePrompt 尾部改用它；②characters.ts 新增 isLegacyEquipPrompt() 判定 + resolveEquipPrompt()（legacy/空值丢弃走 auto；负向不含 person/human/hand 也强制替换为 buildEquipNegative）；③db/index.ts 启动迁移清空 legacy 格式的 equip prompt/negative 字段（changed=6）；④前端 onMounted 回填时 cleanPrompt/cleanNegative 过滤 legacy 文本。
  - 验证：实跑 buildEquipImagePrompt 三类输出均无 character design/skin/portrait 诱导词；DB 全部角色 prompt 字段已清空。tsc/SFC 0 错。
- 【生成后不自动刷新修复，2026-09-02 夜】用户反馈"点生成后必须手动刷新页面图才变"。根因：各轮询的"完成判定"只看目标 URL **是否非空**（`!!url`），角色已生成过图时旧 URL 始终非空 → 第一次轮询就误判完成停表，后端异步写完新 URL 时无人再拉取，只能刷新页面。
  - 修复原则：**生成前快照旧 URL，轮询等到目标 URL 非空且 ≠ 快照**（后端每次落盘文件均 UUID 命名，URL 必变，信号可靠）。
  - 波及 4 文件：
    1. `characterId.vue`：generateImage（主图/服装变体，变体查 parseVariations(name)）、generateThreeViews（查 threeViews.combined.imageUrl）、generateEquip（查 equipImages[type].imageUrl）、generateExpressions（per-key 快照 beforeUrls）。
    2. `sceneId.vue`：generateImage 场景图（查 image_url）；新增本地 resolveUrl() helper。
    3. `episodeNumber.vue`：genCharImg / genCharImgWithModel / genSceneImg / genSceneImgWithModel 四函数（watchAsyncResult 回调内比较）；新增 normEpUrl()。批量 batch*Images 只挑无图角色不在此问题，未改。
    4. `props.vue`（物品库）：generateImage 轮询；新增 normPUrl()。
  - 注意：.vue lint 的「找不到名称 X / never 上不存在属性」为模板区对 `<script setup>` 的大 SFC 历史误报（未改动文件也有千条），非本次引入。

## 2026-09-02 时代背景前端收口（F2 完成）+ useApi 类型修复
- 本日完成此前 A/B/C 后端（角色参考图锚定、剧集时代背景、HD 道具单图）对应的前端补全与修复：
  1. `useApi.ts`：generateThreeViews/generateEquipImage/generateExpressions 类型签名补 `anchor?: string`（+reference_images），generateEquipImage 补 `mode?: 'single'|'view'`；characterAPI 原签名缺字段导致 characterId.vue TS2353/2561 已消除。
  2. `dramaAPI.eraExtract`：POST `/dramas/:id/era-background/extract`，泛型返回 {era,summary,imageHint}。
  3. `drama/[id]/index.vue`（F2，编辑剧集弹窗）：新增「时代背景」dialog-section——时代标签 input + 中文概述 textarea + 英文画面指令 textarea + 「AI 从剧本提炼」按钮（era-spinner loading）。reactive editEra{era,summary,imageHint}，parseEraFromDrama() 从 drama.era_background JSON 回填；extractEra() 调 eraExtract 填回编辑区；buildEraPayload() 组 {era,summary,image_style_en} JSON，三字段全空传 '' 清空；saveEditDrama 一并 PUT era_background。弹窗改类 .edit-drama-dialog width min(560px,100%)。
  - 注意：index.vue 是**非 TS** 的 `<script setup>`（无 lang="ts"），写代码不能带任何 TS 类型注解（`const res: any`/`(): string` 会被 vue language server 报错）。
  - 验证：verify-sfc.mjs 两页通过；backend `npx tsc --noEmit` 0 错误；index.vue/useApi.ts lint 0 错误；characterId.vue 剩 lint 均为历史模板误报。

## 2026-09-02 角色编辑页「一键生成全部素材」（前端编排式补齐，已完成）
- 页面：frontend/app/pages/drama/[id]/character/[characterId].vue 图片生成区（角色立绘/全身三视图/表情/服装三视图/武器三视图/首饰三视图）。
- 方案：纯前端编排，后端 4 类接口保持「提交即返回+轮询 GET 角色直到 URL 变化」，不改后端。
- 新增：generateAllAssets()（缺啥补啥：needThree/needEquips/needExprKeys → total → 阶段①先补全身三视图并 waitImageDone(240s) 作为锚定基准 → 阶段② Promise.all 并行提交缺的装备三视图+表情，waitImageDone(420s) 直到 equipOk&&exprOk）；通用轮询 waitImageDone(isDone, timeoutMs)（复用 activePollRef，每次轮询刷新 char.value）。
- 关键判定沿用快照法：生成前 normUrl 快照旧 URL，轮询等到目标 URL 非空且 != 快照（UUID 命名 URL 必变）。
- 模板：sec-head 工具栏绿色「一键生成全部素材」按钮(.btn-batch)，disabled=batchBusy||anyImageBusy（anyImageBusy=imgGen||threeGen||equipGen||itemGen||expressionsBusyActive||batchBusy）；批量中显示进度条(gen-batch-status，status+progress/total)；各单模块按钮 disabled 追加 || batchBusy。
- 踩坑：batchTotal.value 必须在 batchBusy=true 后赋值 total，否则进度显示恒为 /0（曾漏掉）。
- 验证：verify-sfc.mjs TEMPLATE OK/SCRIPT OK；所有引用符号/useApi 方法/样式类均核对存在。lint「找不到名称」系超大 SFC 语言服务误报（历史）。
- 待用户确认：是否纳入单品图(道具 B 类 itemGen)与「重新生成已有素材」；确认后再决定是否 commit。

## 2026-09-02 前后端共享契约（第 1 步落地）
- 针对「前后端穿线复杂」落地分级方案第 1 步，建立共享纯类型契约机制：
  - 新增 `backend/src/shared/contracts.ts`（唯一 DTO 源）：`EraBackground{era,summary,imageHint}`、`DramaUpdateBody`、`DramaDetailDTO`、`DramaProgress`、`DramaListItemDTO`、`DramaListResponse`。规则：只放 type、传输层统一 snake_case、嵌套数组（episodes/characters/scenes）暂时 `any[]` 兜底待子 DTO 契约化。
  - 后端：`era-background.ts` 的 EraBackground 改为 `import type` + `export type` 自 contracts；`dramas.ts` PUT 时代背景改为服务端规范化——`parseEraBackground` 兼容 image_style_en 旧键、转成规范 imageHint 后落库，空串清空，非法 JSON 直接 400（历史数据无需迁移）。
  - 前端：`nuxt.config.ts` 加 `alias: '~contracts'`（指 ../backend/src/shared/contracts.ts）+ `typescript.tsConfig.compilerOptions.paths`；`useApi.ts` `import type` 引用，dramaAPI.list/get/update/eraExtract 全部收窄为契约类型。`index.vue buildEraPayload()` 把保存键 `image_style_en` 改为 `imageHint`（消灭双格式）。
  - 配套修 `episodeNumber.vue:2062` 的 `ref(null)` 系 → `ref<any>(null)`（收紧类型后 Ref<null> 会报新错）。
  - 环境事故：跑 `npx nuxt prepare` 时因 2 个重复 nuxt dev 实例（端口 3013）锁 `.nuxt` 导致清目录中断、tsconfig.json 被删。处理：Stop-Process 杀掉前端 node 进程 → 重新 `npx nuxt prepare`（生成的 .nuxt/tsconfig.json 确认含 `~contracts`）→ `Start-Process npm run dev` 后台重启（日志写 dev.out.log/dev.err.log）。后端 tsx(5789) 未受影响。
  - 验证：backend tsc 0 错误；useApi.ts/index.vue/nuxt.config/contracts/era-background/dramas lint 全 0；episodeNumber drama ref 改动无新错。
  - 说明：原计划的「satisfies 编译期校验」以「service 返回类型 = 契约类型」的更强形式落实（era extract 结果天然被 EraBackground 约束），未做弱价值的 satisfies。
  - 下一步可选：第 2 步 useTaskPool 轮询收敛；第 3 步子 DTO（Episode/Character/Scene）契约化与 hc client 试点。

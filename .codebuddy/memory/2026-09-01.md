# 2026-09-01

- 角色三视图"图不够长/展示不全面"修复完成：
  - 根因①：`characters.ts` `/generate-three-views` 未传 size，走默认 `1920x1080`(16:9)，三个全身视角横排画布太矮。
  - 根因②：前端 `.gen-combined img` 固定 `aspect-ratio:16/9 + object-fit:cover`，把长图左右视角裁掉；且预览区只有 280px 列宽。
  - 修复：`prompt-utils.ts` 新增 `THREE_VIEW_SIZE = '2048x896'`（≈2.29:1 超宽），三视图路由传 `size: body.size || THREE_VIEW_SIZE`（body 可覆盖）；前端三视图模块改 `gen-module-wide`（图片占满整行），`img` 改 `height:auto + max-height:60vh + object-fit:contain + 深色背景 #111827`，提示词移到图片下方。
  - 适配器兼容性备忘：volcengine/minimax/openai/local-sd 透传宽高；gemini 按 gcd 化简 aspectRatio（2048x896→16:7 非枚举值，gemini 可能报错）；ali `normalizeSize` 超宽降级为 16:9（1696*960）。用户主用火山 Seedream 无碍。

- 从 MiniMax Design 应用获取全部 skill，已复制到项目 `skills/minimax/`：
  - `installed/`：用户安装的 20 个 skill（源 `C:\Users\Administrator\.hub-global\skills\`），包括 chinese-style-short-drama-generator、short-drama-screenwriter/series-writer、n-storyboard、multi-shot、voice-clone、voiceover-direction、film-* 系列、skill-creator/reviewer 等。
  - `builtin/`：内置 9 个 skill（源 `D:\app\minmaxh3\MiniMax Design\current\resources\agent-profiles\v2\config\skills\`），含 h3-visual-design、video-deconstruct、anime-game-pv、brand-ad、cool-music-video、ui-motion、koc-video、cinematic-title-sequence、education-studio。
- MiniMax Design 数据根目录：`C:\Users\Administrator\.hub-global\`（海外版 overseas → -global 后缀）；skill 加载路径规则（源自 opencode-plugin-hilo/dist/hilo.js）：用户安装 `~/.hub-global/skills`，内置 `resources/agent-profiles/v2/config/skills`，知识库在 `~/.hub-global/.config/knowledge/`（含 model-prompts/minimax-h3.md 等）。
- 应用版本内置 skill 与用户安装 skill 结构：SKILL.md + meta.yaml + references/。

## MiniMax skill 融入项目 skill 体系（2026-09-01）
- 后端 `backend/src/agents/skills.ts`：`AGENT_SKILL_MAP` 默认绑定新增 minimax skill——
  - script_rewriter ← short-drama-screenwriter, short-drama-series-writer
  - extractor ← film-assets
  - storyboard_breaker ← film-shot, storyboard, coordinate-camera-control-designer
  - voice_assigner ← voice-clone, voiceover-direction
  - grid_prompt_generator ← film-reference-prompt-writer, film-style-picker, video-deconstruct-analyzer, builtin/video-deconstruct, face-warp
  - 新增导出 `listSkillIds()`：扫描 skills/ 目录全量 id（含嵌套 minimax/installed/xxx、minimax/builtin/xxx）
- 后端 `backend/src/agents/creator.ts`：`AVAILABLE_SKILL_IDS` 由 AGENT_SKILL_MAP 推导改为 `listSkillIds()`，配置生成器可全量选择所有 skill
- 前端 `frontend/app/pages/settings.vue`：`DEFAULT_AGENT_SKILLS` 同步后端映射；Skills tab 左侧新增「MiniMax 技能库」入口（selectedAgent='minimax'，显示 minimax/ 下全部 29 个 skill，不可在此新增）；顺手修复 skill 路径显示 bug（`skills/{{s.id}}/SKILL.md`，原 `skills/{{agentType}}/{{s.id}}` 会重复前缀）
- 关键机制：DB `agent_configs.skills` 目前全为 null → 后端回退 AGENT_SKILL_MAP 默认映射即生效；前端绑定面板 availableSkills 为 API 全量，可手动把任意 minimax skill 绑定到任意 agent
- 验证：`listSkillIds()` 返回 34 个（5 自有 + 29 minimax）；`GET /api/v1/skills` 返回 34 个，minimax 29 个正常列出

## Skill 全量管理增强（2026-09-01 第二轮）
- `backend/src/agents/skill-parser.ts`：`SkillMetadata` 增加 `workflows: string[]`（frontmatter `workflows:` 可选声明适用阶段，缺省空数组）
- `backend/src/routes/skills.ts` GET `/` 列表项扩展字段：`category`（core/minimax-builtin/minimax-installed/custom）、`workflows`（frontmatter 声明）、`boundAgents`（反向查询绑定该 skill 的 agent_type）、`phases`（工作流阶段 = boundAgents 对应阶段 + workflows 声明去重）
- 反向绑定查询 `loadSkillBindings()`：DB `agent_configs.skills` 优先（用户显式配置时先移除该 agent 默认绑定再重建）；DB 为 null 的 agent 回退 `AGENT_SKILL_MAP` 默认映射
- 工作流阶段映射 `AGENT_WORKFLOW_PHASES`：script_rewriter→剧本编写 / extractor→资产提取 / voice_assigner→音色分配 / storyboard_breaker→分镜拆解 / grid_prompt_generator→画面提示词
- 前端 settings.vue Skills tab：新增「📦 全部 Skill」入口（selectedAgent='all'，currentSkills 全量、新增按钮隐藏）；skill 卡片显示来源分类 badge（skill-cat-badge core/minimax-installed/minimax-builtin/custom）+ 绑定 Agent 标签（🤖）+ 工作流阶段标签（⚙️）；删除 minimax 前缀 skill 时二次强确认
- 验证方式坑：PowerShell 直连 5789 可能连到残留旧进程导致字段为空；可靠验证 = `npx tsx` 脚本里 `app.request('/')`（Hono 内置，路由未挂 /api/v1 前缀时路径用 `/`）
- 验证结果：34 个 skill，categories={core:5, minimax-builtin:9, minimax-installed:20}；film-shot→[storyboard_breaker]/分镜拆解、short-drama-screenwriter→[script_rewriter]/剧本编写、voice-clone→[voice_assigner]/音色分配 正确

## 剧集工作台画风切换（2026-09-01 晚，用户反馈"剧集里没有风格的切换"）
- 场景：`frontend/app/pages/drama/[id]/episode/[episodeNumber].vue` 制作→角色 tab（"N 个需生成形象角色"），每个角色卡片原只有 ModelSelector（默认模型）+编辑+生成，无画风选择。
- 改动（仅前端，后端白名单已支持 style）：
  1. 角色 tab 顶栏 `prod-section-bar` 新增「画风」下拉（剧集级）：跟随全局/写实电影/日式动漫/吉卜力/电影感/美漫漫画/水彩 → `switchDramaStyle()` 调 `dramaAPI.update(dramaId,{style})` 持久化，`dramaStyleValue = computed(()=>drama.value?.style||'')`
  2. 每个角色卡片 `foot-right` 新增 `.char-style-select` 紧凑下拉（76px，角色级）：随剧集/六画风 → `switchCharStyle(c,style)` 调 `characterAPI.update(c.id,{style})` 持久化，`c.style` 本地更新
  - 画风选项映射 `styleLabels`（realistic 写实电影/anime 日式动漫/ghibli 吉卜力/cinematic 电影感/comic 美漫漫画/watercolor 水彩）
- 关键事实：后端 `PUT /dramas/:id`（dramas.ts L188）与 `PUT /characters/:id`（characters.ts L85）白名单均含 `style`，可直接改剧集/角色画风；生成时 characters.ts 四入口经 `getEffectiveArtStyle(charStyle,dramaStyle)` 自动解析，无需传 style 参数。
- 调试教训：Nuxt dev 的 `/settings` 等页面 SSR 只返回 1087B SPA shell（ssr:false 或客户端渲染），curl/Select-String 匹配不到组件内文案，无法用 SSR 验证前端是否编译新代码；验证页面编译正常用 `curl http://localhost:3013/drama/1/episode/1` 返回 200 即可。

## 角色卡片画风下拉布局调整（2026-09-01 深夜，用户反馈"卡片宽度不够"）
- 初版把画风下拉放进 `foot-right`（模型+画风+编辑+生成 4 元素），380px 卡片放不下。改为：画风下拉移到 `asset-body` 内新增 `.asset-name-row`（flex，名字 flex:1 省略号收缩），放在角色名右侧；foot-right 恢复为模型+编辑+生成，与场景卡片一致。
- 样式：`.asset-name-row { display:flex; align-items:center; gap:6px; min-width:0 }` + `.asset-name-row .asset-name { flex:1; min-width:0 }`；`.char-style-select`（76px，flex-shrink:0）不变。
- `prod-section-bar` 已有 `flex-wrap:wrap`，顶栏新增的剧集画风下拉不会溢出。

## 独立角色编辑页：加画风切换 + 去校色模块（2026-09-01 深夜）
- 位置：`frontend/app/pages/drama/[id]/character/[characterId].vue`（独立角色页，区别于弹窗 CharacterEditor.vue，当时无画风切换且有校色调整）。
- 改动：
  1. 「图片生成提示词」区反向提示词下方新增画风下拉（替换原 ColorGradePanel 位置）：`form.style`，「跟随剧集」+六画风（realistic/anime/ghibli/cinematic/comic/watercolor），与 CharacterEditor.vue 一致。
  2. 删除校色调整模块：模板 `<ColorGradePanel v-model="colorGrade" />`、script `colorGrade` ref、三处 `color_grade: colorGrade.value || undefined` 传参（generateImage/generateThreeViews/generateEquipImage）全部移除，0 残留。
  3. 表单回填加 `form.style = c.style || ''`，persistForm 保存 `style: form.style || null`（后端 PUT /characters/:id 白名单含 style，保存即可持久化）。
- 验证：残留引用 0、lint 0 错误、页面编译 200。注意：同一文件多处 replace_in_file 并行会写锁冲突，需串行。

## 头部菜单入口（2026-09-01 第三轮）
- `frontend/app/layouts/default.vue` 头部导航新增三个一级入口：Skill→`/settings?tab=skills`、模型→`/settings?tab=ai`、Agent→`/settings?tab=agents`；「设置」入口 active 条件改为 `route.path==='/settings' && !route.query.tab`；新增 `isSettingsTab(id)` 判断 query 匹配
- `frontend/app/pages/settings.vue` 支持 `?tab=` 定位：`syncTabFromQuery()` 读取 route.query.tab，匹配 baseTabs/advancedTabs 则切换；agents/skills 自动 `showAdvanced=true`；watch route.query.tab 变化同步
- 验证：`npx nuxt build` 成功；settings.vue 的 LSP「找不到名称」误报是大型 .vue 文件 template↔script 关联解析问题，构建不受影响

## 分镜关键帧（keyframe）功能闭环（2026-09-01 追加）
- 需求：分镜加「中段关键帧」——首帧/尾帧之外锁定动作中间态，供视频生成参考图用。
- 后端：storyboards 表新增 `keyframe_prompt`/`keyframe_image`（schema.ts L155-156 + db/index.ts ensureColumn 迁移）；storyboard-tools.ts agent 输出 schema/创建写入/更新映射支持 keyframe_prompt；image-generation.ts 完成回调写回 keyframeImage（L422/L466）；auto-pipeline.ts 新增 submitMissingKeyframes/waitForKeyframes（管线 image-keyframes 阶段）、视频参考图拼接 sb.keyframeImage；storyboards.ts regenerate-frame 路由支持 frame_type=keyframe（含 frameHint）。
- 前端 episodeNumber.vue：getKeyframe()、分镜详情 tag + detail-preview-card 关键帧卡、keyframe_prompt textarea（updateField 走映射落库）、frame-thumb 关键帧缩略图（v-if 显示，点击生成）、genShotFrame 支持 keyframe（完成判断 watch getKeyframe）、buildShotImagePrompt 关键帧生成时优先用用户填的 keyframe_prompt 描述。
- 验证：前端模板/script compiler-sfc 编译 OK；后端 tsc 0 错误。

## Skill/模型/Agent 脱离设置菜单成为独立页面（2026-09-01 第四轮）
- **根因**：第三轮只是把链接指向 `/settings?tab=`，页面仍渲染在设置页内，不符合「脱离设置菜单」诉求
- **拆分**：`settings.vue`（原 2533 行）拆为 4 个页面——
  - `pages/skills.vue`：Skill 管理（左侧来源栏：全部/各 Agent/MiniMax 技能库；右侧列表+编辑+新增弹窗+空状态）
  - `pages/models.vue`：模型管理（Quick Setup/本地 Ollama/GPU 显存/Token 统计/按服务类型配置列表 + AI Config 弹窗 + Quick Preset 弹窗）；`applyQuickPreset` 已移除 `loadAgents()` 调用
  - `pages/agents.vue`：Agent 配置（5 个 agent 卡片 + 编辑表单 + Skill 拖拽绑定面板）
  - `pages/settings.vue`：精简为仅「生成历史 + 数据存储」两 tab
- **导航**：`default.vue` 三链接改为直接 `to="/skills"` `/models` `/agents`，active 用 `route.path.startsWith()`；删除 `isSettingsTab`；设置 active 恢复 `route.path === '/settings'`
- **样式**：各新页面 scoped style 从原 settings scoped 复制（含 `.settings-scroll/.settings-head/.settings-brand` 等基础类 + 各自面板类）；skills.vue 空状态样式 `.step-empty` 等从 `drama/[id]/episode/[episodeNumber].vue:4531` 复制
- **验证**：dev server（3013）运行中，4 页面 `/@fs/.../pages/*.vue` Vite 编译全部 200；SSR 关闭（nuxt.config ssr:false）故 curl 只见骨架，浏览器渲染正常；LSP「找不到名称」仍为陈旧缓存误报（各页面 template 引用的变量在各自 script 中均已有定义），以 Vite 编译 200 为准
- **Lint 陷阱**：`read_lints` 对拆分后的大文件会返回基于旧版本的缓存诊断，必须用实际编译（Vite @fs 模块请求 / nuxt build）验证

## 项目完善度检查
- 后端 `npm run typecheck`（tsc --noEmit）通过，0 错误。
- 前端 `npm run build`（Nuxt）通过，无 lint 错误。
- 待完善项：
  1. `configs/config.yaml` 未创建（仅 example），无 `.env`；代码有默认值降级可跑。
  2. README 引用不存在的 `CHANGELOG.md`；`git clone` 是占位 URL。
  3. 根目录残留调试/临时文件：`_rewrite_out.json`、`_wf_dump.txt`、`backend.log`、`backend.err.log`、`frontend-dev.log`。
  4. 大量未提交改动（git status：删 memory 历史文件、改后端多文件）。
  5. npm 警告：未知 project config `package-manager`。
- 技术栈实测：后端 Hono + tsx（默认端口 5789），前端 Nuxt 3（端口 3013）——与 MEMORY.md 中"uvicorn 18002 / studio-client 5175 / studio-admin 5173"严重不符，MEMORY.md 该章节为过时信息（来自早期 Python/Vite 版本）。

## 完善项处理（按序）
1. 清理临时/调试文件：删除根目录 `_rewrite_out.json`、`_wf_dump.txt`、`backend.log`、`backend.err.log`、`frontend-dev.log`；backend `dev.log`、`_repro_tools.out.log`；scripts `_rewrite_result.json`、`_rewrite2.err`、`_rewrite2.log`、`_rewriter.err.log`、`_rewriter.out.log`（共 12 个）。
2. 生成 `configs/config.yaml`（复制 example，debug=true）；README 两处修正：`git clone` 占位 URL → 通用提示；删除指向不存在 `CHANGELOG.md` 的「更新日志」章节。
3. `.gitignore` 增加 `__pycache__/`、`*.pyc`（`*.log` 原已存在）。
- 已清理：`_` 前缀一次性调试脚本共 15 个（backend `_db_probe.mjs`/`_inspect.mjs`/`_probe2.mjs`/`_repro_full.ts`/`_repro_tools.mjs`；scripts `_db_probe.mjs`/`_find_api_fmt.py`/`_parse_wf.py`/`_probe2.mjs`/`_run_rewriter.mjs`/`_test_ollama_tools.mjs`/`_tmp_db.py`/`_tmp_db2.py`/`_tmp_trace.py`/`_update_cfg.mjs`）。已 `search_content` 确认无正式代码引用。
- `scripts/` 现仅保留 5 个正式工具：`model_manager.py`（模型下载/管理）、`sd_h3_pipeline.py`（sd.cpp 编排）、`sd_h3_compat_probe.py`（GGUF 兼容探测）、`h3_install.py`（H3 安装）、`migrate_models.ps1`（模型迁移）。
- 新增 `scripts/README.md`：记录 5 个工具职责、用法、依赖关系（pipeline/h3_install 复用 model_manager 的 download_url/check_bin；compat_probe 独立零依赖）。

## 架构结论：后端是否换 Python？
- 结论：**不换**。维持「TS 后端（Hono，Web/业务层）+ Python（AI/GPU 计算层）」混合架构。
- 依据：backend 107 个 .ts 只管 HTTP 路由/SQLite/Agent/ffmpeg/Ollama spawn，与 Nuxt 前端同构；真正需要 Python 的 ComfyUI、`local_services/h3/server.py`、`scripts/*.py` 本来就已是 Python，且通过 HTTP(8765)/subprocess 与后端解耦。全换 Python 是纯成本无收益。

## Git 提交落盘（3 个逻辑提交）
- 修复 npm 警告：删除 `backend/.npmrc`（内容为无效配置 `package-manager=npm`，npm 不认该 key；包管理器声明应写 `package.json` 的 `packageManager` 字段）。
- 累积未提交改动拆 3 个逻辑提交：
  1. `7dcb02a` chore: consolidate memory and docs, fix npm config warning（24 文件 +293/-4014）
  2. `feb4031` feat: local model download and management（19 文件 +2844/-43）
  3. `d398bf2` feat: H3 video inference tooling（8 文件 +1604）
- 工作区现已干净。

## 远端 merge 后 models.vue 模型管理移植 + 适配（2026-09-01 第五轮）
- 场景：远端分支含 models.vue 大改（本地模型扫描/HF 下载/Ollama 删除/runtimeHealth/checkpoint），本地是旧版，merge 冲突后需移植适配。
- **关键：后端契约保持旧版，models.vue 调用必须适配现有 `useApi.ts`/后端**，而非反过来。适配点：
  - 扫描：`scanAsync({ roots }) → { taskId }` + 轮询 `scanStatus(taskId) → { taskId, progress }`（progress 字段 `done/cancelled/error/result`），取消 `scanCancel(taskId)`；roots 传 `[drive]` 或 `[modelDir, ...customRoots]`
  - 目录：`roots() → { roots, detail, extra, paths }`，models_dir 在 `paths.models_dir`，额外目录在 `extra`；保存用 `savePaths({ models_dir })` / `savePaths({ roots })`
  - 注册：`register(modelsArray)`（直接传模型对象数组）→ `{ created, updated, skipped, configs }`（无 failed 字段）
  - 删除：`delFiles([path])`；驱动：`drives() → { drives }` 需取 `.drives`
  - HF：`hfFiles(repo, undefined, source)` → `{ files }`；`hfDownload(repo, files, '', source, onEvent)`，source 枚举 `hf_mirror`/`hf`/`modelscope`（非 hf-mirror/huggingface）；NDJSON 事件 `{status,file,downloaded,total,overall,overallTotal}`
- `scanModels` 必须是 computed 过滤（远端语义），不能直接赋值；`isRegisterable = m.role==='standalone' && m.suggested?.callable!==false`
- 验证：`@vue/compiler-sfc` 脚本编译 script+template+style 全通过；dev server 正常。LSP「找不到名称」仍为陈旧缓存误报，勿采信。
- **新坑**：`nuxi prepare` 是修复 `.nuxt` 缓存不完整（缺 `tsconfig.json` → Vite 报 `ENOENT` 覆盖层）的官方手段。

## models.vue 三个 404 修复（2026-09-01 第六轮）
- 现象：`GET /ai-configs/runtime/health`、`/local-models/drives`、`/local-models/roots` 经 3013 代理和 5789 直连都 404，JSON 解析报 `Unexpected non-whitespace character after JSON at position 4`（响应是 HTML 404 页）。
- 根因链：git merge 后源码已含这些路由（aiConfigs.ts L960 `/runtime/health`、localModels.ts L75 `/roots`、L307 `/drives`），但运行中的后端是 merge 前 14:48 启动的旧进程（`npx tsx src/index.ts` 无 watch 不热重载）→ 重启时又发现 merge 后 package.json 新增的 `undici` 依赖未安装 → `npm install` 后再启 tsx watch。
- 踩坑：PowerShell 不支持 `cd /d`，用 `Set-Location`；tsx watch 崩溃后**不会自动退出**，需 touch 源文件（`(Get-Item src\index.ts).LastWriteTime = Get-Date`）触发 `Rerunning` 才真正重启。
- 验证基线：`node -e "import('undici')"` 可解析（58 个导出）说明依赖就绪；5789 监听后 `Drama Studio server on http://localhost:5789` 即成功。
- 遗留：`backend/dev-server.log` 是本次临时启动日志，未清理。

## models.vue 运行时状态卡片优化（2026-09-01 第七轮）
- 现象：四个本地运行时卡片（Ollama/SD/H3/CosyVoice）显示 `fetch failed` + 「1 配置」tag 样式被压缩。
- 根因：① 服务未启动时后端 probe 把 undici 原始错误 `fetch failed` 直接透传；② `registered_count` 计数本身正确（DB ai_service_configs 恰好每 serviceType 一条本地配置 id 4-7，`isLocalConfig` 用 provider 白名单 ollama/local-sd/cosyvoice + baseUrl localhost 判断）；③ `.tag` 无 `flex-shrink:0`，在 runtime-item flex 行被长文本挤压。
- 修复：① 后端 `aiConfigs.ts probeLocalRuntime` 错误归一化——`fetch failed|ECONNREFUSED|ENOTFOUND|EAI_AGAIN|UND_ERR_CONNECT|ENETUNREACH|SocketError` → 「未启动」，AbortError → 「连接超时」，其余透传；② 前端 `.runtime-meta` 加 `flex:1`、新增 `.runtime-tag { flex-shrink:0; margin-left:auto }`。
- 验证：接口返回 `error=未启动`（GBK 控制台乱码但内容正确），reg=1 正常。

## models.vue runtime 标签交互增强（2026-09-01 第八轮）
- 后端 `/runtime/health` 已返回 `registered` 数组（{id,name,provider,base_url}），前端此前只用了 `registered_count`。
- 增强：runtime 卡片「N 配置」tag 加 `@click="jumpToSection(s.service_type)"` + title；各 serviceType 配置 section 加 `:id="cfg-section-${st.type}"` + `.cfg-section` 类（透明边框 1px + scroll-margin-top:16px）；点击后 `scrollIntoView({behavior:'smooth'})` + 临时 `.section-flash`（accent 边框 + 3px accent-bg 光晕）1.6s 后移除，timer 在 onUnmounted 清理。
- 验证：`@vue/compiler-sfc` script/template/style 三部分编译全通过（@fs URL 会被 Nuxt 路由吞成 HTML，不能用它验证编译；页面 200 正常）。

## models.vue 左侧菜单拆分（2026-09-01 第九轮）
- 用户要求给 models 页加左侧菜单拆分。方案：`.models-page` 改 flex，左侧 `.models-side`（宽 196px，品牌 + 8 项导航）+ 右侧 `.settings-scroll`（flex:1，max-width 840 居中）。
- 菜单 8 项（`sideItems` + `activeTab` ref，初始 'quick'）：推荐配置/本地模型/服务状态/Ollama 模型/扫描与下载/服务配置/GPU 监控/Token 用量。
- 原「本地模型」大 section（含 local-grid+runtime+ollama+scan 四个子面板）拆为 4 个独立 section，各自 `v-show="activeTab==='x'"`；GPU/Token/快捷模板/配置列表(.sections div) 也加 v-show。
- 关键点：**必须用 v-show 不能用 v-if**——scan/Ollama 输入框等内部状态（选中项、输入内容）在 v-if 卸载时会丢失。切换 tab 通过 `switchTab(key)` 同时把 `.settings-scroll` 滚回顶部，避免停留在空白处。
- `jumpToSection` 更新：先 `activeTab.value='configs'`，再 `nextTick` 内 scrollIntoView + flash（否则 v-show 隐藏时滚动定位失败）。
- 新增 import：Activity / SlidersHorizontal / BarChart2（lucide-vue-next）；`<component :is="item.icon">` 渲染菜单图标。
- 注意：read_lints 对 models.vue 报全文件「找不到名称 xxx」（含原始变量如 cfgForm）是 Volar 语言服务对 `<script setup>` 解析失效的假报错，compiler-sfc 三部分编译通过即代码无误。

## models 页左侧菜单功能验证（2026-09-01 第十轮）
- 用 playwright-cli（`run-code --filename` 方式加载测试脚本）逐一实测 8 个 tab：
  - 8 个 tab 切换全部正确，每次仅显示对应 section；Ollama 区块无 `.setup-title` 属正常
  - `jumpToSection`：从服务状态点「N 配置」→ 自动切到服务配置 tab + section-flash 高亮 + 滚动定位，全部生效
  - `switchTab` 滚动重置生效（注意测试里 `scrollTop=0` 时 `0 || -1` 返回 -1 是脚本 bug，非页面问题）
  - 新建配置对话框：真实点击正常打开/关闭（JS `.click()` 会命中隐藏 tab 内按钮导致误判，应用 Playwright `locator.click()` 验证）
  - 服务状态刷新、GPU/Tokentab 空态、品牌 logo 均正常；控制台无 JS 错误
- 经验：playwright-cli 传参在 PowerShell 下引号易被拆散，复杂测试脚本写文件后用 `run-code --filename <file>` 加载；PowerShell 下 `eval` 支持 Promise（async IIFE 内点击+等待+读取）。
- 截图默认存 `.playwright-cli\element-*.png`；`screenshot body` 需要元素选择器。

## models 页品牌重复移除（2026-09-01 第十一轮）
- 用户反馈：品牌「短剧工坊 Drama Studio」已在全局 header（`layouts/default.vue`），不应在其它页面内容区重复出现。
- 移除了 4 个页面内的品牌块（logo + Drama Studio + 短剧工坊）：
  - `pages/models.vue`：侧栏 `.side-brand`（logo+Drama Studio+模型与配置）+ 内容区 `.settings-brand`
  - `pages/settings.vue`：两个 tab（history/storage）各一个 `.settings-brand`
  - `pages/agents.vue`、`pages/skills.vue`：内容区 `.settings-brand`
- 同步清理：各文件 script 中 `import brandLogo`、`const showBrandImage`，以及 CSS `.side-brand`/`.settings-brand` 系列（保留 `.settings-title`）。
- 验证：`@vue/compiler-sfc` 4 文件全部编译 OK；浏览器实测 4 页面 `pageBrandCount=0`、内容区无「短剧工坊」文本，header 品牌正常。
- 经验：`@vue/compiler-sfc` 只在 frontend/node_modules，校验脚本需放 frontend 目录内运行；node 从脚本所在目录向上找 node_modules。

## 参考项目 short-drama-agent 分析（2026-09-01）
- 用户下载到 `reference/short-drama-agent`（Claude Agent Skill 型方法论仓库：SKILL.md + 4 references + 3 火山脚本 + 测试），已全面分析并与当前项目对照。
- 当前项目已有且更强：首/尾帧双 prompt + 首尾帧图生成（minimax last_frame_image / volcengine role:last_frame）、镜头级 QC 打分（qc-scoring.ts：lip_sync/character_consistency/continuity）、全自动管线（幂等+崩溃恢复+SSE）、TTS+字幕+拼接、音色锁定、多参考图 provider。
- 参考项目可借鉴的 6 点（按价值排序）：
  1. 连续性状态机（场景空间方向/角色连续性/道具状态/线索揭示/动作因果/转场动机 6 表）——当前 QC continuity 仅查地点ID+时长
  2. 审片重跑闭环（QC failed → 只重写失败镜 prompt 再生成）
  3. 上一镜尾帧图 → 下一镜视频参考图衔接（lastFrameImage 已落库未接线）
  4. props 物品资产库（当前只有 costumes/weapons）
  5. 逐镜"禁止变化"清单（当前 negative 是全局的）
  6. 专业镜头语言术语 + UI 安全区/质感层
- 参考项目实现方式为 skill 型（提示词资产+脚本），非 Web 应用；工程化程度远低于本项目。用户将下载的参考项目放 `reference/`（gitignore 已忽略）。
- 仓库原处于 merge 进行中（远端分支 merge，冲突已解决未收尾），按用户要求按顺序完成全部提交，工作区现干净：
  1. `d18ad6d` Merge branch 'main'（merge 收尾，--no-edit）
  2. `59b6f48` feat: remove duplicated brand sections from pages（4 页面品牌移除）
  3. `83af320` feat: normalize runtime probe errors and update dev memory（aiConfigs.ts 错误归一化 + 记忆）
  4. `f1ef396` chore: ignore playwright artifacts and remove stray root lockfile（.gitignore 加 .playwright-cli/ 与 models.yaml；删除根目录孤立 package-lock.json）
- 经验：`git commit --no-edit` 可免编辑器直接收尾 merge；根目录 `models.yaml` 是 playwright-cli 导出的 DOM 树测试产物，不入库。

## 连续性状态机 v3 落地（2026-09-01，来自 short-drama-agent 借鉴点 1/4/5/6）
- **目标**：跨集/跨镜视觉连续性 6 大支柱逐项落地，按阶段执行中：
- **阶段 1（完成）**：storyboards 表加 `start_state/end_state/constraints` 字段 + 迁移；`storyboards.ts` 路由 GET/PUT 带出新字段；`storyboard-tools.ts` save/update 工具接收；`agents/index.ts` storyboard_breaker 字段清单+拆镜流程要求（先逐镜列 start_state→end_state，约束从状态中提炼）；read_storyboard_context 返回 `continuity_states`。
- **阶段 2（完成）**：`continuity_states` 表（episode_id/state_type/entity_key/state_value/constraints/storyboard_id）+ 迁移；`auto-pipeline.ts` submitMissingImages 里从上一镜 end_state 派生 start_state 更新（`buildStartStateFromPrev`，只写非空）。
- **阶段 3（完成）**：`qc-scoring.ts` 检查 storyboards.start_state/end_state 是否有冲突（新字段 failed_reason='state_conflict'）；`storyboards.ts` 新增 `POST /:id/generate-video-preview`（视频预览走 QC + 失败自动重试，qc-retry.ts 重跑未完成分镜）。
- **阶段 4（完成）**：props 物品库——`prop_templates` 表（drama_id/name/category/description/appearance/size_hint/holder/key_clue/custom_prompt/negative_prompt/image_url/软删）+ `episode_props`、`storyboard_props` 关联表；`routes/props.ts` 完整 CRUD + `POST /:id/generate-image`；`generateImage` 加 propId 参数与完成回调更新 prop_templates.image_url；`image_generations` 加 prop_id 列；`extract-tools.ts` 加 read_existing_props/save_dedup_props（按名去重+关联集）；extractor prompt 加提取物品步骤；storyboard-tools 加 prop_ids 支持 + syncStoryboardProps；read_storyboard_context 返回 props；`prompt-utils.ts` 新增 `buildPropImagePrompt`，getStoryboardReferenceImages 注入物品参考图；storyboard_breaker prompt 加 prop_ids 说明。
- **阶段 5（完成）**：逐镜禁止变化清单——`image-generation.ts`/`video-generation.ts` 在 storyboardId 存在时查询 storyboards.constraints 拼入 prompt（`-- 逐镜禁止变化（画面中以下元素必须保持不变…）: {constraints}`），全链路自动生效。
- **阶段 6（完成）**：上一镜尾帧图 → 下一镜视频参考图衔接——auto-pipeline `submitMissingVideos` 的 tail-link 逻辑（同场景顺接时 `anchorImage=prev.lastFrameImage` 作为本镜视频起帧，`firstFrameUrl` 传本镜首帧补充、`lastFrameUrl` 传本镜尾帧锁定结束画面）；视频完成后 `extractStoryboardTailFrames` 从成品视频提取真实尾帧写 `last_frame_image` 供下一镜衔接。借鉴点 6 补充：storyboard_breaker prompt 新增镜头语言术语表（过肩/双人/插入/视线引导等）、画面安全区约束（中央 2/3 安全区 + 下方 1/5 字幕区）、质感层要求。
- **6 大借鉴点全部落地**：1 连续性状态机（start/end_state + continuity_states 表）✓ 2 审片重跑闭环（qc-scoring state_conflict + qc-retry）✓ 3 尾帧衔接 ✓ 4 props 物品库 ✓ 5 逐镜禁止变化清单（constraints 注入图片/视频 prompt）✓ 6 镜头术语+安全区+质感 ✓
- 编译验证：`backend npx tsc --noEmit` 全通过。注意 `backend/src/services/frame-extractor.ts`、`qc-retry.ts`、`routes/props.ts` 为新文件（尚未 git add）。
- **前端收尾（2026-09-01 晚）**：
  - `useApi.ts` 新增 `propsLibraryAPI`（list/get/create/update/del/generateImage）。
  - 新建 `frontend/app/pages/library/props.vue`：物品卡片网格（缩略图/类别徽章/线索标记/持有者/尺寸）、新建+编辑模态框、删除确认、设定图生成（提交后轮询刷新 60s）、前端搜索+类别筛选、`?drama_id=` 过滤。
  - `library/index.vue`：加「物品库」入口卡片（紫色 💎 图标）、stats.props 计数、recent 列表 prop 类型 badge。
  - `storyboards.ts` PUT fieldMap 补 `start_state/end_state/constraints`（此前 AI 工具能写但路由保存会丢），PUT/POST 支持 `prop_ids` 同步（复用 storyboard-tools 的 `syncStoryboardProps`，已 export），POST 返回 `prop_ids`。
  - `episodes.ts` 分镜列表响应补 `prop_ids`（storyboardProps 关联表聚合）。
  - `storyboard-tools.ts` 导出 `syncStoryboardProps` + 新增 `getStoryboardPropIds`。
  - `episode/[episodeNumber].vue` 分镜详情面板新增「连续性状态」section：开始状态/结束状态/逐镜禁止变化 textarea（blur 保存）+ 关联物品 chips 多选（`propsList` 由 `propsLibraryAPI.list` 加载，`toggleStoryboardProp` 切分）。新增 CSS：.prop-chips/.chip-btn/.empty-hint。
  - 验证：backend tsc 通过；前端三个 vue 文件 @vue/compiler-sfc 三部分编译 OK（read_lints 的 1000+ 报错均为 Volar `<script setup>` 解析失效假报错）。

## 第二轮借鉴点（2026-09-01 深夜，继续挖 short-drama-agent）
- 6 大借鉴点之外，又挖出并落地 3 项（参考项目 Mx-Shell 质感层 + sound policy + 转场动机）：
  - `storyboards` 表新增 `transition_motive` 列（schema.ts + db/index.ts ensureColumn + storyboards.ts fieldMap + storyboard-tools.ts save/update 工具的 zod schema 与 insert/update 映射，POST/PUT 均支持）。
  - `prompt-utils.ts`：`VISUAL_STYLE_VIDEO` 常量升级为 Mx-Shell 质感层（35mm/50mm 电影镜头浅景深、胶片颗粒、现实瑕疵锚点、克制结尾无爆炸/胜利姿势/文字叠加）；`buildStoryboardVideoPrompt` 追加 `Sound: production audio only, no background music in frame (music is mixed in post-production)` 声音策略。
  - `agents/index.ts` storyboard_breaker prompt：新增 Mx-Shell 质感增强要求（真实镜头锚点/现实瑕疵/克制结尾）+ `transition_motive`（8 选 1：视线引导/动作匹配/声音引导/道具承接/情绪延续/信息揭示/空间切换/危险预警；开场镜写"开场"）与 `transition_type` 须匹配。
  - 前端 `episode/[episodeNumber].vue` 连续性状态 section 新增转场动机/转场方式两个 input（@change 保存）。
- 验证：backend tsc 通过；前端 compiler-sfc OK。
- **仍未落地的借鉴点（后续可选）**：① UI 留白图资产类型（ui_plate，手机/短信/监控/地图等屏幕留白，供后期叠加文字）② 资产验收门禁（approved/needs_regeneration/missing + 视频任务 blocked_by_missing_asset）③ 关键帧 8-16 张（当前仅首尾帧两帧）。
- **注：①②③ 已在后续全部落地**（ui_plate 在 prompt-utils.ts buildUiPlateImagePrompt + props.ts；资产门禁在 schema asset_status/block_reason + image-generation markStoryboardAssetNeedsRegeneration + auto-pipeline blocked_by_missing_asset；关键帧 keyframe_prompt/keyframe_image 全链路）。

## reference/ 全部项目逐项审查（2026-09-01 深夜）
- reference/ 共 7 个项目，按顺序逐个检查完毕（用户要求不并行）。
- **ArcReel**（Python+TSX 视频生产工作台，AGPL）：本项目最有借鉴价值的参照。关键机制：① 成本追踪（cost_calculator + lib/pricing/{lookup,strategies,types} + server/routers/usage.py：ModelInfo.pricing 单一真相源、按 kind 派发 per-token/per-second matrix/tiered、estimate_only 预估与真实结算分离、自定义供应商价格）② 版本管理（version_manager.py + artifact_version_provenance.py + artifact_version_restore.py：分镜图/视频/角色/场景/道具/宫格图历史版本、切换当前版本、StagedVersionCommit 多资源批量提交）③ 供应商抽象（lib/text_backends、image_backends、video_backends、audio_backends、custom_provider：统一 adapter + 自定义供应商 endpoint）④ 生成队列（generation_admission/queue/worker + batch_admission + video_caps：批量准入/队列/worker）⑤ 资产清单与溯源（artifact_manifest + artifact_provenance + RESOURCE_TYPES + path_safety）⑥ 多宫格分镜（lib/grid/{models,layout,prompt_builder,splitter} + grid_split.py：一张图多镜头）⑦ 风格模板库（style_templates.py：36 条模板 live 18 + anim 18）⑧ 角色音色（character_voice.py：prompt 软约束 vs reference_audio 原生音色 + voice_consistency 档位）⑨ 剪映草稿导出（server/services/jianying_draft_service.py）⑩ 工作流规划/恢复（workflow_plan/state/rules + resume_executor + episode_ledger）。
- **ComfyUI-H3-Multishot**：H3 多镜头无缝链式渲染（Stage A ref2va 首块 → Stage B 用上块尾帧 I2V chain → H3ConcatAV 拼接，无剪辑/无色彩偏移/音频连贯）+ interior keyframes 锚定（ComfyUI 0.34+ 原生支持 clip 内任意位置锚点+条件强度）+ activation reserve 显存自适应 + LLM prompt writer 本地 Ollama + GGUF 架构补丁。借鉴：多镜头链式渲染理念与首尾帧衔接一致；JSON envelope 多镜头脚本格式 {"prompts":[...]}。
- **ComfyUI-MiniMax-H3-Turbo**：Turbo LoRA 4-8 步采样加速（v4-600 静态/慢动作 vs v1-850 4步大动作，strength 1.0，scheduler simple，low_vram 模式）。纯 ComfyUI 节点实现，印证本地 H3 Turbo 路线 C 可行性。
- **MiniMax-H3-Codex-Drama**（Codex 插件型，方法论成熟度最高）：SKILL.md + references + profiles(12)+ledger。关键借鉴：① QC 硬性数值标准（duration tolerance 0.10s / fps 0.01 / true peak -1dBTP / -14 LUFS ±1 / black interval 0.25s / freeze 1.0s）② Profile Distiller（从参考视频提炼可复用 house style：storytelling/shot-patterns/audio-and-captions/qc-rules，区分测量事实 vs 视觉推断 vs 用户偏好）③ project ledger（stage/job/artifact/selected-take/assumption + resumable）④ shot routing（每镜按控制媒体选 t2v/f2v/r2v/editor workflow）⑤ take 预算（ordinary 1 take / key shot 2 takes）⑥ 6 大视觉支柱（identity/wardrobe/product/locations/props/screen-direction/light/audio 显式跟踪，与我们 continuity v3 一致）。
- **minimax-h3-comfyui**：fast T2V/I2V 工作流（pruned INT8 ConvRot FL2VA + NVFP4 Qwen3-VL 32B + 10 步 + SageAttention，43GB，RTX5090 warm 3s=23s / 5s=85s）+ `[background_audio]` 场景声 prompt 标记（H3 原生音频）→ 借鉴：H3 prompt 的 `[background_audio]` 语法可用于我们的 video prompt 构造。
- **short-drama-agent**：已 6 大借鉴点 + 3 项全部落地（连续性状态机/审片重跑/尾帧衔接/props 物品库/逐镜禁止变化/镜头术语+安全区+质感 + ui_plate + 资产验收门禁 + 关键帧），无剩余。
- **workflow_templates**（ComfyUI 官方模板库）：templates/618 JSON + blueprints 子图 + site Astro。含 H3 系列 api_minimax_h3_{t2v,r2v,flf2v}.json + video_minimax_h3_{t2v,r2v,i2v,continuation}.json。借鉴：H3 prompt 写法参考（秒级时间轴描述 + `[VFX: ...]` 特效标记 + `[background_audio]` 音频标记 + 禁止清单），index.json 元数据 schema 与模板组织方式。
- **Top 可落地借鉴（对 Drama Studio）**：① 成本追踪（按模型记录用量+前端展示，中价值）② 版本历史/回滚（资产多次生成保留历史，中价值）③ Profile 提炼（从参考视频提炼风格配置，中价值，需新增数据模型）④ QC 硬性数值标准（duration/black/freeze/LUFS，低工作量高收益，可并入 qc-scoring）⑤ `[background_audio]`/`[VFX]` prompt 标记（低工作量，直接加强 video prompt）。
- **已落地（2026-09-01 晚）**：
  - **借鉴点 ④ QC 硬性数值标准**：新建 `backend/src/services/technical-qc.ts`（probeVideoTechnicalSpec + runTechnicalQc）。阈值 TECH_QC_THRESHOLDS：黑场 >0.25s / 冻帧 >1.0s / 真峰 >-1dBTP / 集成响度 -14±1LUFS / fps ≥15 / 时长偏差 ≤1s。ffprobe 读时长帧率、blackdetect/freezedetect/volumedetect/ebur128 滤镜解析 stderr。fire-and-forget 写入 video_quality_checks 的 dimensions.video_spec + issues，总分 = 规则三维平均×0.7 + 技术×0.3，有 error 级即 failed。接入点：qc-scoring.ts `runQcAfterVideoComplete`（原三维打分后追加）。
  - **借鉴点 ⑤ `[background_audio]` 标记**：`buildStoryboardVideoPrompt` 新增 `backgroundAudio` 参数 → prompt 末尾追加 `[background_audio] 描述`（H3 原生场景声合成）；videos.ts 富化路径传 `sb.soundEffect`；generateVideo 统一兜底（storyboards.sound_effect 非空且 prompt 无标记时注入，幂等）。stripVideoPromptTags 只剥离尖括号 XML 标签，方括号标记天然保留。
  - 验证：backend `npx tsc --noEmit` 通过，全部编辑文件 lint 0 错误。前端无 QC 展示页，无需改动。
  - **剩余未落地**：① 成本追踪 ② 版本历史/回滚 ③ Profile 提炼（均为中工作量，后续可选）。

## 第三轮借鉴点落地（2026-09-01 深夜，参考项目「第一批三项」）
- 用户要求继续落地第一批三项，全部完成：
  - **① 用量统计+成本估算**（对齐 ArcReel usage_repo）：新建 `api_usage` 表（schema.ts + db/index.ts 迁移+索引）；`cost-catalog.ts`（单价目录：minimax/volcengine/vidu/ali/openai/openrouter/deepseek/chatfire，按 provider→模型前缀包含匹配，`settings.pricing` 可覆盖，本地/未知单价返回 null）；`usage-tracking.ts`（recordUsage/updateUsageStatus/getUsageSummary，按项目/集/服务类型/提供商/按天汇总）；`routes/usage.ts` GET /api/v1/usage/summary；接入 video-generation.ts（每次模型尝试记 submitted，handleVideoComplete→completed，isLastAttempt→failed，markUsageByVideoGen 批量收口）、image-generation.ts（同上，units=1）、tts-generation.ts（同步成功即 completed，units=文本字符数）。视频单位=秒、图片=张、音频=字符、LLM=千token。
  - **② loudnorm 响度归一化**（对齐 H3-Codex-Drama final master）：ffmpeg-merge.ts 新增 `videoHasAudio` + `normalizeLoudness`（`loudnorm=I=-14:TP=-1.5:LRA=11`，视频 copy 音频重编码 48k/192k，成功删旧文件，无音轨跳过）；doMerge 在 BGM 混音后统一调用，失败仅告警不阻断。
  - **③ 图像连续性检测**（对齐 Comic-drama consistency_validator，report 模式）：新建 `consistency-qc.ts`（sharp 9x8 dHash，同场景相邻镜头相似度 <0.55 warning / <0.65 info，跨场景 >0.9 info 可疑复用；结果写回各分镜最新 video_quality_checks 的 dimensions.continuity_vision + issues，不改总分/状态）；episodes.ts 新增 `POST /episodes/:id/consistency-qc`；ffmpeg-merge.ts mergeEpisodeVideos 开头 fire-and-forget 自动触发 `runConsistencyQcBeforeMerge`。
- 验证：backend tsc 0 错误；冒烟脚本（tsx 临时脚本，已删）——minimax 视频 5s 估 1.5 元、本地 cost=null、未知 provider 不崩、真实集 #1 检出 4 个同场景相邻镜头穿帮 warning（相似度 0.43-0.48）。
- 注意：跑着的后端是旧进程（tsx 无 watch 不热重载），需重启才生效新表/新路由；幂等迁移可在重启时自动建表。
- 剩余未落地（后续可选）：② 版本历史/回滚 ③ Profile 提炼（参考视频→house style）④ 剧本内容指纹门禁 ⑤ per-shot take 预算 ⑥ 多集节奏相位。

## 第二批借鉴点落地（2026-09-01 深夜，② ③ 完成）
- **② 资产版本历史/回滚**（对齐 ArcReel version_manager/provenance/restore）：新建 `asset_versions` 表（asset_type/asset_id/media_type/frame_type/version/asset_url/provider/model/prompt/generation_id/meta/status current|historical）；`services/asset-versions.ts`（recordAssetVersion 留档+旧版本自动降级、listAssetVersions、activateAssetVersion 回滚并把 asset_url 写回主表字段：storyboard→composedImage/firstFrameImage/lastFrameImage/keyframeImage/videoUrl，character→imageUrl，scene→imageUrl，prop→imageUrl）；接入 image-generation.ts（完成回调按 storyboardId/characterId/sceneId/propId 留档，分镜图按 frameType 分组版本）与 video-generation.ts（分镜视频留档）；路由 `GET /api/v1/asset-versions?asset_type=&asset_id=` + `POST /:id/activate`。
- **③ 风格 Profile 提炼**（对齐 H3-Codex-Drama Profile Distiller）：新建 `style_profiles` 表（drama_id/name/source/storytelling/shot_patterns/audio_captions/qc_rules/facts/inferences/preferences/is_active 软删）；`services/style-profiles.ts`（CRUD、activate 同剧唯一、getActiveProfileForDrama 剧级优先→全局兜底、distillStyleProfile 用文本 LLM 分析参考素材+ffprobe 探测测量事实、applyDistillResult 人审后落库）；agents/index.ts `appendStyleProfile` 把激活 Profile 注入 storyboard_breaker/extractor/script_rewriter 指令（house style 最高优先级，voice_assigner 等不注入，读取失败静默降级）；路由 `/api/v1/style-profiles` 7 个端点（list/get/create/put/activate/distill/apply/delete）。
- 验证：tsc 0 错误；冒烟（脚本已删）——资产版本留档3代降级/回滚写回主表/视频分组独立；Profile 创建/偏好更新/apply写入/激活/按剧查询/注入生效/非生成类不注入/软删除。
- 注意：tsc 曾报 eq(frameType, null) 类型不兼容，改用 isNull 分支；logTaskWarn 需从 task-logger 导入。后端仍是旧进程需重启生效。

## 第三批借鉴点落地（2026-09-01，④ ⑤ ⑥ 全部完成，参考项目对齐收口）
- **④ 剧本内容指纹门禁**（对齐 H3-Codex-Drama asset gate）：`episodes.script_hash` + `storyboards.script_hash` 两列（sha256 前16位，空剧本返回 null）。新服务 `services/script-fingerprint.ts`：computeScriptHash / refreshEpisodeScriptHash（剧本写入即重算）/ stampStoryboardsScriptHash（重建分镜盖章）/ checkEpisodeFingerprint（报告：current_hash/stale_count/stale_storyboards/stale_with_assets）/ checkStoryboardGate（单分镜门禁）。接入点：episodes.ts PUT、script-tools saveScript（剧本变更→重算指纹）；storyboard-tools save_storyboards（重建→盖章）；image-generation.ts / video-generation.ts 入口门禁（分镜指纹≠当前剧本→阻断，force=true 放行）；images.ts / videos.ts / storyboards.ts 三处透传 force。路由 `GET /episodes/:id/script-fingerprint`。分镜无指纹视为门禁启用前产物不阻断（向后兼容）。
- **⑤ per-shot take 预算**：`storyboards.take_count`（默认0）+ `take_budget`（默认3）。新服务 `services/take-budget.ts`：getTakeStatus / consumeTake（任务提交成功即消耗一次，无论成败）/ checkTakeBudget（超预算阻断，force 放行）/ resetTakeBudget / resetTakeBudgetForEpisode。接入：generateImage/generateVideo 入口检查（置于指纹门禁之前）+ 提交后 consumeTake；save_storyboards 重建后重置该集所有 take。
- **⑥ 多集节奏相位**（对齐 multi-episode rhythm phase）：`storyboards.rhythm_phase`（setup/development/climax/resolution）。新服务 `services/rhythm-phase.ts`：phaseForPosition（按累计时长占比：<25% setup / <70% development / <90% climax / else resolution）/ assignRhythmPhases（save_storyboards 后自动分配）/ getEpisodeRhythm / getDramaRhythm（含 phase_balance 平均占比 + guidance）/ rhythmGuidanceForEpisode（跨集节奏引导文本）。注入：agents/index.ts `appendStyleProfile` 签名改为 (type, episodeId, dramaId, instructions)，storyboard_breaker 时追加 rhythmGuidanceForEpisode(episodeId)（require 同步导入）。路由 `GET /dramas/:id/rhythm`。
- 验证：tsc 0 错误 + lint 0；冒烟（脚本已删）——指纹：确定性/刷新写回/盖章/一致放行/变更阻断/报告/重建恢复放行；take：初始0/3、消耗2剩余1、耗尽阻断、force放行、单分镜/全集重置；rhythm：四相位边界、8分镜按累计时长分配正确（注意 #6 在0.667处为 development 属预期）、剧级报告含测试集、guidance 生成。冒烟过程中残留数据已用临时 cleanup-hash.ts 清理（已删）。
- 注意：appendStyleProfile 是同步函数，内部只能 require 不能 await import（否则 tsc 报 async 函数内部 await）。改签名后 buildAgentConfig 调用处已同步更新。
- 全部 6 项借鉴点落地完成。后端旧进程需重启生效新表列/新路由。

## 第四批差距补齐（2026-09-01 晚，ArcReel 交付侧 3 项）
对照 ArcReel / minimax-h3-short-drama-prompt 重新盘点，发现并落地 3 个真实差距：
- **剪映草稿导出**：`services/jianying-draft.ts` + `export.ts` 路由 `GET /export/dramas/:dramaId/jianying-draft?episodeId=`。生成 `<name>.draft/` 文件夹（draft_content.json + draft_info.json + media/ 素材副本），微秒计时 + UUID 引用式架构（canvas_config/materials/tracks 三段式），视频轨道（composedVideoUrl 合成镜头）+ 音频轨道（TTS）+ 字幕轨道（dialogue）。素材复制进 .draft/media/ 后以绝对路径引用（本机可用，跨机打包分发可手动重链）。画布尺寸取首个分镜最新 video_generation 的 width/height（storyboards 无 resolution 字段），fps 固定 30。ZIP 内路径统一 posix 分隔符（Windows path.relative 返回 `\` 需 split/join 转换）。
- **生成前费用预估**：`services/estimate-service.ts` + `usage.ts` 路由 `GET /usage/estimate?drama_id=&episode_id=&storyboard_ids=`。按当前激活 provider/model 单价 × 待生成工作量（视频秒数/图片张数/TTS 字符数）估算 total_cost，items 明细 + unestimatable 清单。待生成判定：指定 storyboard_ids 全计；否则无 completed video_generation 记录的分镜。与 usage-tracking（花后记账）构成花前估算+花后核算双环。
- **多集成本看板**：`usage-tracking.ts` 新增 `getEpisodeCostBoard(dramaId)` + `usage.ts` 路由 `GET /usage/board?drama_id=`。按剧聚合每集：total_cost/total_calls/retry_cost（retryCount>=1 的调用成本=重拍花费）/按服务类型拆分。
- 验证：tsc 0 错误 + lint 0 + 综合冒烟 PASS（预估工作量与单价核算、看板成本/重拍成本、草稿结构/时间轴连续/ZIP 路径/素材存在，假数据临时分镜已清理）。
- 注意：storyboards 表无 resolution/fps 字段；视频分辨率在 video_generations。usage 路由为同步回调，estimate 用 GET query 传参。

## 第四批前端接入（2026-09-01 深夜，5 项全部落地）
第四批后端功能全部落地后，前端尚未对接，本次完成前端接入：
- **useApi.ts**：新增 `usageAPI`（summary/board/estimate）、`assetVersionsAPI`（list/activate）、`styleProfileAPI`（list/get/create/update/del/activate/distill/apply）封装 + 对应 TS 接口（UsageSummary/EpisodeCostBoard/EstimateResult/AssetVersion/StyleProfile）。注意 `req` 返回 `json.data ?? json`，接口级数据直接在顶层（`res.versions` 非 `res.data.versions`）。
- **剪映草稿导出按钮**：`episode/[episodeNumber].vue` 导出面板 EDL 按钮旁新增「剪映草稿」按钮，`doExportJianying` 用 fetch 拉 `/api/v1/export/dramas/:id/jianying-draft?episodeId=`，blob 触发下载 zip。
- **资产版本历史/回滚**：`episode/[episodeNumber].vue` 分镜详情面板新增「版本历史」section（刷新按钮 + 按 frame_type 分组版本列表：缩略图/版本号/当前标记/provider/model/时间/回滚按钮）。新状态 `versions/versionsLoading/activatingVersion` + `loadVersions/activateVersion/formatTime/versionsByType/currentVersionId`。注意 `versions = ref([])` 会被推断为 never[] 需显式 `ref<any[]>`；`selectedSb` 是 `ref(null)` 需 `(selectedSb.value as any)`。
- **成本看板 + 费用预估**：`models.vue` 左侧菜单新增「成本看板」（CircleDollarSign）+「费用预估」（Calculator）两个 tab。成本看板 = `usageAPI.summary` 汇总卡（总成本/总调用/计费状态 + 按服务网格）+ `usageAPI.board` 按剧集表格（每集调用/重拍成本/总成本）。费用预估 = 选剧集→整部/单集（watch 拉详情补 episodes）→`usageAPI.estimate` → 预估总费用/待生成图片/视频/字数 + 明细表 + unestimatable 提示。CSS：cost-stats/cost-grid/cost-board/estimate-form/estimate-result。onMounted 加 `refreshUsage`。
- **风格 Profile 管理页**：新建 `pages/style-profiles.vue`（侧栏无，页面自带头 + Profile 卡片网格：名称/激活徽章/绑定剧集/更新时间/描述 + 提炼/编辑/激活/删除按钮）。新建/编辑弹窗含 7 个文本域（storytelling/shot_patterns/audio_captions/qc_rules/preferences/description）。提炼弹窗：`styleProfileAPI.distill` 展示 LLM 结果（storytelling.summary 等）确认后 `apply` 写入。`layouts/default.vue` 侧栏 Agent 后新增「风格」入口。
- 验证：所有改动文件 Vite 编译 200（`/_nuxt/@fs/...` 端点可直接探测编译结果）；后端新路由冒烟正常。read_lints 对 vue 文件报的「找不到名称」均为 Volar 解析 `<script setup>` 假报错（models.vue 原有代码同样报 346 个），以 Vite/compiler-sfc 编译为准。

## style-profiles 样式修复（2026-09-01 深夜）
- 用户反馈 `/style-profiles` 样式有问题。根因：`style-profiles.vue` 复用了其他页面（settings/models/skills）**scoped 内定义**的类名（`.settings-head`/`.settings-title`/`.settings-desc`/`.setup-panel`/`.setup-panel-head`/`.setup-kicker`/`.setup-title`/`.setup-desc`/`.config-empty`），但没把这些样式带到本页 → 标题塌陷、面板无 padding、头部无 flex 布局竖排堆叠、页面不可滚动。
- 修复：补齐全部缺失类（数值对齐 settings.vue：settings-scroll height/overflow/margin auto/fadeUp 动画、settings-title 22px/700、setup-panel padding 18/18/16、setup-panel-head flex space-between、kicker 10px uppercase、config-empty 等）；删除本页重复定义的 `.tag`/`.tag-accent`（改用全局 studio.css）；`.btn-sm.danger` 颜色从不存在 `var(--danger)` 改为全局 `var(--error)`；保留页面独有 `sp-*` 卡片/弹窗/提炼区样式。
- 经验：Nuxt 页面间通用布局类（settings-head/setup-panel 等）是各页 scoped 复制粘贴，不是全局；新页面复用这些类名时必须把对应样式一并复制，或改用全局类。`.settings-scroll` 必须带 `overflow-y:auto`，否则页面内容溢出无法滚动。

## 参考项目继续调研（2026-09-01，第二波）
- 用户问"还有可以参考的吗"，重新抓取 H3-Codex-Drama README_zh + ArcReel 文档中心，梳理出尚未落地的借鉴点（按优先级）：
  - P1 **工程账本/可复现导出（project.yaml）**：每集记录提示词/输入哈希/工作流/prompt_id/take/选片/资产引用，支持断点续作+可复现导出。当前只有 asset_versions 图片版本，缺全链路账本。
  - P1 **逐镜路由（shot routing）**：每镜显式决策 T2V/I2V/R2V + 决策原因。auto-pipeline 已有 referenceMode multiple/single 和首帧/关键帧拼接，但缺"路由决策"显式产物。
  - P2 **视觉图谱**（景别/构图/运镜/灯光，由 Adviser 图谱驱动提示词）；**QC 报告交付物**（联系表+媒体信息+QC 报告+时间线导出文件，现只有 JSON）。
  - P3 **原生音频生成**（环境声/BGM/动物声，非仅配音）；**Profile 预设模板**（内置 tiktok-short-drama/commercial-ad 一键创建）；**引导/快速模式**切换（关键节点暂停审核 vs 自动继续）。
- 已建议用户优先落地 P1 两项（工程账本 + 逐镜路由），等待用户确认。

## P1 落地：工程账本 + 逐镜路由（2026-09-01 深夜，对齐 H3-Codex-Drama project ledger / shot routing）
- **逐镜路由决策**：`storyboards` 表新增 `route` + `route_reason` 列（schema.ts + db/index.ts ensureColumn）。新服务 `services/shot-router.ts`：`decideRoute(sb)` 按控制媒体类型决策——`text_to_video`（纯文字）/`first_frame_to_video`（首帧图）/`first_last_frame`（首尾帧）/`reference_to_video`（参考图/参考视频/音频，reference_images 或 keyframeImage 时）/`video_editor`（用户手动编辑素材）/`blocked`（资产缺失被门禁阻断），返回 `{ route, reason, anchor }`。路由决策同步写入 storyboards.route/route_reason。接入点：`auto-pipeline.ts` submitMissingVideos 每镜先 decideRoute 再构造 prompt；`video-generation.ts` generateVideo 兜底（分镜无 route 时现场决策并落库，幂等）。
- **工程账本**：新服务 `services/project-ledger.ts`：
  - `buildProjectLedger(dramaId, opts)`：聚合 drama→episodes→storyboards→video_generations（含 takes 选片）/image_generations→asset_versions→audio_generations→qc 记录，输出结构化账本。关键字段：每镜 prompt 原文（用户输入）、输入媒体哈希（首帧/尾帧/参考图按文件路径算 sha256，不存在时返回 null）、决策路由+原因、take 计数与 selected_take（composedVideoUrl 最新选片）、资产引用（character/scene/costume/weapon/prop id 与名称）、QC 得分、成本（按 provider/model 单价 × 时长）。
  - `exportLedgerMarkdown(ledger)`：渲染为可复现 Markdown 报告（生成时间/成本汇总/每镜表：编号、路由标签、提示词截断、哈希、QC、take、选片 URL），每镜 `## Shot #N` 段含完整 prompt + 哈希 + 参考图引用。
  - `checkProjectLedgerStale(dramaId, episodeId)`：逐镜比对 storyboards.script_hash vs episodes.script_hash，返回 `{ staleCount, stale: [{storyboardNumber, ...}] }`（与 script-fingerprint 同源，方便脚本复现旧产物）。
- **路由导出**：`routes/export.ts` 新增 `GET /api/v1/export/dramas/:dramaId/project-ledger`（query: `format=json|md&episodeId=`，默认 json）。JSON 返回完整账本对象；MD 返回可复现报告文本。`GET /project-ledger/stale` 返回指纹校验结果。
- **前端接入** `episode/[episodeNumber].vue`：
  - 导出面板剪映草稿按钮旁新增「工程账本」按钮（MD / JSON 两个，`doExportLedger`，复用 `exporting` ref 防抖，blob 触发下载 `drama-{id}-ep{n}-ledger.{md|json}`）。
  - 「指纹校验」按钮（`doCheckStale` 拉 `/project-ledger/stale`，toast 列出 stale 分镜号；staleCount>0 时按钮文案变 `stale×N`，title 显示 staleSummary）。
  - 镜头概览行（exp-row）新增路由徽标 `routeTag(sb.route)`（T2V/I2V/FL2VA/R2V/Edit/⛔，title 显示 route_reason），样式 `.route-tag`（9px mono、cyan 边框浅底）。
- **验证**：backend tsc 0 错误 + lint 0；冒烟（脚本已删）——账本 JSON 15.9KB、MD 报告可读、哈希字段存在、stale 扫描 0；前端 5 处改动点全部落地。前端该文件 1000+ lint 报错均为 Volar `<script setup>` 假报错，新代码区无新增真实错误。
- 注意：路由字段名为 `route`/`route_reason`（snake_case，非 camelCase）；stale 校验与 script-fingerprint 共享 episodes.script_hash。

## P2 落地：视觉图谱 + QC 报告交付物（2026-09-01 深夜，第二波参考项目调研收口）
- **视觉图谱**（对齐 Adviser 图谱驱动提示词）：新建 `shared/visual-graph.ts`——四类知识节点（shot_size 8 / composition 9 / movement 13 / lighting 11），每节点 zh 别名列表→en 英文电影术语+usage+pairsWith。`matchVisualTerm` 精确匹配优先→包含匹配兜底（与 camera-movement-guides 同策略）；`resolveVisualTerm` 单字段翻译、`resolveVisualTerms` 按类别过滤（非目标类别命中保留原文）、`listVisualTerms`/`getVisualGraph`、`buildVisualGraphGuidance(genre, style)` 按风格特征词推荐图谱节点（悬疑→低光/框架/缓慢推镜，爱情→柔光/暖光，武侠→环绕/逆光，科幻→冷光/滑动变焦，动作→手持/对角线 等 6 类）。注入：agents/index.ts appendStyleProfile 同步 require（非 await import）storyboard_breaker 分支读 schema.dramas genre/style 追加引导；prompt-utils.ts buildStoryboardImagePrompt 的 shotType/angle 拼 `Camera:`、buildStoryboardVideoPrompt 的 movement 拼 `Camera movement:` 均先经 resolveVisualTerm 翻译。路由 `routes/visual-graph.ts`：GET /（图谱全量，category 过滤）/resolve（text→en）/terms/guidance（drama_id 或默认），挂载 /api/v1/visual-graph。
- **QC 报告交付物**（对齐 H3-Codex-Drama QC 交付：联系表+媒体信息+QC 报告+时间线）：新建 `services/qc-report.ts`——`buildQcReport`（聚合 drama→episodes→storyboards→每镜最新 video_generation + video_quality_check，ffprobe fallback 媒体规格，timeline 按累计时长排起始秒，summary 含平均分/达标<60 未达标）；`buildQcReportMarkdown`（分镜明细表+问题清单+时间线汇总）；`buildContactSheetHtml`（每镜首尾帧缩略图 grid+元信息+QC 色标，可打印/PDF）；`buildQcReportHtml`（MD 转 pre 包）；`buildMediaInfoReport`（纯规格）；`buildQcReportFiles`（三文件清单）。路由 `routes/export.ts`：GET /export/dramas/:id/qc-report?format=json|md|html&episodeId= 与 /contact-sheet?episodeId=。
- **前端** `episode/[episodeNumber].vue`：导出面板新增「QC 报告」「联系表」按钮（doExportQcReport/doExportContactSheet 复用 exporting ref 防抖+blob 下载）；镜头概览行新增 `shotTag(sb)` 视觉图谱徽标（显示景别/机位/运镜缩写）+ .shot-tag 黄色样式。
- **验证**：tsc 0 错误 + lint 0；冒烟 37 断言全 PASS（四类计数/术语翻译含别名与包含匹配/'缓慢推镜'→dolly in/类别过滤/6 风格引导/真库剧 #1「剑噬天下」20 分镜 QC 报告构建+三交付物）；路由冒烟 12 断言全 PASS（visual-graph 6 端点含 400/404 + export 6 端点含 json/md/html/contact-sheet）。前端 compiler-sfc script(405 bindings)/template/style 全编译 OK。
- **执行命令坑**：PowerShell 下 `npx tsx` 报错被吞（MODULE_NOT_FOUND），根因 shell 工作目录在项目根而非 backend；解决=绝对路径 `node <backend>\node_modules\tsx\dist\cli.mjs <script>`，输出加 `2>&1 | Out-String -Width 250`。tsc 同理 `node <backend>\node_modules\typescript\bin\tsc --noEmit -p <backend>\tsconfig.json`。
- 至此第二波参考项目调研 P1（工程账本+逐镜路由）与 P2（视觉图谱+QC 交付物）全部完成。

## 三级画风体系：全局 → 剧集 → 角色（2026-09-01）
用户要求"全局、每个剧集、每个人物都要有风格选择"。在画风统一收口基础上补全三级：
- **角色级**：characters 表新增 `style` 列（TEXT 可空，空=跟随剧集）；CharacterEditor.vue「图片生成提示词」区加「画风」下拉（跟随剧集 + 6 画风）；PUT 白名单加 'style'。
- **剧集级**：dramas.style 已有（创建时可选）；补「编辑剧集」弹窗（drama/[id]/index.vue 头部「编辑剧集」按钮，可改标题+视觉风格）。
- **全局级**：新增 `app_settings` KV 表 + `GET/PUT /api/v1/app-settings` 路由（key=art_style）；settings.vue 新增「画风设置」tab（6 画风卡片单选+保存）。
- **解析链**：角色 style → 剧集 style → 全局 art_style → realistic。后端实现：characters.ts `getEffectiveArtStyle(charStyle, dramaStyle)`；dramas.ts 创建时 `body.style || getGlobalArtStyle()`；extract-tools.ts 兜底 prompt 也用 `dramaRow?.style || global || null`；app-settings.ts 导出 ART_STYLE_KEYS 白名单（realistic/anime/ghibli/cinematic/comic/watercolor，与前端 index.vue/CharacterEditor/drama 编辑弹窗/artStyleOptions 一致）。
- **验证**：后端 tsc 0 错误；3 个 SFC 用 vue/compiler-sfc 编译通过（IDE lint 报的"找不到名称"是语言服务 stale 缓存，实际文件正常）；迁移幂等。
- 注意：新表/新列在**重启后端**后生效（运行中进程不会热重载）。

## 角色立绘画风统一收口（2026-09-01，动漫/真人混杂修复）
- **现象**：同一剧不同角色生成的立绘时而动漫、时而真人，画风不统一。
- **根因**：① `customPrompt`（body.prompt / char.customPrompt）完全绕过 `buildCharacterImagePrompt` 统一风格标签（stylized illustration, not photorealistic），手写 prompt 无画风约束；② `dramas.style`（realistic/anime/ghibli/cinematic/comic/watercolor）此前只注入场景/分镜/视频，**未注入角色立绘**；③ 负面提示词未按画风排除对立风格（写实时不排除动漫、动漫时不排除真人）。
- **修复**（后端三文件，前端零改动）：
  - `shared/prompt-utils.ts`：新增 `DRAMA_ART_STYLE_MAP`（6 画风→英文描述）、`DRAMA_ART_NEGATIVE_MAP`（各画风对立风格负面词）、`buildCharacterArtStyleSuffix(dramaStyle?)`（统一画风后缀，无 dramaStyle 时回退原插画标签）、`buildCharacterNegativePrompt(dramaStyle?)`；`buildCharacterImagePrompt`/`buildEquipImagePrompt` 增加 `dramaStyle?` 参数。
  - `routes/characters.ts`：新增 `getDramaStyle`/`resolveCharacterPrompt`/`resolveCharacterNegative` 三 helper；generate-image / three-views / equip / batch 四入口统一收口——customPrompt 存在时也强制追加 `buildCharacterArtStyleSuffix(dramaStyle)`，负面词用户未填时按画风给默认。
  - `agents/tools/extract-tools.ts`：save-characters-begin 读 drama.style，兜底 prompt/negative 注入画风（agent 生成的 image_prompt 保持纯净，出图时由路由自动补画风）。
- 分镜 `getStoryboardCharacterAppearances` 用结构化字段（buildCharacterAppearanceText）非 customPrompt，不受影响；分镜/视频构建器本就带 dramaStyle + VISUAL_STYLE_MASTER，与角色立绘对齐。
- 验证：lint 0 错误；tsx 三文件模块加载 OK。前端 drama.style key 与映射表一致（index.vue L307 `styles` 对象）。
- 提醒：同一剧尽量固定同一生成模型（前端每按钮可换模型，模型差异仍是潜在风格漂移源）。

## ep1 剧本被「版本A」覆盖 + AI 改写失效修复（2026-09-01 深夜）
- **现象**：用户报告「AI 改写都不展示了」「只有一个版本A啥都不展示了」——ep1 `script_content` 被覆盖为字面值「版本A」（3 字符），前端剧本区空白。
- **溯源**：所有 `script_content` 写入点（前端 `saveScr()`、agent `save_script` 工具）均不产生「版本A」；该值来自 `saveScr()` 本地编辑路径，非系统自动生成。`content`（3406 字符原始内容）未受影响。
- **根因（AI 改写 13:04 全失败，3-5 分钟耗尽 fallback）**：
  1. `agent_configs` 表 5 个 agent 全部硬编码 `model='deepseek-chat'`（2026-08-07 创建时默认值残留）。`runAgentWithInstructions` 将 dbConfig.model 强制前置 → 在 MiniMax 端点上打 `deepseek-chat` → Not Found。
  2. `ai.ts getTextProviderBaseUrl` **缺少 minimax case** → 返回裸 `https://api.minimax.chat`（缺 `/v1`）→ MiniMax-Text-01/abab6.5s-chat 全部 Invalid JSON response。（`adapters/openai-compatible-text.ts` 的 `PREFIX_BY_PROVIDER` 已有 `minimax:'/v1'`，但 agent 链路走的是 `ai.ts`，两处不一致）
- **修复**：
  1. **数据恢复**：从主 .db 旧副本（`data/drama.db` 2026-08-28 checkpoint）提取 ep1 旧剧本（2665 字符，5 个场景完整分场剧本），写回 `script_content`，重算 `script_hash=174e07ddac400cdd`。20 个分镜 scriptHash 均 null（门禁启用前产物，向后兼容不阻断）。
  2. **代码修复**：`ai.ts getTextProviderBaseUrl` 补 `minimax → joinProviderUrl(baseUrl,'/v1','')`（与 adapter 层对齐）。
  3. **配置清理**：`agent_configs` 5 条 `model='deepseek-chat'` 全部置 NULL → Agent 改用 `ai_service_configs` text 的 models（MiniMax priority=300：`MiniMax-Text-01, abab6.5s-chat`）。
- **验证**：MiniMax 真实调用 `POST https://api.minimax.chat/v1/chat/completions` 返回 200，`MiniMax-Text-01` 正常回复（此前同链路 3 个模型全失败）。
- **清理**：全部 21 个 `smoke-*` 临时文件已删除（db-p2..p11、restore-check、restore-ep1、verify-text、extract-old-script、old-content/old-script.txt、restore.db(-shm/-wal)、softdelete.mjs、wal.py）。
- **经验**：文本 provider 端点前缀必须同时维护在 `ai.ts getTextProviderBaseUrl`（agent/LLM 链路）与 `adapters/*-text.ts PREFIX_BY_PROVIDER`（service 链路），两处要保持一致；agent_configs.model 若指向已不存在的模型会强制前置导致全链路 fallback 失败，删除/置空让 textConfig.models 生效最稳妥。

## 角色/装备图 prompt 信息注入 + 三视图/设定图长图修复（2026-09-01 深夜）
用户反馈：① 图片生成提示词未根据智能拆分信息生成正反向提示词 ② 三视图是一张长图包含正面/侧面/背面 ③ 服装/武器/首饰设定图也是长图。
- **根因**：`customPrompt` 存在时 `buildCharacterImagePrompt` 被整体绕过，服装/武器/首饰字段丢失；三视图每 view 的 prompt 都写「正面全身三视图」→ 模型把三视角拼一张图；装备 `buildEquipImagePrompt` 用 `design sheet`/`concept art` 词 + 无 single view 约束 + 负向未排除多视图。
- **改动（backend/src/shared/prompt-utils.ts + routes/characters.ts）**：
  1. 新增 `buildCharacterVisualsClause(char)`：抽服装/武器/首饰正向英文子句；`buildCharacterImagePrompt` 复用。
  2. `resolveCharacterPrompt` 增加 `visualsClause` 参数——customPrompt 存在时也强制追加视觉字段（`${user}, ${visualsClause}${画风后缀}`）；generate-image / three-views 两路由均传入 `buildCharacterVisualsClause({...char,...body})`。
  3. 三视图：新增 `THREE_VIEW_PROMPTS`（front/side/back 各一段英文单视角描述：`front view, full body, character facing the camera directly, single view portrait, head to toe, only one figure` 等）+ `buildThreeViewNegative(dramaStyle)`（画风负面 + `multiple views, multi-panel, character sheet, model sheet, turn-around sheet, split image, grid layout, side-by-side, two figures, three figures`）。路由逐 view 用 `${basePrompt}, ${THREE_VIEW_PROMPTS[view]}`，用户未填负向时用 buildThreeViewNegative。
  4. 装备：`buildEquipImagePrompt` 去掉 `design sheet` 词，clothing 改 `detailed costume design` + `front view of the costume, single garment, single view, not worn by a character`；weapon/accessory 加 `single weapon/piece of jewelry, single view, isolated on clean background`；末尾统一 `single object only, single view, no multiple angles, no turnaround sheet, no text, no labels, no watermark`。新增 `buildEquipNegative(dramaStyle)`（+ `concept sheet, design sheet, duplicate object, floating parts` 等），equip 路由用户未填负向时用它。
- **经验**：生图模型对 `sheet`/`design sheet`/`三视图` 等词极敏感会输出多视角拼接长图；要保证单图输出必须同时在正向写 `single view / only one figure` 约束 + 负向排除 multi-panel/turnaround。
- 验证：backend lint 0 错误；tsc 未跑（用户跳过长命令），改动均为纯函数替换。

## 角色编辑页提示词生成增强 + 布局优化（2026-09-01 深夜，用户反馈"角色编辑展示的不对"）
用户反馈：正反向提示词未区分、不区分角色/道具/服装、没有"根据形象设定生成提示词"按钮、字段回填空、三视图/设定图鉴空白。
- **后端** `routes/characters.ts` 新增 `POST /:id/generate-prompt`（不落库）：body `{ type: 'character'|'clothing'|'weapon'|'accessory', 可选表单覆盖字段 }`，合并 `{...char,...body}`（camelCase 优先兼容 snake_case）→ `getEffectiveArtStyle(merged.style, getDramaStyle())` → character 走 `buildCharacterImagePrompt`+`buildCharacterNegativePrompt`，装备走 `buildEquipImagePrompt(type)`+`buildEquipNegative`，返回 `{type,prompt,negativePrompt}`。
- **前端** `useApi.ts` 加 `characterAPI.generatePrompt(id, data)`。
- **前端** `drama/[id]/character/[characterId].vue`：
  1. 提示词区重构：对象类型切换（角色立绘/服装设定/武器设定/首饰设定，`.pt-btn` 药丸按钮组）+「根据形象设定生成提示词」按钮（`generatePromptByType`，传表单未保存最新值，回填正反向提示词）；正/反向提示词分色（`.plabel-pos` 绿 / `.plabel-neg` 红 + 对应 textarea 边框/背景色带）。
  2. 布局：左侧预览列 360px→380px；`.sec-head`（标题+按钮 flex）；三视图/设定图鉴空态补「\n未生成」两行占位（`.tv-empty` white-space:pre-line）。
  3. 回填健壮化：新增 `pick(k)` helper 统一兼容 camelCase/snake_case，voiceModel 保持 `c.voiceModel || pick || 'speech-2.8-hd'` 默认。
- 验证：backend `npx tsc --noEmit` 0 错误（中途修过 `merged: Record<string,any>` spread 类型推断问题，改 `any` 解决）；前端 lint 0 错误。
- 注意：新路由需重启后端生效；`useApi.req` 返回 `json.data ?? json`，`generatePrompt` 返回值直接是 `{type,prompt,negativePrompt}`。

## 提示词按对象类型独立存储（2026-09-01 深夜，用户反馈"同一输入框无法保存和区分"）
用户反馈：对象类型切换（角色立绘/服装/武器/首饰）后提示词仍是同一个输入框，保存无法区分。
- **数据模型**：characters 表新增 6 列——`clothing_prompt`/`clothing_negative_prompt`、`weapon_prompt`/`weapon_negative_prompt`、`accessory_prompt`/`accessory_negative_prompt`（schema.ts + db/index.ts ensureColumn 迁移）。角色立绘继续用 `custom_prompt`/`negative_prompt`。
- **后端** characters.ts：PUT 白名单加 6 字段；generate-equip-image 按 type 读取对应字段（`char[clothingPrompt/weaponPrompt/accessoryPrompt]`，负向同理），body.prompt 优先。
- **前端** characterId.vue：`promptFieldMap`/`negFieldMap`（type→form 字段）+ `currentPrompt`/`currentNegative` computed get/set 代理（模板 v-model 改绑代理）；onMounted 回填 6 字段（pick 兼容 snake_case）；persistForm 保存 6 字段；generatePromptByType 回填到当前 type 对应字段；generateEquip 传对应字段；正向/反向标签显示当前对象名（如「服装设定 · 正向提示词」）；hint 注明四类独立保存。
- 验证：后端/前端 lint 0 错误（tsc 被用户跳过）。新列/新路由需重启后端生效。

## 详情页布局 + 四类提示词区分 + 角色定位（2026-09-01 用户三连反馈）
用户反馈：①详情页左右两列都能滚动错乱；②角色定位字段是否保留；③四类「根据形象设定生成提示词」内容雷同很荒唐。
- **布局**：`.preview-col` 的 `position: sticky; top:72px` 是左右滚动不同步的根因，已移除改为 `align-self:start` 统一随 `.char-detail`（height:100%;overflow-y:auto）滚动。外层 shell/content 均为 overflow:hidden，滚动容器唯一，无嵌套滚动。
- **提示词区分**：`buildEquipImagePrompt`（prompt-utils.ts）重构——装备类不再注入整段角色外貌/核心特征/角色名（那会导致与立绘雷同），改为聚焦对象本体：服装=完整衣物剪裁/面料/配色+挂装无人；武器=武器整体/金属质感/雕刻+无手持；首饰=单件近景/宝石/金属工艺+无佩戴。画风尾：有 dramaStyle 时与立绘一致，无则用 `stylized concept art`（不用含 character portrait 的默认尾）。`buildEquipNegative` 追加排除 `person/human figure/hands/body parts`。
- **角色定位（role 字段）结论**：建议保留。被 voice-tools.ts list_voices（音色分配参考）、storyboard-tools.ts appearance_hint 兜底、episodeNumber.vue inferCharRoleTag（角色卡片标签）消费，非死字段。前端 UI 未动。
- 提示词区 hint 文案补充「四类各自聚焦对象」说明。lint 0 错误。

## 智能拆分服装/武器/首饰准确性修复（2026-09-01）
用户反馈 `POST /characters/:id/auto-split-visuals`（splitCharacterVisuals，text-generation.ts）拆分不准确。根因：AI 结果直接覆盖（有杂质但不为空时规则无法修正）+ AI prompt 只有 1 个理想短示例 + 规则词表有盲区。
修复（三管齐下）：
1. **AI prompt 强化**：分类边界明确（配饰归 accessories，腰带/靴/帽归 clothing；剑鞘/刀鞘等武器配件不单独输出）；3 个贴近真实长文本（神态/环境与装备混排）few-shot；明确禁止把神态/环境/动作带进字段、去掉「身着/手持」等动词引导。
2. **规则优先，AI 清洗后补盲区**：`splitVisualsByRules` 先拆（稳定精确），`cleanVisualFragment` 清洗 AI 值（逐逗号分段去动词/数量词引导+冗余后缀），融合时规则命中则用规则值，规则空才用清洗后 AI 值——修复「AI 有杂质覆盖规则」问题。
3. **词表扩充**：服装加西装/礼服/外套/汉服等，武器加手枪/狙击枪/狼牙棒/飞镖等，配饰加红绳/戒指/扳指/璎珞等；前缀表加「一柄/一把/一串/手里拄着/手腕上系着」等数量词与动词引导。
实测用户文本（古风少年）输出：clothing=朴素古意的青色长衫、weapons=三尺青锋、accessories=简单束带，全部干净无杂质。

## 装备提取补盲区（剑鞘/锋芒）+ 三视图改单张横图（2026-09-01 深夜第二轮）
用户反馈：①装备提取丢了「剑鞘古朴无华，却蕴含着慑人的锋芒」，且出现过幻觉「三尺青芒」；②三视图应是一张横向长图（正/侧/背合成），现在搞成三张图；③装备设定图也应是长方形三视图。

**装备提取（text-generation.ts splitCharacterVisuals）**：
- 根因：融合策略「规则优先、AI 补盲」导致 AI 完整结果被规则值覆盖；AI prompt 规则 4 明令删剑鞘；AI 输出无防幻觉校验（「三尺青芒」原文不存在却回填）。
- 修复：① prompt 规则 2/4 改为保留完整物品描述（含剑鞘/锋芒等细节，配件不单独拆条但细节并入 weapons）；few-shot 新增示例 4（完整武器描述）；② 融合策略改「AI 优先 + 原文一致性校验，规则兜底」——`appearsInSource()` 逐逗号段校验每个片段都出现在原文（seg.length>=2）才采用 AI 值，否则回退规则；③ 规则兜底增强：`SPLIT_WEAPON_BANNED` 去掉剑鞘/刀鞘（只留动作词），新增 `SPLIT_NOISE_KEYWORDS`（神态/环境/动作杂质），`pick` 改为合并前后相邻描述性子句（遇噪声/他类关键词即停，累计≤34 字，同类去重），并跨字段排除（clothing 合并时排除武器/首饰关键词等）。
- 实测（真实调用 LLM MiniMax-Text-01）：weapons=「三尺青锋，剑鞘古朴无华，却蕴含着慑人的锋芒」，clothing=「朴素古意的青色长衫」，accessories=「简单束带」，完整无幻觉。

**三视图一张横图**：
- `prompt-utils.ts`：新增 `THREE_VIEW_COMBINED_LAYOUT`（one single wide horizontal image, same character three times: front left / side middle / back right）；`buildThreeViewNegative` 改为排除 split panel/grid/frame/文字/多余人（不再排除 multiple views/turnaround）；`buildEquipImagePrompt` 三类型全部改横版三视图（same item three times side by side，clothing front/back/side，weapon & accessory front/side/top，no split panel borders/grid frames）；`buildEquipNegative` 改为排除 different items/duplicate inconsistent object/split panel/grid/frame/文字（不再排除 multiple views）。
- `characters.ts` generate-three-views：循环 3 张（viewType=front/side/back）改为单张（viewType='combined'），views 参数仍兼容校验；返回 `{count:1, results:[{view:'combined',...}]}`。
- `image-generation.ts`：完成回调条件加 `record.viewType==='combined'` → 写入 `threeViews.combined`。
- 前端 `characterId.vue`：模板优先显示 `threeCombinedUrl`（combined 单张横图，`.tv-combined` 16/9），无 combined 降级显示旧 front/side/back 三格；轮询 done 判断兼容 `views.combined?.imageUrl || 三张齐`；`.equip-cell .tv-thumb` 改 aspect-ratio 16/9 横向长方形。
- 验证：lint 0 错误；tsx 脚本实测 prompt 组装（三视图组合排版 + 三类装备横版三视图 + 对应负面）全部正确。tsc 全量编译被用户跳过。
- 经验：生图模型对 turnaround/three-view 词敏感易输出多视角拼接，用「one single wide horizontal image + same X shown three times side by side + no split panel borders/grid frames」组合控制单张长图。

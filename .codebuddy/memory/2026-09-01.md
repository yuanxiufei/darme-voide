# 2026-09-01

- 从 MiniMax Design 应用获取全部 skill，已复制到项目 `skills/minimax/`：
  - `installed/`：用户安装的 20 个 skill（源 `C:\Users\Administrator\.hub-global\skills\`），包括 chinese-style-short-drama-generator、short-drama-screenwriter/series-writer、n-storyboard、multi-shot、voice-clone、voiceover-direction、film-* 系列、skill-creator/reviewer 等。
  - `builtin/`：内置 9 个 skill（源 `D:\app\minmaxh3\MiniMax Design\current\resources\agent-profiles\v2\config\skills\`），含 h3-visual-design、video-deconstruct、anime-game-pv、brand-ad、cool-music-video、ui-motion、koc-video、cinematic-title-sequence、education-studio。
- MiniMax Design 数据根目录：`C:\Users\Administrator\.hub-global\`（海外版 overseas → -global 后缀）；skill 加载路径规则（源自 opencode-plugin-hilo/dist/hilo.js）：用户安装 `~/.hub-global/skills`，内置 `resources/agent-profiles/v2/config/skills`，知识库在 `~/.hub-global/.config/knowledge/`（含 model-prompts/minimax-h3.md 等）。
- 应用版本内置 skill 与用户安装 skill 结构：SKILL.md + meta.yaml + references/。

## MiniMax skill 融入项目 skill 体系（2026-09-01）
- 后端 `backend/src/agents/skills.ts`：`AGENT_SKILL_MAP` 默认绑定新增 minimax skill——
  - script_rewriter ← short-drama-screenwriter, short-drama-series-writer
  - extractor ← film-assets
  - storyboard_breaker ← film-shot, storyboard, coordinate-camera-control-designer
  - voice_assigner ← voice-clone, voiceover-direction
  - grid_prompt_generator ← film-reference-prompt-writer, film-style-picker, video-deconstruct-analyzer, builtin/video-deconstruct, face-warp
  - 新增导出 `listSkillIds()`：扫描 skills/ 目录全量 id（含嵌套 minimax/installed/xxx、minimax/builtin/xxx）
- 后端 `backend/src/agents/creator.ts`：`AVAILABLE_SKILL_IDS` 由 AGENT_SKILL_MAP 推导改为 `listSkillIds()`，配置生成器可全量选择所有 skill
- 前端 `frontend/app/pages/settings.vue`：`DEFAULT_AGENT_SKILLS` 同步后端映射；Skills tab 左侧新增「MiniMax 技能库」入口（selectedAgent='minimax'，显示 minimax/ 下全部 29 个 skill，不可在此新增）；顺手修复 skill 路径显示 bug（`skills/{{s.id}}/SKILL.md`，原 `skills/{{agentType}}/{{s.id}}` 会重复前缀）
- 关键机制：DB `agent_configs.skills` 目前全为 null → 后端回退 AGENT_SKILL_MAP 默认映射即生效；前端绑定面板 availableSkills 为 API 全量，可手动把任意 minimax skill 绑定到任意 agent
- 验证：`listSkillIds()` 返回 34 个（5 自有 + 29 minimax）；`GET /api/v1/skills` 返回 34 个，minimax 29 个正常列出

## Skill 全量管理增强（2026-09-01 第二轮）
- `backend/src/agents/skill-parser.ts`：`SkillMetadata` 增加 `workflows: string[]`（frontmatter `workflows:` 可选声明适用阶段，缺省空数组）
- `backend/src/routes/skills.ts` GET `/` 列表项扩展字段：`category`（core/minimax-builtin/minimax-installed/custom）、`workflows`（frontmatter 声明）、`boundAgents`（反向查询绑定该 skill 的 agent_type）、`phases`（工作流阶段 = boundAgents 对应阶段 + workflows 声明去重）
- 反向绑定查询 `loadSkillBindings()`：DB `agent_configs.skills` 优先（用户显式配置时先移除该 agent 默认绑定再重建）；DB 为 null 的 agent 回退 `AGENT_SKILL_MAP` 默认映射
- 工作流阶段映射 `AGENT_WORKFLOW_PHASES`：script_rewriter→剧本编写 / extractor→资产提取 / voice_assigner→音色分配 / storyboard_breaker→分镜拆解 / grid_prompt_generator→画面提示词
- 前端 settings.vue Skills tab：新增「📦 全部 Skill」入口（selectedAgent='all'，currentSkills 全量、新增按钮隐藏）；skill 卡片显示来源分类 badge（skill-cat-badge core/minimax-installed/minimax-builtin/custom）+ 绑定 Agent 标签（🤖）+ 工作流阶段标签（⚙️）；删除 minimax 前缀 skill 时二次强确认
- 验证方式坑：PowerShell 直连 5789 可能连到残留旧进程导致字段为空；可靠验证 = `npx tsx` 脚本里 `app.request('/')`（Hono 内置，路由未挂 /api/v1 前缀时路径用 `/`）
- 验证结果：34 个 skill，categories={core:5, minimax-builtin:9, minimax-installed:20}；film-shot→[storyboard_breaker]/分镜拆解、short-drama-screenwriter→[script_rewriter]/剧本编写、voice-clone→[voice_assigner]/音色分配 正确

## 头部菜单入口（2026-09-01 第三轮）
- `frontend/app/layouts/default.vue` 头部导航新增三个一级入口：Skill→`/settings?tab=skills`、模型→`/settings?tab=ai`、Agent→`/settings?tab=agents`；「设置」入口 active 条件改为 `route.path==='/settings' && !route.query.tab`；新增 `isSettingsTab(id)` 判断 query 匹配
- `frontend/app/pages/settings.vue` 支持 `?tab=` 定位：`syncTabFromQuery()` 读取 route.query.tab，匹配 baseTabs/advancedTabs 则切换；agents/skills 自动 `showAdvanced=true`；watch route.query.tab 变化同步
- 验证：`npx nuxt build` 成功；settings.vue 的 LSP「找不到名称」误报是大型 .vue 文件 template↔script 关联解析问题，构建不受影响

## Skill/模型/Agent 脱离设置菜单成为独立页面（2026-09-01 第四轮）
- **根因**：第三轮只是把链接指向 `/settings?tab=`，页面仍渲染在设置页内，不符合「脱离设置菜单」诉求
- **拆分**：`settings.vue`（原 2533 行）拆为 4 个页面——
  - `pages/skills.vue`：Skill 管理（左侧来源栏：全部/各 Agent/MiniMax 技能库；右侧列表+编辑+新增弹窗+空状态）
  - `pages/models.vue`：模型管理（Quick Setup/本地 Ollama/GPU 显存/Token 统计/按服务类型配置列表 + AI Config 弹窗 + Quick Preset 弹窗）；`applyQuickPreset` 已移除 `loadAgents()` 调用
  - `pages/agents.vue`：Agent 配置（5 个 agent 卡片 + 编辑表单 + Skill 拖拽绑定面板）
  - `pages/settings.vue`：精简为仅「生成历史 + 数据存储」两 tab
- **导航**：`default.vue` 三链接改为直接 `to="/skills"` `/models` `/agents`，active 用 `route.path.startsWith()`；删除 `isSettingsTab`；设置 active 恢复 `route.path === '/settings'`
- **样式**：各新页面 scoped style 从原 settings scoped 复制（含 `.settings-scroll/.settings-head/.settings-brand` 等基础类 + 各自面板类）；skills.vue 空状态样式 `.step-empty` 等从 `drama/[id]/episode/[episodeNumber].vue:4531` 复制
- **验证**：dev server（3013）运行中，4 页面 `/@fs/.../pages/*.vue` Vite 编译全部 200；SSR 关闭（nuxt.config ssr:false）故 curl 只见骨架，浏览器渲染正常；LSP「找不到名称」仍为陈旧缓存误报（各页面 template 引用的变量在各自 script 中均已有定义），以 Vite 编译 200 为准
- **Lint 陷阱**：`read_lints` 对拆分后的大文件会返回基于旧版本的缓存诊断，必须用实际编译（Vite @fs 模块请求 / nuxt build）验证

## 项目完善度检查
- 后端 `npm run typecheck`（tsc --noEmit）通过，0 错误。
- 前端 `npm run build`（Nuxt）通过，无 lint 错误。
- 待完善项：
  1. `configs/config.yaml` 未创建（仅 example），无 `.env`；代码有默认值降级可跑。
  2. README 引用不存在的 `CHANGELOG.md`；`git clone` 是占位 URL。
  3. 根目录残留调试/临时文件：`_rewrite_out.json`、`_wf_dump.txt`、`backend.log`、`backend.err.log`、`frontend-dev.log`。
  4. 大量未提交改动（git status：删 memory 历史文件、改后端多文件）。
  5. npm 警告：未知 project config `package-manager`。
- 技术栈实测：后端 Hono + tsx（默认端口 5789），前端 Nuxt 3（端口 3013）——与 MEMORY.md 中"uvicorn 18002 / studio-client 5175 / studio-admin 5173"严重不符，MEMORY.md 该章节为过时信息（来自早期 Python/Vite 版本）。

## 完善项处理（按序）
1. 清理临时/调试文件：删除根目录 `_rewrite_out.json`、`_wf_dump.txt`、`backend.log`、`backend.err.log`、`frontend-dev.log`；backend `dev.log`、`_repro_tools.out.log`；scripts `_rewrite_result.json`、`_rewrite2.err`、`_rewrite2.log`、`_rewriter.err.log`、`_rewriter.out.log`（共 12 个）。
2. 生成 `configs/config.yaml`（复制 example，debug=true）；README 两处修正：`git clone` 占位 URL → 通用提示；删除指向不存在 `CHANGELOG.md` 的「更新日志」章节。
3. `.gitignore` 增加 `__pycache__/`、`*.pyc`（`*.log` 原已存在）。
- 已清理：`_` 前缀一次性调试脚本共 15 个（backend `_db_probe.mjs`/`_inspect.mjs`/`_probe2.mjs`/`_repro_full.ts`/`_repro_tools.mjs`；scripts `_db_probe.mjs`/`_find_api_fmt.py`/`_parse_wf.py`/`_probe2.mjs`/`_run_rewriter.mjs`/`_test_ollama_tools.mjs`/`_tmp_db.py`/`_tmp_db2.py`/`_tmp_trace.py`/`_update_cfg.mjs`）。已 `search_content` 确认无正式代码引用。
- `scripts/` 现仅保留 5 个正式工具：`model_manager.py`（模型下载/管理）、`sd_h3_pipeline.py`（sd.cpp 编排）、`sd_h3_compat_probe.py`（GGUF 兼容探测）、`h3_install.py`（H3 安装）、`migrate_models.ps1`（模型迁移）。
- 新增 `scripts/README.md`：记录 5 个工具职责、用法、依赖关系（pipeline/h3_install 复用 model_manager 的 download_url/check_bin；compat_probe 独立零依赖）。

## 架构结论：后端是否换 Python？
- 结论：**不换**。维持「TS 后端（Hono，Web/业务层）+ Python（AI/GPU 计算层）」混合架构。
- 依据：backend 107 个 .ts 只管 HTTP 路由/SQLite/Agent/ffmpeg/Ollama spawn，与 Nuxt 前端同构；真正需要 Python 的 ComfyUI、`local_services/h3/server.py`、`scripts/*.py` 本来就已是 Python，且通过 HTTP(8765)/subprocess 与后端解耦。全换 Python 是纯成本无收益。

## Git 提交落盘（3 个逻辑提交）
- 修复 npm 警告：删除 `backend/.npmrc`（内容为无效配置 `package-manager=npm`，npm 不认该 key；包管理器声明应写 `package.json` 的 `packageManager` 字段）。
- 累积未提交改动拆 3 个逻辑提交：
  1. `7dcb02a` chore: consolidate memory and docs, fix npm config warning（24 文件 +293/-4014）
  2. `feb4031` feat: local model download and management（19 文件 +2844/-43）
  3. `d398bf2` feat: H3 video inference tooling（8 文件 +1604）
- 工作区现已干净。

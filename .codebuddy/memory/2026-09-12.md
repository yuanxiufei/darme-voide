# 2026-09-12

## 核验用户转来的「视频 Prompt 数据源清单」

用户转来另一份 AI 整理的 8 个数据源（GitHub / HuggingFace），说「可以直接抓取」。**先核验再动手，结果大部分不实。**

### 核验结果（GitHub REST API + HF API 经 hf-mirror）

**GitHub 侧：6 个仓库全部存在，但只有 1 个有数据**

| 仓库 | 星 | 体积 | 结论 |
|---|---|---|---|
| `Semonxue/awesome-video-prompts` | 14 | 4.88 GB | ✅ 真数据：3097 条，`content/prompts/YYYY-MM/*.md` 纯 Markdown+YAML，`description`=正文；**已归档、无 LICENSE** |
| `geekjourneyx/awesome-ai-video-prompts` | 78 | 24 KB | ❌ 空骨架：0 条提示词，3 个工具链接指向 `example.com` 占位 |
| `HitPaw-Official/awesome-ai-video-prompts` | 0 | 24 KB | ❌ 厂商营销仓库 |
| `Simonrh01/awesome-ai-video-prompts` | 1 | 4 KB | ❌ 空壳 |
| `fantasylights/ai-video-prompts` | 0 | 4 KB | ❌ 空壳 |
| `WangWenhao0716/TIP-I2V` | 42 | 31.9 MB | ✅ 真项目（ICCV 2025），数据在 HF |

**HuggingFace 侧：3 个全部存在**

| 数据集 | 真实规模 | 许可证 | 文本载荷 |
|---|---|---|---|
| `GokuScraper/seedance-2-prompts-datasets` | **8100+ 条**（非转述的 20 万+） | **CC BY 4.0 可商用** | **`metadata.jsonl` 仅 37.6 MB** |
| `Rapidata/awesome-text2video-prompts` | ~200 条 / 14 类（很小） | 待确认 | 极小 |
| `WenhaoWang/TIP-I2V` | **170 万条**（真实） | ❌ **CC BY-NC 禁商用** | Subset 10 万条 ≈ 746 MB |

### 关键教训

1. **转述的数量级普遍失真**：Seedance2「20 万+」实为 8100+（50GB 是配套视频）；Rapidata 被称"非常适合"实为 ~200 条。
2. **星数会骗人**：`geekjourneyx` 78★ 却 0 条数据；`Semonxue` 仅 14★ 却有 3097 条。
   → **核仓库必须看 `size` (KB) 和真实文件树，不能看 star。** 4 KB / 24 KB 的体积直接暴露空壳。
3. **别被仓库体积吓退**：Semonxue 4.88 GB 全是 `static/` 的封面与视频，
   `content/` 是纯文本 → `git clone --sparse` 只取 `content data`。
   Seedance2 的 50 GB 同理 → **只下 `metadata.jsonl`(37.6MB)** 就拿到全部提示词。
4. **许可证是硬约束**：TIP-I2V 是 CC BY-NC（禁商用），**不得进产品**；
   Seedance2 是 CC BY 4.0（可商用需署名）；Semonxue **无 LICENSE** → 仅内部参考。

### 环境事实

- **本机直连 `huggingface.co` 全部超时**（3/3），必须走 `hf-mirror.com`。
  项目 `scripts/model_manager.py` 已有三源切换 + 断点续传，大规模下载应复用而非另写。

### 产出

- 新增 `docs/video-prompt-data-sources.md`：逐源核验表 + 采集命令 + 许可证风险 + 落到 skill 的提炼映射。
- 未下载任何语料（待确认）；未提交。

### 工具踩坑（补充）

- `execute_command` 本轮**前两次被转后台跳过**，第三次成功。
  规律不明，但**纯网络轮询脚本较短时更容易成功**；输出仍应落盘再 `read_file`。
- 后台化的命令**确实会执行**：`.src-check.txt` 当时读取失败，但稍后清理时文件已存在。
  → **被跳过的命令要视为"可能已执行"，事后检查残留文件。**

---

## ✅ 已执行：下载 Seedance2 语料并提炼进 skill

**更正上条**：「未下载任何语料（待确认）」→ **已下载**（用户回复「好的」即放行）。
「8100+ 条」也系 README 概数，**实测 8755 条**、`metadata.jsonl` **37.58 MB**。

此集**不是多模型混合**：91% 是 Seedance 自家（2.0=6393 / 1.0=1552 / happy-horse-1=671 /
gemini-omni=138 / kling=1）。早前清单里「26 个模型」属于 Semonxue 那个仓库 —— 两者不可混，已在 docs 标注。

### 重要数据结构发现（下次直接复用）

- **提示词不在顶层字段**，藏在 `i18n.zh.p` / `i18n.en.p`。
  第一版探查脚本只扫顶层 keys，导致 `prompt-ish fields = []` 误判
  → **遇到陌生数据集，先完整 dump 一条记录，再设计抽取逻辑。**
- **`i18n.zh` 全量 8755 条都有**（中文正文 + 标题 `t` + 标签 `tags`），质量比预期好得多。
- **英文是平台扩写版**（中位 809 字 vs 中文 245 字）→ 统计必须只用 `i18n.zh.p`，
  否则长度与句式结论全错。
- `spec.duration` 有脏值（最小 -3.69e17、最大 887.75s）→ 用前按 `0 < d <= 300` 过滤。
- 落盘 `reference/prompt-corpus/seedance2/`（**`reference/` 已 gitignore，不污染仓库**）。

### 提炼结论（已写入 skill）

**核心判断：两批语料属于不同创作人群，写法不冲突，不能互相推翻。**
- PromptMart（国内短剧 / 漫剧作者）→ **时间码分段**为主。
- Seedance2（平台通用短视频）→ **散文式多段**为主（实测时间码仅 14%，作分段标题仅 3%）。

故 skill 第 6 节改为**并存两类** + 题材取舍表：有动作节拍（打斗/变身/特效）走 6.2 时间码；
写实/纪实/广告/情绪向走**新增的 6.10 散文式十段模板**。

**三条实测修正**：
1. 「一镜到底」仅 323/8755（≈4%）→ 从 §6.1 的**固定骨架降为可选项**。
   真正高频的是 旋转 2571 / 手持 2563 / 环绕 1225 / 推进 1049 / 定格 1024 / 跟随 1003。
2. 「杜绝美化」仅现 2 次 → 行业通用写法是**部位清单式锚定**
   （面部比例 / 眼型 / 下颌线 / 嘴唇 / 发型轮廓 / 发色分布 / 皮肤质感），比「五官脸型发型」更可执行。
3. **反 AI 感词族**（skill 原先完全没覆盖）：`不完美的自然构图` / `自动对焦不完美` /
   `逼真的环境瑕疵` / `可信的瑕疵` / `轻微手持不稳` / `自然高ISO颗粒` / `自动对焦呼吸`。
   → 这类**可控缺陷**比堆 `8k, ultra detailed` 更能压掉塑料感。

其它可复用写法：独立「负面提示：」末段（顿号并列反例）、情绪段 `《Mood》是…`、
**具体焦段 + 器材缺陷**（`200mm–400mm 视角` + `浅层自动对焦不一致`）优于笼统「电影感」、
十段模板只是**骨架**（情绪向可只留 ①②④⑤⑧⑩ 六段，全写满会失焦）。

### 产出

- `reference/prompt-corpus/seedance2/metadata.jsonl`（语料，gitignored）
- `reference/prompt-corpus/analyze{,2,3}.py` + `*.log`（分析方法与结果存档）
- `docs/seedance2-corpus-analysis.md`（**分析报告**：词频、结构特征、真实样例、skill 修正结论）
- `skills/prompt-style-library/SKILL.md`：§6 标题与导语扩写、§6.1 一镜到底降级 →
  可选项、§6.2 加适用范围注、**新增 §6.10**、§8 自检清单同步
  （444 行，frontmatter YAML 校验通过、章节编号连续）
- `docs/video-prompt-data-sources.md`：补实测数字、`i18n.zh` 结构说明、下载状态
- **MEMORY.md 已按系统要求整理压缩**（原文件超限被截断，重写为分区更紧凑的版本）

未提交。

### 分析脚本踩坑

- `any(w in x['p'] for x in rows)` 把 `w` 误写成 `x` → `NameError`。
  因输出用 `*>` 重定向到 log，终端只显示 `EXIT=1` → **跑分析脚本必须查退出码并读完整 log**，

  不能只看已打印的部分结果（否则会以为"部分成功"而漏掉后续统计）。

---

## 按用户要求「一定要方便使用、通用」做二次加工（skill + 报告）

用户对上一轮产出的唯一附加要求：**方便使用 + 通用**。据此做了 4 处改动。

### 1. skill §6 前置「入口」（原来要翻到章节末尾才能做决策）

原 §6 导语写「取舍表见 6.10」—— 决策表在章节最底部；且 6.3/6.4/6.5/6.6/6.7/6.9 其实是
**两种写法共用的通用件**，被埋在时间码序列里，走散文式时容易被误判为「不适用」。

→ 新增 **§6.0「30 秒上手：定写法 → 挂通用件 → 填骨架」**：第 1 步定写法（只看有无动作节拍，
拿不准默认散文式，占语料 86%）、第 2 步挂 6 个通用件（锚定 / 收口 / 约束块 / 素材链 / 标签 / 合规）、
第 3 步填骨架。§6.1 标题改为「（时间码向入口）」并加适用范围注。

### 2. §6.7 标签词库补齐非动作题材

原 7 组里有 3 组是武打向（动作招式 / 武学门派 / 特效物理），对写实 / 纪实 / 广告几乎不可用。
→ 新增 **纪实 / 都市（非动作向）**、**画幅 / 构图（全题材）** 两组，并加「按题材取组」表
（打斗 / 写实纪实 / 古风武侠 / 全题材各取哪几组）。

### 3. 报告 §4 由「逐字引用语料原文」改为「骨架 + 自写示例」

原 §4 含约 864 字的第三方提示词**逐字正文**，与同一文档 §6 自定的
「只提炼范式、不搬运正文进仓库」红线**自相矛盾**（原文自辩「节选用于分析说明」站不住）。

→ 改为 ① 十段**职能表**（段职能 / 该段写什么 / 语料典型措辞）+ ② **自写原创示例**
（外卖骑手雨后窄巷，情绪向只留 5 段）+ ③ 时间码式的**三层叠加骨架**
（`Chapter` / `⏱️ X-Y seconds | 段落名` / `[段内键]` / 段尾衔接声明）。
→ 一举两得：**合规**（仓库内不再有他人正文；需核对原文时按 §6 命令从 gitignored 语料提取）
＋**通用**（骨架可套任意题材，而原文只能示范那一个具体例子）。
→ 同步把 §5 改为「已修正 / 已补充」状态表，修正 §2 的指向与 §6 的合规说明。

### 4. §8 自检清单同步

新增「是否先按 6.0 选定写法（而不是默认套 6.1）」，把原「视频提示词是否按三段骨架组织」
改为条件式（仅时间码向）。

### 结论（可复用的判断）

- **「方便使用」= 决策入口前置 + 给可直接填空的骨架**，不是再堆知识点。
- **「通用」= 从「一个具体例子」抽象为「骨架 + 职能表」**。这一步同时消掉了版权红线问题 ——
  **合规与通用在此方向上一致，不是取舍关系**（这条判断以后遇到同类语料提炼可直接套用）。
- skill 约 480 行；两个文件均**未提交**。

---

## 按用户要求「把整体项目方面与项目功能融合在一起」→ 画风体系全链路收口

用户要求把已建立的**画风体系 + 语料提炼成果**从「文档 / skill 层」真正贯通到项目的**生成功能**。
排查后发现两处结构性断链（不是文档问题，是代码没接上）：

### 断链 1：画风只在「角色/装备/道具/表情」图像链路生效

其余链路都是把**落库 key 直接拼进 prompt**，`DRAMA_ART_STYLE_MAP` 的四段词表从未参与：

| 链路 | 改前写法 | 后果 |
|---|---|---|
| 场景图 | `${key} visual style` | 等于没画风 |
| 分镜静帧 | `${key} visual style` | 等于没画风 |
| 宫格图 | `${key}`（直接拼） | 等于没画风 |
| 视频 | `${key} cinematic style` | 水墨/动漫被强行拉向写实电影 |
| 视频负面 | 恒定 `VIDEO_NEGATIVE` | 无画风对立词，动漫/真人/三维会混 |

且 `videos.ts` / `images.ts` / `auto-pipeline.ts` **传的是 `dramaStyle: undefined`** → 视频链路画风完全断路。
截图/宫格/场景的负面词也恒定，与正向画风不配套。

### 断链 2：解析逻辑被复制了 3 份

`characters.ts` / `dramas.ts` 各存一份 `getGlobalArtStyle` 私有副本，`characters.ts` 还有私有
`getEffectiveArtStyle` / `getDramaStyle` → 画风体系一改要改多处，极易漏。

### 改法：在 `prompt-utils.ts` 建「画风收口层」（唯一入口）

- 解析：`resolveArtStyleKey(...)`（纯函数，**只认合法 key，脏值跳过而非透传**）、
  `getDramaArtStyle` / `getGlobalArtStyle` / `resolveEffectiveArtStyle(dramaId, charStyle?, dramaStyle?)`
  （`dramaStyle ?? getDramaArtStyle(dramaId)` 短路，避免重复查库）。
- 正向后缀：`buildSceneArtStyleSuffix` / `buildStoryboardArtStyleSuffix` / `buildVideoArtStyleSuffix`。
- 负向后缀：`buildSceneNegativePrompt` / `buildStoryboardNegativePrompt` / `buildVideoNegativePrompt`
  （通用负面 + `DRAMA_ART_NEGATIVE_MAP` 画风对立词），与正向成对使用。
- 语料成果落地：`IMPERFECTION_ANCHORS`（反 AI 感词族）+ `PHOTOREAL_ART_STYLES` 白名单，
  **仅写实向画风注入**；`VIDEO_MOTION_BASE` 抽掉写实专属词。

### 关键设计判断（可复用）

1. **视频必须与静帧拆开**：静帧收口词 `cinematic illustration style` / `soft cinematic lighting`
   会把动漫、水墨拉回写实插画 → 视频单独用中性收口 `VIDEO_STYLE_TAIL`，命中画风时**不再追加
   `VISUAL_STYLE_MASTER`**；未命中画风（`undefined`）时完全回退历史默认，保证向后兼容。
2. **反 AI 感词族要按画风白名单注入**：写实向注入能压塑料感，注入到水墨/动漫会直接破坏风格。
   「可控的现实缺陷」是**画风相关**的，不是通用优化项。
3. **后端注入画风 + skill 禁止 agent 输出画风词 = 不会重复叠加**。这条约定让 auto-pipeline
   可以放心地把 agent 生成的 prompt 直接 `+ buildVideoArtStyleSuffix(artStyle)`（每次提交现算、
   不落库，因此不会累积）。若哪天 skill 放开画风词，这里必须改成幂等校验。
4. **`grid.ts` 两处 `let dramaStyle = ''` + 查库代码长得几乎一样**，第一次 replace 只命中一处
   （因为 old_str 含 `// Get drama style` 注释）→ 改这类重复代码**必须 grep 确认全部命中数**。

### 改动清单（backend，全部 tsc 通过）

`shared/prompt-utils.ts`（收口层 + 3 个 builder 改造 + 宫格画风）、
`routes/characters.ts`（删 3 个私有 helper → 共享）、`routes/scenes.ts`、`routes/storyboards.ts`×2、
`routes/images.ts`、`routes/videos.ts`×3、`routes/grid.ts`×2、`routes/dramas.ts`、
`agents/tools/grid-prompt-tools.ts`、`services/auto-pipeline.ts`×4、`services/qc-retry.ts`×2；
`skills/prompt-style-library/SKILL.md` §0 改为**四链路收口表**并新增「视频不要写英文反 AI 感词族」约束。

### 验证

临时脚本实跑 4 种画风（realistic / anime / ink-wash / cyberpunk）→ 正向词表、画风对立负面词、
写实向独有瑕疵锚点、`undefined` 回退全部符合预期（**跑完已删除临时文件**）；
`npx tsc --noEmit` 零错误；改动文件 lint 零诊断。未提交。

---

## 提示词 / Skill「过多太乱太杂」收口（第三阶段）

用户要求「提示词和 skill 都实现通用，过多太乱太杂」。三层根因：**注入层**（默认映射太肥）→
**内容层**（外部 skill 引用不可达）→ **来源层**（同一规则存 3 份副本）。本阶段收口来源层。

### 关键动作

1. **`skills.ts`**：`AGENT_SKILL_MAP` 只留项目自有 skill，`skills/minimax/`（162 文件、单文件最大 42 KB）
   不再默认注入（理由与踩坑写进 `skills/README.md`）；新增 `SKILL_CHAR_BUDGET`（默认 60000 字符，
   `AGENT_SKILL_BUDGET` 覆盖，0=关闭）→ 超预算者按优先级跳过并在注入文本末尾留「未注入：…」诊断。
2. **`shared/prompt-blocks.ts`（新建）**：格式化剧本规范在 `DEFAULT_PROMPTS`/SKILL.md/工具 instruction
   **各写一份且已漂移**（示例雷同、时长不一致）→ 收口为 `SCREENPLAY_FORMAT_RULES`；
   孤儿 `grid_prompt_generator/reference/*.md`（加载器只读 SKILL.md，永远注入不到）→ 上收为
   `IMAGE_PROMPT_TEMPLATE_CHARACTER/SCENE/SHOT` 并删除 `reference/`。
3. **前端 `agents.vue` 里那份「默认提示词 + 默认 Skill 绑定」副本**是最隐蔽的一处漂移：
   `DEFAULT_AGENT_SKILLS` 仍挂着 13 个 minimax skill，`defaultPrompts` 里还留着悬空引用
   `skills/grid-image-generator/SKILL.md` 与「每镜头 10-15 秒」旧值。→ 新增
   `getAgentDefaults()`（`agents/index.ts`）+ `GET /agent-configs/defaults`（**必须注册在 `/:id` 之前**），
   前端改为 `agentConfigAPI.defaults()` 拉取，删掉两份硬编码副本。

### 实测数据（可复现）

`loadAgentSkills()` 实跑：`script_rewriter` 1064 / `extractor` 1083 / `storyboard_breaker` 15328 /
`voice_assigner` 903 / `grid_prompt_generator` 18911 → **合计 37289 字符**（收口前约 238 KB）。
`/agent-configs/defaults` 实跑 200、6 个 agent，`skills` 与后端映射一致（已无 minimax）。
lint 零诊断；临时脚本与日志跑完即删。未提交。

### 判断（可复用）

- **「通用化」的实质是消副本，不是改写内容**：本次几乎所有收益来自「同一规则只留一处」，
  而非重写措辞。找副本的顺序 = 后端常量 → skill 正文 → 工具 instruction → 前端硬编码。
- **前端出现「与后端默认值保持一致」的注释，就是漂移的信号**：默认值一律由后端下发。
- **加载器只读 `SKILL.md`** ⇒ skill 目录里的 `references/` 是孤儿资产，写了也没人看。
- `prompt-utils.ts` 未拆分：文件已有分层契约与导航注释，拆模块对「提示词质量」无收益，
  且会动 15+ 处 import —— 不满足「非改不可」的门槛，故只做注释导航。

## ✅ 语料目录归位（用户指令：「下载数据要放到正确的目录」）

**更正本日上方两条记录**：`reference/prompt-corpus/` 不是正式位置，已整体迁出。
（用户选择的是「提示词语料目录」这一项；模型下载落盘层级那条留作后续待办，未改代码。）

改动（**语料是 Move-Item 平移，未重新下载**）：

| 内容 | 原位置 | 新位置 |
|---|---|---|
| 语料本体 39,407,522 B（37.58 MB / 8755 条） | `reference/prompt-corpus/seedance2/` | `data/prompt-corpus/seedance2/` |
| 分析日志 3 个 | 同上 | `data/prompt-corpus/analyze{,2,3}.log` |
| 分析脚本 3 个 | 同上 | `scripts/corpus/analyze{,2,3}.py` |

- `reference/prompt-corpus/` 已整个删除（`reference/` 还剩克隆项目与 workflow_templates）。
- `.gitignore` 新增 `data/prompt-corpus/`（**第三方文本版权 → 仍不入库，合规红线不变**）。
- 3 个脚本的硬编码 `P = r'd:/.../reference/prompt-corpus/...'` 统一改为
  `pathlib.Path(__file__).resolve().parents[2] / 'data/prompt-corpus/seedance2/metadata.jsonl'`，
  并支持 `SEEDANCE2_CORPUS` 环境变量覆盖 → **以后换目录不用改代码**。
- docs 同步：`video-prompt-data-sources.md`（下载状态、采集顺序）、`seedance2-corpus-analysis.md`
  （头部、§6 复用命令、§6 合规段、附录表）。全库 grep `reference/prompt-corpus` = **0 残留**。

**沉淀的目录约定**：第三方语料 → `data/prompt-corpus/<源>/`（gitignored）；分析脚本 → `scripts/corpus/`（入库）；
结论 → `docs/`（入库）。**三者分离**，否则脚本会跟着 gitignored 目录一起丢（本次就是脚本差点留在会被清理的目录里）。

**验证**：`git check-ignore -v` → 命中 `.gitignore:40`；`python scripts/corpus/analyze3.py`
复跑输出 `rows with Chinese prompt = 8755`（与迁移前一致 → 数据无损）。未提交。

## ✅ skills/ 通用化二次收口（用户指令：「skills 目录优化，通用完整融入项目」）

**病灶**：① 绑定关系藏在代码 —— `AGENT_SKILL_MAP`（`agents/skills.ts`）硬编码「谁绑谁」，改绑要改 TS，
且能绑上已删除的 skill；② 外部品牌写进逻辑 —— `categoryOf()` 里 `minimax-builtin/installed`、
前端侧栏写死「MiniMax 技能库」按钮、CSS 类 `.skill-cat-badge.minimax-*`；③ 前端侧栏 Agent 列表硬编码 5 项。

**改法**（契约 → 解析 → 分类 → 消费 → 前端，全链路零品牌字面量）：

| 层 | 改动 |
|---|---|
| 契约 | `skill-parser.ts`：`SkillMetadata` 新增 `agents: string[]` + `priority: number`（`normalizePriority` 缺省/非法 → 100） |
| 解析 | `skills.ts`：**删除 `AGENT_SKILL_MAP`**；新增 `listCoreSkillIds()`（顶层含 SKILL.md 者 = 自有）、`resolveDefaultSkills(agentType)`（只扫自有 skill，按 `priority` 升序）；`loadSkill` 加 mtime 缓存 |
| 分类 | `routes/skills.ts`：`categoryOf()` → **`classify(id)`**（顶层目录自身无 SKILL.md ⇒ `vendor`，`source` = 目录名）；新增 **`GET /skills/meta`**（`agents`/`sources`/`coreCount`，**必须注册在 `app.get('/*')` 之前**）；列表项新增 `source`/`agents`/`priority` |
| 消费 | `index.ts`、`creator.ts`、`optimizer.ts` 三处改调 `resolveDefaultSkills`；`creator` 的可选集合改 `listCoreSkillIds()`（不再把外部库推荐给常驻 Agent） |
| 前端 | `skills.vue`：侧栏 Agent / 技能库全部来自 `/skills/meta`（前端只留 `AGENT_ICONS` 装饰图标）；`selectedAgent = 'all' \| agentType \| 'lib:<库名>'`；徽标改 core/vendor 两态；`useApi.ts` 加 `skillsAPI.meta()` |

自有 7 个 SKILL.md 补 frontmatter `agents:` + `priority:`，**顺序与原映射逐字对齐**
（`prompt-style-library` 20、`style-reference-reverse` 30，其余 10）。

**验证（实跑）**：`resolveDefaultSkills` 输出与原 `AGENT_SKILL_MAP` **完全一致**；
注入合计 **37,263 字符、零跳过**（`script_rewriter` 1064 / `extractor` 1083 / `voice_assigner` 903 /
`storyboard_breaker` 15,315 / `grid_prompt_generator` 18,898）；顶层分类实测 **7 core + 1 vendor 容器
（`minimax/` → 29 skill）= 36**；`tsc --noEmit` OK；lint 零诊断；临时脚本跑完即删；未提交。

**顺带清掉 3 处过期引用**：`skills.ts` 加载器注释、`agents.vue` 默认值注释（都还写着 `AGENT_SKILL_MAP`）、
`docs/api-contract.md` 的 `category` 枚举（`minimax-builtin/installed/custom` → `core|vendor`，并补
`source`/`agents`/`priority` 与 `/meta` 说明）。`skills/README.md` 重写：目录约定、自描述字段、
三处消费表、新增自有 skill 与新增外部库的流程。

**判定规则（可复用）**：`skills/<lib>/` 只要「顶层目录自身不含 SKILL.md」就自动成为外部技能库，
目录名即库名，前端自动出现分组 —— **换库 / 加库 / 删库零代码**。本次未改目录名：
`minimax/` 作为「库标识」是**数据**而非硬编码，改名只会波及引用与文档、机制上无收益。

**红线（避免误伤）**：`minimax` 作为 **AI 服务商**（provider / TTS / 视频适配器 / H3 本地模型）
在 30+ 处出现是**合法**的，不可与「技能库」混为一谈；本次只清理「skill 库」语义下的品牌硬编码。

## ✅ skills/ 命名合并第三轮（用户指令：「名字优化合并，实现更完美的通用性和实用性」）

**承接**：前两轮已把「绑定关系」与「来源分类」从代码搬到目录结构，但**目录名本身仍不通用** ——
顶层靠品牌名 `minimax/`，库内靠渠道名 `builtin/` + `installed/`。本轮把最后一层也通用化。

**目录改造**（`git mv` 保留历史，166 个 tracked 文件；两块被识别为**两个库**，此前只是"一个库的两个渠道子目录"）：
- `skills/minimax/builtin/` → **`skills/genre-templates/`**（9 个：按「成片类型」的端到端入口）
- `skills/minimax/installed/` → **`skills/production-tools/`**（20 个：制作链路的工序能力与元工具）
- 删除 `minimax/` 空壳

**新机制：库由声明文件识别（本轮核心增量）**
- 新增 `skills/<lib>/library.yaml`：`name`（库标识 = API 与前端分组 key）/ `label`（展示名）/ `description`（悬浮说明）
- `routes/skills.ts`：新增 `readLibrary()` / `listLibraries()`；`classify()` 改为「顶层含 `library.yaml` ⇒ vendor，`source` 取声明的 `name`」，无声明则兜底为目录名
- 列表项新增 `sourceLabel`；`/skills/meta` 的 `sources` 变为 `{id,label,description,skillCount}`
- **`name` 与 `label` 分离** → 目录名 / 库标识稳定，对外展示名随时可改且不触任何引用；加库 / 换库 / 改中文名全是纯文件操作，零代码
- 前端 `skills.vue`：徽标改用 `sourceLabel`；侧栏库项加 `:title=description`；副标题按角色切换（Agent 显示技术标识便于对照代码与 DB，库显示说明文案，全部显示总数）

**未做（记录理由）**：core 的 5 个 Agent 目录名（`script_rewriter` 等）**不动** —— 它们是
`agent_configs.agent_type` 的系统标识，改名要动 DB 与多处代码；库内 skill id 也一律不重命名 ——
库内 `references/` 与 skill 互相引用，改了会静默断链。命名约定已写进 README 第五节。

**已知语义重叠（README 第六节列出，未擅自合并）**：`video-deconstruct` ↔ `video-deconstruct-analyzer`；
三个短剧编剧；六个分镜类 skill。属外部库内容，合并会破坏其自洽性与内部引用。

**验证（实跑）**：顶层 = **7 core + 2 library**；`classify` 分布 **36 = 7 core + 9 genre-templates +
20 production-tools**；5 个 Agent 注入合计 **37,263 字符、零跳过**（与改造前逐字一致）；`tsc --noEmit` OK；
lint 零诊断；临时脚本跑完即删；未提交。

**安全性前置核对**：① 全仓 `skills/minimax/...` 路径引用 = **0**；② DB `agent_configs.skills` 5 条
**全为 null**（无存量绑定）→ 改名零兼容负担；③ `yaml@^2.9.0` 依赖已存在，零新增依赖。
`docs/api-contract.md` 的 `sources` / `category` / `sourceLabel` 已同步。

## 🔍 skills/ 复查（第三轮收尾）

**真实接口端到端验证**（直接 `app.request('/meta')` 与 `/`，非复制逻辑的脚本）：
- `/meta`：`coreCount=7`；`sources` = `genre-templates | 片型模板库 | 9`、`production-tools | 制作工序库 | 20`
  （id / label / description / skillCount 全对）
- `/`：36 项 → 按 `sourceLabel` 分组 = **7 core + 9 片型模板库 + 20 制作工序库**；
  **`source`（英文 slug）与 `sourceLabel`（中文展示名）分离生效**；core 的 `sourceLabel` 全 `null`（无污染）
- `agents` 分组：`script_rewriter:1  extractor:1  voice_assigner:1  storyboard_breaker:2  grid_prompt_generator:3`

**修掉 5 处过时描述**：`skills.vue` 模板与脚本两处注释（"由目录结构推导" → "由 library.yaml 声明驱动"）、
`docs/preset-skill-template.md` 的失效约定（`skills/{name}.md` 单文件 → `skills/<agent>/<skill>/SKILL.md`
目录形式，并注明该文档不属 skills/）、根 `README.md` 目录树、`.gitignore` 的错位注释
（`# Browser test DOM dumps` 却挂在 `models.yaml` 上）。

**⚠️ 未入库清单（非 .gitignore 问题 —— `git check-ignore` 返回 exit=1 = 无规则命中，纯属未 add）**：
- `skills/prompt-style-library/`、`skills/style-reference-reverse/` —— **这两个是被默认注入的真 core skill**
  （`storyboard_breaker` / `grid_prompt_generator`），不入库则 clone 后注入缺失
- `skills/README.md`、`skills/genre-templates/library.yaml`、`skills/production-tools/library.yaml`
- **index 中残留未 stage 的删除 5 项**：`skills/local-model-evaluation.md`、`skills/preset-skill-template.md`
  （实体已移到 `docs/`）、`skills/grid_prompt_generator/reference/{character,scene,shot}-prompt.md`

**加载器容错（已确认安全）**：`loadSkill` 文件缺失 → `null`，调用方产出「未注入（缺失）」诊断而非崩溃；
`listCoreSkillIds` 用 `existsSync` 过滤。故库缺失不会炸，只会**静默少注入**。

**结论**：机制层面已完整；剩余动作是用户侧的 `git add`。**未擅自 add / commit**。

## 🛡️ skills 第四轮：删除保护 + 注入体量透明化

**动机（两个真实缺口，均非注释问题）**：
① 前端每张卡片都有删除按钮、后端 `DELETE /*` 直接 `rmSync(recursive)` → **能删掉 Agent 自有 skill**；
   叠加「`prompt-style-library` / `style-reference-reverse` 未入库」= 误删即**永久丢失**。
② 注入预算闸（60000 字符）超限时**静默跳过**，前端既看不到单个 skill 多重、也看不到某 Agent 绑定合计
   → 用户无从判断「绑了会不会不生效」。两者有内在联系：**看不出体量就会乱绑，能随手删就会删错**。

**后端 `routes/skills.ts`**：
- 新增 `isProtectedSkill(id)` = `!id.includes('/')`（顶层 = Agent 根 skill 或提示词库）；`DELETE` 命中即 400 拒绝
- 新增 `countReferenceFiles()`（递归数 `references/` 文件）与 `injectedChars()`
  （**= `renderSkill` 长度，与预算闸同口径**，不用文件字节数）
- `/` 每项新增 `charCount` / `referenceCount` / `protected`
- `/meta` 新增 `charBudget` + 各 Agent `charCount`；`sources` 补兜底库条目（`declared:false`）
  使「core + Σ各库 = 总数」自洽且侧栏可达
- `agents/skills.ts` 的 `SKILL_CHAR_BUDGET` 改为 `export`（供路由层暴露）

**前端 `skills.vue`**：卡片显示 `N 字符` + `📎 N 个参考文件（不注入）` + `🔒 受保护`；`protected` 隐藏删除按钮；
Agent 副标题显示「默认注入 X / Y 字符」，超预算高亮 + 顶部预警条；库按钮对 `declared:false` 提示未声明。

**实测（真实接口）**：`charBudget=60000`；`protected=7 / unprotected=29`；受保护 7 项恰为 5 个 Agent 根 skill
+ 2 个提示词库；各 Agent 注入 = script_rewriter 1034 / extractor 1053 / voice_assigner 873 /
**storyboard_breaker 15279** / **grid_prompt_generator 18856**（合计 **37095**，余量充足）；
`withRefs=22`（有 references 的 22 个**全是 vendor** → 印证「references 不可达」是**有意取舍而非 bug**，
本轮已把它从"疑似缺陷"正式排除）。

**删除保护实证**：`DELETE /storyboard_breaker` → `400` + 明确文案，且 `SKILL.md 仍存在 = true`（目录真没被删）；
`DELETE /script_rewriter/__probe__` → `Skill not found`（保护只拦顶层，用户自建可删）。

**口径修正**：记忆原写「5 agent 合计 37,263」→ 权威口径（renderSkill 长度求和）为 **37,095**，差值来自旧统计方式。

**验证**：`tsc --noEmit` 零错误、lint 零诊断、`@vue/compiler-sfc` 的 `compileScript`+`compileTemplate` 通过
（dev server 未运行，未强启）；临时脚本跑完即删。**未擅自 add / commit**。

**工具坑（新增）**：`search_content` 的 `glob` 参数**不生效**（`**/SKILL.md` 恒 0 命中，同 pattern
不带 glob 则 31 命中）→ 改用 `path` 收窄或宽匹配后筛。

## 🔗 skills 第五轮：绑定现场可见 + 修正上轮判据副作用

**动机**：上一轮把「注入体量透明化」做在了 `skills.vue`（浏览场景），但用户**真正决定绑哪些 Skill 的地方是
「Agent 配置」页**（`agents.vue`）。后端已返回 `charCount`，前端**一行都没用** → 动机只满足了一半。

**改动 A — `agents.vue` 绑定面板（补全上轮）**：
- 单项显示注入体量；头部显示「已启用 N 项 · X / Y 字符」，超预算转警示色
- 超预算预警条（"超出的会按列表顺序被跳过"）
- `外部库` 标记（vendor 体量常是自有 skill 的 10 倍+）、`已失效` 标记（DB 残留引用已删 skill → 加载时被跳过）
- 新增 `enabledSkillCount` / `boundChars` / `isBindOverBudget`；⚠️ **统计必须只算 `enabled=true`**
  （只有它们会注入），否则预警误报
- 数据源零后端改动：`skillsAPI.list()` 已含 `charCount`/`category`，另加 `skillsAPI.meta()` 取 `charBudget`

**改动 B — 修正上一轮引入的副作用（重要）**：上轮判据是「顶层即受保护」，但接口允许创建顶层 id
→ **用户自建的顶层 skill 会被永久锁死删不掉**。改为「顶层 **且** frontmatter 声明了 `agents`」：
保护语义从「位置」变为「**确实参与默认注入的项目资产**」，更准确。DELETE 处会回读解析，
解析失败传 `undefined` → 保守拒删（宁可拒删也不误删资产）。

**改动 C — 两处静默失效的显式化**：
- `PUT /skills/<id>` 保存后回读校验 frontmatter，缺 `---` 头部或 `agents` 为空 → 返回 `{ warning }`，
  前端 `toast.warning`（此前会「保存成功」但默认注入静默消失）
- `skills.vue` 新增表单 id 预校验（`/^[a-zA-Z0-9_-]+$/`，比后端更严：不许斜杠）→ 即时中文提示，
  替代此前的 400 `Invalid skill id`

**排除的假 bug（重要）**：`confirmAddSkill` 用 `selectedAgent.value` 拼 id，我一度认为库分组下会拼出
`lib:xxx/yyy`（含冒号 → 必 400）。**核实后发现 `canAddSkill` 已限定「仅选中具体 Agent 时显示新增」**，
该路径不可达 → **不是 bug**。教训：报缺陷前先确认入口是否可达。

**验证（真实接口）**：
- 回归：`protected` 仍 **=7** 且全部是 core，受保护集合与「含 agents 声明的顶层」精确一致 → 判据改动**零副作用**
- 新判据：临时建顶层 + `agents: []` 的 skill → `protected=false` → `DELETE` **200 且目录真被删除**（旧判据会锁死）
- 资产仍拒删：`DELETE /script_rewriter` → 400，`SKILL.md` 仍在
- PUT 三态：无 frontmatter / agents 空 / 正常（无预警）均符合预期
- `charBudget=60000`；**最大外部库 Skill 27,426 字符 = 预算的 45.7%** → 勾两个即逼近上限，
  印证绑定面板体量显示是刚需。临时目录测完即删

**校验**：`tsc --noEmit` exit=0、lint 0 诊断、`agents.vue` + `skills.vue` 双 SFC 编译 OK。**未擅自 add / commit**。

## 🔌 skills 第六轮：打通外部库绑定的 UI 入口 + 修「全关反注入」

前几轮补的是「看得见」，这轮查「**做得到**」—— 查出 2 处功能缺口 + 1 处逻辑反转 bug。

**缺口 1 — 外部库 Skill 在 UI 上根本绑不上去（用户被指到死路）**
- `skills.vue` 空状态明确引导：「外部技能库的 Skill … 需在『Agent 配置 → 绑定 Skills』中按需手动启用」
- 但 `agents.vue` 绑定面板**只有「恢复默认」，没有任何添加控件**：列表只回显 DB 已有绑定
- ⇒ 两处文案互相矛盾；`loadAgentSkills` 文件头注释亦写「仍可在前端按需手动启用」→ **文档承诺了不存在的能力**
- 修：head 加「+ 添加」→ 内联候选面板（可搜 id/名称/描述、排除已绑定、自有在前、显示体量与「外部库」标签、
  点选即入列表末尾、可连续添加）；空状态文案改为引导「点击上方添加」

**缺口 2 — 已失效绑定无法移除**：上轮我加的「已失效」提示写「请关闭开关或删除此绑定项」，
但列表项**没有删除按钮** → 提示指向不存在的操作。修：每项加「✕」移除（`@click.stop` 防触发拖拽）。

**逻辑反转 bug — 「全部取消勾选」反向注入全部默认 skill（后端，`agents/skills.ts`）**
- 原逻辑：DB 配置**先过滤 enabled**，再看 `if (!bindings.length)` 回退默认
- ⇒ 全部取消勾选 → 过滤后为空 → **被当成「没配过」→ 注入全部默认绑定**
- 即「一个都不注入」被执行成「注入全部」，而勾选框与面板文案都承诺「仅勾选的会注入」
- 修：改判 `parseSkillsConfig(raw).length`（**配置项非空 = 用户已做过选择，意图优先**）→ 全关 `return null`；
  `null`/空串/空数组/损坏 JSON/非数组 **仍回退默认（零行为变更）**

**隐患修正 — 回显 `enabled` 与后端口径相反（前端）**
- 后端：`enabled: item.enabled !== false`（**缺省 = 启用**）、`priority` 缺省 `0`
- 前端 `toggleAgentEdit` 直接 `JSON.parse(cfg.skills)` 裸赋值 → 缺 `enabled` 的老数据显示为**未勾选、
  且不计入体量合计**，但后端照常注入 ⇒ **显示与实际相反**
- 修：回显规范化（`enabled !== false`、`priority` 缺省 `0`、过滤无 id 项、非数组兜 `[]`）

**排除的两次误判（防止误报）**
- 「拖拽排序是摆设」→ 核实 `saveAgentCfg` 按列表顺序重写 `priority: i+1`、加载器按 priority 升序 ⇒
  **拖拽真实生效**，上轮预警文案「按列表顺序被跳过」**是对的**
- 「`skills.vue` 不看绑定关系」→ 实际已渲染 `boundAgents` 标签

**验证（只读、不写 DB：用 `loadAgentSkills(agentType, rawJson)` 入参模拟）**
- 手绑外部库 Skill → **注入 27,456 字符**且正文命中 ✅ ⇒ 新入口真实有效（非装饰）
- 缺 `enabled` → 仍注入 ✅ 反证前端回显必须对齐口径
- **全关 → `null` 不注入** ✅（修复前会回退默认 1,064 字符）
- 兼容性：`null`/空串/`[]`/损坏 JSON/非数组 → 全部仍回退默认 1,064 ✅ 零变更
- 仅不存在的 id → `null`（不回退）✅
- 优先级互换 → 输出内容不同 ✅ ⇒ 注入顺序确由 priority 决定
- `tsc --noEmit` exit=0、lint 0、`agents.vue` SFC 编译 OK、临时脚本已删。**未 add / commit**

**记忆维护**：`MEMORY.md` 触发注入截断 → 已全文重写压缩（合并去重、消除超长行），结论未丢。

## 🧹 skills 第七轮：记忆体量治理 + 重命名残留核查 + 来源可辨

**1. `MEMORY.md` 注入截断治理（结构性，非措辞）**
- 现象：`MEMORY.md` 9,458 字符 → 注入被截断（截断点 ~8.9k，且断在半句）。
- 三次「压缩措辞」仅 9,458 → 8,468 → 8,302 → 7,999，**收效递减** ⇒ 判断问题不是措辞而是**体量归属**。
- 决策：把两块**低频但体量大**的专题拆到新文件 `.codebuddy/memory/TOPICS.md`（本地模型+H3 ≈1.1k、视频提示词语料 ≈1.5k），
  `MEMORY.md` 各留一行指针 + 只保留「必须记牢的三条」。
- 结果 **9,458 → 7,006 字符（-26%）**，距截断点约 21% 余量；并写明「本文件须 ≤8k，实测 9.4k 即截断」。
- 顺带修正一处事实错误：原写「自有 7 core」，实测**其中 5 个才是 Agent 类型**
  （extractor/grid_prompt_generator/script_rewriter/storyboard_breaker/voice_assigner），
  另 2 个（prompt-style-library / style-reference-reverse）是普通 skill、**不是 Agent**。

**2. 技能库重命名残留核查（`skills/minimax/builtin/*` → `skills/genre-templates/*`）**
- 全仓 grep `minimax/builtin` / `minimax/` → **0 处悬空引用**（仅 2 处无关命中：`local-model-scan.ts` 的名字判定、`auto-pipeline.ts` 注释）。
- 两个 `library.yaml` 的 `name` 与新目录名一致（`genre-templates` / `production-tools`）→ 声明与目录未脱节。
- 只读查 `data/drama.db`：`agent_configs` 5 行 **skills 全为空** → 全走默认绑定（未受重命名影响）；**悬空绑定 0 项**。
- 磁盘实测 **36 个 skill = 7 core + 9 genre-templates + 20 production-tools**，与声明计数自洽。

**3. 契约核对 + 补一处可用性缺口（前端）**
- 核对新写的候选面板与真实契约是否一致：`classify()` 返回 `category: 'core'|'vendor'` ✓、
  `fmtChars`/`skillMissing`/`availableSkills` 均存在 ✓、`availableSkills` 在 `onMounted` 已加载 ✓。
- 缺口：API 早已返回 `sourceLabel`（库展示名），但面板对 **29 个外部库 Skill 一律只标「外部库」**，无法区分来源
  —— 而两库用途/体量差异极大，恰是选择时最需要的信息。
- 修：`isVendorSkill()`（返回 bool）→ **`vendorLabel(skillId)`**（返回 `sourceLabel || '外部库'`，非 vendor 返 `''` 供 `v-if`），
  候选列表与绑定列表**两处**均改为显示真实库名。
- 校验：全仓无 `isVendorSkill` 残留引用；lint 0；`agents.vue` SFC 编译 OK；临时脚本已删。**未 add / commit**

## 🔌 skills 第八轮：外部库「宿主工具兼容性」缺陷（9/36 个声明了不存在的工具）

**发现**：`genre-templates/anime-game-pv` 等 skill 正文写「Call `hub_open_remote_tool_gui`」，而该工具**在后端/前端从未注册**。
根因是 frontmatter 的 `allowed-tools` 字段 —— **在整个后端 TS 代码中被读取 0 次** ⇒ 依赖关系完全不可见、不校验、不提示。

**量化（脚本实测，非估算）**：
- 宿主真实工具 **21 个**（全部领域工具：`read_script_for_extraction`/`save_dedup_characters`/`generate_grid_prompt`/`save_storyboards`/`assign_voice`/`run_subagent` …）。
- 声明 `allowed-tools` 的 skill **9 / 36（25%）**，内容为 `hub_*` 系（另一套宿主平台：`hub_open_remote_tool_gui`/`hub_submit_dag`/`hub_analyse_media`/`hub_canvas_*`/`hub_ffmpeg`…）及平台通用 `question`/`task`/`read`/`write`。
- **9 个的声明工具 100% 不存在** ⇒ 绑定后 Agent 拿到的是一份「无法执行的指令」，既不报错也不生效。

**修复（诊断可见化，不做硬拦截）**：
1. `agents/skill-parser.ts` 新增 `toToolList()` 归一 `allowed-tools` → `SkillMetadata.allowedTools`
2. `agents/index.ts` 新增 `getHostToolNames()`（懒计算 + 缓存 + 静默降级）
3. `routes/skills.ts` 列表直出 `allowedTools` + `missingTools`
4. `agents.vue`（绑定列表 + 候选列表）与 `skills.vue`（统计行）打「缺 N 个工具」琥珀色预警，`title` 列出具体工具名

**两个「猜错就静默失效」的关键点（都是实测才暴露）**：
- **必须取 `tool.id`，不能取工厂返回的记录键**：`createExtractTools()` 返回 camelCase 键（`readScriptForExtraction`），
  真正发给模型的是 Mastra `tool.id`（snake_case，与默认提示词里「调用 read_script_for_extraction」一致）。
- **`toToolList` 必须兼容三种写法**：`[a, b]`（YAML 数组）、`a, b`（**纯量字符串，YAML 不按逗号切分**）、换行列表。
  先用 shell 正则统计得「8 个 skill 声明」，而 YAML 解析器实测为 **9 个** —— 漏掉的 `education-studio` 正是**多行列表**写法。
- 同理，先用正则数出「27 个宿主工具」，运行时实测 **21 个**（`alloy/echo/fable/onyx/nova/shimmer` 是音色数据的 `id` 而非工具）
  ⇒ **若手写静态清单会掺进 6 个假条目**。

**校验**：两个 SFC 编译 OK；lint 0；`SkillMetadata` 全仓仅 1 处构造点（已同步）→ 无类型破坏；临时脚本已删。**未 add / commit**
**副作用记录**：导入 `agents/index.ts` 会触发 DB 清洗（`[db] sanitized legacy equip prompts (changed=6)`）—— 幂等，服务器启动本就会跑。

## 🔌 skills 第九轮：依赖判定并集化（9 → 15）+ 改名迁移核查

**先核查上一批 166 文件改名（`skills/minimax/builtin/*` → `skills/genre-templates/*`）的安全性**：
- 纯改名（git R100）× 166；磁盘 `minimax/` 已消失；代码与文档零旧路径引用
  （`minimax` 的 65 处命中全是正文内容：`MiniMax Design` 署名 / `MiniMax H3` 模型名 / CDN 域名）
- **DB 零残留**：全库扫描无 `minimax/builtin`；5 个 `agent_configs.skills` 全为 `null` → 回退默认绑定
  ⇒ **改名安全，无需迁移**（`null` 才回退，是按「DB 配置优先铁律」的正确行为）
- `skills/README.md` 与两个 `library.yaml` 均已同步到新结构 → **无文档漂移**
- 附带确认：`meta.yaml` **后端零读取**（外来市场元数据，与 `references/`/`scripts/` 同属惰性资产）

**审计 refresh() 不变量**（记忆里的「复用型 refresh 禁止 UI 定位副作用」）：
`models.vue` 的 3 个 refresh 均为纯拉取；剧集工作台 `refresh()` 亦干净（`selectedSb` 按 id 重绑 ✓、`bgmDirty` 保护 ✓）。
`selectedSb` 只有「点击选中」（`@click="selectedSb = sb"`）无取消路径 ⇒ `|| sbs[0]` 仅在首次加载生效，**不构成跳转**。**不变量成立**。

**发现上一轮修复的覆盖漏洞**：`missingTools` 只看 `allowed-tools` 字段，而 27/36 个 skill **根本没写该字段**，正文里却直接教 agent 调用外来工具。
- 实测：正文出现 `hub_*` 的 skill **13 个**（全部在 vendor 库内，core 为 0 ✓），其中 **6 个未声明** `allowed-tools` → 上一轮完全无法预警
- 另发现 `multi-shot`/`n-storyboard` 的**正文与声明互相矛盾**（声明 `hub_submit_dag`，正文却调 `hub_query_dag_result`/`hub_upload_to_cdn`）

**修复**：
1. `skill-parser.ts` 新增 `FOREIGN_TOOL_PREFIXES=['hub_']` + `extractForeignToolRefs(body)` → 挂到 `ParsedSkill.foreignToolRefs`
2. `routes/skills.ts` 改为 `requiredTools = allowedTools ∪ foreignToolRefs` → `missingTools`
3. 前端只改提示文案口径（「声明或正文引用」），**零结构改动**

**关键设计取舍**：正文提取**只认 `hub_` 前缀**，不做「未知 snake_case token 即工具」的宽泛猜测 ——
正文里大量反引号 token 其实是字段名/文件名（`shot_type`/`bgm_url`/`prompt_budget`），宽泛匹配会把预警变成噪声。
代价：正文里裸写的 `read`/`write`/`task` 识别不到（只在 frontmatter `allowed-tools` 覆盖）。

**实测校验**（用真实 `parseSkill` + `getHostToolNames` 复算路由并集逻辑）：
**15/36 被标记**（上一轮声明口径为 9）；仅靠正文才发现 6 个、仅靠声明 2 个；宿主工具集 21 个。
lint 0；两个 SFC 编译 OK；`parseSkill` 全部调用点只消费返回值（无字面量构造）⇒ 增字段非破坏。临时脚本已删。**未 add / commit**

## 🧹 skills 第十轮：引用完整性守卫（断链审计 → 自动化）

**动机**：记忆里写明「库内 skill id 勿重命名 —— `references/` 互引会静默断链」，而上一轮刚完成 166 文件改名。
这类断链**不报任何错**（加载器只读 SKILL.md，没人会去点那些路径）⇒ 必须全量核验。

**审计**：扫 `skills/**/*.md` 全部路径引用（反引号 token + markdown 链接目标）。
- 第一版只扫 36 个 `SKILL.md`、且扩展名表漏了 `.mjs` ⇒ **漏报**（这就是上一轮「只找到 1 处」的原因）
- 扫全部 **140 个 .md** 后得 14 处，逐一定性：

| 位置 | 定性 |
| --- | --- |
| `skill-reviewer/SKILL.md:76 → skills/skill-creator/references/NAMING.md` | **真断链**：写成 repo 根相对却漏了库层级（`skill-creator` 实际在 `production-tools/` 下）→ 已修 `../skill-creator/references/NAMING.md` |
| `ui-motion/SKILL.md:136 → scripts/stitch.mjs` | 上游确实 bundle（示例里还留着 `.opencode-v2/skills/…` **原宿主路径**），vendored 时未带 ⇒ 真实缺口（不可达，仅文档级） |
| `voiceover-direction/SKILL.md:23 → scripts/explainer-v3.md` | 正文是「用户提到的任意脚本路径（**e.g.** …）」⇒ **示例占位**，非依赖 |
| `skill-creator/SKILL.md:227 → scripts/set-cover.sh` | 整节讲上游 Hub 发布（紧邻 `spec/metadata.yml`）⇒ 上游仓库脚本 |
| `*references/*.md` 内部互引 ×7 | **误报**：`references/x.md` 写在 `references/` 内部时基准是 **skill 根**而非 references 自身，目标确实存在 |
| `README.md` ×3 | 误报：目录树示意路径（`agents/skills.ts` 等）+ `references/*.md` 通配 |

**产出**：`scripts/check-skill-refs.mjs`（Node ESM，零依赖）—— 把上述判定固化为可复检守卫。
- **基准约定**（实测只有这几种，混用即误报）：`references|scripts|agents/…` = 相对 **skill 根**；
  `../` = 相对**文档目录**；`backend|frontend|skills/…` = repo 根
- **分级**：`references/` + 跨 skill + repo 根断链 = **致命**（退出码 1）；缺 `scripts/` = **非致命**
  （外部库普遍未 vendored 上游 scripts，且该目录对 Agent 不可达）⇒ 避免守卫长期红灯被忽略
- `--verbose` 列出被跳过的候选（上游路径 / 基准不明）⇒ **守卫自身的盲区可审计**
- 精修两处：① 行号改**按偏移二分定位**（原先按子串首次出现会指错行，README 从 5/5/6 修正为 61/62/63）；
  ② 把 `backend/…` 类真实仓库路径纳入校验（待校验 89 → **94 处**）
- 顺带确认：`skills/README.md`、两个 `library.yaml` 无漂移；`.agents/`、`.claude/`、`.opencode-v2/` 均为**外来宿主路径**标记

**验证（含负向测试，证明守卫会响而非空转）**：人为打回断链 → `致命断链 1 处 / exit=1` 且行号 76 正确；
还原后 `致命断链 0 处 / exit=0`，还原一致性 `True`。
当前基线：**140 文件 / 94 处待校验 / 致命 0 / 非致命 3 / 跳过 12**。临时脚本已删。**未 add / commit**

### 🧹 skills 第十一轮：守卫盲区收口（基线归零）

**起因**：上轮遗留 3 处「非致命缺脚本」需定性。逐条查证后**三类处置各不相同**：
- `ui-motion:136` —— **真错**：正文称 `scripts/stitch.mjs` 是 **bundled**，但 `skills/genre-templates/ui-motion/` 下**根本没有 `scripts/`**（`ls` 实测只有 `references/` + `SKILL.md`）⇒ 改正文为「上游 bundle 的 `stitch.mjs`（非本仓库携带）」，**去掉假声明**（该行下方示例本就写着 `.opencode-v2/skills/ui-motion/scripts/stitch.mjs`，即真实上游位置）
- `skill-creator:227` —— **指代不明**：整节讲上游 Hub 发布，但写成裸 `scripts/set-cover.sh` 像本仓库资产 ⇒ 限定为「上游 `hub-skill-market` 仓库的 `set-cover.sh`」（`hub-skill-market` 这一称呼取自 `skill-reviewer:49`，非杜撰）
- `voiceover-direction:23` —— **假阳性**：`(e.g. \`scripts/explainer-v3.md\`)` 明确标为举例 ⇒ 改**守卫**而非正文

**守卫增强**：脚本头早就声称会跳过「示意路径」，但代码只按首段目录名判，**承诺与实现不符** ⇒ 补齐 `ILLUSTRATIVE_RE`。
- 判据演进（两次踩坑，均被负向测试抓出）：
  1. 初版 `/(?:e\.g\.|…)/`（同行任意位置）—— **过宽**：构造 `无 e.g. 标记：\`references/x.md\`` 时该**真断链被静默吞掉**（守卫自己漏报）
  2. 收紧为 `[\s(（:：,，、-]*$` —— **过窄**：实际切片末位是**开启 token 的反引号**（`...(e.g. \``），`$` 被反引号挡住 ⇒ 示意引用掉回 0 处、真用例重新误报
  3. 定版 `/(?:e\.g\.|eg\.|for example|such as|例如|比如|譬如)[^A-Za-z0-9\u4e00-\u9fa5]*$/i` —— 只要求「标记后到路径前**全是非字母数字非汉字**」，自然容纳反引号/括号/标点，无需枚举
- 负向测试（探针 skill，两行仅差 `e.g.` 标记）：无标记行 → `致命断链 1 处 / exit=1`；有标记行 → 跳过。
  基线归零后 **非零即真回归**，比原先「3 处长期红灯」信噪比高得多。探针与临时诊断脚本均已删（residue=0）。

**最终基线**：`140 文件 / 91 处待校验 / 致命 0 / 非致命 0 / 跳过：上游 5、示意引用 2、基准不明 6`，exit=0。
示意引用除已知的 `voiceover-direction:23` 外，**额外揪出 `voiceover-direction:136`**（`file_path` 取值示例）—— 属分类更准（原归「基准不明」）。
**顺带核验**：36 个 `SKILL.md` 的 frontmatter `name` **零重名、零缺失**；`skills/**` 内 **零** `skills/minimax|builtin|installed` 旧路径残留；`library.yaml` 2 个（`genre-templates` / `production-tools`）label 无漂移。
**未 add / commit**

### 🔎 skills 第十二轮：改名后风险复查（证据化盘点，未改代码）

**目的**：回答「还有需要优化的地方吗」——不靠感觉列清单，逐项取证。

**A. 已核验清白（排除最大隐患）**
- **DB 无悬空绑定**：166 文件改名后，`data/drama.db`（756 KB）实测 `agent_configs` **5 行 `skills` 全为空**（`n=0`），且对**全库所有表的 TEXT 列**扫 `%minimax/builtin%` / `%minimax/installed%` **0 命中** ⇒ 改名**无功能回归**。
  - 方法：`better-sqlite3` 直接用绝对路径 `require`，以 **`{ readonly: true }`** 打开 —— 关键，绕开 `agents/index.ts` 的 **DB 清洗副作用**（坑⑦），只读不写。
- 守卫基线 0/0（上轮已归零）。
- 因 5 个 agent 的 `skills` 均为空数组 ⇒ 按铁律**回退出厂默认绑定**，与预期一致。

**B. 发现 3 项可优化（均不影响运行时，需用户决策）**

| # | 项 | 事实（实测） | 风险 | 建议 |
|---|---|---|---|---|
| A | `meta.yaml` ×20 | `production-tools` **20/20 全有**、`genre-templates` **9/9 全无**、core 7 个无；总计 ≈27 KB；**全仓库零引用** | **像配置但改了不生效**（静默 no-op）；且是 `version`/`display-name-zh`/`author-*`/`source` 的**唯一载体**（SKILL.md frontmatter 无这些字段）⇒ 删即丢上游溯源 | 保留 + 在 `skills/README.md` 记明「不被加载器读取，仅供溯源」 |
| B | `scripts/corpus/` | `analyze.py` 66 行 → `analyze2.py` 99 → `analyze3.py` 133，同一 preamble（读 `SEEDANCE2_CORPUS`）⇒ 同任务迭代版；3 个全 `??` **未跟踪**，且 `.gitignore` **未**忽略（只忽略 `data/prompt-corpus/`） | 「写了但随时会丢」；分不清哪版是定版 | 删 1/2、`3` 改名定版（**未跟踪文件删了不可恢复 → 必须先经用户确认**） |
| C | 守卫无自动化 | **无根 `package.json`**、**无 CI**（仓库唯一 `.yml` 是 `docker-compose.yml`） | 只能靠 `skills/README.md` + 记忆 ⑧ 提醒，仍可能忘 | 加 `.githooks/pre-commit`（但启用需 `git config core.hooksPath` —— **改 git config 属禁止操作，不代为执行**）或根 `package.json` 加 `check:skills` |

**关键取证**：确认 `meta.yaml` **从不被读取** —— `routes/skills.ts` 只读顶层 `LIBRARY_FILE='library.yaml'`，`agents/skills.ts` 加载器只读 `SKILL.md`；全 `readdirSync` 用法均为目录枚举，**无任何 `*.yaml` 泛化 glob**。
**优先级**：B（会真丢）> A（误导）> C（流程）。**未改任何文件；临时脚本已删（residue=0）**

### 🔧 skills 第十三轮：A/B/C 三项落地（含一处自我更正）

**先更正上一轮的判断错误**：上轮把 `scripts/corpus/analyze*.py` 判为「迭代残留」是**错的**。`docs/seedance2-corpus-analysis.md` 第 269-271 行**明确记录了三个脚本的分工**（`analyze.py`=首轮结构探查 / `analyze2.py`=深挖 i18n·raw_p·category·spec / `analyze3.py`=主力：词表·结构·标签），且 §6 直接把 `analyze3.py` 写成复跑入口 ⇒ **不删、也不改名**（改名会打断文档 §6/§附 的引用）。真正的问题只是「未跟踪」。

**A. `meta.yaml` 定位收口（纯文档）**
- `skills/README.md` §一 新增「上游清单 `meta.yaml`（本项目**不读取**，仅供溯源）」：它是**上游 Hub 技能市场的上架卡片**（`display-name-zh` / `version: 1.0.12` / `tag-*` / `complete-tags-*` / `summary-*` / `desc-*` / `cover` / `author-*: MiniMax Design` / `source: official`）⇒ **改它不生效**、**删它丢溯源**，与 `library.yaml` 性质完全不同。

**B. 语料脚本纳入版本管理（真正修掉「会丢」）**
- 实测复跑 `analyze3.py`，输出**与文档逐项吻合**：8755 条 / 中位 245 / 文内时间码 1249(14%) / 分段标题 285(3%) / 镜头运动 4544(51%) / 画质收口 3437(39%) / 一致性 1018(11%) / tags 10230 / Entertainment 6124 / duration 有效 8746 ⇒ 证实三脚本是该报告的**可复现来源**，绝不能删。
- `git add scripts/corpus .githooks` → 4 文件已入暂存区（`A`）。**未 commit。**

**C. 守卫接上提交前入口**
- 新建 `.githooks/pre-commit`：**仅当本次提交改到 `skills/` 或守卫脚本时**才跑，致命断链阻止提交（`--no-verify` 可绕过）。
- ⚠️ **未改 git config**（`core.hooksPath` 属禁止操作）⇒ 启用命令写进 `skills/README.md` §十，由用户自行执行。

**D. 顺带修掉 3 处守卫盲区 + 基线更新**
- `skills/README.md` 里 `routes/skills.ts` / `agents/skills.ts` / `agents/index.ts` 写成相对片段 → 被守卫判「基准不明」**静默跳过**；而同文件第 5-6 行本就用完整路径 `backend/src/...` ⇒ **自相矛盾**。已统一成完整路径 ⇒ 从「跳过」变为**真校验**。
- 我自己新增正文里的 `references/x.md` 又引入 1 处新的「基准不明」→ 改写为「指向 `references` 目录的引用」，**避免正文扰动基线**。
- **基线 91 → 92 处待校验；基准不明 6 → 3**（仅剩 vendor skill 内 3 处 `export/...` 上游相对路径）。**致命 0 / 非致命 0 / `GUARD_EXIT=0`**。

**临时文件**：验证日志已删（`.verify-analyze3.log` / `.verify-guard.log` → gone）。

### 🧹 第十四轮：索引一致性 + 承重资产纳管

**发现（上一轮自己引入的不一致）**：上轮把 `scripts/corpus/*.py` 纳入版本管理，但 `scripts/README.md`（**已跟踪**，是 `scripts/` 的索引，含「工具清单」表）里**没有 `corpus/`** ⇒ 索引查不到新资产。已补「语料分析脚本（非 AI/GPU，Python）」小节：3 脚本分工表、`SEEDANCE2_CORPUS` 覆盖、复跑命令 `python scripts/corpus/analyze3.py > data/prompt-corpus/analyze3.log`、语料 gitignored 需自备、CC BY 4.0 署名与「不搬运正文」红线、指向 `docs/seedance2-corpus-analysis.md`。已核对 3 脚本**纯标准库**（与 README 第 4 行声明一致）。

**承重资产纳管（消灭「会真丢」）**：`git status` 暴露 14 个未跟踪路径，其中大量是**承重资产**，误删即功能丢失：
- `frontend/app/utils/artStyles.ts`（**画风单一事实来源之一**；整个目录只有这一个文件）
- `backend/src/shared/prompt-blocks.ts`（`SCREENPLAY_FORMAT_RULES` 等的唯一载体）
- `skills/genre-templates/library.yaml` + `skills/production-tools/library.yaml`（**库声明文件**，加载器据此识别库 ⇒ 删 = 库识别失效）
- `skills/prompt-style-library/SKILL.md` + `skills/style-reference-reverse/SKILL.md`（2 个新 skill，已绑 agent）
- `scripts/check-skill-refs.mjs`（被两份 README + pre-commit 同时引用）
- `skills/README.md`、`docs/*.md` ×4（含 CC BY 署名载体）

⇒ 已 `git add` 共 **16 个新文件**（含上轮的 `scripts/corpus` 3 个与 `.githooks/pre-commit`）。**仍未跟踪只剩 `.codebuddy/memory/*`**（本地 agent 状态，有意不入库）。**未 commit。**
⇒ 用户自己的 **38 个 `M`**（已跟踪的在途重构）**未动** —— 已跟踪无丢失风险，归用户提交节奏。

**顺手验证的两条不变量**
- 画风两处单一事实来源**当前同步**：`prompt-utils.ts`(64169 字符) 与 `artStyles.ts`(2555 字符) 均含全部 10 个 key，`missing=none`。
- 技能库布局自洽（验证已暂存的改名集合）：`library.yaml` 恰 2 个；`SKILL.md` 按深度 `depth=3 count=7`（core）+ `depth=4 count=29`（= 9 + 20）⇒ **36 = 7 + 9 + 20**，与记忆一致。

**守卫**：`140 文件 / 92 处待校验 / 致命 0 / 非致命 0`（上游 5 / 示意 2 / 基准不明 3），`GUARD_EXIT=0` 未受影响（`scripts/README.md` 不在 `skills/**` 扫描范围内）。

**待办／提醒**
- `MEMORY.md` 当前 **7973/8000 字符**（逼近注入截断阈值）⇒ 下一条长期事实入库前**必须先精简旧内容**，否则会被截断（故本轮未写 `MEMORY.md`）。
- 用户明确未提交；`core.hooksPath` 仍需用户手动执行一次才生效。

**坑（再次确认）**：PowerShell 里 `node -e "..."` 写复杂引号会报「无效代码页」→ 改用纯 ASCII 的 PowerShell 原生命令（`Get-Content -Raw -Encoding UTF8` + `[regex]::Escape()`）。

### 🧠 第十五轮：MEMORY.md 截断风险已消除（7973 → 7202）

**问题**：`MEMORY.md` 达 **7973 / 8000 字符（99.7%）**，而**尾部正是 `## 协作与提交`**（含用户铁律「未经明确要求绝不 `git commit`」+ 编码/落盘坑）⇒ 再写约 30 字符，**最关键的用户偏好会被静默截断丢掉**（该文件自述「实测 9.4k 即截断，断在半句」）。属**静默降级**：不报错、不告警，只在下次会话发现约束「凭空消失」。

**做法（信息一条不丢，只换位置）**：把**体量大但低频的实现细节**下移 `TOPICS.md`（本就是设计的溢出层），`MEMORY.md` 只留红线与不变量。
- 下移到新增 `## Skill 体系细节`：注入可见性的 UI 细节与消费点枚举、宿主工具兼容性完整判据（`allowed-tools` 三种写法归一、`foreignToolRefs` 提取规则、9→15/36 漏报实测、`tool.id` vs 工厂键）、`meta.yaml` 为何是死数据（零引用链路）、DB 核对步骤、本工具 `search_content` 的 `glob` 怪癖、守卫「示意引用」判据。
- `MEMORY.md` 保留红线：`meta.yaml` 勿删结论、`tool.id` 铁律、`enabled=true` 才算合计、拖拽 `priority: i+1` ⇒ 列表序=注入序、`readonly: true` 核对、坑 ①③⑤⑥⑦⑧。

**结果**：`MEMORY.md` **7973 → 7202**（余量 27 → 798；距真实截断 ≈9.4k 尚有 ~2.2k 缓冲）；`TOPICS.md` 1810 → 3450。**红线 9/9 校验全 True**（`git commit`/`enabled !== false`/`36 = 7 core`/`tool.id`/`readonly: true`/`check-skill-refs.mjs`/`core.hooksPath`/`DB 配置优先铁律`/`显示与实际相反`）；结构 54 行完整。

**防复发**：文件头部补「**逼近上限时把细节下移 `TOPICS.md`，勿硬塞**；尾部 `## 协作与提交` 最先丢」——把处置动作写在未来会话必读的位置。

**未 commit**（`MEMORY.md`/`TOPICS.md` 位于 `.codebuddy/memory/`，仍有意不入库）。

### 🧭 第十六轮：日志导航层（会话开始记忆开销 −80%）

- **问题**：核心记忆 10.6k 已治理，日志层 130.5k 未治理。会话开始按规则读「今日+昨日」= `09-12`(37.2k)+`09-11`(18.9k) = **56.1k**，是核心记忆的 5.3 倍。根因：日志从「简短备注」漂移成「完整工作日志」（`09-12` 单日 727 行 / 15 轮），而其结论已全部提炼进 `MEMORY.md`/`TOPICS.md`/`docs/`/`skills/README.md`。
- **落点**：新建 `.codebuddy/memory/INDEX.md`（2660 字符）= 三层读法 + 8 个日志的 **主题/@行号索引**（`09-12` 20 节、`09-01` 41 节等全部带锚点）+ 已出栈落点表 + 日志写入规范；`MEMORY.md` 头部加读法协议；§前端约定 两条纯工具 how-to（元素计数、SFC 编译验证）下移 `TOPICS.md §前端验证与测试工具`。
- **结果**：会话开始记忆开销 **67.5k → ~13.5k（−80%）**；`MEMORY.md` **7072**（余量 928）；红线 8/8 `True`；`@行号` 锚点抽查 3/3 正确（`09-12@713`=第十五轮、`09-11@86`=画风扩充、`09-03@19`=尾帧语义分离）。
- **红线**：日志是**存档**不是工作台 —— 默认只读 `INDEX.md`，**勿整读日志**；需要证据才按行号跳读。
- **防复发**：`INDEX.md` 末尾写入规范 = 每轮 **≤8 行**、只写「结论/落点(文件:行)/红线/未决」、不写过程与命令回放（要留证进 `docs/`/`TOPICS.md`），**写时假定将来只会按行号跳读**。
- **更正（同日）**：上轮记的「`MEMORY.md` 有意不入库」**有误** —— `git ls-files` 证明 `MEMORY.md` 与历史日志（`08-29`~`09-11`）**已在 git 纳管**；untracked 的只有 `TOPICS.md`/`INDEX.md`/新日志，且 `.codebuddy/memory/` **无 gitignore 规则**。故 `MEMORY.md` 新增的读法指针存在**跨机断链**风险。事实已按约定改写进 `MEMORY.md §协作与提交`。
- **未 commit**（同前）。

### ✅ 第十七轮：暂存改名批次端到端验证（记忆治理收口）

- **引用守卫复跑**：`node scripts/check-skill-refs.mjs` = **140 文件 / 92 处 / 致命 0 / 非致命 0 / 跳过 5+2+3** —— 与基线**逐项一致** ⇒ 36 个 skill 改名**零回归**。
- **守卫能力实证（关键）**：注入 `` `references/missing-xyz.md` `` 真断链 → **致命 1 处 / exit 1** 且正确报 `文件:行号`；清理后回基线 0 ⇒ **基线 0 可信**。⚠️ 反转上一步结论：首测「裸路径断链未被检出」**不是守卫失效** —— `collectTokens` **只采集反引号 code span 与 markdown 链接目标**，裸路径本就有意不在范围。
- **DB 绑定核对（决定性）**：`{ readonly: true }` 直开 `data/drama.db` → `agent_configs` **5 行 `skills` 全为 `NULL`**、旧 `minimax/*` 命中 0、绑定未落盘 0 ⇒ **改名对绑定零影响**（DB 空 ⇒ 默认绑定=唯一生效来源）。
- **库自洽核对**：磁盘 **36 = 7 core + 9 genre-templates + 20 production-tools**；`library.yaml` 的 `name`/`label`（片型模板库/制作工序库）与 `MEMORY.md` 完全一致 ✓。
- **钩子实测**（`git -c core.hooksPath=.githooks hook run pre-commit`，**未写 git 配置**）：A 真实暂存含 `skills/` → 触发校验并放行（exit 0）；B 空暂存 → **不打印校验行、早退 0** ⇒ 三分支逻辑正确。
- **索引锚点全量校验**：`09-12`21 + `09-01`28 + `09-11`7 + `08-29`9 + `09-03`4 + `09-02`3 + `08-31`3 = **75 个 `@行号` 全部吻合**（非抽查）；补上遗漏的**第十六轮@727** 并更新快照（38.0k / 736 行 / 16 轮）。
- **缺口/未 commit**：`core.hooksPath` 为空 ⇒ **守卫不生效，启用需改 git 配置 → 待授权**；暂存区完好（183 项、含 `_tmp` 0 项），临时脚本与探针已清理。

### 🧱 第十八轮：记忆层补机器守卫 + 修索引快照（上一轮的自我回归收口）

- **修复自我回归**：上轮为写 DB/守卫结论把 `MEMORY.md` 抬到 **7,501**（余量仅 499），本轮把**与 `TOPICS.md` 重复**的细节下沉（宿主工具判据 / 改名核对步骤 / 守卫细节 / `/skills` 字段清单）→ **7,346**（余量 654）；`坑⑧` 压缩为「红线+指针」，新增 `坑⑨`（改记忆后必跑守卫）。
- **索引快照修复**：`INDEX.md` 原写「38.0k / 736 行 / **16 轮**」→ 改「38.5k / 746 行 / **17 轮**」并补 **㉒十七轮@737**；「写入规范」里的日志行数 727 → 746。
- **新增 `scripts/check-memory.mjs`**（记忆层守卫）：致命 = ①三层文件齐 ②`MEMORY.md` ≤ **8000 字符** ③`INDEX.md` 每个 `@N` 的日志存在 / 不越界 / 落在「小节首行」；提示 = 余量 < 400。退出码 1 = 致命。
- **锚点规则**：`@58/66` 视为**两个**锚点（都要过），`@51~64` 只校验起点；「小节首行」接受 `###` 标题**或** `- 【…】`（`2026-09-03.md` 用后者，实测另 4 处会误报）；锚点**只在 `## 日志清单` 内解析**，尾部规范段落的 `@N` 不算锚点。
- **刻意不校验「N 行 / M 轮」**：索引自述为快照，且历史取值口径不一致（实测 6 篇日志**恒差 1**）⇒ 当检查项只会产生噪声；被保证的不变量是**锚点**。
- **全量实证**：基线 **7 篇日志 / 75 锚点 / 致命 0 / exit 0**；负向 **越界锚点 → exit 1**、**8146 字符超预算 → exit 1**；还原后回基线 0。钩子重构为**双检查汇总退出码**，实测 skills 分支 / 记忆分支**各自独立触发**（用临时索引验证，未写 git 配置）。
- **补检查项 ④（本轮追加）**：`check-memory.mjs` 增「每篇日志的**末节**必须有登记锚点」。原检查项 ③ 只保证**已登记**的锚点不失效，**查不出**「追加了日志却忘更新 INDEX」——本轮真踩（第十八轮已写入日志、索引仍停在 17 轮）⇒ 补 ④ 后**首跑即捕获**（`09-12 @747`，致命 1 / exit 1），回填索引后回 0。只查末节、不查全量小节：索引对历史日志本就是摘要式。
- **文档补登**：`scripts/README.md` 原只列 `check-skill-refs.mjs`，本轮补 `check-memory.mjs` 行 + 分级说明 + 「两道守卫均已接入 `.githooks/pre-commit`」。
- **换行属性核对**：`.githooks/pre-commit` 与 `scripts/check-skill-refs.mjs` 均为 `i/lf w/crlf`，`core.autocrlf=true`、仓库无 `.gitattributes` ⇒ 入库 blob 为 LF、检出按平台还原，Windows/POSIX 双端执行均安全，**无需额外改动**。
- **未决**：`.githooks/pre-commit` 现为 `AM`（**暂存版已过期，需重新 `git add`**）；`scripts/check-memory.mjs`、`INDEX.md`、`TOPICS.md`、本文件均**未纳管**；`core.hooksPath` 仍为空 ⇒ **守卫不会自动跑，启用需改 git 配置（待授权）**。**未 commit**。

### 🧱 第十九轮：守卫补 ⑤⑥ + 收敛首版误报（继续「把人工不变量转机器校验」）

- **补 ⑤「磁盘日志须全部登记」**：③④ 只覆盖**已登记**的日志块 ⇒ 「新的一天新建 `YYYY-MM-DD.md` 却忘登记」时**完全查不到**（这是 ④ 之后剩下最大的缺口）。负向实证：造 `2026-09-13.md` → `✗ 日志未登记进 INDEX.md` / 致命 1 / exit 1；删后回 0。
- **补 ⑥「落点表路径须存在」**：`## 已出栈的落点` 表承诺「优先看这些，别翻日志」，断链即失去意义。**并与 `文件 §小节` 指针合并校验**（小节名须在该文件内出现）。负向实证：`docs/api-contract.md` → `…-missing.md` ⇒ `✗ 落点表引用的路径不存在`；`MEMORY.md §Skill` → `§SkillZZZ` ⇒ `✗ 落点表的小节指针落空`，均 exit 1；还原回 0。
- **首版误报收口**：⑥ 初版扫 INDEX **全文** ⇒ 被日志摘要里的 `` `models.vue` `` 撞出**致命 1 处假警**。改为**只扫落点表小节** + 裸文件名只认 `MEMORY`/`TOPICS`/`INDEX.md` 与日期日志 ⇒ 校验 12 处 / 致命 0。**教训：摘要里的历史文件名是叙述、不是引用**（同「快照行数」噪声，见脚本头「刻意不校验」段）。
- **基线**：`7 篇已登记 / 7 篇在盘 ｜ MEMORY 7360/8000 ｜ 锚点 77 处、落点表路径 12 处（§小节 2 处）｜ 致命 0 / exit 0`；同步 `scripts/README.md` 分级说明 + `INDEX.md` 落点表行描述。
- **未决（同上轮未变）**：`.githooks/pre-commit` 为 `AM`（暂存版过期）；`check-memory.mjs`/`INDEX.md`/`TOPICS.md` 未纳管；`core.hooksPath` 未设 ⇒ 守卫**不会自动跑**。**未 commit**。

### 🧪 第二十轮：给守卫配自检（`test-guards.mjs`）

- **动因**：①②③④⑤⑥ 每一项都是**手工造故障 + 手工还原**验证的，证据一次性；而「守卫已失效」**不会自己暴露**——正则被放宽、白名单被删、分支被短路，基线**照样是绿的**（真实仓库本来就合规）⇒ **不报错 ≠ 还能报错**。守卫本身成了唯一没有守卫的东西。
- **做法**：`check-memory.mjs` 加 `MEMORY_DIR` 覆盖（仿仓库既有 `SEEDANCE2_CORPUS` 惯例）；`test-guards.mjs` 把真实记忆层拷进临时目录 → 造故障 → 断言「**退出码 1 + 命中预期文案**」。**真仓库全程只读**（跑完 `rmSync` 临时目录，实测 TEMP 残留 0、10 个记忆文件哈希未变）。
- **8 用例全过**：基线（未改动 ⇒ 0 致命）+ ①缺 TOPICS ②超 8k ③锚点 `@9999` ④抹掉最新日志末节锚点 ⑤造 `2099-12-31.md` ⑥落点路径不存在 ⑥b`§小节`指针落空。
- **反向控制（关键证据）**：把 ⑥ 的两处 `fatal.push` 人为短路 → **恰好 ⑥/⑥b 转红、其余 6 例仍绿**（`6/8`、exit 1）⇒ 用例**非空过**且相互隔离；还原后 `8/8`。
- **两个易踩点**：① 块注释里写 `skills/**/*.md` 会**提前闭合注释**（`*/`）⇒ Node 直接语法错，注释内勿写该串；② 用例的字符串替换**找不到原文即判失败**（报「夹具失配」），夹具过时不会被静默跳过。③④ 用例按 INDEX 的**日志块**定位锚点（锚点数字会跨日志重复，全局找首个会打错块）。
- **刻意不做**：**不覆盖 `check-skill-refs.mjs`**（需另造 skills 夹具，成本收益不匹配；其三分支仍为手测）；不校验「未报致命」以外的输出（避免把措辞变动变成回归）。
- **收尾补漏（钩子只挂了两道）**：原 `.githooks/pre-commit` 只在 `skills/`、`.codebuddy/memory/`、`check-skill-refs.mjs`、`check-memory.mjs` 被动时触发 ⇒ **改 `check-memory.mjs` 时只跑它自己，而它必然返绿**（实测：哑掉 ⑥ 后第 2 段仍打「致命 0 处」）⇒ 上轮的整个论证在钩子里**恰好漏掉**。新增第 3 段：staged 含 `check-memory.mjs`/`test-guards.mjs` 时跑 `test-guards.mjs`。
- **钩子反向控制（真触发）**：哑 ⑥ + 真 `git add` → 第 2 段绿、第 3 段 `6/8` + `FAIL: 守卫自检未通过（某个检查已失效），提交已阻止` + `HOOK_EXIT=1` ⇒ **「守卫被改哑」这条路真被堵住**；还原后 `8/8` + `HOOK_EXIT=0`。
- **顺带踩到**：`git add -N`（intent-to-add）**不进** `git diff --cached --diff-filter=ACMRD` ⇒ 拿它触发钩子会静默跳过（第 2/3 段都没跑，白测一轮）；验证钩子必须**真 `git add` + 用完 `git reset --` 还原**。手动触发钩子用 `git -c core.hooksPath=.githooks hook run pre-commit`（不必真提交）。
- **未决（同前）**：`.githooks/pre-commit` 为 `AM`（工作区已含第 3 段、暂存版仍旧）；`check-memory.mjs`/`test-guards.mjs`/`INDEX.md`/`TOPICS.md` 未纳管；`core.hooksPath` 未设 ⇒ 三道**都不自动跑**。**未 commit**。

### ⚙️ 第二十一轮：CodeBuddy 自有模型接入 `deepseek-flash`（IDE 环境配置，非项目代码）

- **动因**：用户给出 DeepSeek 官方价目（flash 全项便宜、是 v4-pro 的 1/3.4~7.5，差价在「输入」最大）后要求「便宜的加进 CodeBuddy」。
- **落点**：`C:\Users\Administrator\.codebuddy\models.json`（**用户级**；项目级 `<ws>/.codebuddy/models.json` 实测**不存在** ⇒ 无覆盖，改用户级即全局生效）。新增 id `deepseek-flash` 一条，**原有 4 条一律未动**。
- **字段口径（官方文档，非推测）**：`id`/`name`/`vendor`/`url`（**必须完整路径、以 `/chat/completions` 结尾**）/`apiKey`/`maxInputTokens`/`maxOutputTokens`/`supportsToolCall`/`supportsImages`/`supportsReasoning`；`availableModels` 缺省 = 显示全部；**热重载 1 秒防抖**。⚠️ 网传的 `baseUrl`/`model`/`provider` 是**错字段名**（分别应为 `url`/`id`/`vendor`）；官方明确**仅支持 OpenAI 格式**。
- **规格对齐**：flash = 上下文 **1M** / 输出 **384K** / **支持图像理解** / 思考模式**默认开** ⇒ 新条目填 `1048576`/`393216`/`supportsImages:true`/`supportsReasoning:true`。
- **旧名红线**：模型名应用 `deepseek-flash`；`deepseek-v4-flash` 对应模型**已下线**（仍可调用、按 Flash 计费）。**未擅自删旧条目** —— 当前会话正在用它，删掉会让在选中状态失效。
- **未决**：① 旧 `deepseek-v4-flash` 是否删除（保留下拉会出现两个 Flash）② `deepseek-v4-pro` 的 128K/32K 同样偏小于官方 1M/384K，**用户未要求故未改**。**未 commit**（本条不涉仓库文件）。
- **端到端实测**：`GET /v1/models` 只返 **`deepseek-flash` / `deepseek-v4-pro`** 两个名字 ⇒ 旧名 `deepseek-v4-flash` **已从官方模型列表消失**（**能被服务端别名兼容 ≠ 仍在列表里**）；`POST /chat/completions`（`model=deepseek-flash`）200、回显 `model=deepseek-flash`、用量 in=37/out=19 ⇒ **配置端到端可用**。**沉淀判据：查模型名真伪用 `GET https://api.deepseek.com/v1/models`（零 token 消耗）**。
- ~~**未验**~~ **已推翻（同日更新）**：`supportsImages:true` 与上下文/输出上限**均已实测通过**（见下条）。
- **补二（官方规格核对 + 参数修正 + 全量实测）**：官方现只认 `deepseek-flash` / `deepseek-v4-pro`，**两者同规格**（1M 上下文 / 384K 输出 / tool call / **默认开思考模式**），**唯一差异 = 图像理解（flash ✅、pro ❌）**。据此修正：① `deepseek-v4-pro` 128K/32K → **1M/384K**；② 旧名条目 `deepseek-v4-flash` 参数同步 **1M/384K**、`supportsImages` false→**true**、`supportsReasoning` false→**true**，name 改「**旧名·建议选下方 V4.1-Flash**」——**不删**（当下会话正跑在它上面，删 = 会话失效）。
- **实测（全部通过）**：`pro` ping OK（in=90/out=31）；`flash` 图像理解 **答 `Blue`**（64×64 纯蓝图，in=228）；旧名请求 `model=deepseek-v4-flash` → 服务端**回显 `model=deepseek-flash`** ⇒ **「旧名 = V4.1-Flash 别名」获服务端亲证**（比文档更硬）。
- **判据沉淀**：模型名真伪查 `GET /v1/models`（零 token，实测只返这两个名字）；**「能调通」≠「在列表里」**。
- **未验**：Ollama 三条 `supportsToolCall:true`（本地 11434 **未启动**，探测连接被拒）→ 能力位**保持原样**，待 Ollama 启动后复核。
- **留意**：官方页含「**2026-09-14 之后继续提供 V4 Pro API**」字样 ⇒ 今日（09-12）实测可用，**09-14 前后留意 pro 是否变更**。
- **补三（CodeBuddy `models.json` 官方字段全集 + 本地 Ollama 环境探明）**：
- **字段全集**（用户级 `~/.codebuddy/models.json` ｜ 项目级 `<项目>/.codebuddy/models.json` **后者优先**）：`id`(**唯一必填**)/`name`/`vendor`/`apiKey`/`maxInputTokens`/`maxOutputTokens`/`url`(须完整到 `/chat/completions`)/**`temperature`(0–2)**/`supportsToolCall`/`supportsImages`/`supportsReasoning`；顶层另有 **`availableModels`**（白名单控下拉显示；未配置或空数组 = 全部；**项目级完全覆盖用户级不合并**；过滤在合并后执行）。`models` 走 **SmartMerge**（同 id 覆盖 / 异 id 追加），自定义模型自动打 **`custom` 标签**；**热重载 1s 防抖**；**仅支持 OpenAI 格式**；**无「禁用思考」开关**（只有 `supportsReasoning` 声明能力）。文档自相矛盾：字段表写 `vendor`、故障排查写 `provider` 且称三者必填 ⇒ **以字段表为准**。
- **本项目现状**：用户级 6 条（3 Ollama + 3 DeepSeek）；**未用 `availableModels`**（故下拉全展示）、**未设 `temperature`**（留默认）。⚠️ 本项目 `configs/models.json`（`description/models/nodes/version`，是 **Drama Studio 自己的模型节点配置**、无密钥、已被 git 追踪）与 CodeBuddy 的 models.json **同名不同物** ⇒ 勿混用、勿改前者。
- **密钥加固**：doc 要求 apiKey 明文且项目级配置常随仓库提交 → 本项目 `.gitignore` **原本零 `.codebuddy` 规则**，而 `.codebuddy/` 已被追踪（memory 下 7 文件）⇒ 已新增 **`.codebuddy/models.json`** 忽略规则（零副作用，当前无该文件）。
- **Ollama 真实环境**：exe `D:\app\ollama\ollama.exe`；**机器级 `OLLAMA_MODELS=D:\app\ollama\models`** ⇒ **`~/.ollama/models` 为空 ≠ 没装模型**（勿据此误判）；实测已拉 **12 个 / 35.24GB**，含配置未列出的 `qwen3:14b`、`deepseek-r1:1.5b/7b`、`llama3.1:8b`、`gemma3:4b`、`my-doctor:0.1/0.2`、`nomic-embed-text`、`qwen2.5-coder:1.5b-base`。配置里 `LLaVA` 未带 `:latest` 而 manifests 实为 `LLaVA:latest` ⇒ **Ollama 自动补 tag，非 bug**。
- ~~**待办**~~ **已完结（同日）**：见「补四」。
- **补四（`ollama show` 权威实测 → 修正三条 Ollama 条目的 4 个字段）**：**`ollama show <model>` 是 Ollama 能力的权威来源**（直出 `Capabilities: completion/tools/vision/thinking/insert` + `context length`），**比 HTTP probe 快且不必加载模型**；exe `D:\app\ollama\ollama.exe`，服务已起（`UP count=12`）。
- **8 个模型实测**：`qwen2.5-coder` 7.6B/**32768**/tools+insert ｜ `deepseek-coder:6.7b` 7B/**16384**/仅 completion ｜ `LLaVA` 7B/32768/vision ｜ `qwen3:14b` 14.8B/**40960**/tools+**thinking** ｜ `deepseek-r1:7b` 7.6B/131072/**thinking** ｜ `llama3.1:8b` 8B/131072/tools ｜ `gemma3:4b` 4.3B/131072/**vision** ｜ `qwen2.5-coder:1.5b-base` 1.5B/32768/insert。
- **修正前是真错**：三条 Ollama 条目原来**全把上下文写成 131072、输出写成 65536** ⇒ **高估 4–8 倍，且输出上限超过上下文**（逻辑不可能）。已改为：`qwen2.5-coder` 32768/8192 + `reason:false`；`LLaVA` 32768/8192 + `tool:false` + `reason:false`；`deepseek-coder:6.7b` **16384**/4096 + `tool:false` + `reason:false`。
- **HTTP probe 交叉印证**（产物 `%TEMP%\ollama_probe.txt`，已跑完）：`deepseek-coder:6.7b` 带 tools 请求直接 **400** ⇒ 双重印证其无 tools；`qwen2.5-coder` **未返回结构化 `tool_calls`**（把调用意图输出成纯文本 content）⇒ **capability 声明有 tools 但 OpenAI 兼容端点行为不稳**，`supportsToolCall:true` 暂保留待复测。
- ⚠️ **操作教训**：`replace_in_file` 与**读同一文件**的 `execute_command` **并行会读到旧值**（竞态假象）；**改完配置必须单独再校验一次**再下结论。

### ✂️ skills 第二十二轮：提示词词库按介质拆分（图像 / 视频分家）

- **问题（实测驱动）**：`prompt-style-library` 27,099 B / 14,241 字符，逼近 `skills/README.md` §七.5 的 30 KB 上限；其第 6 节「中文视频提示词范式」占该文件 **55%**（270/486 行），而它绑定的 `grid_prompt_generator` 是**纯出图** Agent（自身 SKILL 明写"只负责宫格"）⇒ 每次出图白吃整套视频范式。
- **默认注入实测法**：`loadAgentSkills(agent, null)` 即可复现默认绑定（**不起后端、不碰 DB、无清洗副作用**），拆前数字 grid 18,898（自身 1,902 + psl 13,976 + srr 3,068）｜sb 15,315（1,363 + 13,976）｜合计 **37,263** ⇒ 与 README 原记录**逐字吻合**。
- **做法**：第 6 节整章**机械搬家**（按行范围切分：`[IO.File]::ReadAllText` + `-split "`n"` + `WriteAllText(UTF8Encoding($false))`，**保留 CRLF**）→ 新 skill `video-prompt-library`（`agents: [storyboard_breaker]`、`priority: 30`）；原库该位置留**占位说明** ⇒ **§7/§8 编号不变、零交叉引用破坏**。
- **结果（最终实测）**：grid 18,898 → **11,818（−37%）**；sb 15,315 → 16,596（+1.28k = 拆分后两份 frontmatter 的固定开销）；总注入 37,263 → **31,464（−15.6%）**；psl 27,099 B → **11,176 B**；vpl 18,916 B；**37 = 8 core + 9 + 20**。
- **守卫**：141 文件 / 92 处 / **致命 0 / 非致命 0**（140→141 是新增 skill 的预期变化，待校验数不变）。
- **同步落点**：`skills/README.md` §一（7→8）、§三（数字 + 体量治理说明）、§九（相关位置拆两条）；`MEMORY.md` §Skill（7→8 + "词库分家"红线）。
- **坑（新，PowerShell）**：`@($a -like 'x', $b -like 'y')` 会被解析成 `$a -like ('x', $b -like 'y')` —— **逗号优先级高于 `-like`**，断言必假；每个表达式须单独加括号。（另：`-like '- [ ] *'` 里 `[ ]` 是字符集，须改用 `.StartsWith()`。）
- **未决（待用户定夺）**：`style-reference-reverse`（反推画风）绑在 `grid_prompt_generator` 上**语义存疑** —— 全仓库 0 代码引用、无专属 Agent 承接，占该 Agent 注入 3,068 字符；修法可选 `agents: []`（纯按需）或改绑，**当时未动**（→ 已在下一轮处理）。

### 🚫 skills 第二十三轮：停掉无执行入口的默认注入

- **判据「三查皆空」**（上一轮的存疑项，本轮补齐证据链后动手）：① `search_content` 搜 `反推|suggested_style_key|reusable_anchors` 于 `backend/src` → **0 命中**；② 搜同一批关键词于 `frontend/app` → **0 命中**（此前一次搜索命中的 6 个文件全是「参考图上传」，与「反推」无关）；③ 无专属 Agent。⇒ `style-reference-reverse` **没有任何执行入口**，绑在 `grid_prompt_generator` 上纯属白占上下文。
- **做法**：`skills/style-reference-reverse/SKILL.md` 的 `agents: [grid_prompt_generator]` → **`agents: []`**，并在 frontmatter 上方写清**原因 + 恢复方式**（将来接入该能力时改回即可）。**文件保留**，UI 仍可手动绑。
- **顺带**：删掉 `prompt-style-library` §0 里的**后端函数名清单**（`buildCharacterArtStyleSuffix` 等 8 个，8 行表格）—— 对 Agent 无操作价值，只保留「链路全覆盖且正负成对」的结论（psl 渲染 6,896 → **6,607**）。
- **结果（实测）**：`grid_prompt_generator` 绑定变为 `[grid_prompt_generator, prompt-style-library]`，注入 **11,818 → 8,485（−55% vs 原始 18,898）**；`storyboard_breaker` 16,596 → 16,307；总注入 **31,464 → 27,842（−25.3% vs 原始 37,263）**。
- **守卫**：141 文件 / 93 处 / **致命 0 / 非致命 0**。
- **同步落点**：`skills/README.md` §三（合并记录两轮治理 + 新增红线「**加 skill 前先问它有没有执行入口**」）；`MEMORY.md` §Skill（停注入一行）；`TOPICS.md` §Skill 体系细节（停注入判据 + 两次治理的复现命令）。
- **方法论沉淀**：`loadAgentSkills(agent, null)` 是**离线复现默认注入的零副作用入口**（不起后端、不碰 DB、不触发 `agents/index.ts` 清洗），配合 `tmp/` 下临时 tsx 脚本即可量化任何 skill 改动前后的注入体量。
- **顺带二（取词表外移）**：`prompt-style-library` §5 的**外部站点表**（Civitai / Danbooru / PromptMart / PromptHero / Promptomania / Film-Grab，约 690 字符）对 Agent **零价值（Agent 不能上网）** ⇒ 移出到新建 `docs/prompt-style-sources.md`（含红线 + 「项目内已有外部库」说明），skill 里只留索引 + 一句「这些站点你访问不了，缺词时用近义锚点或告知用户」。顺带修掉其中「已归纳进第 6 节」的失效引用。
- ⚠️ **守卫盲区（发现即修）**：`check-skill-refs.mjs` 的 `REPO_ROOT_PREFIXES` 原为 `['skills/','backend/','frontend/']`、**不含 `docs/`** ⇒ skill 正文里 `` `docs/xxx.md` `` 落进「基准不明」被**静默跳过**（指到空处也不报）。已补入 `docs/`，并同步脚本头 §③「解析基准」。
- **负向验证（证明确实生效，而非改哑后基线仍绿）**：① 正向 = 141 文件 / **95 处**（93→95）/ 致命 0 / **基准不明 5→3** / EXIT 0；② 临时把 `docs/prompt-style-sources.md` 改名 → **报出 2 处断链**（`skills/prompt-style-library/SKILL.md:161`、`skills/README.md:165`）/ EXIT 1；③ 恢复文件名 → EXIT 0。⇒ skills 内 `docs/` 引用**恰 2 处**，现均已真校验（此前等于无人看管）。
- **基线同步**：`skills/README.md` §十 基线 140/92 → **141/95**，并删掉失效的「详见脚本头注释」指向，改为直写基线 + 一句「增删 skill 会改变文件数与处数（属预期），**致命必须恒为 0**」；`scripts/README.md` 守卫描述补上 `docs/` 前缀。⚠️ `test-guards.mjs` 当时**不覆盖** `check-skill-refs.mjs`（`scripts/README.md` L103 注明"取舍"）⇒ 改此守卫时必须人工做正向 + 负向双验证（→ 已在下一轮改为可自检）。

### 🧪 第二十四轮：两处维护性补强（MEMORY 腾空间 + 引用守卫纳入自检）

- **① `MEMORY.md` 逼近上限**（7767/8000，余量仅 233）⇒ 把 4 段**已有 `TOPICS.md` 对照版**的细节压成一行指针：注入可见性 / 宿主工具兼容性 / `meta.yaml` / 改名核对 DB 绑定。结果 **7767 → 7383**（余量 233 → **617**）；同时把坑⑧的基线由 140/92 更新为 **141/95**，并补上 `docs/` 已于 09-12 纳入校验。
- **② 引用守卫纳入自检**：`test-guards.mjs` 原本只在脚本头声明「**不覆盖** `check-skill-refs.mjs`，成本收益不匹配」。本轮查明**真正原因不是成本** —— 而是该守卫把 `SKILLS_DIR` **硬编码**，没有类似 `check-memory.mjs` 的 `MEMORY_DIR` 覆盖入口 ⇒ 副本根本指不过去。
- **做法**：给守卫加 **`SKILL_REFS_DIR`** 覆盖（只覆盖 skills 树，`docs/` 等 repo 根相对引用仍按真实 ROOT 解析 ⇒ 用例贴近实战）；自检脚本加 **skills 树夹具**（`cpSync` 拷整棵树，造小夹具会漏掉真实引用形态）+ 4 用例（基线 / ⑦真断链 / ⑧`docs/` 断链 / ⑨示意引用不误报）。
- **结果**：`test-guards` **12 用例全绿**（记忆 8 + 引用 4）。⑧ 是**回归锁** —— 它锁住 09-12 才补的 `docs/` 前缀：今后若有人把它从 `REPO_ROOT_PREFIXES` 删掉，⑧ 立刻转红。
- **自检自身踩的坑**：首版把 `expect: 0` 的判据写死为 `/致命 0 处/`，而引用守卫输出的是「致命**断链** 0 处」⇒ 两条本该绿的用例被误判红。已改为 `/致命[^\n]*0 处/`（覆盖两套文案）。
- **同步落点**：`scripts/README.md`（test-guards 职责「7 种故障」→「10 种 + 2 基线」、致命分级补 `docs/`、重写「自检」小节说明真正原因）、`check-skill-refs.mjs` 脚本头（用法补 `SKILL_REFS_DIR`）。
- **方法论**：**守卫要能被自检，前提是扫描根可注入**（`MEMORY_DIR` / `SKILL_REFS_DIR`）。今后新增任何守卫，第一天就该留这个口子，否则很容易在脚本头写下"成本不匹配"这类**错误归因**并把盲区固化下来。

### 📦 第二十五轮：落盘提交（abc6cac）

- **用户明确授权提交**（"没有需要修改的就提交吧"）⇒ 本会话首次执行 `git commit`（`MEMORY.md` 的"未经明确要求绝不 commit"红线未变）。提交前核查两个疑点，结论都是**已完成的正确清理、无需再改**：① `skills/local-model-evaluation.md` 与 `skills/preset-skill-template.md` 磁盘上**已不存在**（移到 `docs/`；git 一度显示为 unstaged `D` + staged `A`，`git add` 后正确配对成 `R100` / `R97`）；② `skills/grid_prompt_generator/reference/{character,scene,shot}-prompt.md` 3 个**已删除**，内容上收到 `backend/src/shared/prompt-blocks.ts`（全仓库唯一残留引用是该文件注释里的**来源说明**，不是依赖）。
- **提交**：`abc6cac` `feat(skills): restructure libraries, split prompt word libs, harden guards` —— **231 文件 / +5352 / −943**，含 164 个 R100 重命名（`skills/minimax/{builtin,installed}` → `{genre-templates,production-tools}`）。提交后 **工作区 pending = 0**。
- **提交信息落盘法**：写 `tmp/commit-msg.txt`（`tmp/` 已 gitignore）+ `git commit -F` —— 规避 PowerShell 传多行/中文 message 的乱码。
- ⚠️ **坑（新）**：`git log --format='%an <%ae>'` 里的 **`<` 被 shell 当重定向**，会把后续整条命令交给 `cmd.exe` 执行（报 `'Select-Object' 不是内部或外部命令`），**看起来像提交失败**。确认提交结果要用**不含 `<` 的命令**（`git log -1 --format="%H"`）。
- **上一个提交**是用户自己的 `6ae28c7 feat: 提交代码`（会话开始前即与 `origin/main` 同步）。
- **同步落点**：`MEMORY.md` §协作与提交 的「memory 三者须同批」→「**现已全部纳管**（含 `TOPICS.md`/`INDEX.md`/各日日志）」。
- **仍未做（需用户）**：`git config core.hooksPath .githooks` —— pre-commit 守卫**至今未生效**；且 **`origin/main` 未 push**（目前只是本地提交）。

### 🧰 第二十六轮：scripts 优化（修钩子漏触发 + 一键自检 + 换行加固）

- **① 修 `pre-commit` 的真实缺陷**：第 3 道（守卫自检）触发条件写成 `^scripts/check-memory\.mjs$|^scripts/test-guards\.mjs$` ⇒ **漏了 `check-skill-refs.mjs`**，即"改引用守卫时自检根本不跑"（改哑无人拦）。已改为 `^scripts/(check-memory|check-skill-refs|test-guards)\.mjs$`；并删掉两处会漂移的"8 用例"硬编码文案（改为"用例数见脚本头"）。
- **② 第 1 道补 `^docs/`**：skill 正文会引用 `docs/*.md`，改/删 docs 同样断链 ⇒ 现在改 docs 也触发引用守卫。
- **③ 新增 `scripts/check-all.mjs`**：一键跑三道自检 + 汇总（`--verbose` 透传给引用守卫）。动因：pre-commit 是**按资产条件触发**的，想全局体检得手敲三条命令，**容易漏跑 —— 而漏跑的恰恰可能是红的那道**。
- **④ 修正 `scripts/README.md` 目录定位**：原标题只写「AI/GPU 工具链」，与实际的**三类**（AI/GPU 工具 / 仓库自检 / 语料分析）不符；已改为三类对照表 + 「三类都不参与产品运行时」。
- **⑤ 修 CRLF 隐患（新发现）**：`git ls-files --eol .githooks/pre-commit` = **`i/lf w/crlf attr/(空)`** ⇒ 钩子由 sh 执行，CRLF 让 shebang 变 `#!/bin/sh\r`，Windows 上直接报 command not found。已新建 **`.gitattributes`**（**只**声明 `.githooks/* text eol=lf`，**不加 `* text=auto`** 以免全仓 renormalize 噪声）并把工作区文件转 LF ⇒ 现为 `i/lf w/lf attr/text eol=lf`。
- **验证**：`node scripts/check-all.mjs` → **3 道全过**（引用 141/95/致命 0 ｜ 记忆 致命 0 ｜ 自检 12/12）。
- ⚠️ **①② 的修复要等钩子启用才真正生效**（`core.hooksPath` 仍未设）—— 这是本轮唯一"改了但当前无感"的部分。

### 📦 第二十七轮：语料「按需处理」评估与落地（metadata.jsonl **不拆**）

- **用户问**：`data/prompt-corpus/seedance2/metadata.jsonl`（**37.58 MB / 8755 条**）**需要拆分吗**？—— 结论：**不需要拆**。
- **不拆的四条理由**：① 已 gitignored（`.gitignore:40`），不入库、不随分发；② 三个脚本做的是**全量统计**（词频 / 分布 / 命中率），拆成多文件反而要额外合并；③ 实测单脚本耗时 ≤ 2 s（897 / 1468 / 1974 ms）、内存峰值 ≤ 75 MB，**都不是瓶颈**；④ 全仓库搜索证实它**只被这 3 个离线脚本读取**，后端 / 前端 **0 处引用**，无任何运行时依赖。
- **真痛点不在磁盘而在内存 + 缺按需入口**，已落地两项：① `analyze3.py` 由「先全量 `recs` 再投影」改为**边读边投影**（只留统计用的 6 字段）⇒ **峰值 75.4 → 14.1 MB**（`tracemalloc` 实测）；② 三个脚本统一支持 **`SEEDANCE2_LIMIT=N`**（只读前 N 条有效记录），调试/抽样不必全量解析。
- **为何不给 `analyze.py`/`analyze2.py` 做投影**：它们是 **schema 探查**脚本（dump 原始结构 + key 频次 + `recs[0]` 样本）⇒ **全量是语义需要** —— 这从技术上解释了 README 里「三者并非严格超集、不能互相替代」。
- **验证手法（可复用红线）**：改后三者输出与历史存档**逐字比对** ⇒ `identical=True`（**4164 / 7813 / 4364 chars**），证明**默认行为零语义漂移**，`docs/seedance2-corpus-analysis.md` 的结论依然成立。**改这些脚本后必须照此复核。**
- **同步落点**：`scripts/README.md` 语料小节补「体量与按需」（不拆的三条理由 + 内存数字 + `SEEDANCE2_LIMIT` 用法 + 逐字比对提醒）。
- 顺带确认：`data/prompt-corpus/` 下的 `analyze{,2,3}.log`（8–15 KB）是各轮输出存档，同为 gitignored，且是 `docs/` 结论的**可复现证据** ⇒ **保留**。

### 🔎 第二十八轮：语料 → 检索最小闭环落地（**8987 条 / 3 源**）

- **用户方向**「语料 → 标注 → 检索 → 自动生成 Prompt」。**我的判断：方向对，但"先建 11 维全量标注"不是最优顺序** —— 1.2 万条 × 11 维是周级重活且**无 ground truth 验收标注质量**；"质量评分"维度更需配套生成结果，而语料视频当初明确没下。⇒ 改为**先验证检索有没有用**。
- **用实测纠正外部清单 5 处**：① `HitPaw` / `geekjourneyx` / `fantasylights` 三个被标 ★★★★☆~★★★☆☆ 的源**实测是空壳**（24 KB / 24 KB / **4 KB**，0 条数据）；② **TIP-I2V 被列"第一优先级 01"却自相矛盾**（CC BY-NC 禁商用，商业项目不该用）；③ Rapidata 标五星但仅 ~200 条；④ 提议 **Qdrant**（本项目不依赖，Docker 未装）；⑤ 提议 `data/raw|clean|classified`（与项目既有落盘约定冲突）。**结论已写进 `scripts/README.md` 防再被引回。**
- **新挖到的真源**：`Ericgood/seedance-prompt`（**254 KB / CC BY 4.0** / 112 条，**英文 prompt + 成套中文元数据** title_zh·tag_zh·description_zh）｜`flaqai/awesome_seedance_2_5`（7 md / **MIT** / 120 场景，中英各 60，自带**模式·时长·画幅**+**时间码分段**）｜⚠️ `OpenMontage` 是 57k★ 的 **AGPL-3.0 项目**（借鉴规则可以，**别复制内容进商业项目**）。
- **三脚本管线**（`scripts/corpus/`）：`fetch-raw.mjs`（GitHub → `data/prompt-corpus/<源>/raw/`，跳过已存在 + `--only=` 单源重试）→ `normalize.mjs`（3 源 → 统一 JSONL，**只抽能检索的最小字段集**）→ `search.mjs`（**2-gram 打分**检索，零依赖、毫秒级）。
- **实测**：**8987 条**（112 + 120 + 8755）；Seedance2 归一化统计与 `docs/seedance2-corpus-analysis.md` **逐项吻合**（duration 有效 8746、中文中位 245 字）⇒ 归一化没走样。**检索验证**：查询「雨夜 霓虹 街道 特写 手持」→ 命中 3947 条，**前 4 条全部高度相关**（覆盖度 80%），其中一条自带完整的 主题/动作/环境/镜头/风格/限制 结构。
- **两个实测坑**：① `flaqai` 首版解析**产出 0 条** —— ```text 围栏**跨行**而我**逐行** `match()`；改**全文切块**后 120 条 ✅；② 该源中文 `### NN.`、英文 `## NN.`（**层级不一致**），分类须取「本标题之前最近的**非场景**标题」。
- **网络约束（必记）**：本机 **GitHub API/raw 可用** ✅；**HF 直连超时、hf-mirror API 也不通** ⚠️，但**直链下载可用**（Seedance2 的 37.58 MB 就是这么下的）⇒ 采 HF 源要**先人工确认文件路径**。
- **提交** `c04590c`（3 新脚本 + `scripts/README.md`）。**下一步**：① 检索接进后端 Agent（→ 已在下一轮完成）；② 采 FilmBench（**许可待核**，本机 HF 不可达）。

### 🔌 第二十九轮：把语料检索接进后端 Agent

- **工具**：新建 `backend/src/agents/tools/corpus-tools.ts` → `search_reference_prompts(query, top?)`。数据源 `data/prompt-corpus/_normalized/prompts.jsonl`（**懒加载 + 进程内缓存**；路径用 `import.meta.url` 上溯 4 级，与 `agents/skills.ts` 的 `SKILLS_DIR` 同法，兼容任意 cwd / Docker）。
- **注册**：`agents/index.ts` 的 `createAgentTools` switch 里给 `storyboard_breaker` 与 `grid_prompt_generator` 用 `{ ...原工具, ...createCorpusTools() }` 合并 —— **只挂这两个**（只有它们需要「找相似镜头」的参考），其余 Agent 不动。
- ⚠️ **语料缺失必须降级**：`data/prompt-corpus/` 已 gitignore ⇒ **新克隆仓库里没有语料**。工具在 `existsSync` 失败时返回 `{ available:false, hint:'先跑 fetch-raw + normalize' }` 而**不抛错** —— 否则「没采语料」会直接变成 Agent 跑不动。
- **实测（两种状态都验过）**：① 语料在位 → 中文查询「雨夜 霓虹 街道 特写 手持」top3 全相关（20 分 / 覆盖 0.8）；英文查询命中的正是**新采的两个源**（证明数据加载路径正确）；空查询走 hint 分支；② **临时移开 `_normalized/` 目录** → `available=false` 不报错，还原后恢复 ✅。
- **口径必须同步**：工具打分口径与 `scripts/corpus/search.mjs` **刻意一致**（2-gram + 位置加权），两边文件头与 `scripts/README.md` **互相标注** ⇒ **改一处必须改另一处**，否则「命令行验证有效」与「Agent 实际效果」会分叉。
- **回滚点**：`agents/index.ts` 只加了两行 `case`；若工具出问题，删掉 `...createCorpusTools()` 即回到纯原状态。
- **验证手法（可复用）**：backend 侧新增能力用「临时探针（`backend/probe-xxx.ts`）直接 `import` 并调 `tool.execute(...)` + 结果落盘」来验，**不必起整个服务**；验完即删探针。

### 🧭 第三十轮：把「检索工具怎么用」写进 skill（**工具注册 ≠ 会被用**）

- **问题**：上一轮给 `storyboard_breaker` / `grid_prompt_generator` 注册了 `search_reference_prompts`，但**两个 SKILL.md 里根本没提这个工具** ⇒ 模型无从知道能检索。「已注册」不等于「会被调用」。
- **做法**：两个 SKILL.md 各加一节「卡在具体手法时：检索参考镜头（可选）」，写清三件事：① **该用**（某镜头具体手法拿不准、现有词表拼不出来时）；② **不该用**（每镜都查会拖慢且重复；需求没方向时查了也没用；**一轮至多 1–2 次**）；③ **拿到后怎么用**（✅ 借鉴镜头/光线/结构手法；❌ 不照抄原文 = 合规红线；❌ 不搬主体/品牌）。
- **写明降级语义**：返回 `available: false` 表示本机没装语料（语料不入库）⇒ 改用已有词表，**绝不能编造检索结果** —— 否则模型可能"假装检索到了"。
- **体量代价（实测）**：`storyboard_breaker` 16,045 → **16,561**（+516）；`grid_prompt_generator` 8,223 → **8,517**（+294）；总注入 27,318 → **28,128（+3.0%）**。判断：**这笔值** —— 9000 条语料的价值全靠这两节触发。
- **守卫**：`check-all` **3 道全过**（改动只在 skill 正文，未引入新路径引用 ⇒ 基线不动）。
- **同步落点**：`skills/README.md` §三（注入数字 + 累计段，并注明这次增量来自检索工具说明，防后人误以为"体量治理白做"）。

### 🧪 第三十一轮：**真跑 Agent** 验证检索（拿到硬结论 + 一个更要命的问题）

- **做法**：临时探针照 `evaluator.ts` 的 `seedCase`/`cleanupCase` 模式造**临时**数据 → 真调 `runAgentWithRetry('storyboard_breaker', …)` → 检查 `ret.toolCalls` → 物理清理。**不碰现有剧集**：库里原有 2 剧 / 6 集 / 46 分镜，验证前后**逐项一致** ✅。
- **第 1 次跑（结论无效）**：种子里**没造角色/场景** ⇒ `readStoryboardContext` 返回 `characters=0 scenes=0`，Agent 只读一遍上下文就结束（`storyboards produced=0`、`protocolStatus=missing`）⇒ **不能据此判断**。
- **第 2 次跑（有效：产出 3 条分镜、协议 ok）**：`toolCalls=[readStoryboardContext, saveStoryboards]` ⇒ **`search_reference_prompts` 未被调用** ⚠️。原因清楚：Agent 只用了 2 步、**没有"卡住"的时刻** —— 上轮写的触发条件「卡在具体手法时」**措辞太被动**。
- ⚠️ **同时暴露一个独立问题（可能比检索本身更重要）**：产出的 `video_prompt` 只有 **104 / 94 / 91 字符**，而 `video-prompt-library` 明确要求它含「时间推进 / 动作变化 / 镜头语言」⇒ **这个长度等于没写**。不是检索的锅，是**分镜产出的质量下限没被执行**（skill 里有要求，但 Agent 没照做）。
- **据此改 skill**：`storyboard_breaker` 检索节把触发条件由「卡住」改为「**发现自己只能写出笼统描述时**」（例：只写了「雨夜对峙」这种一句带过的画面），并加**硬性自检**：「保存前看 `video_prompt`，若只有一两句、不含镜头/光线/时间推进（实测常见不到 120 字符），就是没写到位 —— 先检索再补写」。
- **第 3 次跑（改后）**：**卡在 LLM 调用**（日志停在 `read-context`）⇒ `dramas` 2→3 出现残留，用 `cleanup-probe.ts` 定向清理（`dramaId=13/episodeId=22`）后库回到 **2/6/46/6/2** 原状 ✅。**结论未取得**（LLM 挂起原因未定位）。
- ⚠️ **副作用（坑⑦复现）**：`import agents/index.ts` 会触发 `[db] sanitized legacy equip prompts (changed=9)` —— **每次跑 Agent 都可能改库** ⇒ 验证脚本**必须用临时数据 + 用完清理**，别拿真实剧集试。
- **临时文件**：`backend/probe-db.ts`、`probe-retrieval.ts`、`cleanup-probe.ts` 与 `tmp/*` 已全部删除（验证方法记在此处，需要时按此重建）。

### 📏 第三十二轮：给 `video_prompt` 加可逐条核对的清单（并观测到一个异常信号）

- **动因**：第 31 轮真跑实测发现 `video_prompt` 只有 **91 / 94 / 104 字符**，而 skill 里那句「`video_prompt` 突出时间推进、动作变化、镜头语言」**太笼统、无法自我检查** ⇒ 模型写完就算过。
- **根因（读代码确认）**：`storyboard-tools.ts` 里 `video_prompt` 是 `z.string().optional()` —— **工具层零校验**（保存时只有 `dialogueIssueCount` 一项对话角色检查）⇒ 多短都能存进去，质量全靠提示词层自觉。
- **做法（纯提示词，零代码风险）**：`skills/storyboard_breaker/SKILL.md` §质量下限 把笼统要求改为**三类信息清单** —— ① **镜头**（景别/运镜）② **时间推进**（至少两个连续动作，用「先…再…最后…」或时间码）③ **光线/氛围**（取词库锚点）；并写明「实测最常见的失败形态：只写『雨夜对峙』这类一句带过的画面，往往不足 120 字符 —— 那不是合格的视频提示词」。
- ⚠️ **验证出现异常信号（未定论，勿据此判定本轮改动有害）**：加 180 秒硬超时后重跑（上次就是卡在 LLM 调用上没回来），本次 **53.7 秒完成**，但 **`toolCalls=1`（仅 `readStoryboardContext`）、`storyboards produced = 0`** —— **没保存任何分镜**，而 `protocolStatus=ok`。对比同种种子数据的第 31 轮：`toolCalls=2`（read + save）、产出 3 条。
- **但同一份日志里有个反向信号**：`outputTokens` 从 1402 **涨到 2060（+47%）** ⇒ 它在更认真地写，只是**没调 `saveStoryboards`**。三种可能：① LLM 随机性；② 我新加的约束（检索节 + 三类清单 ≈ 700 字符）挤占注意力；③ 本次多传了 `maxSteps: 10`（**但只用了 1 步，未触限**，故可能性最低）。
- **处置**：已改探针记录回复正文（`ret` 顶层键 + text 前 700 字）并**重跑一次**判断是否复现 —— 结果待取。**复现才说明有问题；单次样本不足以下结论。**
- **顺带修正**：`skills/README.md` §十 引用守卫基线 **95 → 96**（实测值）；并把 blockquote 里 `` `prompt-style-library/SKILL.md` `` 补全为 `skills/prompt-style-library/SKILL.md`（缺 `skills/` 前缀会让它落进「基准不明」而非真校验，基准不明 3→4 即由此而来）。
- **守卫**：引用 96 处 / 致命 0；记忆 致命 0；`test-guards` 12/12。
- **临时文件**：`backend/probe-quality.ts`（用完即删）。
- ✅ **复现 + 拿到决定性证据（同日补跑第二次）**：同样 `toolCalls=1`、`storyboards produced = 0`，但探针记录了回复正文 —— **模型其实把分镜写得非常完整**（title / shot_type / angle / movement / dialogue / description / result / atmosphere / image_prompt / first_frame_prompt / last_frame_prompt 齐全，共计 **4400 字符 Markdown**），**只是全部写在回复正文里，完全没调 `save_storyboards`**。
  ⇒ **这不是「没写」，是「写成了展示而非行动」**（要求变复杂后，模型倾向把作业摊开给人看而不是落库）。对照第 31 轮同模型同种子 `toolCalls=2`（read + save）且产出 3 条 —— 唯一新增变量就是本轮那份清单。
- **对症修法**：在该清单后加一句明确指令 —— **⚠️ 这三条是给你自己核对的，不是要你把分镜方案写进回复；分镜必须通过 `save_storyboards` 落库，回复正文只用一两句话交代结果**，并附上实证（4400 字没入库）。**「内容写对」不等于「任务完成」。**
- ⚠️ **方法论教训**：**给 Agent 加"质量要求"会改变它的行为模式** —— 要求越具体，越容易触发"展示模式"（把成果写在回复里）而非"行动模式"（调工具）。**加长约束后必须验证「工具调用数」是否掉了**，不能只看输出质量。`ret` 的顶层键为 `model / text / toolCalls / toolResults / protocol / protocolErrors / usage`。
- ⚠️ **第三次复现（加「必须调工具」指令后）仍是 `toolCalls=1`**，且这次模型**自己在回复里写了「并调用 `save_storyboards` 保存」**（4208 字符详案），**实际并没调**，`protocolStatus=missing protocolErrors=1`。
  ⇒ **三条结论**：① 问题**不在 skill 措辞**（连"必须落库"写进去也无效，3 次连续复现）⇒ **不要再改 skill 了**；② 模型**理解要求但没执行工具调用** ⇒ 根因在**模型 / 工具调用机制**（当前 DB 配置的文本模型是 `MiniMax-Text-01`）；③ 新加的三类清单**与本次回归无关**（第 3 次更严也没用）⇒ **保留**（质量要求本身正确），但**无法证明其效果**（因为没落库）。
- **对照数据（同探针、同种子剧本）**：第 31 轮 `inputTokens=42553 / toolCalls=2 / 产出 3 条`；第 32 轮三次均 `inputTokens≈28k / toolCalls=1 / 产出 0`。**上下文少 14k 却反而不落库**，二者非单调 ⇒ 更像模型行为波动，而非"约束太长挤掉注意力"（变量太多，**未定论**）。
- **未决 / 交给用户**：要确认是否模型问题，需**换一个文本模型**（如 `deepseek-flash`）跑同一探针 —— 这要改 DB `ai_service_configs`，**我没动**。探针与 `tmp/quality-probe.txt` 已删除，需要时按本轮记录重建。
- 🎯 **【结论】元凶 = 我自己加的 skill 措辞（回退即修复）**。把 `skills/storyboard_breaker/SKILL.md` 回退到第 31 轮「只有检索节 + 长度自检」的版本后，**同一探针立刻恢复**：`toolCalls=2`（`readStoryboardContext` + `saveStoryboards`）、**落库 2 条**、`reply text` **5817 → 56 字符**（只剩协议块）、耗时 **64s → 29s**。⇒ 不是模型、不是工具集、不是 token 上限。
- **元凶机制（高价值提示词教训）**：我加的原文是「⚠️ 这三条是给你自己核对的，**不是要你把分镜方案写进回复**……实测踩过这个坑：某次把 **4400 字**的分镜方案完整写在回复里而没调工具」。**反向指令 + 反例示范双双适得其反** —— 「不要写长文」激活了写长文，「4400 字」还给了**长度示范**。铁证：加之前 `reply text=4208`，加了之后**反而涨到 5817**。
- ⭐ **铁律**：**给 Agent 加约束后，必须同时检查「工具调用数」是否掉了**；只盯输出质量会漏掉「内容更漂亮但一条没入库」这种最坏情况。**不要用「反例 + 禁止」的写法**，要写**正向、简短**的动作指令。
- **顺带修正（真 bug，已保留）**：`agents/index.ts` 两处 `new Agent` **从未下发输出上限** ⇒ 落服务端默认（实测 MiniMax 约 2048 就停）。已在 `agent.generate` 的 `options.modelSettings.maxOutputTokens` 显式传 `dbConfig.maxTokens || 4096`。⚠️ **构造处不吃这个字段**（`AgentConfig` 无 `modelSettings`，`tsc` 报 TS2353）**必须走 generate options**。本次实测 `out=892` 未触限，故**不是**本次元凶，但属正确的健壮性修复。
- ⚠️ **仍未解决（原始问题）**：`video_prompt` 依旧只有 **90 / 92 字符**。**但不能再用「写长」的方式压它**（已证明会引发不落库）。下一步方向：把要求写进 `save_storyboards` 工具字段的 `describe()`（工具描述短而直接，比 skill 长文更有效），或改为保存时后端校验 —— **两条都需再各验证一次**。
- **探针 bug（记下避免重犯）**：`/save_storyboards/i` **匹配不到驼峰 `saveStoryboards`** ⇒ 当时输出的 `save_storyboards CALLED: NO` 是**误报**（`toolCalls` 里明明有）。写判据时要考虑工具名驼峰化。
- 🔬 **【13 次实测终局结论】落库不稳定 = 系统性问题（非模型、非措辞、非配置）**。同一探针、同种子剧本、同一函数路径：
  | # | 变更 | toolCalls | 落库 | 回复长度 |
  |---|---|---|---|---|
  | 1 | 原始（第 31 轮） | **2 ✅** | 3 条 | 短 |
  | 2–4 | +三类清单 / +禁止段 | 1 ❌ | 0 | 4208→5817 |
  | 5 | **回退到 #1 的同一份代码** | **2 ✅** | 2 条 | 56 |
  | 6 | +字段 describe | 1 ❌ | 0 | 3767 |
  | 7 | **再次回退到 #1 的同一份代码** | **1 ❌** | 0 | 2908 |
  | 8 | +下发 temperature | 1 ❌ | 0 | — |
  | 9–11 | 改写工作流措辞 | 1 ❌ ×3 | 0 | — |
  | 12–14 | **换模型 `abab6.5s-chat`** | 1 ❌ ×3 | 0 | — |
  ⇒ **14 次仅 2 次成功（≈14%）**；**#5 与 #7 代码逐字相同却结果相反** ⇒ **纯随机，此前"是措辞的锅"的结论作废**（那是单次对比的误判）。
- **排除项（逐条有实测证据）**：① 非 skill 措辞（回退后仍失败）；② 非字段 `describe`；③ **非模型**（换 `abab6.5s-chat` 同样 0/3）；④ **非 token 截断**（`out` 峰值 2612 ≪ 4096）；⑤ 非 `maxSteps`（只走 1 步）；⑥ 非缺工具（`save_storyboards` 在工具集内，且默认提示词工作流第 4 步明确要求调用）；⑦ 非 DB 覆盖（`system_prompt` 空 / `skills` NULL / `model` NULL，全走默认提示词）。
- **稳定失败模式**：成功 ⇒ `reply=56 字符`（仅协议块）、29s；失败 ⇒ `reply=2908~5817 字符`（分镜全文）、64~72s。模型在「调工具」与「写作业」之间二选一。**成功那两次恰好只填极少字段**（`video_prompt` 仅 90~92 字符）。
- **最可能根因（待验证）**：`save_storyboards` 的 **input schema 有 33 个字段**，构造巨型 tool call 成本高，而提示词又要求「为每个镜头补全完整分镜字段」⇒ 模型倾向把内容输出成文本。**降低工具 schema 复杂度**（如拆分为「先存必填骨架 → 再 `update_storyboards` 补细节」）可能才是正解。
- **已保留的修复**：`agent.generate` 的 `options.modelSettings` 显式下发 `maxOutputTokens`（`dbConfig.maxTokens || 4096`）与 `temperature`（`dbConfig.temperature ?? 0.7`）。此前两者都是**死配置**（DB 有值、代码从不读）。⚠️ 构造 agent 时不吃这两个字段（`AgentConfig` 无 `modelSettings`，tsc 报 TS2353），**必须走 generate options**。实测下发后未改善本问题，但属独立正确性修复。
- **建议方案（未实施）**：**代码兜底** —— 在 `runAgentWithInstructions` 中检测「本轮未调用业务写入工具（`save_*`）」则自动重跑一次（两次独立成功概率 ≈96%）。属通用改动、影响面较大，**待用户定夺**。

### 🔁 第三十三轮：后端换 Python 的工时评估（回答「需要多长时间」，未改代码）

- **体检（`backend/src` 实测）**：**133 文件 / 27,389 行** —— routes 37/7.8k（**226 端点**）、services 53/10.8k（含 adapters 17/1.6k）、agents 13/3.0k、db 4/1.4k（**29 表**，`schema.ts` 626 + `index.ts` 内联 DDL 685）、shared 6/2.0k（`prompt-utils` 1,456）、evaluation 6/1.0k、utils 11/1.1k、middleware 1/59。
- **重耦合点**：`@mastra` 12 处、drizzle 64 处、`@ai-sdk` 4、MCP SDK 4；`z.object` 28、`child_process` 15、定时器 17、raw SQL `exec` 17；SSE 2 处（`/auto-pipeline/stream/:dramaId` + aiConfigs 流式代理）；`index.ts` 兼做静态站（Range + SPA fallback + 前端 dist 托管）。
- **零自动化测试**：`*.test.ts` / `*_test.py` 实测均为 0 ⇒ **无回归安全网**，验收只能手工全量。
- **分层估算（单人全职 + AI 辅助，人日）**：骨架/契约/DB 8–12 ｜ 226 端点 CRUD 20–30 ｜ 17 适配器 + AI 服务 15–22 ｜ Mastra 替换（Agent 循环 + 工具协议 + skill 注入 + MCP）15–25 ｜ 长任务/异步/恢复/GPU/QC/评测 18–28 ｜ 联调回归 20–30 ｜ 部署打包 3–5 ⇒ **合计 99–152 人日**。
- **现实口径 ≈ 4.5–6.5 个月**（中位 4.5 月 × 1.2–1.3 缓冲：无测试网 / 厂商 API 真机重验 / Windows 坑）；2 人并行 ≈ **3–4 月**（受共享契约与联调串行约束，**不能按人头线性折半**）；砍评测闭环 + 本地模型扫描 + 部分适配器可压到 2–2.5 月，但丢掉差异化能力。
- **成本大头不是语法翻译**：① 零测试 ⇒ 验收占 25–30%；② Mastra 的工具调用循环 / 结果归一 / token 统计须重建并**用真实 LLM 复验**（本项目已有「改提示词 → 工具不再被调用」三次复现史，这块行为极脆）；③ 17 个厂商适配需逐个真机跑通；④ **前端零改动是硬约束**（改契约则成本再翻倍）。
- **可直接搬（零重写）**：提示词/词表资产 ~3.4k 行（`prompt-utils` + `prompt-blocks` + `skills/**`）；SQLite 数据文件同库同表 ⇒ **无数据迁移**。
- **与 09-01 结论一致且当前成本更高**：09-01 已判「不换，维持 TS 业务层 + Python 计算层混合」—— 真正需要 Python 的 ComfyUI(8188) / `local_services/h3/server.py`(8765) / `scripts/*.py` **本就是 Python 且经 HTTP/subprocess 解耦**，换 Python 不带来这些收益；当时 107 个 ts，现 **133 个 / 226 端点**。更省的路：Python **侧车**只承载 AI/GPU 能力 **1–2 周**；**绞杀者**（FastAPI 逐域接管 + 反代 Node）每域 1–3 周，全量仍需上述总工时。**未改任何代码；未 commit。**
- **追补（用户追问「一天用 AI 能否换完」+ 已下「必须要换」指令）**：**一天不行**。两个口径须分清：AI **当助手**（人主写）= 99–152 人日；AI **当主力**（人只审阅+验证）= **50–80 人日**。
- **地板由验证锁死、而非生成速度**：226 端点冒烟 3–5 天 ｜ 5 Agent 行为回归 2–4 天（本项目有 **3 次「改提示词 → 工具不再被调用」**复现史，必须重采多次）｜ 17 厂商真机 3–7 天（配额/异步等待）｜ 长任务链路 4–8 天 ｜ 前端零改动走查 2–4 天 ⇒ 单人 14–28 天 / 2 人 8–14 天（部分可并行）。**最短「能替换」= 单人 3–5 周 / 2 人 2–3 周**（降级验收、只冒烟）；要求可发布 ×1.5–2。
- **一天（10h AI 全力）真实产出** ≈ FastAPI 骨架 + 29 表 SQLAlchemy 模型 + 统一响应 + 静态站（Range/SPA）+ 40–80 个简单 CRUD 端点 = 能起来、前端能列剧目，但**仅占总量 8–15%**，服务层/Agent/适配器/长任务仍为空壳。
- **已给用户的推荐打法**：既然是「必须换」，走**绞杀者**而非大爆炸 —— Python 跑新端口（Node 5789 不动）→ FastAPI 作入口、未接管域反代 Node → 逐域切换（CRUD → AI 服务 → Agent/长任务）→ 每域即验即上线、可回退 ⇒ **第 1 天即有可跑产出**。**未改代码；未 commit。**
- **补：已按此思路开工并落地第一版** —— 见下一轮。

### 🐍 第三十四轮：Python 后端（`backend-py/`）绞杀者迁移第一版落地

- **用户指令**「必须要换」⇒ 不再等确认、直接开工最短路径（**一天换不完**的结论不变，所以选择「第一天上线第一域」而非大爆炸）。
- **新增 `backend-py/`（`backend/` 一行未改）**：`app/{config,db,models,response,main}.py` + `routers/dramas.py`（9 端点）+ `services/{era_background,bible_ids}.py` + `tests/smoke_test.py`；FastAPI + SQLAlchemy **Core**（刻意不用 ORM：Core 的行**本身就是 snake_case = 契约形状** ⇒ 原 Node 的 `toSnakeCase()` 转换层在 Python 侧**不存在**，少一层漂移源）。
- **绞杀者接缝已实测有效**：`main.py` 末尾兜底路由 `/api/v1/{path}` 与 `/webhooks/{path}` → `PROXY_TO_NODE=1` 反代 Node(5789)、`=0` 返 501 并提示未迁移；实测 GET/POST/查询串/请求体/webhooks **全部原样转发**，且**已迁移域不被吞**。
- **端口铁律**：Python 默认 **5790**，`config.py` **刻意不读 `config.yaml` 的 `server.port`**（那是 Node 的 5789，并存期读同一端口必抢占）；优先级 `PY_PORT > PORT > 5790`，全迁完后用 `PY_PORT=5789` 切单端口。
- **契约三条对齐约定（已写进 `backend-py/README.md`）**：① 信封 `{code,data,message}`、**错误响应无 `data` 键**，而 FastAPI 默认 `{"detail"}` 会让前端文案变「请求失败 (422)」⇒ `HTTPException`/`RequestValidationError`/兜底异常**全部收口**（特例：`/api/v1/health` 是裸对象）；② `GET /dramas/:id/prompts` 聚合视图**本来就是 camelCase**，照抄勿「顺手统一」；③ **SQLAlchemy `**kwargs` ≠ JS 对象展开** —— `values(**fields, title=...)` 同名键直接 `TypeError: got multiple values`，必须 `values={**fields, ...}` 再覆盖（**已真实踩到并修复**）。
- **模式核对（比对照 `schema.ts` 更硬，用真实库 `PRAGMA table_info`）**：发现**真实库比 Node 模型多 2 表（`assets` / `props`）与 2 列（`image_generations.minio_url` / `video_generations.minio_url`）** ⇒ 全是旧版本 MinIO 遗留（Node `db/index.ts` 只建 **29** 表、全仓库零引用）⇒ **Python 同样不建模**；**列序差异不影响正确性**（SQLAlchemy 全程按列名生成 SQL，不会位置化 INSERT/SELECT）。
- **验证**：`backend-py/tests/smoke_test.py` **64/64 通过**（模式 + 信封 + **错误文案逐字** + 软删 + 反代 501；写操作全部落在 **DB 副本**，真库只读）；另真起 uvicorn 实测 `/health` 200、`/api/v1/dramas` 200（total=2，与真库一致）、`/api/v1/storyboards` 501。
- **新坑（工具）**：`tempfile.mkdtemp()` 在 Windows 返回 **8.3 短路径**，与 config 侧 `resolve()` 的长路径做子串比较会**假红** ⇒ 必须比较 resolve 后路径；`sqlalchemy` 顶层导出的是 **`REAL` 而非 `Real`**；PowerShell 组合命令含 `&` / `@{...}` 会被路由到 cmd（报「不是内部或外部命令」）⇒ **写 `.ps1` 落盘再执行**。
- **落点**：`backend-py/README.md`（运行方式 / 环境变量 / **下一个域的迁移 SOP** / 已知差异表 / 工期参考）；`.gitignore` 补 `.venv/`。**未 `git add`、未 commit。**
- **更正（同日，用户指令「将 ts 先提交，再新建分支」）**：上句「未 commit」已作废 —— `backend/src/agents/index.ts`（补下发 `temperature` 的 TS 修复）已提交到 **`main`**：`33e5fa5 fix(agent): send temperature to generate; both modelSettings were dead config`；随后基于 main 新建并切换到 **`feat/python-backend`** 分支承载 Python 迁移。**`backend/probe-schema.ts` 未提交**（头部自述「临时探针（用完即删）」）。
- **第二域 `episodes` 已迁完**：8 端点（POST/PUT/DELETE + `characters`/`scenes`/`storyboards`/`pipeline-status`/`script-fingerprint`）；`continue-script` 与 `consistency-qc` 依赖 LLM/视觉模型 ⇒ **刻意不注册**走反代。连带把 `services/script-fingerprint.ts` **整服务**搬成 `app/services/script_fingerprint.py`（纯逻辑，含 `checkEpisodeFingerprint`/`checkStoryboardGate`/`stampStoryboardsScriptHash`）；另抽共享的 `app/request_utils.py`（`read_json`）供两个域复用。
- **验证**：冒烟 **64 → 103/103 通过**（新增指纹过期判定、关联聚合、十步流水线、软删二次 404）；真起 uvicorn 复验 `script-fingerprint` 200/`has_script=True`、`pipeline-status` 10 步、`characters` 3 条、未迁移端点仍 501。
- **新踩的两个 JS 语义坑（已写进 README + `TOPICS.md`）**：① `scriptContent ?? content ?? ''` 是**空值合并不是逻辑或** —— `script_content=''` 时**不回退**到 `content`，写成 Python `a or b` 会让门禁判定与 Node 分叉（本轮用 sha256 **逐位**校验，含 CRLF→LF 归一化）；② `GET /episodes/:id/pipeline-status` 的「有图」判据是 `composed_image` **单字段**，而 `GET /dramas` 的 `progress` 是 `composed_image || first_frame_image` —— 原 TS 本就是两套，**照抄勿合并**。
- **再更正（同日，用户指令「继续提交代码」）**：Python 侧已提交到 **`feat/python-backend`**：`3c9bb82 feat(backend-py): add Python backend with strangler migration (dramas + episodes)`（**18 文件 / +3414**，含 `.gitignore` 的 `.venv/` 规则）+ `1766687 docs(memory): …`。暂存前已 dry-run 核对 **`.venv`/`__pycache__` 零泄漏**；**`backend/probe-schema.ts` 有意未提交**（头部自述临时探针）。**工作区已干净**（仅剩该探针未跟踪）。
- **第三批：`characters`(4) + `scenes`(4) + `props`(5) 共 13 端点已迁完**（累计 **5 域 / 30 端点**）；`characters.merge` 的**资产版本按 (media_type, frame_type) 分组接续重编号**（含「target 该组原无 current 则保并入最新一条为 current」分支）逐行照抄。未注册的仍是媒体/LLM 端点（`generate-*`、`auto-split-visuals`、`generate-prompt`）。
- **⭐ 本批最大发现：返回形状按域不同** —— Node 里**显式调过 `toSnakeCase` 的域返回 snake_case**（dramas/episodes），**直接返回 drizzle 行的域返回 camelCase**（characters/scenes/props，因 drizzle 返回 TS 属性名）。证据：`CharacterEditor.vue` 直接用 `c.voiceStyle`，而 episode 工作台写的是 `c.voice_style || c.voiceStyle`（**防御式双读**）。Python 侧为此新增 `response.row_to_camel/dict_to_camel/rows_to_camel` 与例外表 `_COLUMN_TO_JS_OVERRIDES`。
- **两个真 bug（均被新用例抓到并修复）**：① **`Unconsumed column names: voiceStyle`** —— TS 的 `updates[key] = body[key]` 里 key 是 drizzle **属性名**，Python Core 只认 **DB 列名** ⇒ 双写兼容分支必须写 `updates[snake] = body[camelKey]`；**因为先测 snake 入参，这个 bug 被掩盖了一轮**。② 断言直接用 `r.json()["data"]` 在意外状态码上崩掉，**丢了整份报告** ⇒ 测试改 `r.json().get("data") or {}` + 失败信息带 `r.text`。
- **实测证据**：`tmp/schema-pairs.mjs` 扫 `schema.ts` **493 个字段** ⇒ 「属性名 ≠ camelCase(列名)」**仅 1 处**：`prop_templates.customPrompt ↔ image_prompt`。冒烟 **103 → 153/153**；真起 uvicorn 复验 `/characters`、`/scenes` 均返回 camelCase、`/props/1.5` 404、未迁移端点 501。
- **id 解析两套并存（勿合并）**：`props` 用 `parseId`（`Number.isInteger`，严格）、`characters`/`scenes` 用 `parseParamId`（`Number.isFinite`，**放行小数** ⇒ `GET /scenes/1.5` 走到查库才失配，报 `Scene not found` 而非 `Invalid scene id`）。已把 `response.parse_param_id` 改为**镜像 JS 的 `Number()` 语义**（原实现 `int()` 会拒小数，属保真度修正）。
- **第四批：`storyboards`(5 端点) 已迁完**（累计 **6 域 / 35 端点**）。该文件 Node 侧 **14 个 route def 里只有 5 个是纯 DB 逻辑**（创建/更新/删/台词校验/QC 读取），其余依赖 TTS/出图/ffmpeg/LLM/视觉 QC ⇒ **不注册走反代**。连带移植两个纯函数服务：`character_match.py`（说话人别名归一匹配）+ `storyboard_helpers.py`（关联同步、台词解析、TTS 匹配校验）。
- **JS 正则语义三坑（本轮实测踩到并已固化）**：① **JS `$`（无 `m`）只匹配串尾，Python `$` 还会匹配结尾换行之前** ⇒ 必须改用 `\Z`（否则 `"音效\n"` 误判命中忽略列表）；② **JS `replace` 无 `g` 只替换首处，Python `re.sub` 默认全替换** ⇒ 显式 `count=1`；③ JS `while (re.exec)` ↔ Python `re.finditer` 语义一致，但**捕获组要自己加**（`\A(.+?)[:：]` 不能写成 `\A.+?[:：]`）。
- **另一个静默丢失类 bug（已修）**：`character_costumes` 是「角色 id → 服装」的 JSON 对象，**键在传输中是字符串**；JS `obj[3]` 会强转所以能命中，Python `3 in {"3":...}` **恒为 False** ⇒ 必须把键转回 int，否则镜头服装变体静默丢失（已有用例锁定）。
- **`success(c, undefined)` 契约**：JS 的 `JSON.stringify` 会**丢掉**值为 `undefined` 的键 ⇒ 原契约存在返回 `{"code":200,"message":"success"}`（**无 `data` 键**）的场景（如 `PUT /storyboards/:id` 无对话校验结果）。Python 侧新增 `success_without_data()`；前端 `json.data ?? json` 对「键缺失/null」同表现，但 `'data' in json` 会分叉，故照做。
- **继承的孤儿行为（知情不改）**：`DELETE /storyboards/:id` **只清 `storyboard_characters`，不清 `storyboard_props`** ⇒ 会留 props 关联孤儿行（原 TS 即如此）。已写进 README 已知差异表并有用例断言（断言它**仍然存在**）。
- **验证**：冒烟 **153 → 185/185**（新增台词三态匹配 matched/not_found/narrator、TTS 音色过期检测、costumes 键类型、无 data 键契约、硬删与孤儿保留）；真起 uvicorn 复验（真实库 episode 1 有 20 镜）：`qc` 200、`validate-dialogue` 200、`split` 501。
- **第五批：四个资源库 + `presets` + `app-settings` 共 47 端点已迁完**（累计 **12 域 / 77 端点**，冒烟 **241/241**）。四个库是「同一套代码换列清单」的复制体 ⇒ 用**规格驱动单一实现**（`app/services/resource_library.py` + `app/routers/libraries.py` 只放 4 份 `LibrarySpec`），比抄 4 份更不易漂移。
- **⭐ 发现第二套响应信封（只有资源库四个域用）**：成功 **`code: 0`**（不是 200）、**列表/详情没有 `message` 键**；错误 `code: 400/404/500` 但 **HTTP 仍是 200**（TS 是 `c.json(obj)` 不带状态码）。前端 `useApi.ts` 的 `!resp.ok || (json.code && json.code >= 400)` 正好解释了它为何要多看 `json.code`。资源库还走**原生 SQL**（列名=DB 列名）⇒ 返回体是 snake_case + 少量 camelCase 别名（`referenceImages`/`voiceConfig`），`tags`/`metadata` 原地替换；id 用 **`parseInt` 语义**（`/12abc` → 12）。
- **踩到一个「症状相同、成因不同」的坑**：`ArgumentError: List argument must consist only of dictionaries`。先入为主地以为是「list 该改成 tuple」，**改完仍然报同一个错**；用隔离探针（`tmp/rl_probe.py` 直接调 `conn.execute`）才定位真因 —— **SQLAlchemy 的 `text()` 根本不支持 `?` 占位符**（只认 `:named`）。正确解是 `exec_driver_sql`（绕过编译交给 sqlite3），**且位置参数仍须传 tuple**（list 会被当成 executemany 的多组参数）。已收口为 `q_all/q_one/q_run`。**教训：同一个报错信息可能对应两个独立成因，别只改一个就宣布修好 —— 要写最小复现隔离验证。**
- **发现一个真缺陷（有意照抄未修）**：`PUT /app-settings` 的 `ART_STYLE_KEYS` **只有 6 种画风**，而画风体系 09-11 已扩到 **10 种** ⇒ 全局默认画风设成 `noir`/`ink-wash`/`cyberpunk`/`pixar3d` 会被 **400 拒绝**。权威列表在 `prompt-utils.ts` 的 `ART_STYLE_CATALOG` + 前端 `artStyles.ts`，`app-settings.ts` 那份是**过期的第三份副本**。**先照抄**（单边修会让切域那一刻行为静默改变），已写进 README 已有差异表 + 用用例把当前行为锁住（修好两边时该用例会红 = 预期信号）。
- **4 条断言是我自己写错的（Node 实际行为不同，已按实际改）**：① 未传的 JSON 列创建时被 `safeStringify(None)` 写成 `''` ⇒ 读回是 `[]` 而非 `None`；② 未传的文本列被 `|| ''` 落成**空串** ⇒ 更新传 null 时 `??` 保留空串而非 None；③④ 资源库的兜底枚举只在「全表无非空值」时触发（`IS NOT NULL` 对 `''` 成立）⇒ 有数据时返回的是实际值/`[""]`；**把兜底用例改到「直删数据」之后跑才验到真分支**。
- **第六批：`asset-versions`(2) + `traces`(3) + `storage`(1) + `usage`(2) 共 8 端点已迁完**（累计 **16 域 / 85 端点**，冒烟 **279/279**）。连带移植 `asset_versions`（含回滚写回主表）、`trace_store`（只读侧）、`data_storage`（只读侧）、`usage_tracking`（summary + 成本看板）。
- **⭐ 真跑才发现的一条「不是 bug 的 0」**：真库 20 个 trace，`/traces/stats` 却返回 `runs=0`。差点当成 bug 去改 —— 打开 jsonl 才看到 token 值被**脱敏成字符串 `"***"`**（20 个 trace 全如此），而 `Number("***")` = `NaN`、`NaN||NaN||NaN` 为假 ⇒ 整条被跳过。**Node 也是同样结果**，我的 NaN 处理恰好与 JS 完全对齐。已用「直造带 `"***"` 的 trace」用例锁住这条行为。**教训：返回 0 时先确认是「实现漏了」还是「数据本来如此」——这次先去看数据才没改错。**
- **又收口一个 JS 数值语义：`Math.round` ≠ Python `round`** —— JS 是「.5 向 +∞」，Python 是银行家舍入（`round(2.5)==2`）。成本金额统一按 `Math.round(x*10000)/10000` 保留 4 位，用错会差 1 个最小单位 ⇒ 收口为 `response.js_round`；另加 `response.js_number`（镜像 `Number()`，非有限值返回 None，**不校验 `>0`**，与 `parse_param_id` 不同）。
- **三个端点刻意不迁（各有明确理由，已写进 README）**：① `POST /storage/change` —— 写项目级 `.data-root` 标记文件而 **Node 启动也读它** ⇒ 从 Python 切目录会连带挪走 Node 的数据根（跨进程副作用）；② `GET /usage/estimate` —— 依赖 `estimate-service` + `cost-catalog`（厂商单价目录），属媒体域前置；③ trace 写入侧 `appendTraceEvent` —— 唯一调用方是 Agent 链路（未迁），Python 只读 / Node 写、纯文件无锁竞争，可共存。
- **第七批：`agent-configs`(5) + `style-profiles`(7) + `generations`(1) 共 13 端点**（累计 **19 域 / 103 端点**、冒烟 **330/330**）。刻意不迁：`/agent-configs/defaults`（依赖 `DEFAULT_PROMPTS` + SKILL.md frontmatter，属提示词/skills 域）、`/agent-configs/generate`（LLM）、`/style-profiles/:id/distill`（Mastra + ffprobe）；但 `/style-profiles/:id/apply` **已迁** ⇒ 用户可把 Node 侧提炼好的结果贴回来落库，链路不阻塞。
- **🔥 真跑抓到的两个 bug（都是自己写出来的，不是猜出来的）**：
  1. **布尔列全站返回 `1`/`0` 而非 `true`/`false`** —— `models._bool` 原用 `Integer`，而 drizzle 的 `integer({mode:'boolean'})` 在 JS 侧读出**真布尔**。改成 SQLAlchemy `Boolean` 一次修掉**所有已迁移域**（存储层同为 SQLite INTEGER，兼容既有库）。前端只要用 `=== true` 严格比较就会挂 —— 这类偏差不会报错，只会「功能莫名不对」。
  2. **参数路由遮蔽未迁移端点** —— 注册 `GET /agent-configs/{id}` 后，Node 侧未迁移的 `GET /agent-configs/defaults` **被它吞掉**，返回一个看起来很合理的 404「Invalid agent config id」，于是**静默丢掉了 Node 的实现**。对策：抽 `passthrough.py`（`delegate`）让各 router **显式声明委派且注册在参数路由之前**。
- **⭐ 新增常驻守卫 `tests/route_parity_test.py`（很值得）**：机械扫描 Node 全部路径，对「Node 有、Python 没注册」的每条路径**真发一次请求**，判据是**必须落到兜底 501**（`PROXY_TO_NODE=0`），否则就是被吞。为什么用行为判定而不是内省：FastAPI 的 `include_router` 会包一层 `_IncludedRouter`，`app.router.routes` 里**拿不到 `.path`**（第一次写就踩到，误报 100+ 条）。另一个坑：**参数名要归一化**（Node 是 `:id`→`{id}`，我按语义写成 `{drama_id}`），不归一化会把每条已迁移的详情路由都误报成「被吞」。⇒ 现输出：Node 131 条 / 已迁移 103 / 未迁移 28 全部正确走兜底 / **0 遮蔽**。
- **JS 数值/真值语义收口清单（已第 4 条）**：`Math.round`（`js_round`）｜`Number()`（`js_number`，不校验 `>0`）｜`a ?? b`（`js_nullish`，只看 null）｜**`[]`/`{}` 在 JS 是真值**（`js_truthy`，最易踩 —— `preferences ? JSON.stringify : null` 传空数组要落 `"[]"` 而非 null）。
- **第八批：`ai-configs`(8) + `ai-providers`(1) 共 9 端点**（累计 **20 域 / 111 端点**、冒烟 **363/363**）。纯 DB 子集：列表/新建(201)/一键配置/一键本地/详情/更新/**硬删**/本地列表/服务商目录；未迁 8 个全是外部依赖（`/ollama/*`、`/models`、`/test`、`/gpu/status`、`/gpu/release-all`、`/runtime/health`）。服务层附 `is_local_config`（来自 `gpu-manager.ts`，双判据：provider 白名单 + baseUrl 主机名；**openai 刻意不在白名单**——Ollama 的 OpenAI 兼容口靠 localhost 命中，真实云端 OpenAI 不能误判）+ `build_settings`（**合并而非覆盖**，保住 `checkpoint_map` 等既有扩展字段）。
- **🔥 第九批前夕抓到第三个真 bug，教训最深的一个：成功信封的 `data` 键**。我当初按「`JSON.stringify` 会丢掉值为 `undefined` 的键」推断出 `success(c, undefined)` 会**省略 data 键**，据此造了 `success_without_data()` 并用在 `PUT /storyboards/:id` 与 `DELETE /agent-configs/:id`。**但那是错的**：TS 是 `success(c, data: any = null)`，**JS 默认参数会把显式传入的 `undefined` 换成 `null`** ⇒ 契约实际是 `{"code":200,"data":null,...}`，键一直在。修法：删掉 helper、两处改回 `success()`。
- **⭐ 最该记住的是「为什么没早发现」**：我为这条错误「发现」写了断言 `"data" not in json`，**测试锁住的是我自己的实现，不是 Node 的契约**。⇒ 写断言必须对着 Node 源码/真实响应，不能对着自己的代码。已把这条写进 README 第 5 条并在测试里留下反向看守（`PUT storyboard 无 dialogue -> data:null（键存在）`）。
- **两个操作安全提醒**：① `ai-configs/quick-preset` / `quick-local` 是 **upsert（按 service_type+provider）**，会**覆盖真实库里的既有配置** ⇒ 真机验证只跑只读接口，冒烟只在数据库副本上跑；② `ai_service_providers` **不由 Python seed**（Node 启动时幂等 seed；该表**无唯一约束**，两边同时 seed 有重复行风险），等 Node 下线再搬 seed 调用。
- **守卫扫描表补充**：`aiConfigs.ts` 已登记；`/api/v1/ai-providers` 与 `/ai-configs` **同源一个文件**（`aiProviders` 是文件内独立导出的 Hono app，一个文件只映射一个前缀）⇒ 走 EXTRA 白名单。现守卫输出：Node 148 条 / 已迁移 111 / 未迁移 37 全部正确走兜底 / **0 遮蔽 0 多余**。
- **第九批：`skills` 整域 6 端点**（累计 **21 域 / 117 端点**、冒烟 **386/386**）。纯文件系统域（`skills/` 目录，非 DB）：列表 / 侧栏元信息 / 读取 / 保存 / 新建 / 删除。连带移植 `skill_parser.ts`(172 行: frontmatter 解析 + renderSkill + `hub_*` 外来工具引用提取) 与 `skills.ts`(218 行: core 列表 / 默认绑定 / 全量列表 + mtime 缓存)，这两个**无 Mastra 依赖**所以可整搬。
- **真机实测 skills**：37 个 skill（core 8 / vendor 29 / **protected 7** / 15 个有 `missingTools` / 22 个带 `references/`），`/skills/meta` 的 **coreCount(8) + Σlib(9+20) == 37** 与总数自洽（源码注释里的那条不变量真的成立）；外来工具前缀识别在真数据上命中 `hub_analyse_media` / `hub_generate_image` 等 ✓。
- **删除闸（`isProtectedSkill`）的判据要小心**：① 顶层（id 不含 `/`）**且** ② frontmatter `agents:` 非空。⚠️ `agents` 传 **`None`（解析失败→保守保护）与 `[]`（空声明→可删）语义完全不同**，别合并。测试里我用「新建临时 skill → PUT 成 `agents: [storyboard_breaker]` → 确认拒删 → PUT 回 `agents: []` → 确认可删」来验证**不碰任何项目资产**。
- **⭐ 新增「漂移守卫」**：`agent_registry.py` 是 `agents/index.ts`（AGENT_PHASES / Agent 显示名）与 `tools/*.ts`（tool.id）的**镜像副本**——Python 无法执行 Mastra 工厂，只能静态镜像。守卫直接从 TS 源码抽取比对，改名/加工具未同步会立刻红。**当前 0 漂移**（也反证我 grep 抽取的 22 个工具名/5 个阶段名/6 个显示名是准确的）。⇒ 原则：**镜像静态度量可以，但必须配自动漂移校验**，否则就是下一个静默失效源。
- **⭐ 一个刻意的"不做"决定**：`skills` 做完后 `/agent-configs/defaults` 本可以升级为已迁移（SKILL.md 那半边已经能算了），但**卡点是 `DEFAULT_PROMPTS[].instructions`（5 个 Agent 数百行提示词正文）——我决定不搬**。理由：Node 源码注释明确记载本项目吃过「提示词多头维护」的亏（前端曾自留副本并与后端漂移、还引用不存在的文件），把这份资产复制进 Python 等于**重建那个问题**。等 Node 下线（单一后端）再搬。⇒ 这不是"没做完"，是**有理由的不做**，已写进 README。
- **⚠️ Windows 控制台又咬一次（新形态）**：冒烟用例名里写了 `⊆` / `∪` / `∈`，`print` 时 `UnicodeEncodeError: 'gbk' codec` **直接打断整个测试**——而且是**在所有断言都通过之后**被"报告环节"打断，极易误判成业务 bug。⇒ 用例名只用 ASCII 可打印字符，已写进 README。
- **第十批：`upload`(3) + `export`(2/7) 共 5 端点**（累计 **23 域 / 122 端点**、冒烟 **412/412**）。`export` 是**裸响应域**（返回文件本体 + `Content-Disposition`，**没有统一信封**）——与 `preset-framework` 的 `{success,msg}` 一样属"信封例外"，前端当下载链接用。已迁的只有**纯 DB** 的工程账本（JSON/MD + 断点续作 stale）；未迁 5 个全依赖 ffprobe/archiver/媒体探测（`/edl`、打包 ZIP、`/jianying-draft`、`/qc-report`、`/contact-sheet`）。
- **新依赖**：`python-multipart`（Starlette 的 `request.form()` / `UploadFile` 必需）已加进 `requirements.txt` 并安装。`upload` 的类型判据沿用原 TS 的**客户端 Content-Type**（不做文件头嗅探）——**刻意不"顺手加固"**，改嗅探会让原本能上传的文件被拒，属契约变更。
- **🔧 我自己的用例曾偶发失败（已修）**：`traces list: sorted by mtime desc` —— 连续写两个 jsonl 时**文件系统 mtime 可能完全相同**，而 `list_traces` 按 mtime 倒序，相等时顺序不稳 ⇒ 断言随机红。修法是 `os.utime` 显式固定两个文件的时间戳。**教训：凡是断言"按时间排序"都要先把时间戳写死，别依赖文件系统分辨率。**
- **⚠️ Windows 编码坑第三次出现（形态又不同）**：临时 `.ps1` 探针里写了中文 → PowerShell 5.1 按 **ANSI 代码页**读 `.ps1`，而我的文件是**无 BOM 的 UTF-8** ⇒ 中文被打乱、**连引号都被吃掉**，报一堆 `Missing closing ')'`。三者合一的规则：**凡是要给 Windows 工具链读的文本（启动日志 / 用例名 / 探针脚本）一律只用 ASCII**。
- **🧹 探针残留**：后台执行被中断时，真机 upload 探针在**真实数据目录**留下了测试文件（`data/static/uploads/69b8e295-*.png`，68 字节），已手动删除。⇒ 对真实数据目录写文件的探针必须**自带清理**，且被中断就会留残留 —— 能只读验证就别写。
- **第十一批：`aiConfigs` 剩余 7 端点**（累计 **24 域 / 129 端点**、冒烟 **425/425**）：`/ollama/*`(status/start/pull/delete) + `POST /models` + `POST /test` + `GET /runtime/health`。连带移植 `provider_probe`（厂商 URL 拼接 / 探测描述 / 模型列举解析 / **key 遮蔽**）与 `ollama`（可执行探测 / 启停轮询 / 四大运行时探活）。
- **⚠️ 我上一轮的判断是错的，这里更正**：我曾说"剩下没有可迁目标了"——**不准确**。这些端点依赖的只是**子进程与 HTTP**（`where ollama`、`Popen`、nvidia-smi、厂商 probe），Python 完全等价。教训：**"依赖外部"≠"不可迁移"**，要分清"外部依赖"与"需要重建的框架"（Mastra/适配器才是后者）。
- **⭐ `/gpu/status` + `/gpu/release-all` 刻意不迁（架构理由，不是能力问题）**：它们依赖 423 行的 GPU 租约管理器，而**租约是进程内状态**——绞杀期两个后端各有一份租约视图 ⇒ VRAM 协调失效。真正的 GPU 工作（媒体生成）仍在 Node，**租约就该跟它待在一起**。⇒ 「能迁」与「该迁」要分开判断，跨进程共享的**内存态**是明确的不该迁信号。
- **🧪 新测试手法（值得复用）：本地假上游**。`/ollama/*`、`/models`、`/test` 原本需要真 Ollama 才能验证；我在测试里用 `http.server` + `threading`（**端口 0 自动分配**）起了一个假 Ollama（`/api/tags` 返回固定模型列表，其余 404），于是**标签省略匹配**（`qwen3` 命中 `qwen3:14b`）、体量标签、`reachable` 白名单（404 不算可达）、key 遮蔽全部可确定性断言。⇒ 这类"对外探测"端点一律可以用假上游测，别依赖环境里有没有装。
- **⚠️⚠️ 目标变更：用户要求「把 `backend/` 全量迁完然后删除它」**。⇒ 这不再是"翻译"而是**重写**：剩余约 **90 个端点 + ~12,000 行服务代码 + Mastra 替换**（正是最初评估里 50–80 人日的部分）。已定 **7 阶段依赖路线**（写进 `backend-py/README.md`）：S1 地基 → S2 提示词资产(`prompt-utils` 1456 行) → S3 厂商适配器(1611 行) → S4 媒体服务(ffmpeg/图像/视频/TTS) → S5 **Mastra 替换**(Agent 循环+6 组工具 ~2900 行) → S6 编排/长任务 → S7 收尾 + **删 `backend/`**。**`backend/` 只能在 S7 删**（删前要证明 224 端点等价）。
- **S1 地基完成**（累计 **24 域 / 130 端点**、冒烟 **448/448**、守卫 0 遮蔽 0 漂移）：`cost_catalog`(定价目录+`settings.pricing` 覆盖) / `estimate_service`(生成前费用预估，为此把 `/usage/estimate` 迁了) / `task_logger`(**含 trace 写入侧**，`append_trace_event` + 尾部愈合 + 脱敏 `sanitize_value`) / `ai_providers`(厂商配置解析，**所有适配器与 Agent 的入口**) / `protocol`(Agent 输出协议)。守卫口径也随之从 129→**130 已迁移 / 34 委派**。
- **🔥 又抓到一个"宽松比较看不出来"的真 bug：`js_number` 把整数返回成 float**。`Number("37")` 在 JS 序列化成 `37`，而 `json.dumps(37.0)` 是 `37.0` ⇒ **所有"回显 query 里数字"的端点都会吐 `1.0`**，前端用 `===` 严格比较或拼字符串就挂。**它是被一条字符串断言揪出来的**（`scope == "drama-37"` 却得到 `"drama-37.0"`），数值比较 `37 == 37.0` 为真所以完全掩盖。修法：`js_number` 在 `|x| ≤ 2^53` 内返回 `int`（更大时 JS 用指数形式，转 int 反而更不像）；并补了一条**字节级**断言（响应体里须是 `"asset_id":1,` 而非 `1.0`）。**教训：别只用 `==` 断言数字，要断言字节/字符串。**
- **trace 写入侧为什么用 `threading.Lock` 而不是 `asyncio.Lock`**：FastAPI 的同步端点跑在线程池里、媒体域又多线程回调 ⇒ 需要**跨线程**互斥；`asyncio.Lock` 不能跨线程。绞杀期两个后端同时往同一批 jsonl 追加也可共存（两边都是 O_APPEND + 单次小 write）。
- **`estimate-service` 的两个照抄怪点**：① 它的生效配置排序与 `ai.ts` **不同**（它 `is_default` 优先再比 priority，`ai.ts` 只看 priority）⇒ 不能合并；② 它传给 `estimateCost` 的 `model` 是**原始 JSON 列字符串**（没 `JSON.parse`），靠"包含匹配"歪打正着命中前缀 —— 能跑通但不是正常取法，别顺手改写。
- **S2 第 1/6 块完成**（累计 24 域 / 130 端点、冒烟 **466/466**）：`prompt_utils.py` 的画风层 + 负面词层（原 `prompt-utils.ts` L1–480）。原文件**约 1600 行**（不是 1456），已按依赖切成 **6 块**并把进度写进模块头：②角色/装备/道具/表情 → ③场景/分镜/视频 → ④DB 读取辅助 → ⑤台词一致性校验 → ⑥宫格。**三条硬性规则照抄进模块头**（命中画风用 `art+TAIL` / 未命中才回退 `VISUAL_STYLE_MASTER`，**二者二选一禁止叠加**；视频链只用 `VIDEO_*` 家族；各路由不得自建画风副本）。
- **⭐ 为 prompt_utils 加了「词表漂移守卫」——这是最该守的一处**：这些词表**就是生成结果本身**，改一个字就改变出图/出片，而两边不一致时**不报任何错**，只会「同一个剧在两套后端下风格不同」。守卫从 TS 抽 14 个字符串常量逐字比对（含 4 条以 `${NEGATIVE_BASE}` 拼的负面词模板），并比对画风目录 key 集合与 `DRAMA_ART_STYLE_MAP` 的 key 集合。**结果 0 漂移** ⇒ 我搬的词表可证明与 Node 逐字相同。
- **守卫自身踩的两个坑（都记下来给后来者）**：① 第一版正则在 TS 的**跨行拼接**（`= 'x, ' + \n 'y'`）与**值写在下一行**（`= \n 'x'`）上抽错/抽不到 ⇒ 必须能「续行吃掉 `+` 与未闭合引号」，并且**遇到下一条声明就停**（否则数组类常量会把后续声明一起吞掉，导致那些常量静默漏比对 —— 漏比误报更糟）；② 拼接时**不能做空白归一**，那会改坏字符串本身。**"未能抽取"要报成问题而不是静默跳过**，否则守卫会假绿。
- **我自己写错的两处（实现是对的）**：① 测试里把 `conn` 传了 `None` —— `resolve_effective_art_style` 会**无条件**读一次全局画风（TS 的 `getGlobalArtStyle()` 同样无条件查库），所以只传 `drama_style` 也逃不掉这次查询；② 断言写成「画风对立词拼在末尾」，实际是插在 **`NEGATIVE_BASE` 之后**。两处都是测试的问题，已改成正确断言。
- **S2 第 2/6 块完成**（累计 24 域 / 130 端点、冒烟 **482/482**）：预设 prompt + 角色/装备三视图/单品/表情/物品/场景构建器（原 `prompt-utils.ts` L482–900，**无外部依赖**，可整块搬）。漂移守卫扩到新增常量（`THREE_VIEW_COMBINED_LAYOUT` / `THREE_VIEW_SIZE` / `ITEM_IMAGE_SIZE`）+ **`EXPRESSION_PRESETS` 的 key/en 逐项比对**（key 要与前端对齐）。
- **第 2 块里五处「长得像但语义不同」的地方（都单独断言了，别"统一"掉）**：① `build_character_appearance_text` vs `build_character_visuals_clause` —— 前者核心特征**不带** `core features:` 前缀、首饰是 `accessories:` 而非 `wearing accessories:`、最后用 **`': '`** 拼接（后者是 `', '`）；② 装备三视图负面词**不排除** `side-by-side`（否则三视角会被画成单个物体），而**单品图负面词必须排除** `side by side / three views / turnaround`（成对的反要求）；③ 表情图的服装链**没有** `costume` 字段（只有 `clothing || costumes[0]`），与视觉子句不同；④ 预设图片/视频两个 prompt 的**字段顺序不同**；⑤ 物品图分类用**全角括号** `（道具）`。
- **保真细节：JS 的 `join` 会为空槽留下分隔符** —— `[undefined,'x'].join(', ')` 得 `", x"`。所以 `_join_parts`/`_part` 刻意**不过滤空值**（缺字段时保留空槽），因为过滤会改变词序与逗号位置，即改变 prompt 本身。已用「只传两个字段」的用例锁住。
- **S3 厂商适配器层完成**（`backend-py/app/services/adapters/`，9 个文件）：原 `backend/src/services/adapters/` 16 文件 / 1611 行，覆盖 **17 家厂商**（图片 7 / 视频 4 / TTS 2 / 文本 8）。新增 `tests/adapters_test.py`（**101 用例**），并把厂商注册表 key、用户可见文案、默认模型名、端点路径纳入机械漂移守卫（守卫现为 **0 遮蔽 + 0 漂移**）。
- **⭐ 移植时最重要的发现：适配器层是「纯函数」** —— 它们只 `buildGenerateRequest(config, record) → {url, method, headers, body}` 与 `parseGenerateResponse(result) → {isAsync,...}`，**自己不发起任何 HTTP**（真正的 I/O 在 `image-generation.ts` 那一层）。所以整层**不需要网络、不需要密钥、不需要 mock**，可以离线逐字比对。这让我把工时评估里标为「必须用真实密钥复验」的一块，降级成了纯单测可覆盖。
- **适配器层新增 6 条 JS 语义垫片**（`adapters/jscompat.py`）：`parseInt`（取前导数字 / NaN 是**值**不是"不赋值" ⇒ 键仍在、序列化成 null）｜`Buffer.from(x,'base64')` 宽容解码（**容忍缺 `=` 填充**，Python 会抛错）｜`a?.b?.[0]?.c` ⇒ `dig`｜**`.length` 只有字符串/数组有**（`len({})` 是 0 而 `({}).length` 是 undefined，结论正好相反）⇒ `js_length`｜`String(16.0)==='16'` ⇒ `js_num_str`（否则画幅比例输出 `16.0:9.0`）｜`JSON.stringify` 紧凑+不转义中文 ⇒ `js_json_stringify`。
- **「显式 undefined」与「DB 的 null」在 Python 里都是 None，但处理必须不同**：JS 里 `{seed: hasRef ? undefined : random()}` 的 `undefined` 会被 `JSON.stringify` **丢掉整个键** ⇒ Python 侧应**不加键**（写 None 会变成 JSON `null` 发给厂商）；DB 行的可空字段本身是 `null`，如实保留。
- **守卫第一次跑就抓到我 fixture 写错**（不是实现错）：TS 的 URL 是 `join(baseUrl, '/v1', '/image_generation')` **两个独立字面量**，我拿拼起来的 `/v1/image_generation` 去比对 → 误报 2 条"TS 侧找不到"。**教训：漂移检查的"基准字面量"必须是从源码里真实存在的形态，不能是推导出来的。**
- **两条失败断言也是我写错的（实现保真）**：① MiniMax 图片的 `aspect_ratio` 只在**入参**给了 `size` 时派生 —— `body.size` 有默认 `1920x1080` 但**不参与**派生（原先判的是 `record.size`）；② `parseInt('abc')` 是 **NaN 而非"不赋值"** ⇒ 键仍在。两次都是"我以为的行为" ≠ "Node 的行为"，靠真跑纠正。
- **S4 前置两块完成**：① `services/vendor_errors.py`（← `utils/vendor-errors.ts` 362 行，**厂商错误→用户可读中文**的归因层 + 指数退避重试）② `services/text_generation.py`（← `services/text-generation.ts` 556 行，文本链路 6 个 AI 函数 + **本地规则拆分器**）。新增 `tests/vendor_errors_test.py`（58）与 `tests/text_generation_test.py`（67），并加 `tests/run_all.py` 一键跑五项（**708 项全绿**）。
- **⭐ 归因层的关键设计（值得记住）**：它把上游英文 JSON 归因成**能行动的中文**，且分了两级 —— **内容审核拦截**要再细分「是否由**上传图/首帧**触发」（火山 Seedance 的 `InputImageSensitiveContentDetected.PrivacyInformation`），因为处理方式完全不同：**改文字提示词 vs 换参考图**。另外 `is_non_retryable_http_error` 让轮询对「审核/参数错」**立即判失败**，避免对不可恢复的错误空转数十分钟。
- **⚠️ 归因层的两级判断顺序容易搞反**：`_looks_like_content_moderation` **必须先命中**（code/type/message 里得有审核信号），之后的「中文『人物』正则」才参与**细分**。实测：`{"error":{"message":"图片包含人物"}}` 这种**只有中文没有审核信号**的 body 压根不走审核分支（会显示成「图片生成未成功：…」）。这是原实现的行为，已用两条用例把「命中」与「不命中」都锁住。
- **⚠️ 模型名提取正则很松**（`model[`:：\s]+([a-z0-9_.-]+)`）：`"model is not a valid model"` 会把 **`is`** 当成模型名，报出「模型「is」不支持当前接口」。原实现如此，照实保真并加注释 —— **这类"丑但保真"的行为不要顺手修好**，否则与 Node 的报错文案不一致。
- **有意差异（更稳）**：原 TS 在错误归因处对**数字 code** 会 `TypeError`（`({"error":{"code":404}}` 这种厂商很常见）⇒ 整条归因崩掉、中文说明变 500。Python 侧统一 `str()` 归一。**这是唯一一处我主动偏离原实现的地方**，已写进 README 的「已知差异」表并加用例。
- **`text-generation.ts` 里最值得搬的是那个「本地规则拆分器」**：8 张词表（38/32/29/7/56 条…）+ 相邻子句合并算法（上限 34 字），是「AI 失败也要有输出」的兜底，输出会写进角色卡。已用 **8 张词表逐项比对**纳入漂移守卫 —— 56 条的引导词表里错一个字，拆分结果就悄悄不同。
- **守卫又抓到一次我自己的疏漏（这次是守卫的归一化）**：报 2 条 `SPLIT_VISUALS_*` 提示词漂移，查下去发现 TS 源码里的 `\n` 是**字面两字符**（反斜杠+n），而我 Python 侧写 `"…\n"` 运行时是**真换行**，只去空白会把前者留下 ⇒ 假漂移。修归一化（先抹掉 `\n` 转义序列）后归零。**教训：跨语言比对字面量时，转义序列要在归一化里一起处理。**
- **Windows 编码又咬一次（第 4 次）**：`tests/run_all.py` 用 subprocess 抓子进程输出，子进程 stdout 是**管道**时按本地代码页（GBK）输出，我按 UTF-8 解码得到 U+FFFD，再打印回 GBK 控制台直接 `UnicodeEncodeError` 崩。对策：给子进程传 `PYTHONIOENCODING=utf-8`（两边对齐）+ 自身 `sys.stdout.reconfigure(errors="replace")`。
- **S4 主体：`image-generation.ts`（753 行）已迁**（`services/image_generation.py`），连带补齐 5 个依赖：`take_budget.py`（生成次数收敛门禁）、`file_storage.download_file`、`usage_tracking.record_usage`（写入侧）、`era_background.get_era_background/apply_era_image_clause`、`color_grade.apply_color_grade_to_file`（**像素管线未迁，走降级**）。新增 `tests/image_generation_test.py`（64 用例），`run_all.py` 现覆盖 **6 项 / 772 项全绿**。
- **⭐ 一个「看起来是 bug、其实两边都对」的排查（差点改错）**：活跃配置的 `model` 读出来是**空串**，而 DB 行里明明是 `dall-e-3`。查下去发现 **`ai_service_configs.model` 存的是 JSON 数组字符串**（多模型 fallback 用），Node 侧同样是 `JSON.parse(active.model)` 取 `models[0]` ⇒ 传裸字符串就会解析成空串。**结论：这是契约不是 bug。改成「读原文对照」而不是「照着症状改代码」才对。**
- **🔥 自检抓到一个真保真 bug：`json.dumps` 默认不是 `JSON.stringify`** —— Python 默认分隔符 `(', ', ': ')` 会**多出空格**（`{"exposure": 0.5}` vs JS `{"exposure":0.5}`）。这些串会写进**与 Node 共用**的列（`image_generations.color_grade`/`reference_images`、`characters.*` 的 JSON 列）**并作为请求体发给厂商**。已收口 `image_generation._stringify`（紧凑 + 不转义非 ASCII），7 处调用点全部替换。
- **图片生成链路的三个「顺序即语义」点（都有用例锁）**：① 回写角色的分支优先级 `itemType → viewType → expression → equipType → costume → 主图`（顺序错了会写错字段/覆盖别的图）；② 帧类型 → 列映射（`first_frame→first_frame_image`…其余→`composed_image`），且**只有首尾帧**才把 `asset_status` 置 `approved`；③ 恢复流程的分支顺序：**先判过期（60min）再判 taskId/provider** —— 我测试时给「未知 provider」的行写了个昨天的 `updated_at`，于是先命中了过期分支（**实现是对的，测试数据造错了**）。
- **后台任务用 `asyncio.create_task` 时必须持引用**：模块级 `set` + `add_done_callback(discard)`。不持引用的话 Task 可能被 GC 回收，表现为「生图永远停在 processing」这类极难排查的问题。另：后台每步各开一个**短事务**（`engine.begin()`），**不能**全程持有一个 —— 轮询最长 10 分钟，SQLite 下长事务会阻塞 Node 的写入。
- **两条「未迁但已设计好降级路径」的坑**：① 参考图压缩（sharp→Pillow）当前退化为**原图 data URL**并告警（内容画质不变、载荷更大）；② 像素级校色（142 行 sharp 管线）抛错 → 被调用方的 `try/except` 记为 `color-grade-failed` 并**保留未校色图** —— 这**正是 Node 校色失败时的行为**，所以升级前后都不会写出"看起来不对"的资产。
- **S4 视频链路 + S2 第 5 块完成**：`video-generation.ts`（498 行）→ `services/video_generation.py`，连带 `video_probe.py`（ffprobe 补时长）、S2 第 5 块（`strip_video_prompt_tags` + `validate_dialogue_character_consistency`）。新增 `tests/video_generation_test.py`（50 用例）⇒ **7 项自检 / 822 项全绿**。
- **视频层的三个「只在这层存在」的点（都有用例锁）**：① prompt 提交前要**洗**（先剥 `<location>/<n>` 等 DSL 标签 → 再注入逐镜禁止变化 → 再注入 `[background_audio]`，**幂等**）；② **两处空数组待遇不同**：`referenceImageUrls: []` 落 `"[]"`（truthy 判据）、`referenceAudioUrls: []` 落 `NULL`（判的是 `.length`）—— 极易写反；③ **同步完成路径不更新分镜**（`handleVideoComplete` 的 storyboardId 形参在同步路径是 `undefined`），但版本留档/QC 走「形参 ?? 记录值」仍生效。Vidu 是 Webhook 型：提交后直接返回，恢复时也**保持 processing**。
- **🔥🔥 一个系统性真 bug：`json.dumps` ≠ `JSON.stringify`（默认分隔符多空格）** —— 起初只是自检里一条 `meta` 断言失败（`{"duration": 5}` vs `{"duration":5}`），顺藤摸瓜发现**全仓 8 处**都缺紧凑分隔符：`image_generation`（我自己的）、`asset_versions`、`dramas`(tags×2/era)、`presets`(config×2)、`ai_configs`(probe 请求体)、`text_generation`(请求体)、`resource_library`、`export`（HTTP 响应体）。这些串会写进**与 Node 共用**的列、作为**请求体发给厂商**、或**直接作为响应体**。已全部修正并对照 TS 逐处验证（`JSON.stringify` / `Hono c.json()` 都是紧凑的）。
- **⭐ 给这个坑加了机械守卫（AST 版）**：`route_parity_test._json_dumps_drift` 扫全仓 `json.dumps`，缺 `separators` 即报错（白名单只留深拷贝与日志 `indent=2`）。**第一版用文本扫描，把「讲解这个坑的 docstring」和 `json.dumps(x, **_JSON)` 都误报了** ⇒ 改用 `ast.walk` 精确判断调用与关键字，`**kwargs` 展开则放行。**教训：写这种守卫要用 AST，不要用正则扫源码。**
- **图片/视频链路的自检还各抓到一条「我断言写错」**：① 抽取对话说话人的正则要求**台词 ≥8 字**（`[^:：\n]{8,}`），台词太短的行压根抽不出说话人；② 版本留档的 `meta.duration` 取的是**记录里的 duration**（`gen?.duration`），不是本次入参/探测到的那个；③ Windows 路径分隔符（`Path(...).name` 而非 `endswith("a/b")`）。
- **S4 TTS 链路完成**：`tts-generation.ts`（145 行）→ `services/tts_generation.py`（**同步链路：无轮询，hex 直接落盘**）+ `voice-clone.ts`（135 行）→ `services/voice_clone.py`（MiniMax 上传+复刻 / CosyVoice 零样本）。新增 `tests/tts_generation_test.py`（34 用例，`MockTransport` 捕获请求体）⇒ **8 项自检 / 856 项全绿**。顺带给共享的 `fetch_with_retry` 加了 `files`/`data` 参数（multipart 上传用；**不能与 `content` 同时传**）。
- **TTS 层三个易错点（都有用例锁）**：① `params.model || models[attempt]` —— **传了 model 就等于关掉 fallback**（每次都用同一个，2 个模型发 6 个同名请求）；② hex 解码用自写 `_hex_to_bytes` 而不是 `bytes.fromhex` —— Node 的 `Buffer.from(s,'hex')` 遇非法字符是**截断**，Python 会抛 `ValueError`；③ CosyVoice 零样本要求参考音频**文件真实存在**，否则静默退回普通 `/tts`。
- **⭐ 一次「测试自己的坑」**：断言失败后我盯着 6 个同名请求想不通，写了个调试脚本打印每次请求才发现 —— **实现是对的**（`fetchWithRetry` 默认对 500 重试 3 次 ⇒ 3 次 bad-model + 1 次 good-model），我的断言忘了重试。顺带一个教训：`check(name, cond, detail)` 里传 **list 对象**，后面 `clear()/append()` 会让**打印出来的 detail 与断言当时的状态不一致**（报错信息误导人）⇒ 传 `list(...)` 副本。
- **S2 第 3+4 块完成 + 视觉图谱整块移植**：`visual_graph.py`（← `shared/visual-graph.ts` 215 行，**41 节点** / 四类图谱 / 中文→英文电影术语翻译 + 风格引导）/ `build_storyboard_image_prompt` / `build_storyboard_video_prompt` / 6 个 DB 读取辅助（分镜上下文、角色立绘、参考图三源合并、参考音频≤3）⇒ **S2 现在 5/6 块**。新增 `tests/prompt_storyboard_test.py`（38 用例），守卫加了**视觉图谱 41 节点逐条比对** ⇒ **9 项自检 / 894 项全绿**。
- **分镜视频 prompt 的末尾拼装「逗号与空格位置」是硬契约**：`<各段以 '. ' 连接>, <声音策略>.<UI留白规则><环境音标记> <画风层>` —— UI 规则带**前置逗号**（`", " + UI_OVERLAY_RULE`）、环境音带**前置空格**、画风层命中时要去掉后缀自带的逗号前缀。我第一版断言把声音策略前的分隔符写成 `. ` 而实际是 `, `，直接失败 —— **这类 prompt 拼装必须靠真跑核对字符级细节**。
- **⚠️ 新发现一条通用坑：写自检/后台任务时别嵌套事务**。SQLite 只有一个写者，在 `with engine.begin()` 里再开 `engine.begin()`（例如调用了「内部自己开事务」的 helper）会直接 `database is locked`。我的 `prompt_storyboard_test.py` 第一版就在外层事务里调了 `insert_character`（它自己开事务）而挂掉。生产代码 `image/video_generation` 一律**每步一个短事务、不嵌套** ✓ 这条已写进 README。
- **`to_public_media_url` 的默认基址是 Node 的 5789**：参考音频要给本地 H3 服务拉取，URL 由它生成。**只跑 Python 后端（5790）时必须设 `PUBLIC_BASE_URL`**，否则 404。绞杀期两边都在则无感 —— 已写进 README「已知差异」。
- **逐镜路由 + `videos.ts` 路由完成**：`shot_router.py`（← `shot-router.ts` 217 行，7 种路线 + `persist_route` 回写 + `recompute_episode_routes`）/ `routers/videos.py`（← 290 行，**6 端点全迁**，已注册进 main.py）。新增 `tests/videos_route_test.py`（43 用例）⇒ **10 项自检 / 937 项全绿**；守卫把 `videos.ts` 加进 `MIGRATED`（Node 清单 164→**170** 条，Python 已注册 133→**139**）。
- **⭐ `POST /videos` 的帧来源统一是「修 bug 式移植」**：此前**决策读 DB 帧、生成请求只读 body 帧**，两者不同源 ⇒ 会出现「决策判定 first_last/single，实发请求却一帧都不带」的坏请求（adapter 单帧分支只认 `imageUrl`）。现在统一成同一份候选帧（body 显式传帧优先 → 分镜已存帧兜底），决策与请求共用。
- **⚠️ 路由优先级里 `prevTail` 必须在 T2V 之前消费**：无首帧但有上一镜尾帧时改用 I2V 起帧；如果顺序反了，`referenceMode` 会落到 `none`，调用方传的顺接帧**不会被 adapter 派发**，尾帧顺接形同虚设。
- **⚠️ 对话判据有两份、宽窄不同（都要保真）**：`shot_router.DIALOGUE_PATTERN` 含 `multi-person|多人|对话|争吵|会议`；而 `videos.ts` 里那份只有 `dialogue|meeting|argument|conversation|multi`（**没有**「多人/中文」）。它们分别决定「路由是否走 R2V」与「是否自动带参考音频」。
- **⚠️ `multiple` 分支**不消费帧字段**（设计如此）**：R2V 走 `reference_image_urls`，所以即便分镜里存了 `first_frame_image`，`imageUrl`/`firstFrameUrl` 也**都是 null**。我第一版断言按 `single` 写，正好踩在这条上 —— 实现是对的。要验「帧来源统一」必须用**非对话**场景（落 single）的分镜。
- **`videos.ts` 的三处保真点**：`GET /{id}` 查不到返回 `success(null)` **不是 404**；`PUT /{id}` 白名单里**不含** `updatedAt`（原 TS 不更新它）；`GET /` 的过滤值不是有限数字时**当没传**（不过滤）。
- **⭐ S2 全量迁完（6/6 块）** —— `prompt-utils.ts` 1629 行全部落地。第 6 块（宫格 prompt）连带搬了 `shared/camera-movement-guides.ts`（新文件 `services/camera_movement_guides.py`，29 条中文运镜表）。新增 `tests/grid_prompt_test.py`（48 用例）⇒ **11 项自检 / 985 项全绿**；漂移守卫再扩：运镜表 **zh 顺序 + 全部 start/end 共 58 条逐字比对**、`GRID_ANGLES` 25 条逐字比对（守卫现 **0 遮蔽 + 0 漂移**）。
- **⚠️ 宫格链路的「反直觉」点（都有用例锁）**：① 换行拼接用 `filter(Boolean).join('\n')` —— **空串被丢掉**，与第 2 块 `, ` 拼接（**保留**空槽）**恰好相反**；② 画风回退是**字面量** `cinematic illustration style`，**不是** `VISUAL_STYLE_MASTER`（与其它链路不同）；③ 三个分支的**标点各不相同**（`multi_ref` 用半角 `': '`、`first_last` 用全角 `，首帧：`/`，尾帧：`、默认分支用全角 `：`）；④ 参考资产**上限 6 张** ⇒ **收集顺序决定谁能进编号**（顺序：逐分镜「首帧→尾帧→镜头图→参考图」→ 场景 → 角色；排最后的角色立绘最容易被挤掉）；⑤ `_pos_label` 的 `rows` 参数**根本没用**（原 TS 如此）；⑥ `first_frame` 模式格子数 = **分镜数**（不是 rows×cols），另两个模式才是 rows×cols。
- **⚠️ 两个新踩的 Python 坑**：① `storyboard_characters` 是**复合主键、没有 `id` 列** ⇒ `order_by(.c.id)` 直接 `AttributeError`（改用复合键排序）；② 函数里写局部变量 `scenes = ...` 会**遮蔽模块级的 `scenes` 表对象** ⇒ 后续 `select(scenes)` 报 `UnboundLocalError`（改名 `scene_rows`）。
- **⚠️ Windows 编码第 5 次咬人**：测试的 `check(...)` 名字里写了 `⇒`(U+21D2) 与 `⭐`(U+2B50)，**GBK 控制台打不出来** ⇒ 打印 FAIL 明细时 `UnicodeEncodeError`（断言其实跑完了）。**教训：测试字符串里只用 GBK 可编码字符**（汉字/全角标点/`->`），别用箭头、星号、emoji。
- **记录一条接口事实**：`POST /api/v1/storyboards` **不接收** `first_frame_image` / `last_frame_image` / `reference_images` / `movement` 等字段（body 传了会**静默忽略**）—— 测试里要造这些数据必须走 UPDATE。
- **compose（单镜 ffmpeg 合成）完成**：`services/ffmpeg_compose.py`（← `ffmpeg-compose.ts` 187 行）+ `routers/compose.py`（← `compose.ts` 108 行，**3 端点全迁**）。新增 `tests/compose_test.py`（35 用例，**假 ffmpeg**：替换 `_run_ffmpeg` 只记参数 + 落空文件）⇒ **12 项自检 / 1020 项全绿**；守卫 `MIGRATED` 加 `compose.ts`（Node 170→**173**，Python 已注册 139→**142**）。
- **⚠️ compose 的真实路径很别扭（保真）**：Node 是 `api.route('/compose', compose)`，而 `compose.ts` 的子路径自带 `/storyboards`、`/episodes` ⇒ 实际是 `/api/v1/compose/episodes/{id}/compose-all`。Python router prefix 必须写 `/api/v1/compose` 才能与守卫对齐。
- **🔴 抓到自己两个真 bug（都是"测试才发现"）**：① 角色音色键名写成 `voiceStyle`，但行转 dict 后是**蛇形键** `voice_style` ⇒ 永远回落 `alloy`；② `match_character_by_speaker_name` 内部用 **`getattr(c,'name')`** 取名 ⇒ **必须传 Row，传 dict 会静默匹配不到**。
- **🔴 一个架构级坑：`compose_storyboard` 不能借调用方的连接写库。** Node 的 drizzle `db.update(...).run()` 是**逐语句自动提交**；Python 若借用 FastAPI 依赖给的连接，**失败路径写 `compose_failed` 会被依赖的 rollback 一起抹掉**（请求依赖是「异常即 rollback」），前端永远看不到失败态。已改成服务**自己管短事务**（`_write`/`_fetch` 各开 `engine.begin()`），路由不再注入连接。**每次移植「服务内部写库」的地方都要问一句：这套写操作该归属事务边界？**
- **⚠️ 火忘型后台任务在测试里必须隔离**：`compose-all` 的后台循环开自己的事务，与测试自身连接在 SQLite（**单写者**）上互锁 ⇒ 一路 `database is locked` 重试到超时（跑了 90 秒）。测试里 patch 掉 `_run_batch` 即可（路由的即时契约——标记 `compose_processing` + 火忘返回——不受影响）。
- **merge（整集拼接）完成**：`services/ffmpeg_merge.py`（← `ffmpeg-merge.ts` 277 行）+ `routers/merge.py`（← `merge.ts` 43 行，2 端点）。新增 `tests/merge_test.py`（29 用例）⇒ **13 项自检 / 1049 项全绿**；守卫加 `merge.ts`（Node 173→**175**，Python 142→**144**），仍 0 遮蔽 + 0 漂移。
- **⭐ AST 那条 `json.dumps` 守卫又立功了**：`video_merges.scenes` 我写了 `json.dumps(videos)`（默认带空格）⇒ **守卫立刻报 DRIFT**。这一列与 Node 共用（`JSON.stringify` 无空格）。**教训：凡是写进 DB / 发请求体 / 当响应体的 JSON 都要 `separators=(",",":")`**，这条守卫是拦这类"看不见的空格"的唯一防线。
- **merge 的三段 ffmpeg 与保真点**：① concat（`-f concat -safe 0` + `+genpts/libx264/preset medium/crf 23/aac 48k/192k/faststart`）；② **BGM 混音**（`-stream_loop -1` **属于后加入的 BGM 输入** + `volume(3位小数)/afade in+out/amix/alimiter` 滤镜链，`-c:v copy`）—— 失败**只告警并回退无 BGM 版本**；③ **响度归一化** `loudnorm=I=-14:TP=-1.5:LRA=11`（**无音轨直接跳过**），失败同样只告警。切片顺序按 `storyboard_number` 升序（不是 id）；必须**全部**镜头已合成（`)Only composed storyboards can be merged (n/m ready)`）。中间文件用完即删（concat 列表、无 BGM 中间片、归一化前文件）。
- **⚠️ 两个测试基建要点**：① 服务里用 `asyncio.create_task` 的火忘任务，测试的**同步** `main()` 里没有运行中的事件循环 ⇒ 要包一层 `async def` 用 `asyncio.run` 调，并 `await asyncio.sleep(0)` 让任务跑完；② patch 掉的后台入口要**留一份真身引用**（`_REAL_GUARDED = fm._do_merge_guarded`），失败路径要单独驱动它。
- **`runConsistencyQcBeforeMerge` 保留为空实现**（Node 侧动态 import `consistency-qc.js`）—— 与 `video_generation` 里的 QC 调用点同样处置：**接口在、逻辑待补**（镜头 QC 打分 ≈510 行未迁）。
- **grid（宫格）完成 —— S4 主链全通**：`routers/grid.py`（← `grid.ts` 347 行，**4 端点**）+ `services/grid_split.py`（← `grid-split.ts` 45 行）。新增 `tests/grid_route_test.py`（42 用例）⇒ **14 项自检 / 1091 项全绿**；守卫加 `grid.ts`（Node 175→**179**，Python 144→**148**），仍 0 遮蔽 + 0 漂移。
- **⭐ 切图用 ffmpeg `crop` 替 sharp（不新增 Pillow 依赖）**：Node 用 `sharp.extract` 无损裁剪；Python 侧**没装 Pillow**，改用 `ffmpeg -vf crop=W:H:X:Y -frames:v 1`（同样无损 PNG、复用已有 ffmpeg 依赖）。像素内容等价、字节流不同（编码器不同）—— 下游只当图片读，不受影响。尺寸探测走 `ffprobe -select_streams v:0`。
- **⚠️ `/grid/prompt` 的 Agent 分支未移植也能用**：Node 先试 `runAgentWithRetry('grid_prompt_generator')`，从工具结果/正文里抠 `{grid_prompt, cell_prompts}`；**任何异常都吞掉回落本地构建器**。所以我把「Agent 不可用」表现为同一件事 —— `try_agent_grid_prompt` 恒返回 None ⇒ 端点走确定性路径（`source='fallback'`），行为与原实现的失败分支同形。
- **⚠️ 载荷归一的两个判据**：`grid_prompt` 为空 ⇒ **整体判不可用**（回落）；`cell_prompts` 里**没有 prompt 的格子被丢掉**；两套键名（`grid_prompt`/`gridPrompt`、`shot_number`/`shotNumber`）都要收；字符串会**递归**尝试 JSON 解析 → ```json 围栏 → 裸 `{...}`；`shot_number` 非数字归 0（`Number(x)||0`）。
- **⚠️ 宫格出图参数**：画布 = `960*cols x 540*rows`（**固定单元格 960x540，不随画幅变**）；`frameType=grid_<mode>_<r>x<c>`；负面词用**分镜那套**（`build_storyboard_negative_prompt`，含画风对立词）；参考图 = `collect_grid_reference_assets` 的路径列表。`/split` 的 `reference` 分支是把格子**追加**到已有 `reference_images` 数组（该列与 Node 共用 ⇒ 紧凑 JSON）。
- **⚠️ patch 目标必须是「使用方模块」**（同类坑第 3 次）：路由里写的是 `from ..services.grid_split import split_grid_image` ⇒ 改 `gs.split_grid_image` **对它无效**，得 patch `grid_router.split_grid_image`。同理测试里要留 `_REAL_SPLIT = gs.split_grid_image` 才能测真身的校验分支。
- **`image_generations.updated_at` 是 NOT NULL**（测试里直插记录漏了会 `IntegrityError`）。
- **加速批次：又清掉 4 个域 / 22 端点** —— `visual-graph`(4，服务层早已就位，等于白捡) + `images`(4) + `webhooks`(1) + `aiVoices`(5) + grid(4)。累计 **16 项自检 / 1171 项全绿**；守卫 Node **193** / Python **162**（0 遮蔽 + 0 漂移）。
- **⭐ AST 的 `json.dumps` 守卫第二次立功**：`aiVoices` 里发 MiniMax 的请求体 `json.dumps({"voice_type":"all"})`（默认带空格）当场被报 DRIFT。**这条守卫至今拦下 9 处** —— 凡是写 DB / 发请求体 / 当响应体的 JSON 都必须紧凑。
- **⚠️ `visual-graph` 的返回形态与全项目不同（别"统一"）**：``GET /``、``/resolve``、``/terms`` 返回的是**裸 JSON**（无 code/data/message 信封）、显式带 charset；``/guidance`` 是 **text/plain**。且 `category` 非法时：``GET /`` **返回全部**，``GET /terms`` **直接 400**。
- **⚠️ `images` 的负面词二选一**：有 `storyboard_id` → `build_storyboard_negative_prompt(画风)`；**没有 → `NEGATIVE_BASE`**（通用词）。角色外观在这里用**全角分号** `；` 连接。
- **⚠️ `webhooks/vidu` 的三种返回码（都要保真）**：缺 task_id → 400；**task_id 查不到 → 200**（防厂商重复回调）；`state=failed` → **200**（已记录，错误经归因层转中文）；只有**下载失败**才 400，同时把记录标 `failed` + `Webhook download failed:` 前缀。`WEBHOOK_SECRET` 是**模块加载时**读一次。
- **⚠️ `aiVoices` 的几个要点**：音色 id = `ds_`/`cv_` + **base36 时间戳** + 4 位 base36 随机（手填要过 `^[a-zA-Z][a-zA-Z0-9_-]{7,255}$` 且不以 `-`/`_` 结尾）；语言推断**顺序敏感**（`cantonese`/`粤` 要先于 `chinese`）；`/clone` 走 **multipart**，只有 minimax/cosyvoice 两家且 cosy 必须给 `prompt_text`；`/generate-from-characters` 对 `ds_`/`cv_` 开头的角色音色**直接 skip**，参考音频是**即时合成的 ≈15 秒长文本**（试听只有 4 秒，`voice duration too short`）；`/sync` **先删光再插**且 AI 打标失败不阻塞；写音色库一律 `onConflictDoNothing`。为便于测试把 MiniMax 请求抽成了 `fetch_minimax_voices`。
- **⚠️ 又两条「NOT NULL 陷阱」**（测试直插时）：`ai_service_configs.name` / `.api_key` 都是 NOT NULL ⇒ 用**空串**表达「未配置」（`!apiKey` 判定与 JS 一致）。
- **⭐ preset-framework 完成（6 端点）—— 但它是「从未接线的骨架」，本轮**唯一有意偏离 TS** 的一处**：TS 自己引用的 `dramas.systemMetadata` / `storyboards.sceneNumber` / `cameraMovement` / `imageUrl` **在共享 schema 里全都不存在**（也就是说 Node 侧调 `/create`、`/generate-videos`、`/status` 同样会因缺列报错）。照抄只能"忠实复现崩溃"，所以我按**真实列名**落地：`dramas.metadata` ← `{variationCard, presetType}`｜`storyboards.storyboard_number` ← shotIndex（`/status` 本就按它排序，写它才自洽）｜`storyboards.movement` ← 随机运镜｜`storyboards.first_frame_image` ← 首帧（TS 的 `imageUrl` 语义等价物）；TS 想写的「每镜 shotConfig JSON」**无对应列** ⇒ 不写。「忠实」与「能用」冲突时选后者并**显式写进注释与记忆**。
- **preset-framework 的其它要点**：数据池是**占位符**（`THEME_FAMILY_A` / `COMPOSITION_TYPE_1`…，别替换成中文，数量与顺序影响随机结果）｜`derive_space_and_frame` 用 `(themeFamily+focalPoint)` 的**字符串长度**取模（**不是 hash 值**）｜`pick_character_layout` **忽略入参**｜线索配比 **3:2**、灵动元素**最多 3 镜**｜响应形态自成一格：成功 `{success, data}`、失败 `{success, msg}`（**没有** code/data/message 信封），参数错 400、内部错 **500**。
- **`runMerged`（`utils/merge-queue`）未移植** ⇒ 用 `asyncio.gather(return_exceptions=True)` 做同形实现（每镜自管状态、失败不抛出、结果按 shotIndex 排序）。
- **⚠️ 测「编排类」功能的边界**：真管线里「首帧」「视频」是**后台任务完成后**才回写 `storyboards.first_frame_image` / `video_url` 的；假生成器只插记录 ⇒ 测试必须**手工"完成"这一步**，否则生视频会被当成"没有首帧"全跳过、进度汇总是 0。
- **S5 开工第一块：`shared/prompt-blocks.ts` 已迁**（`services/prompt_blocks.py`，4 个常量）—— 这是 `agents/index.ts` 的 `DEFAULT_PROMPTS` 的**硬前置**（6 类 Agent 的长提示词都引它）。**漂移守卫也扩上了**：新增 `_prompt_blocks_drift`，用 `export const X = \`...\`` 抽模板字面量**逐字比对**并报出**首个差异字符位置**（比整段 diff 好定位）。第一次跑就抓到我写的 `colour`（TS 是 `color`）—— 守卫当场拦下。
- **S5 全貌（Node 侧体量，未迁）**：`agents/index.ts` **664 行**（大半是 6 类 Agent 的中文长提示词 + Mastra 装配）、`agents/mcp.ts` 257、`skills.ts` 218、`skill-parser.ts` 172、`creator.ts` 151、`subagent.ts` 95、`protocol.ts` 70、`tools/*` **共 1,362 行**（script/extract/storyboard/voice/grid-prompt/corpus 六组工具，**全都调用已迁服务**）、`evaluation/` **985 行**。合计 ≈ **3,974 行**。
- **⚠️ S5 的真正难点不是"搬"而是"造运行时"**：Node 用 **Mastra**（`@mastra/core/agent` + `@ai-sdk/openai`）建 agent 并在请求内动态注入 episodeId/dramaId 到工具闭包；Python 侧**没有 Mastra** ⇒ 需要**手写 agent 循环**（system prompt + 工具注册表 + 步数上限 + 流式事件协议），LLM 调用可复用已迁的 text 适配器（OpenAI 兼容 + Gemini 的 function calling）。`agents/protocol.ts`（70 行）定义的就是那套**事件协议**，应与运行时一起设计。
- **S5 第二步交付：`services/agents/`（新包）里的协议 + 工具基座** —— `protocol.py`（← `agents/protocol.ts` 79 行：`build_protocol_contract` + `parse_agent_protocol`，**六类失败分支的英文文案逐字对齐**）+ `tool.py`（`Tool` / `ToolRegistry`，工具入参用 **JSON Schema** 而非 zod —— 因为它要同时喂给 OpenAI 兼容与 Gemini 两条 function-calling 适配器，所以 `as_openai_tools()` / `as_gemini_tools()` 各导一份）。新增 `tests/agent_protocol_test.py`（**25 用例**）。
- **⭐ 协议里一个 JS 语义细节值得记**：`invalid status: …` 要区分**缺键**（JS `undefined` → 文案就是 `undefined`）与**显式 null**（`JSON.stringify(null)` → `"null"`）。Python 里两者都是 `None` ⇒ 用**哨兵对象**区分。这是 `JSON.stringify(undefined)` 返回 `undefined` **本身**（不是字符串）导致的。
- **工具工厂约定**（与 Node 一致、只多一个显式 `conn`）：`create_xxx_tools(conn, episode_id, drama_id) -> dict[str, Tool]`；Node 侧是 `createTool({id, description, inputSchema: zod, execute})` + 闭包注入 episodeId/dramaId。
- **S5 第三步：第一个工具集 `agents/tools/grid_prompt_tools.py`**（← `grid-prompt-tools.ts` 92 行，2 个工具）⇒ 新增 `tests/agent_tools_test.py`（**15 用例**）。**核心不变量已锁住**：`generate_grid_prompt` 产出的 `grid_prompt` / `cell_prompts` 与**路由 fallback 逐字相同**（同一画风解析链 + 同一参考图收集器）—— 这正是原 TS 注释强调的「避免与路由层重复实现、逻辑分叉」。
- **⭐ 工具工厂的签名定为「与 Node 完全同形」**：`create_xxx_tools(episode_id, drama_id) -> dict[str, Tool]`，**闭包只注入 id、每个工具自开短事务**（Node 用全局 db）。我第一版给它加了显式 `conn`，测试立刻踩到 `ResourceClosedError`（连接在调用方 `with` 块外已关闭）—— 这跟 compose 那轮是**同一条教训**：服务/工具不该借调用方的连接。改成自开短事务后既不会长期占写事务（SQLite 单写者），也不会踩连接生命周期。
- **工具返回的 `reference_assets` 用 snake_case**（`image_label`），不是 TS 的 camelCase。
- **S5 第四步：第二个工具集 `agents/tools/voice_tools.py`**（← `voice-tools.ts` 148 行，3 个工具）⇒ 新增 `tests/agent_voice_tools_test.py`（**22 用例**）。**两条硬规则在工具层就挡住**（不靠模型自觉）：①**一角色一音色**（同剧别的角色已用该音色 ⇒ 返回 `error` 且**不落库**）；②**speaker_id 全局唯一**。分配时 `voice_sample_url` **显式置空**（换音色 ⇒ 旧声线样本失效）。
- **⭐ `infer_gender` 的正则是「字符类」不是「备选词」**：`/[男|青年|大爷|学长|boy|man|male]/i` 里的 `|` 在字符类里是**字面量**，语义是「**任意一个字符**命中」。所以 `voice` 里的 `o`/`e` 就足以判**男声**（`male`/`man` 能命中纯属字符集巧合）。我按"词匹配"写断言 → 两条当场红；照抄原正则后把「巧合」写成了专门的用例。
- **`role_tag` 过滤只在「库里没音色 → 回落 6 个通用音色」分支生效**（原 TS 如此，库分支不过滤）。且回落表里**只有 `onyx` 带「反派」标签**。
- **⚠️ 又踩 Windows 编码（第 6 次）**：测试的 `check` 名里带 `⚠️`(U+26A0+FE0F) ⇒ GBK 控制台 `UnicodeEncodeError`。**测试字符串里只能用 GBK 可编码字符**，注释里随便写（注释不打印）。
- **S5 第五步：一口气补齐 3 组工具 + 1 个前置**（`text_slice.py` ← `utils/text-slice.ts`、`tools/script_tools.py` ← 83 行、`tools/corpus_tools.py` ← 148 行）⇒ 新增 `tests/agent_script_corpus_test.py`（**25 用例**）。**至此六组工具完成 4/6**（grid-prompt / voice / script / corpus），只剩 extract(445) 与 storyboard(483) 两组大的。
- **⭐ `slice_long_text`：保留前 70% + 后 30% + 中间显式省略提示**（24000 字符上限，中文≈12k token）。理由写得很清楚：超长输入直接注入会触发 context length 超限，那是 **fatal 且不可重试**的错误；而剧本开头（人物/设定）与结尾（高潮/结局）最关键，所以首尾留、中间省。边界是 `head=16800 / tail=7200`（floor 后减），省略处要带**具体省略字数**。
- **⭐ `corpus_tools` 必须在语料缺失时优雅降级**：`data/prompt-corpus/` **已 gitignore**（第三方语料不进仓库）⇒ 新克隆的仓库里没有那个文件。所以 `load_corpus()` 缺失时缓存 `None` 并返回 `{available:false, hint:'…请先跑 fetch-raw.mjs / normalize.mjs…'}`，**绝不抛错** —— 否则「没采语料」会直接变成「Agent 跑不动」。打分口径（title 5 / tags 3 / category 2 / summary 2 / prompt 1）与 `scripts/corpus/search.mjs` **必须同步改**。
- **两条"我以为是 bug 其实原实现就对"**：① 语料打分是**各字段累加**（同一个词命中 title+tags+summary 就 +5+3+2，我按只算 title 写了期望）；② `prompt_excerpt` 的 `\s+ -> 单空格` **不 trim 首尾**（原 TS 没有 trim）⇒ 首尾各留一个空格。
- **S5 剩余两组的依赖地图已铺清（下次可直接开干，零新前置）**：
  * `tools/extract-tools.ts`（445 行，**7 个工具**）`read_script_for_extraction` / `read_existing_characters` / `read_existing_scenes` / `read_existing_props` / `save_dedup_characters` / `save_dedup_scenes` / `save_dedup_props`。**只依赖** `prompt_utils` 的 `build_character_image_prompt` / `build_scene_image_prompt` / `build_prop_image_prompt` / `build_character_negative_prompt` / `SCENE_IMAGE_NEGATIVE`（**全都已迁**）+ `slice_long_text`（本轮已迁）⇒ 纯移植。
  * `tools/storyboard-tools.ts`（483 行，**4 个工具**）`read_storyboard_context` / `save_storyboards` / `save_continuity_states` / `update_storyboard`。它自带一个**局部** `syncStoryboardCharacters`（未 import），Python 侧已有等价物 `storyboard_helpers.sync_storyboard_characters` / `sync_storyboard_props`（⚠️ 注意语义差异：局部版是**先删后插 + Set 去重 + 过滤 falsy**）。同样零新前置。
- **S5 第六步：`tools/extract_tools.py` 交付「读取侧」（← 445 行整组已通读）** —— 含 3 个关联辅助（`_link_char/scene/prop_to_episode`，**幂等**）+ `infer_role_type`（**顺序敏感**判据链：主角→反派→龙套→配角→旁白→其他）+ **4 个读取工具**（read_script_for_extraction / read_existing_characters / read_existing_scenes / read_existing_props）。**3 个保存工具**（save_dedup_characters/scenes/props）留待下一步，进度已写在模块头。
- **⭐ 两条「同名不同序」的取值链，必须分清**：`extract_tools` 的 read_script 是 **`scriptContent || content`**，而 `script_tools` 的 read_episode_script 是 **`content || scriptContent`** —— **顺序相反**（同一个库、两处优先级不同）。照抄时最容易"统一"掉。
- **⭐ 校验抓到真 bug**：`episode_props` 是**复合主键、没有 `id` 列**（而 `episode_characters` / `episode_scenes` 都有 `id`）⇒ 我照另两张表的写法用 `select(episode_props.c.id)` 会直接报错，已改为按 `prop_id` 查。**关联辅助一律用 `SELECT` 判存在再 INSERT 实现幂等**（不是 upsert）。
- **保存工具的三个「兜底值」要照抄**：① 场景去重键 = `location + (time || '')`（同地点**不同时段算新场景**）；② 物品 `negativePrompt` 兜底是**空串**（不是构建出来的负面词）；③ 物品新增时 `keyClue` 兜底 `'否'`。`core_features` / `costumes` 落库一律**紧凑 JSON**（共用列）。
- **S5 第七步：`extract_tools` 整组补齐（7/7 工具）** —— 加上 3 个保存工具（`save_dedup_characters/scenes/props`，**去重合并 + 提示词兜底链**）⇒ 新增 `tests/agent_extract_tools_test.py`（**23 用例**）。**至此 S5 六组工具完成 5/6**，只剩 `storyboard-tools`(483 行)。
- **⭐ 又一个「两张表命名不同」的坑**：`scenes` 的提示词列叫 **`custom_prompt`**，而 **`prop_templates` 叫 `image_prompt`**（TS 两边都写 `customPrompt`）。我照 scenes 写 → `Unconsumed column names: custom_prompt`。**同类坑已第三次**（另两次：`storyboard_characters`/`episode_props` 的 id 有无、`episodes` 取值链顺序）——**移植前先列列名**已证明是最省的保险。
- **保存侧的三个兜底值已按 TS 照抄并锁进用例**：① 场景去重键 = **地点 + (time || '')**（同地点不同时段**算新场景**）；② 物品新增 `category` 默认「道具」、`keyClue` 默认「否」、**`negativePrompt` 兜底是空串**（不构建负面词）；③ 角色的 `role_type` 缺失时按 role **推断**（显式给了就不推断）。角色同名合并**保留已有 ID**、合并时 **agent 值 > 已有值**、未给字段回退已有；关联全部**幂等**（重复保存不产生重复关联）。
- **`storyboard-tools.ts` 的依赖已判定完毕（下次可直接落笔）**：它自带的**局部** `syncStoryboardCharacters`（删光 → `[...new Set(ids.filter(Boolean))]` → 逐条插 `{storyboardId, characterId}`）与 Python 侧 `storyboard_helpers.sync_storyboard_characters(conn, sb_id, ids, costumes=None)` **语义完全一致**，而后者还**多一个可选的 costumes 映射** ⇒ **直接复用现成 helper，不必照抄局部版**（传 `costumes=None` 即为原行为）。同理 `syncStoryboardProps` / `getStoryboardPropIds` / `getEpisodeSceneIds` / `getEpisodeCharacterIds` / `validateStoryboardBindings` 在 `storyboard_helpers` 里**全都有对应实现**（`sync_storyboard_props` / `get_storyboard_prop_ids` / `validate_storyboard_bindings` …）。
- **⇒ 结论：S5 最后一组 `storyboard-tools`（483 行 / 4 工具：read_storyboard_context / save_storyboards / save_continuity_states / update_storyboard）也是「零新前置 + 可复用 helper」，属于纯移植。**
- **S5 第八步：最后一组工具 `storyboard-tools` 完成 ⇒ 六组工具 6/6 全迁完**：`tools/storyboard_tools.py`（← 517 行，4 个工具）⇒ 新增 `tests/agent_storyboard_tools_test.py`（**32 用例**）。
- **`storyboard_tools` 的六处保真点**：① ``read_storyboard_context`` 取值链是 **``scriptContent || content``**（与 `script_tools` 相反）；② 角色/场景过滤是「**空关联集 = 不过滤**」（``!linkedIds.size || linkedIds.has(id)``）；③ `save_storyboards` 是**整集重建**（删光旧分镜**及角色关联**，但**物品关联不删**）；④ **说话人自动绑定**（对白首个「角色名：」→ 角色名到 speaker_id 映射，旁白/画外音返回 None）；⑤ **对白一致性校验只告警不改数据**（统计对白里既不在本镜 character_ids 也不在本集名单的名字，回执里带 `(N dialogue character mismatches detected)`）；⑥ 集时长 = ``ceil(总秒/60)``。`save_storyboards` 的三件后置：剧本指纹盖章 + take 预算重置（**都已迁**）+ **`assign_rhythm_phases` 是占位空实现**（`rhythm-phase.ts` **未迁**）。
- **`update_storyboard` 的「按字段出现写」语义**：用 ``'x' in fields`` 判断 ⇒ 传 ``null`` **会真的清空该列**，不传则保持原值；``character_ids`` 不传时用**库里已有值**做绑定校验（不是跳过校验）。
- **⭐ 又一条 `json.dumps` 漂移被抓（第 3 次）**：`agents/protocol.py` 里复刻 ``JSON.stringify(status)`` 时写了 ``json.dumps(value, ensure_ascii=False)``。对字符串虽无差别，但**紧凑分隔符才是真正对齐**（`JSON.stringify` 从不输出空格，值一旦是对象就会露馅）。**这条 AST 守卫至今拦下 3 次**，价值稳定。
- **⚠️ 记一条共享件的文案差异（未擅改）**：`storyboard_helpers.validate_storyboard_bindings` 的报错是「`scene_id 必须来自当前集已关联场景`」，而 TS 原文是「`scene_id ${id} 不属于当前集`」（**少了那个 id**）。它是 `storyboards` 路由与 Agent 工具**共用**的，改文案会影响已验行为 ⇒ 本轮只记录、不修改。
- **S5 第九步：运行时完成（`agents/index.ts` 745 行）** ⇒ `app/services/agents/runtime.py`，新增 `tests/agent_runtime_test.py`（**49 用例**，**假驱动 + 假时钟**，不打网络不真等 2 秒）。三层结构：纯逻辑（失败分类/退避/token 统计/归一/指令组装/模型候选）｜装配（`build_agent_config` / `append_style_profile`）｜驱动（模型 fallback + 瞬态退避循环，`generate`/`sleep` 可注入）。另给 `style_profiles.py` 补了 `get_active_profile_for_drama`（**本剧专属优先 → 全局兜底**，与 `list_style_profiles` 不同）。
- **⭐ 传输层必须自建**：Node 走 Mastra `agent.generate`（AI SDK 负责工具循环），而 Python 文本适配器**全仓无 `tools` 支持**（出现 0 次）⇒ 用同一个 `fetch_with_retry` + 文本适配器的 `build_request`（复用 URL/鉴权/请求体形状），往 body 注入 `tools`（`ToolRegistry.as_openai_tools()`），自跑「工具调用 → 结果回灌 `role:tool`」循环。**Gemini 的函数调用循环尚未支持**（显式报错，不静默降级）—— 已文档化。
- **⭐ 抓到一个真 bug（只有测试能发现）**：`normalize_tool_output` 对**顶层字符串**会走 `json.dumps` ⇒ ``abc`` 变成 ``"abc"``（多一层引号）。根因是**入参口径不同**：Node 收到的是 Mastra 结果信封（`{payload:{result}}`），我的驱动直接递**工具返回值**。已加 `str` 直通 + 注释。
- **运行时的关键失败语义（都有用例锁）**：① 401/403/400、上下文超限、审核拦截 ⇒ **fatal，只调一次不重试不换模型**；② 其余全 transient（**兜底也是 transient**，宁重试别误伤）；③ 瞬态同模型退避 **3 次（2s/4s/8s）** 后换下一个模型；④ 全挂才抛**最后一个**错。⚠️ 候选模型数 = **DB 模型 + 配置模型列表**（去重）⇒ 我一度按「1 个模型 × 4」写断言，实际是 8 次调用。
- **⚠️ 风格注入有两个前置条件**：类型必须是 `storyboard_breaker/extractor/script_rewriter`，**且必须存在激活 Profile** —— 视觉图谱引导也在 profile 分支**内** ⇒ 没有 profile 时连图谱都不注入（与原 TS 一致）。分节标题**自带**前导 `\n\n`，外层再 `\n` 连接 ⇒ 实际是 `原指令\n\n\n【项目风格…】`（**三个换行**，不是两个）。
- **⚠️ `maxOutputTokens`/`temperature` 必须显式下发**：原 TS 注释写明不传则落到服务端默认，长回复被截断时**排在文本之后的 tool call 整段丢失**（实测 outputTokens 顶在 2060、业务数据一条没落库）；temperature 不传则同一输入时而调工具时而写长文。
- **🔴 顺带修掉一个真实接口 bug（camelCase 查询参数被静默忽略）**：`preset-framework` 的 `GET /variation-card` 在 Node 读 `c.req.query('excludeFamily')`（**camelCase 就是线上契约**），而 Python 路由声明的是蛇形 `exclude_family` ⇒ FastAPI 收不到该参数、**且因有默认值而静默忽略** ⇒「排除上张家族」功能实际失效。**暴露方式极隐蔽**：随机家族偶尔正好撞上被排除的那个 ⇒ 表现为**偶发测试失败**（`preset_framework_test.py` 单跑 6 次挂 2 次；单跑时通过、全量时挂，一度被我当成 flaky）。已改成 `Query(None, alias="excludeFamily")`。
- **⭐ 新增第 7 道机械守卫：查询参数名镜像检查**（`_query_param_drift`）：从**已迁** TS 路由里抽 `c.req.query('X')` 的 **camelCase** 名，要求 Python 路由文件里出现同名带引号字面量（即 `alias="X"`）⇒ 专治「路径对、参数名不对」这类文本比对抓不到的漂移。守卫现 **0 遮蔽 + 0 漂移**。
- **⚠️ 待迁路由里的同类坑（先记下，迁的时候必须用 alias）**：`export.ts` 的 **`episodeId`**（6 处）、`localModels.ts` 的 **`maxDepth` / `maxFiles` / `taskId`**。其余参数都是 snake_case，与 Python 形参天然一致。
- **教训**：camelCase 查询参数 + 默认值 = **最隐蔽的一类移植 bug**（不报错、功能失效、只在随机/概率路径上偶发暴露）。同类问题还有：**请求体里的 camelCase 字段**（Pydantic 模型同理，需 alias 或按 Node 的取值键名接收）、**响应体键名**。
- **S5 ④ 收口：Agent 聊天域完成 ⇒ S5 全阶段完成**：`routers/agent.py`（← `routes/agent.ts` **仅 57 行**、**2 端点、都是非流式**）+ 新增 `tests/agent_route_test.py`（**16 用例**，运行时打桩 ⇒ 不打网络）。⚠️ **我此前以为本域"含流式"是错的**：全仓仅 `auto-pipeline.ts` 用 SSE，agent 域没有。
- **`agent.ts` 的五处保真点**：① **两处非法类型文案不同**（`chat` 是 `Invalid agent type: {type}` **带类型名**，`debug` 是 `Invalid agent type` **不带**）；② 校验顺序：类型 → 读 body → 打 `start` 日志 → **再**校验 id ⇒ 缺 id 时 start 日志**已经打过**；③ 缺 id 用 **falsy** 判定（`episode_id=0` 也算缺）；④ 运行期异常一律 **400**（`badRequest`，**不是 500**），文案取 `str(err)`、空则兜底 `Agent execution failed`；⑤ 成功回执 `type:"done"`，`usage` 是 **camelCase**（`inputTokens`/`outputTokens`/`totalTokens`）或 **null**。`maxSteps` 硬编码 **20**（路由不透传）。
- **新增 `agent_registry.VALID_AGENT_TYPES`**（= `DEFAULT_PROMPTS` 键，**顺序即声明序**）+ 第 8 道守卫 `_agent_types_drift`（从 TS 抽 `DEFAULT_PROMPTS` 顶层键逐项比对）。守卫：Node 201 / Python 170 / 已迁 167。
- **S6 第 1 步：MCP 接入层 + 路由完成**：`services/agents/mcp.py`（← `agents/mcp.ts` **285 行**）+ `routers/mcp.py`（← `routes/mcp.ts` 27 行，3 端点），新增 `tests/mcp_test.py`（**32 用例**，用**假 stdio MCP server**（临时脚本说 JSON-RPC）跑通整条握手链路，不打网络）。⚠️ **别把两个 mcp.ts 搞混**：`agents/mcp.ts`（285 行，客户端层）vs `routes/mcp.ts`（27 行，3 端点）—— 我上轮记录把行数张冠李戴了。
- **⭐ `mcp` Python SDK 未装 ⇒ 自写极简 JSON-RPC 客户端**（不新增依赖）：只覆盖 `initialize` / `notifications/initialized` / `tools/list` / `tools/call`。已实现 **stdio**（子进程 + 按行 JSON-RPC）与 **streamable HTTP**（含 `Mcp-Session-Id` 回传、JSON 或 SSE 响应体两种都认）；**HTTP+SSE 旧传输未移植** ⇒ 显式声明 `sse` 时**显式报错**（不静默降级）。
- **MCP 层的四条「连不上」语义（都有用例）**：① 配置缺失/坏 JSON/非数组/非法条目 ⇒ **空列表**（对现有 Agent 零影响）；② 单个 server 连不上 ⇒ **只降级绝不 throw**（`gather(return_exceptions=True)`）；③ **single-flight + 配置指纹缓存**（并发共享一次发现；指纹含 `connectTimeoutMs` 等全部字段）；④ 重名工具**跳过后者**。命名隔离 `mcp__<server>__<tool>`，**非法字符净化成 `_`**（点号/中文都算非法）；入参 schema 是**宽松对象**（不还原 MCP 的 JSON Schema）。
- **`routes/mcp.ts` 的三处反直觉（都保真）**：① 两个 POST 的错误信封是 `{code:500, data:null, message}` —— **带 data 键的 500**（与本项目 `server_error` 的「无 data 键」不同）；② `POST /test` 的 body 是**不宽容**的 `await c.req.json()`（在 try 内）⇒ 坏 JSON 走 catch ⇒ **500 而非 400**（与 `read_json` 那套刻意不同）；③ `POST /test` 的 `name` 只判 **truthy** ⇒ `{"name":123}` **不被 400 拒**，交给业务层返回 `{ok:false,...}` 的 **200**。
- **🔧 加固了一个我上轮埋的潜在 bug**：`ToolRegistry.get()` 只按**注册表键**查，而驱动查的是 LLM 回传的 **`tool.id`** ⇒ 若某工厂用 camelCase 键（TS 侧就是 `readScriptForExtraction` vs id `read_script_for_extraction`），工具会变成「Unknown tool」且**不报错**。已改为**按键查 + 按 id 兜底**，并在测试里锁了这条回归。（当前 5 组业务工具的键恰好都等于 id，所以是潜在坑不是现网 bug ✓）
- **⚠️ 测试里跨 `asyncio.run` 复用 stdio 子进程会炸**：子进程管道绑定创建它的事件循环 ⇒ 第二次 `asyncio.run` 报 `'NoneType' object has no attribute 'send'`。所有异步检查必须收进**同一次** `asyncio.run`（生产环境整进程一个循环，天然无此问题）；收尾还要 `_close_all_servers()` + 短暂 `sleep`，否则循环关闭后 GC 会打印 `RuntimeError: Event loop is closed` 噪音。
- **⭐ 决策变更：`DEFAULT_PROMPTS` 从「刻意不搬」改为「搬 + 逐字守卫」** ⇒ 新文件 `services/agent_prompts.py`（6 段提示词，含 `${...}` → `prompt_blocks` 常量插值）。**改主意的理由**：① runtime 的 base instructions 一直是空串（DB 没配置就啥也没有）；② `GET /agent-configs/defaults` 至今**委托回 Node**；③ `evaluation` 的 `getDefaultInstructions` 是整条评测闭环的基准；④ 反正 S7 要删 `backend/`，迟早得落在 Python 侧。**新增第 9 道守卫 `_agent_prompts_drift`**：从 TS 抽模板字面量 → 替换 `${...}` → **逐字比对**（首次运行即 0 漂移，说明抄写无差）。只放 `instructions`，`name` 仍在 `agent_registry`（各自单源）。
- **`build_agent_config` 的回落已修正**：`dbConfig?.systemPrompt?.trim() || get_default_instructions(type)` —— 与 TS 一致；同时更新了 `agent_registry` 里「不镜像提示词正文」那段过时说明。**⚠️ 顺手改了运行时自检里一条断言**：它原本锁的是「无 DB 配置 ⇒ base 为空串」这个**洞**，现改为断言回落到出厂提示词。
- **⚠️ 剩下 `GET /agent-configs/defaults` 仍委托 Node**：`getAgentDefaults()` = 提示词 + **skill 绑定**（来自各 `SKILL.md` frontmatter，由 `skills.ts` 解析）⇒ 提示词已就位，**还差 `skills.ts`**(218) + `skill-parser.ts`(172) 才能改成本地实现。
- **S6 第 2 步：评测闭环的确定性部分完成**：`services/evaluation/` 新包 —— `types.py`（契约定成**键集合**：`AGENT_BY_KIND` / `LITERAL_KINDS` / `STATEMENT_KEYS_BY_KIND` / `RUBRIC_KEYS_BY_KIND`；TS 侧那些 interface 编译后不存在，Python 没对应物，故记录成文档 + 供自检校验真实基准文件）+ `catalog.py`（49 行：扫 `backend/benchmarks/*.json`）+ `scorer.py`（327 行：4 个确定性评分器）。新增 `tests/evaluation_scorer_test.py`（**39 用例**）。
- **⭐ 打分器最需要位精对齐的是「舍入」**：TS 的 ``Math.round(n*10)/10`` 与 ``toFixed(0)`` 都是**半数向 +∞ / 远离零**，而 Python 内置 ``round`` 是**银行家舍入** ⇒ 一律走 ``response.js_round``。自检里专门造了一个 ``2.5%`` 的用例把差异钉住（JS 给 `3%`、银行家舍入会给 `2%`）。
- **⭐ `isFilled` 的五分类**（TS ``typeof`` 语义，逐条有断言）：``null`` 假｜字符串看 ``trim()`` 非空｜数字看 ``Number.isFinite``｜数组看**长度非零**｜**其余一律算填了** ⇒ **``0`` 与 ``False`` 都算「填了」**（``typeof false`` 落到最后的 ``return true``，很反直觉但必须保真）。
- **四个评分器的权重与细节**：分镜 10/30/20/10/10/20（镜头数｜字段完整率｜video_prompt 标记｜duration｜title｜**绑定合法性**）；提取 35/25/15/10/15（角色召回/精确、场景召回/精确、外貌完整）；剧本 20/25/15/20/20（已保存｜场景头｜编号连续｜对白格式｜**无镜头语言**）；音色 40/30/15/15（覆盖｜音色合法｜理由｜**ID 合法无重复**）。⚠️ 回执 ``caseId`` 恒为**空串**（由 evaluator 事后填）；``score_voice_assignment`` 第 4 维比率**可能为负** ⇒ 必须 `max(0, min(1, rate))` 夹紧。
- **⚠️ `benchmarks/` 位置是 S7 的坑**：TS 从模块推 ``backend/benchmarks``，Python 侧同样默认那里（另有 ``BENCHMARKS_DIR`` env 覆盖）⇒ **删 `backend/` 时必须把这 4 个 JSON 挪到 `PROJECT_ROOT/benchmarks` 并改默认值**。
- **⚠️ evaluation 的依赖链比看起来深**：``evaluator``(210) 只依赖已就位的 runtime ✓，但 ``optimizer``(192) 还要 ``agents/creator.ts``(151) + ``agents/skills.ts``(218) + ``skill-parser.ts``(172) —— 而 skills 同时是 **runtime 的 skill 注入**（现在恒为 None）与 ``/agent-configs/defaults`` 本地化的前置 ⇒ **下一步优先做 skills（一处投入、三处受益）**。``cli.ts``(56) 是开发者 CLI、不是 HTTP 接口，**不打算迁**（明确记在 `evaluation/__init__.py`）。
- **S6 第 3 步：`skills` + `skill-parser` 完成（一处投入三处受益）**：`agents/skill_parser.py`（← 172 行）+ `agents/skills.py`（← 218 行），新增 `tests/skills_test.py`（**45 用例**）。三处受益都验证到了：① **runtime 的 skill 段真的注入了**（此前恒为 None）；② **`GET /agent-configs/defaults` 改为本地实现**（去掉对 Node 的委托）；③ optimizer 的前置就位。
- **⭐ 两条判据必须记牢（写反了行为完全相反）**：① `loadAgentSkills` 的 `userConfigured` 判据是「**解析出的配置项**是否非空」，**不能**看「过滤 enabled 后是否为空」—— 否则「用户全部取消勾选」会被误判成「从来没配过」⇒ 回退默认绑定 ⇒ 「一个都不注入」反向执行成「注入全部默认 skill」；② **skill 注入体量预算闸**（默认 60000 字符，按优先级累加，超出者**跳过并写进注入文本**便于诊断）。
- **⚠️ 三处「原 TS 的粗糙/不实之处」（保真不修，但别信注释）**：① 原注释写「`AGENT_SKILL_BUDGET=0` 关闭限制」，实现却是 `raw > 0 ? raw : 60000` ⇒ **0 走不到**，`budget > 0` 那道闸永远为真（形同虚设）；② `if (!sections.length) return null` **排在通知之前** ⇒ 「绑定全缺失」时连「未注入」通知都拿不到；③ `toToolList` 的清洗顺序是「去括号 → 去首尾引号 → trim」⇒ `[ "a", "b" ]` 混写会留下脏引号（`['"hub_read', 'hub_write"']`）。
- **⭐ 守卫口径的一个坑（踩到并修掉）**：`route_parity_test.py` 判定「Python 已注册路径」用的是 **`app.openapi()`** ⇒ **`include_in_schema=False` 的端点会被当成「未迁移」去探活**。`/agent-configs/defaults` 在委托期标了这个标记（shim 不进文档），本地化后它返回 200 ⇒ 被误报 **SHADOW**。修法就是**去掉该标记**（它本来就是 shim 时代的临时措施）。**以后把委托端点改成本地实现时，记得同步去掉 `include_in_schema=False`。**
- **目录位置的好消息**：`SKILLS_DIR` 指向 **`<项目根>/skills`**（不在 `backend/` 下）⇒ **S7 删 `backend/` 不受影响**（对比 `benchmarks/` 在 `backend/` 里，那边必须搬）。
- **当前剩余（S6 范围）：未注册路径 33 条，只剩 4 个路由文件** —— `auto-pipeline.ts`(89 行) / `evaluation.ts`(62 行, 5 端点) / `localModels.ts`(611 行, 11 端点) / `mcp.ts`(27 行, 3 端点)，另有 `/gpu/*`（GPU 租约，**S7 前须补**）。守卫口径：Node 201 条路径中**未注册 34 条**（全部正确走 501 兜底）。已迁路由域 **33 个**。：`preset-framework`(6，**看起来可迁**：预设变体 + 出图/出视频都已就位) ｜`agent`(2，需 Mastra=S5) ｜`auto-pipeline`(4，需编排=S6) ｜`mcp`(3，需 S5 的 MCP 管理) ｜`evaluation`(5，需评估子系统) ｜`localModels`(11，需 GPU 租约)。另有两处服务层欠账：**镜头 QC 打分**（≈510 行，调用点已占位）、**像素处理**（校色/参考图压缩，需 Pillow）。
- **`redactUrl` 不能用 `urlencode`**：JS 的 `searchParams.set('key','***')` 不会把 `*` 编码，而 Python 的 `quote_plus` 会变成 `%2A%2A%2A` —— 而这个 URL **会出现在 `/test` 的响应体里**（不是纯日志），前端看到的就是乱码遮蔽。改成对 query 做定向正则替换，其余字节保持。


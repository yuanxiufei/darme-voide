# 记忆索引 —— 会话开始读这里，不要整读日志

> **三层读法**：`MEMORY.md`（不变量与约定，**必读**）→ `TOPICS.md`（低频长专题，按需）→ 本索引（日志定位）。
> 日志是**过程存档**，其结论/红线**已提炼**进 `MEMORY.md` / `TOPICS.md` / `docs/` / `backend-py/app/skills/README.md`，**通常不需要读原文**。
> 确需过程细节/证据/命令时：按下方 `@行号` 用 `read_file(offset, limit)` **只读那一节**。
> ⚠️ 下方**体量与行号为 2026-09-12 快照**；日志只追加 ⇒ **`@行号` 锚点不会失效**（只有总行数在涨）。

## 日志清单（按需跳读）

**`2026-09-24.md`**（消费方核查 + 网格规则逐值钉住）
① 把昨天「两条掩码与 `segments` 不在同一坐标空间」的警告**往消费方查实** ✓：`app/` 里只有
`H3FormTrunk.forward` 用 ✓，且只取 `numel()` 当行数 ✓ ⇒ **只是文档问题不是隐藏 bug** ✓
（⭐ 教训：发现"两个坐标空间"要先找全消费方再下结论 ✓）；② ⭐⭐ 三条「混了不报错」的**网格规则**
从摘要式断言升级为**逐值** ✓（`engine_h3_form_test` 74 → **76 项**）@㉓′~㉓″：`cond` 用**目标**网格 ✓
而 `ref_img` 用**它自己的** ✓（两者**必须不相等** ✓）；两段 `ref_audio` **同名不同源** ✓（`audio` 类 ⇒
**目标**两端 ✓、`video` 类 ⇒ **它自己**两端 ✓）+ 钉住 video 类参考块「**音频行在前、视频行在后**」✓；
③ ⭐⭐ 混合形态下的**模态标签**逐段钉住 ✓（`engine_h3_form_test` 76 → **77 项**）@㉔ ——
`cond`/`cond_audio`/`ref_img`/`ref_audio` 这**四档只在混合形态出现** ✓✗，此前**只有文档没有断言** ✗
（标签只是 adaLN 的**行内偏移** ⇒ 错了**完全不报错** ✓，只是模态接到别的模态那组参数 ✓✗）；
④ ⭐⭐ 对着**事实表**逐条核覆盖 ✓：`dit.H3_PACK_FACTS` 第 3/4 条此前只有摘要式覆盖 ✗ ⇒ 补逐值判据 ✓
（`engine_h3_form_test` 77 → **79 项**）—— 跨度表 **(1,4,4,4,4) 循环** × 5/3 ✓（给错照样单调 ✓✗，
只有回绕那格会露 ✓）+ 立体声 **channel-major 的落点是 `w` 两端** ✓（两种声道顺序的行数与 t **完全一样** ✓✗，
只有 w 分得开 ✓）；⑤ ⭐⭐ 修掉**产品侧终态竞态** ✓（这才是那两次「假红」的真因 ✗✗）：`h3` 服务此前**先写 `succeeded`** ✓
再 `finally` 里 `/free` ✗ ⇒ 中间态「成功了但 `freed_vram=null`」✓✗（API 自相矛盾 ✓；真机同样中招 ✗）
⇒ 改成**先卸载、再落终态** ✓（两条执行体 ✓）+ 补 ⑭′「失败路径也 `/free`」✓（`h3_stage2_test` 20 → **21 项**）@⑥；
⚠️ **更正昨天的判断** ✗✗：昨天判成「并发资源竞争、不是代码」 ✓✗ —— 真因是竞态 ✓，并发只是让它更容易露 ✓
⇒ ⭐ **「单独复跑就好了」≠「不是代码问题」** ✗（概率低而已 ✓）：有红要**单独连跑 3 次** ✓，还绿再查时序 ✓；
⑥ 我自己的错 ✗：期望段表漏掉目标两条流 ✓✗、把 video 类参考块顺序写反 ✓✗、**第三次**在双引号串里写半角引号 ⇒
语法错 ✓；⑦ **干净全量：104 套 / 3629 项 / 0 失败** ✓（只跑一次 ✓；⚠️ `h3_stage2_test` 自带轮询 ⇒
会**慢一阵** ✓ 不是卡死 ✓，判据是「有没有 `结论：` 行」✓）；⑦ ⭐⭐ **记忆层第四次腾预算** ✓（起因：顺手
发现 `MEMORY.md` **13906 字符** ✗，而它的守卫 `app/scripts/check_memory.py` **不在 `run_all.py` 里** ✗
⇒ 一直在悄悄烂 ✓✗；跑守卫 ⇒ **7 处致命** ✓：预算超限 ✓ + **5 篇日志的末节锚点早漂** ✗（⚠️ 末节/步骤小节
锚点**会随日志增长漂移** ✗，写 `@末节` **不算登记** ✓✗）+ 一处步骤小节未登记 ✓）⇒ 细节**整体下移**
`TOPICS.md` §自 MEMORY.md 下移（2026-09-24）五节 ✓（§A 分词器/§B 跨来源校验/§C 前端类型检查/§D 上机自检/
§E 记忆守卫自身/§F 环境与自检纪律 ✓）⇒ **13906 → 7907** ✓、守卫 **exit 0 ✓ 致命 0 ✓**；⑧ 逆向
**SLM 无限漫剧创造台** ✓（`D:\app\SLM` 只是**壳** ✓，本体是 `H3EasyDirector` 工作台 ✓ **在同一模型家族
MiniMax H3** ✓ 上 ✓ —— ⭐ 最值三件：**三档引擎表** ✓（官方 25 步/标准 8 步/极速 4 步 + 每档必备模型与
显存建议 ✓）/ **已知冲突台账** ✓（DaSila ⇒ WinError 433 ✓ 等 7 条 ✓）/ **契约同步+离线兜底** ✓
（权威在工作台 `/api/workflow_tiers` ✓）⇒ 细节见 `TOPICS.md` §SLM 逆向笔记 ✓；⚠️ 它的提示词/工作流在
74.7 MB 的 `v3.5.40` 包里 ✗ 未拉 ✓）@108

⭐ **⑨~⑳ 两轮「接线」**（用户「没有实现的继续实现」✓）：**判据层立了却只被自检调用** ✗✗ ⇒ 一轮补
**提交前那几道关**（配音契约 / H3 形态决策 / prompt 契约 + 加速链 / 分段与超清计划端点 ✓ ③④⑤⑨⑩⑪⑫⑬⑭⑮⑯⑰⑱），
二轮补**加载层与张量层**（混合加载**加载层** ✓ / 超清**二采阶段** ✓ / 段级音频合成接进单镜合成 ✓ ⑲⑳）
⇒ 落点表见 `TOPICS.md` §接线进度 ✓、判据见 `MEMORY.md` §接线 ✓；⚠️ 早先「仍未接 `cache_key`」已过时 ✗ —— 二轮已接上 ✓
（`find_reusable_video` ✓ 先补「标量按值进指纹」✓✗）。⭐ **㉑ 第三轮收尾**（2026-09-25 ✓，用户「请继续」✓）：「能力已有、没接出去」最后两处 ✓
① 多集节奏相位注入 ✓（`runtime.py` 早先「`rhythm-phase.ts` 未迁」warn 占位 ✗，`rhythm_phase.py` 早已迁好 ⇒ `storyboard_breaker`
少一段跨集节奏引导 ✓✗ ⇒ 真调 ✓）；② `refineNote` **自述与实现相反** ✓✗（`denoise:False` ✗ 而 `_second_pass` 已实现 ✓ ⇒ 对齐 ✓）。
全量 **125 套 / 4099 项 / 0 失败** ✓（+3 项 ✓；顺带校正 `engine_refine_test` 记 14 条、实测 23 条 ✓）。
⭐ **㉒ 逆向收口 + 自述对齐**（2026-09-25 ✓，用户「有用的就逆向，没用的就不做」+「继续完善」✓）：① 逆向待做项判定收口 ✓ ——
TTS 三件套**有用部分早已落地**（`voice_contract.EMOTION_ORDER` 8 维 = 照抄 IndexTTS ✓，另两条情绪入口 emo_audio/文本情绪是
IndexTTS 专属、本仓 CosyVoice 用不上 ✗）；工作台 HTTP 面**核心已落地**（三档表→`tiers.py` ✓）后端 PyInstaller 编译读不到 ✗ ⇒ 不做 ✓
（详见 `TOPICS.md` §逆向待做项收口 ✓）；顺手纠正「IndexTTS 情绪 9 维」✗ → 实为 **8 维** ✓。② 清掉 3 处**自述与实现相反**的 stale ✓
（`refineNote.denoise` ✓ / `torch_backend` 模块头「架构装载还没写」✗ / `engine/__init__`「本机无 torch」✗ —— 实已装 CPU 版 torch ✓）。
⚠️ 功能层面**已无「不需要真权重」的可做** ✓，余下全卡真权重/真机 ✓。
⭐ **㉓ 模型目录自主化**（2026-09-25 ✓，用户「完全自主不依赖第三方」+「也要能检测电脑内模型」✓）：① **下载/存储自主** ✓ ——
models_dir 默认从 ComfyUI 目录改为 **`<data_root>/models`** ✓（`local_model_scan.default_models_dir()` ✓ + `get_model_paths` 与
`model_manager.resolve_paths` 默认回落 ✓；`configs/model-paths.json` 清空 ComfyUI 硬编码 ✓）；② ⚠️ **「检测/扫描」不排斥第三方** ✗ ——
`get_default_roots` 仍覆盖电脑内模型（本仓目录 + 本地服务 + ComfyUI 目录探测 + extra_roots ✓）：自主的是「从哪下载、存到哪」，
**不是**「不许看见别人已装的模型」✓（先误删了 ComfyUI 扫描、用户一句话纠正 ✓）；③ 全量 **125 套 / 4099 项 / 0 失败** ✓（项数不变 ✓
只改 1 条「清空 models_dir 回落到默认」的断言 ✓）。
⭐ **㉔ 自研 LLM 运行时起步**（2026-09-25 ✓，用户「要自研实现」✓）：文本生成此前走 ollama（HTTP 11434）✗ ⇒ 新建
`engine/llm.py` ✓ —— 自研 decoder-only transformer（RMSNorm / GQA 分组查询 / RoPE / SwiGLU / 因果掩码 / KV cache /
采样 greedy+top-p+top-k ✓）；架构参数**全显式** ✓（`LlmConfig` 不写死 Qwen3 ✗ 本仓纪律 ✓）；自检 `engine_llm_test` **13 条** ✓
（⭐⭐ KV cache 不变量：整段 vs 逐 token 逐位相同 ✓ / 因果掩码「改后不动前」✓ / RoPE 保范数 ✓ / GQA ✓ / tie embeddings ✓）。
⚠️ **尚未接线** ✗：GGUF 权重加载 + k-quant 反量化**未做** ✗（quant.py 现只 cover fp8/int8 ✗）⇒ 下一轮 ✓；
全量 **126 套 / 4112 项 / 0 失败** ✓（+1 套 / +13 项 ✓）。
⭐ **㉕ GGUF 反量化**（2026-09-25 ✓，用户「继续」✓）：新建 `engine/gguf_dequant.py` ✓ —— 把 GGUF 数据区的量化字节 ⇒
fp32 张量 ✓（F32/F16/BF16/Q8_0/Q4_K ✓；公式照 llama.cpp `ggml-quants.c` ✓ **MIT** ✓ web_fetch 取权威 ✓）；⭐⭐ 判据 = 手造
Q4_K block（d=1/dmin=0.5/scales 全 0xFF/qs 全 0x11 ⇒ 反量化**每元素精确 = 31.5** ✓✗ —— 字节布局或 6-bit 解包错一处就对不上 ✓）；
⚠️ 其余 k-quant（Q2_K/Q3_K/Q5_K/Q6_K）**具名拒绝** ✗；`load_weights` 能整文件提权重 ✓ 但大文件（14B≈17GiB fp32）要**逐张量**读 ✓。
⚠️ **下一步** ✗：GGUF 张量名 → `llm.LlmModel` 参数的**映射**（`model.layers.N.self_attn.q_proj.weight` → `blocks.N.attn.q_proj.weight` ✓）未做 ✗；
全量 **127 套 / 4121 项 / 0 失败** ✓（+1 套 / +9 项 ✓）。
⭐ **㉖ GGUF → LlmModel 装载**（2026-09-25 ✓，用户「继续」✓）：新建 `engine/gguf_to_llm.py` ✓ —— ggml 命名
（`blk.N.attn_q.weight` ✓）→ LlmModel 命名（`blocks.N.attn.q_proj.weight` ✓）+ 6 类线性层**转置** ✓（GGUF (in,out)→PyTorch (out,in) ✓）
+ tie embeddings 时 `output.weight` 跳过 ✓；⭐⭐ 判据 = **往返恒等**（LlmModel state_dict → 反向转 GGUF 命名 → 正向映射回 ⇒ 逐张量相等 ✓✗）；
`load_llm_from_gguf` 逐张量读 + `load_state_dict(strict=True)` ✓（漏/形状错当场报 ✓）。⚠️ **下一步** ✗：自研文本后端
（describe+generate）替换 `OllamaTextAdapter` ✗（这才是「不调 ollama」的最后一跳 ✓）；全量 **128 套 / 4128 项 / 0 失败** ✓（+1 套 / +7 项 ✓）。
⭐ **㉗ 自研文本后端**（2026-09-25 ✓，用户「继续」✓）：新建 `engine/llm_backend.py` ✓ —— 把「架构+反量化+装载」收成**一个后端**
（像 `TorchBackend` ✓）：`describe` 如实报缺（权重/词表/模型缺啥报啥 ✓ 不冒充 ✗）/ `load` 幂等装载 / `generate` 走 encode→生成→decode ✓
（⚠️ **同步** ✗ CPU 推理会阻塞 ✗ 接线要 `asyncio.to_thread` ✓；⚠️ chat 模板未做 ✗ 裸拼接 ✓）。⭐⭐ 判据 = 未装载就 generate ⇒ 明确拒 ✗
（不静默返回空串 ✗）。⚠️ **下一步** ✗：`text_generation` 的 engine 分支 + `infer_llm_config`（从 GGUF 元数据读架构参数 ✓）⇒ 这才是「接线」闭环 ✗；
全量 **129 套 / 4134 项 / 0 失败** ✓（+1 套 / +6 项 ✓）。
⭐ **㉘ 接线闭环**（2026-09-25 ✓，用户「继续」✓）：① `gguf_to_llm.infer_llm_config` ✓ —— 从 GGUF 元数据读架构参数
（`{arch}.embedding_length/block_count/attention.head_count[_kv]/feed_forward_length/rope.freq_base/...` ✓，Qwen3 GGUF 沿用 `llama.` 前缀 ⇒ 回退读 ✓）；
缺必需字段 ⇒ **具名拒绝** ✗（不猜默认 ✗ —— 猜了装出「名字对、形状全错」的模型 ✗）；head_dim 缺省 = hidden/heads 推导 ✓。② **接线** ✓ ——
`text_generation.generate_text` 加 engine 分支 ✓：provider=engine ⇒ 走自研后端（`asyncio.to_thread` 包同步推理 ✓）而非 HTTP 调 ollama ✗；
后端缓存（大权重只 load 一次 ✓）。⭐⭐ **文本不依赖 ollama 已闭环** ✓（剩「下载真权重 + chat 模板」这两件数据/细节 ✓）；
全量 **129 套 / 4141 项 / 0 失败** ✓（+7 项 ✓：infer 5 + 接线 2 ✓）。
⭐ **㉙ 对话骨架（chat 模板）**（2026-09-25 ✓，用户「继续实现功能」✓）：㉘ 自述「剩『下载真权重 + chat 模板』」⇒ 补 chat 模板 ✓ ——
新建 `engine/chat_template.py` ✓（**骨架从权重元数据取** ✓ 不内置 ✗✗；`StrictUndefined` ✓✗ 缺变量**报错**而非静默空串 ✓；
注入 `raise_exception` ✓）；接线 `llm_backend` ✓（load 读骨架 / generate 走骨架 / describe 如实报 ✓，**无骨架时老行为一字未动** ✓）；
⭐⭐ **「没有模板」与「模板坏了」是两回事** ✗✗（坏了**当场拒** ✗ 不静默回落裸拼接 ✗）；自检 `engine_chat_template_test` **18 项** ✓
（+1 套 / +18 项 ✓；既有 `engine_llm_backend_test` 8/8 ✓ 未破）；⚠️ 如实报「未核」✗：用骨架时 `add_special_tokens`
仍 `True` ✓（待真词表定 ✓）。⭐ 同轮**核查用户点的方向** ✓ ⇒ 配音契约**早已闭环** ✓（那条「下一步」过时 ✗）、
音色克隆**样本面无「静默错误」级真缺口** ✓（详见日志 ⑨ ✓）；⚠️ 清理 `.codebuddy` 踩坑 ✗✗：连带删了 `memory/` 10 篇日志
⇒ `git restore` 完整恢复 ✓ 守卫复绿 ✓ 致命 0 ✓（教训：删除逐条 `-LiteralPath` ✓）。@486

**`2026-09-23.md`**（H3 混合打包形态 + 掩码坐标空间）
① 找洞的路子换成**扫「注释里写着要求、代码不核」的措辞** ✓（`应与`/`必须一致`/`应当与` …）⇒ 多数已在
自检钉住 ✓，这条路子暂时收口 ✓；② ⭐⭐ **关键帧 + 参考块一起给**（现实常用组合 ✓）此前**一条用例都没有**
✗ ⇒ 补 4 条（`engine_h3_form_test` 70 → **74 项**）@㉒⁶~㉒⁹：段序 ✓ / `cursor = text_len + Σ参考跨度` ✓
（**参考块推后 ✓、条件块不推后** ✗✗）/ 行数对账 ✓ / 行序「能失败」✓；③ ⚠️⚠️ **`img_update`/`audio_update`
与 `segments` 不在同一坐标空间** ✗✗（前者只按视频行/音频行打包 ✓ **不含 text 行** ✗ ⇒ 拿段表下标切掩码
**必错位且不报错** ✓✗）—— 不是产品 bug ✓（打包空间有意 ✓）但文档缺一句 ✗ ⇒ 已在 `packed_layout`
docstring 补显式警告 ✓；④ 夹具坑三条（关键帧要按**目标网格**给 ✓ / 参考块**潜通道数**也得等于
`latents_dim` ✓ / 「对调 ⇒ 必变」要换**同形**块且**视频源与音频源各自都要换** ✓✗）；⑤ **干净全量：
104 套 / 3623 项 / 0 失败** ✓（全量只跑**一次** ✓）@37
⚠️ **2026-09-24 更正** ✗：那次的「假红」**不是并发** ✗✗ —— 是**产品侧终态竞态** ✓（见 `2026-09-24.md` §⑥ ✓）。

**`2026-09-22.md`**（跨来源校验六字段 + 环境陷阱 + 前端类型检查清零）
① ⭐⭐ **跨来源校验**（「同一事实两个来源」✗）从潜通道数扩到 **6 个字段** ✓：`latents_dim` ↔ VAE
`latent_channels` ✓（给错的 `patch_size` 从此在**挂载期**被拒 ✓）/ VAE `spatial_scale` ↔ **H3 事实
`vaeScale=16`** ✓（不等 ⇒ 画面尺寸与请求的不是一回事 ✓✗）/ 音频声道 ↔ `AUDIO_LATENT_CHANNELS` ✓ /
音频帧率 ↔ `AUDIO_LATENT_HZ=40` ✓（不等 ⇒ **wav 时长错** ✓✗）/ TE `output_dim` ↔ 主干 `text_dim` ✓；
落点 `TorchBackend._check_attached_vaes` ✓ / `_check_text_encoder` ✓（`describe().vaeCheck` ✓），
⚠️ 挂载守卫一律**先核后改** ✓（反例：先挂上再回滚会把**先前挂好的**那个也清掉 ✗✗）；
② ⚠️⚠️ **WinGet 的 ffmpeg 是应用别名（重解析点）** ⇒ `shutil.which` 返回 `None` ✓ + **裸名** spawn 报
`WinError 448 不受信任的装入点` ✗✗ ⇒ 5 套假红 ✓；**追加 PATH 无效** ✗ ⇒ 必须**前置**真实 bin 目录 ✓
（已写进 `run_all.py` 表头 ✓）；③ ⚠️⚠️ **并发跑全量会假红** ✗（`h3_stage2_test` 19/20 ✓✗，单独复跑两次
都 20/20 ✓）；④ BPE `byte_fallback` 的拒绝理由**带实证** ✓（`'be'`/`'eb'` 同输出 ⇒ 顺序不是位置语义 ✗✗）；
⑤ 前端 typecheck 已清零 ✓（本轮之外）；**105 套 / 3619 项 / 0 失败** ✓@308

**`2026-09-21.md`**（自研分词器补齐 + 前端类型检查清零）
① 自研预分词器补 `Split`（**五种 behavior** ✓ 逐例实测 ✓）/ `FixedLength` ✓；
② 自研 `byte_fallback`（Unigram ✓ **段级判据** ✓：段内每个字符都能展开才逐字符展开 ✓ 否则整段一个
`unk` ✓）+ 顺带修掉 Metaspace 补前缀的既有偏差 ✗（首字符是 `replacement` ⇒ **不再补** ✓）；
③ ⚠️ 教训：**「实测过」必须写明前置条件** ✗（第一轮的拒绝理由就因为复现残缺而判错 ✓✗）；
④ 前端类型检查从 537 条清到 **0** ✓ + 抓到真 bug ✓（`api.get` 只收一个参数 ⇒ 第二参数被**静默忽略** ✓✗）。@280

**`2026-09-20.md`**（3.2k / 47 行，S7：记忆层第三次腾预算 + 归位 5 条规则）
S7 第 101 步：**记忆层「第三次腾 8k 预算」** ✓（`MEMORY.md` **7848 → 6873** ✓ 致命 0 ✓）—— 起因：上一轮末尾只剩 **152** 字符 ✗ ⇒ **任何新规则都塞不进去** ✗；做法照本仓自己的规矩「**逼近上限时把细节下移 `TOPICS.md`，勿硬塞**」✓：`TOPICS.md` 新增 §自 MEMORY.md 下移（2026-09-20 ✓）收下 §Skill 体系 / §画风体系 的**完整表述**（绑定的**唯一 UI 挂载入口**与拖拽语义 ✓、注入闸口径 = `renderSkill(parseSkill(...)).length` ✓ **不等于字节数** ✓、兜底库 ✓、`meta.yaml` 死数据但溯源只此一处 ✓、`prompt-utils` 不拆 ✓、解析链唯一入口 ✓、**正负成对收口** ✓）；`MEMORY.md` 精简成**只留会导致 bug 的判据** ✓ + 指针 ✓；⭐⚠️ 顺手抓到**过期指针** ✗✓：§画风 原文写着后端单一事实来源是 **`shared/prompt-utils.ts`** ✗，可 **Node 侧 2026-09-15 就删了** ✓✗（指向**不存在的文件** ✓ 且它正是「加画风只改这 2 个文件」的一半 ✓）⇒ 核准真源 = **`backend-py/app/services/prompt_utils.py`** ✓（消费方 `routers/app_settings.py`/`routers/dramas.py` ✓）并原地更正 ✓ ⇒ ⭐**规则：删掉一侧实现时，必须同时搜一遍「指向它的文档指针」** ✗（代码路径删了会有守卫报错 ✓，**文档里的路径不会** ✓）；⭐ 顺带把 09-18 只落在日志里的 **5 条规则**归位 `MEMORY.md` §代码约定 ✓（检测器模式要比解析器更宽松 ✓ / 追加小节只锚「末节首行」✓ / 模块级状态入口清零 ✓ / `compile()` 过了 ≠ 名字在 ✓ / 汇总打分母 + 点名异常项 ✓ / 规模数字指向唯一权威或带实测日期 ✓）@3｜补记：**全量刷新** ✓（第 100 步改了项数 ⇒ 旧数字作废 ✗）⇒ 实测 **94 套 / 3224 项 / 0 失败** ✓（**不推算** ✓；⭐ 交叉校验 3224−3213=**11** = 本次新增用例数 ✓ 对得上 ✓）；顺手又改掉 README **另一处陈旧数** ✗（「82 套件 / 2911 项」⇒ 94/3224 ✓）⇒ ⭐**规则：改计数类数字后要全文再搜一遍所有同类数字** ✗（多处散落、各自过期时间不同 ✓；上轮只搜了 3213 才漏掉 2911 ✓）@49｜S7 第 102 步：**把「零调用」的提示词端点接进前端** ✓ —— 按本仓判据「**没人调用的库不算功能**」✗，把 `prompts/resolve`/`prompts/polish` 接上 ✓（另三个 `assets/*`、`continuity/check` 的**聚合体**已被 `PreflightPanel` 用着 ✓ ⇒ 不重复铺 UI ✓）；落点选**提示词页**（386 行 ✓）而非 2300+ 行的分镜页 ✗ ⇒ 新增**独立组件** `components/PromptQcPanel.vue` ✓（①占位符解析 ✓ ②五段质感层 ✓；⚠️ 面板**只呈现不判分** ✗ 判据只在后端一份 ✓）+ `useApi.ts` 加 `promptToolsAPI` ✓（**返回形状按后端真键写在注释里** ✓ 不猜 ✗）+ 页面只插一行 ✓；核准两件事：照抄 `PreflightPanel.vue:13` 的导入写法 ✓、从 `to_dict()` 取真键 ✓（`resolve`→text/ok/used/unresolved/durationsMs/residualTags/bareIds/problems ✓；`polish`→sections/text/ok/issues/vagueTerms/inserted ✓）；⚠️ 我自己**写坏过一次** ✗（SFC 里多写一个 `<script>` 块 ✗ + 模板调不存在的函数 ✗）⇒ **自审**抓到 ✓（不是工具抓到 ✗）；实测 SFC **真编译** OK ✓✓（脚本跑完即删 ✓ 已核 True ✓）/ 前端调用覆盖 **5/5** ✓（调用点 **166→170** ✓ **0 未匹配** ✓，后端 251 条 ✓）/ `route_parity` 251 已注册 漂移 **0** ✓ / 层级 6/6 ✓@71｜S7 第 103 步：**坏文件不再拿满分** ✓（`technical_qc_test` 42→**43/43** ✓）—— 第 100 步修完三项死检测后发现的**同族另一个洞** ✗：`_run` 的判失败条件是「**非零退出且 stderr 为空**」✓（**保持不动** ✗ —— 改成"非零即失败"会把正常视频判死 ✓，因为 ffmpeg 滤镜正常结束也会非零退出 ✓），但连带后果是**坏文件 ⇒ ffprobe 非零 + 有 stderr ⇒ 不报错 ⇒ 各滤镜读不出特征 ⇒ 100 分零 issue** ✗✗（**损坏的成片看起来"干净"** ✓）；修法只补**报出** ✓：`duration` 与 `fps` **同时**读不出来 ⇒ warning「无法读取视频规格…检测会全部空转」+ 扣 15 分 ✓（与异常分支同口径 ✓）；⚠️ 判据刻意写成「**两条都缺**」✗ 而非「stdout 为空」✓（容器/裸流本就少字段 ✓ 无音轨或某些容器没有 `avg_frame_rate` 也不该报 ✓）；原「满分且零 issue」用例**按预期翻红** ✓（它本就是**信号灯** ✓）；实测 qc_report 42 ✓ / qc_scoring 29 ✓ / qc_retry 14 ✓ / **smoke 476/476** ✓✓ / 251 已注册 漂移 0 ✓ / 层级 6/6 ✓；⭐ 规则：**"判定输入读不出来"本身必须是一条判据** ✗（会话第 3 次同族：常量不同源 ✓ 空集当绿灯 ✓ 读不出当通过 ✓）@107｜S7 第 104 步：**「没比出来」不再冒充「通过」** ✓（`consistency_qc_test` 30→**32/32** ✓）—— 顺「静默绿灯」一族继续扫 ✓，在 `services/consistency_qc.py` 找到同一个病 ✗：一对都比不出来时（分镜 <2 ✓ / 图缺失 ✓ / 图读不出 ✓）原实现直接 `continue`/`return` ⇒ 返回体只剩 `checkedPairs:0, warningCount:0` ✗✗ ⇒ **与「比过且都没问题」完全同形** ✓（消费方看 `warningCount==0` 就得出「连续性没问题」✓）；修法与本仓同口径 ✓：加**三态 `consistent`**（`None`=没判 ✓ / `True`=比过零 warning ✓ / `False`=有 warning ✓）+ **`coverage`**（shots/pairs/compared/skippedMissingImage/skippedUnreadable ✓，没比过时给 note「**没判**（不是通过）」✓）；⚠️ **有意**与冻结 TS 接口不一致 ✓（Node 已删 ⇒ 不再是行为契约 ✓；「空报告 = 全绿」会误导调用方 ✓，依据写进注释 ✓）；⚠️ 自检又抓到我**期望写错** ✗（断言「1 号无图 ⇒ 跳过 1 对」✗，实测 `shots=5 / compared=4 / skipped=0` ✓）⇒ 顺手把断言换成**自洽不变量**（`pairs == shots−1` ✓ 且 **`compared + 各 skipped == pairs`** ✓ —— 能防"新 skip 路径忘了计数" ✓✓）；实测 smoke **476/476** ✓✓ / qc_scoring 29 ✓ / qc_report 42 ✓ / 251 已注册 漂移 0 ✓ / 层级 6/6 ✓；⭐ 规则：**「没比出来 / 没判」必须是机器可读的状态** ✗（本会话第 4 次同族 ✓）；同族还剩一处**未动** ✗：`qc_scoring.py` 的**死规则**（「同一场景但地点 ID 不一致 −20」**永不触发** ✓ —— 两镜同场景行 ⇒ 必然同 `location_id` ✓）@137｜S7 第 105 步：**删掉一条死规则** ✓（`qc_scoring_test` 29→**31/31** ✓）—— `qc_scoring.py` 的「同一场景但地点 ID 不一致 ⇒ −20」**结构上不可能触发** ✓✗（判据要求 `prev.scene_id == sb.scene_id` ✓ 而 `prev_scene` 又按 `prev.scene_id` 查 ⇒ **同一个场景行** ⇒ `location_id` 必然相同 ✓；测试里一直写着「原 TS 死分支，永远不触发」✓ —— 都知道没用但都留着 ✗）；⭐ **判断：删掉** ✓ 而不是"让它能触发" ✗（真要比**不同场景**的地点 = **换语义** ✗ ⇒ 跨场景换地点由 `continuity.py` §11 转场动机管 ✓；删除依据：**Node 已删 ⇒ 无需对拍** ✓ 且**分数不变** ✓）；注释里写明留下的**两条真判据**（本函数的「未锁 location_id ⇒ −5」✓ + `continuity.py` §6 场景空间/越轴 ✓）；⭐ 补一条**正对照**（场景无 `location_id` ⇒ continuity=95 ✓ + 文案点名 ✓）—— ⚠️⚠️ 但这条正对照**自己先红了两次** ✗✓：①放同集 ⇒ 上一镜（3 号）没填状态 ⇒ 额外 −5 ⇒ 实测 90 ✗；②给 3 号补状态 ⇒ 4 号挤进兄弟序列 ⇒ **改掉 9 号镜的"上一镜"** ⇒ **弄红另外两条接线用例** ✗✗（期望 93 实测 95 ✓）；③最终解法：对照镜放**另一个集**（`ep2` ✓ 无兄弟 ⇒ 零波及 ✓）+ 给 `ep2` 补一条 `continuity_states`（否则再命中「本集未保存跨镜状态 −5」✗）⇒ ⭐**规则：测试夹具是共享状态** ✗（加一行要问"谁把这一行当自己的上下文" ✓：上一镜/兄弟排序/计数 ✓）；**只想测一件事就给它独立容器** ✓；实测 smoke **476/476** ✓✓ / qc_report 42 ✓ / consistency_qc 32 ✓ / technical_qc 43 ✓ / 251 已注册 未注册 0 **漂移 0** ✓ / 层级 6/6 ✓@179｜S7 第 106 步：**接上一个「什么都不做」的工具** ✓（`agent_storyboard_tools_test` 33→**34/34** ✓）—— 本步是**换扫法**的收益 ✓：把后端所有「永不/恒为/空转/未实现/尚未/死分支」标记**一次挖出来**逐个判真伪 ✓（`app/**/*.py` 命中 **66 处** ✓）；第一条就是真货 ✗✓：`agent/tools/storyboard_tools.py` 的 `assign_rhythm_phases` 是**空实现** ✗（docstring 写「尚未移植」✗），而 `services/rhythm_phase.py` **早就在** ✓ 且**自带套件 22/22** ✓ ⇒ agent 每次 `save_storyboards` 都"分相位"✓ 而 `storyboards.rhythm_phase` **一直是空的** ✗✗（⭐ 比"报错"更坏 ✓：报错有人看见 ✓，**静默不做只有查库才发现** ✓）；修 = **委托**那份实现 ✓（同一事务内调 ✓ 判据只留一份 ✓）+ 条数进**任务日志**（不塞回执 ✗ 回执有严格断言 ✓）+ docstring 改 ✅ ✓；证明用**查库里的列**不看回执 ✓（全非空 ✓ 首镜恒 `setup` ✓ 三镜不止一个值 ✓ ⇒ 空实现会立刻红 = 信号灯 ✓）；实测 `rhythm_phase_test` **22/22** ✓ / agent_runtime 50/50 ✓ / smoke **476/476** ✓✓ / 251 已注册 漂移 0 ✓ / 层级 6/6 ✓；还剩几处待判 ✗（`ffmpeg_compose` 不支持 subtitles 时**只跳过不报错** ✓；`image_generation` 的 `processing` **永久卡死** ✓；runtime 的 Gemini 未支持是**显式报错** ✓ 不是病 ✓）@217｜S7 第 107 步：**给守卫补上它自己承认的盲区** ✓ —— 词表镜像「值」逐字守（`route_parity` 251 已注册 / 未注册 0 / 漂移 **0** ✓）—— 新扫法（去**测试文件**里找「永不 / 不覆盖 / 靠人工核对」这类**自我声明** ✓）命中：校验 `prompt_utils` 词表的守卫**自己写着盲区** ✗（「**不覆盖**跨行拼接的常量 `VISUAL_STYLE_VIDEO` 与映射表的**值**，**靠人工核对**」）；⭐ **先取证**（一次性探针 ✓ 跑完即删 ✓）：**盲区 1 是误记** ✗（`VISUAL_STYLE_VIDEO`/`VIDEO_MOTION_BASE`/`IMPERFECTION_ANCHORS`/`QUALITY_TAIL` 四个**都抽得准且与 Python 逐字相同** ✓ ⇒ 只是**没列进 `expected`** ✗ —— **「没被断言」≠「断不了」** ✓，补上 ≈ 白捡的覆盖 ✓）；**盲区 2 是真的** ✗（`re.findall` 只吃多行值的**第一行** ⇒ 10 条只抽到 1 条、且抽到的是**残值却看着像对的** ✓✗）；修：`expected` 补 4 常量 ✓ + 新增 `_extract_ts_object_string_values()`（与常量抽取器**同款**的「引号拼接 + 续行」✓ 两张表**逐条比值** ✓ ⇒ 顺带守住**负面表的 key** ✓ 此前**只**守正表 key ✗）+ ⭐**检查器自检**（抽到的条数必须等于 Python 表 ⇒ 否则报「抽取器漏了」✗ —— **别把「没抽到」当成「没漂移」** ✓）；⚠️ 自己踩两坑：①抽取器初版**漏了「值在下一行」这一形态** ✗（正表抽到 **0** 条 / 负面表 **10** 条 ✓✓ —— **不对称本身就是线索** ✓）②修时又把**半角引号**写进中文文案 ⇒ f-string **直接语法错误** ✓（**第 10 次** ✗）；实测 251/0/**0** ✓（含两张表全部值逐字比对 ✓）/ 层级 6/6 ✓；⭐ 规则：**守卫写"不覆盖 X"时先验 X 到底能不能覆盖** ✗ + **抽取器必须能自证覆盖** ✗@255｜S7 第 108 步：⭐ **用户立了一条长期约定** ✓ —— 原话「**所有功能不要对外依赖，自己实现所有的功能，要参考我给你的几个项目**」⇒ 已入 `MEMORY.md` §⭐ 自研优先（三条硬约束：**能力自研** ✓ / **不对外依赖** ✓ / **参考 `reference/` 六个项目并把有用的搬来接线** ✓；边界：**功能不外包** ✗，`ffmpeg`/SQLite/标准库这类**本地基础设施**不算"对外依赖" ✓）+ 明细下移 `TOPICS.md` §自研引擎现状 ✓；⭐ 按规矩**先实测再记** ✗ ⇒ 实测：引擎 **19 模块 / 9 套 343 用例全绿** ✓（core 26 / dit 26 / inventory 34 / io 39 / loader 28 / mappings 17 / pipeline 98 / segments 54 / text 21 ✓）且**零依赖可跑** ✓ ⇒ **能力已在自己手里** ✓；⚠️ 顺手抓到第 106 步那族的**第 2 例** ✗：`torch_backend.PENDING_PARTS`（**对外宣告**的未完清单 ✓ 被 `describe()` 与自检引用 ✓）**过期** ✗（写着「TE 前向 ✗ / VAE 解码与落盘 ✗」，而 `encode_text`/`attach_text_encoder`/`decode`/`attach_vae`/`write` **全在** ✓ 且已跑通「TE→DiT→VAE→**真 mp4**」✓）⇒ 按实测更正为「**真正缺的只有真权重 + 真配置**」✓（`canGenerate=False` 仍正确 ✓ 但**不许再把原因归到机制上** ✗）；⭐ 口径结论：四类生成**本地实现都已有** ✓（ollama ✓ / 自研引擎 ✓ / CosyVoice ✓）⇒ **要做的不是造能力、而是让默认配置走本地链路** ✓（外部队商留作**可选通道** ✓ 不能是唯一出路 ✗）；`MEMORY.md` **7765/8000** ✓（下次进内容前继续下移 ✓）@294｜S7 第 109 步：⭐⭐ **用户加严约定** ✓ —— 原话「**所有都要自己实现，不要调用外部的**」⇒ 外部队商 API **不是"可选通道"、是要去掉的** ✗；按规矩**先核准现状** ✓（只读查真库 ✓ 8 条 active ✓）⇒ ⭐⭐ **四类生成当前全部解析到外部** ✗✗（文本/音频→`api.minimax.chat` ✓、图片/视频→`ark.cn-beijing.volces.com` ✓，priority **200/300** ✓）**而本地行只是摆设** ✓（85/84/83/82 ✓）—— 决定性口径：`ai_providers.py:87` `rows.sort(key=priority, reverse=True)` ⇒ **priority 越大越优先** ✓（⚠️ `estimate_service.py` 是另一套：`is_default` 先 ✓ 别混）；成因：`LOCAL_PRESET_SERVICES` 本地预设 85/84/83/82 ✗ 天生低于 `PRESET_SERVICES` 厂商预设 100/99/98/97 ✗ ⇒ "先一键配置再切本地"厂商行仍在且更高 ✗；交付：`MEMORY.md` 加严 ✓ + `TOPICS.md` §**外部调用审计** ✓（8 行表 ✓ + 代码行号 ✓ + **三层依赖阶梯**：①外部厂商 ✗✗ 要去 → ②本地第三方运行时 ⚠️ ollama/SD-WebUI/ComfyUI/CosyVoice → ③**完全自研** ✓ `engine/` ✓ + **动作按代价排序** ✓）；⚠️ **本轮没动真库、没改默认值** ✗（改产品行为+写用户数据 ⇒ 需点头 ✓）；⭐ 规则：用户说"去掉 X"时**先查 X 现在到底会不会被执行** ✗（本次查出"会、且四类全走 X" ✓✗ 比"以为只是可选"严重得多 ✓）；⚠️ `MEMORY.md` 一度 **8015 > 8000** ✗ 被守卫拦下 ⇒ 已压缩 ✓@323｜S7 第 110 步：⭐ **执行「尽量本地」** ✓（用户批准「可以按你说的尽量本地」✓ + 点名 **5 个可照抄项目** ✓ 原话「这几个项目**功能可以直接抄**…**不要忘记**」✓）—— ① **抄的清单**落 `TOPICS.md` §可照抄项目清单 ✓（`ComfyUI` = 节点图执行/校验/队列这套**机制** ✓ ⚠️ 别整包搬 ✗；`minimax-h3-comfyui` = H3 节点与 T2V/I2V 工作流 ✓；`ollama` = 本地模型服务形态 ✓；`ollama-python` = 工具 schema/结构化输出（**已有等价件** ✓）；`minimax-desgin-plugin` = 节点注册 + 参数校验 ✓；并定性为**长期挂账的待办源** ✓ —— 能力缺口优先从这五个里找现成实现 ✓）；② **代码**：`LOCAL_PRESET_SERVICES` priority **85/84/83/82 → 503/502/501/500** ✓（口径 = priority **降序**取首条 ✓ 厂商 97–100 ✓ 遗留外部行 200/300 ✓ ⇒ 500+ 留足余量 ✓）；③ **数据**：真库切本地 ✓（按同一口径**逐类复算** ✓：text→`localhost:11434` ✓ / image→`:7860` ✓ / video→`:8765` ✓ / audio→`:9880` ✓）+ **4 条外部行 `is_active=0`** ✓（`api.minimax.chat` ×2 + `ark.cn-beijing.volces.com` ×2 ✓）★**可逆** ✓（快照 `tmp/ai_configs_before.json` / `after.json` ✓；迁移脚本跑完即删 ✓）；④ 顺手排雷 ✓：5 个 Agent 的 `model` **全 `NULL`** ✓ ⇒ 一律走解析出的模型 ⇒ **不必改** ✓（⚠️ "切完 provider 但 model 还钉别家"这类坑要每次都查 ✓）；⚠️ **语义变化**：**不再有外部兜底** ✗ ⇒ 四个本地运行时（11434/7860/8765/9880）任一没在跑 ⇒ 那类生成**直接失败** ✓✗ —— 这正是「不要调用外部的」的语义 ✓；实测 `route_parity` 251 已注册 / 未注册 0 / 漂移 **0** ✓、层级 6/6 ✓；⚠️ `MEMORY.md` 本轮两次超限（8015 ✗ / 8009 ✗）均被守卫当场拦下 ⇒ 已压缩至 **7875/8000** ✓；补记：用户澄清末句 = 「**不要忘记**」✓（我上轮猜"打字串了" ✓ 猜对了 ✓）@362｜S7 第 111 步：⭐⭐ **从点名项目里挖出 H3 全套事实** ✓（并**当场证伪自己一条假设** ✗✓）—— 按「不要忘记」开工 ✓，第一站 `reference/minimax-h3-comfyui` ⇒ ⚠️ **先纠偏** ✗✓：该 repo **0 个 `.py`** ✓（只有 2 个 workflow JSON + README + shell ✓），真身全在**隔壁 `reference/ComfyUI`** ✓（JSON 里节点 `cnr_id: "comfy-core"` ✓）⇒ 另确认本仓 `local_services/h3/workflows/*.json` 与它**同源原样复制** ✓（同名同字节 ✓），而三个 `.py` 是**本仓自研** ✓；挖到并**带行号落库**的 H3 事实 ✓：DiT hidden **5376** / depth **50** / heads **56** / headDim **128** / ffn **14336** / patch **(1,2,2)** / video 潜 **24** / audio 潜 **32** / text_dim **5120** / time embed **256→5376→2688** / eps **1e-5** / rope 16→96 / token_refiner **2 层** ✓（→ `dit.H3_SHAPE_FACTS` ✓）；**vae_scale 16** ✓ 帧网格 `17k+5↔5k+2` ✓ `audio_t=round(sec*40)` ✓ 条件时间步 `0.999/1.0` ✓；采样默认 **steps 10 / simple / denoise 1.0 / res_multistep / 无 CFG** ✓，shift **video 12.0 / audio 3.0**（⇒ audio_scale **4.0** ✓）；**键名表** ✓（→ `mappings.PRESETS["minimax-h3"]` ✓ **故意无合并规则** ✓ + 本仓没有对应物的键**留原样并在 note 点名** ✓）；TE→DiT 喂法：第 **50 层未归一 hidden [B,L,5120]** → `condition_proj` → `token_refiner` → **与 video/audio 潜 token 拼进同一条序列**（⚠️ **不是 cross-attention** ✗）+ per-token 模态 tag + **无 attention mask** ✓；⭐⭐ **交叉核对数字时证伪了自己** ✗✓：**56×128=7168 ≠ 5376** ✓ ⇒ H3 走 `qkv_proj`→`out_proj` ✓ 而本仓 `nn.MultiheadAttention` 推 head_dim **96** ✗ ⇒ **真 `qkv_proj`(21504×5376) 与本仓 `in_proj_weight`(16128×5376) 形状不同** ✗ ⇒ 把「**能构造**」与「**能装载**」**切开** ✓，5 条结构差异写进 `dit.H3_STRUCTURAL_GAPS` ✓（注意力维度 ✓ / **双输出 video+audio** ✓ / adaLN **18 vs 6** ✓ / 缺 `token_refiner`+`condition_proj` ✓ / 文本拼同序列+模态 tag ✓）+ `torch_backend.PENDING_PARTS` ✓；实测 `engine_mappings` **17→20** ✓ `engine_dit` **26→29** ✓ pipeline 98 ✓ loader 28 ✓ inventory 34 ✓ `route_parity` 251/0/**0** ✓；⚠️ 半角引号又犯（**第 11 次** ✗）⇒ `compile()` 一行定位（86 行）修 ✓@417｜S7 第 112 步：⭐ **拆掉「装不上真权重」的第一道门槛** ✓ —— DiT 支持 H3 的注意力形状（`engine_dit` **26→37/37** ✓）—— 上一步抄回的事实里最硬的第 ① 条 ✗：H3 是 **56 头 × 128 维 = 7168** ✓ 而 **hidden 只有 5376** ✓ ⇒ `nn.MultiheadAttention` 只会推 head_dim **96** ✗ ⇒ **形状根本对不上** ✗✗；交付（**向后兼容** ✓，默认路径一字不改 ✓）：`DiTConfig.attn_dim`（默认 **0 = 与 hidden 相同** ✓ ⇒ 旧行为 ✓ 仍走 MHA ✓ 键名 `attn.in_proj_weight` 不变 ✓；非 0 且 ≠hidden ⇒ **显式** `qkv_proj`(hidden→3·attn_dim) + `out_proj`(attn_dim→hidden) ✓ = **H3 形态** ✓ 5376→3×7168 ✓、7168→5376 ✓）+ 新属性 `inner_dim`/`attn_head_dim`（H3 ⇒ **128** ✓）+ `to_dict`/`from_metadata` 都带上 ✓；`DiTBlock` 按 `attn_dim` 分流 ✓，显式形态用 `scaled_dot_product_attention` ✓、**qkv 按 [q\|k\|v] 三段切** ✓（与 H3 检测器"形状÷(3·head_dim) 反推头数"口径一致 ✓）；⚠️ 两处**如实拒绝** ✗：显式形态**不做 cross-attention** ⇒ **构造时**就拒绝 `attn_dim≠hidden` + `cross_attention=True` ✓✓、H3 的 `q_norm`/`k_norm` **暂未实现** ✗（注释写明「先不假装有」✓）；⚠️ 自检**先红后教** ✗✓：我写的「给了 context 就该报错」红了 ✓ —— 因 `DiT.forward` 在 `cross_attention=False` 时**根本不把 context 往下传** ✓ ⇒ 改成构造时拒绝 + block 内层双保险 ✓，并把「context 被忽略」单独钉成一条事实 ✓；实测 `engine_dit` **26→37** ✓ `engine_mappings` 20 ✓ `pipeline` 98 ✓ `core` 26 ✓ `io` 39 ✓ `text` 21 ✓ `segments` 54 ✓ **`smoke` 476/476** ✓✓（默认路径没被碰 ✓）`route_parity` 漂移 **0** ✓；⚠️ 半角引号连犯两次 ✗（**第 12、13 次**）⇒ 已先 grep 再改 ✓ 并得出更准判据 ✓（**grep 命中绝大多数在 docstring/注释=无害 ✓，真正要看的只有落在真字符串里的那些 ✓**；最快仍是 `compile()` 一行定位 ✓）@463｜S7 第 113 步：⭐⭐ **关掉 H3 缺口 ② 与 ④** ✓（`engine_dit` **37→42/42** ✓）—— 接第 112 步（① 注意力维度 ✓）继续 ✓，两条都**加法式** ✓（各自 opt-in ✓、默认路径一字不改 ✓）；③ adaLN 18 路 + ⑤ 文本拼同一序列 要一起做 ⇒ 留下一步 ✓；交付：`audio_latents`（默认 0 ✓；非 0 ⇒ H3 **双输出** ✓ `final_layer.video_out`(hidden→pT·pH·pW·in_channels ✓) + `final_layer.audio_out`(hidden→audio_latents ✓)；`forward` 返回 **(视频, 音频)** ✓ 而旧形态仍返回**单张量** ✓ —— **不静默换形状** ✗）+ `text_refiner_layers`（默认 0 ✓；非 0 ⇒ `condition_proj` + `token_refiner.blocks.N`（每层自注意力 ✓，H3 是 2 层 ✓））+ ⚠️**构造期守卫**（`audio_latents>0` 却不给 `attn_dim` ⇒ **直接报错** ✓ 拒绝「**半套 H3**」✗）；⭐⭐ 缺口清单改成「**只减不骗**」✓（原先把**已关的 ①** 还挂在缺口里 ✗✗）⇒ 拆成 `H3_STRUCTURAL_GAPS`（**还剩 3** ✓：adaLN 18 路 ✓ / 文本拼同一序列 + 模态 tag ✓ / `q_norm`·`k_norm` ✓）+ `H3_GAPS_CLOSED`（**已关 3** ✓，各注明**靠什么关的** ✓），并写明「**关掉 ≠ 核过真权重**」✗（② 音频头、④ refiner 内部都是**可装载的近似** ✓ —— H3 真形态音频是**与视频 token 拼在同一条序列**上出的 ✓）；`torch_backend.PENDING_PARTS` 同步 5→3 ✓；⚠️ 自检又抓到我**期望写错** ✗✓（41/42）：`video_out` 我按 H3 写成 **(96,32)** ✗ 实测 **(16,32)** ✓（缩放版 `patch 1×2×2 × in_channels 4 = 16` ✓，H3 `2×2×24 = 96` ✓ —— **同一个公式** ✓）⇒ 与第 111 步「把 H3 数字直接套进缩放配置」**同类错** ✓ ⇒ **换比例时每个数字都要重算** ✗；实测 `engine_mappings` 20 ✓ **`pipeline` 98** ✓ `core` 26 ✓ `io` 39 ✓ `text` 21 ✓ `loader` 28 ✓ `inventory` 34 ✓ `segments` 54 ✓ **`smoke` 476/476** ✓✓（默认路径没被碰 ✓）漂移 **0** ✓@493｜末节@515｜S7 第 114 步：**把 H3 规格读全** ✓ —— 缺口 5 → **7 条**（⭐**没记 ≠ 没有** ✗✓）：按规矩**先取证** ✓ ⇒ 把 `reference/ComfyUI/comfy/ldm/minimax/model.py` **读全** + 它的单元测试 ⇒ **打脸** ✗：我以为"只剩 3 件" ✓，实际还漏着 **RoPE**（3 轴 ✓ partial split-half ✓ **面积归一化**坐标 ✓ —— H3 **没有位置表** ✗ vs 我学习式 `pos_embed` ✗）/ **正弦 `TimeEmbedder`** ✓ + **每 token 独立 timestep** ✓（我 `Linear(1,hidden)` 单标量 ✗）/ **RMSNorm + SwiGLU** ✗ / **fp32 输出头 + denoise mask** ✓ / **2D 打包序列**（`[总行, 通道×patch]` ✓ **连 batch 维都没有** ✗）顺序 `text|cond/refs|audio|video` ✓（音频在视频**之前** ✓）**无 attention mask** ✓、行数 = `latent_t×(lat_h//2)(lat_w//2)` ✓ / `audio_t×2` ✓（立体声 channel-major ✓）、`adaln_proj` = 6×3 = **18** ✓ **且按 per-token mod-row 分段** ✓ ⇒ **结论写死在代码里** ✓：③⑤ **不是"小改"** ✗ ⇒ 要**单开一个 H3 形态 forward** ✓；交付（这一步**故意不写代码** ✗，只固化规格 ✓）：`H3_STRUCTURAL_GAPS` **5→7** ✓（并注明"第 114 步补全"✓）、**新建** `H3_PACK_FACTS` ✓（打包规格**单独立表** ✓ —— 与形状数字混一起会让两类断言互相牵连 ✗）、`schedules.time_shift_sigma` ✓（闭式 **自己推导** ✓ `b = σ/(s+σ(1−s))` ✓，且**有真消费者** ✓）；⚠️ **没建打包模块** ✗（暂无调用方 ⇒ 按本仓判据先不建 ✓，免得又攒一个零调用库 ✗）；⚠️ 自检又抓到**我方向记反** ✗✓（30/31）：断言 12→3 会「抬向 1」✗，实测 **0.5→0.2（压低）** ✓ ⇒ ⭐**断言只写"能自己算出来的不变量"，不写"方向直觉"** ✗（恒等/不动点/往返全过 ✓，唯一错的就是**凭感觉**那句 ✗）；实测 `engine_core` 26→**32/32** ✓、`engine_dit` 42→**43/43** ✓、`engine_pipeline` 98 ✓、`smoke` 476/476 ✓；⚠️ 改自检时**多打一个括号** ⇒ 编辑**直接失败** ✓（严格锚点又挡一次静默错改 ✓）@526｜末节@558｜S7 第 115 步：**建起 H3 形态的积木** ✓ —— `engine/h3_form.py`（新 ✓ **17/17** ✓）：`RMSNorm` ✓ / `SwiGLU` ✓ / 显式 `Attention`（qkv_proj + per-head `q_norm`·`k_norm` + **partial split-half RoPE** ✓）/ `TimeEmbedder`（正弦 ✓ **cos 在前** ✓）/ `AdalnProj`（**6×3=18** 路 ✓ 按 `mod_segments` **逐 token 分档** ✓）/ `DiTBlock` ✓ / `RefinerBlock`+`TokenRefiner`（2 层 + `final_norm` ✓）/ `FinalLayer`（双头 **fp32** ✓）；⚠️ **它现在零调用** ✗ ⇒ 明写 `H3_FORM_TODO`（打包网格 ✓ RoPE 频率分配 ✓ forward 接线 ✓ mask ✓ 接管线 ✓）并**登记进 `run_all.TESTS`** ✓（否则"没登记 = 没跑"✗）；⚠️⚠️ 自审抓到 **5 处**自己的错 ✗✓（**2 处会静默出错** ✗✗）：① **占位类 `class RMSNorm` 留在模块里** ✗✗ ⇒ 会**静默遮住** `__getattr__` 的真实现 ✓✗ ⇒ ⭐**占位符不许留在模块体里** ✗；② **`_mod_row` 被我自己编辑掉** ✗✓（old_str 把它一起圈走 ✗）⇒ `compile()` 照样过 ✓、跑到 forward 才 `NameError` ✓ ⇒ **第 85 步那条规则的第三次应验** ✓；③ `RefinerBlock.forward` 写成**一串废算式** ✗；④ `__getattr__` **顺序反了** ✗（问不存在的名字也会去 `import torch` ✗）⇒ 改成**先查 `__all__`** ✓；⑤ 第 5 次「**代码对、我的期望错**」✗（`nn.Linear(in,out).weight` = **(out,in)** ✓ ⇒ `fc2` = (8,12) ✓ 我写 (12,8) ✗）⇒ ⭐**断言优先写"能自己算出来的不变量"** ✗（本轮"尺度不变 ✓ 旋转保范数 ✓ 尾部不动 ✓ cos 在前 ✓ 分档双向验 ✓"**一次就对** ✓✓）；顺手改掉 `run_all.py` docstring 的陈旧 **82 套 / 2911 项** ✗（改成以 `TESTS` 为唯一权威 ✓，套数 **95** ✓ 总数**待实测** ✗）；实测 h3_form **17/17** ✓ dit 43 ✓ core 32 ✓ mappings 20 ✓ **smoke 476/476** ✓ 漂移 0 ✓@577｜末节@609｜S7 第 116 步：**把「打包层 + RoPE 角度」落成代码** ✓ —— `h3_form` **17 → 27/27** ✓：先取证 ✓（读 `MiniMaxH3Model` 的 `__init__` + forward 尾部 ✓）⇒ ⭐ `rope_freqs` 算法 = 三轴各乘 `inv_freq` ✓ → **按 t,h,w 拼接** ✓ → **复制一份** ✓ ⇒ `rot_dim = 6·len(inv_freq)` ✓（F=16 ⇒ **96** ✓，作用在 head_dim 128 的**前 96 维** ✓）；⭐⭐ **`inv_freq` 是权重的缓冲区** ✓（`torch.empty(16)` ✓）⇒ **不自己套** `1/10000^(2i/d)` ✗（那会得到"看着像、装不上"的表 ✗）；`t_vals` 走**唯一时间戳** ✓、mod-row = `序号×3 + 模态` ✓（与 `AdalnProj` reshape 自洽 ✓）；velocity **取负** ✓；音频按 `σ_a/σ_v` 带进带出 ✓；⚠️ **新发现**：`video_patch_proj`/`audio_patch_proj`（**fp32** ✓）+ `unpatchify`/`unpack_audio` ✓ ⇒ 写进 TODO ✓；交付 6 个纯函数 ✓（`packed_rows` 段表**音频在视频之前** ✓ 钉方向 ✓ / `position_ids` ✓（video **一帧内共用 t** ✓）/ `frame_grid_coords` 面积归一化 ✓（跨度**按公式自算** ✓）/ `video_t_grid` 跨度表 ✓ / `rope_angles` ✓ **先 cast fp32** ✓ / `mod_row` ✓）⇒ PARTS/TODO 同步 ✓ 自检 ⑱ **双向**钉"只减不骗" ✓；⚠️⚠️ **本轮 7 处红全是我的期望** ✗✓（代码一次没错 ✗）：⭐ **`__all__` 漏加** ⇒ `AttributeError` ✓✓ —— **上一步刚立的"先查名单"守卫当场建功** ✓（**响亮报错 > 静默空壳** ✓）；半角引号 ×1（第 6 次 ✗）；dtype 混算 ×2 ✓；角度行数≠S ✓；**笛卡尔积当成两列逐位相等** ×2 ✓（正解=值集合相同 ✓）；video t 顺序 ✓；⭐ 规则入 `MEMORY.md`：**判据要"响亮"不要"静默"**（惰性导出必配 `__all__` 且**先校验** ✓）+ **断言里凡"顺序/布局/形状/dtype"都当场算一遍** ✗；⚠️ 更正：`MEMORY.md` 下移后实测 **7876 → 7848** ✗（只腾 28 ✓，我的"约 7700"估算差 7 倍 ✗）⇒ **腾预算没跑守卫前只能当估算** ✗；实测 h3_form **27/27** ✓ dit 43 ✓ core 32 ✓ **pipeline 98** ✓ segments 54 ✓ **smoke 476/476** ✓ 漂移 0 ✓@620｜末节@653｜S7 第 117 步：**把 H3 主干接起来** ✓ —— `h3_form` **27 → 36/36** ✓（做到「行级前向」✓）：交付 `H3_DEFAULTS` ✓（H3 出厂尺寸**全抄自参考 `__init__` 的事实** ✓：5376 / 50 层 / 56×128 / ffn 14336 / text 5120 / 潜 24·音频 32 / t 256→5376→**2688** / F 16 / shift **12·3** ✓）、`t_vals_for` ✓（各段 `t=1−σ` ✓ audio 用**换算后**的 σ ✓ + ⭐**去重** ✓ ⇒ 不去重会让行号全错**且不报错** ✗✗）、`mod_segments_for` ✓（`row = 唯一序号×3 + 模态` ✓ 模态档 text 0 / audio 1 / video 2 ✓）、`audio_carry` ✓（`σ_a/σ_v` ✓ —— ⭐ **`schedules.time_shift_sigma` 的真消费者** ✓✓ 终于不再是"没人调用的库"✓）、**`H3FormTrunk`** ✓（模块名与参考 `__init__` **逐字对齐** ✓：`video_patch_proj`/`audio_patch_proj`（**都 fp32** ✓）/`condition_proj`/`time_embedder`/`rope`（**`inv_freq` 缓冲区** ✓ 不自算 ✗）/`token_refiner`/`blocks`/`final_layer` ✓ ⇒ 行级前向出 **(视频行, 音频行)** ✓ 且**按参考取负** ✓；段长**从行数反推** ✓ 除不尽**报错** ✓ 不猜 ✗）；⚠️⚠️ 三个错**全是响亮的** ✓（这次无静默错 ✓✓）：① `TokenRefiner` 少 `final_eps` ✗（参考里是**独立**的 `final_norm_eps` ✓）⇒ `TypeError` ✓；② ⭐⭐ **同一份数据、两套编号空间** ✗✓ —— `mod_row` 给的是主干要的 **18 路**行号 ✓，而 `FinalLayer` 的 adaLN 是 **1 路**（行号=**唯一时间戳序号** ✓）⇒ 直接喂过去 ⇒ `IndexError: index 5 … size 3` ✓✓ ⇒ ⭐**跨模块传"行号/索引"必须区分编号空间** ✗（`row`/`mod_row` 两词太像 ✓）；③ 我自己的**魔法阈值** `TODO>=5` ✗ 因"只减不骗"降到 4 条而红 ✓ ⇒ 改判「**非空**」✓ ⇒ ⭐**别用魔法阈值，用非空 + 双向** ✗；⭐⭐ 自检最值钱那条 = **反套套逻辑** ✓✓（㉔′：改 σ ⇒ 输出必变 ✓ + 改文本 ⇒ 输出必变 ✓ —— 否则就是「**条件没接进去**」的假绿 ✗✗，本仓老毛病 ✓）；另 ㉓ 用 `state_dict` **键集合**对参考模块名 ✓、㉑ 钉 carry 不变量（shift 相同 ⇒ 恒 **1** ✓）；实测 h3_form **36/36** ✓ dit 43 ✓ core 32 ✓ **pipeline 98** ✓ text 21 ✓ io 39 ✓ **smoke 476/476** ✓ 漂移 0 ✓@676｜末节@703｜S7 第 118 步：**补上 H3 的「外壳」与「去噪掩码」** ✓ —— `h3_form` **36 → 43/43** ✓：外壳 `patchify_video`/`unpatchify_video`（潜帧 ⇄ 行 ✓ 行序**先帧后行列** ✓）+ `pack_audio`/`unpack_audio`（`[C,ch,T] ⇄ [ch·T,C]` ✓ **channel-major** ✓）⇒ 自检用 ⭐**往返恒等**钉住 ✓✓（`unpatchify(patchify(x)) == x` ✓ **最强的那种不变量** ✓）+ 逐段等于源 ✓ + 与 `packed_rows` 的 `video_rows` **交叉验证**同口径 ✓；`mask_row_values` ✓（**三态** ✓：全在生成 ⇒ **`None`** ✓ / 挖一个 2×2 洞 ⇒ **恰好一行 < 1** ✓ / 比潜尺寸小 ⇒ **replicate 补齐**后仍全 0 ✓）；⚠️ 两条**语义**讲究 ✓：① **`None` ≠ 全 1 张量** ✗（「**没有掩码**」与「**掩码全是 1**」必须分开 ✓ —— 本仓「没数据 ≠ 通过」的同族 ✓）② 行数必须与 `packed_rows` **同口径** ✓（两处算同一个数 ⇒ 最该钉 ✓）；有意取舍 ✓：本仓行级表示**无 batch 维** ✓ ⇒ 外壳只收单条 ✓（参考是把 batch 折进行数但内部只取第 0 条 ✓）；⚠️ 又一个**形状方向**记反 ✗✓（今天第 **7** 次同类：`audio_rows[:5]` 是 `[T,C]` ✓ vs `latent[:,0,:]` 是 `[C,T]` ✗ ⇒ `RuntimeError` ✓）⇒ 再次印证 ⭐**凡顺序/布局/形状/dtype 都当场算一遍** ✗；⚠️ 跑批时**命令被判后台** ✗ ⇒ 按规矩**没跑完不写"通过"** ✓（从落盘读回 ✓，冒烟与漂移**另跑一次**确认 ✓）；实测 h3_form **43/43** ✓ dit 43 ✓ core 32 ✓ **pipeline 98** ✓ io 39 ✓ segments 54 ✓ **smoke 476/476** ✓ 漂移 0 ✓；**H3 形态现状**：积木 ✓ + 打包 ✓ + 主干（行级前向 ✓）+ 外壳 ✓ + 掩码 ✓ ⇒ **TODO 只剩 3 条**（`cond`/`refs` 段 ✗ / 输出头 bank PDD ✗ / **接进管线**零调用 ✗）@714｜末节@742｜S7 第 119 步：⭐ **把 H3 形态接进后端** ✓ —— `h3_form` **43 → 47/47** ✓（**不再是零调用** ✓✓ 按本仓判据「没人调用的库不算功能」✓）：⭐⭐ 接线**前真实存在的静默错** ✗ —— `TorchBackend.load_weights` 原来**直接** `build_dit(config)` ✗ ⇒ 喂 H3 权重会被**静默按 DiT 建** ✓✗，表面症状只有「**装载报告缺一堆键**」✓ ⇒ 人极易读成「权重没下全」✗✗（实为**结构根本不是那个模型** ✗）；交付：`looks_like_h3_form` ✓（**双向**判据 ✓：招牌键 `video_patch_proj`/`audio_patch_proj`/`rope.inv_freq`/`final_layer.audio_out`/`token_refiner.blocks.0.attn.qkv_proj` 全在 ⇒ True ✓；**混进 DiT 招牌键 ⇒ False** ✓ 防误判 ✗；**只按文件名/大小猜都不行** ✗）+ `H3_TRUNK_DEFAULTS`/`H3_SIGMA_SHIFTS`（提到**模块级** ✓ **无 torch 也能读** ✓、**只有一处** ✓ `H3_DEFAULTS` 由它派生 ✓）+ `load_weights` **先判形态** ✓ ⇒ 按 `H3FormTrunk` 建并装载 ✓、`_form="h3-form"` ✓；⭐ 集成自检 ㉛ 走**真路** ✓（缩小主干 `save_file` → `TorchBackend().load_weights` → 断言 `_form`/`H3FormTrunk`/`complete=True` ✓）+ ㉜ 临时文件**已清理** ✓ —— **一次通过** ✓✓；⚠️ 老毛病**半角引号第 8 次** ✗（`PARTS` 里写 `"装载报告缺一堆键"` ⇒ `SyntaxError` ⇒ 复跑 `exit=1` **且没 SUMMARY** ✓ —— 正在「**没 SUMMARY ≠ 通过**」✓，若只看到 exit=1 就当"红几条"会白找 ✓；`compile()` 一行定位第 62 行 ✓）；⚠️ 有两次运行**输出被管道吞掉** ✗ ⇒ 按规矩**不当它通过** ✓、改**落盘再读** ✓ 才拿到确凿结果 ✓；⚠️ TODO 如实**收窄** ✓：「接管线」剩的是**音频流**那半 ✗（`GenerationBackend` 是**单流**协议 ✗ ⇒ 要同时扩 `init_latents`/`denoise`/`decode`/`write` ✓）；实测 h3_form **47/47** ✓ dit 43 ✓ **smoke 476/476** ✓ 漂移 0 ✓；**H3 现状**：积木 ✓ 打包 ✓ 主干 ✓ 外壳 ✓ 掩码 ✓ **后端可认可建** ✓ ⇒ TODO 剩 3 条（`cond`/`refs` ✗ / PDD 头库 ✗ / **音频流进 pipeline** ✗）@752｜末节@777｜S7 第 120 步：**音频流的第一块** ✓ —— ⭐ **把「没核过的地方」逼成必填参数** ✗✓：接着 TODO 最大那块（**音频流进 pipeline**）✓ 先落它前面的两个事实 ✓：**已核实**（来源 = H3 模型**文件头说明**「stereo audio (32ch, 40 Hz) latents」✓）⇒ `geometry.AUDIO_LATENT_HZ = 40` ✓ + `AUDIO_LATENT_CHANNELS = 2` ✓，⚠️ 顺带钉易混点 ✗：那个 **32 是特征维** ✓（`audio_latents_dim`）**不是声道数** ✗；**没核过** ✗ = 「秒数 → 音频潜帧数**怎么取整**」✓（参考里 `audio_t` 是**从调用方张量读**的 `audio_x.shape[-1]` ✓ ⇒ 怎么算**没核到** ✗；搜了一圈太宽泛没命中 ⇒ **找不到就不猜** ✓）⇒ 按**本模块自己的纪律**办 ✓（`geometry.py` 文件头写着「给个看起来合理的数只会**在真机上错得莫名其妙**」✓）⇒ `audio_latent_frames(seconds, *, mode, hz=40)` ✓ **mode 必填** ✓（`round|ceil|floor` ✓）不给就 `TypeError` ✓✅、一旦核清**只改这一处** ✓；⭐ **规则**：**「没核过」不许用默认值糊过去** ✗（好默认值会把「**未核实**」伪装成「**已实现**」✓✗ —— 逼成必填 ⇒ 错误只出现在**调用方**且**当场**出现 ✓✓）；实测 `engine_core` **32 → 34/34** ✓（⑮′ 音频事实 ✓ ⑮″ 三种取整 + **不给 mode 报错** ✓）h3_form 47 ✓ **smoke 476/476** ✓@788｜末节@809｜S7 第 121 步：**把整条链串起来** ✓ —— `denoise_step`（`h3_form` 47 → **50/50** ✓）：⚠️ 原计划要做 `audio_sigmas_from` ✗，但转念发现那又是**一个"预备函数"** ✗（连续第三次"没人调用"✗）⇒ 换成做**真把东西串起来**的 ✓ —— `denoise_step(trunk, video_latent, audio_latent, text_states, sigma_v, …)` ✓ 一次用掉 `patchify_video`/`pack_audio`/`H3FormTrunk`/`unpatchify_video`/`unpack_audio` ✓ ⇒ **潜帧进 ⇒ 两条 velocity 同形出** ✓（视频 `[C,T,H,W]` ✓ 音频 `[C,ch,T]` ✓）⇒ H3 从"一堆零件"变成"**一次去噪**" ✓✓（采样器按 σ 循环调即可 ✓）；⚠️ 输出 dtype **跟随输入** ✓（主干内部 fp32 ✓ ⇒ 转回来 ✓ 与参考同义 ✓）；⚠️ 潜帧与 patch **不整除 ⇒ 报错** ✓（不猜不补零 ✗）；⚠️ 双流 carry（`σ_a/σ_v`）**刻意不在这里** ✓（那是采样器的事 ✓ —— 因为"少做一次换算"**不会报错** ✗✗ 所以必须写清 ✓）；⚠️ 两个错都**响亮** ✓✓：① **半角引号第 9 次** ✗ ⇒ `SyntaxError` ✓；② ⭐ **`__all__` 加了但返回字典忘了加** ✗ ⇒ `KeyError: 'denoise_step'` ✓✓ ⇒ ⭐**惰性导出有两道门**（名单 ✓ + 返回字典 ✓）✗，两道都**响亮** ✓（若返回 None 会一路漂到采样才炸 ✓✗）；实测 h3_form **47 → 50/50** ✓（端到端 ㉝ ✓ + 端到端**反套套逻辑** ㉝′ ✓ + 不整除报错 ㉞ ✓）**smoke 476/476** ✓@814｜末节@828｜S7 第 122 步：**双流采样器** ✓ —— `h3_form` 50 → **55/55**（**一次过** ✓）：先取证 ✓（双流最容易"看起来对、数值错" ✗）⇒ 参考 `CONST.calculate_denoised = x − σ·v` ✓（与它**取负**的 velocity 自洽 ✓）⇒ 欧拉更新式 `x_{n+1} = x_n + (σ_{n+1} − σ_n)·v` ✓（等价于「先算 `x0 = x − σ·v` 再按新 σ 加噪」✓）；另核到**易混点** ✗✓：`ModelSamplingAV.audio_scale = shift/audio_shift` = **4.0** ✓ 是**常数标志** ✓，而逐步那个是 `σ_a/σ_v` ✓（`audio_carry` ✓）—— **混了不会报错** ✗✗；交付 `sample_dual_stream`（视频走 `sigmas` ✓ 音频走**逐步换算**的 σ ✓ + `callback(step, σ_v, σ_a)` ✓ + 返回两条流/步数/两条 σ 序列 ✓；末位非 0 或 <2 个 ⇒ **报错** ✓；⚠️**刻意不做 carry** ✗ ⇒ 每条流待在自己的 σ 上 ✓ 与 `denoise_step` 口径一致 ✓ —— 做与不做**都不报错** ✗✗ 所以写清 ✓）+ `audio_scale(shift_v, shift_a)` ✓；⭐⭐ 自检 ㊱ = **双流专属不变量**：**两条 shift 相同时音频 σ 与视频逐位相同** ✓（「换算 = 恒等」的证明 ✓）而 12/3 时**必须不同** ✓ 且音频仍**单调递减到 0** ✓ —— 比"跑通不报错"值钱得多 ✓（同时证明「换算真在用」✓ 与「没换错方向」✓）；⭐ 本轮**一次过** ✓（把上轮「惰性导出有**两道门**：`__all__` + 返回字典」当真 ✓ ⇒ **实现 + 名单 + 字典 + `PARTS` 四处一起改** ✓ ⇒ 没再 `KeyError` ✓✓，半角引号也**没犯** ✓）；实测 h3_form **50 → 55/55** ✓@838｜末节@866｜S7 第 123 步：**取证两个 VAE 的结构事实** ✓ —— 且 ⭐**故意不扩协议** ✗：原计划（第 122 步选项①）是"扩 `GenerationBackend` 为双流"✗，但动手前想清：音频路要 `decode` 就得有**音频 VAE** ✓，而我们连其**结构**都不知道 ✗ ⇒ **现在扩协议只会做出"看着接上了"的假接线** ✗✗ ⇒ 改成**取证** ✓；⭐⭐ 核出：**视频 VAE** = **3D 因果 CNN 编码 + ViT3D 解码** ✓（**不是** UNet VAE ✗）+ ⭐⭐ **`LATENTS_MEAN`/`LATENTS_STD` 各 24 个** ✓（**潜变量归一化统计** ✓ 24 正对 `latents_dim=24` ✓）+ ImageNet mean/std ✓ ⇒ ⚠️ **这组数字猜不出来** ✗（漏了它画面「看着还行、数值全错」✗✗ 且**不报错** ✗）；**音频 VAE** = **DAC 血统编码 + BigVGAN 解码** ✓（**与视频 VAE 毫无关系** ✗）+ **Snake/SnakeBeta** ✓ + 抗混叠 ✓ + ⭐ **weight-norm 已折叠**成普通 conv ✓（好加载 ✓）⇒ **`VideoVAE` 服务不了音频** ✗ ⇒ 音频侧要**单开** ✓（未做 ✗）；交付 `engine/vae.py`：两个 facts 字典 ✓（含 24 个数 + 「猜不出来」注记 ✓）+ `VideoVAEConfig.apply_latent_stats`（新 ✓；⚠️ 默认 False 的含义**只有一个** = **本模块合成结构不做归一化** ✓ —— **不是**「H3 不需要」✗，已核实**它就是需要** ✓）+ `normalize_latents`/`denormalize_latents` ✓（⚠️ 统计数与潜通道**对不上 ⇒ 报错** ✓ —— 凑一组「差不多」的统计只会得到**看着对的错图** ✗✗）；⚠️ 半角引号**第 10 次** ✗ ⇒ `SyntaxError` ✓（`compile()` 一行定位第 47 行 ✓）+ 一次命令**被判后台** ✗ ⇒ 按规矩**不当它通过** ✓、落盘重跑 ✓；⚠️ **如实说**：归一化这条路径**还没有落盘自检** ✗（本次用一次性命令验 ✓：roundtrip True ✓ / 24 24 ✓ / 关着时透明 ✓ / 数不对报错 ✓）⇒ 应补进 `engine_io_test` ✓（已列下一步 ✓ **不假装已进套件** ✗）；`engine_io` **39/39** ✓ **smoke 476/476** ✓@875｜末节@909｜S7 第 124 步：⭐⭐ **实现音频 VAE** ✓ —— **真功能**（能出**真 wav** ✓ `engine_audio_vae` **12/12** ✓，已登记 `run_all` ✓）：用户明确要求「**一定要实现真的功能**」✓ ⇒ 不停在事实与编排 ✗，**真把模型写出来** ✓（`engine/audio_vae.py` ✓ 自己实现 ✓）；⭐⭐ **先取证救了一次大错** ✗✓：我原以为音频是 **48 kHz** ✗（"常见值"直觉 ✗）⇒ 事实是 **32000** ✓、编码 strides `(2,4,4,5,5)` ⇒ `hop = **800**` ✓ ⇒ `32000//800 = **40**` ✓ **正好对上** `geometry.AUDIO_LATENT_HZ = 40` ✓✓，解码 BigVGAN `(5,5,2,2,2,2,2)` 乘积**也是 800** ✓ ⇒ 编解码 hop 自洽 ✓（写成**构造期断言** ✓：不等就报错 ✓ —— 否则"解出来的时长"与"编进去的"对不上 ✓✗ 且**不报错** ✗）；交付：`AudioVAEConfig` ✓（全事实显式 ✓ + hop/decoder_hop/latents_per_second ✓）、`Snake1d`/`SnakeBeta` ✓（`x+(1/β)sin²(αx)` ✓ beta 存 log 域 ✓）、`ResBlock1`/`BigVGANDecoder` ✓（每 kernel 一组 dilations ✓ 输出夹 **[-1,1]** ✓）、`Encoder`/`AttnProjection` ✓（⚠️ 参考那层内部**没读** ✗ ⇒ 写明**不等价** ✗）、`decode`/`encode` ✓（`[B,32,2,T] ⇄ [B,2,L]` ✓ `L=T×800` ✓ 非整数倍**右侧补零** ✓）、`write_wav` ✓（真 16-bit PCM ✓ 标准库 ✓ 超范围**夹紧**不回绕 ✓）；⭐⭐ "真功能"的证明：③ 形状契约 ✓ + **[-1,1]** ✓；④ 补零 `T=ceil(18/4)=5` ✓；⑤ 统计没给⇒**恒等**（`is` ✓）/给了⇒**真用上**（`std=2` 解码必变 ✓）；⭐⭐⑥ **真写 wav + 标准库读回**（2 声道 ✓ / 采样率 ✓ / 20 帧 ✓ / 16-bit ✓）+ ⑥′ 用完清理 ✓；⚠️ 三个结构错**全响亮** ✓✓：① `kernels × dilations` 当成**叉乘** ✗ ⇒ 元组 `//2` `TypeError` ✓（正解：每个 kernel 配一组 ✓）② `Encoder` 照抄"先翻倍再建块" ✗ ⇒ `4 vs 8` ✓（改成自洽 ✓）③ 自检里写过**套套逻辑**比较 ✗✓（自己和自己比 ✓）⇒ 自审改成"给统计 vs 不给统计**必须不同**" ✓@922｜S7 第 125 步：⭐ **把 H3 双流接进管线** ✓（`engine_dual_stream` **39/39** ✓ + `engine_io_test` 补归一化落盘自检 **48/48** ✓）—— 先还第 123 步欠账 ✓（归一化：逐通道手算对照 ✓/往返恒等 ✓/常量不动点（`x=mean⇒0`✓、`x=mean+std⇒1`✓）/**改数必变**（反套套 ✓）/数不对报错 ✓ —— **一次过** ✓），再落 `H3_FORM_TODO` 最大那条 ✓：`TorchBackend.dualStream`（**逐条报缺什么** ✓：形态/音频 VAE/取整口径 ✓）+ `attach_audio_vae`/`init_dual_latents`（视频 `[C,T,H,W]` + 音频 `[C,2,T_a]` ✓ **无 batch** ✓ 共用 generator ⇒ 逐位可复现 ✓）/`sample_dual`（**直接调 `h3_form.sample_dual_stream`** ✓ 判据只有一份 ✓）/`_decode_dual`（两个 VAE 各管一条 ✓）/`write`（mp4 + wav **各落一份** ✓）+ `pipeline` **按后端自述分流** ✓（单流默认路径**一字未动** ✓）+ 三条**当场拒绝** ✓（CFG ✓/首帧 ✓/heun ✓ —— 事实：H3 **无 CFG** ✓、循环是**一阶欧拉** ✓、`cond` 段没做 ✓）；⭐⭐ 四条**真 bug**：①**config 两种形态**（H3 是 **dict** ✓ DiT 是 dataclass ✓）⇒ `describe()` 里 `.to_dict()` 直接崩 ✓✗（`/engine/backends` **就是调它** ⇒ 装了 H3 权重后端 **500** ✓✗）+ `condition_width`/`attach_text_encoder` 在 dict 上**悄悄回落 64** ✗✗ ⇒ 收口 `_configured_text_dim` ✓；②条件 **dict 被当文本状态** ⇒ `'dict' object has no attribute 'shape'` ✓（**响亮** ✓ 走运 ✓）⇒ 取 `positive` ✓ + 带 negative **明确拒绝** ✓；③**两条流 batch 处理不同** ✓✓（`write_video` 要 **5 维** ✓ / `write_wav` 要 `(channels,N)` ✓）⇒ 视频也去维 ⇒ `write` **认不出帧张量 ⇒ 默默走成「张量清单 JSON」** ✗✗；④音频 VAE 每级**通道减半** ⇒ `decoder_dim < 2^级数` 造出 `in_channels=0` 的**非法卷积** ✓✗ 而构造期不报 ✗ ⇒ 报的是**一句 oneDNN 反卷积消息** ✓✗ ⇒ **构造期钉住** ✓ + 补「能触发」的反向证明 ✓；⭐ **别把「理想值」当事实** ✗（wav 帧数 6400 是理想 ✓ 实测 **6208** ✓ = `ConvTranspose1d` 带 padding 每级少 1 样本 ✓ **结构使然** ✓）⇒ 改成「盘上读数 vs 写盘回执**逐位相等**」✓；⚠️ **跑批工具坑**：`*>>` 写 **UTF-16** ✗ 叠加 `Add-Content -Encoding UTF8` ⇒ **混编码** ⇒ `Select-String` **0 命中** ✓✗（"一个 FAIL 都没有" ✓）、子进程 stdout 默认 GBK ✗ ⇒ `smoke_test` 因 `UnicodeEncodeError('✓')` 退 1 ✓✗（**看起来失败、其实只是打印炸** ✓）⇒ 跑批必须 `PYTHONIOENCODING=utf-8` ✓ + 脚本内 UTF-8 落盘 ✓；实测引擎 **12 套全绿** ✓（core 34/dit 43/mappings 20/h3_form **55**/pipeline 98/io **48**/audio_vae **13**/**dual 39**/text 21/loader 28/inventory 34/segments 54 ✓）**冒烟 476/476** ✓✓ `layering` 6/6 ✓ `read_json_guard` 10/10 ✓ `route_parity` exit 0 ✓；`H3_FORM_TODO` **只剩 2 条** ✓（cond/refs 段 ✓ PDD 头库 ✓）；双流**默认不启用** ✓（要 `attach_audio_vae()` **且**显式 `audio_latent_mode` ✓ —— 「秒数→音频潜帧数」的取整口径**没核过** ✗ ⇒ 逼成**必填** ✓）@967｜S7 第 126 步：⭐⭐ **修两处静默错** ✓ + **cond/refs 打包层** ✓（`engine_h3_form` **60/60** ✓）—— 用户「继续」✓ ⇒ 按上一步计划做 cond/refs ✓，但**先取证**（读参考 `PackedLayout` ✓）时发现本仓两处**一直在静默错**的口径 ✓✗ ⇒ 先修 ✓：①**模态标签三档全错** ✗（事实 = **video/cond/ref_img=0** ✓、**text=1** ✓、**audio 系=2** ✓；初版写「text=0/audio=1/video=2」✗ —— 标签只是 adaLN **行内偏移** ✓ ⇒ 真权重上每个模态接**别的模态**那组参数 ✓ 而**不报错** ✗）；②**text 的 t 档错** ✗（事实 = 与 video **同档** `1−σ_v` ✓；初版钉 0.999 ✗ —— 那是**条件行**的档 ✓，初版自注「未逐条核过」✓✗ 现删 ✓）+ 唯一时间戳改 `sorted(set())` ✓（⚠️ 老实说：旧口径「首次出现顺序」在现有段类下与升序**恰好等价** ✓ —— 我一度以为「正好相反」✗ **没验证就不写进代码** ✓，换口径只为与参考同构 ✓）；交付 `packed_layout` ✓（**段表+坐标+更新掩码一处算全** ✓，`packed_rows`/`position_ids` 委托它 ✓ ⇒ 不可能「段表与坐标不同序」✓✗）+ `audio_grid`/`video_grid`/`ref_time_span`/`video_t_spans` ✓ + forward 收 `keyframes`/`refs`/`latent_t`/`audio_t` ✓（⚠️ 带条件块**段长推不出来** ✗ ⇒ 必填 ✓ + 按布局总行数**反向校验** ✓）；核清规则：段序 `text→[cond/cond_audio]*→[ref_img/ref_audio]*→audio→video` ✓（**目标两条流永远在最后** ✓）、`cond` 用**目标网格**且 `t=cursor+FRAME_RESCALE×帧号` ✓、`ref_img`（图像）**整块共用一个 t** ✓ 且每块 cursor **+1.0** ✓ 用**自己**的网格 ✓（两种块**不一样** ✓✗）、`ref_audio` 的 w 取**目标**两端 ✓ 而 `video` 类块内音频行取**自己**两端 ✓ 且排**视频行之前** ✓、更新掩码**只有两条** ✓ 且 **text 行两条都不进** ✓；实测 **60/60** ✓ + `dual 39/39` ✓ + **冒烟 476/476** ✓；⚠️ **「能触发」的反向证明** ✓（改前两断言**当场红** ✓：㉒ 给出 `text→1/audio→5/video→0` ✓、⑳ 给出 4 行 ✓）；状态：`H3_FORM_TODO` 第 1 条改精确 ✓（「**打包层有、调用方没有**」✓ —— 后端还没把首帧编成行 ✓、`denoise_step` 还没收额外行 ✓）⇒ 双流仍**明确拒绝首帧** ✓（诚实 ✓）；下一步 = 后端侧接线解锁首帧 ✓ @1016｜S7 第 127 步：⭐⭐ **首帧条件接进双流** ✓（`engine_dual_stream` **40/40** ✓ —— fl2va 那条链通了一半 ✓）—— 用户「继续实现」✓ ⇒ 做上一步写下的「后端侧接线」✓：`TorchBackend._dual_keyframes` ✓（首帧 PNG → `load_image_tensor(width=plan.width, height=plan.height)` ⚠️ **按目标尺寸缩放进编码** ✓（`cond` 用**目标空间网格** ✓ 事实 ✓）→ 参考视频 VAE `encode` ✓ → `keyframes=[{resolved_frame_index: 0, latent, audio_latent: None}]` ✓）、`sample_dual` ✓（条件块 `patchify_video`/`pack_audio` 后**前插**目标行之前 ✓）、`denoise_step`/`sample_dual_stream` ✓ 新增 `extra_video_rows`/`extra_audio_rows`/`keyframes`/`refs` 透传 ✓、`pipeline` ✓ 双流+首帧**不再拒绝** ✓（改查后端自述 `dualStream.firstFrame` ✓，没自述就**仍明确拒绝** ✗；走通则记 `conditioning.mode="h3-keyframe-cond"` ✓）；⭐⭐ **两个当场被抓住的错**（都是**校验救的** ✓）：①**只传行不传结构** ✗ ⇒ `forward` 算 layout 时 `keyframes=None` ⇒ **总行数当场红** ✓（收到 294 vs 布局 245 ✓）；②**照抄参考维数** ✗ ⇒ 参考取 `latent.shape[2]` 当帧数是因它是 **5 维**（`[B,C,T,H,W]` ✓），本仓是 **4 维**（`[C,T,h,w]` ✓ 无 batch ✓）⇒ 取到 **h=14** ✗ ⇒ cond 段长算成 **686** ✗ ⇒ 又被**总行数校验**抓住 ✓（`收到 294 / 布局要 931` ✓）⇒ 现**显式校验维数** ✓；实测 **40/40** ✓（新增 ㉜ 跑通且标注 `h3-keyframe-cond` ✓、㉜′ ⭐⭐ **反套套**：给首帧 vs 不给 ⇒ **wav 字节必不同** ✓ = "条件真接进去了"唯一算得上的证明 ✓）+ `h3_form 60/60` ✓ + **冒烟 476/476** ✓；状态：`H3_FORM_TODO` **只剩 1 条** ✓（`refs` 打包层有 ✓ 无调用方 ✗ + **多关键帧**明确拒绝 ✓）@1056｜S7 第 128 步：⭐ **`refs`（参考图）也接进双流** ✓（`engine_dual_stream` **42/42** ✓）—— `TorchBackend._dual_references` ✓（`reference_frames` → 逐张 `load_image_tensor`（按 plan 尺寸 ✓）→ 视频 VAE `encode` ✓ → `refs=[{kind:"image", latent, latent_h, latent_w}]` ✓，⚠️ **用图片自己的网格** ✓ —— 与 `cond` 用**目标**网格**不同** ✓ 事实 ✓）；⭐ **行序责任收敛到一处** ✓：`_dual_extra_rows` ✓ 按 `packed_layout` 段序拼 ✓（**先关键帧、后参考块** ✓）—— 上轮是"按类型分组"且**只敢收一块** ✗（多块行序会错而**不报错** ✓✗）⇒ 顺手**解掉**「多关键帧明确拒绝」✓；管线 ✓：双流 + `referenceFrames` 而后端**没自述** `references` ⇒ **明确拒绝** ✗（否则 `init_dual_latents` 会**静默忽略**参考图 ✓✗）⇒ 自述项 `dualStream["references"]` ✓（**只有图片**这一类 ✓，参考音频/视频没入口 ⇒ 不声明 ✓ 诚实 ✓）；实测 **42/42** ✓（新增 ㊴ 跑通 ✓、㊵ ⭐ **反套套**：给参考图 vs 不给 ⇒ **wav 字节必不同** ✓）+ `h3_form 60/60` ✓ + **冒烟 476/476** ✓；⚠️ **新记一条账** ✓（`H3_FORM_TODO` 第 3 条）：**「行序正确性」没有独立判据** ✗ —— `forward` 只校验**总行数** ✓ ⇒ "同数不同序"全靠**两处代码人工对齐** ✓✗（㊵ 只证明"参考进去了" ✓ **不证明顺序对** ✗）⇒ 要更强得让 `packed_layout` **直接产出行序** ✓ @1087｜S7 第 129 步：⭐⭐ **行序判据落地**（**布局说了算** ✓ 不再是两处人工对齐 ✓）—— 上一步自己记的账这轮还掉 ✓：`packed_layout` 新增**块清单** ✓（`video_blocks`/`audio_blocks` = `[((块标识, 序号), 行数), …]` ✓，标识 = `("keyframe", i)`/`("ref", i)`/`("target", 0)` ✓ ⇒ **行序的权威来源** ✓；实现走"进循环就改 `current` ✓、`push` 顺手记账 ✓"（**零改动**各调用点 ✓）并在进目标段前**显式重置** `current` ✓ —— 不然目标两条流会被记到最后一个块名下 ✓✗ 错得不响 ✓）+ `assemble_blocks(layout, video_sources, audio_sources)` ✓（**照块清单取** ✓ 顺序不问调用方 ✓；缺块/行数不符 ⇒ **报错** ✓）+ `_dual_extra_rows` **改用它** ✓（自己算一遍 layout ✓ 同一个纯函数 ⇒ 参数一致就一致 ✓）⇒ "先关键帧后参考块"的人工对齐**删掉** ✓；实测 `h3_form` **63/63** ✓（㊶ 块清单与段表**对账** ✓、㊷ ⭐⭐ **判据「能失败」**：两个块 sources **对调** ⇒ 拼出行**必变** ✓、㊸ 缺块报错 ✓）+ `dual 42/42` ✓（后端已改用它 ✓ 端到端仍通 ✓）+ **冒烟 476/476** ✓；⚠️ 顺手抓到真错 ✗✓：只改函数体、**没改签名** ⇒ `takes 2 positional arguments but 3 were given` ✓（靠**端到端跑**才现形 ✓）；`H3_FORM_TODO` 现 **2 条** ✓（参考音频/视频无入口 ✓ PDD 头库 ✓）@1106｜S7 第 130 步：**PDD 头库取证** ✓（**只取证、没实现** ✗ —— 事实先固化 ✓）—— 挑 TODO 里价值最高那条（PDD 头库 = **真权重能否装上的阻塞项** ✓）动手 ✓ 但**先读参考**（`FinalLayer.forward` + `_pdd_head` ✓）⇒ 事实全核清 ✓：`n = weight.shape[0]//out_features` ✓（`n==1` ⇒ 普通头 ✓ **本仓现状就是对的** ✓）；`n>1` ⇒ ① 按**整条日程**定位当前 σ（`i = argmin|sample_sigmas − σ|` ✓、`σ_next = sample_sigmas[i+1]` ✓，**没日程就报错** ✓）；② `start,stop = round((1 − time_shift_sigma(s, shift_v, **1.0**)) × n)` ✓ ⚠️⚠️ **第三参是 1.0** ✗ —— 与 `audio_carry` 的 `(shift_v, shift_a)` **不是一回事** ✓（**混了不报错** ✓✗ 又一处同族静默错候选 ✓）+ `start=min(start,n−1)` ✓ `stop=max(stop,start+1)` ✓；③ 头权重重塑 `(n,−1,in)` ✓、**第 0 块完整头** ✓、**后续块是偏移** ✓，按 `dt`（`linspace(1,0,n+1)` 经 `flow_shift` 形变后差分 ✓）加权和 ✓（音频头用 `shifts[1]` ✓，①②定位两条流**共用** ✓）；⚠️ 真权重里 `n` 是几 **还没核到** ✗（不猜 ✓）；⇒ 本轮**只把事实写进 `H3_FORM_TODO`** ✓（含"落地要动 **6 处**"✓：`FinalLayer`（`head_banks` + 三入参）✓、`pdd_head` ✓、`H3FormTrunk.forward` ✓、`denoise_step` ✓、`sample_dual_stream` ✓、`torch_backend.sample_dual` ✓）—— **没写实现** ✗ 也**没假装写完** ✓（改 6 处要动前向签名链 ✓，上下文不足以保证"改完还全绿" ⇒ 与其改一半不如事实固化 + 下一轮一次做完 ✓）；实测 `h3_form` **63/63** ✓ 未受影响 ✓ @1127｜S7 第 131 步：⭐ **PDD 头库实现** —— ⚠️ **未全绿** ✗（如实记 ✓）—— 用户「继续实现、按参考、独立实现」✓ ⇒ 落地 PDD ✓：`h3_form.pdd_head` ✓（**自写** ✓：`linspace(1,0,n+1)` 经 `flow_shift` 形变→差分→加权和 ✓、第 0 块为基 ✓ 后续块是偏移 ✓）+ `FinalLayer`（`head_banks` ✓ + `sigma`/`sample_sigmas`/`shifts` 三入参 ✓、`n==1` 走老路 ✓、`n>1` 缺日程 ⇒ **报错** ✓）+ `H3FormTrunk(head_banks=…)` ✓ + `denoise_step`/`sample_dual_stream` 递**整条**视频 σ 日程 ✓ + `__all__`/惰性字典 ✓；⭐⭐ **两个真教训**：①**`out_features` 陷阱** ✗✓ —— 参考的 `n = 行数 // out_features` ✓ 是因为**它构造时给的是单头宽度** ✓，本仓照 `nn.Linear(hidden, vd*banks)` 默认值 ⇒ `n` **恒为 1** ✓✗ ⇒ **PDD 静默失效** ✓（不报错 ✓ 只是头库白装 ✓）⇒ 现显式改回单头宽度 ✓；②⚠️ **工具层** —— `str_replace` 报"成功"但我插的 5 行缩进是 **20 空格** ✗ ⇒ `IndentationError` ✓，而"按空格数替换"**0 命中** ✗（真实缩进不是 20 ✓）⇒ 改按**锚点行重排** ✓ 但脚本先命中 `FinalLayer` 里**同名**那行 ✓ ⇒ 把它的 `if` 块**压平**了 ✗（二次修订才修回 ✓）⇒ **教训：同名锚点多次出现时 `next(...)` 是错的** ✓（取最后一次或用行区间 ✓）；**状态（如实 ✓）**：`h3_form` 最后一次实测 **66/67** ✗（唯一红 = ㊺「PDD 能失败」✗）⇒ 已改成**不依赖传参链**的插值单元判据（㊺ span 不同 ⇒ 插值必不同 ✓、㊺′ span 只含第 0 块 ⇒ **恰好等于第 0 块本身** ✓）⚠️ **改完没跑过** ✗（末尾几次跑批被判后台、产物未落盘 ✗）⇒ **不能声称全绿** ✗；`dual 42/42` ✓ + 冒烟 **476/476** ✓（均为改 ㊺ **之前**的实测 ✓）；⚠️ **另记一账** ✓：**「主干把 σ 日程递到 `final_layer`」那条链本轮没验证过** ✗（㊺ 已绕开 ✓）⇒ 下一轮先跑套件确认绿 ✓ 再补那条链判据 ✓ @1147｜S7 第 132 步：✅ **PDD 全绿 + 整链判据补齐**（`engine_h3_form` **70/70** ✓）—— 上一步留的两条账都还了 ✓（先跑绿 ✓ 再补整链判据 ✓）：⚠️ **先说清**：上一步 66/67 **不是实现错** ✗ —— 是我新写的**用例自己语法错**（中文文案嵌半角 `"` ✓✗ **第 3 次犯** ✓）⇒ 修掉后直接 **68/68** ✓，PDD 三条判据（㊺ 插值「能失败」✓、㊺′ span 只含第 0 块 ⇒ **恰好等于第 0 块本身** ✓、㊻ 缺日程报错 ✓）**本来就是对的** ✓；补 **㊽ 整链判据** ✓（σ 日程经 `H3FormTrunk.forward` 递到 `FinalLayer` ⇒ 换日程 ⇒ 输出必变 ✓）+ ㊽′（整链上缺日程也报错 ✓）；⭐⭐ **最大教训**：㊽ **连红两版** ✗，两版都是**我先猜方向** ✓✗（第一版 `banks=3` 且以为「σ 小 ⇒ start 小」✗；第二版 banks 提到 64 但**方向照旧猜错** ✗✗）⇒ 第三版才去**打印取证** ✓：`1 − time_shift_sigma(σ, 12, 1.0)` 随 σ **增而减** ✓ ⇒ **σ 大 ⇒ start 小** ✓，σ ≤ 0.05 时它 ≈ 1 ⇒ `start` 一律被 `min(start, banks−1)` **夹到末段** ✓✗ ⇒ 两日程算成**同一段** ⇒ 输出相同 ✓；改 σ=0.9 后**一次通过** ✓ ⇒ **又一次「先取证再动手」的学费** ✓（与「外部工具真实输出先落盘」同源 ✓）；实测 **70/70** ✓ + `dual 42/42` ✓ + **冒烟 476/476** ✓；`H3_FORM_TODO` 仍 **2 条** ✓（参考音频/视频无入口 ✓；PDD = **机制已验证** ✓ 但**真权重里 `n` 还没核到** ✗ ⇒ `H3_TRUNK_DEFAULTS` 未写 `head_banks` ✓，拿到真权重按元数据补 ✓）@1172｜S7 第 133 步：⭐⭐ **头库大小从权重推断** ✓ + **语法预检守卫** ✓（`dual` **46/46** ✓）—— ①PDD 的「怎么知道 `n` 是几」落地 ✓：`head_banks_from_shape(shape, single_dim)` ✓（**模块级纯函数** ✓ 不需 torch ✓）+ `H3_VIDEO_OUT_KEY` ✓（`final_layer.video_out.weight` ✓ —— 模块名与参考逐字对齐 ⇒ 键名一致 ✓）+ `video_patch_dim()` ✓；`load_weights` 的 H3 分支 ⇒ **从权重形状推断** `head_banks` ✓ ⇒ ⭐ 真权重**不给**这个数 ✓ 既不猜也不硬编 ✓（默认 1 装 banks>1 ⇒ 形状不符 ✗；写死 ⇒ 换检查点就错 ✓✗），显式与推断**不一致** ⇒ **报错** ✓ ⇒ **真权重 n>1 现在能自动装上了** ✓（自检 4 条 ✓：推断出 3 ✓/不一致报错 ✓/非整数倍报错 ✓/单头宽度 = 24×4=96 ✓）；②⭐⭐ **语法预检守卫**（一天栽 **4 次** ✗）：`run_all.syntax_problems(root)` ✓ + `main()` 开跑前先跑 ✓ —— 起因：中文文案嵌**半角双引号** ⇒ `SyntaxError` ✓✗，其表现是「**套件连一行都不输出**」✓ ⇒ 看起来像"被判后台/输出被吞" ✓✗（两轮都误判过 ✓）；⚠️ **「能触发」证明** ✓：造坏文件 ⇒ 当场点名 `tests\_syntax_probe.py:1 unterminated string literal` ✓，删掉 ⇒ 全过 ✓；③顺带发现（**没修** ✗）：`app/services/color_grade.py:32` 有 `SyntaxWarning: invalid escape sequence '\*'` ✗（**早已存在** ✓ 非我改出 ✓ ⇒ 将来 Python 会变错误 ✓，建议改 `\\*` 或 raw ✓）；实测 `dual` **46/46** ✓（42→+4 ✓）+ `h3_form` **70/70** ✓ + **冒烟 476/476** ✓ + `run_all` AST OK ✓ + 预检基线全过 ✓ @1196｜S7 第 134 步：⭐ **参考音频入口打通** ✓（`dual` **50/50** ✓ —— `H3_FORM_TODO` 只剩 **1 条** ✓）—— `media.load_wav_tensor` ✓（**自己实现** ✓ 标准库 `wave` ✓ 零依赖 ✓）：wav → `(channels, N)` ✓ 值域 [-1,1] ✓；⚠️ 与 `write_wav` **成对** ✓；采样率/声道不符 ⇒ **报错** ✓（**不重采样、不混声道** ✗ —— 那会**悄悄改掉素材本身** ✓✗）；位宽非 2 字节 ⇒ 报错 ✓（不猜格式 ✗）+ `GenerationRequest.reference_audio` ✓（wav 路径元组 ✓）+ `_dual_references` ✓ 加音频分支（`load_wav_tensor` → **音频 VAE** 编码 ✓ ⚠️ 走**另一族** VAE ✗ 视频 VAE 顶替不了 ✓ → `{"kind":"audio", ref_audio_t}` ✓）+ `_dual_extra_rows` ✓（图片进**视频行** ✓、音频进**音频行** ✓ —— 两条流**同名字段** `latent` ✓ 别混 ✗）+ `dualStream` 自述**拆两项** ✓（`referencesImage`/`referencesAudio` ✓ —— 合成一个 bool 就分不清"缺哪类" ✓✗），pipeline 分别校验 ✓（没自述就**明确拒绝** ✗）；实测 `dual` **50/50** ✓（46→+4：自己写的 wav 读 ✓/不符即报错 ✓/跑通 ✓/⭐ **反套套**：给参考音频 vs 不给 ⇒ **wav 字节必不同** ✓）+ `h3_form` **70/70** ✓ + **冒烟 476/476** ✓；⚠️ `media.py` 是共用件 ⇒ 本想再跑 `engine_io_test` 复核 ✗ 但那两次跑批**都被判后台、没拿到结果** ✗ ⇒ **本轮未复核** ✓（如实记 ✓）；⚠️ 核实：`frontend/app/types/contracts.ts` 里**没有** `referenceFrames` ✗ ⇒ 两字段前端**未镜像** ✓ ⇒ 本次**无需**同步 ✓；状态：`H3_FORM_TODO` **只剩 1 条** ✓（参考**视频**块无入口 ✓）；PDD 那条**已移除** ✓（机制已验证 ✓ 且 `n` 现在**从权重自动推断** ✓）@1221｜S7 第 135 步（参考视频入口打通）@1246｜末节@1341

**`2026-09-18.md`**（7.7k / 109 行，S7：参考项目移植（提示词契约 / 资产验收门 / 连续性表））
S7 第 83 步：**参考项目移植第二批** ✓（同源 `short-drama-agent` 的 `asset-to-video-pipeline.md`）：**资产清单 + 验收门** ✓（`asset_manifest.py`：清单校验 ✓ + ⭐**依赖拓扑/成环点名** ✓ + ⭐**逐镜阻断 + `canGenerateVideo`** ✓ + 生成顺序 ✓）+ `/assets/manifest/validate`、`/assets/gate/check` ✓；⭐ 三条写死语义：**`generated` 不算验收通过** ✗（「生成」≠「人验收」✓）/ `approved` 只能人给 ✓ / `ui_plate` 留给后期叠字 ✓；⚠️ 自检抓到**真 bug** ✗✓（`BLOCKING` 内写下划线、而归一化输出连字符 ⇒ **永不相等** ✗ ⇒ 「待重做」的资产被排除出生成顺序 ✗ —— 而那个顺序恰恰就是返工清单 ✓）⇒ ⭐**规则：归一化产物与判断常量必须同源；断言钉后果、不钉计数** ✓；31/31 ✓；套件 86 ✓；245 已注册 / 漂移 0 ✓@5｜S7 第 84 步：**参考项目移植第三批** ✓（同源最后一块 = `production-plan-contract.md` **§6-§11**）：**连续性表** ✓（`continuity.py` 37/37 ✓）—— ⭐ 六张表体检：§6 **越轴** ✗ / §7 角色连续性卡必填 ✓ / §8 ⭐⭐**道具状态时间线**（刀掉地上再回手里 ⇒ **必须有捡的镜头** ✓✓）/ §9 ⭐⭐**线索不许提前暴露** ✓✓ / §10 ⭐⭐**动作必须"做成点什么"**（五类作用全无 ⇒ **可删或可并** ✓ 不阻断 ✓）/ §11 ⭐⭐**永远不许只写「切到下一镜」** ✓✓；`/continuity/check` ✓ + `/continuity/rules`（**把判据暴露给前端** ✓）；⚠️ 我自己**先看出**一个坑 ✓：跨镜方向记忆是**模块级**的 ⇒ 初版**忘了每次清零** ✗ ⇒ 上一部剧的方向会污染下一部 ✗✓ ⇒ 已清零 + 自检 ㉜「两次调用互不影响」✓；⚠️ 另 8 条红里 **7 条是我测试期望写错** ✓（夹具默认补了合法转场动机 ⇒ 7 条非转场用例全被 §11 拦住 ✗；`actor` 不在场时该报更强的那条 ✓）⇒ 只有 1 条是实现该改（线索声明镜头不在计划里 ⇒ **判不了就说判不了** ✓ 降级预警 ✓）；37/37 ✓；247 已注册 / 漂移 0 ✓@61｜S7 第 85 步：**参考项目移植第四批** ✓ —— 先**判掉两件不该搬的** ✓（`ollama-python` 函数→schema **已有** ✗不搬；`ollama` 流式工具调用解析 ⇒ 本项目**没有那个调用方** ⇒ 搬了就是没人调的库 ✗ 不搬 ✓）；搬的是**真缺**的 ✓：**Agent 上下文预算** ✓（`app/agent/context_budget.py` 25/25 ✓）—— 估算（CJK/ASCII 分开 ✓ 不引 tokenizer ✗）+ ①**机械层**（老 tool 输出换占位 ✓ **零 LLM 调用** ✓）②**摘要层**（提示词要求**保结构** ✓ **不许编** ✗，**花不花这次调用由调用方决定** ✓）+ **接进真循环**（`runtime.py` 每步开头 ✓）；⭐⭐ 核心不变量：**tool 往返必须成对** ✗（删消息会留下没人回答的 tool_call ⇒ 下请求 400 ✗✗）⇒ 本模块**只换 content、从不删消息** ✓（自检 ⑨⑩ 钉条数不变 + 成对成立 ✓，⑫⑬ 还钉**检查器本身**能识破残缺 ✓）；⚠️⚠️ **新坑**：`compile()` 过了 ≠ 可用 ✗（我补 import 的锚点写错没生效 ✓，而 compile 照样通过 ✓ ⇒ 运行时第一步 NameError ✗✗）⇒ **规则：改了模块级引用还要 `import` 一次那个模块；compile 只证明语法、证明不了名字在** ✓；⚠️ route_parity 抓 1 条漂移（`json.dumps(indent=1)` 提示词 ✓，与 dryrun 同类判例）+ 白名单 ✓；10 个 agent 套件全绿 **253 项** ✓；套件 88 ✓@111｜S7 第 86 步：**开跑前体检** ✓（把四块串成**一次调用** ✓ 前端不必自己拼四个端点 ✗）：`production_preflight.py` + `/production/preflight` ✓ + `/preflight/schema` ✓（200 路由 ✓）25/25 ✓；**不产生新判据** ✗ —— 只调用那四块 ✓（自检 ④⑤⑥⑦ 钉「sections 与直接调用逐字段一致」✓✓ 防"又抄一遍判据"✗）；两条设计判断：**质感层问题只预警不阻断** ✓✓（约束不硬 ⇒ 画得差点 ✓ 不是接不上 ✗）+ §10 可删可并也只预警 ✓（删戏是创作决定 ✓）；⭐ `nextActions` **按成本排序** ✓✓（补映射 → 改分镜 → 最后才花钱 ✓ 自检 ⑮ 用 index 关系钉死 ✓）；⚠️⚠️ **抓到跨全仓约定**：`core/request_utils.read_json` **有意**把非法 JSON/非对象**归一成 `{}`** ✓（对齐 TS ✓ 让**业务校验**给 400 ✓ 且不升级成 500 ✓）⇒ **路由里 `isinstance(body, dict)` 是死代码** ✗✗（我在 engine/prompt_tools/asset_gate/continuity **每个**路由都写了 ✗）—— 无害 ✓ 但必须知道它是死的 ✗；**正解：判据写在业务层** ✓（"一段都没给 ⇒ 400" ✓）；**规则：先读会归一/兜底的工具函数，再决定校验写哪一层** ✓@165｜S7 第 87 步：**立守卫 + 清掉 9 处死代码** ✓ —— `tests/read_json_guard_test.py` ✓（**10/10** ✓，含**合同锚点**（`read_json` 必须仍保证 dict ✓）/ **正对照** / **负对照** / **扫描面非空** ✓）；清掉 9 处（我写的 8 处 engine 3/prompt_tools 2/asset_gate 2/continuity 1 ✓ + **`comfyui.py` 历史遗留 1 处** ✓）—— **行为保持** ✓（那些判断永远进不去 ✓）由九套复跑背书 ✓（**冒烟 476/476** ✓）；⚠️⚠️ **守卫自己返工一次**：正则版**两头都错** ✗（**误报**：我在 `preflight.py` **文档串**里写了这个写法来说明"不做它" ⇒ 被当违规 ✗；**漏报**：`prompt_tools.py` 有一处**形状略不同**的 ⇒ 匹配不到 ✗）⇒ 改成 **AST 认语义** ✓（注释/字符串不进 AST ✓、变量名与形状变体都跟得住 ✓）+ 删除前先 `ast.parse` 验证再写回 ✓；**规则：判"代码里有没有某种用法"要用 AST、不要用正则** ✓，且**负对照**与**扫描面非空**同样必要 ✓（只做正对照发现不了 ✓）；另：清理脚本从**错 cwd** 跑 ⇒ 输出"删除 0 处" ✓ ⇒ **脚本报 0 先怀疑扫描面** ✓；套件 90 ✓@205｜S7 第 88 步：**去接前端 ⇒ 挖出更值钱的结论** ✗✓（`storyboard_continuity.py` **16/16** ✓）：前端是 **Nuxt** ✓（`frontend/app/composables/useApi.ts` ✓ `api.post` 自动脱 `json.data` ✓）；分镜页 **2300+ 行** ✗ ⇒ 选 **~300 行**的剧集页 `pages/drama/[id]/index.vue` 作落点 ✓；⚠️⚠️ 写请求体时发现：**前端手里没有 plan/manifest** ✗（那是分镜表/资产表的东西 ✓）**而后端也组装不出来** ✗✗ —— 契约 §6-§11 要的字段（道具状态 §8 / 揭示顺序 §9 / 画面方向 §6 / 转场动机 §11 / 动作效果 §10 / 起止状态 ✗）**当前 `storyboards`/`scenes` 表里根本没有** ✗；⇒ 交付**不撒谎的适配器** ✓：①只映射**真实存在**的列 ✓ ②⭐⭐**绝不给缺列补默认值** ✗✗（补"状态没变"会让 §8 **假绿** ✓ 比不做还糟 ✓ 自检 ⑤⑥ 钉死 ✓）③`schemaGaps` 逐条报缺**带契约出处** ✓（= 要加什么列的可执行答案 ✓）；适配器**不判任何东西** ✗（判据仍只 `continuity.py` 一份 ✓）；**教训：接口该长什么样，往往在写调用方时才看清** ✓ ⇒ **能由 id 在服务端组装的别让调用方拼** ✓、**组装不出来的如实报缺** ✓；另：PowerShell 含 `[ ]` 的路径必须 `-LiteralPath` ✓；套件 91 ✓@244｜S7 第 89 步：⚠️⚠️ **上一步结论是错的** ✓✗ 纠错（22/22 ✓ **不加任何表** ✓）：`models.py` 里**早就有 `continuity_states`** ✓★（`episode_id/storyboard_id/scene_id/state_type/entity_key/state_value/constraints/meta` ✓）—— 它**正是**契约要的通用状态表 ✓ ⇒ **不需要加表** ✓（也没加 ✓ 冒烟白名单不动 ✓）；**为什么会错**：我只查了 `storyboards`/`scenes` 的**列** ✗ 就宣布"无处安放" ✗✗，**从没去找专用表** ✗ ⇒ **规则：凡"schema 里没有"的断言必须查过"专用表"再说；先找现成的家，再决定要不要盖房子** ✓；交付：`STATE_TYPES` **词汇表**（7 条 ✓ 每条标契约出处 ✓ —— 真正缺的是词汇约定 ✓ 不是存储 ✗）+ `CONTRACT_MAP`（六要素全有承载 ✓）+ ⭐⭐`coverage_report`（报**分母** ✓，"一镜都没有"要明说「**没判**」✓ —— 没有它 3/12 会全绿 ✓✗；`storyboard_props` 无状态 ⇒ 覆盖率里注明"**未知**，不是没变"✓）；⚠️ 自检又抓两处真问题：①`state_type=action` **没法表达"碰了哪件道具"** ✗ ⇒ 用 `meta` 的 `{"prop":"9"}` ✓；②状态行动作与分镜原文被当**两个**动作 ✗ ⇒ **状态行是权威、原文只兜底** ✓；22/22 ✓；套件 91 ✓@284｜S7 第 90 步：**按 `episodeId` 组装** ✓（`preflight_source.py` **17/17** ✓）—— 后端自己取数（分镜/场景/角色/道具 + `continuity_states` + `storyboard_props` ✓）→ 适配器 → 判定 ✓ ⇒ **前端一个调用** ✓；⭐ 又找到两个**现成的家** ✓（`storyboards.asset_status` 默认 `missing` ✓ = 验收状态不必新造 ✓ 但它是**逐镜**列 ⇒ 取「最早需要它的那一镜」并**写明取法** ✓；`storyboard_characters.costume` ✓ = §7 的逐镜角色 ✓）；⚠️⚠️ 最要紧一条：**没有分镜 ⇒ 绝不能给绿灯** ✗✗（纯函数"没给就跳过" ⇒ **不存在的集会看起来可开跑** ✓ 最坏的一类误导 ✓）⇒ 源层补阻断 `ready=False` +「无从体检 ≠ 通过」✓ + 行动项换成"先生成分镜"并撤掉"可以开跑" ✓；三条口径与既有模块一致 ✓：**`generated` 不算验收通过** ✗（`ASSET_STATUSES` 里只有 `approved` 为真 ✓）/ **认不出的状态按最保守当 missing 并报出** ✓✓（默认放行更坏 ✗）/ **`dependencies` 一律空要明说** ✓✓（空会被当成"无依赖"✓ 而真相是 schema 没这列 ✗）；`MANIFEST_GAPS` 列余下两缺口 ✓；冒烟 476/476 ✓；套件 93 ✓@328｜S7 第 91 步：**前端面板落地** ✓（链子接完）：`useApi.ts` 加 `productionAPI`（`preflightForEpisode(id)` ✓）/ `components/PreflightPanel.vue`（`ready` 灯 + **行动项按成本排序** + 阻断/建议带 stage + **数据与覆盖率**单独一区 ✓ `scoped` 样式 ✓）/ 剧集页挂上（原生 `<select>` **选集才跑** ✓ 不自动全跑 ✓）；★ 四条设计全来自后端结论 ✓：只给 `episodeId` ✓ / 必须显示 `nextActions` ✓ / ⭐⭐**原样显示「无从体检 ≠ 通过」不自己改写** ✗ / ⭐界面**必须能区分「没判」与「通过」** ✗；刻意**不猜** `BaseSelect` 的 props ✗（猜错=坏控件 ✗）⇒ 用原生 select ✓；⚠️⚠️ **本轮编译校验没跑成** ✗（三条命令连被判"后台" ✓；本想用 `@vue/compiler-sfc` 真编译两个改过的 SFC ✓）⇒ **结果未知** ✓，**下次先确认** `frontend/_sfc_check.mjs` 已删（清理与校验同条命令 ✓ 命令跑了 ⇒ 应已删 ✓）+ **补跑编译** ✓；**教训：命令被判后台时，有重定向就去读产物 ✓；没有重定向的就是真没结果 ✗ ⇒ 不要当它跑过了** ✓@365｜S7 第 92 步：**补跑前端编译校验** ✓（上一轮欠账还清）：`@vue/compiler-sfc` 真编译 `PreflightPanel.vue` + `drama/[id]/index.vue` ⇒ **全部通过** ✓✓（不再"未知" ✓）；前端调用点 ↔ 后端路由守卫 **5/5** ✓（调用点 **166→168** ✓ = 新增的两个 ✓、**0 未匹配** ✓、后端 249 条 `(method,path)` ✓）；**做法小结：凡"必须跑一次才知道结果"的校验，都写成「落盘脚本 + 重定向输出」** ✓（命令被判后台也不怕 ✓ 产物流在盘 ✓；**不重定向的校验一跳过就等于没做** ✗ 且极易被当成做过了 ✗）；⚠️ `MEMORY.md` 余量仅 23 ⇒ **下轮进内容前先下移 `TOPICS.md`** ✓@399｜S7 第 93 步：**记忆层维护** ✓ —— 按本仓约定先把 `MEMORY.md` **下移 `TOPICS.md`**（**8121 → 7685** ✓ 致命 1→0 ✓）再把第 82→92 步的规则入库：新增 `TOPICS.md` §自 MEMORY.md 下移（2026-09-18 第二次腾预算 ✓：`backend-py/` 迁移明细 + Skill 体系明细 + **新引擎与"事前省钱链"速查** ✓），`MEMORY.md` 只留**短红线 + 指针**；⭐ **入库 4 条真踩出来的规则**（§代码约定 ✓）：①**判"有没有某种写法"用 AST 别用正则** ✗（正则把文档串里的说明当代码 ✓ 又漏换写法的同类 ✓）⇒ 守卫三段：合同锚点 + **正/负对照** + 扫描面非空 ✓；②**归一化产物与判断常量必须同源** ✗（`-` vs `_` ⇒ 待重做资产被排除出生成顺序 ✓✗）⇒ 断言钉**后果** ✓；③**"schema 里没有"先找专用表** ✗（`continuity_states` 早就在 ✓）⇒ 先找现成的家 ✓；④**"没数据/没读到" ≠ 通过** ✗（空集会被读成绿灯 ✓✗）⇒ 外层补阻断 + 报覆盖率 ✓；守卫仍提示"余量仅 315 ⇒ 下轮先下移" ✓（是**提示**非致命 ✓）@421｜S7 第 94 步：**写入侧** ✓（`continuity_store.py` **18/18** ✓ —— **链子通了** ✓）：`PUT /production/continuity-states` ✓ + `GET …/continuity-states/vocabulary` ✓（回显词汇表 ✓ 251 路由 ✓）；⭐⭐ **抓到会毁数据的 bug**：初版顺序是「校验 → 归属检查 → **先删旧行** → 写合格行」✗ ⇒ `state_type` 填错时**把该镜原本的状态整片抹掉** ✗✗（自检 ⑩ 当场抓到 ✓）⇒ 修成 **「拒收」必须是「什么都不做」** ✓✓ **全有或全无（按镜）** ✓（**半套状态比没有更糟** ✗ 体检会拿残缺时间线当真 ✓）；四条写入语义：**词汇拒收而非静默忽略** ✓（报错回显可选值 ✓）/ **幂等**（同镜先删后写 ⇒ 行数不涨 ✓，涨了会让体检把旧行当另一状态 ⇒ 假阳性 ✗）/ **只动显式给出的镜** ✓ / ⭐⭐**归属校验**（镜不属于该集 ⇒ 一条都不写 ✗ 写坏别的集极难查 ✓）；⭐⭐**闭环 ⑫**：写完 ⇒ `run_episode_preflight` 真看得见（覆盖率 `0/N` → `§6 1/`、`§8 2/` ✓）且结论仍由判据给 ✓；⚠️ **边界如实**：只有 `action` 部分可自动推 ✓（`effects` 推不出 ✗），状态/方向/动机/起止/线索**无列可推** ✗ ⇒ **LLM 侧（分镜生成顺带输出）是下一步** ✓，本轮**不假装**已自动生成 ✗；⚠️ 测试**用临时数据根** ✓（写入测试真插行 ⇒ 绝不能写进 `data/drama.db` ✗）@448｜S7 第 95 步：**LLM 侧** ✓ —— ⚠️ **前提被纠正**：LLM 侧**早就存在** ✗（`storyboard_tools.py` 已有 `save_continuity_states` ✓，描述写着 *Replaces all previous states* ✓）⇒ 该做的是**对齐 + 修 bug** ✓：⭐⭐ **真 bug = 整集 `DELETE`** ✗✗（LLM 分批调用时**第二批把第一批的镜整片删掉、且无提示** ✓）⇒ 改成**按作用域替换** ✓（镜/场景/集级各一桶 ✓ 没提到的一行不碰 ✓），**显式空数组仍=明确清空本集** ✓（既有能力保留 ✓）；⚠️⚠️ 我**两次过度纠正** ✗✓（先拒了 `scene_id` 档 ✓ 又拒了"两 id 都空"的集级档 ✓ —— **都是既有能力** ✓，被测试拦住 ✓）⇒ ⭐**规则：改既有工具行为前先读它的测试** ✓✓（测试=既有能力清单 ✓）；**要修的是"部分列表静默清其余"这个 bug ✗，不是把能力整片删掉** ✗；⭐ 又一处**必须兼容的既存事实**：老 `state_type`（`scene_space`/`prop_state`/`character_pose` ✓）⇒ 加 `LEGACY_STATE_TYPES` **照收不拒** ✓（否则把既有数据判成非法 ✗）；⭐ 工具返回**回灌词汇表**给模型 ✓（`allowedStateTypes`/`vocabulary`/`problems` ✓ = 它的反馈通道 ✓）；另：我两处失误 ✓（`{**json_string}` ⇒ 它是**函数** ✗；集级写入写到 `with` **外面** ✗）；收据断言是整字典相等 ✗ ⇒ **有意扩展**（message/count 逐字不变 ✓）并在测试写明理由 ✓；33/33 ✓ + 18/18 ✓ + 22/22 ✓ + 25/25 ✓；251 已注册 / 漂移 0 ✓@497｜S7 第 96 步：**真数据最后一公里验收** ✓（`tmp/diag_real.py` **只读** ✓）：真库 `episodes=7 storyboards=46 scenes=4 **continuity_states=0**` ⇒ **链子从没被喂过真数据** ✓（此前全在合成数据上绿 ✓）；⭐ 真机体检（episode 1 / 20 镜）**报告可信** ✓✓：`ready=False` + **覆盖率全 `0/20` 且如实写「没判」** ✓（**没假装干净** ✓ = 覆盖率设计生效 ✓）+ manifest 6 资产 / 0 已验收并明说「**数据问题不是代码问题**」✓；⚠️⚠️ 但阻断**全是 `table-field`**（§6 `left/right/…` ✓ §7 `current_state` ✗）⇒ **不填就永远红** ✗（"永远报错的检查 = 噪声 ⇒ 迟早被无视"✓）—— 而这两个**正是** `scene_space`/`character_pose` 的**家** ✓✓ ⇒ 第 95 步只做了"**收**"没做"**读**" ✗ ⇒ **光收不读 = 没兼容** ✗（从"非法"变成"**合法但无效**"✓✗，更难查 ✓）⇒ 修：适配器把**场景级 `scene_space`** 回填 `locations[].{left,right,back,foreground,axis}` ✓、`character_pose` 回填 `characters[].current_state` ✓（+ 写进 notes ✓ 不做隐性换算 ✓）⇒ 22 → **25/25** ✓；真库结论**不变且正确** ✓（0 条状态 ⇒ 没东西可回填 ✓，但那些字段现在**有可写的家** ✓ ⇒ 写侧能写 ✓ 读侧会读 ✓ 体检说得清 ✓）；**规则：兼容一个老名字必须同时"读"它** ✗（同"没数据 ≠ 通过"一族 ✓）@545｜S7 第 97 步：**端到端证明「填齐了就是绿的」** ✓（`chain_e2e_test.py` **9/9** ✓ 临时库 ✓）：分镜 DB → **真工具** `save_continuity_states`（= LLM 那条路径 ✓）→ `run_episode_preflight` ⇒ 终于**证实**（此前只能推测 ✓）填齐六样 ⇒ **`ready=True`** ✓✓（覆盖率满格 ✓ 结构性误报清零 ✓ 门开着 ✓ 判定真跑过 ✓）；⭐⭐ 端到端抓到**真 bug**：`shots[].location` 打了前缀 `L5` ✓ 而 `locations[].id` 是裸 `5` ✗ ⇒ 判定器全不匹配 ⇒ **每镜都报 `unknown-location`** ✗✗（真机必然全红 ✗；合成套件因为断言写同一侧而**测不出来** ✗）⇒ 两侧统一用裸 id ✓；**规则（本会话第 2 次）：同一份数据的两侧写法必须同源** ✗（上次 `-`/`_` ✓ 这次 `L5`/`5` ✓）—— **且只有端到端能发现** ✓；⭐⭐ 同时抓到**我自己设计里的缺口**（写测试时发现 ✓）：六项覆盖率**全 0 时 `ready` 仍是 True** ✗✗（拿"没判"冒充"通过" ✓）⇒ 新增**结构化** `coverage_counts()`（与 `coverage_report` **同源** ✓ 判据只有一份 ✓）+ **`continuity-coverage` 阻断** ✓✓ + 把"先写状态"排到行动项**第一位** ✓（真机报告立刻可执行 ✓）；⑧⑨ 一正一反钉住 ✓；另 ⑥ 是**我期望写错** ✓（只链 1 个角色 ⇒ 资产 2 个 ✓）；套件 96 ✓@593｜S7 第 98 步：**全量复测（发起 ✓ 未跑完 ✗）** —— 已实测 **48 套 / 1971 项 / 0 失败** ✓（b1=24 套/1411 项 ✓、b2=24 套/560 项 ✓，含 **冒烟 476/476** ✓ = 今天 16 步改动没破坏它 ✓）；⚠️ **b3/b4 仍在后台跑** ✗ ⇒ **96 套总数仍未实测完整** ⇒ 文档继续写"总数待实测"**不推算** ✓（本会话因推算栽过两次 ✓）；收尾命令见日志 ✓：`tmp/batch_run.py 48 72 b3` → `72 96 b4` → `--sum` ✓（⚠️ `tmp/batch_run.py` 是**可复用工具** ✓ 别当垃圾删 ✓：分批 + **落盘** ✓ + 按文件名**去重**汇总 ✓ —— 命令被判"后台"也不丢证据 ✓ 本会话靠这招救过三次 ✓）@673｜S7 第 99 步：**全量复测收口** ✓（b3/b4 跑完 ✓）—— 实测 **94 套 / 3213 项 / 0 失败** ✓✓（分四批落盘 ✓ 汇总按文件名去重 ✓）；⭐ 同批还抓到**汇总脚本自己的坑** ✗：初版取 `split()[-1]` 当项数 ✓ 而那一列是 `passed` ✗ ⇒ **总数静默变成 0** ✓✗（"看起来跑完了 ✓ 一个数都没读到" ✓ = 与「没数据 ≠ 通过」同族 ✓）⇒ 改成从 `SUMMARY: a/b` 正则取 ✓ + **同时打分母**（盘上套数 / `run_all.TESTS` 条数 ✓）+ **点名读到 0 项的套件** ✓（⇒ 确认那 2 套 `route_parity`/`h3_backend_live` **本身通过** ✓ 只是**不打印 SUMMARY** ⇒ 项数未计入 ✓ 不是「0 项」✗）；⚠️ 改脚本时又犯「`compile()` 过了但 `re` 没 import」✗ ⇒ 第 85 步那条规则**当场再验证一次** ✓；⭐ 顺手改掉 `README.md` **三处陈旧总数** ✗（2911 项 ✗ / 八十三项 ✗ / 十六项 ✗ ⇒ 一律改**实测** 3213 / 九十四 ✓，并**删掉逐套罗列** ✓ 换成「权威清单在 `run_all.py`」✓ —— **逐套数字会随套件增减而腐烂** ✓）；**规则：规模数字要么指向唯一权威、要么带实测日期** ✓@630｜⚠️ **本节实际排在「第 98 步」之前** ✗（我锚在第 98 步的**最后一段** ✗ ⇒ 锚点前插 ✓）⇒ 按 `@行号` 读即可 ✓，勿按顺序读 ✗；S7 第 100 步：**修掉技术 QC 的「三项死检测」** ✓（`technical_qc.py` 42/42 ✓，原 31 ✓）—— README 挂很久的 🔴（冻帧/真峰/集成响度**永不生效** ✗）终于修 ✓✓：**先取证**（本机有 ffmpeg 9.0.1 ✓ ⇒ 实测输出落盘 `tmp/qc_probe/` ✓ 不靠猜 ✗）⇒ ⚠️ `freezedetect` 是**三行带前缀**（start→duration→end ✓）、且**冻到片尾时只打 freeze_start** ✗（初版对这类片**整段漏报** ✓）；⚠️ `ebur128` **有** summary 但值是**标题下一行**（`I:` / `Peak: dBFS` ✗）；⭐⭐ **新陷阱**：逐帧行的 `I:` 是**滚动累计值** ⇒ **首行恒 -70 LUFS** ✗ ⇒ 取首条会造出「永远 -70」的**假读数** ✓✗（比解析不出更坏 ✓）⇒ 三级回退（跨行 summary → 同行老形态 → **末条**进度行 ✓）；断言用**自洽不变量** ✓（不写死 LUFS ✓）+ ⭐**反向证明**（另造削波素材 `volume=20dB` ⇒ **必须报真峰** ✓ —— 否则"判定已生效"证明不了 ✓）；⚠️ 自检抓到**我把 `open` 当两个含义** ✗✓（"没给 freeze_end"=事实 ✓ vs "排到片尾"=结论 ✓ ⇒ 文案会说谎 ✓）⇒ ⭐**一个布尔字段只许一个含义** ✓；⚠️ 改 README 时**打错函数名** ⇒ 编辑**直接失败** ✓（严格锚点挡了一次静默错改 ✓）；smoke 476/476 ✓ / qc_report 42 ✓ / qc_scoring 29 ✓ / 251 已注册 漂移 0 ✓@709｜末节@745

**`2026-09-17.md`**（61.0k / 743 行，S7：参考项目能力搬运 + 方向纠正 + 自研引擎起步）
S7 第 65 步：搬两个参考项目的**有用功能** ⇒ **H3 阶段 2 接线完成**（`_run_h3` 真提交/轮询/取片/`/free`，20 项）@3｜S7 第 66 步：**ComfyUI 能力全覆盖**（26 条原生路由逐条对照 + 8765 新增 18 条含通用工作流与提交前校验，25 项）@70｜S7 第 67 步：⚠️ **方向纠正**：用户指出「是实现功能不是调用 ComfyUI」✗ ⇒ 开自研引擎（调度/几何/采样，零依赖 26 项）+ 核实 ComfyUI 是 **GPL-3.0**（不能抄 ✗）、本机无任何推理依赖@120｜S7 第 68 步：自研引擎·**权重体检层**（纯 Python 读 safetensors + 就绪报告 + 体检端点，34 项）；⚠️ 修掉**会真炸的 DB 缺陷**：加表在已有库不生效 ⇒ 启动期崩溃恢复 `no such table`（冒烟 476/476 ✓）@171｜S7 第 69 步：自研引擎·**管线编排**（六阶段 + 进度事件 + 取消 + 错误归因 + 干跑后端 + `/plan`、`/dry-run` 两端点；46 项 ✓）@221｜S7 第 70 步：自研引擎·**加载计划**（量化配套/层号连续性/显存排班 + `/load-plan`，28 项 ✓）；⚠️ 抓到跨模块**口径不一致**（大小不符在 inventory 不阻断、在 loader 却阻断 ✗）；全量补齐实测 **78 套 / 2762 项 / 0 失败** ✓@265｜S7 第 71 步：引擎的**采样语义** —— 引导 CFG + 重标定 + 步区间/斜坡（`guidance.py` 零依赖 ✓；`encode_text` 改回 `{positive,negative}` ✓；`cfg` 字段经 API 生效 ✓）；⭐ 硬证据「CFG 开启 ⇒ 每步真调两次模型」；套件 46 → **65/65** ✓；全量重测 **78 套 / 2781 项 / 0 失败** ✓@313｜S7 第 72 步：**首帧条件（图生视频）** —— 引擎算掩码、后端套布局（`conditioning.py` ✓ 可选阶段 `condition` ✓）；⭐ 后端不支持就**明确失败**✗（绝不悄悄退回文生视频 ✗）；相对耗时 `costFactor` ✓；套件 65 → **82/82** ✓@354｜S7 第 73 步：**torch 后端的接入点 + 依赖闸门**（懒导入 ✓；⭐ 两类错误分开 deps/pending ✓；`/engine/backends` ✓）；核实本机 torch/diffusers/numpy/safetensors/PIL **全无** ✗ ⇒ 按纪律**先不写**实现 ✓；套件 82 → **90/90** ✓；全量实测 **78 套 / 2806 项 / 0 失败** ✓@396｜S7 第 74 步：⚠️ **本机无 NVIDIA 显卡** ✗（Iris Xe ✓ 无 `nvidia-smi` ✓）⇒ 装 **CPU 版 torch 124MB** ✓ 而非 CUDA 2.5GB ✗；⚠️⚠️ **PyPI 在本机下不动** ✗ ⇒ 依赖走清华镜像（20–58 MB/s ✓）；`TorchBackend` 变**真张量**实现 ✓（真 seed 逐位可复现 ✓；`realTensors=True` 与 `synthetic=True` **两个标志同时给** ✓）；新套件 **safetensors 交叉验证 11/11** ✓（与官方库逐字节一致 ✓、截断两边都拒 ✓）；管线套件 90 → **98/98** ✓@430｜S7 第 75 步：**把真模型这条路实现出来** ✓（用户方向：以实现功能为先 ✓）：`dit.py` 真 DiT ✓（adaLN-Zero ✓ 配置全显式不猜 ✓）+ `weights.py` 权重装载 ✓（差集逐条报 ✓ 形状不符**中止且不污染** ✓）；⭐ 权重往返**逐位相同** ✓✓；⚠️ 被自己断言绊了一下 —— **adaLN-Zero 零初始化 ⇒ 条件暂无效是定义** ✓ ⇒ 断言改成成对 ✓；新套件 `engine_dit_test` **26/26** ✓；引擎六套合计 **223** 项 ✓@474｜S7 第 76 步：**真 VAE + 真出片** ✓：`vae.py`（真 3D 编解码 ✓）+ `media.py`（帧→**真 mp4** 用 ffmpeg ✓、音频→真 wav ✓，产物一律用 **ffprobe/标准库独立复核** ✓）；⭐⭐ **整链自检一口气揪出 4 个单测抓不到的真 bug** ✓（属性与实现不一致 ✗ / 位置表写死 ✗ / 条件宽度漏改 ✗ / 一维条件崩 ✗）⇒ **规则：终点必须端到端 + 外部工具复核** ✓；`engine_io_test` **30/30** ✓；引擎七套合计 **253** 项 ✓@514｜S7 第 77 步：**文本编码器 + 注入式 tokenizer** ✓ ⇒ 最后一个占位（条件）换成真实现 ✓：真 TE 结构 ✓（pre-norm ✓）、**本仓不内置词表** ✗（桩 + HF 适配器 ✓）、**截断必须被看到** ✓（回报原长/截断 ✓ + `byBranch` ✓）、padding mask ✓（然 dit 暂忽略，如实标 ✓）；⚠️ 命名类自查：meta 字典塞进了 `ids` 键 ✗；`engine_text_test` **21/21** ✓（含整链 TE→DiT→VAE→真 mp4 ✓）；引擎八套合计 **274** 项 ✓；**PENDING_PARTS 只剩「真权重相关」三条** ✓@543｜S7 第 78 步：**首帧条件接真 VAE 编码** ✓（`media` 加图片 I/O ✓ + `condition_first_frame` 真路径 ✓ + `conditioningNote` 后端自述经管线透出 ✓）；⚠️ 形状校验写太死（连 T 一起比 ⇒ 自己拦住自己 ✗）；`engine_io_test` 30 → **39/39** ✓（⭐ 第 0 潜帧与图片编码**逐位相同** ✓、掩码外潜帧**完全未动** ✓）；引擎八套合计 **283** 项 ✓@583｜S7 第 79 步：**全量补齐实测** ✓ **82 套 / 2911 项 / 0 失败**（三批 m1/m2/m3 ✓ 互不重叠 ✓；与 `run_all.TESTS` 权威数 82 一致 ✓；独立互证 2806+11+8+26+39+21=2911 ✓）；⚠️ 工具教训：**批次日志标签不可复用** ✗（复用 z1 读到上次会话的陈旧结果 ✓，差点误判 ✓）⇒ 已写进 MEMORY.md ✓@609｜S7 第 80 步：**长视频分段** ✓（`segments.py` 零依赖 ✓ 54/54 ✓）：三条不变量（网格长度 ✓ / 重叠恰好 N 帧且覆盖完整 ✓ / ⭐**保留帧数守恒** ✓）+ `build_segment_requests` 把边界帧落 PNG ⇒ 接上**真 VAE 首帧条件** ✓；⚠️ 守恒断言**在实现里当场抓住自己的 bug** ✓（非末段忘了交出重叠帧 ✗ 592≠496 ✓）；⚠️ 测试期望也写错过一次 ✓（接缝帧 = `start−1` ✓ 而非 `endFrame−1` ✓）@632｜S7 第 81 步：**命名映射骨架** ✓（`mappings.py` 17/17 ✓）：预设改名 ✓ + q/k/v **按序合并** ✓ + ⭐**干跑报告**（缺哪些目标键 / 哪些源键没用上 ✓）⇒ 工作站「填表」按报告补 ✓；⚠️ 往返自检抓到**真设计缺陷** ✗✓（`apply_renames` 命中一条就 break ✗ ⇒ 需要三条接力的键永远改不到 ✓）⇒ 改成按序应用全部 ✓；⚠️ 另两条是测试假前提 ✓（`endswith` 写错 ✓；**adaLN-Zero 下未训练模型不看注意力** ⇒ 整模型比不出 q/k/v 打乱 ✓）@663｜S7 第 82 步：**参考项目移植第一批** ✓（用户要求把 reference/ 六个项目有用的都办过来 ✓）：先派探查子代理盘点 + 查本项目已有什么（retry/tool_call/estimate **已有** ⇒ 不重复造 ✗）⇒ 定为 **short-drama-agent 的提示词生产契约** ✓：`shot_placeholders.py`（**把 `<location>L1</location>` 解析成具体内容** ✓ —— 原来只会剥标签留下裸编号 ✗）+ `prompt_polish.py`（**Mx-Shell 五段质感层** ✓ + 手持浮动/独白不张嘴自动补 ✓ + ⭐**空话必须成对** ✓）+ `/prompts/resolve`、`/prompts/polish` ✓；⚠️ 自检抓到**检测器真漏洞** ✗✓（残留检测复用严格正则 ⇒ **畸形标签被当成「没有残留」** ✗ ⇒ 改用宽松模式 ✓）⇒ **规则：体检模式要比解析模式更宽松** ✓；33/33 ✓；套件 85 ✓@693｜待办@737

**`2026-09-16.md`**（25.9k / 320 行，S7：本地服务活体 + 层级收口/守卫 + 参考项目能力搬运）
S7 第 60 步：**本地文本此前实际不可用**（qwen3 是思考模型 + OpenAI 兼容端点 ⇒ `content` 恒空 ✗）⇒ 原生 `OllamaTextAdapter` + 本地预设收敛@3｜S7 第 61 步：修守卫自检夹具失配（新日记只有 1 个步骤小节 ⇒ ④b 崩 ✗）⇒ 夹具改「按条件找」不「按位置取」@132｜S7 第 62 步：H3 **全栈活体**自检（真 8765 + 落库 task_id 交叉证明）+ ⚠️ 教训：规模数字必须**实测**不能推算@158｜S7 第 63 步：核实 CosyVoice 接缝（「未验证假设」**被证伪** ✗ 官方无 `/tts`）⇒ 加薄封装 + 接缝自检 18 项@203｜S7 第 64 步：搬 ComfyUI **执行能力**（客户端 + UI→API 转换，38 项）；含转换器判据修正 + `json.dumps` 紧凑性被路径守卫抓到@255｜待办@314

**`2026-09-15.md`**（2.2k / 27 行，S7：QC 打分 + 原 TS 死分支发现）
S7 第 9 步：镜头 QC 打分@3｜S7 第 10 步：QC 接线（视频完成→自动打分写库，闭环）@26｜S7 第 11 步：审片重跑闭环（软删产物/FL2VA/首帧等待，/retry-qc 关闭）@41｜S7 第 12 步：set-frame + 抽帧泛化（⚠️ 自检未跑，见日记）@59｜S7 第 13 步：重生成镜头帧（帧类型白名单/帧提示词/拼接，17 用例）@87｜S7 第 14 步：图像连续性 QC（真实图 dHash 三档/并发缓存竞态，28 用例）@105｜S7 第 15 步：快照重冻结 + 自动发现（守卫读文件必须在快照里；真源码/冻结结论一致）@120｜S7 第 16 步：对拍补齐新增 GET（实测 一致 12 / 新差异 0；文本响应时间戳归一化）@133｜S7 第 17 步：技术维度 QC（补行为缺口；三项检测是两侧共同的继承缺陷）@142｜S7 第 18 步：清过期 docstring + 修两个真缺口（webhook QC 空实现 / grid Agent 存根）@157｜S7 第 19 步：全面体检（修 merge 空实现；结论=剩校色/subagent 工具/storage.change）@170｜S7 第 20 步：校色落地（ffmpeg；实测 sharp 的 gamma 是 no-op / exposure 是 L 星乘法）@191｜S7 第 21 步：子 Agent 调度工具（ALS→ContextVar；注册表逐字镜像）@205｜S7 第 22 步：请求日志中间件（入口级行为已对齐，未实现清单清零）@215｜S7 第 23 步：清 tmp + README 遗留物表校真（剩唯一缺口=参考图压缩）@225｜S7 第 24 步：参考图压缩落地（功能性缺口清零；geq/夹具两坑）@240｜S7 第 25 步：对账抓出快照第二次缺件（自动发现补两种形态 + 端到端验收）@255｜S7 第 26 步：全量 61 套件一次跑绿（实测 2401 项 / 0 失败，分批取证）@269｜S7 第 27 步：迁完 storage/change（**未注册 0 条**，Python 227 全量覆盖 Node 224；含一处「旧 501 断言迁移后真切数据根」的险情）@278｜S7 第 28 步：scripts 收拾（体检健康 + 补第四类指路牌 + 修 run_all 陈旧 docstring）@295｜S7 第 29 步：全仓去 Node 化（scripts/ 与 frozen_ts 全改 Python 并收进 backend-py；JS→Py 六坑）@302｜S7 第 30 步：去 Node 化后全量回归（实测 62 套件 / 2441 项 / 0 失败；抓修 1 处替换双前缀回归）@328｜S7 第 31 步：技能库并入后端（skills/ → backend-py/app/skills/；三处根同步 + 旧写法判致命）@338｜S7 第 32 步：核对 skills 新址 → 补 Dockerfile/Node 两处手尾（挪目录要三查）@352｜S7 第 33 步：干跑删库（可回滚）→ 全绿；修 3 类只有真删才现形的问题 + 记下当天清单@362｜S7 第 34 步：提前清掉清单第 1、2 项（前端代理切 5790 + 契约类型搬到前端，npm run generate 真构建验证）@379｜S7 第 35 步：Dockerfile 重写为 Python 镜像 + 新增镜像一致性自检（19 用例）+ parity/docs 指路语收口@393｜S7 第 36 步：顶层目录盘点（local_services/benchmarks/data/tmp 多数不该迁；又抓到 compose 是 Node 时代）@407｜S7 第 37 步：全量回归抓到冒烟 2 条红（断言依赖「本机构建过前端」⇒ 夹具化 + 编码穿越；实测 63 套件/2465 项）@423｜S7 第 38 步：local_services 并入 backend-py（三处默认值同步 + gitignore 反向包含坑）@438｜S7 第 39 步：最后一次 Node↔Python 对拍（4 条「新差异」真相=前端 SPA 兜底回 200 HTML，非缺端点；判据改环境无关，回到 一致12/新差异0）@456｜S7 第 40 步：**删掉 backend/**（删后守卫自动回退快照、结论逐字一致；冒烟因硬读 TS 崩过已修；全量 63 套件/2463 项/0 失败）@476｜S7 第 41 步：删库后一致性清扫（清掉 5789/启动命令/反代文案等 26 处误导指令；历史溯源引用保留）@508｜S7 第 42 步：parity_run 前置拦截（历史工具不再干等 90s）+ docs 指路语清零 + **首次进程级验证**（真起 uvicorn，临时数据根）@532｜S7 第 43 步：后端结构收口（收掉三处重复的 SKILLS_DIR → app/config.py；写清代码 vs 资产边界；明确 services/ 刻意扁平**不可重排**）@550｜S7 第 44 步：前端调用点 ↔ 后端路由覆盖（新套件；实测 166/166 已注册 0 未匹配；含反套套逻辑用例）@573｜S7 第 45 步：路径常量再收一处（BACKEND_PY_ROOT 唯一权威）+ scripts 路径推导表 + 记忆预算下移（7716→6993）@594｜S7 第 46 步：共享契约镜像守卫（新套件；contracts.ts 31 字段 0 未匹配；实测 65 套件/2473 项）@614｜S7 第 47 步：修掉**真 bug**——前端 dev 代理硬编码 5789（已删的 Node）⇒ 改用 backendTarget + 加守卫（65 套件/2476 项）@631｜S7 第 48 步：复答「agents 与 skills 为何不同目录」（证据链 + 不建议合并的理由）+ 修 check_skill_refs 过时注释@655｜S7 第 49 步：**真实浏览器 E2E**（playwright-cli：标题/导航/0 console error；后端日志证明 /dramas、/skills 均 200 打通）+ 0.1.13 三坑@678｜S7 第 50 步：三答「层级要不要再优化」⇒ 不需要（落成「五棵树」生命周期表 + 触发条件）@700｜S7 第 51 步：层级收口第一步 app/core/（平台层独立；146 文件 381 处重写；新增**层级守卫** layering_test；含我造成的 venv 事故与修复）@718｜S7 第 52 步：agent/mcp 提到顶层（守卫抓出并断开 services↔agent 双向依赖；三种 import 形态的坑）@759｜S7 第 53 步：**方案 A 定稿** agent/mcp 收回 app/ 内（六块同层级；92 行改写；技能文档 9 断链→0）@797｜S7 第 54 步：skills/scripts/local_services 并入 app/（用户拍板；深度+2 事故、硬编码拼接路径、守卫扫描面外溢、块搬重复 四坑）@824｜S7 第 55 步：tests/ 是否标准目录 ⇒ 是惯例，**留在 backend-py/ 顶层**（68 文件含深度推导，搬动风险高收益零）@870｜S7 第 56 步：继续优化（体检取证：无死代码；skill_parser 两份实现合一 → 权威下沉 services + agent 转发壳；陈旧引用收口；三教训：批量替换形态盲区/失败留脏态/长字符串）@892｜S7 第 57 步：修冒烟非幂等（三目录合并漏网的分段拼接路径 ⇒ 兜底清理静默失效 + 无残留断言假绿）+ 新增资产布局守卫 12 用例 + config.APP_ROOT@931｜S7 第 58 步：H3 链接缝契约自检（适配器 ↔ 8765 薄封装；阶段2 缺口钉成断言）+ 路径字面量排查 0 死 + 全量实测 68 套件/2515 项@963｜S7 第 59 步：CosyVoice(9880) 接缝排查（普通合成假设 /tts 非官方端点 ✗ ⇒ 就地标注 + 两步确认法）@999｜待办（已无阻塞项，删库完成）@1013

**`2026-09-14.md`**（4.6k / 87 行，S7 收尾：GPU 租约接线补完）
S7 第 4 步：GPU 租约接线补完（image/video 长租约；4/3 处释放点，少一处即锁泄漏）@3｜S7 第 5 步：Node↔Python 差分对拍工具（三态判定/白名单越界/PowerShell 退格坑）@24｜S7 第 5-7 步：真对拍 0 新差异（MISSING 类）· 守卫冻结快照：**跑通真对拍**（一致10/新差异4：4 条裸列表端点 501；路数守卫有盲点）@42｜S7 第 7 步：守卫冻结快照（72 个 .ts / 707 KB，两条路径结论一致）@87｜S7 第 8 步：多集节奏相位（**修掉原 TS 无限递归**）@105｜待办（storyboards 4 条 · consistency-qc · 删库决策 · MEMORY 已满）@124

**`2026-09-13.md`**（35.0k / 225 行，承接绞杀者迁移：S6 评测执行器 + 评测域前 2 端点）
S6 第 4 步：评测执行器 + cases/evaluate 两端点（seed 提交/抽取口径/清理）@7｜S6 第 5 步：creator + optimize（含 /agent-configs/generate 解锁，状态机四条判据）@19｜S6 第 6 步：评测调度器（防重入/失败隔离/running 卡死缺陷保真）=> 评测域整域关闭 5/5@30｜S6 第 7 步：auto-pipeline 前置（sse-hub + frame-extractor，含尾帧红线）@41｜S6 第 8 步：全自动管线整域关闭（8 阶段 + SSE + AnyIO 事件循环坑）@53｜S6 第 9 步：localModels 整域关闭（12 条规则优先级 + NDJSON 下载 + 守卫假阳性修正）@65｜S6 第 10 步：补齐 10 条「延后理由已过期」的生成端点（characters 8 + props/scenes，含 smoke 硬编码耦合教训）@78｜S6 第 11 步：storyboards 5 条生成/LLM 端点（TTS 两套回执 + 对话解析贪婪坑 + 拆分顺延）@90｜S6 第 12 步：续写端点 + **剩余 16 条成因分类**（storage/change 有意延后、export 缺 854 行服务、distill 需媒体探测）@102｜S6 第 13 步：smoke 的「未迁移⇒501」清单改**动态派生**（根除三轮反复踩的坑 + import 顶掉 env 的教训）@118｜S6 第 14 步：export 开工（export-service 192 行 + EDL/ZIP 2 端点 + Math.round 银行家舍入坑）@129｜S6 第 15 步：剪映草稿导出（分 4 阶段；素材编号从 0、字幕错位照抄、全剧任一集缺则 400）@139｜⚠️记忆层修复（7 篇旧日记已不在盘，INDEX 悬空锚点已清）@151｜S6 第 16 步：QC 报告交付物 → **export 整域关闭 7/7**（探测回退链 / 60 分边界 / toFixed 陷阱 / GBK stdout）@161｜S6 第 17 步：时代背景提炼 + 风格提炼 2 端点（宽松判据 / 异常分层 / 冒烟别真发 LLM）@174｜S7 第 1 步：基准资产搬迁 + 评测 CLI（删 backend/ 的前置；app 已 0 依赖）@187｜S7 第 2 步：GPU 显存管理器整域（队列/驱逐/卸载策略 + /gpu/* 两端点）@197｜S7 第 3 步：GPU 租约接线（text/tts；契约：逐次申请+异常也释放）@214｜待办（image/video 长租约 · 全量对拍 · 删 backend/ · 冒烟偶发 · MEMORY 已满）@225


> 动**视频首尾帧 / 尾帧 / 角色合并**时**先读本文件**。





## 已出栈的落点（优先看这些，别翻日志）

| 内容 | 权威落点 |
|---|---|
| 全部 API 契约 | `docs/api-contract.md` |
| H3 本地视频链路 | `docs/local-h3-video-system.md` |
| 本地模型评估/下载 | `docs/local-model-evaluation.md`、`TOPICS.md` |
| 语料源与合规 | `docs/video-prompt-data-sources.md`、`docs/seedance2-corpus-analysis.md` |
| preset skill 模板 | `docs/preset-skill-template.md` |
| **Skill 体系（改前必读）** | `backend-py/app/skills/README.md` + `MEMORY.md §Skill` + `TOPICS.md §Skill 体系细节` |
| **Python 后端（绞杀者迁移）** | `backend-py/README.md`（运行/环境变量/迁移 SOP）+ `TOPICS.md §backend-py` + `backend-py/tests/smoke_test.py` |
| 引用完整性守卫（skills 路径） | `backend-py/app/scripts/check_skill_refs.py`（基线见脚本头） |
| 记忆层自检（8k 预算 + 锚点/登记/落点路径）+ 守卫自检 | `backend-py/app/scripts/check_memory.py`、`backend-py/app/scripts/test_guards.py`（规则见脚本头） |

## 日志写入规范（防再膨胀 —— `09-12` 单日已 780 行）

- 每轮 **≤ 8 行**：只写 **结论 / 落点(文件:行) / 红线 / 未决**。
- **不写**过程叙述与命令回放；需长期留证的内容 → `docs/` 或 `TOPICS.md`。
- 结论属「日后会导致 bug 的不变量」→ 同步进 `MEMORY.md`；属低频长专题 → `TOPICS.md`。
- 写时假定「将来只会按 `@行号` 跳读」，**不要依赖前后文**。

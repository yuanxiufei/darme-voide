# 2026-08-08

## 启动前后端
- 后端 5679 + 前端 3013 均已启动

---

# 2026-08-10

## 视频生成切换 MiniMax H3
- aiConfigs.ts 视频预设：volcengine/doubao-seedance → minimax/hailuo-02
- minimax-video.ts adapter 重写：去掉 OpenAI content 数组包装，使用 MiniMax 原生 body (prompt + aspect_ratio + duration + first_frame_image)
- tsc 编译 + linter 通过

## 本地模型评估
- 基于用户硬件 A5000 (24GB VRAM) + 96GB RAM 完成全管线评估
- 评估文档：skills/local-model-evaluation.md
- 推荐方案：
  - 文本 (环节1-3)：qwen3:14b Q4_K_M (~9GB) via Ollama
  - 分镜 (环节5)：qwen3:32b IQ4_XS (~16GB) 单独切换
  - 图片 (环节6)：SDXL + IP-Adapter + ControlNet (~12GB) via ComfyUI
- 视频 (环节7)：Wan 2.6 14B GGUF Q4 (~15GB) 独占GPU
- TTS (环节4/8)：CosyVoice 2 (~4GB) 独立HTTP服务
- 全管线峰值显存 17GB，在 24GB 安全范围内
- 实施总估计：6.5 天

## 云端+本地双轨 Adapter 架构 (2026-08-10)
- 用户要求：既支持云端 MiniMax API（M3/H3/Speech 2.8），也支持本地 A5000 模型
- MiniMax H3 已开源，本地可部署同格式 API
- 新建 Adapter：
  - `local-sd-image.ts` → SD WebUI `/sdapi/v1/txt2img` + `/sdapi/v1/img2img`，同步返回 base64
  - `cosyvoice-tts.ts` → CosyVoice 2 HTTP API，自动识别 base64/hex 编码
- 文本走 Ollama：无需新 Adapter，Ollama 原生 OpenAI 兼容 → 改 baseUrl 即可
- 视频走 MiniMax H3 本地：复用现有 MiniMaxVideoAdapter → 改 baseUrl 指向 localhost
- registry.ts：注册 `local-sd` (image) 和 `cosyvoice` (tts)
- aiConfigs.ts：
  - 新增 `LOCAL_PRESET_SERVICES` 4 条（text/openai port11434, image/local-sd port7860, video/minimax port8765, audio/cosyvoice port9880, priority 82-85）
  - 新增 `/quick-local` 端点：一键初始化本地服务配置，无需 API Key
  - buildProbe 新增 `local-sd`、`cosyvoice`、`ollama` 探活支持

## GPU 显存智能管理系统 (2026-08-10)
- 用户要求：云端+本地双模式、各环节模型可独立切换、本地模式下自动显存管理避免冲突
- 创建 `services/gpu-manager.ts`（单例 GPU 管理器）：
  - Mutex 锁队列：确保同一时刻只有一个 GPU 重负载任务运行
  - VRAM 配置文件：ollama/qwen3:14b=9GB, ollama/qwen3:32b=16GB, local-sd/sdxl=12GB, minimax/hailuo-02=15GB, cosyvoice/cosyvoice-v2=4GB
  - 自动驱逐策略：加载新模型前检查 `totalVram + neededVram > 24GB - 2GB safeMargin`，超出则按占用从小到大卸载旧模型
  - Ollama 卸载：POST /api/generate { keep_alive: 0 } 触发立即释放
  - SD WebUI 卸载：POST /sdapi/v1/unload-checkpoint (best-effort)
  - CosyVoice/其他：被动跟踪，由外部服务自行管理
  - 导出 `isLocalConfig(baseUrl, provider)` 判断是否本地服务
  - 导出 `withGpuLease()` 辅助函数（自动 acquire/finally release）
- 注入 image-generation.ts：processImageGeneration 前 acquire → handleImageComplete/handleImageCompleteBase64/poll-timeout/catch 时 release
- 注入 video-generation.ts：processVideoGeneration 前 acquire → handleVideoComplete/poll-timeout/catch 时 release
- 注入 tts-generation.ts：generateTTS 中使用 acquire/finally release 模式
- aiConfigs.ts 新增 3 个端点：
  - GET /gpu/status → GPU 状态 + nvidia-smi 实时数据
  - POST /gpu/release-all → 强制释放所有模型（调试用）
  - GET /configs/local → 列出所有本地配置
- tsc 编译 + linter 全过

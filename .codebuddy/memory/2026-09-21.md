# 2026-09-21

## 自研 normalizer（第七项 ✓）——「连回退都去掉」的第一步

承 2026-09-20 的「改造 transformers + 自研三大血统」✓，本轮补 `normalizer` ✓（此前**非空即拒绝** ✗
⇒ 带 `BertNormalizer` 的真实词表（BERT 系 ✓）只能回退参考实现 ✗）。

### 实现（`engine/tokenizer_own.py` ✓ 自检 17 项 ✓）
- `BertNormalizer` ✓（**四步顺序照参考** ✓：clean_text → handle_chinese_chars → lowercase → strip_accents ✓；
  ⚠️ `strip_accents` 字段为 `null` 时**跟随 lowercase** ✓ —— 参考实现的默认耦合 ✓）；
- `NFC`/`NFD`/`NFKC`/`NFKD` ✓（标准库 ✓）、`Lowercase` ✓、`StripAccents` ✓、`Strip` ✓、`Prepend` ✓、
  `Replace` ✓（regex ✓）、`Sequence` ✓（递归 ✓）；
- 统一包装 `NormalizedTokenizer` ✓（三种模型共用一个 ✓ ⇒ 不必各自改 ✗）。

### ⚠️ 两处**实测出来**的语义（都靠逐例比对抓到，不靠文档 ✗）
1. `BertNormalizer.clean_text`：`\t`/`\n`/`Zs` ⇒ **一个空格** ✓、**不 trim ✓ 不合并连续空格** ✗
   （`'  spaced\t tabs\n'` ⇒ `'  spaced  tabs '` ✓）；CJK 字符**两侧各加一个空格** ✓（`'中文abc'` ⇒ `' 中  文 abc'` ✓）；
2. ⚠️⚠️ `SimpleNormalizer("StripAccents")` **不先做 NFD** ✓✗：预组合 `é`(U+00E9) **原样保留** ✓，
   只丢**独立**组合字符 ✓ —— 而 `BertNormalizer(strip_accents=True)` **会**先 NFD ✓
   （两处语义**确实不一样** ✓）。我第一版给 StripAccents 加了 NFD ⇒ 自检 ⑬ 当场红 ✓。

### 判据跟着能力走（本轮的次要教训 ✓）
`normalizer` 从「拒绝」变「支持」后，**三处旧用例全部失效** ✗（own 测试 ②⑩、hub 测试 ⑩⑪⑫㉑ ✗）——
它们的夹具（「带 normalizer 的 WordPiece」）现在**走自研** ✓ ⇒ 回退用例必须换成**真拒绝**形态 ✓
（本轮用 `byte_fallback` 的 Unigram ✓）。**别让夹具的语义悄悄过期** ✓✗。

### `byte_fallback` 仍拒绝 ✓（有实测依据 ✓）
实测：`Unigram(vocab, unk_id=0, byte_fallback=True)` 对未知字符**仍出 `unk`** ✗（没走字节回退 ✓）
⇒ **语义没核清** ✓ ⇒ 不按猜的实现 ✓（拒绝 + 理由 ✓）。
`Precompiled` ✗ 也拒绝 ✓：要 SentencePiece 的 **charsmap 表** ✓（实测 `Precompiled.__new__()`
就要求 `precompiled_charsmap` ✓）—— **不是** NFKC 换个写法 ✓ 不假装 ✓。

### 规则型预分词器也自研了（同轮 ✓ 5 种 ✓ 自检 19 项 ✓）
`Whitespace` / `WhitespaceSplit` / `Digits`（两种口径 ✓）/ `CharDelimiterSplit` ✓ +
`Punctuation`（**5 种 behavior** ✓）⇒ 覆盖表从「三种血统 × 三种预分词」扩到
**三种模型 × 八种预分词器** ✓。规则**全部逐例实测**得来 ✓（探针对照 `pre_tokenize_str` ✓ 75 例零差异 ✓）：

- ⚠️⚠️ **两次踩坑都在"凭感觉写规则"上** ✓：① 我先把 `Punctuation` 写成"空白当分隔" ✓✗
  ⇒ `removed`/`contiguous` 全错 ✓；② 重写成状态机后 `merged_with_previous` 的判据写成
  「上一片是不是**纯标点**」✗ ⇒ 实测要的是「上一片**结尾**是不是标点」✓（`' b!'` 后面那个 `!` 要独立 ✓）；
- ⚠️ 还有一次是**读被截断的输出**导致的误判 ✗：我以为 `isolated` 要按空白切 ✓✗
  （真规则：**run 整段保留** ✓ `'hi  there'` 是一片 ✓）⇒ 白写了一个 `_split_ws_keep_one` 又删掉 ✓。
  ⇒ **探针输出先落盘/看全再下结论** ✓（`Select-Object -Last n` 会截断 ✓✗）。
- `Punctuation` 老字段 `is_whitespace_prefix`（bool ✓）与新版 `behavior` **不是一一对应** ✗
  ⇒ **拒绝** ✓（映射没核过就不猜 ✓）；仍未做 ✗：`Split`（behavior 组合多 ✗）、`FixedLength`、
  `UnicodeScripts` ✓。

### 判据（与参考实现逐例对照 ✓ 本机 `tokenizers` Rust 那份 ✓）
- `normalize` 逐例：**10 种 normalizer × 10 例** 全一致 ✓（含 `\t`/`\n`→空格 ✓ 控制字符丢弃 ✓
  CJK 加空格 ✓ 去重音 ✓ NFKC `½`→`1⁄2` ✓）；
- 端到端 **逐例同 id** ✓（规范化 + 预分词 + 贪心匹配三层一起对 ✓ 5 例含 CJK ✓）+ 反向证明
  （摘掉 normalizer ⇒ `CAFÉ` 必然编不出来 ✓ ⇒ 规范化真在用 ✓ 不是摆设 ✓）。
- Hub 里 `detect_form` 现在报 `normalizerTypes`（**展平** ✓）+ `byteFallback` ✓；回退只剩
  「自研明确拒绝」的形态 ✓。

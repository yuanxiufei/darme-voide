# 2026-09-20

## S7 第 101 步：记忆层「第三次腾 8k 预算」+ 归位 09-18 攒下的规则

### 为什么先做这件事

上一轮末尾 `MEMORY.md` 已 **7848/8000**（余 **152** ✗）⇒ **任何新规则都塞不进去** ✗；
而 09-18 那天攒下 **5 条**只落在当日日志里的长期规则 ✗（按本仓读法，长期规则该进 `MEMORY.md` ✓）。
⇒ 先按本文件自己的规矩动手：「**逼近上限时把细节下移 `TOPICS.md`，勿硬塞**」✓。

### 交付

**`TOPICS.md`** 新增 §自 MEMORY.md 下移（2026-09-20，第三次腾 8k 预算）✓：

* §Skill 体系的**完整表述**：绑定解析入口唯一 `resolveDefaultSkills`（**只扫自有**）✓、
  注入闸口径 = `renderSkill(parseSkill(...)).length` ✓（**不等于字节数** ✓）、
  绑定面板是外部库 Skill **唯一 UI 挂载入口** 且**拖拽真实生效**（列表顺序 = 注入顺序 = 超预算跳过顺序 ✓）、
  兜底库合成条目 ✓、`meta.yaml` 是死数据但 `version`/`source` **只此一处** ✓、
  改名/挪库后核对 DB 绑定的**只读直开**做法 ✓；
* §画风体系的**完整表述**：`prompt-utils` **不拆** ✓、找副本顺序 ✓、
  解析链唯一入口 `resolveEffectiveArtStyle()` ✓、**正负成对收口** ✓。

**`MEMORY.md` 7848 → 6873** ✓（腾出 975 ✓，余量 1127 ✓）：精简成**只留会导致 bug 的判据** ✓ + 指到 `TOPICS.md` ✓。

### ⚠️ 顺手抓到一个**过期指针** ✗✓（比省字符更值钱）

§画风体系的「单一事实来源」原文写着后端是 **`shared/prompt-utils.ts`** ✗ ——
而 **Node 侧 2026-09-15 就已删除** ✓✗ ⇒ 这条指针**指向不存在的文件** ✓，
且它正是「**加画风只改这 2 个文件**」这句话的一半 ✓✗（照着做会找不到文件 ✓）。

已核准真源 = **`backend-py/app/services/prompt_utils.py`** ✓
（`ART_STYLE_CATALOG` + `DRAMA_ART_STYLE_MAP`/`DRAMA_ART_NEGATIVE_MAP`/`EQUIP_ART_STYLE_MAP` ✓；
消费方 `routers/app_settings.py` / `routers/dramas.py` ✓）⇒ 原地更正 ✓
并写明「见到旧指针就是在读旧文 ✓」。

> **规则**：**删掉一侧实现时，必须同时搜一遍"指向它的文档指针"** ✗ ——
> 代码路径删了有守卫报错 ✓，**文档里的路径不会** ✓（它只是静静地把人送到 404 ✓）。

### 归位的 5 条规则（09-18 只落在日志里）

1. **检测器的模式要比解析器更宽松** ✗（复用同一严格正则 ⇒ 畸形输入「解析不了」也「检测不到」⇒ 被当成干净 ✓✗）；
2. **追加小节只锚「末节首行」** ✗（锚在正文中间 ⇒ 新小节被插到旧小节**前面** ✓）；
3. **模块级状态入口必须清零** ✗（凡"为跨项比较"引入的 ✓；单次调用测不出 ✓，连调两次才现形 ✓）；
4. **`compile()` 过了 ≠ 名字在** ✗（改了模块级引用还要**真的 `import` 一次那个模块** ✓）；
5. **汇总必须打分母 + 点名异常项** ✗；**文档里的规模数字要么指向唯一权威、要么带实测日期** ✗。

**实测**：`check_memory` **致命 0** ✓（6 篇已登记 / 121 锚点 ✓）；`MEMORY.md` **6873/8000** ✓。

### 补记：全量复测刷新（第 100 步改了项数 ⇒ 旧数字作废 ✗）

第 100 步把 `technical_qc_test` 从 **31 → 42** ✓ ⇒ README 里的 **3213** 立刻过期 ✗
⇒ 按本仓规矩**不推算、只实测** ✓：重跑四批（证据落盘 `tmp/run2_status.txt` ✓；
`--sum` 会**打分母 + 点名读到 0 项的套件** ✓ —— 这套自检这次真派上用场 ✓）：

```text
实测：套=94/94  项=3224  通过=94  失败=0
⚠️ 读到 0 项的套件：['h3_backend_live_test.py', 'route_parity_test.py']   ← 已知 2 套（不打印 SUMMARY ✓ 本身通过 ✓）
```

⭐ **交叉校验**：3224 − 3213 = **11** ✓ = 这次新增的用例数 ✓（**对不上就说明"我以为只加了 11 条"是错的** ✓）。

顺手又改掉 `README.md` **另一处陈旧数** ✗：第 699 行写着「**82 套件 / 2911 项**」✗（比上一处漏得更久 ✓ ——
它是更早一版快照 ✓）⇒ 改成 **94 套件 / 3224 项（2026-09-20 实测）** ✓。

> **规则**：**改"规模/计数"类数字后，要全文再搜一遍所有同类数字** ✗ ——
> 它们往往散落**多处**、且**各自过期时间不同** ✓（本次两处差了 300 多项 ✓；
> 我上一轮只搜了 `3213`，所以漏掉了 `2911` ✓）。
>
> 另：本日志开头那条「6 篇已登记」是**写入当时的**事实 ✓（现在 7 篇 ✓）—— 日志只追加 ✓ 不回改 ✓。

## S7 第 102 步：把「零调用」的提示词端点接进前端 ✓（166 → 170 调用点 / 0 未匹配）

前几轮交付的五个端点里，`prompts/resolve`、`prompts/polish` 一直**没人调用** ✗ ——
按本仓判据「**没人调用的库不算功能**」✗ ⇒ 这一步把它们接上 ✓
（另三个 `assets/*`、`continuity/check` 的**聚合体**已经由 `PreflightPanel` 用着 ✓ ⇒ 不重复铺 UI ✓）。

### 落点选择：挂在**提示词页**，而不是分镜页 ✓

分镜页 2300+ 行 ✗（改它风险高 ✓）；提示词页 `pages/drama/[id]/prompts.vue` 只有 **386 行** ✓
⇒ 新增**独立组件** `components/PromptQcPanel.vue` ✓，页面里**只插一行** `<PromptQcPanel />` ✓（+3 行 ✓）。

### 交付

| 文件 | 内容 |
|---|---|
`composables/useApi.ts` | 新增 `promptToolsAPI` ✓（`resolve(payload)` / `polish(payload)` ✓）—— **返回形状按后端真键写在注释里** ✓（不猜 ✗） |
`components/PromptQcPanel.vue` | ①占位符解析（贴提示词 ⇒ 解析成具体内容 ✓ + `ok` 徽章 ✓ + `problems` 逐条 ✓ + 残留标签 / 裸编号警告 ✓）②五段质感层（六个字段 + 手持 / 独白开关 ⇒ `sections` 分段显示 ✓ + `issues` ✓）；⚠️ **面板只呈现、不自己判分** ✗（判据只在后端一份 ✓） |

### 核准的两件事（都不是猜的 ✓）

* `PreflightPanel.vue:13` 的导入写法 = `import { productionAPI } from '~/composables/useApi'` ✓ ⇒ 照抄 ✓；
* 后端 `to_dict()` 的**真键**：`resolve` → `text/ok/used/unresolved/durationsMs/residualTags/bareIds/problems` ✓；
  `polish` → `sections/text/ok/issues/vagueTerms/inserted` ✓。

### ⚠️ 我自己写坏过一次（**自审**抓到 ✓，不是工具抓到 ✗）

第一版 SFC 里我**多写了一个 `<script lang="ts">` 块** ✗，并在模板里调一个"并不存在"的辅助函数 ✗✗
（本想复用后端返回值，却绕了一道）⇒ 自审时删掉 ✓，改成直接渲染后端字段 ✓。

**实测**：`@vue/compiler-sfc` **真编译**两个改过的 SFC ⇒ 全 OK ✓✓（脚本跑完即删 ✓ 已核 `True` ✓）；
`frontend_api_coverage_test` **5/5** ✓（调用点 **166→170** ✓、**0 未匹配** ✓，后端 251 条 ✓）；
`route_parity` 251 已注册 / 漂移 **0** ✓；`layering` 6/6 ✓。

> ⚠️ 又一次「命令被判后台且**没重定向**」✓ ⇒ 按自己立的规矩**不当它跑过** ✗，带 `> 文件` 重跑 ✓
> —— 这条规则又救了自己一次 ✓。

## S7 第 103 步：**坏文件不再拿满分** ✓ —— 收口 `_run` 语义的"静默绿灯"（43/43 ✓）

第 100 步修好三项死检测 ✓，紧接着发现**同一族的另一个洞** ✗：`_run` 的判失败条件是
「**非零退出且 stderr 为空**」✓（这条**保持** ✓ —— ffmpeg 滤镜正常结束也会非零退出 ✓，
改成"非零即失败"会把正常视频全判死 ✗），但它的**连带后果**是：

> 坏文件 / 空文件 ⇒ ffprobe 非零退出但**打了 stderr** ⇒ 不报错 ✓ ⇒ 各滤镜读不出任何特征
> ⇒ 段数全空、响度读不到 ⇒ **最终 100 分、零 issue** ✗✗

⇒ **一个损坏的成片看起来是"干净的 100 分"** ✓✗ —— 正是本会话反复在打的那一类：
**"检测不到"与"通过"长得一模一样** ✓。

### 修法（只补**报出** ✓，不动退出码判定 ✗）

`probe_video_technical_spec` 的 ffprobe 段：`duration` 与 `fps` **同时**读不出来 ⇒
报 warning「无法读取视频规格（ffprobe 未给出时长与帧率）⇒ 黑场/冻帧/响度这些检测会全部空转」✓
+ 扣 15 分 ✓（与异常分支的扣分口径一致 ✓）。

⚠️ 判据刻意写成「**两条都缺**」✓ 而不是「stdout 为空」✗：有些容器 / 裸流本来就没有那些字段 ✓，
只缺一个也不该报 ✗（例如无音轨 ✓、某些容器没有 `avg_frame_rate` ✓）。

### 实测

`technical_qc_test` **42 → 43/43** ✓（原来那条「满分且零 issue」的用例**按预期翻红** ✓ ——
它本来就是**信号灯** ✓）；`qc_report` 42/42 ✓；`qc_scoring` 29/29 ✓；`qc_retry` 14/14 ✓；
**`smoke_test` 476/476** ✓✓；`route_parity` 251 已注册 / 未注册 0 / 漂移 **0** ✓；层级 6/6 ✓。

> **规则**：**"判定输入读不出来"本身必须是一条判据** ✗ —— 否则退出码与解析都"没报错" ✓，
> 而结论是满分 ✓✗（本会话第 3 次撞同一族：状态常量不同源 ✓、空集当绿灯 ✓、这次是"读不出来当通过" ✓）。

## S7 第 104 步：**"没比出来"不再冒充"通过"** ✓ —— 一致性 QC 补三态与覆盖率（32/32 ✓）

顺着「静默绿灯」这一族继续扫 ✓，在 `services/consistency_qc.py` 找到同一个病 ✗：

> 一对都比不出来时（**分镜不足 2 个** ✓ / **图缺失** ✓ / **图读不出**（ffmpeg 解不开）✓）
> 原实现是 `continue` 或直接 `return` ✗ ⇒ 返回体只剩 `checkedPairs: 0, warningCount: 0` ✓✗
> ⇒ **与"比过且都没问题"长得一模一样** ✓✗ —— 消费方（或人）看一眼 `warningCount == 0`
> 就会得出"这一集画面连续性没问题" ✓，而**其实什么都没比** ✓。

### 修法（与本仓其它模块同口径 ✓）

| 项 | 内容 |
|---|---|
`consistent`（**三态** ✓） | `None` = **没判** ✓ / `True` = 比过且零 warning ✓ / `False` = 有 warning ✓ |
`coverage` | `shots` / `pairs` / `compared` / `skippedMissingImage` / `skippedUnreadable` ✓（分开数 ✓；一对都没比过时附 `note`：「没有任何可比对的一对…**没判**（不是通过）」✓） |

⚠️ 这是**有意**与冻结的 TS 接口（只有 `checkedPairs`/`warningCount`）不一致 ✓：
Node 侧 2026-09-15 已删 ✓ ⇒ 该接口不再是行为契约 ✓，而"空报告 = 全绿"会**误导调用方** ✗
（判断依据写在代码注释里 ✓）。

### ⚠️ 自检又抓到我一次**期望写错** ✗✓（31/32 那条红 ✓）

我断言 e1 夹具「1 号无图 ⇒ 跳过 1 对」✗，而**实测**是 `shots=5 / pairs=4 / compared=4 / skipped=0` ✓。
⇒ 顺手把这条断言从"写死夹具形状"换成**自洽不变量** ✓✓：
`pairs == shots − 1` ✓ 且 **`compared + 各类 skipped == pairs`** ✓（每对要么比过、要么被**计数**跳过 ✓
—— 这条能防"以后新增一条 skip 路径忘了计数" ✓✓）。

**实测**：`consistency_qc_test` **30 → 32/32** ✓（三态 False / True / None 各有用例 ✓）；
`smoke_test` **476/476** ✓✓；`qc_scoring` 29/29 ✓；`qc_report` 42/42 ✓；
`route_parity` 251 已注册 / 未注册 0 / 漂移 **0** ✓；层级 6/6 ✓。

### 同族扫描还剩一处（**本轮未动** ✗，留作下一步）

`services/qc_scoring.py` 里有一条**死规则** ✗：注释自己写着「同一场景但地点 ID 不一致 −20」
**永远不会触发** ✓ —— 因为判据要求 `prev.scene_id == sb.scene_id` ✓，而 `prev_scene` 又是按
`prev.scene_id` 查出来的 ✓ ⇒ **同一个场景行 ⇒ `location_id` 必然相同** ✓✗。
（另两处「无对白 ⇒ lip_sync 90 / 无角色 ⇒ 95」更像**产品取舍** ✓，不是同一个病 ✓。）

> **规则**：**「没比出来 / 没判」必须是一个能被机器读出来的状态** ✗ ——
> 只报计数（0 / 0）时，它与"全绿"在返回体里**完全同形** ✓✗
> （本会话第 4 次同族：常量不同源 ✓ → 空集当绿灯 ✓ → 读不出当通过 ✓ → 现在是"没比出来当通过" ✓）。

## S7 第 105 步：**删掉一条死规则** ✓ —— `qc_scoring` 的「地点 ID 不一致」永不触发（31/31 ✓）

上一轮扫描时发现、这一轮处理 ✓。`qc_scoring.py` 里写着「同一场景但地点 ID 不一致 ⇒ −20」✓，
而它**结构上不可能触发** ✓✗：判据要求 `prev.scene_id == sb.scene_id` ✓，`prev_scene` 又是按
`prev.scene_id` 查出来的 ⇒ 两次读的是**同一个场景行** ⇒ `location_id` 必然相同 ✓
（测试里也一直写着「原 TS 死分支，永远不触发」✓ —— **所有人都知道它没用，但都留着** ✗）。

### 判断：**删掉** ✓ 而不是"让它能触发" ✗

让它真能触发 = 比较**不同场景**的地点 ⇒ 那是**另一条判据** ✗（跨场景换地点在剧里完全正常 ✓
—— 该由**转场动机**管 ✓，已在 `continuity.py` §11 ✓）⇒ 属**改语义** ⇒ 不擅自做 ✓。
删掉的依据：**Node 已删 ⇒ 不再需要对拍** ✓，且**分数不变**（它从来没触发过 ✓✓）。

留下的两条**真**判据（写进注释 ✓）：
① 「关联场景未锁定地点 ID（`location_id` 为空）⇒ −5」✓（本函数内 ✓）；
② `services/continuity.py` §6 场景空间体检（含**越轴** ✓）。

### ⭐ 顺带补一条**正对照**（否则容易误会成"地点完全不查了" ✗）

新增用例：场景 `location_id` 为空 ⇒ `continuity = 95` ✓ + 文案点名「未锁定地点 ID」✓。

### ⚠️⚠️ 但这条正对照**自己先红了两次** ✗✓ —— 两次都是**夹具共享状态**踩的坑

1. 第一版放在**同集**（编号 4）⇒ 它的上一镜（3 号）没填状态 ⇒ 额外命中
   「相邻镜头缺少 start/end_state ⇒ −5」✗ ⇒ 实测 **90** 而不是 95 ✓；
2. 我把 3 号补上状态 ⇒ 4 号挤进兄弟序列 ⇒ **改掉了 9 号镜的"上一镜"** ✗✗ ⇒
   把**另外两条**接线用例弄红（期望 93、实测 95 ✓）；
3. 最终解法 ✓：对照镜放到**另一个集**（`ep2` ✓，无兄弟可比 ⇒ 零波及 ✓✓）+
   给 `ep2` 也补一条 `continuity_states`（否则命中「本集未保存跨镜状态 −5」✗）。

> **规则**：**测试夹具是共享状态** ✗ —— 加一行夹具要问「**谁会把这一行当成自己的上下文**」✓
> （上一镜 ✓ / 兄弟排序 ✓ / 计数 ✓）。**只想测一件事，就给这件事一个独立的容器** ✓
> （独立集 / 独立项目 ✓），别在同一条时间线上插队 ✗。

**实测**：`qc_scoring_test` **29 → 31/31** ✓；`smoke_test` **476/476** ✓✓；
`qc_report` 42/42 ✓；`consistency_qc` 32/32 ✓；`technical_qc` 43/43 ✓；
`route_parity` 251 已注册 / 未注册 **0** ✓。

## S7 第 106 步：**接上一个"什么都不做"的工具** ✓ —— `assign_rhythm_phases` 空实现（34/34 ✓）

本步是「**换一种扫法**」的直接收益 ✓：不再一处一处碰运气 ✓，而是把后端里所有
**「永不 / 恒为 / 空转 / 未实现 / 尚未 / 死分支」**类标记**一次挖出来** ✓ 逐个判真伪 ✓
（`app/**/*.py` 命中 **66 处** ✓）⇒ 第一条就是真货 ✗✓。

### 病：**对外宣告了工具，实际什么都不做** ✗✗

`agent/tools/storyboard_tools.py` 里：``assign_rhythm_phases(episode_id)`` ⇒ **空实现** ✓
（docstring 写「尚未移植」✗），而 `services/rhythm_phase.py` 的实现**早就在** ✓ —— 而且
**还带自己的套件** ✓（`rhythm_phase_test.py` **22/22** ✓，含相位阈值 / 累计占比 / 无时长回退 ✓）。
⇒ agent 每次 `save_storyboards` 都"分节奏相位"✓，**而 `storyboards.rhythm_phase` 一直是空的** ✗✗。

> 这类比"函数报错"更坏 ✓：报错会有人看见 ✓，**静默不做**只有查库才发现 ✓。

### 修：**委托**那份实现（判据只留一份 ✓）

* `assign_rhythm_phases(conn, episode_id)` ⇒ 直接调 `rhythm_phase.assign_rhythm_phases` ✓
  （在**同一个事务内**调 ✓ —— 刚插入的分镜要能被它读到 ✓）；
* 写入条数进**任务日志**（`rhythmPhases` ✓）—— 不塞进回执 ✗（回执形状有严格断言 ✓）；
* 模块 docstring 的「尚未移植」段改成 ✅ 并写明原委 ✓。

### 证明：直接查**库里的列** ✓（不看回执 ✗）

自检新增 `phases = [r.rhythm_phase for r in rows]` ⇒ 断言 **全非空 ✓ + 首镜恒 `setup` ✓
+ 三镜不止一个值**（⇒ 真按占比分了 ✓）。空实现会让这条**立刻红** ✓ = 信号灯 ✓。

**实测**：`agent_storyboard_tools_test` **33 → 34/34** ✓；`rhythm_phase_test` **22/22** ✓；
`agent_runtime_test` 50/50 ✓；**`smoke_test` 476/476** ✓✓；
`route_parity` 251 已注册 / 未注册 0 / 漂移 **0** ✓；层级 6/6 ✓。

### 那 66 处标记里还剩几处待判（**本轮未动** ✗）

* `agent/runtime.py`：**Gemini 函数调用循环未支持** ⇒ **显式报错** ✓（这是**对的**做法 ✓ 不是病 ✓）；
* `services/ffmpeg_compose.py`：不支持 `subtitles` 滤镜时「**只跳过烧录、不报错**」✗ —— 待判 ✓；
* `services/image_generation.py`：「循环蒸发 ⇒ `status=processing` **永久卡死**」✗ —— ✅ **判过：不是病** ✓
  （docstring 在解释**根因** ✓，而且**真有恢复机制** `recover_image_tasks_on_startup` + 60 分钟孤儿判失败 ✓）。

## S7 第 107 步：**给守卫补上它自己承认的盲区** ✓ —— 词表镜像「值」逐字守（0 漂移 ✓）

新扫法（去**测试文件**里找「永不 / 不覆盖 / 靠人工核对」这类**自我声明** ✓）扫到一条好料 ✗：
校验 `prompt_utils` 词表的守卫**自己写着盲区** ✓ ——「**不覆盖**跨行拼接的常量（`VISUAL_STYLE_VIDEO`）
与映射表里的**值**，那两块**靠人工核对**」✗。

### 先取证：那两块到底能不能守 ✓（不猜 ✗）

写了一次性探针 ✓（跑完即删 ✓ 已核 ✓）：

* **盲区 1 是误记** ✗：`VISUAL_STYLE_VIDEO` / `VIDEO_MOTION_BASE` / `IMPERFECTION_ANCHORS` /
  `QUALITY_TAIL` 四个**都抽得准、且与 Python 逐字相同** ✓ ⇒ 它们只是**没被列进 `expected`** ✗
  —— ⭐ **「没被断言」≠「断不了」** ✓；补进去 ≈ **白捡的覆盖** ✓；
* **盲区 2 是真的** ✗：`re.findall(r"'key': '([^']*)'")` 只吃到多行值的**第一行** ✓✗ ⇒
  10 条里只抽到 **1** 条，而且抽到的那条是**残值** ✓（后半句没了 ✓，**看起来却像对的** ✓✗）。

### 修

* `expected` 补上那 4 个常量 ✓；
* 新增 `_extract_ts_object_string_values()` ✓：与常量抽取器**同款**的「引号拼接 + 续行」逻辑 ✓，
  对 `DRAMA_ART_STYLE_MAP` / `DRAMA_ART_NEGATIVE_MAP` **逐条比值** ✓
  ⇒ 顺带把**负面表的 key** 也守上了 ✓（此前**只**守了正表的 key ✗）；
* ⭐ **检查器自检**：抽到的条数必须与 Python 表**一样多** ✓ ⇒ 否则报「抽取器漏了」✗
  —— **别把「没抽到」当成「没漂移」** ✓。

### ⚠️ 两个自己踩的坑（都当场现形 ✓）

1. 抽取器初版**漏了「值在下一行」这一形态** ✗（正表就是这写法 ✓：`realistic:` ↓ `'...' +`）
   ⇒ 正表抽到 **0** 条、负面表 **10** 条 ✓✓（👈 **这个不对称本身就是线索** ✓）；
2. 修的时候又把**半角引号**写进中文文案 ✗ ⇒ f-string **直接语法错误** ✓（**第 10 次** ✗）——
   规则就写在 `MEMORY.md` 头一条 ✓，**还是犯了** ✓；跑一次就能现形 ✓。

**实测**：`route_parity` ⇒ **251 已注册 / 未注册 0 / 镜像常量漂移 0** ✓
（含**两张表的全部值**逐字比对 ✓）；层级 6/6 ✓；探针已删 ✓。

> **规则**：**守卫写"不覆盖 X"时，先验一遍 X 到底能不能覆盖** ✗ —— 本次验出来一半是**误记**
> （只是没写进断言 ✓）、一半是**真盲区**（正则只吃第一行 ✓）。
> 并且：**抽取器必须能自证覆盖** ✗（条数对不上就报错 ✓），否则"没抽到"会被读成"没漂移" ✓。

## S7 第 108 步：**用户立了一条长期约定** —— 「自研优先 / 不对外依赖」✓（已入 `MEMORY.md`）

**用户原话**（2026-09-20）：「**所有功能不要对外依赖，自己实现所有的功能，要参考我给你的几个项目**」✓

按本仓规矩 ✓：**先核准现状再写进记忆** ✗（不凭印象 ✓）⇒ 实测出三件事：

1. 自研引擎 `app/services/engine/` **19 个模块** ✓、自检 **9 套 / 343 用例全绿** ✓（core 26 / dit 26 /
   inventory 34 / io 39 / loader 28 / mappings 17 / pipeline 98 / segments 54 / text 21 ✓）
   且**零依赖可跑** ✓ ⇒ **能力已经在自己手里** ✓；
2. ⚠️ 但 `torch_backend.PENDING_PARTS`（**对外宣告**的"仍未实现"清单 ✓，被 `describe()` 与自检引用 ✓）
   **过期了** ✗ —— 它写着「文本编码器前向 ✗」「VAE 解码与出片落盘 ✗」✓，而 `TorchBackend` 里
   `encode_text` ✓ / `attach_text_encoder` ✓ / `decode` ✓ / `attach_vae` ✓ / `write` ✓ **全都在** ✓，
   `engine_io_test`(39) / `engine_text_test`(21) 也已跑通「TE → DiT → VAE → **真 mp4**」✓✓
   ⇒ 已按实测更正 ✓（**真正缺的只有「真权重 + 真配置」** ✓ ⇒ `canGenerate=False` 依然正确 ✓，
   但**不能再把原因归到机制上** ✗）。**这是第 106 步「宣告 vs 实际」那一族的第 2 例 ✓。**
3. 四类生成的**本地实现都已有** ✓：LLM → ollama ✓（原生 `/api/chat` ✓）、图片 / 视频 → 自研引擎 ✓
   （缺真权重 ✗）、TTS → 本地 CosyVoice 薄封装 ✓ ⇒ ⭐ **要做的不是"造能力"、而是"让默认配置走本地链路"** ✓
   （外部队商适配器保留为**可选通道** ✓，但不能是唯一出路 ✗）。

### 落进记忆的形式

`MEMORY.md` 新增 §⭐ 自研优先（用户 2026-09-20 明确）✓：三条硬约束（**能力自研** ✓ / **不对外依赖** ✓ /
**参考 `reference/` 那几个项目并把有用的搬来接线** ✓）+ 边界（**功能/能力不外包** ✗；`ffmpeg` / SQLite /
标准库这类**本地基础设施**不算"对外依赖" ✓ —— 自己实现一个 H.264 编码器不在合理范围 ✗）
+ 现状指针 ✓；明细下移 `TOPICS.md` §自研引擎现状 ✓（腾 8k 预算 ✓）。

> **规则**：用户立"长期约定"时，**先实测现状再写** ✗ —— 否则记下来的会是**愿望** ✓ 而不是**事实** ✓
> （本次就顺手抓出一条过期清单 ✗）。

## S7 第 109 步：⭐⭐ **用户加严约定** —— 「所有都要自己实现，不要调用外部的」⇒ 一查就见血 ✗✓

**用户原话**（2026-09-20 追加）：「**所有都要自己实现，不要调用外部的**」✓
⇒ 与上一步的「别只靠外部」相比**加严了**：外部队商 API **不是"可选通道"** ✗，是**要去掉的** ✓。

### 按规矩先核准现状（不凭印象 ✓）—— 一查就见血 ✗✓

**真库 `data/drama.db`（只读 ✓）** 的 `ai_service_configs` 共 **8 条 active** ✓：

| type | provider | base_url | 外部? | priority |
|---|---|---|---|---|
audio / image / video / text | minimax、volcengine ×3 | `api.minimax.chat`、`ark.cn-beijing.volces.com` | **外部 ✗** | **200 / 200 / 200 / 300** |
text / image / video / audio | openai(=ollama) / local-sd / minimax / cosyvoice | `localhost:11434` / `:7860` / `:8765` / `:9880` | 本地 ✓ | 85 / 84 / 83 / 82 |

**取用口径（决定性 ✓）**：`services/ai_providers.py:87`
`rows.sort(key=lambda r: r.priority or 0, reverse=True)` ⇒ **priority 越大越优先** ✓
（⚠️ `estimate_service.py` 是**另一套**：`is_default` 先、再 priority 降序 ✓ 别混 ✓）

⇒ ⭐⭐ **四类（文本 / 图片 / 视频 / 音频）当前都会选中「外部」那一行** ✗✗ ——
即：**今天真跑一次生成，请求会打到 `api.minimax.chat` / `ark.cn-beijing.volces.com`** ✓✗
（本地行全都**更低优先** ✓ 只是摆设 ✓）。

### 为什么会长成这样（设计层面的成因 ✓）

`ai_configs.LOCAL_PRESET_SERVICES` 的本地预设 priority = **85/84/83/82** ✓，
而 `PRESET_SERVICES`（厂商一键配置）是 **100/99/98/97** ✓ ⇒ **本地预设天生排在厂商之后** ✗
⇒ "先一键配置、再切本地"之后，**厂商行仍然在、而且更高** ✓✗。

### 交付

* `MEMORY.md` §⭐ 自研优先 加严 ✓（写明"外部不是可选通道" ✗ + 一条 ⚠️⚠️ 实测结论 + 指针 ✓）；
* `TOPICS.md` 新增 §**外部调用审计** ✓：8 行表 ✓ + 取用口径与代码行号 ✓ + **三层依赖阶梯** ✓
  （① 外部厂商 ✗✗ 要去 → ② 本地第三方运行时 ⚠️ ollama / SD-WebUI / ComfyUI / CosyVoice → ③ **完全自研** ✓ `engine/` ✓）
  + **动作按代价排序** ✓（抬 priority ✓ / 置 `is_active=0` 或删行 ✓ / 改预设优先级 ✓ / 长期换自研引擎 ✓）。

> ⚠️ **本轮没有动真实库、也没有改默认值** ✗ —— 那是**改产品行为** ✓（且要写用户数据 ✓）⇒ 需用户点头 ✓。
> **规则**：用户立"要去掉 X"这类约定时，先查「**X 现在到底会不会被执行**」✗ ——
> 本次查出来是「**会，而且四类全都走 X**」✓✗，比"以为只是可选"严重得多 ✓。

## S7 第 110 步：⭐ **执行「尽量本地」** ✓ —— 四类生成切本地 + 停用 4 条外部行（可逆 ✓）

用户在上一轮批准「**可以按你说的尽量本地**」✓ 并点名了**可照抄项目** ✓ ⇒ 本轮动手 ✓。

### ① 可照抄项目清单（用户 2026-09-20 点名 ✓）

`reference/ComfyUI`、`reference/minimax-h3-comfyui`、`reference/ollama`、`reference/ollama-python`、
`reference/minimax-desgin-plugin` ⇒ **功能可以直接抄** ✓。已落 `TOPICS.md` §可照抄项目清单 ✓
（逐条对「提供什么 → 抄进本仓哪里」✓）：
`ComfyUI` = 节点图执行 / 校验 / 队列 / 进度这套**机制** ✓（⚠️ **别整包搬** ✗ 十万行级 ✓）；
`minimax-h3-comfyui` = H3 节点与 T2V/I2V 工作流 ✓（对照 `engine/` 的 dit/conditioning/sampler/pipeline 补齐 ✓）；
`ollama` = 本地模型服务形态 ✓；`ollama-python` = 工具 schema / 结构化输出 ✓（第 85 步已判：本项目**已有等价件** ✓）；
`minimax-desgin-plugin` = 节点注册 + 参数校验 ✓。
⚠️ 用户末句「…不要玩家」像是**打字串了** ✗ ⇒ 我按「可照抄」记录 ✓ 并已请其纠正 ✓。

### ② 代码侧：本地预设**抬到厂商之上** ✓

`ai_configs.LOCAL_PRESET_SERVICES` priority **85/84/83/82 → 503/502/501/500** ✓
（取用口径是 **priority 降序** ✓；厂商预设 100/99/98/97 ✓、历史遗留外部行最高 300 ✓ ⇒ 500+ 留足余量 ✓）。

### ③ 数据侧：真库切本地 ✓（可逆 ✓）

`tmp/local_only.py`（**跑完即删** ✓；快照留 `tmp/ai_configs_before.json` / `after.json` ✓ —— 要回滚照 before 写回 ✓）：

| 类型 | 改后选中（**按 `ai_providers` 口径逐类复算** ✓） | priority |
|---|---|---|
text | `openai` @ `http://localhost:11434`（其实是 ollama ✓） | 503 |
image | `local-sd` @ `http://localhost:7860` | 502 |
video | `minimax` @ `http://localhost:8765`（H3 门面 ✓） | 501 |
audio | `cosyvoice` @ `http://localhost:9880` | 500 |

**已停用（`is_active=0` ✓ 行还在，可一键恢复 ✓）**：`api.minimax.chat`（audio / text ✓）、
`ark.cn-beijing.volces.com`（image / video ✓）—— 共 **4 条** ✓。

### ④ 顺手排掉一个会立刻绊倒的雷 ✓

切完本地后我马上查了 `agent_configs` ✗（怕 5 个 Agent 钉着**厂商模型名** ✓ ⇒ 本地 ollama 会被要一个不存在的模型 ✓）：
实测 **5 个 Agent 的 `model` 全是 `NULL`** ✓✓ ⇒ 一律走 `ai_configs` 解析出的模型 ✓ ⇒ **不需要改** ✓
（⚠️ 这类"切完 provider 但 model 还钉着别家"的坑，值得每次都查 ✓）。

### ⚠️ 语义变化（必须让人知道 ✓）

现在**不再有外部兜底** ✓ ⇒ 本机若 `ollama(11434)` / `SD-WebUI(7860)` / `H3(8765)` / `CosyVoice(9880)`
任一**没在跑** ⇒ 对应那类生成会**直接失败** ✓✗ —— 这正是「不要调用外部的」的语义 ✓。

**实测**：`route_parity` **251 已注册 / 未注册 0 / 镜像常量漂移 0** ✓（改 priority **不触发**镜像守卫 ✓
—— 预设优先级不在镜像清单里 ✓）；层级 6/6 ✓；迁移脚本已删 ✓（快照 `tmp/ai_configs_before.json` / `after.json` 都在 ✓）。

### 补记：用户澄清末句 ✓

用户重发了一遍，末句是「你记住这几个项目**不要忘记**」✓（上一轮我猜的是"打字串了"✓、按"可照抄"记的 ✓ —— **猜对了** ✓）。
⇒ 已把措辞改准 ✓（`MEMORY.md` 该条 + `TOPICS.md` §可照抄项目清单 都写明「不要忘记」✓），并定性为
**长期挂账的待办源** ✓：**能力缺口优先从这五个项目里找现成实现** ✓。
⚠️ 另：`MEMORY.md` 本轮又一度 **8009 > 8000** ✗ 被守卫拦下 ✓ ⇒ 已压缩（画风那条旧指针说明压缩 + 该条精简 ✓）。

## S7 第 111 步：⭐⭐ **从点名项目里挖出 H3 全套事实** ✓ —— 并**当场证伪了自己一条假设** ✗✓

按「不要忘记」✓ 开工，第一站选**离"自己出片"最近**的 ✓：`reference/minimax-h3-comfyui`。

### ⚠️ 第一件事就是**纠偏** ✗✓

探查子代理一挖：`reference/minimax-h3-comfyui` **只有 2 个 workflow JSON + README + 1 个 shell** ✓
——**0 个 `.py`** ✗ ⇒ 我们缺的 DiTConfig / 张量名 / VAE·TE 结构**都不在那里** ✓；
真身全在**隔壁 `reference/ComfyUI`** ✓（工作流 JSON 里节点 `cnr_id: "comfy-core"` ✓）。
另确认：本仓 `local_services/h3/workflows/*.json` 与它**同源原样复制** ✓（同名同字节 ✓），
而 `comfyui_client.py` / `workflow.py` / `server.py` 三个是**本仓自研** ✓（上游没有 ✓）。

### 挖到的**事实**（逐条带 `文件:行号` ✓ 不是猜的 ✗）⇒ 已落库

| 事实 | 落点 |
|---|---|
DiT：hidden **5376** / depth **50** / heads **56** / headDim **128** / ffn **14336** / patch **(1,2,2)** / video 潜 **24ch** / audio **32ch** / text_dim **5120** / time embed **256→5376→2688** / eps **1e-5** / rope 16→96 / token_refiner **2 层** | `engine/dit.py` → **`H3_SHAPE_FACTS`** ✓ |
**vae_scale 16** ✓；帧网格 `17k+5 ↔ 5k+2` ✓；`audio_t = round(sec*40)` ✓；条件时间步 `visual 0.999 / audio 1.0` ✓ | 事实已记 ✓（接 `geometry`/`vae`/`conditioning` 下一步 ✓） |
采样默认：steps **10** / scheduler **simple** / denoise **1.0** / sampler **res_multistep** / **无 CFG** ✓；shift **video 12.0 / audio 3.0**（⇒ audio_scale **4.0** ✓） | 事实已记 ✓（落 `schedules` 下一步 ✓） |
**键名**：`video_patch_proj` / `audio_patch_proj` / `condition_proj` / `rope.inv_freq` / `final_layer.{video_out,audio_out}` / `blocks.N.{attn.qkv_proj, attn.{q,k}_norm, mlp.fc1/fc2, adaln_proj.linear}` / `token_refiner.blocks.N` ✓ | `engine/mappings.py` → **`PRESETS["minimax-h3"]`** ✓ |
**TE→DiT 喂法**：Qwen3-VL-32B 第 **50 层未归一 hidden [B,L,5120]** → `condition_proj` → `token_refiner` → **与 video/audio 潜 token 拼进同一条序列**（⚠️ **不是 cross-attention** ✗）、per-token 模态 tag（text=1/video=0/audio=2 ✓）、**无 attention mask** ✓ | 事实已记 ✓（落 `conditioning`/`dit` 下一步 ✓） |

### ⭐⭐ 交叉核对数字时**当场证伪了自己一个默认假设** ✗✓

我原以为「H3 的 hidden/heads 能直接填进 `DiTConfig`」✓ —— 一算：**56 × 128 = 7168 ≠ 5376** ✗✗
⇒ H3 走的是 **`qkv_proj` → `out_proj`**（attention inner dim ≠ hidden ✓），
而本仓 `DiTBlock` 用 `nn.MultiheadAttention(hidden, heads)` ⇒ 推出来 head_dim = **96** ✗
⇒ **真权重 `qkv_proj`(21504×5376) 与本仓 `in_proj_weight`(16128×5376) 形状根本不同** ✗。
⇒ 于是把「**把 H3 数字填进去**」与「**能装 H3 权重**」**明确切开** ✓：
`DiTConfig(hidden=5376, depth=50, heads=56, …)` **能构造** ✓ 但**装不上** ✗ ——
5 处结构差异逐条写进 **`dit.H3_STRUCTURAL_GAPS`** ✓（注意力维度 ✓ / **双输出 video+audio** ✗ /
adaLN **18 路 vs 6 路** ✓ / 缺 `token_refiner`+`condition_proj` ✓ / 文本拼同序列 + 模态 tag ✓），
并**进了 `PENDING_PARTS`** ✓ ⇒ 以后没人会以为「填完数字就完事」✓。

> **规则**：**抄参数时要交叉验算**（56×128 vs 5376 这种一步就算得出来的关系 ✓）——
> 抄来的数字**能构造** ≠ **能装载** ✗；混在一起就会得到「名字对、形状错」的模型 ✓✗（比不填更坏 ✓）。

### 实测

`engine_mappings` **17 → 20/20** ✓（H3 预设：**无合并规则** ✓ / 前缀 + `qkv_proj` ✓ /
本仓**没有对应物**的键**留原样并在 note 点名** ✓）；
`engine_dit` **26 → 29/29** ✓（事实自洽 56×128=7168≠5376 ✓ / 数字能构造 ✓ / **缺口清单非空** ✓）；
`engine_pipeline` 98/98 ✓；`engine_loader` 28/28 ✓；`engine_inventory` 34/34 ✓；
`route_parity` 251 已注册 / 未注册 0 / 漂移 **0** ✓。
⚠️ 自检前我又犯了**半角引号** ✗（**第 11 次** ✓）⇒ `compile()` **一行定位**（86 行 ✓）→ 修 ✓。

## S7 第 112 步：⭐ **拆掉「装不上真权重」的第一道门槛** ✓ —— DiT 支持 H3 的注意力形状（37/37 ✓）

上一步抄回来的事实里，第 ① 条最硬 ✗：H3 是 **56 头 × 128 维 = 7168**，而 **hidden 只有 5376** ✓
⇒ `nn.MultiheadAttention(hidden, heads)` 只会推 head_dim = **96** ✗ ⇒ **形状根本对不上** ✗✗。本步拆掉它 ✓。

### 交付（**向后兼容** ✓：默认路径一字不改 ✓）

| 改动 | 内容 |
|---|---|
`DiTConfig.attn_dim`（新 ✓） | 默认 **0 = 与 hidden 相同** ✓（= 旧行为 ✓，仍走 MHA、键名 `attn.in_proj_weight` 不变 ✓）；非 0 且 ≠ hidden ⇒ **显式** `qkv_proj`(hidden→3·attn_dim) + `out_proj`(attn_dim→hidden) ✓ —— **H3 形态** ✓（5376→3×7168 ✓、7168→5376 ✓） |
新属性 ✓ | `inner_dim`（= attn_dim or hidden ✓）、`attn_head_dim`（H3 ⇒ **128** ✓）；`to_dict`/`from_metadata` 都带上 ✓ |
`DiTBlock` | 两种形态按 `attn_dim` 分流 ✓；显式形态用 `scaled_dot_product_attention` ✓、**qkv 按 [q\|k\|v] 三段切** ✓（与 H3 检测器"按 `qkv_proj` 形状÷(3·head_dim) 反推头数"的口径一致 ✓） |
⚠️ 两处**如实拒绝** ✗ | ① 显式形态**不做 cross-attention** ✗ ⇒ **构造时**就拒绝 `attn_dim≠hidden` + `cross_attention=True` ✓✓；② H3 的 `q_norm`/`k_norm` **暂未实现** ✗（注释写明"先不假装有" ✓） |

### ⚠️ 自检又一次**先红后教** ✗✓

我写的「给了 `context` 就该报错」那条**红了** ✓ —— 原因很有意思 ✗：
`DiT.forward` 在 `cross_attention=False` 时**根本不把 context 往下传** ✓ ⇒ 整模型层面**到不了**那条守卫 ✓。
⇒ 改成**两处更早/更靠内**的守卫 ✓：**构造时**拒绝自相矛盾的组合 ✓ + block 内层也报错（双保险 ✓），
并把「context 被忽略」这个**事实**单独钉成一条 ✓（不报错也不使用 ✓ ⇒ 必须在配置里说清楚 ✓）。

**实测**：`engine_dit` **26 → 37/37** ✓（新增：H3 尺寸**只构造不建模型** ✓ / `attn_head_dim==128` ✓ /
默认形态不变 ✓ / 显式形态形状 = 缩放版 H3 ✓ / context 拒绝 ✓）；`engine_mappings` 20/20 ✓；
`engine_pipeline` 98/98 ✓；`engine_core` 26/26 ✓；`engine_io` 39/39 ✓；`engine_text` 21/21 ✓；
`engine_segments` 54/54 ✓；**`smoke_test` 476/476** ✓✓（默认路径没被碰 ✓）；`route_parity` 漂移 **0** ✓。

⚠️ 半角引号这回**连犯两次** ✗（第 12、13 次 ✓）⇒ 已按自己立的规矩**先 grep 再改** ✓，
并得到更准的判据 ✓：**grep 命中里绝大多数是 docstring/注释（无害 ✓）** ⇒ 真正要看的只有
**落在真字符串里的那些** ✓；最快仍是用 **`compile()` 一行定位** ✓（grep 只用来确认"是不是只有一组" ✓）。

## S7 第 113 步：⭐⭐ **关掉 H3 缺口 ② 与 ④** ✓ —— DiT 现在**键名逐字对齐 H3**（42/42 ✓）

接着第 112 步（① 注意力维度 ✓）继续 ✓，这一轮关 **② 双输出** + **④ `condition_proj`/`token_refiner`** ✓
—— 两条都是**加法式** ✓（各自 opt-in ✓，默认路径**一字不改** ✓）；③ adaLN 18 路 + ⑤ 文本拼同一序列
要一起做 ✗ ⇒ 单独留给下一步 ✓。

### 交付（新增两个配置字段 ✓，各自带来 H3 对应的**键名** ✓）

| 字段 | 作用 |
|---|---|
`audio_latents`（新 ✓，默认 0 ✓） | 非 0 ⇒ H3 双输出形态 ✓：`final_layer.video_out`（hidden→pT·pH·pW·in_channels ✓）+ `final_layer.audio_out`（hidden→`audio_latents` ✓）；`forward` 返回 **(视频, 音频)** ✓（旧形态仍返回**单张量** ✓ —— 不静默换形状 ✗） |
`text_refiner_layers`（新 ✓，默认 0 ✓） | 非 0 ⇒ H3 文本侧 ✓：`condition_proj`（text_dim→hidden ✓）+ `token_refiner.blocks.N`（每层**自注意力** ✓，H3 是 2 层 ✓） |
⚠️ **构造期守卫** ✓ | `audio_latents>0` 但不给 `attn_dim` ⇒ **直接报错** ✓（拒绝「**半套 H3**」✗ —— 那种模型装不上真权重 ✓） |

### ⭐ 缺口清单改成「**只减不骗**」✓（这条比代码更值钱）

原来 `H3_STRUCTURAL_GAPS` 把 5 条一律列着 ✗ —— 第 112 步关掉 ① 之后**它还挂着 ①** ✓✗。
⇒ 现在拆成两份 ✓：`H3_STRUCTURAL_GAPS`（**还剩 3 条** ✓：adaLN 18 路 ✓ / 文本拼同一序列 + 模态 tag ✓ /
`q_norm`·`k_norm` ✓）+ `H3_GAPS_CLOSED`（**已关 3 条** ✓，每条注明**靠什么关的** ✓）。
⚠️ 并在已关那份里写明「**关掉 ≠ 核过真权重**」✗：② 的音频头与 ④ 的 refiner 内部细节都是**可装载的近似** ✓
（H3 真形态的音频是**与视频 token 拼在同一条序列**上出的 ✓）—— 不假装等价 ✓。

### ⚠️ 自检又抓到我一次**期望写错** ✗✓（41/42）

`video_out` 我按 H3 的写到 **(96, 32)** ✗，实测 **(16, 32)** ✓ —— 因为**缩放版**是
`patch 1×2×2 × in_channels 4 = 16` ✓，而 H3 是 `2×2×24 = 96` ✓ ⇒ **同一个公式** ✓✓。
（和第 111 步"把 H3 数字直接套进缩放配置"是**同一类错** ✓ ⇒ 记下：**换比例时每个数字都要重算，不能照搬** ✗。）

**实测**：`engine_dit` **37 → 42/42** ✓（键名对齐 ✓ / 形状=缩放版 H3 ✓ / 双输出元组 ✓ /
关掉即回旧形态 ✓ / 半套 H3 报错 ✓）；`engine_mappings` 20/20 ✓；**`engine_pipeline` 98/98** ✓；
`engine_core` 26 ✓；`engine_io` 39 ✓；`engine_text` 21 ✓；`engine_loader` 28 ✓；`engine_inventory` 34 ✓；
`engine_segments` 54 ✓；**`smoke_test` 476/476** ✓✓（默认路径没被碰 ✓）；`route_parity` 漂移 **0** ✓。

## S7 第 114 步：**把 H3 规格读全** ✓ —— 缺口从 5 条补到 **7 条**（**没记 ≠ 没有** ✗✓）

接第 113 步的下一步（③ adaLN + ⑤ 打包序列 ✓）。按本仓规矩**先取证** ✓ ⇒ 去读
`reference/ComfyUI/comfy/ldm/minimax/model.py` **读全** ✓ + 它的单元测试 ✓ —— 结果**打脸** ✗✓：
我以为「只剩 3 件 ✓」，实际 **7 件** ✗。

### ⭐⭐ 最值钱的一条：**清单没写全 = 下一步照着假前提开工** ✗

先前只记了 5 条（第 111 步从**片段**里核出来的 ✓）⇒ 读全之后才发现还漏着：

* **RoPE**：3 轴 ✓ partial split-half ✓ 坐标**按面积归一化**（`_axis_from_sqrt_area` ✓ ×32 ✓ endpoint=False ✓）
  —— H3 **没有位置表** ✗，而我实现的是**学习式** `pos_embed` ✗；
* **正弦时间嵌入** `TimeEmbedder(freq_dim→hidden→out)` ✓（cos 在前 ✓）+ **每 token 独立 timestep** ✓
  （`t = 1 − σ` ✓ 音频另按 shift 换算 ✓）—— 我是 `Linear(1,hidden)` 单标量 ✗；
* **RMSNorm**（全模型 ✓ 含 per-head `q_norm`/`k_norm` ✓）vs 我的 LayerNorm ✗；**SwiGLU** MLP ✓ vs 我的非 SwiGLU ✗；
* **fp32 输出头** ✓ + **denoise mask**（部分去噪 ✓ per-2×2 行 ∈[0,1] ✓）+ 音频**立体声 channel-major** 打包 ✓；
* **打包序列是 2D**（`[总行, 通道×patch]` ✓ **连 batch 维都没有** ✗）、顺序 `text|cond/refs|audio|video` ✓
  （目标音频在视频**之前** ✓）、**无 attention mask** ✓、行数 = `latent_t×(lat_h//2)(lat_w//2)` ✓ / `audio_t×2` ✓；
* `adaln_proj` 是 `AdalnProj(expand=6, modalities=3)` = **18** ✓ ✓（这条原先猜对 ✓）**且按 per-token mod-row 分段** ✓。

⇒ **结论（写死在代码里 ✓）**：③⑤ **不是"小改"** ✗ —— 它要的是一个**单开的 H3 形态 forward** ✓
（`(B,N,D)` + 学习式位置 + 单标量 σ 的现有 DiT 与它**不是同一类模型** ✗）。

### 交付（这一步**故意不写代码** ✗，只固化规格 ✓）

| 项 | 内容 |
|---|---|
`dit.H3_STRUCTURAL_GAPS` | **5 → 7 条** ✓（补 RoPE / 时间嵌入 / RMSNorm+SwiGLU / fp32 头+mask ✓），并**注明**"第 114 步补全"✗✓ |
`dit.H3_PACK_FACTS`（新 ✓） | 打包规格**单独一张表** ✓（与形状数字分开 ✓ —— 混一起会让两类断言互相牵连 ✗）：顺序 ✓ / 行数公式 ✓ / 时间跨度表 ✓ / 模态 tag ✓ / **无 mask** ✓ / σ 换算 ✓ |
`schedules.time_shift_sigma`（新 ✓） | σ 跨 shift **闭式换算** ✓（自己推导 ✓：`b = σ/(s+σ(1−s))` ✓）；⚠️ 它**有真消费者** ✓（`schedules` 已被 pipeline 用 ✓）⇒ 符合"没人调用不算功能"✓ |
⚠️ **没建打包模块** ✗ | 打包层目前**没有调用方** ✗ ⇒ 按本仓判据**先不建** ✓（等 H3 形态 forward 落地再建 ✓ —— 免得又攒一个"零调用库" ✗） |

### ⚠️ 自检又抓到我一次**方向记反** ✗✓（核心套件 30/31）

我断言 12→3 会把 σ **「抬向 1」** ✗，实测 **0.5 → 0.2** ✓（**压低** ✓）。正确理解：
同一份噪声水平，**越小的 shift 对应越小的 σ** ✓ ⇒ 换算是「换刻度」✓，不是「变噪/变净」✗。
⇒ 断言改写成**能自己算出来的**性质 ✓（恒等 ✓ / 两个不动点 ✓ / 单调 ✓ / 往返互逆 ✓ / 区间 ✓）
+ 一条**实测锚点** ✓（0.5→0.2 ✓、0.9→0.6923… ✓）✅；**方向直觉那部分删掉** ✗。

> **规则**：**断言只写"能自己算出来的不变量"，不写"方向直觉"** ✗ —— 直觉正是出错的那一半 ✓
> （本次：恒等/不动点/往返都过了 ✓，唯一错的是我**凭感觉**写的那句 ✗）。

**实测**：`engine_core` **26 → 32/32** ✓（新增 ⑦¹…⑦⁵ + 实测锚点 ✓）；`engine_dit` **42 → 43/43** ✓
（缺口 7 条 ✓ + `H3_PACK_FACTS` 立表 ✓）；`engine_pipeline` 98/98 ✓；**`smoke_test` 476/476** ✓（改动只在 facts/日程 ✓）。
⚠️ 另：改自检时我**多打一个括号** ✗ ⇒ 编辑**直接失败** ✓（严格锚点又挡一次静默错改 ✓）。

⚠️ 操作教训（本轮踩了一次 ✓）：跑自检**忘了设 `PYTHONIOENCODING=utf-8`** ✗ ⇒ 输出乱码 + **看起来像崩了** ✗✗
（`tmp/smoke114.txt` 里那段 Traceback 其实是编码/PowerShell 层的 ✓，不是代码问题 ✓）。
⇒ 规矩：**跑自检一律带 `$env:PYTHONIOENCODING='utf-8'` + `> 文件`** ✓，读回只看 `^SUMMARY|^FAIL` ✓
（乱码时先怀疑编码 ✓，别先怀疑刚改的代码 ✗）。

## S7 第 115 步：**建起 H3 形态的积木** ✓ —— `h3_form.py`（17/17 ✓）

规格第 114 步已钉死 ✓ ⇒ 这一步按规格建**积木** ✓（**自己写** ✓，只在结构事实上对齐参考 ✓）：
`RMSNorm` ✓ / `SwiGLU` ✓ / 显式 `Attention`（qkv_proj + per-head q_norm·k_norm + **partial split-half RoPE** ✓）/
`TimeEmbedder`（正弦 ✓ **cos 在前** ✓）/ `AdalnProj`（**6 × 3 = 18** 路 ✓ 按 `mod_segments` 逐 token 分档 ✓）/
`DiTBlock` ✓ / `RefinerBlock` + `TokenRefiner`（2 层 + `final_norm` ✓）/ `FinalLayer`（双头 **fp32** ✓）。

⚠️ **它现在零调用** ✗ —— 于是**明写** `H3_FORM_TODO`（打包网格 ✓ / RoPE 频率分配 ✓ / forward 接线 ✓ /
denoise mask ✓ / 接管线 ✓）✓，并把它**登记进 `run_all.TESTS`** ✓（否则就是"没登记 = 没跑"✗）。
理由写清楚：pipeline 里那些 2D 打包 / RoPE / 每 token timestep **还没有连接点** ✗
⇒ 只交付"**能独立自检的积木**" ✓，**不假装它已经是功能** ✗。

### ⚠️⚠️ 自审抓到 **5 处**自己的错 ✗✓（其中 2 处会**静默**出错 ✗✗）

1. **占位类 `class RMSNorm` 留在模块里** ✗✗ —— 它会把 `__getattr__` 那个**真实现静默遮住** ✓✗
   （导入得到的是空壳 ✓，直到调用才炸 ✓）⇒ 删掉 ✓ ⇒ ⭐**规则：占位符不许留在模块体里** ✗
   （要"可选依赖"就用**取名字时才构造** ✓，别放一个同名空壳 ✓）；
2. **`_mod_row` 被我自己"编辑掉"** ✗✓ —— 替换 `_mod_*` 那两块时 old_str 把它**一起圈走**了 ✗
   ⇒ `compile()` **照样过** ✓，一跑到 forward 就 `NameError` ✓ ⇒ **本仓第 85 步那条规则的第三次应验** ✓
   （**改了模块级引用，必须真的 import 一次并跑到那条路径** ✓，只看语法不算 ✗）；
3. `RefinerBlock.forward` 我写成了一串**废算式** ✗（`a+x-(a+x)+…` ✗）⇒ 自审改回两行 ✓；
4. `__getattr__` **顺序反了** ✗：先 `_build_torch_parts()` 再查名字 ⇒ 问一个**不存在的名字**
   也会去 `import torch` ✗（没装 torch 时就变成 `ImportError` 而不是 `AttributeError` ✗）⇒ 已改成**先查 `__all__`** ✓；
5. 第 5 次「**代码对、我的期望错**」✗：`nn.Linear(in, out).weight` 是 **(out, in)** ✓
   ⇒ `fc2` 是 (8, 12) ✓ 而我写了 (12, 8) ✗ —— 与"方向记反"同族 ✓。

> **规则（两条，都进 `MEMORY.md` 候选 ✓）**：
> ⭐ **占位符不许留在模块里** ✗（同名空壳会**静默遮住**真实现 ✓）；
> ⭐ 本轮 5 次"代码对、期望错"全部出在**我记的形状/方向**上 ✓ ⇒
> **断言优先写"能自己算出来的不变量"** ✓（本轮：尺度不变 ✓ / 旋转保范数 ✓ / 尾部不变 ✓ /
> cos 在前 ✓ / 分档双向验 ✓ —— 这些**一次就对** ✓✓）。

### 交付与实测

| 文件 | 内容 |
|---|---|
`app/services/engine/h3_form.py`（新 ✓） | 8 类积木 + `H3_FORM_PARTS` / `H3_FORM_TODO` ✓ + `__getattr__`（无 torch 时**导入不炸** ✓，用到才 `ImportError` ✓） |
`tests/engine_h3_form_test.py`（新 ✓） | **17** 项 ✓（键名 ✓ 形状 ✓ **18 路** ✓ **cos 在前**（用 pre-hook 抠出 proj_in 输入验 ✓）✓ RoPE **保范数/尾部不动** ✓ 分档**双向**验 ✓ 双 fp32 头 ✓） |
`tests/run_all.py` | 登记新套件 ✓ + 顺手改掉 docstring 里 **82 套 / 2911 项** 的陈旧总数 ✗（改成指向 `TESTS` 为唯一权威 ✓，只留最近一次实测 94 套 / 3224 项 ✓，套数 **95** ✓ 总数**待实测**✗） |

**实测**：`engine_h3_form` **17/17** ✓（新 ✓）；`engine_dit` 43/43 ✓；`engine_core` 32/32 ✓；
`engine_mappings` 20/20 ✓；**`smoke_test` 476/476** ✓；`route_parity` 漂移 **0** ✓。

## S7 第 116 步：**把「打包层 + RoPE 角度」落成代码** ✓ —— `h3_form` 17 → **27/27** ✓

接着第 115 步的 `H3_FORM_TODO` ✓，先做里面**两条已能取证**的 ✓（打包网格 ✓ / RoPE 频率分配 ✓）。

### 先取证：把上轮标「未核 ✗」的那条钉成事实 ✓

读参考实现的 `MiniMaxH3Model`（`__init__` + `forward` 尾部 ✓）⇒ 得到：

* ⭐ **`rope_freqs` 的算法**：``per_axis = pos[:, :, None] * inv_freq``（**[S,3,F]** ✓）
  → `unbind` 成 t/h/w → **按 t,h,w 拼接** ✓ → **再复制一份** ✓ ⇒ 长度 `6F` ✓
  ⇒ ``rot_dim = 6·len(inv_freq)`` ✓（H3 默认 F=**16** ⇒ **96** ✓，作用于 head_dim **128** 的前 96 维 ✓）；
* ⭐⭐ **`inv_freq` 不是算出来的** ✗ —— 它是 `torch.empty(16)` 注册的**缓冲区** ✓ ⇒ **由检查点给** ✓
  （本仓**不自己套** `1/10000^(2i/d)` 那种通式 ✗，那会得到一个"看着像、装不上"的表 ✗）；
* `t_vals` 走**唯一时间戳** ✓，mod-row 布局 = `唯一序号 × 3 + 模态` ✓（`rows_to_mod_index(...)//3` ✓，
  与 `AdalnProj` 的 `reshape(S×modalities, -1)` **自洽** ✓）；
* 返回的 velocity **取负** ✓（`-video_out` / `-audio_out` ✓）；音频按 ``σ_a/σ_v`` 带进带出 ✓；
* ⚠️ **新发现一条结构项** ✗：`video_patch_proj` / `audio_patch_proj`（**都存 fp32** ✓）＋
  `unpatchify_video` / `unpack_audio` ✓ —— 已写进 TODO ✓（**不假装已做** ✗）。

### 交付（`h3_form` 新增 6 个纯函数 ✓，全在自己的自检里钉住 ✓）

| 函数 | 内容 |
|---|---|
`packed_rows` | 段表 `text → audio → video` ✓（**音频在视频之前** ✓ 钉方向 ✓）、行数公式 ✓、**首尾相接** ✓ |
`position_ids` | `[S,3]` 的 `(t,h,w)` ✓：text（t=0…L−1 ✓ h=w=0 ✓）/ audio（t 从 `text_len` 起 ✓ **两遍** ✓ w 取**网格两端** ✓）/ video（t 按跨度表 ✓ **一帧内共用 t** ✓） |
`frame_grid_coords` | **面积归一化**坐标 ✓（`ratio=dim/√area` ✓ ×32 ✓ endpoint=False ✓）；跨度 = `(n−1)·ratio/n·32` ✓（**按公式自算** ✓） |
`video_t_grid` | 跨度表 `(1,4,4,4,4)×5/3` ✓ 的前缀和 ✓ |
`rope_angles` | 3 轴各一份 ✓ 按 **t,h,w** 拼 ✓ **复制一份** ✓；⚠️ **先 cast fp32** ✓（事实如此 ✓，也避免混算 ✗） |
`mod_row` | `唯一序号×3 + 模态` ✓（越界报错 ✓） |

`H3_FORM_PARTS` / `H3_FORM_TODO` 同步 ✓（**只减不骗** ✓：做掉的两条从 TODO **移出** ✓，
新增的 `*_patch_proj` 等**写进去** ✓）—— 自检 ⑱ 把这件事**双向**钉住 ✓。

### ⚠️⚠️ 本轮 **7 处红全是我的期望** ✗✓（代码一次没改错 ✗）

* **`__all__` 漏加** ✗ ⇒ `AttributeError: h3_form 没有 packed_rows` ✓✓ —— ⭐ 这是**上一步刚立的**
  「先查名单」守卫**当场建功** ✓：**响亮报错**而不是静默空壳 ✓（若它返回 None，会一路漂到采样里才炸 ✗）；
* **半角引号**又犯一次 ✗（第 6 次 ✓，`SyntaxError` ⇒ `compile()` 一行定位 ✓）；
* **dtype 混算** ×2 ✗（`Double did not match Float` ✓ ⇒ 真实现本来就先 cast fp32 ✓，我少了一次 ✓）；
* **角度行数 ≠ S** ✗（3 行配 5 行 ✓ ⇒ size mismatch ✓）；
* **「笛卡尔积」当成「两列逐位相等」** ✗（×2 ✓ ⇒ 正解是**值集合相同** ✓）；
* **token 顺序**：我以为视频 t **逐 token 递增** ✗，实为**先帧后行列** ⇒ **一帧内共用同一个 t** ✓。

> ⭐ **本轮规则（已入 `MEMORY.md`）**：**判据要"响亮"不要"静默"** ✗ —— 惰性导出**必配名字名单**
> （`__all__`）且**先校验** ✓：漏加立刻点名 ✓。
> ⭐ 并补一条：**写断言时，凡"顺序 / 布局 / 形状 / dtype"都要当场算一遍** ✗
> （一天 7 处红全在「我记的形状/方向/布局」上 ✓ —— 不变量那批**一次就对** ✓✓）。

**实测**：`engine_h3_form` **27/27** ✓（17 → 27 ✓）；`engine_dit` 43 ✓；`engine_core` 32 ✓；
**`engine_pipeline` 98/98** ✓；`engine_segments` 54 ✓；**`smoke_test` 476/476** ✓；漂移 **0** ✓。
另：`MEMORY.md` 这次做了**真**下移（把两条细则挪进 `TOPICS.md` §代码约定坑清单 ✓）⇒ 体积从 7876 降到
**约 7700** ✓（⚠️ 仍接近上限 ✗ ⇒ 下轮再进内容前先看一眼余量 ✓）。
⚠️ **更正**（写日志时是估算 ✓，跑守卫后是实测 ✓）：实为 **7876 → 7848** ✗ ⇒ 只腾出 **28** 字符 ✗
（我的估算差了 **7 倍** ✗✓ —— 因为新写的那条"判据要响亮"本身就有 ~300 字符 ✓）。
⇒ 结论：**"腾预算"在没跑守卫之前都只能当估算** ✗，写进日志时要标"待实测"✓。

## S7 第 117 步：**把 H3 主干接起来** ✓ —— `h3_form` 27 → **36/36** ✓（做到"行级前向"）

`H3_FORM_TODO` 还剩 4 条 ✓ ⇒ 这一步做掉其中的**接线** ✓（`cond`/`refs` 段 ✗、mask ✗、外壳 ✗ 仍留着 ✓）。

### 交付

| 项 | 内容 |
|---|---|
`H3_DEFAULTS` | H3 出厂尺寸 ✓（5376 / 50 层 / 56×128 / ffn 14336 / text 5120 / 潜 24 · 音频 32 / t 256→5376→**2688** / F 16 / shift **12·3** ✓）—— 全部抄自参考 `__init__` 的**事实** ✓ |
`t_vals_for` | 各段 ``t = 1−σ`` ✓（audio 用**换算后**的 σ ✓）+ ⭐**去重** ✓ ⇒ `t_vals` 与「段→行号」映射 ✓ |
`mod_segments_for` | 段表 → adaLN 分段 ✓（`row = 唯一序号 × 3 + 模态` ✓、模态档 text 0 / audio 1 / video 2 ✓） |
`audio_carry` | ``σ_a/σ_v`` ✓ —— ⭐ **`schedules.time_shift_sigma` 的真消费者** ✓（那个函数终于不是"没人调用的库"✓✓） |
**`H3FormTrunk`** | 模块名与参考 `__init__` **逐字对齐** ✓（可直接装它的权重 ✓）：`video_patch_proj` / `audio_patch_proj`（**都 fp32** ✓）/ `condition_proj` / `time_embedder` / `rope`（**`inv_freq` 缓冲区** ✓ 不自算 ✗）/ `token_refiner` / `blocks` / `final_layer` ✓；行级前向 ⇒ **(视频行, 音频行)** ✓ 且**按参考取负** ✓ |

⚠️ 段长**从行数反推** ✓（`audio_t = 行/2` ✓、`latent_t = 行/每帧行数` ✓）⇒ **除不尽就报错** ✓（不猜 ✗）。

### ⚠️⚠️ 三个错，**全是"响亮的"** ✓（这次没出现静默错 ✓✓）

1. **`TokenRefiner` 少了 `final_eps`** ✗ ⇒ 一跑就 `TypeError` ✓ —— 参考里它是**独立的**
   `final_norm_eps` ✓（我省了 ✗）⇒ 补上 ✓；
2. ⭐⭐ **行号语义：同一份数据、两套编号** ✗✓ —— `mod_row` 给的是主干要的 **18 路**行号 ✓，
   而 `FinalLayer` 的 adaLN 是 **1 路**（行号 = **唯一时间戳序号** ✓）⇒ 我把 18 路直接喂过去 ✗
   ⇒ `IndexError: index 5 … size 3` ✓✓。**教训**：**跨模块传"行号/索引"时，必须在类型或命名上区分编号空间** ✗
   （`row`/`mod_row` 两个词长得太像 ✓ ⇒ 已在代码里写死注释 ✓）；
3. 我自己写的**魔法阈值** `TODO >= 5` ✗ 因"只减不骗"降到 4 条而红 ✓ ⇒ 改成判「**非空**」✓
   （真正的不变量是 ⑱ **双向**那条 ✓）—— ⭐**别用魔法阈值，用非空 + 双向** ✗。

### 自检里那条最值钱的：**反套套逻辑** ✓✓

㉔′ 同时钉两件事 ✓：**改 σ ⇒ 输出必须变** ✓ **改文本 ⇒ 输出必须变** ✓ ——
否则就是「**条件没接进去**」的假绿 ✗✗（本仓的老毛病 ✓：一个"永不生效"的接线，与"没接"长得一模一样 ✗）。
另：㉓ 直接拿 `state_dict` 的**键集合**去对参考的模块名 ✓；㉑ 钉 carry 的不变量（shift 相同 ⇒ 恒 1 ✓）；
⑳′ 钉**去重**（shift 相同时 audio/video 的 t 相同 ⇒ 只 2 行且**共用行号** ✓ ——
不去重会让行号全错**且不报错** ✗✗）。

**实测**：`engine_h3_form` **36/36** ✓（27 → 36 ✓）；`engine_dit` 43 ✓；`engine_core` 32 ✓；
**`engine_pipeline` 98/98** ✓；`engine_text` 21 ✓；`engine_io` 39 ✓；**`smoke_test` 476/476** ✓；漂移 **0** ✓。

## S7 第 118 步：**把 H3 的「外壳」与「去噪掩码」补上** ✓ —— `h3_form` 36 → **43/43** ✓

`H3_FORM_TODO` 还剩 4 条 ✓（第 117 步记的）⇒ 这一步收掉其中两条 ✓。

### 交付

| 函数 | 内容 | 钉住的不变量 |
|---|---|---|
`patchify_video` / `unpatchify_video` | 潜帧 ⇄ 行 ✓（行序 = **先帧、后行列** ✓）；`H`/`W` 传的是**网格数** ✓（参考的调用就是 `lat_h // 2` ✓） | ⭐ **往返恒等** `unpatchify(patchify(x)) == x` ✓✓（自检里**最强**的那种 ✓） |
`pack_audio` / `unpack_audio` | `[C, ch, T] ⇄ [ch·T, C]` ✓ **channel-major** ✓（先某声道的 T 帧 ✓ 再换声道 ✓） | ⭐ **往返恒等** ✓ + **逐段等于源**（声道 0 / 声道 1 ✓） |
`mask_row_values` | 去噪掩码 → **per-2×2 行** ∈[0,1] ✓；**replicate 补齐**到潜尺寸 ✓ | ⭐ **三态**：全在生成 ⇒ **`None`** ✓ / 挖一个 2×2 洞 ⇒ **恰好一行 < 1** ✓ / 比潜尺寸小 ⇒ **补齐后仍全 0** ✓ |

⚠️ 两条**语义**上的讲究（都不是随手写的 ✓）：

1. **`None` ≠ 全 1 张量** ✓ —— 「**没有掩码**」与「**掩码全是 1**」必须分开 ✓✗
   （本仓那条「**没数据 ≠ 通过**」的同族 ✓：混成一个全 1 张量，下游就再也分不清了 ✓）；
2. 行数与 **`packed_rows` 的 `video_rows` 必须同口径** ✓ —— 自检 ㉕′ **交叉验证**两处 ✓✓
   （这种"两处算同一个数"的地方，最该钉 ✓）。

另：本仓的行级表示**没有 batch 维** ✓（与 `H3FormTrunk` 一致 ✓）⇒ 外壳只收单条 ✓
（参考实现是把 batch 折进行数的 ✓，但它内部处处只取第 0 条 ✓）—— 这是**有意取舍** ✓ 已写进 docstring ✓。

### ⚠️ 又一个"形状方向"记反 ✗✓（今天第 **7** 次同类）

㉖ 里我拿 `audio_rows[:5]`（`[T, C]` ✓）去比 `latent[:, 0, :]`（`[C, T]` ✗）⇒ `RuntimeError` ✓
⇒ 加一次 `transpose` ✓。**自检当场红** ✓ —— 这正是那条规则的价值 ✓：
**凡"顺序 / 布局 / 形状 / dtype"都当场算一遍** ✗，别凭记忆 ✓（记忆已经在同一天错 7 次了 ✗）。

### 实测与守卫

**实测**：`engine_h3_form` **43/43** ✓（36 → 43 ✓）；`engine_dit` 43 ✓；`engine_core` 32 ✓；
**`engine_pipeline` 98/98** ✓；`engine_io` 39 ✓；`engine_segments` 54 ✓；**`smoke_test` 476/476** ✓；漂移 **0** ✓
（⚠️ 其中两三套是**命令被判后台**后我从落盘文件读回来的 ✓ —— 按规矩**没跑完不写"通过"** ✓，
冒烟与漂移这两条另跑了一次确认 ✓）。

**H3 形态现在的状态** ✓：积木 ✓ + 打包层 ✓ + 主干（行级前向 ✓）+ 外壳 ✓ + 掩码 ✓
⇒ **TODO 只剩 3 条** ✓：`cond`/`refs` 段 ✗、输出头 bank（PDD ✗）、**接进管线**（零调用 ✗）。

## S7 第 119 步：⭐ **把 H3 形态接进后端** ✓ —— `h3_form` 43 → **47/47** ✓（**不再是零调用** ✓✓）

按本仓判据「**没人调用的库不算功能**」✓ ⇒ 这一步把上一轮说的"接进管线"里**真能落地的那半**做掉 ✓
（另一半——**音频流**进 pipeline——`GenerationBackend` 是**单流**协议 ✗ ⇒ 那要同时扩
`init_latents`/`denoise`/`decode`/`write` ✓ ⇒ **如实留作 TODO** ✓，不硬凑 ✗）。

### ⭐⭐ 接线前**真实存在的静默错** ✗（这一步的价值就在这里 ✓）

`TorchBackend.load_weights` 原来**直接** `build_dit(config)` ✗ ⇒ 喂给它一份 **H3 权重**，
它会**静默地按 DiT 建** ✓✗ —— 表面症状只有一条：**装载报告里缺一堆键** ✓ ⇒
人**极易**读成「权重没下全」✗✗（而其实是**结构根本不是那个模型** ✗）。
⇒ 现在**先判形态** ✓（纯函数、只看**头部键名** ✓ 毫秒级 ✓ 不读大文件 ✓）：

| 交付 | 内容 |
|---|---|
`looks_like_h3_form` | **双向**判据 ✓：招牌键（`video_patch_proj` / `audio_patch_proj` / `rope.inv_freq` / `final_layer.audio_out` / `token_refiner.blocks.0.attn.qkv_proj` ✓）全在 ⇒ True ✓；**出现 DiT 招牌键 ⇒ False** ✓（防误判 ✗）；**只按文件名 / 只按大小猜都不行** ✗ |
`H3_TRUNK_DEFAULTS` / `H3_SIGMA_SHIFTS` | 出厂尺寸提到**模块级** ✓（**无 torch 也能读** ✓）且**只有一处** ✓（`H3_DEFAULTS` 由它派生 ✓ —— 两份写死迟早漂移 ✗） |
`TorchBackend.load_weights` | **先判形态** ✓ ⇒ H3 权重按 `H3FormTrunk` 建并装载 ✓、`self._form = "h3-form"` ✓；判错就**明确报错** ✓（不再静默建错模型 ✗） |

### ⭐ 集成自检（这一步真正"接上"的证据 ✓）

㉛ 走**真路**：把缩小版主干 `save_file` 成 safetensors ✓ → `TorchBackend().load_weights(path, config)` ✓
→ 断言 **`_form == "h3-form"`** ✓ + `_model` 是 **`H3FormTrunk`** ✓ + 装载报告 **complete=True** ✓
+ ㉜ 临时文件**已清理** ✓（自检不许留垃圾 ✗）—— **一次通过** ✓✓。

### ⚠️ 老毛病又犯一次 ✗（半角引号，第 **8** 次）

在 `PARTS` 里写 `"装载报告缺一堆键"` ✗ ⇒ `SyntaxError` ✓ ⇒ 复跑 `exit=1` **且没有 SUMMARY** ✓ ——
这正是"**没 SUMMARY ≠ 通过**"✓（若我只看到 `exit=1` 就当成"红了几条"，会白找半天 ✓）；
`compile()` 一行定位到第 62 行 ✓ ⇒ 改「」✓ ⇒ **exit=0 / 47/47** ✓。
另：有两次运行**输出被管道吞掉** ✗ ⇒ 按规矩**不当它通过** ✓，改**落盘再读** ✓ 才拿到确凿结果 ✓。

**实测**：`engine_h3_form` **47/47** ✓（43 → 47 ✓）；`engine_dit` 43 ✓；**`smoke_test` 476/476** ✓；漂移 **0** ✓。
**H3 形态现状**：积木 ✓ 打包 ✓ 主干 ✓ 外壳 ✓ 掩码 ✓ **后端可认可建** ✓ ⇒ **TODO 剩 3 条** ✓
（`cond`/`refs` ✗、PDD 头库 ✗、**音频流进 pipeline** ✗）。

## S7 第 120 步：音频流的**第一块** ✓ —— 把"没核过的地方"逼成**必填参数** ✗✓

接着 TODO 里最大那块（**音频流进 pipeline** ✓）。先做它前面的**两个事实** ✓。

### ⭐ 交付：`geometry` 落音频事实 + **一个必填的取整参数** ✗

* **已核实的** ✓（来源：H3 模型的**文件头说明**「stereo audio (32ch, 40 Hz) latents」✓）：
  `AUDIO_LATENT_HZ = 40` ✓、`AUDIO_LATENT_CHANNELS = 2` ✓
  ⚠️ 顺带钉一个易混点 ✗：那个 **32 是特征维** ✓（`audio_latents_dim` ✓）**不是声道数** ✗；
* **没核过的** ✗：秒数 → 音频潜帧数**怎么取整** ✓ —— 参考实现里 `audio_t` 是**从调用方的张量读**的
  （`audio_t = audio_x.shape[-1]` ✓）⇒ 「怎么算出来」那一步**我没核到** ✗
  （去找了一圈、搜太宽泛没命中 ✓ ⇒ **找不到就不猜** ✓）。
  ⇒ 按**本模块自己的纪律**办 ✓（`geometry.py` 文件头就写着：「给个看起来合理的数
  只会**在真机上错得莫名其妙**」✓）⇒ **把没核过的地方逼成必填参数** ✓：
  `audio_latent_frames(seconds, *, mode, hz=40)` ✓ —— `mode` 必须显式给 ✓（`round|ceil|floor` ✓），
  不给直接 `TypeError` ✓✅；一旦核清**只改这一处** ✓。

> ⭐ **规则（本步的要点 ✓）**：**"没核过"不许用默认值糊过去** ✗ —— 一个好的默认值会把
> 「**未核实**」伪装成「**已实现**」✓✗（本项目已经见过太多这种 ✓）；把它逼成**必填/显式**，
> 于是错误只会出现在**调用方**、而且是**当场**出现 ✓✓。

### 实测

`engine_core` **32 → 34/34** ✓（新增 ⑮′ 音频事实 ✓ + ⑮″ 三种取整 + **不给 `mode` 就报错** ✓）；
`engine_h3_form` 47/47 ✓；**`smoke_test` 476/476** ✓。

## S7 第 121 步：**把整条链串起来** ✓ —— `denoise_step`（`h3_form` 47 → **50/50** ✓）

上一轮末尾我说"下一步做 `audio_sigmas_from`"✗ —— 但转念发现那又是**一个"预备函数"** ✗
（连续第三次都是"没人调用的库"✗）⇒ 换成做**真把东西串起来**的那一个 ✓：

**`denoise_step(trunk, video_latent, audio_latent, text_states, sigma_v, …)`** ✓ ——
一次用掉 `patchify_video` ✓ / `pack_audio` ✓ / `H3FormTrunk` ✓ / `unpatchify_video` ✓ / `unpack_audio` ✓：
**潜帧进 ⇒ 两条 velocity 同形出** ✓（视频 `[C,T,H,W]` ✓、音频 `[C,ch,T]` ✓）。
⇒ 至此 H3 形态从"一堆零件"变成"**一次去噪**" ✓✓ —— 采样器只要按 σ 循环调它就行 ✓。

* ⚠️ 输出 dtype **跟随输入** ✓（主干内部是 fp32 的 `*_patch_proj`/输出头 ✓ ⇒ 这里转回来 ✓，与参考同义 ✓）；
* ⚠️ 潜帧与 patch **不整除 ⇒ 报错** ✓（不猜、也不补零 ✗）；
* ⚠️ 双流 carry（`σ_a/σ_v`）**不在这里做** ✓ —— 那是**采样器**的事 ✓（写清楚 ✓，因为"少做一次换算"**不会报错** ✗✗）。

### ⚠️ 两个错，都是**响亮**的 ✓✓（又验证了一遍"惰性导出有两道门"✗）

1. **半角引号第 9 次** ✗ ⇒ `SyntaxError` ✓（`compile()` 一行定位 ✓）；
2. ⭐ **`__all__` 加了、但返回字典忘了加** ✗ ⇒ `KeyError: 'denoise_step'` ✓✓
   （`__getattr__` 先查名单 ✓ 再取字典 ✓ ⇒ **两道门** ✗，我过了第一道栽在第二道 ✓）。
   ⇒ 幸好两道门都**响亮** ✓（若返回 `None` 就会一路漂到采样里才炸 ✓✗）。

**实测**：`engine_h3_form` **47 → 50/50** ✓（端到端 ㉝ ✓ + 端到端**反套套逻辑** ㉝′ ✓ + 不整除报错 ㉞ ✓）；
**`smoke_test` 476/476** ✓。

## S7 第 122 步：**双流采样器** ✓ —— `h3_form` 50 → **55/55**（**一次过** ✓）

先取证 ✓（双流最容易"看起来对、数值错"✗）：参考的 `CONST.calculate_denoised = x − σ·v` ✓
⇒ 与它**取负**的 velocity 自洽 ✓ ⇒ 欧拉更新式就是

    x_{n+1} = x_n + (σ_{n+1} − σ_n) · v

（等价于「先算 `x0 = x − σ·v` ✓ 再按新的 σ 重新加噪」✓ —— 两条路子是同一个式子 ✓）。
另核到一个**易混点** ✗✓：`ModelSamplingAV.audio_scale = shift/audio_shift` = **4.0** ✓ 是
**常数标志** ✓，而逐步那个是 ``σ_a/σ_v`` ✓（`audio_carry` ✓）—— **两者混了不会报错** ✗✗。

### 交付

| 函数 | 内容 |
|---|---|
`sample_dual_stream(trunk, 视频潜帧, 音频潜帧, 文本, sigmas, …)` | 视频走 `sigmas` ✓、音频走**逐步换算后**的 σ ✓（`schedules.time_shift_sigma` ✓）；带 `callback(step, σ_v, σ_a)` ✓；返回 **两条流 + 步数 + 两条 σ 序列** ✓ |
`audio_scale(shift_v, shift_a)` | `shift_v/shift_a` ✓（H3 = 4.0 ✓）；docstring 里写明与 `audio_carry` 的区别 ✗ |

* ⚠️ `sigmas` 末位必须是 **0** ✓、至少 2 个 ✓ ⇒ 否则**报错** ✓（不猜 ✗）；
* ⚠️ **刻意不做 carry** ✗ —— 本实现让每条流**待在自己的 σ 上** ✓（与 `denoise_step` 的内部口径一致 ✓），
  把"缩放带过去"那套留给需要它的调用方 ✓（写清楚 ✓，因为**做与不做都不报错** ✗✗）。

### ⭐⭐ 自检里那条**双流专属**的不变量

㊱：**两条 shift 相同时 ⇒ 音频 σ 序列与视频逐位相同** ✓（=「换算是**恒等**」的证明 ✓）；
`12/3` 时两者**必须不同** ✓，且音频那条仍**单调递减到 0** ✓ ——
这条比"跑通不报错"值钱得多 ✓：它同时证明了「换算真的在用」✓ 和「没换错方向」✓。

### ⭐ 这一轮**一次过**（前两轮的教训当真了 ✓）

上一轮记的是「惰性导出有**两道门**：`__all__` ✓ + 返回字典 ✓」✗ ⇒ 这次**三处一起改** ✓
（实现 ✓ + 名单 ✓ + 字典 ✓ + `PARTS` ✓）⇒ **没有出现 `KeyError`** ✓✓。
（另：半角引号这次也**没犯** ✓ —— 写中文引用时一律「」✓。）

**实测**：`engine_h3_form` **50 → 55/55** ✓（双流 ㉟ ✓ 回调 ㉟′ ✓ **关键不变量** ㊱ ✓
`audio_scale` 4.0 ✓ 参数校验 ㊳ ✓）。

## S7 第 123 步：**取证两个 VAE 的结构事实** ✓ —— 且**故意不扩协议** ✗

原计划是"扩 `GenerationBackend` 为双流"（第 122 步末尾的选项 ①）✗ —— 动手前想清楚一件事 ✓：
音频那一路要 `decode` 就得有**音频 VAE** ✓，而我们连它的**结构**都不知道 ✗ ⇒
**现在扩协议只会做出一个"看着接上了"的假接线** ✗✗（正是我前面反复在防的那种 ✓）⇒ 改成**取证** ✓。

### ⭐⭐ 核出来的事实（一条比一条值钱 ✓）

**视频 VAE**（`comfy/ldm/minimax/vae.py` 头部 ✓）：
* 家族 = **3D 因果 CNN 编码器 + ViT3D 解码器** ✓（**不是**常见的 UNet VAE ✗）；
* ⭐⭐ **`LATENTS_MEAN` / `LATENTS_STD` 各 24 个** ✓ —— **潜变量归一化统计** ✓
  （24 正好对上 `latents_dim = 24` ✓ ✓）；外加 ImageNet 的 mean/std ✓；
* ⚠️ 这组数字**猜不出来** ✗ —— 漏了它，画面会「**看着还行、数值全错**」✗✗，而且**不会报错** ✗
  ⇒ 又一个"必须取证、不许凑"的实例 ✓。

**音频 VAE**（`audio_vae.py` 头部 ✓）：
* 家族 = **DAC 血统编码器 + BigVGAN 解码器** ✓（**与视频 VAE 毫无关系** ✗）；
* 激活是 **Snake / SnakeBeta** ✓、带抗混叠重采样 ✓；
* ⭐ **weight-norm 已折叠**成普通 conv ✓ ⇒ 用普通 `Conv1d`/`ConvTranspose1d` 就能装 ✓（好加载 ✓）；
* ⇒ 结论：**`VideoVAE` 服务不了音频** ✗ ⇒ 音频侧要**单开**一个 ✓（还没做 ✗）。

### 交付（`engine/vae.py`）

| 项 | 内容 |
|---|---|
`H3_VIDEO_VAE_FACTS` / `H3_AUDIO_VAE_FACTS` | 上面那些事实 ✓（含 24 个数 ✓），**带"猜不出来的那一条"注记** ✓ |
`VideoVAEConfig.apply_latent_stats`（新 ✓） | 是否做潜变量归一化 ✓；⚠️ 默认 `False` 的含义**只有一个**：**本模块那套合成结构（自检用）不做归一化** ✓ —— **不是**「H3 不需要归一化」✗（那条已核实：**它就是需要** ✓）；两者别读混 ✗ |
`normalize_latents` / `denormalize_latents` | `(x−mean)/std` ✓ 与其逆 ✓；⚠️ 统计个数与潜通道**对不上 ⇒ 报错** ✓（**凑一组「差不多」的统计只会得到看着对的错图** ✗✗） |

### ⚠️ 两件小事（老规矩，如实记 ✓）

* **半角引号第 10 次** ✗ ⇒ `SyntaxError` ✓（`compile()` 一行定位到第 47 行 ✓）；
* 第一次跑验证时**命令被判后台/跳过** ✗ ⇒ 按规矩**不当它通过** ✓，改**落盘重跑** ✓ 才拿到结果 ✓。

### ⚠️ 如实说：这步**还没有落盘的自检** ✗

归一化那条路径这次是用**一次性命令**验的 ✓（结果见下 ✓）—— 按本仓规则「**没登记 = 没跑**」✗，
它**应当**补进 `engine_io_test` ✓（VAE 的套件 ✓）⇒ 已列入下一步 ✓（**不假装它已经进套件** ✗）。

```
roundtrip -> True ✓（normalize/denormalize 互为逆 ✓）
facts     -> 24 24 ✓
off       -> 透明 ✓（没开开关时原样返回 ✓）
mismatch  -> 报错 ✓（统计数 ≠ 潜通道 ⇒ 明确报错 ✓）
engine_io 39/39 ✓ ｜ smoke 476/476 ✓
```

## S7 第 124 步：⭐⭐ **实现音频 VAE** ✓ —— **真功能**（能出真 wav ✓ 12/12 ✓）

用户明确要求「**一定要实现真的功能**」✓ ⇒ 这一步不停在事实与编排上 ✗，**真的把模型写出来** ✓：
`app/services/engine/audio_vae.py`（自己实现 ✓）+ `tests/engine_audio_vae_test.py` ✓ + 登记进 `run_all` ✓。

### ⭐⭐ 先取证救了一次大错 ✗✓

我原以为音频是 **48 kHz** ✗（那是"常见值"的直觉 ✓）⇒ 读参考得到的事实是：
**sample_rate = 32000** ✓、编码 strides ``(2,4,4,5,5)`` ⇒ ``hop = **800**`` ✓ ⇒
``32000 // 800 = **40**`` ✓ —— 与 `geometry.AUDIO_LATENT_HZ = 40` ✓ **正好对上** ✓✓；
解码器 BigVGAN 的 ``upsample_rates=(5,5,2,2,2,2,2)`` **乘积也是 800** ✓ ⇒ 编解码 hop **自洽** ✓
（这条自洽本身就被我写成**构造期断言** ✓：不等就报错 ✓ —— 不等的话"解出来的时长"与
"编进去的时长"对不上 ✓✗，而且**不会报错** ✗）。

### 交付（能真跑 ✓）

| 件 | 内容 |
|---|---|
`AudioVAEConfig` | 全部事实显式 ✓（32000 / (2,4,4,5,5) / (5,5,2,2,2,2,2) / 32 通道 / 立体声 2 ✓）+ ``hop_length`` ✓ ``decoder_hop`` ✓ ``latents_per_second`` ✓ + **hop 不自洽 ⇒ 构造期报错** ✓ |
`Snake1d` / `SnakeBeta` | ``x + (1/β)·sin²(αx)`` ✓（beta 存 log 域 ✓ 事实 ✓） |
`ResBlock1` / `BigVGANDecoder` | BigVGAN 血统 ✓（每 kernel 一组 dilations ✓、组内相加、组间平均 ✓）+ **输出夹到 [-1,1]** ✓ |
`Encoder` / `AttnProjection` | DAC 血统下采样 ✓ + 自己的跨通道注意力近似 ✓（⚠️ 参考那层内部**没读** ✗，已写明**不等价** ✗） |
`decode` / `encode` | ``[B,32,2,T] ⇄ [B,2,L]`` ✓，``L = T×800`` ✓；``L`` 非 hop 整数倍 ⇒ **右侧补零** ✓（编任意长度是正常需求 ✓） |
`write_wav` | ⭐ **写真的 16-bit PCM wav** ✓（标准库 `wave` ✓ 零依赖 ✓；超范围**夹紧** ✓ 不回绕 ✗） |

### ⭐⭐ "真功能"的证明（不是"跑通不报错"✗）

* ③ ``[1,4,2,5] ⇒ [1,2,20]`` ✓（``L = T×hop`` ✓）且**落在 [-1,1]** ✓（能直接写盘 ✓）；
* ④ ``[1,2,18] ⇒ [1,4,2,5]`` ✓（补零 ✓，``T = ceil(18/4)`` ✓）；
* ⑤ 统计没给 ⇒ **恒等**（`is` 同一对象 ✓）；给了 ⇒ **真用上** ✓（``std=2`` 解码结果必须变 ✓
  —— 否则就是「统计没接进去」的假绿 ✗✗）；
* ⭐⭐ ⑥ **真写 wav 并用标准库读回** ✓：声道 **2** ✓ / 采样率 ✓ / 帧数 **20** ✓ / 16-bit ✓
  ⇒ **盘上真出现过一个能播的文件** ✓（不是"函数没抛异常"✗）；⑥′ 用完**已清理** ✓。

### ⚠️ 三个结构错误，**全是响亮的** ✓✓（没一个是静默的 ✓）

1. `ResBlock1` 我把 ``kernels`` × ``dilations`` 当成**叉乘** ✗ ⇒ ``dilation`` 变成元组 ⇒
   ``tuple // 2`` 直接 `TypeError` ✓（正解：**每个 kernel 配一组** dilations ✓）；
2. `Encoder` 照抄「先翻倍再建块」✗ ⇒ 块输入宽度比张量多一倍 ⇒ ``4 vs 8`` size mismatch ✓
   （改成**自洽**写法 ✓：输入 = 当前宽度 ✓、输出 = 翻倍 ✓）；
3. 自检里我写过一条**套套逻辑**式的比较 ✗✓（拿同一个表达式和自己比 ✓）⇒ **自审**时改成
   「给统计 vs 不给统计，解码结果必须不同」✓（这才是**能失败**的断言 ✓）。

**实测**：`engine_audio_vae` **12/12** ✓（新 ✓，已登记进 `run_all.TESTS` ✓）。

## S7 第 125 步：⭐⭐ **把 H3 双流接进管线**（真 mp4 + 真 wav ✓ `engine_dual_stream` **39/39** ✓）

用户「继续执行未完成任务」✓ ⇒ 先还第 123 步欠账 ✓，再落 `H3_FORM_TODO` 里**最大**那条（音频流接管线 ✓）。

### 交付
* **欠账** ✓：`engine_io_test` 补潜变量归一化的**落盘自检**（㊵–㊽ ✓ 39 → **48/48** ✓ **一次过** ✓：逐通道手算对照 ✓ /
  往返恒等 ✓ / 常量不动点（`x=mean⇒0` ✓、`x=mean+std⇒1` ✓）/ **改数必变**（反套套逻辑 ✓）/ 数不对报错 ✓）；
  顺带 `vae.H3_VIDEO_VAE_FACTS` 补 `vaeScale=16` ✓（已核事实 ✓）。
* **双流** ✓：`TorchBackend` 新增 `dualStream`（**逐条报缺什么** ✓：形态 / 音频 VAE / 取整口径 ✓）、
  `attach_audio_vae` ✓、`init_dual_latents` ✓（视频 `[C,T,H,W]` + 音频 `[C,2,T_a]` ✓ **无 batch** ✓、
  共用 generator ⇒ 逐位可复现 ✓）、`sample_dual` ✓（**直接调 `h3_form.sample_dual_stream`** ✓ 判据只有一份 ✓）、
  `_decode_dual` ✓（两个 VAE 各管一条 ✓）、`write` ✓（真 wav 与 mp4 **各落一份** ✓）；
  `pipeline` **按后端自述分流** ✓（单流默认路径**一字未动** ✓）+ 三条**当场拒绝** ✓：CFG ✓ / 首帧 ✓ / heun ✓
  （事实：H3 参考**无 CFG** ✓、循环是**一阶欧拉** ✓、`cond` 段还没做 ✓）；
  新套件 `engine_dual_stream_test` **39/39** ✓（真 mp4 + ffprobe 复核 ✓ + 标准库读回 wav ✓ +
  换提示词 **wav 字节必变** ✓ + 拒绝时**一个文件都不写** ✓ + 干跑后端照旧单流 ✓）。

### 踩到的（四条真 bug ✗✓ + 一条工具坑 ✓）
1. ⭐⭐ **config 有两种形态**：H3 形态是 **dict** ✓、DiT 是 dataclass ✓ ⇒ `describe()` 里 `.to_dict()` 直接
   `AttributeError` ✗（`/engine/backends` 端点**就是调它** ✓ ⇒ 装了 H3 权重后端会 **500** ✓✗）；同族两处更阴 ✗：
   `condition_width()` / `attach_text_encoder()` 在 dict 上**悄悄回落**成 64 ✗ ⇒ TE 宽度与主干 `text_dim` 对不上
   （那次会报形状错 ✓ 但别指望 ✓）⇒ 收口 `_configured_text_dim()` ✓ + `describe` 两态都认 ✓。
2. ⭐ **条件 dict 被当成文本状态**：双流把 `encode_text` 的 dict 直接递给主干 ✗ ⇒
   `'dict' object has no attribute 'shape'` ✓（**响亮** ✓ 走运 ✓）⇒ 取 `positive` ✓ + 带 `negative` ⇒ **明确拒绝** ✓。
3. ⭐⭐ **两条流的 batch 处理不一样** ✗✓：`media.write_video` 要 **5 维** ✓ 而 `write_wav` 要 `(channels,N)` ✓ ——
   视频也去掉那一维 ⇒ `write` **认不出帧张量** ⇒ **默默走成「张量清单 JSON」** ✗✗（错在 `json.dumps(Tensor)`
   才炸 ✓ 响亮但指不到真因 ✓）。
4. ⭐⭐ **结构不变量没在构造期钉** ✗：音频 VAE 解码每级**通道减半** ✓ ⇒ `decoder_dim(16) < 2^7` ⇒ 造出
   `in_channels=0` 的**非法卷积** ✓ 而构造期不报 ✗ ⇒ forward 里报的是**一句 oneDNN 反卷积消息** ✓✗
   ⇒ 加构造期校验 ✓（真 H3 1024 ≥ 128 ✓ 本不触发 ✓）+ 套件补 ①‴「能触发」的反向证明 ✓（13/13 ✓）。
5. ⭐ **别把「理想值」当事实** ✗：我断言 wav 帧数 = `T×hop` = 6400 ✗ ⇒ 实测 **6208** ✓（`ConvTranspose1d` 带
   padding ✓ 每级少 1 个样本 ✓ **结构使然** ✓）⇒ 改成「盘上读数 vs 写盘回执**逐位相等**」✓（两个独立来源 ✓）。
6. ⚠️ **跑批工具坑**（工具层 ✗ 不是代码 ✗）：PowerShell `*>>` 写 **UTF-16** ✗ 叠加 `Add-Content -Encoding UTF8`
   ⇒ **混编码** ⇒ `Select-String` **0 命中** ✓✗（"看起来一个 FAIL 都没有" ✓）；子进程 stdout 默认 GBK ✗ ⇒
   `smoke_test` 因 `UnicodeEncodeError('✓')` 退出 1 ✓✗（**看起来失败、其实只是打印炸** ✓）⇒ 跑批必须
   `PYTHONIOENCODING=utf-8` ✓ + 在**脚本里** UTF-8 落盘 ✓（已按此重跑 ✓）。

### 实测（引擎 12 套 + 守卫 ✓）
core **34** ✓ / dit **43** ✓ / mappings **20** ✓ / h3_form **55** ✓ / pipeline **98** ✓ / io **48** ✓ /
audio_vae **13** ✓ / **dual 39** ✓ / text **21** ✓ / loader **28** ✓ / inventory **34** ✓ / segments **54** ✓；
**冒烟 476/476** ✓✓；`route_parity` exit 0 ✓（无 SUMMARY 是它的常态 ✓）；`layering` **6/6** ✓、
`read_json_guard` **10/10** ✓。

### 状态
`H3_FORM_TODO` **只剩 2 条** ✓（`cond`/`refs` 段 ✓ / 输出头 bank PDD ✓）；双流**默认不启用** ✓
（要 `attach_audio_vae()` **且**显式 `audio_latent_mode` ✓ —— 「秒数 → 音频潜帧数」的取整口径**没核过** ✗
⇒ 按 `geometry` 的纪律**逼成必填** ✓：好默认值会把「未核实」伪装成「已实现」✓✗）；
`run_all.TESTS` 已登记 ✓（套数 **96** ✓、**总数待全量实测** ✗ 不推算 ✓）。

## S7 第 126 步：⭐⭐ **修两处静默错** ✓ + **cond/refs 打包层** ✓（`engine_h3_form` **60/60** ✓）

用户「继续」✓ ⇒ 按上一步写的「下一步做 cond/refs」动手 ✓ —— ⚠️ 但**先取证**（读参考 `PackedLayout` ✓）
时就发现本仓有**两处一直在静默错**的口径 ✓✗ ⇒ **先修它们** ✓（不修的话 cond/refs 做完还是错的 ✓）。

### ⭐⭐ 两处静默错（都在 `h3_form.py` ✓；两处都**不会报错** ✗）
1. **模态标签全错** ✗：事实（参考前向的 `seg_tag` ✓）= **video/cond/ref_img = 0** ✓、**text = 1** ✓、
   **audio/cond_audio/ref_audio = 2** ✓；本仓初版写「text=0 / audio=1 / video=2」✗ —— **三档全错** ✓✗。
   标签只是 adaLN 的**行内偏移** ✓ ⇒ 真权重上每个模态会接到**别的模态**那组参数 ✓（表现只是"画面不对" ✓✗）。
2. **text 的 t 档错** ✗：事实 = text **与 video 同档**（`t_v = 1 − σ_v` ✓）；初版钉在
   `VISUAL_COND_TIMESTEP`(0.999) ✗ —— 那是**条件行**的档 ✓（初版注释自己也标了「未逐条核过」✓✗ ⇒ 现删 ✓）。
   + 唯一时间戳改用 **`sorted(set(...))`** ✓（= 参考 ✓）。⚠️ **老实说**：本仓旧口径（首次出现顺序）在
   现有段类集合下与升序**恰好等价** ✓ —— 我一度以为「正好相反」✗ 但**没验证** ⇒ **不写进代码** ✓
   （换口径只为**与参考同构** ✓：将来加段类时两种顺序会静默分叉 ✓✗）。

### 交付：cond / refs 打包层 ✓
`h3_form.packed_layout` ✓ —— **段表 + 坐标 + 更新掩码一处算全** ✓（`packed_rows` / `position_ids` 都**委托**它 ✓：
本文件早先那条教训就是「段表与坐标各拼一遍迟早**错位而不报错**」✗✗）+ `audio_grid` / `video_grid` /
`ref_time_span` / `video_t_spans` ✓ + `H3FormTrunk.forward` 收 `keyframes` / `refs` / `latent_t` / `audio_t` ✓
（⚠️ 带条件块时**段长从行数推不出来** ✗ ⇒ 那两条**必须显式给** ✓，再按布局总行数**反向校验** ✓ 对不上报错 ✓）。

**核清的规则**（全得自参考 ✓ 且都是「混了不报错」那类 ✓✗）：
* 段序 ``text → [cond/cond_audio]* → [ref_img/ref_audio]* → audio → video`` ✓（**目标两条流永远在最后** ✓）；
* ``cond`` 用**目标网格** ✓、``t = cursor + FRAME_RESCALE × frame_index`` ✓；而 ``ref_img``（图像）**整块共用一个 t** ✓
  且每块让 cursor **+1.0** ✓、空间网格用它**自己的**尺寸 ✓ —— 两种块**不一样** ✓✗；
* ``ref_audio`` 的 w 取**目标**两端 ✓，但 ``video`` 类块里的音频行取**它自己**两端 ✓、且音频行排**视频行之前** ✓；
* 稀疏更新掩码**只有两条** ✓（`img_update` / `audio_update` ✓），各自只覆盖视频行 / 音频行 ✓，
  **text 行两条都不进** ✓（塞进去会多出一批假阳性 ✓）。

### 实测
`engine_h3_form` **60/60** ✓（55 → +5：cond 段序与行数 ✓、cond 的 t 起点 ✓、掩码归属 ✓、四类参考块段序 ✓、
cursor 把目标推后 ✓、行数校验报错 ✓）；`engine_dual_stream` **39/39** ✓；**冒烟 476/476** ✓。
⚠️ **「能触发」的反向证明** ✓：改之前那两条断言**当场红** ✓（㉒ 实测给出 `text→1 / audio→5 / video→0` ✓、
⑳ 给出 4 行 ✓）⇒ 不是"改完就绿"的自证 ✓。

### 状态
`H3_FORM_TODO` 第 1 条**改成精确表述** ✓（不再是"没实现"✗，而是「**打包层有 ✓、调用方没有**」✓ ——
后端还没把首帧编成行 ✓、`denoise_step` 还没收额外行 ✓）；⇒ 双流仍**明确拒绝首帧** ✓（诚实 ✓ 不假装 ✓）。
下一步 = **后端侧接线**：首帧/参考图 → VAE 编码 → 行 → 放进 `init_dual_latents`/`sample_dual` ✓ 并解锁首帧 ✓。

## S7 第 127 步：⭐⭐ **首帧条件接进双流** ✓（`engine_dual_stream` **40/40** ✓ —— fl2va 那条链通了一半 ✓）

用户「继续实现」✓ ⇒ 做上一步写下的"后端侧接线" ✓ 目标 = **首帧 → H3 `cond` 段** ✓。

### 交付
* `TorchBackend._dual_keyframes` ✓：首帧 PNG → `media.load_image_tensor(width=plan.width, height=plan.height)`
  （⚠️ **按目标尺寸缩放进编码** ✓ —— `cond` 用**目标空间网格** ✓ 这是事实 ✓）→ 参考视频 VAE `encode` ✓
  → `keyframes=[{resolved_frame_index: 0, latent, audio_latent: None}]` ✓ 挂在 `init_dual_latents` 的返回里 ✓；
* `sample_dual` ✓：`patchify_video`/`pack_audio` 把条件块变成行 ✓ **前插**在目标行之前 ✓；
* `denoise_step` / `sample_dual_stream` ✓：新增 `extra_video_rows`/`extra_audio_rows`/`keyframes`/`refs` 透传 ✓；
* `pipeline` ✓：双流 + 首帧**不再拒绝** ✓ —— 改为查后端自述 `dualStream.firstFrame` ✓（没自述就**仍明确拒绝** ✗
  —— 静默忽略首帧会产出与首帧无关的画面 ✓✗），走通则记 `conditioning.mode = "h3-keyframe-cond"` ✓。

### 两个当场被抓住的错 ✓✓（都是"校验救的" ✓ 不是运气 ✓）
1. **只传行、不传结构** ✗：extra 行传下去了 ✓ 但 `forward` 算 layout 时 `keyframes=None` ✗ ⇒ 总行数
   **当场红** ✓（收到 294 行 vs 布局 245 ✓）—— 这正是那条行数校验存在的意义 ✓；
2. ⭐⭐ **照抄参考的维数** ✗：参考取 `latent.shape[2]` 当帧数 ✓ 是因为它是 **5 维**（`[B,C,T,H,W]` ✓），
   本仓是 **4 维**（`[C,T,h,w]` ✓ 无 batch ✓）⇒ 取到的是 **h=14** ✗ ⇒ cond 段长算成 **686** ✗
   ⇒ 仍被**总行数校验**当场红 ✓（`收到 294 / 布局要 931` ✓）。⇒ 现在**显式校验维数** ✓
   （非 4 维直接报错 ✓，并在注释里写明"5 维是参考的口径 ✓ 别混 ✗"）。

### 实测
`engine_dual_stream` **40/40** ✓（新增两条：㉜ 双流+首帧跑通且标注 `h3-keyframe-cond` ✓；
㉜′ ⭐⭐ **反套套**：给首帧 vs 不给 ⇒ **wav 字节必不同** ✓ —— 这是"条件真接进去了"唯一算得上的证明 ✓）；
`engine_h3_form` **60/60** ✓；**冒烟 476/476** ✓。

### 状态
`H3_FORM_TODO` 现在只剩 **1 条** ✓：`refs` 段打包层有 ✓ 但**没有调用方** ✗（参考图/音频/视频还没入口 ✓）
+ **多关键帧**明确拒绝 ✓（参考段序是**逐块** ``cond, cond_audio`` ✓ 只接了单块 ✓）。
下一步候选：`refs` 接线 ✓ / 输出头 bank（PDD ✓）/ 或者转去「真权重到位后的真跑」那条主线 ✓。

## S7 第 128 步：⭐ **`refs`（参考图）也接进双流** ✓（`engine_dual_stream` **42/42** ✓）

* `TorchBackend._dual_references` ✓：`request.reference_frames` → 逐张图片 `load_image_tensor`（按 plan 尺寸 ✓）
  → 视频 VAE `encode` ✓ → `refs=[{kind: "image", latent, latent_h, latent_w}]` ✓（⚠️ **用图片自己的网格** ✓
  —— 与 `cond` 用**目标**网格**不同** ✓ 事实 ✓）；挂进 `init_dual_latents` 的返回 ✓。
* ⭐ **行序责任收敛到一处** ✓：`_dual_extra_rows` ✓ 按 `packed_layout` 的段序拼 ✓（**先关键帧、后参考块** ✓）——
  上一轮那是"按类型分组"的写法 ✓ 且**只敢收一块**关键帧 ✗（多块时行序会错而**不报错** ✓✗）⇒ 顺手把
  「多关键帧明确拒绝」那条**解掉** ✓（`H3_FORM_TODO` 里那条 → 变成"参考音频/视频没有入口" ✓）。
* 管线 ✓：双流 + `referenceFrames` 而**后端没自述** `references` ⇒ **明确拒绝** ✗（否则 `init_dual_latents`
  会**静默忽略**参考图 ✓✗ —— 产物与参考无关而用户看不出来 ✓）⇒ 自述项加在 `dualStream["references"]` ✓
  （**只有图片**这一类 ✓：参考音频/视频没有入口 ⇒ 不声明 ✓ 诚实 ✓）。

**实测**：`engine_dual_stream` **42/42** ✓（新增 ㊴ 跑通 ✓、㊵ ⭐ **反套套**：给参考图 vs 不给 ⇒
**wav 字节必不同** ✓）；`engine_h3_form` 60/60 ✓；**冒烟 476/476** ✓。

⚠️ **新记的一条账**（`H3_FORM_TODO` 第 3 条 ✓）：**「行序正确性」没有独立判据** ✗ —— `forward` 只校验
**总行数** ✓ ⇒ "同数不同序"全靠**两处代码人工对齐** ✓✗（㊵ 只证明"参考进去了" ✓，**不证明顺序对** ✗）。
要更强就得让 `packed_layout` **直接产出行序**（而不是只给段表 ✓）✓ —— 留给下一轮 ✓。

## S7 第 129 步：⭐⭐ **行序判据落地**（布局说了算 ✓ 不再是两处人工对齐）

上一步自己记的那条账 ✓ 这轮还掉 ✓：

* `packed_layout` 新增 **块清单** ✓：`video_blocks` / `audio_blocks` = ``[((块标识, 序号), 行数), …]`` ✓
  （标识 = ``("keyframe", i)`` / ``("ref", i)`` / ``("target", 0)`` ✓）—— **行序的权威来源** ✓；
  ⚠️ 实现上走的是"进循环就改 `current` 块标识 ✓、`push` 顺手记账 ✓"（**零改动**各调用点 ✓），
  并在进目标段前**显式重置** `current` ✓（不然目标两条流会被记到最后一个块名下 ✓✗ —— 错得不响 ✓）。
* 新增 `assemble_blocks(layout, video_sources, audio_sources)` ✓：**照块清单取** ✓（顺序不问调用方 ✓），
  缺块 / 块行数对不上 ⇒ **报错** ✓（不静默跳过 ✗）。
* `TorchBackend._dual_extra_rows` ✓ **改用**它 ✓（自己算一遍 layout ✓ —— 同一个纯函数 ✓ 参数一致就一致 ✓）
  ⇒ "先关键帧、后参考块"那套人工对齐**删掉了** ✓。

**实测**：`engine_h3_form` **63/63** ✓（新增 ㊶ 块清单与段表**对账** ✓、㊷ ⭐⭐ **判据「能失败」**：
把两个块的 sources **对调** ⇒ 拼出的行**必变** ✓（不变 ⇒ 就是按插入序拼 ⇒ 顺序根本没受约束 ✓）、
㊸ 缺块 ⇒ 报错 ✓）；`engine_dual_stream` **42/42** ✓（后端已改用它 ✓ 端到端仍通 ✓）；**冒烟 476/476** ✓。
⚠️ 顺手抓到一个真错 ✗✓：只改了函数体、**没改签名** ⇒ `takes 2 positional arguments but 3 were given` ✓
（响亮 ✓ —— 这属于"改一处忘一处" ✓，靠**端到端跑**才现形 ✓）。

`H3_FORM_TODO` 现在 2 条 ✓（参考音频/视频没有入口 ✓ PDD 头库 ✓）。

## S7 第 130 步：**PDD 头库取证** ✓（**只取证、没实现** ✗ —— 事实先固化 ✓）

用户「继续」✓ ⇒ 挑 TODO 里价值最高的那条（PDD 头库 = **真权重能否装上的阻塞项** ✓）动手 ✓，
但**先读参考**（`FinalLayer.forward` + `_pdd_head` ✓ 1 次 grep ✓）⇒ 事实**全核清了** ✓：

* `n = weight.shape[0] // out_features` ✓；`n == 1` ⇒ 普通头 ✓（**本仓现状就是对的** ✓）；
* `n > 1` ⇒ ① 按**整条日程**定位当前 σ：`i = argmin|sample_sigmas − σ|` ✓、`σ_next = sample_sigmas[i+1]` ✓
  （**没给日程就报错** ✓）；② `start, stop = round((1 − time_shift_sigma(s, shift_v, **1.0**)) × n)` ✓
  —— ⚠️⚠️ **第三参是 1.0** ✗：与 `audio_carry` 的 `(shift_v, shift_a)` **不是一回事** ✓（**混了不报错** ✓✗，
  又一处同族静默错候选 ✓）；再 `start = min(start, n−1)` ✓、`stop = max(stop, start+1)` ✓；
  ③ 头权重重塑成 `(n, −1, in)` ✓、**第 0 块是完整头** ✓、**后续块是偏移** ✓，按 `dt`
  （`linspace(1, 0, n+1)` 经 `flow_shift` 变形后差分 ✓）加权求和 ✓（音频头用 `shifts[1]` ✓，
  但 ①②的定位两条流**共用** ✓）。
* ⚠️ 真权重里 `n` 是几 **还没核到** ✗（**不猜** ✓）。

⇒ 本轮**只把事实写进 `H3_FORM_TODO`** ✓（含"落地要动哪 6 处" ✓：`FinalLayer`（`head_banks` + 三个入参）✓、
`pdd_head` ✓、`H3FormTrunk.forward` ✓、`denoise_step` ✓、`sample_dual_stream` ✓、`torch_backend.sample_dual` ✓）
—— ⚠️ **没写实现** ✗、也**没假装写完** ✓；理由：改 6 处要动前向签名链 ✓，本轮上下文不足以保证"改完还能全绿" ✓
⇒ 与其改一半 ✓ 不如把事实固化 + 下一轮一次做完 ✓（本仓判据：**先取证再动手** ✓）。

## S7 第 131 步：⭐ **PDD 头库实现** —— ⚠️ **未全绿** ✗（如实记 ✓ 不粉饰 ✓）

用户「继续实现，按参考的项目、独立实现」✓ ⇒ 按上一步固化的事实落地 ✓：
`h3_form.pdd_head` ✓（**自写** ✓ 不抄 ✓：`linspace(1, 0, n+1)` 经 `flow_shift` 形变 → 差分 → 加权和 ✓、
第 0 块为基 ✓、后续块是偏移 ✓）+ `FinalLayer`（`head_banks` ✓ + `sigma`/`sample_sigmas`/`shifts` 三入参 ✓ +
`n == 1` 走老路 ✓、`n > 1` 缺日程 ⇒ **报错** ✓）+ `H3FormTrunk(head_banks=…)` ✓ +
`denoise_step`/`sample_dual_stream` 把**整条**视频 σ 日程递下去 ✓ + `__all__` / 惰性字典 ✓。

⭐⭐ **两个真教训**（都是"实跑才现形" ✓）：
1. **`out_features` 陷阱** ✗✓：参考的 ``n = 行数 // out_features`` ✓ 成立是因为**它构造时给的是单头宽度** ✓；
   本仓照 ``nn.Linear(hidden, vd*banks)`` 留下的默认 `out_features` ⇒ ``n`` **恒为 1** ✓✗ ⇒
   **PDD 静默失效** ✓（不报错 ✓ 只是头库白装 ✓）⇒ 现在显式改回单头宽度 ✓；
2. ⚠️ **工具层**：`str_replace` 报"成功"但我插的 5 行缩进是 **20 空格** ✗ ⇒ `IndentationError` ✓；
   而"按空格数替换"**0 命中** ✗（真实缩进不是 20 ✓）⇒ 改成**按锚点行重排** ✓，但脚本先命中了
   `FinalLayer` 里**同名**的那行 ✓ ⇒ 把它的 `if` 块**压平**了 ✗（第二次修订才修回 ✓）。
   **教训**：同名锚点在文件里多次出现时 `next(...)` 是**错的** ✓ ⇒ 要么取最后一次 ✓ 要么用行区间 ✓。

**状态（如实 ✓）**：`engine_h3_form` 最后一次**实测 66/67** ✗（唯一红 = ㊺「PDD 能失败」✗）；
我把 ㊺ 改成**不依赖传参链**的插值单元判据（㊺ span 不同 ⇒ 插值必不同 ✓、㊺′ span 只含第 0 块 ⇒
**恰好等于第 0 块本身** ✓ —— 事实：第 0 块是完整头 ✓）⇒ ⚠️ **改完没跑过** ✗（会话末尾几次跑批都被判后台、
产物未落盘 ✗）⇒ **不能声称全绿** ✗；`dual 42/42` ✓、冒烟 **476/476** ✓（都是**改 ㊺ 之前**的实测 ✓）。

⚠️ **另记一条账** ✓：**「主干把 σ 日程递到 `final_layer`」那条链本轮没验证过** ✗（㊺ 已绕开它 ✓）
⇒ 下一轮先跑一遍套件确认绿 ✓，再补那条链的判据 ✓。

## S7 第 132 步：✅ **PDD 全绿 + 整链判据补齐**（`engine_h3_form` **70/70** ✓）

上一步留的两条账都还了 ✓（先跑绿 ✓ 再补"整链"判据 ✓）：

* ⚠️ 先说清一件事 ✓：上一步的 66/67 **不是实现错** ✗ —— 是我新写的**用例自己语法错**
  （中文文案里嵌了半角 ``"`` ✓✗ **第 3 次犯** ✓）⇒ 修掉后直接 **68/68** ✓；PDD 三条判据
  （㊺ 插值「能失败」✓、㊺′ span 只含第 0 块 ⇒ **恰好等于第 0 块本身** ✓、㊻ 缺日程报错 ✓）**本来就是对的** ✓。
* 补 **㊽ 整链判据** ✓（σ 日程经 `H3FormTrunk.forward` 递到 `FinalLayer` ⇒ 换日程 ⇒ 输出必变 ✓）
  + ㊽′（整链上缺日程也报错 ✓）。

⭐⭐ **最大的教训**：㊽ 我**连红两版** ✗，两版都是**我先猜方向** ✓✗：
* 第一版用 `banks=3` ✓ 且以为「σ 小 ⇒ start 小」✗；
* 第二版把 banks 提到 64 ✓ 但**方向照旧猜错** ✗✗。
⇒ 第三版才去**打印取证** ✓：``1 − time_shift_sigma(σ, 12, 1.0)`` 随 σ **增而减** ✓ ⇒ **σ 大 ⇒ start 小** ✓；
σ ≤ 0.05 时它 ≈ 1 ⇒ `start` 一律被 `min(start, banks−1)` **夹到末段** ✓✗ ⇒ 两个日程算成**同一段** ⇒ 输出相同 ✓。
改成 σ = 0.9 的日程后**一次通过** ✓。**这是「先取证再动手」那条纪律的又一次学费** ✓
（与「外部工具真实输出先落盘」同源 ✓）。

**实测**：`engine_h3_form` **70/70** ✓、`engine_dual_stream` **42/42** ✓、**冒烟 476/476** ✓。

### 状态
`H3_FORM_TODO` 仍 **2 条** ✓（参考音频/视频没有入口 ✓；PDD 那条 = **机制已实现并验证** ✓，
但**真权重里 `n` 是几还没核到** ✗ ⇒ `H3_TRUNK_DEFAULTS` 里没写 `head_banks` ✓ —— 拿到真权重后按元数据补 ✓）。

## S7 第 133 步：⭐⭐ **头库大小从权重推断** ✓ + **语法预检守卫** ✓（`dual` **46/46** ✓）

### ① PDD 的「怎么知道 n 是几」落地 ✓
`h3_form.head_banks_from_shape(shape, single_dim)` ✓（**模块级纯函数** ✓ 不需要 torch ✓）+
`H3_VIDEO_OUT_KEY` ✓（= `final_layer.video_out.weight` ✓ —— 本仓模块名与参考逐字对齐 ⇒ 键名一致 ✓）+
`video_patch_dim(latents_dim, patch_size)` ✓；`TorchBackend.load_weights` 的 H3 分支 ⇒ **从权重形状推断**
`head_banks` ✓ —— ⭐ 真权重**不给**这个数 ✓ ⇒ 既不猜也不硬编 ✓✓（拿默认 1 去装 banks>1 的权重 ⇒ 形状不符 ✗；
写死一个数 ⇒ 换检查点就错 ✓✗）；显式给且与推断**不一致** ⇒ **报错** ✓（不静默取一个 ✓）。
⇒ **真权重 n>1 现在能自动装上了** ✓（"能装真权重"的一步 ✓）。自检 4 条 ✓（推断出 3 ✓ / 不一致报错 ✓ /
非整数倍报错 ✓ / 单头宽度算法一致 = 24×4=96 ✓）。

### ② ⭐⭐ **语法预检守卫**（我一天里栽了 **4 次** ✗）
`run_all.syntax_problems(root)` ✓ + `main()` 开跑前先跑它 ✓ —— 起因：中文文案里嵌**半角双引号** ``"``
⇒ `SyntaxError` ✓✗，而那种错的**表现是「套件连一行都不输出」** ✓ ⇒ 看起来像"被判后台 / 输出被吞" ✓✗
（这一轮与上一轮我都误判过 ✓）。⚠️ **「能触发」的证明** ✓（不是"写完就算"✓）：造一个坏文件 ⇒ 它当场点名
`tests\_syntax_probe.py:1 unterminated string literal` ✓；删掉 ⇒ 又全过 ✓。

### ③ 顺带发现（**没修** ✗ 如实记 ✓）
`syntax_problems` 扫出 `app/services/color_grade.py:32` 有 `SyntaxWarning: invalid escape sequence '\*'` ✗
—— **早已存在** ✓（不是我改出来的 ✓）：普通字符串里写了 `L\*` ✓ ⇒ 现在只是**警告** ✓，将来 Python 版本
会变**错误** ✓ ⇒ 建议顺手改 `\\*` 或 raw ✓（改的是文档串 ✓ 无断言依赖 ✓ 风险低 ✓）。

**实测**：`engine_dual_stream` **46/46** ✓（42 → +4 ✓）、`engine_h3_form` **70/70** ✓、**冒烟 476/476** ✓、
`run_all` AST OK ✓、语法预检基线**全过** ✓。

## S7 第 134 步：⭐ **参考音频入口打通** ✓（`dual` **50/50** ✓ —— `H3_FORM_TODO` 只剩 **1 条** ✓）

* `media.load_wav_tensor` ✓（**自己实现** ✓ 标准库 `wave` ✓ 零依赖 ✓）：wav → ``(channels, N)`` ✓ 值域 [-1,1] ✓；
  ⚠️ 与 :func:`write_wav` **成对** ✓；采样率/声道不符 ⇒ **报错** ✓（**不重采样、不混声道** ✗ ——
  那会**悄悄改掉素材本身** ✓✗，而参考音频唯一在意的就是它的内容 ✓）；位宽非 2 字节 ⇒ 报错 ✓（不猜格式 ✗）；
* `GenerationRequest.reference_audio` ✓（wav 路径元组 ✓）；
* `TorchBackend._dual_references` ✓ 加音频分支：`load_wav_tensor` → **音频 VAE** 编码 ✓（⚠️ 走**另一族** VAE ✗
  —— 视频 VAE 顶替不了 ✓）→ ``{"kind": "audio", ref_audio_t}`` ✓；
* `_dual_extra_rows` ✓：图片进**视频行** ✓、音频进**音频行** ✓（两条流**同名字段** `latent` ✓ 别混 ✗）；
* `dualStream` 自述**拆两项** ✓（`referencesImage` / `referencesAudio` ✓ —— 合成一个 bool 就分不清"缺哪类"✓✗）；
  pipeline 分别校验 ✓（没自述就**明确拒绝** ✗，不静默忽略 ✓）。

**实测**：`engine_dual_stream` **50/50** ✓（46 → +4：自己写的 wav 读 ✓ / 不符即报错 ✓ / 跑通 ✓ /
⭐ **反套套**：给参考音频 vs 不给 ⇒ **wav 字节必不同** ✓）+ `engine_h3_form` **70/70** ✓ + **冒烟 476/476** ✓。
⚠️ `media.py` 是**共用件** ⇒ 本想再跑一次 `engine_io_test` 复核 ✓，但那两次跑批**都被判后台、没拿到结果** ✗
⇒ **本轮未复核** ✓（如实记 ✓ 不写成"已复核"✗）。

⚠️ **核实**：`frontend/app/types/contracts.ts` 里**没有** `referenceFrames` ✗ ⇒ 这两个字段前端**没有镜像** ✓
⇒ 本次**无需**同步 ✓（本仓"改后端字段要同步它"的前提是**它里面有** ✓）。

### 状态
`H3_FORM_TODO` **只剩 1 条** ✓：参考**视频**块（``video`` / ``video_audio``）没有入口 ✓
（打包层与行拼装都已支持 ✓，缺的只是请求侧通道 ✓）；PDD 那条**已从 TODO 移除** ✓（机制已验证 ✓，
且 `n` 现在**从权重自动推断** ✓ ⇒ 不再需要"事先知道" ✓）。

## S7 第 135 步：✅ **参考视频入口打通** ✓（`dual` **58/58** ✓ —— `H3_FORM_TODO` **清零** ✓）

* `media.load_video_tensor` ✓（**自己实现** ✓ ffmpeg 解码 ✓）：视频 → ``(1,3,T,H,W)`` ✓ **原生尺寸** ✓
  （**不缩放、不抽帧** ✗）；字节数非整数帧 ⇒ **报错** ✓（不静默截断 ✓）；与 `write_video` **成对** ✓。
* `media.extract_wav` ✓：容器抽音轨 → 16-bit PCM wav ✓（**原生**采样率/声道 ✓ 不带 `-ar`/`-ac` ✓）⇒
  采样率/声道不符由 `load_wav_tensor` 那一步**明确报错** ✓。
* `GenerationRequest.reference_videos` ✓ + 管线守卫 ✓（后端没自述 `referencesVideo` ⇒ **当场拒绝** ✓ 不静默忽略 ✓）。
* `TorchBackend._dual_references` 视频分支 ✓：视频 VAE 编帧 ✓；**有音轨 ⇒ 块升为 `video_audio`** ✓
  （抽轨 → 音频 VAE ✓ —— **不静默丢音轨** ✗）；宽高不能整除 vae_scale ⇒ **报错** ✓（不悄悄取整 ✗）。
  `_dual_extra_rows` 补视频块：视频行进视频源 ✓、`video_audio` 的音频行进音频源 ✓。
* `dualStream` 自述加 `referencesVideo: True` ✓（三类分立 ✓ 不合成一个 bool ✓）。
* ⭐ **测试素材坑**：往返忠实用例**不能用随机噪声** ✗（噪声是有损编码**最坏情况** ✓ 实测 crf=12 平均误差
  0.35 ✓✗ 假红 ✓）⇒ 换**平滑渐变**素材 ✓ 一次过 ✓。`video_audio` 用例的带音轨文件用 **mkv** ✓
  （pcm_s16le 进 mp4 容器不被支持 ✗）。
* ⭐ **还清一笔旧账** ✓：日志第 134 步声称「PDD 条目已从 `H3_FORM_TODO` 移除」**实际没落盘** ✗
  （又一次"编辑报成功但没生效"✓）⇒ 本轮核实后删掉 ✓，并同步修正 `h3_form` 模块 docstring、
  `H3FormTrunk` 类 docstring、`run_all.py` 两处过时描述 ✓、`torch_backend.PENDING_PARTS` 过时句 ✓。
* **实测**：`engine_dual_stream` **58/58** ✓（50 → +8 ✓）、`engine_h3_form` **70/70** ✓、
  `engine_io` **48/48** ✓（上轮欠的 `media.py` 共用件复核账 ✓）、**全量 97 套件全部通过** ✓。
* ⚠️ `frontend/app/types/contracts.ts` 无任何 reference 字段 ✓ ⇒ 无需同步 ✓。

### 状态
`H3_FORM_TODO` **清零** ✓（判据翻转：test ① 从「TODO 非空」改为「TODO == 0」✓ ——
再往 `h3_form` 加没接线的东西必须先进 TODO ✗）。引擎侧剩余缺口全部收敛在
`torch_backend.PENDING_PARTS` ✓（真权重 / 命名映射 / DiTConfig / TE·VAE 权重 / 端到端真跑 ✓）。







## GGUF 读取器（引擎最后一块「未实现」已补齐 ✓）

- 新 `backend-py/app/services/engine/gguf.py`（纯 Python 零依赖 ✓）：头格式 / 张量表 / 截断-重叠-块不整除检测 / 量化方案名。
- **结构事实源 = `reference/ollama/fs/gguf/`**：`gguf.go`（长度一律 u64 ✓、v1 字符串带 null 终止且长度含终止符 ✓）、`tensor.go`（块布局公式：Q4_K=144B/256、Q6_K=210B 等；未列 id（TQ1_0/Q4_0_4_x…）一律「未知块布局」不猜 ✗）、`file_type.go`（`general.file_type` 15=Q4_K_M 这类打包名的权威 ✓）。
- 接线：`inventory.component_status` / `loader.plan_component` 的 .gguf 分支从「gguf-unknown + 不假装验过」升级为真体检（verified/tensorCount/dtypeCounts/quantScheme/层块连续性 ✓，与 safetensors 同一「结构坏⇒阻断」口径 ✓）；`_blocks_of` 拆出 `_block_gap_of(names)` 两条路共用。
- 测试 `tests/engine_gguf_test.py`（23 项 ✓）+ `run_all.py` 注册；loader ⑩ / inventory ⑫ 旧断言同步（垃圾 GGUF 现在真去读 ⇒ 结构坏 ⇒ 阻断 ✓）。受影响 3 套：23/23、28/28、34/34 ✓。
- 踩坑：① `_KV_SCALARS` 格式串不能带字节序前缀（游标 `prefix` 会再拼 ⇒ `<<I` 坏格式冒充「文件损坏」✗）；② magic 4 字节直读没过游标 ⇒ `cursor.offset` 漏计 4 ⇒ **data_start 偏 4**（顺序读不受影响，只影响对齐计算 ✗）；③ 测试合成器要**顺序分配 offset**（全 0 ⇒ 真重叠——读取器判对了、合成器自己错 ✗）、大端文件的 KV 载荷必须同字节序打包。
- 本机 `D:\Comfy-Desktop\ComfyUI-Shared\models` 目录树已在（三个目录空），H3 必装 4 件 ≈ 39.55 GiB（DiT 19.53 + TE 14.61 + 视频VAE 4.85 + 音频VAE 0.56，来源 `Comfy-Org/MiniMax-H3`，清单 `configs/models.json`）—— 用户明确「实现功能，不下模型」，权重留工作站跑。

## H3 键名核对器（真权重到手的「插上就验」✓ 2026-09-20 第二项）

- 新 `backend-py/app/services/engine/h3_keys.py`（零依赖 torch-free ✓）：给定「键名→形状」清单（safetensors/GGUF 体检都能给 ✓），不建模型直接核对能不能装进 `H3FormTrunk`。
- 关键设计：**尺寸从 checkpoint 自己推**（hidden/head_dim/inner/ffn/t_dim/text_dim/video_dim/audio_dim，推导源缺失⇒点名回落出厂值 ✗ 不静默 ✓）⇒ 非出厂尺寸的检查点也能核「结构自洽」；期望键表 `expected_h3_keys()`（出厂全尺寸 535 键 = 19 固定 + 50×10 + 2×8）。
- 结构事实（`reference/ComfyUI/comfy/ldm/minimax/model.py` 读全 ✓）：DiT 每层 10 键（18 路 adaLN=6×hidden×3）；**RefinerBlock 每层 8 键没有 adaln**（近似实现的老坑，多出的会被当 unexpected 报 ✗）；final_layer 2 路 adaLN；`adaln_t_table`（curve 变体）与 `time_embedder.*` **互斥**；PDD 头库行数= banks×单头宽度（非整数倍 ⇒ 报错 ✓）；可选键 `attn.to_gate_compress`（VSA 门）不算 unexpected。
- 接线：`inventory.component_status` 两条路（safetensors/GGUF）都过 `_audit_h3_form()`（`looks_like_h3_form` 命中才跑 ✓ 不误伤 ✗）；核不过 ⇒ problems ⇒ 阻断就绪 ✓；报告进 `status["h3Audit"]`。
- 测试 `tests/engine_h3_keys_test.py`（20 项 ✓）+ run_all 注册。回归：inventory 34/34、loader 28/28、gguf 23/23、h3_form 70/70、dual_stream 58/58 全绿 ✓。

## 继续实现（第三、四项 ✓ 2026-09-20）

### 1. H3 结构推导（补 PENDING_PARTS 第 4 条 ✓）
- `h3_keys.infer_h3_trunk_config(tensors)` ✓：尺寸/层数/head_dim/ffn/text_dim/modalities/inv_freq_len/PDD 头库**全从权重读** ⇒ 出厂常量 `H3_TRUNK_DEFAULTS` 只作**回落**；每个字段带 `sources`（"权重" vs "参考默认"）✓ ⇒ 防「回落」被读成「权重事实」。
- `torch_backend.load_weights` H3 分支改写：**权重推导优先** ✓，显式 config 只做**交叉校验**（不一致 ⇒ 报错 ✓ 不静默取一个）；装载报告加 `configSources`/`derivedFields`。
- 踩坑：拿**出厂全尺寸** config 真建 `H3FormTrunk` ⇒ 80 GB 参数 ⇒ 进程**访问越界崩**（exit 0xC0000005）✗ ⇒ 自检只能建**缩小版** ✓。
- 测：`engine_h3_keys_test` 29 项 ✓（含 `H3FormTrunk(**推导config)` 与期望键表**名字+形状**逐键一致 ✓）。

### 2. 自研 BPE 分词器（用户红线：不对外依赖 ⇒ 补掉 tokenizer 的外包 ✓）
- 新 `engine/tokenizer_bpe.py` ✓（零依赖 ✓ 离线 ✓）：byte↔unicode 映射**按算法生成** ✓；预分词用**自写扫描器**（stdlib `re` 无 `\p{L}` ✗ 不引 `regex` ✗）；按 merges **排名**合并（一次合并该对全部出现 ✓）；`tokenizer.json` / `vocab.json`+`merges.txt` 两种装载 ✓；Metaspace 等非 ByteLevel **明确报错** ✗。
- 强不变量：`decode(encode(text)) == text`（12 例 ASCII/CJK/emoji/空白/零宽 ✓）；与 **HF `tokenizers`（Rust ✓）逐例同 id** ✓（10 例 ✓，含 CJK/emoji/制表符/前后空白 ✓）。
- 踩坑（都当场红了 ✓）：① 预分词在「**单字符空白 + 后面非空白**」（如 `a\tb`）时 `index = end-1 == index` ⇒ **死循环**（正则那边是 `\s+` 兜底 ✓）；② `decode` 按 **token** 反查映射表 ✗（合并 token 是多个映射字符的拼接 ✓ 必须按**字符** ✓）；③ `tiny` 词表/merges 必须**自洽**（合并产物要在词表里 ✓，且链要按字面相邻：`h e`→`l l`→`he ll`→`hell o` ✓）。
- 接线：`TorchBackend.attach_text_encoder(tokenizer=… / tokenizer_path=… / 都不给=stub ✓)` ✓；不给 config 时 TE 的 `vocab_size` 自动取 **`required_vocab_size`**（含特殊符 id ✓；用 `vocab_size` 会**越界** ✓✗ 守卫当场拦下 ✓）；给太小 ⇒ 明确报错 ✓。`describe().tokenizer` 报 `bpe` ✓（一眼区分真词表 / `stub` 假桩 ✓）。

### 3. transformers 起「可选依赖」✓（用户：可以直接放进来）
- 用镜像装上：`transformers 5.17.0` + `tokenizers 0.23.2` ✓（清华源 ✓ 走通了 ✓）。
- `torch_backend._Dependency` 新增 `optional: bool` ✓ ⇒ `dependency_status()` 分 **`missing`（必需 ✓ 缺则后端不可用）/ `optionalMissing`（可选 ✓ 只少一条通道）** ✓ —— ⚠️ 别把可选塞进 `DEPENDENCIES` 的必需位 ✗（`ready`/`torch_available()` 会被它判死 ✗✗）。
- 装上后暴露既有点：`HFTokenizer` 把 HF 的裸异常漏出去（`OSError: Repo id must use…` ✗）⇒ 已包成**可行动** `TextEncoderError` ✓；对应断言改成**两种世界都成立** ✓。
- `backend-py/requirements.txt` 加了引擎依赖说明段（权威清单仍在代码 ✓ 不写死 torch ✗）。
- 测：`engine_tokenizer_test` 25 项 ✓（新增参考实现核对 ✓）、`engine_text_test` 25 项 ✓（+3 真词表接线 ✓）、`engine_pipeline_test` 100 项 ✓（新增必需/可选分栏守卫 ✓）。

## 分词器「优化改造层」（第五项 ✓ 2026-09-20）

用户指示：**也可以在 transformers 基础上做改造 / 优化升级** ✓。落地为 `engine/tokenizer_hub.py` ✓：

- **形态嗅探**（读 `tokenizer.json` 的 `model.type` + `pre_tokenizer.type` ✓ `Sequence` 递归 ✓；`vocab.json`+`merges.txt` ⇒ ByteLevel-BPE ✓）⇒ **调用方只给路径** ✓。
- ⭐ **自研优先**：ByteLevel-BPE ⇒ 走 `tokenizer_bpe` ✓（**不加载** transformers ✓ 实测在「参考实现没装」的世界里照样装得上 ✓）；⭐ **参考实现回退**：`Unigram`/`WordPiece`/`Metaspace`（Llama/Qwen 系 ✓）等自研未覆盖形态 ⇒ 自动走 `transformers` ✓ ⇒ **能力从「1 种形态」升级为「覆盖 BPE/Unigram/WordPiece」** ✓；两者都没有 ⇒ 报错 + `pip install` 命令 ✓。
- ⭐ 性能：`encode` 走 **LRU**（可观测 `cache_stats()` ✓ 同文本命中 ✓ 新文本不误命中 ✓）、`encode_batch` ✓（**批量 == 逐条** 是钉住的不变量 ✓；参考侧走 Rust `encode_batch` ✓）。
- ⭐ transformers **用法优化**（不 fork 源码 ✗）：`load()` 两条路都落环境变量 ⇒ `HF_HUB_OFFLINE`/`TRANSFORMERS_OFFLINE` ✓、`HF_HUB_DISABLE_TELEMETRY` ✓、`HF_HOME` 指到**本仓数据根** `<dataRoot>/hf-cache` ✓（不写用户主目录 ✓）。踩坑：数据根权威 API 是 `core.config.get_data_root()` ✓ —— 按模块属性 `DATA_ROOT` 取会**静默 None** ✓✗（缓存目录悄悄回主目录 ✓）。
- ⭐ **运行期互校** `verify_against_reference()` ✓（自研 ↔ 参考逐例同 id ✓；形态覆盖不了时**如实说** `compared=0` ✓ 不假装比过 ✓）。
- 接线：`TorchBackend.attach_text_encoder(tokenizer_path=…)` 改走 hub ✓ ⇒ Metaspace/Unigram 词表也能直接挂 ✓；报告里给 `backend`（`own-bpe`/`transformers` ✓）+ `detail` + notes ✓。
- 版本：`transformers` **5.17.0 已是 PyPI 最新** ✓（`pip index versions` 实测 ✓）⇒ **没有"升版本"这件事** ✓。
- 测：新 `engine_tokenizer_hub_test.py` **23 项** ✓（形态嗅探 5 ✓ / 自研优先 4 ✓ / 回退 4 ✓ / 优化 6 ✓ / 互校 4 ✓）。**全量回归：101 套 / 3516 项 / 0 失败** ✓（实测 ✓）。
- ⚠️ 自检**汇总行格式**是硬约束 ✗：必须 `SUMMARY: n/m passed` ✓ —— 写「n/m 项通过」会让 `run_all.py` 总表显示**空摘要**、项数汇总缺一套 ✓✗（已修 `engine_gguf_test` / `engine_h3_keys_test` / `engine_tokenizer_test` / `engine_tokenizer_hub_test`）。

## 「改造 transformers」落地（第六项 ✓ 2026-09-20 用户重申后）

用户重复指示：「**你也可以改造 transformers 并实现优化升级这个 transformers**」⇒ 上一轮只做了「用法层包装」✗ 不够 ⇒ 本轮**两件都做**：

### A. 对 transformers 打**运行时补丁**（`engine/tokenizers_tuning.py` ✓ 10 项自检 ✓）
- 取舍记在案：**不 fork / 不 vendored** ✗（数千文件 / 数十 MB ✓ 许可虽允许（Apache-2.0 ✓）但**不必要** ✓）⇒ **运行时补丁** ✓ 只改行为开关 ✓ 不碰数学/权重语义 ✗。
- 五条改造：① **离线兜底** ✓ 包装 `PreTrainedTokenizerBase` / `PreTrainedModel` / **Auto 工厂**的 `from_pretrained` 强制 `local_files_only=True` ✓；② HF 缓存钉到 `<数据根>/hf-cache` ✓；③ 降噪（logging→ERROR ✓ `TOKENIZERS_PARALLELISM=false` ✓ 关遥测 ✓）；④ **计数**（打了补丁要能自证 ✓ `lastKwargs`/`lastKind` ✓）；⑤ **可撤销** `unpatch()` ✓。
- ⚠️⚠️ 实测挖到真漏点 ✗：**只包基类会漏** ✓ —— `AutoTokenizer.from_pretrained` 在**解析出具体类之前**就外呼 ✓✗（第一版自检 ⑤ 当场红：计数全 0 ✓）⇒ **Auto 工厂必须单独包** ✓。
- ⚠️ 幂等是硬要求（连打两次只包一层 ✓）；自检里 `unpatch()` 后**重打**也要干净 ✓。
- `tokenizer_hub` 的两处重复实现（`_apply_offline_env` / `_default_cache_dir`）已**删除并收口**到这里 ✓（离线开关落在哪是会咬人的 ✓）。

### B. **自研补齐三大血统**（`engine/tokenizer_own.py` ✓ 11 项自检 ✓）⇒ 参考实现降级为「核对用」
- `Unigram`（**Viterbi** 走最优路径 ✓ 前缀树 ✓ `fuse_unk` ✓ 未知得分 = min_score − 10 ✓）；
- `WordPiece`（贪心最长匹配 ✓ `##` 续接 ✓ ⚠️ **整词失败 ⇒ 整词 `[UNK]`** ✓）；`BertPreTokenizer` ✓（空白 + 标点，标点判据同 HF ✓）；
- `Metaspace` ✓（⚠️ **实测规则**：只对**半角空格**动手 ✓ `\t`/`\n` **不切** ✓；首字符非空格才补一个 `▁` ✓；按 `▁` 切后**丢第一个空块** ✓ —— 第一版按「空白归一」写 ✓✗，双空格当场与参考不一致 ✓）；
- 判据只有一条：**与参考实现（`tokenizers` Rust）逐例同 id** ✓（WordPiece 10 例 ✓ Unigram 13 例 ✓ 含双空格/前导/尾随/`\t` ✓）。
- 明确**拒绝**的形态（不硬套 ✗）：`normalizer` 非空 ✓（会改文本 ✓）、`byte_fallback` ✓、其它预分词器 ✓ ⇒ 由 hub 决定回退还是报错 ✓。
- `tokenizer_hub` 覆盖表随之从 1 种扩到 **3 种血统** ✓（`own-bpe` / `own-unigram` / `own-wordpiece` ✓）；回退只剩「自研明确拒绝」的形态 ✓。
- ⚠️ 顺手修掉一个**会说谎的判据** ✗：`verify_against_reference` 原先把「参考侧造不出来」也写成「参考实现没装」✓✗（实测：手写极简 `tokenizer.json` 缺字段 ⇒ 参考侧报 `missing field single_word` ✓）⇒ 现在返回 `(encoder, reason)` ✓ 分开说 ✓；`detect_form` 补 `normalizerType` ✓（拒绝理由要用它 ✓）。
- **全量回归：103 套 / 3540 项 / 0 失败** ✓（实测 ✓）。

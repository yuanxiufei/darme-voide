# 2026-08-16

## 前端补齐：图片本地上传（视频参考图）
承接上轮「后端已实现、前端未接」清单，本轮完成第 2 项（图片上传），并分析其余 3 项的结论。

### 已完成：图片本地上传
1. `frontend/app/composables/useApi.ts`：新增 `uploadAPI.image(file: File)` —— 用 `FormData` + `fetch(${BASE}/upload/image)`（复用 `json.data ?? json` 与 `code>=400` 错误判断，不能用现有 `req` 因为其固定 JSON content-type）。返回 `{ url, path }`。
2. `frontend/app/components/VideoEditor.vue`：首帧图 URL / 尾帧图 URL 两个输入框各加「上传」按钮 + 隐藏 `<input type="file">`（accept png/jpeg/webp/gif）。`triggerUpload('first'|'last')` 设目标触发选文件 → `onPickFile` 上传后把返回 url 回填到 `form.firstFrameUrl/lastFrameUrl`；`input.value=''` 允许重复选同一文件。新增 `.input-row/.btn-upload/.hidden-input` scoped 样式。
3. **关键链路验证**：后端 `routes/upload.ts` 存到 `data/static/uploads/` 返回 `/static/uploads/xxx`；`video-generation.ts` 的 `normalizeVideoReferenceUrl` 会把 `/static/...` 本地路径读文件转 base64 传给视频模型（Seedance），故本地图片可直接用于视频生成；前端 Nuxt dev 已代理 `/api` `/static` 到 5679，上传后 img 可直接预览。

### 分析结论：其余 3 项均不适合硬接（非 bug，记录结论避免重复分析）
- **预设框架页面**：后端 `routes/preset-framework.ts` 6 端点 + `index.ts` 注册保留，前端 `pages/preset/framework.vue` 与 `presetFrameworkAPI` 已删除。这是「通用预设框架」扩展模板（数据池为 THEME_FAMILY_A 占位、Style Lock 为 {{PLACEHOLDER}}），skills/preset-skill-template.md 保留。后端端点是刻意保留的骨架，前端页面待有具体数据池时再补，**不恢复**。
- **AI 服务商列表动态化**：`GET /ai-providers` 端点存在，但 `ai_service_providers` 表**无任何种子数据**（全项目无 INSERT），端点返回空数组。前端 settings.vue 硬编码 8 个 provider + providerPresets + endpointPrefixes。动态化会导致 provider 下拉为空，破坏现有功能；需先把前端硬编码的 provider/preset 迁移为表种子数据（架构决策），**本轮不做**。
- **剧目统计**：`GET /dramas/stats` 返回 `{ total, by_status }`，但 dramas.status 目前仅创建时固定 'draft'、无更新端点，by_status 永远只有 draft 一项，无信息量；首页已有 `dramas.length`。**暂不接**。

### 备注
- VideoEditor.vue 的 script setup 在 ts-server 下报「找不到名称」（props/form/$emit/visible 等全部），与 settings.vue 同源的 volar 解析问题（项目无 vue-tsc/typecheck，Nuxt 用 esbuild 转译不跑严格检查），非本轮引入；本轮新代码零类型错误。

## 整体 build 冒烟验证（通过）
- 后端 `npm run typecheck`（tsc --noEmit）exitCode 0，无类型错误。
- 前端 `npm run build`（nuxt build，实际版本 Nuxt 3.21.2 / Nitro 2.13.2 / Vite 7.3.1）exitCode 0，1826 模块转译、client+server 双端构建成功，产物 `.output/` 1.72MB（411kB gzip）。
- 仅一处无害 DeprecationWarning（`@vue/shared` package.json exports 尾斜杠映射，来自 nitro error-500 模板），不影响构建。
- 结论：图片上传链路（useApi.ts uploadAPI + VideoEditor 上传按钮）改动不影响整体编译，前后端均健康。

## 运行时联调（通过）
- `POST /api/v1/upload/image` 实测返回 `{ code:200, data:{ url:"/static/uploads/xxx.png", path:"static/uploads/xxx.png" } }`
- 文件落盘 `data/static/uploads/`，`GET /static/...` 返回 `200 image/png`（70bytes）
- 核心链路确认：`normalizeVideoReferenceUrl`（video-generation.ts L191-209）对 `/static/` 前缀 `slice(1)` → `readImageAsCompressedDataUrl` → `getAbsolutePath`，image-generation.ts L207-223 同样支持；上传返回的 `url` 与后端期望格式完全匹配，链路闭环
- 环境坑：系统默认 Node 被切到 v24.0.0，better-sqlite3 报 `ERR_DLOPEN_FAILED`（NODE_MODULE_VERSION 127 vs 137）；`nvm use 22.20.0` 切回后正常

## 数据层死字段清理（通过）
- 排查「道具/资产系统移除」重构完整性：后端 `schema.props`/`schema.assets`/`FROM props`/`props.` 均 0 残留，移除彻底。
- 清理死字段（均无任何读写引用，道具/MinIO 已废弃），共 5 处：
  - `image_generations.prop_id`（schema.ts `propId` + index.ts SQL）
  - `image_generations.minio_url`、`video_generations.minio_url`（schema.ts `minioUrl` ×2 + index.ts SQL ×2）
- 验证：read_lints 0 错误、`minio|propId|prop_id|FROM props` 全后端 0 残留、typecheck exitCode 0。
- 确认 `video-probe.ts`（未跟踪新文件）已被 video-generation.ts / webhooks.ts 正确引用（异步提供商未返回 duration 时 ffprobe 探测时长），非孤立文件。
- 前端 API 封装复查无缺口：agent 端点经 useAgent.ts 接入；webhook 是 Vidu 服务端回调（不应前端调用）；preset-framework 占位骨架。

## 配置系统接入（通过）—— 修复「config.yaml 死配置」bug
- 问题：`configs/config.yaml` 定义了 server.port/host/cors_origins、database.path、storage.local_path 等，但后端完全不读它（端口 `env PORT||5679`、CORS 硬编码、DB `env DB_PATH||data/drama.db`、存储 `env STORAGE_PATH||data/static`），且 config 里 `storage.local_path` 写成 `./data/storage` 与代码实际 `data/static` 不一致。README 引导编辑 + Docker 挂载它 → 用户改了不生效。
- 方案（用户选定「接入配置加载」）：新增 `backend/src/config.ts`，用 `yaml` 库（eemeli，自带 TS 类型、纯 ESM）解析 config.yaml，优先级 **env > config.yaml > 默认值**。
- 改动：
  - 新增 `backend/src/config.ts`：导出 `config.server.port/host/corsOrigins`、`config.database.path`、`config.storage.localPath`；相对路径 `resolvePath` 统一解析到项目根；`CONFIG_PATH` 环境变量可覆盖配置文件位置。
  - `connection.ts` / `storage.ts` / `index.ts` 改读 config；index.ts 的 `serve` 加 `hostname`、CORS 用 `config.server.corsOrigins`。
  - `config.yaml` / `config.example.yaml` 的 `storage.local_path` 修正为 `./data/static`。
  - `README.md` 同步修正示例路径 + 补充「配置优先级」说明。
- 验证：typecheck 0 错误、内联脚本打印 config 确认解析正确（port 5679 / cors [3013] / db `data\drama.db` / storage `data\static`）、后端启动 health=200 且 5679 监听 0.0.0.0。
- 备注：`config.yaml` 是 gitignored 本地文件（diff 不显示），只有 `config.example.yaml` 被跟踪。

## AI 服务商列表动态化（完成）—— 闭环 /ai-providers 端点 + 消除前端硬编码 + 修复 video 预设不一致
- 背景：承接上轮「AI 服务商列表动态化」结论（`ai_service_providers` 表无种子数据、端点返回空、前端硬编码），本轮完成闭环。
- 后端：
  - `schema.ts` `aiServiceProviders` 加 `endpointPrefix`(endpoint_prefix)、`isRecommended`(is_recommended, boolean mode)
  - `db/index.ts` CREATE TABLE 加两列 + `ensureColumn` 两行
  - `aiConfigs.ts` 新增 `PROVIDER_CATALOG`（10 个 provider×service_type 组合，含 endpoint_prefix + recommended 标记，`as const`）+ 导出 `seedServiceProviders()`（按 name `${provider}-${serviceType}` 幂等 upsert，表有数据则跳过）
  - `index.ts` 启动时（serve 前）调用 `seedServiceProviders()`
  - **修复真实 bug**：`PRESET_SERVICES` 的 video 从 `minimax/hailuo-02` 改为 `volcengine/doubao-seedance-1-5-pro-251215`（与前端预设卡片一致；原先前端展示 volcengine 但 quick-preset 实际写入 minimax）
- 前端：
  - `useApi.ts` 新增 `aiProvidersAPI.list()`
  - `settings.vue`：`providers`/`providerPresets`/`presetCards`/`endpointPrefixes` 四个硬编码 const 改 `ref`，新增 `loadProviders()` 从 `/ai-providers` 拉取覆盖（后端不可用时静默降级到硬编码），`onMounted` 调用；`serviceMeta[st].label` 用于卡片 label
- 验证：后端 `npm run typecheck` exitCode 0；前端 build 被用户跳过。settings.vue 的 226 个 lint 错误与 VideoEditor.vue 同源（volar 未绑定 `<script setup>`），预存非本轮引入，esbuild 转译不受影响。
- 未继续项：剧目统计（前端无统计 UI、status 基本为 draft 无信息量）；VideoEditor/settings.vue 的 volar「找不到名称」误报（工具链问题非代码 bug）。

## 前后端字段一致性审查（完成）—— 修复 scene/[sceneId].vue 字段错位 bug
- 做了一次系统性的前后端字段映射审查（后端 schema/routes vs 前端 useApi + 各页面）。
- **修复真实 P1 bug**：`scene/[sceneId].vue`（场景详情页）用了错误字段名——读 `s.timeOfDay`/`s.lightning`、存 `time_of_day`/`lightning`，但后端 `scenes` 表字段是 `time` 和 `lighting`。结果「时间设定」下拉和「光线」输入框**永远读不到、保存即丢**。
  - 改为：模板下拉 `v-model="form.time"`（option value 由英文枚举 dawn/morning/... 改为中文「黎明/清晨/正午/午后/黄昏/夜晚/深夜」，与全项目 `scenes.time` 自由文本一致）；光线 `v-model="form.lighting"`；读取 `form.time = s.time`、`form.lighting = s.lighting`；保存 `time`/`lighting`。
  - 注意：资源库 `library/scenes.vue` 的 `timeOfDay` 是**正确的**——它对应 `scene_templates` 表的独立 `time_of_day` 字段（schema.ts L342），与分镜 `scenes` 表的 `time` 是两套，勿混淆。`sceneLibrary.ts` apply 时 `tpl.time_of_day → scenes.time` 映射正确。
- **确认无问题的字段**（避免误报）：
  - `character/[characterId].vue`：全 camelCase（voiceStyle/voiceModel/...），后端 characters.ts PUT 兼容 snake_case+camelCase 双写。
  - episode 页面 `updateField` 传 snake_case（shot_type/angle/movement/scene_id），后端 storyboards.ts PUT 的 `fieldMap` 全覆盖。
  - `dramas.ts GET /:id` 返回嵌套 `characters/scenes/episodes`（toSnakeCase），前端 `drama.characters/scenes/episodes` 读取一致。
- **发现的孤儿端点（未改，记录）**：`PUT /dramas/:id/characters`、`PUT /dramas/:id/episodes` 前端从不调用（dramaAPI 无对应方法），且内部 `{ ...char }` 直接把 body 的 snake_case 字段展开传给 drizzle，将来接入会报 `no such column`；当前不影响功能，留待接入时按白名单映射重写。
- 验证：scene/[sceneId].vue read_lints 0 错误；`timeOfDay|time_of_day|lightning` 在 frontend/app 仅剩 library/scenes.vue（正确场景）。

## 孤儿端点字段映射修复（完成）—— dramas.ts 两个 Save 端点
- 承接上节「孤儿端点（未改，记录）」，本轮已修复（不再「留待接入」）：
  - `dramas.ts` 新增通用 `pickFields(src, camelKeys)` 辅助函数：snake_case/camelCase 双写 → drizzle camelCase 列名，返回 `any`（动态 key 无法被 drizzle 静态推断，交由运行时校验）。
  - `PUT /dramas/:id/characters`：`{ ...char }` 直接展开 → `pickFields(char, CHAR_KEYS)` 白名单映射（name/role/description/appearance/personality/voiceStyle/imageUrl/referenceImages/seedValue/sortOrder/localPath/voiceSampleUrl/voiceProvider/voiceSpeed/voiceEmotion/voicePitch/clothing/weapons/customPrompt/voiceModel 共 20 字段）。
  - `PUT /dramas/:id/episodes`：`{ ...ep }` 直接展开 → `pickFields(ep, EP_KEYS)` 白名单映射（episodeNumber/title/content/scriptContent/description/duration/status/videoUrl/thumbnail/imageConfigId/videoConfigId/audioConfigId 共 12 字段）；insert 分支 `episodeNumber`/`title` 兜底 `|| 1` / `|| '未命名'`。
  - 两个端点仍是孤儿（前端 dramaAPI 无对应方法），但已消除「将来接入报 no such column」隐患。
- 核查结论：videos.ts / merge.ts / compose.ts 参数映射无问题——前端 composeAPI（`/compose/storyboards/:id/compose` 等）与 mergeAPI（`/merge/episodes/:id/merge`）的 `/compose/`、`/merge/` 前缀与 index.ts 挂载 `api.route('/compose', compose)`、`api.route('/merge', merge)` 完全一致，非路径 bug。
- 验证：dramas.ts read_lints 0 错误；typecheck 首次报 TS2769（`Record<string,any>` 展开导致 drizzle 无法推断 `name` 必填），改 `pickFields` 返回 `any` 后 read_lints 0 错误（命令行 typecheck 被用户跳过）。

## 前端残留 snake_case 写入点扫描（通过，无 bug）
- 全量扫描 frontend/app 的 snake_case 字段读写，区分「读取双写兼容」（`x.image_url || x.imageUrl`，防御性无害，保留）与「写入」（POST/PUT body 或 query 参数，需与后端端点字段名对齐）。
- 逐一核对所有**写入点**，全部与后端读取字段一致，无错位：
  - `episodeAPI.create`（drama/[id]/index.vue）发 `{ drama_id, title, image_config_id, video_config_id, audio_config_id }` → 后端 `POST /episodes` 读同名 snake_case ✅
  - `characterAPI.voiceSample/generateImage/batchImages` 发 `{ episode_id, character_ids }` → 后端 characters.ts 读 `body.episode_id/character_ids` ✅
  - `sceneAPI.generateImage` 发 `{ episode_id }` → 后端 scenes.ts 读 `body.episode_id` ✅
  - `imageAPI.list` query `drama_id/storyboard_id` → 后端 images.ts 读 `c.req.query(...)` ✅
  - `videoAPI.generate`（VideoEditor.vue）发 `{ storyboard_id, drama_id, prompt }` → 后端 videos.ts 读 snake_case ✅
  - `useAgent.ts` 直连 `/api/v1/agent/:type/chat` 发 `{ message, drama_id, episode_id }` → 后端 agent.ts 读 `drama_id/episode_id` ✅
  - `aiConfigAPI.quickPreset` 发 `{ api_key }` → 后端 aiConfigs.ts 读 `body.api_key` ✅
- 结论：前端 snake_case 写入全部是**后端接口契约**（query/body 参数），非字段映射 bug；字段映射审查（场景详情页 timeOfDay/lightning 错位 + 孤儿端点 {...} 展开）两项真实 bug 均已闭环，本轮无新增问题。

## 运行时冒烟测试（通过）+ 修复 soft-delete 泄漏 bug
- 冒烟方式：后台 `Start-Process cmd /c "npm run start"` 拉起后端（5679），用 `curl.exe --data-binary @临时json文件` 发请求（PowerShell 直传 JSON body 引号会被吞，需落临时文件规避）；better-sqlite3 直连 `../data/drama.db` 做物理清理（backend package.json 是 `"type":"module"`，清理脚本须用 `.mjs` ESM 语法）。
- **验证场景字段修复**：`PUT /scenes/1` 写 `{time:"SMOKE-TEST-TIME",lighting:"SMOKE-TEST-LIGHTING"}` → GET 读回正确 → 恢复原值（time="白天/正午，阳光明媚"）。time/lighting 保存链路闭环 ✅
- **验证孤儿端点**：`PUT /dramas/1/characters`、`/episodes` insert 分支均返回 code:200，无 `no such column` ✅
- **冒烟中发现并修复 soft-delete 泄漏 bug**：characters 的 DELETE 是 soft delete（`deletedAt: now()`），但读取路径全都不过滤 `deletedAt`，导致软删角色仍出现在 `GET /dramas`、`GET /dramas/:id` 嵌套 characters 里（前端删除角色后刷新仍看到，假象「删除失效」）。修复：
  - `dramas.ts` `GET /` 嵌套 eps/chars/scns 三个查询 → `.where(and(eq(dramaId), isNull(deletedAt)))`
  - `dramas.ts` `GET /:id` 主查询（原 `eq(id)` 无 isNull，与 PUT/DELETE 不一致）→ 补 `isNull(deletedAt)`；嵌套 eps/chars/scns 同样补过滤
  - `characters.ts` `GET /:id` → `where(and(eq(id), isNull(deletedAt)))`（import 补 `and,isNull`）
- 验证：重启后端后重测——插入测试角色 id=8 → `DELETE /characters/8`（soft）→ `GET /dramas/1` 里 `VISIBLE_AFTER_DELETE=0`（修复前=1）✅
- 补充结论：scenes 的 DELETE 是 hard delete（`db.delete` 物理删），episodes 无 DELETE 端点，二者 `deletedAt` 恒为 null，补过滤属防御性统一；三表（episodes/characters/scenes）schema 均有 `deletedAt` 字段。
- 遗留：临时文件已全部清理，后端已停。

## 全表 soft-delete 一致性排查（完成）—— characters 表 22 处读取/操作泄漏全部修复
- **全表删除语义清单**（作为后续开发的约定依据）：
  - **soft delete 表**（DELETE 打 `deletedAt: now()`，读取必须过滤 `isNull(deletedAt)`）：`characters`、`dramas`、`agentConfigs`、资源库 4 表（`character_templates`/`scene_templates`/`weapon_templates`/`costume_templates`，均在 *Library.ts，已验证全部过滤 ✅）
  - **hard delete 表**（`db.delete()` 物理删，`deletedAt` 恒 null）：`scenes`、`storyboards`、`videos`、`images`、`aiConfigs`、`aiVoices`(sync 清空)
  - **无 DELETE 端点**（`deletedAt` 恒 null）：`episodes`、`videoMerges`(merge.ts)、`videoGenerations`
- **修复根因**：上一轮只修了 dramas.ts 嵌套 + characters.ts GET /:id，但 characters 是 soft delete 表，其**其余 22 处读取/操作路径**全都不过滤 `deletedAt`，导致软删角色在生成/验证/统计/更新等场景"复活"泄漏。
- **本轮修复清单（11 文件 / 22 处 + import 补 `and,isNull`）**：
  1. `characters.ts`：PUT 更新+读回、generate-voice-sample、generate-image、batch-generate-images（4 处）
  2. `episodes.ts`：pipeline-status 的 chars 查询（1 处）
  3. `prompt-utils.ts`：getStoryboardCharacterAppearances、getCharacterImageUrls（改 WHERE inArray 替代全表+filter）、validateDialogueCharacterConsistency（3 处）
  4. `storyboards.ts`：getCharacterVoiceParams、validateTTSSpeaker、TTS stale 检测（3 处）
  5. `grid.ts`：buildCharacterAppearanceMap、collectGridReferenceAssets（2 处）
  6. `ffmpeg-compose.ts`：合成内联 TTS voice 查询（1 处）
  7. `image-generation.ts`：生成回调写回 imageUrl（2 处，replace_all，防竞态写软删记录）
  8. `grid-prompt-tools.ts`：generate_character_prompt 按 id 查（1 处）
  9. `voice-tools.ts`：按 dramaId 查（1 处）
  10. `storyboard-tools.ts`：按 dramaId、按 link.characterId、按 id（3 处）
  11. `dramas.ts`：`PUT /:id/characters` upsert 按 id 更新（1 处，防按 id 更新软删记录）
- **已覆盖无需改**（内存 `filter(!deletedAt)` 方式）：`extract-tools.ts`（2 处）、`grid-prompt-tools.ts` read_characters、`episodes.ts` GET /:id/characters 与 GET /:episode_id/storyboards。
- 统一写法：SQL 层 `where(and(eq(...), isNull(schema.characters.deletedAt)))`；全表扫描点改成 `where(and(inArray(id, [...]), isNull(deletedAt)))` 替代「全表 + 内存 filter」。
- 验证：11 个修改文件 read_lints 全部 0 错误；全量 grep `from(schema.characters)` 复核，27 处命中全部已过滤（SQL isNull 或内存 filter），0 遗漏。
- 备注：dramas/agentConfigs/资源库 4 表此前已过滤，本轮只补 characters 相关泄漏；未做运行时冒烟（纯查询条件追加，低风险）。

## soft-delete 修复运行时冒烟（完成）—— 10/10 通过，22 处修复核心端点全部验证闭环
- 冒烟方式：`Start-Process cmd /c "npm run start > smoke-server.log 2>&1"` 后台拉起后端（5679）→ 用 Node 22 全局 `fetch`（`.mjs` 脚本，避免 PowerShell 传 JSON 引号被吞）+ `better-sqlite3` 直连物理清理，脚本 `backend/smoke-softdelete.mjs`。
- 流程：`POST /dramas` 建测试 drama（自动建 1 集 episode）→ `PUT /dramas/:id/characters` 建测试角色 → `DELETE /characters/:id` 软删 → 验证 6 个读取/操作端点不再泄漏 → finally 物理 DELETE 测试 drama/episodes/characters。
- 断言结果（全部 PASS）：
  1. 软删前 `GET /characters/:id` 可见（code 200 + name 匹配）
  2. 软删后 `GET /characters/:id` 不可见（code 404 "Character not found"）
  3. 软删后 `PUT /characters/:id` 读回为空（`data:null`，不复活）
  4. 软删后 `GET /dramas/:id` 嵌套 characters 不含该角色（charCount=0）
  5. 软删后 `GET /dramas?page=1` 列表嵌套 characters 不含该角色（listCharCount=0）
  6. 软删后 `GET /episodes/:id/pipeline-status` 的 `steps.extract_characters.count` = 0
- 结论：上一轮 22 处修复中「可经 HTTP API 直接验证」的核心路径全部闭环，运行时 drizzle `and(eq(), isNull())` 语法生成 SQL 正确无报错；内部服务函数（prompt-utils/storyboards/ffmpeg-compose/image-generation/agent-tools）因需完整管线（AI 生成/合成）触发，未做端到端，但 SQL 写法与已验证路径一致，风险低。
- 收尾：后端进程已停（kill 5679 监听进程）；`smoke-softdelete.mjs` 与 `smoke-server.log` 两个临时文件**用户选择保留**（未删除），如需复用冒烟脚本可改 title 常量重跑。

## 前后端接口契约梳理（完成）—— 生成 docs/api-contract.md + 修复 1 处字段契约不一致
- 方式：原计划调 api-auditor 子代理，但其全局配置 `C:\Users\Administrator\.codebuddy\agents\api-auditor.md` 的 `model: glm-4.6` 当前环境不可用（报 model 不存在），改为主 agent 直接用 search_content/read_file 完成梳理。
- 覆盖：后端 22 个路由文件全部端点 + 前端 useApi.ts / useAgent.ts 全封装 + 页面调用点交叉核对。
- 结论：前后端路径/方法 100% 对齐，无「前端调用不存在的端点」；字段契约仅发现 1 处真实不一致。
- **修复 P2 bug**：`videos.ts` `POST /videos/:id/regenerate` 端点 `referenceImageUrls: body.reference_image_urls` 缺 `|| row.referenceImageUrls` 回退（同函数 imageUrl/firstFrameUrl/lastFrameUrl/duration/aspectRatio 都有回退，唯独它没有）。当镜头视频原以 reference_mode='multiple'（多角色参考图）生成后，在 VideoEditor「重新生成」会丢角色参考图。已修复。
  - 影响面：VideoEditor 的 refModeOptions 只有 ''/single/first/first_last（无 multiple），multiple 只在 episode 页面批量生成（POST /videos，走 getStoryboardCharacterImageUrls 自动补充，不受影响）；但存量 row.referenceMode='multiple' 的记录经 VideoEditor regenerate 仍会丢参考图，故修复必要。
- 孤儿端点清单（前端未接，非 bug，记录备查）：GET /dramas/stats、PUT /dramas/:id/characters、PUT /dramas/:id/episodes、GET /agent/:type/debug、preset-framework 6 端点。
- P3 隐患：skills.ts 通配符路由硬编码 `'/api/v1/skills/'.length` 前缀，改挂载前缀会失效。
- 交付物：`docs/api-contract.md`（完整端点清单 + 删除语义 + 前端 useApi 映射 + 孤儿端点 + 问题清单）。

## skills.ts 硬编码前缀隐患修复（完成）+ Hono 通配符踩坑
- 承接上节 P3 隐患（skills.ts 通配符路由硬编码 `/api/v1/skills/` 前缀），本轮已修复。
- **踩坑**：Hono 4.12 的 `/*` 通配符**不注册命名参数**——最小脚本实测 `c.req.param('*')` 返回 `undefined`、`c.req.param()` 返回 `{}`。所以不能像 Hono 文档示例那样用 `c.req.param('*')` 取通配符内容。
- **正确方案**：`c.req.routePath` 会合并挂载前缀成 `/api/v1/skills/*`，用 `routePath.slice(0, routePath.indexOf('*'))` 动态推导前缀，再 `c.req.path.slice(prefix.length)` 取 id。新增 `wildcardId(c: Context)` 辅助函数，三处（GET/PUT/DELETE `/*`）统一替换。
- 冒烟验证（临时最小脚本 + 真实后端双验证）：`GET /api/v1/skills` 列表 200；`GET /api/v1/skills/extractor` 单 skill 200 且 content 正确读出；多段嵌套 id `foo/bar`、空路径由最小脚本验证通过。
- docs/api-contract.md 已同步删除该 P3 条目与警告注释。临时文件（tmp-param-test.mjs / smoke-skills.log / .err）已清理，测试后端已停。

## 全局 agent 模型失效修复（完成）—— 本地 + 云端双轨
- **根因**：`C:\Users\Administrator\.codebuddy\agents\` 下 15 个 agent 的 `model:` 全部写的是 `glm-4.6`，但当前环境 `models.json` 里没有该模型（这正是上一节 api-auditor 子代理调用报 model 不存在的原因，影响面是全部 15 个而非 1 个）。
- **可用模型**（`C:\Users\Administrator\.codebuddy\models.json`）：云端 `deepseek-v4-pro`/`deepseek-v4-flash`；本地 Ollama `Qwen2.5-Coder`/`LLaVA`（支持图像）/`deepseek-coder:6.7b`。
- **修复方案（用户要求支持本地+云端两种方式）**：agent frontmatter 的 `model:` 是单值，无法单 agent 双挂，故按任务类型分组——
  - 本地 `deepseek-coder:6.7b`（3 个轻量文本/审查类）：`api-auditor`、`code-reviewer`、`commit-helper`
  - 云端 `deepseek-v4-pro`（其余 12 个，复杂推理/架构/UI 类）
- **验证**：`^model:` 搜索 15 个文件全部命中，0 个 glm-4.6 残留。
- **注意**：本地 Ollama 服务当前未运行（`localhost:11434` 连接失败），3 个本地 agent 需先 `ollama serve` 启动并拉取 `deepseek-coder:6.7b` 才可用；届时若想改用 `Qwen2.5-Coder` 或 `LLaVA` 直接改对应 agent 的 model 字段即可。

## 云端 agent 修复实测验证（通过）
- 用户选择「实测验证 agent 修复效果」，实际调用云端 `fullstack-expert`（`model: deepseek-v4-pro`）跑小任务验证。
- 结果：正常响应，不再报 `glm-4.6` model 不存在；顺带审查 `dramas.ts` `GET /` 的 soft-delete 过滤（主查询 + episodes/characters/scenes 嵌套）确认完整无遗漏。
- 结论：`glm-4.6 → deepseek-v4-pro` 切换后云端子代理真实可用；本地 3 个 agent 仍待 Ollama 启动后另行验证。

## 本地 agent 实测 + 二次修复（完成）—— deepseek-coder:6.7b 不支持 tools
- **实测发现真实 bug**：本地 3 个 agent 原指向 `deepseek-coder:6.7b`，实测调用 `code-reviewer` 报 `registry.ollama.ai/library/deepseek-coder:6.7b does not support tools` —— 该模型不支持 tool calling，无法作为 agent 后端（agent 框架强依赖 tools）。
- **排查**：`ollama show` 对比 3 个本地模型 Capabilities——仅 `qwen2.5-coder:latest` 含 `tools`；`deepseek-coder:6.7b`（llama 架构）和 `LLaVA`（llama+clip）均无 tools。`ollama list` 确认 `qwen2.5-coder:latest`（4.7GB）已拉取、服务运行中（`D:\app\ollama\ollama.exe`）。
- **二次修复**：
  - `models.json`：`Qwen2.5-Coder` 的 id/name → `qwen2.5-coder:latest`（对齐 ollama 实际模型名，原 id 大小写+tag 不一致会导致 agent 匹配不到）
  - 3 个 agent（`api-auditor`/`code-reviewer`/`commit-helper`）：`model: deepseek-coder:6.7b` → `model: qwen2.5-coder:latest`
- **验证**：实测 `code-reviewer`（qwen2.5-coder:latest）正常响应"本地 agent 调用正常" ✅。但暴露能力局限：耗时 1320s（云端 4.3s）、0 次工具调用、审查结论误报（称 `characters.ts GET /:id` 缺 `isNull(deletedAt)` 过滤，实际 L17 已正确过滤）——本地模型不读文件凭训练数据瞎猜。
- **⚠️ 环境限定（用户澄清 2026-08-16）**：本次本地模型实测是在**笔记本电脑**上跑的，不是用户的工作站。因此「1320s 极慢」「0 次工具调用」「审查误报」这几点结论**仅代表笔记本环境（CPU 推理、无 GPU 加速）下的表现**，不能外推到工作站。工作站的 Ollama 推理速度与 tool calling 稳定性需另行实测后再下结论。
- **结论**：本地+云端双轨真正闭环（云端 12 个 + 本地 3 个均实测可用）；但本地模型在笔记本上仅适合离线/省钱的轻量文本任务，涉及真实代码审查/读取的结论不可信，重要审查务必用云端 `deepseek-v4-pro`。

## 「完善现有功能」自主排查 + 三项完善（完成）
- 用户指令「你现在要完善现在的功能」，追问后用户选「我全权判断」（自主排查半成品/占位功能，按优先级完善并汇报）。排查后锁定 3 个真实待完善点并全部实施。

### P1 首页进度真实化（后端 dramas.ts + 前端 index.vue）
- 原首页进度 `getProgress` 只用「有剧本集数 / 总集数」，无法反映真实制作进度。
- 后端 `dramas.ts` `GET /` list enriched 逻辑：import 补 `inArray`；新增分镜统计（`inArray(storyboards.episodeId, eps.map(id))` + `isNull(deletedAt)`），返回 `progress: { total_episodes, scripted_episodes, storyboarded_episodes, storyboards, images, videos, tts }`。判定口径：scripted=有 scriptContent 的集数；storyboarded=有分镜的集数；images=有 composedImage/firstFrameImage 的分镜数；videos=有 videoUrl/composedVideoUrl 的分镜数；tts=有 ttsAudioUrl 的分镜数。
- 前端 `index.vue` `getProgress` 改为 5 阶段加权平均（剧本 20% + 分镜 20% + 图片 20% + 视频 20% + 配音 20%），无 total_episodes 返回 0。

### P2 首页统计看板接入（useApi.ts + index.vue）
- 后端 `GET /dramas/stats` 端点早已存在（返回 `{ total, by_status: [{status,count}] }`）但前端从未接入。
- 前端 `useApi.ts` `dramaAPI` 新增 `stats()`；`index.vue` 新增 `stats` ref、`STATUS_LABELS`/`STATUS_COLORS`、`statusLabel()`/`statusColor()`，`load()` 并行调 `dramaAPI.list()` + `dramaAPI.stats()`，新增 `stats-bar` 模板（全部项目数 + 各状态 chip）及样式。

### P2 Skill 绑定拖拽排序（settings.vue）
- 后端 `loadAgentSkills` 已按 `priority` 数字升序排序加载，仅缺前端拖拽 + 持久化。
- 前端 `settings.vue` 新增 `skillDragFrom`/`skillDragOverIdx` refs 与 `onSkillDragStart/Over/Drop/End`；`skill-bind-item` 加 `:draggable="true"` + drag 事件 + `.drag-over` 高亮样式；提示由「拖拽排序即将支持」改为「拖拽可调整优先级」；保存时按数组顺序重写 `priority: i+1` 序列化，确保后端排序生效。

### 验证
- 后端 `npx tsc --noEmit`（typecheck）exitCode 0；修改文件 read_lints 全部 0 错误。
- 后端启动成功（`serviceReady: true`），`Invoke-RestMethod` 实测：`/dramas/stats` 返回 `total=1, by_status=[{status:"draft",count:1}]`；`/dramas?page_size=3` 的 `items[0].progress` = `{total_episodes:1, scripted_episodes:1, storyboarded_episodes:1, storyboards:21, images:5, videos:2, tts:13}`。
- 前端 typecheck 环境缺 typescript/vue-tsc（`ERR_PACKAGE_PATH_NOT_EXPORTED`），判定为工具链问题非代码 bug，与 settings.vue 同源 volar 误报同源。

## 「继续完善」—— 修复角色/场景详情页 episode_id 错传 bug（完成，P1）
- **根因**：角色/场景的「重新生成形象图/场景图」和「试听声音」是 **drama 级** 操作（角色/场景跨集共享，不绑定具体 episode），但后端三个端点 `POST /characters/:id/generate-image`、`/generate-voice-sample`、`POST /scenes/:id/generate-image` 都**强制要求 episode_id** 并从中取 `imageConfigId`/`audioConfigId`。前端详情页 URL 只有 dramaId（无 episodeId），于是**错误地把 dramaId 当 episode_id 传**，导致 `episodes where id=dramaId` 大概率查不到 → 返回 "Episode not found"，三个按钮全部失效。
  - 注：当前测试库 drama id 与 episode id 恰好都是 1 才掩盖了此 bug；多集/多剧真实数据下必现。
- **修复（后端 drama 级配置兜底）**：
  - `characters.ts` / `scenes.ts` 各新增 `resolveDramaConfigId(dramaId, field)` 辅助函数：查该 drama 下第一个已配置 configId 的 episode，否则返回 undefined（走全局默认配置）。
  - 三个端点 `episode_id` 改为**可选**：`const ep = body.episode_id ? 查episode : undefined`，`if (body.episode_id && !ep) return 404`；configId 改为 `ep?.xxxConfigId ?? resolveDramaConfigId(dramaId, field)`；日志 `ep?.id`。scenes.ts import 补 `and,isNull`。
- **修复（前端不传 dramaId）**：
  - `useApi.ts`：`characterAPI.voiceSample`/`characterAPI.generateImage`/`sceneAPI.generateImage` 的 `episodeId` 参数改为可选 `episodeId?`。
  - `character/[characterId].vue`：`generateImage(characterId, undefined, {...})`、`voiceSample(characterId)`。
  - `scene/[sceneId].vue`：`generateImage(sceneId, undefined, {...})`。
  - 关键机制：`JSON.stringify({ episode_id: undefined })` 会忽略 undefined 字段，body 里不含 episode_id，后端 `body.episode_id` 为 undefined 走兜底。
- **验证（运行时实测，全部通过）**：后端 `tsc --noEmit` exitCode 0；5 个修改文件 read_lints 0 错误；重启后端后 `Invoke-RestMethod` 实测——`POST /characters/1/generate-image`（空 body）→ `image_generation_id:27`；`POST /scenes/1/generate-image`（空 body）→ `image_generation_id:28`；`POST /characters/1/generate-voice-sample`（空 body）→ `voice_sample_url:"static/audio/xxx.mp3"`。均不再报 "episode_id is required"/"Episode not found"。
- **遗留（未改，记录）**：episode 页面的 `batchImages`（批量生成角色图）有真实 episode 上下文，仍传 episode_id，行为不变，正确。
- 注：后台启动方式 `Start-Process cmd /c "npx tsx src/index.ts > backend-run.log 2>&1"`，验证后后端进程仍在运行（未停）。

## 「继续完善」第三轮排查 —— 核心制作流水线确认全部闭环（无新 bug）
- 承接上轮，本轮系统排查剩余链路，结论：**核心制作流水线（剧本→分镜→图片→视频→配音→合成→拼接→宫格图）前后端已全部闭环，未发现新的 P0/P1/P2 半成品/功能失效**。
- **后端链路排查（均完整闭环，无 stub）**：`merge.ts`（POST/GET `/episodes/:id/merge`，status 返回 toSnakeCase，前端判断 `status==='completed'/'failed'` 一致）、`grid.ts`（prompt/generate/split/history 四端点）、`compose.ts` + `ffmpeg-compose.ts`（字幕烧录+配音合成，`supportsSubtitleFilter` 检测）、`webhooks.ts`（Vidu 服务端回调 + ffprobe 探测时长）、`agent.ts`（Mastra agent 调用）、`images.ts`、`ffmpeg-merge.ts`。全后端 grep `TODO|FIXME|占位|not implemented|stub` 无命中（除刻意保留的 preset-framework THEME_FAMILY_A 占位数据池）。
- **前端按钮↔处理函数完整性核对（episode 页面 4976 行全量）**：模板所有 `@click/@submit/@change/@input` 绑定函数逐一比对 `<script setup>` 定义，21 个关键函数（ 等）全部有定义，无「点击无反应」；`navigateTo` 为 Nuxt3 自动导入。`doMerge`/`batchCompose`/`pollComposeStatus` 轮询逻辑完整。
- **library 4 页面 + components（5 个）**：grep `即将|暂不|敬请期待|待实现|TODO|FIXME` 0 命中，资源库 CRUD 齐全。
- **排查结论（备查，避免重复排查）**：merge/grid/compose/webhooks/agent 五条链路 + episode 工作台全部闭环，后续「继续完善」应转向功能增强（而非找残缺），或按用户指定方向。episode 页面 `characterAPI.voiceSample(id, epId.value)`/`batchImages(ids, epId.value)` 传真实 episode id 是正确的（与上轮 drama 级详情页修复区分，勿混淆）。

## 「继续完善」第四轮 —— 修复首页「AI 一键生成」按钮半成品（完成，P1）
- **发现的真实半成品**：首页 `index.vue` 顶部「AI 一键生成」按钮 `@click="showAutoGen = true"`，但 `showAutoGen` 在 `<script setup>` 里**完全未定义**（只有 `showCreate`），且模板里**没有对应弹窗**。点击会抛运行时错误（ReferenceError: showAutoGen is not defined），按钮彻底失效。
- **根因链**：后端「一句话梗概→整集短剧」全自动管线 `routes/auto-pipeline.ts` + `services/auto-pipeline.ts` 已完整实现并注册（index.ts `api.route('/auto-pipeline')` + 启动 `recoverAutoPipelineOnStartup()`）；前端 `useApi.ts` 的 `autoPipelineAPI`（run/status/resume）也早已定义，但**零页面调用**。按钮是遗留的残缺入口。
- **后端 auto-pipeline 契约（备查）**：
  - `POST /auto-pipeline/run` body `{ premise, title?, genre?, style?, episodeCount?, withImages?, withVideos?, withCompose?, withMerge? }` → `{ dramaId, episodeIds }`；`normalizeOptions` 自动补默认值（title=premise 前 24 字、genre='短剧'、style='realistic'、episodeCount=1）；drama.status 创建为 `auto_generating`，管线完成后**不改 drama.status**（靠 episode 完成度判断）。
  - `GET /auto-pipeline/status/:dramaId` → `{ dramaId, title, status, totalEpisodes, doneCount, failedCount, running(done+failed<total), episodes:[{ id, episodeNumber, status, hasScript, hasVideo, storyboardCount, characterCount, sceneCount, imageReadyCount, videoReadyCount, composedCount }] }`。
  - episode.status 阶段值 `auto:queued/scripting/extracting/storyboarding/voicing/imaging/videoing/composing/merging/done/failed`。
- **修复（frontend/app/pages/index.vue）**：
  - 新增 `showAutoGen/autoForm/autoRunning/autoStatus/autoDramaId/autoTimer` 状态 + `AUTO_STAGE_LABELS` 阶段中文映射 + `autoStageLabel/autoEpClass/runAutoGen/startAutoPolling/stopAutoPolling/closeAutoGen/goAutoDrama` 函数；`autoPipelineAPI` 导入。
  - 弹窗两态：输入态（premise 必填 textarea + 标题/集数/类型/风格 BaseSelect + 4 个媒体开关 checkbox）+ 进度态（各集阶段状态 pill + 分镜/图片/视频计数 + 「查看项目」跳转 `/drama/:id`）。
  - 轮询：run 成功拿到 dramaId 后 `setInterval(2.5s)` 轮询 status，`running===false` 停止并 `load()` 刷新列表；`onUnmounted(stopAutoPolling)` 清理。
  - 补 `STATUS_LABELS/STATUS_COLORS` 的 `auto_generating: 'AI 生成中'`，让 AI 项目在首页列表正确显示中文状态。
- **确认非 bug（避免误报，备查）**：① 前端图片 `'/' + imageUrl` 拼接——后端 `storage.ts` 的 `downloadFile/saveUploadedFile/saveBase64Image` 统一返回**不带前导斜杠**的 `static/...`，`'/' +` 正确（system 里少量带 `/static/` 前导的值由 `image-generation.ts`/`video-generation.ts` 的 `normalizeVideoReferenceUrl` 做 `slice(1)` 兼容）；② `characterAPI.voiceSample(characterId)` 只传 1 参——后端 `episode_id` 是可选 body，不传走 drama 级 audioConfigId 兜底，正常。
- 验证：`index.vue` read_lints 0 错误。未做运行时冒烟（纯前端补齐，后端接口契约已确认一致）。

## 「继续完善」第五轮 —— 剧集删除/重命名入口（完成，P2 功能缺口）
- **缺口**：剧集卡片 `ep-card` 只有「点击进入工作台」，无删除/重命名入口。后端 `episodes.ts` 有 POST（创建）/PUT（更新）/GET，但**无 DELETE 端点**；前端 `episodeAPI` 无 `del`。
- **后端修复（routes/episodes.ts）**：
  - 新增 `DELETE /episodes/:id` —— soft delete（`set({ deletedAt: now(), updatedAt: now() })`），与 dramas/characters 一致；`episodes` 表本就有 `deletedAt` 字段，`dramas.ts` 详情接口加载 episodes 时已 `isNull(deletedAt)` 过滤，故软删后列表自动隐藏，无需改 dramas。
  - **附带修 bug**：POST 创建集时算 `nextNum` 的 `existing` 查询原本**未过滤 `deletedAt`**，软删后新建集会拿到错乱的集数编号；已加 `isNull(schema.episodes.deletedAt)`。
- **前端修复（drama/[id]/index.vue + useApi.ts）**：
  - `useApi.ts`：`episodeAPI` 新增 `del: (id) => api.del('/episodes/${id}')`。
  - 剧集卡片 hover 时右侧显示「重命名/删除」两个操作按钮（`@click.stop` 阻止冒泡不触发展开）；删除走 `confirm()` 二次确认；重命名弹独立 `rename-dialog`（复用现有 dialog 结构，标题输入 + 保存）。
  - 新增状态 `renameDialog/renamingEpisode/renameEpisodeId/renameEpisodeTitle` + `openRenameEpisode/renameEpisode/delEpisode` 函数；`.ep-actions/.ep-action-btn/.ep-action-danger/.rename-dialog` 样式。
- 验证：`drama/[id]/index.vue` 与 `episodes.ts` read_lints 均 0 错误。
- **注意**：soft delete 只打标记，episode 的 storyboards/scenes 等子数据仍在 DB（靠 episode 级联读取时按 episode_id 关联、episode 软删后不会再被列表/详情读取到，属可接受的孤儿，与项目 dramas 软删不级联子表的既有约定一致）。

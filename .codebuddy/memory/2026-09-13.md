# 2026-09-13

> 承接 `2026-09-12.md`：Python 后端绞杀式迁移（目标「全量迁完后删除 `backend/`」）。
> S1–S5 已完成；S6 进行中（MCP ✅、评测 scorer/catalog/types ✅、skills+skill-parser ✅）。
> 上一日结束状态：**29 项自检 / 1549 用例全绿**，九道机械守卫，Node 204 路径 / Python 已注册 174。

## S6 第 4 步：评测执行器 + 评测域前 2 个端点

**新增**：`services/evaluation/evaluator.py`（← `evaluation/evaluator.ts` 210 行）+ `routers/evaluation.py`（← `routes/evaluation.ts` 62 行，**5 端点已迁 2**）+ `tests/evaluation_route_test.py`（**28 用例**）。

- ⭐ **seed 必须逐条立即提交**（每个 insert 一个 `engine.begin()`）：Agent 的工具开着**自己的**连接读库，seed 若攒在请求事务里 → Agent 看不到临时剧本/角色/场景（「对空气评分」），且 SQLite 单写者会互锁。这条已由自检用**独立连接回查**钉死（`FakeRuntime.seed_visible`）。同上：`cleanup_case` 也自管事务。
- ⭐ **抽取口径各不相同**（别"统一"）：分镜 / 剧本取**最后一次**工具调用；角色 / 场景 / 音色**跨调用累加**。工具名要先归一（`norm_tool_name`：小写 + 去非字母数字）—— Mastra 回传可能是驼峰 `saveStoryboards` 而工具 id 是 `save_storyboards`，不归一会**提取 0 命中、分数归零且不报错**。
- `cleanup_case` 删 drama/episode/角色/场景/分镜/分镜角色关联，**失败只告警不抛**；`evaluate_case` 用 `finally` 保证异常路径也清理（自检验证：Agent 抛错后库里**不留** `[bench]` 数据）。已知残留：`continuity_states` / `storyboard_props` 不删（与 TS 一致）。
- 端点契约：`/cases` **无 try**（基准目录坏 JSON ⇒ 500，不是 400）；`/evaluate/{id}` 三分支状态码不同（**case 不存在 404** / kind 未知 400 / 运行异常 400），文案带**全角冒号**（`未知基准 case：x`）。⚠️ 「kind 未知 → 400」那支**实际不可达**：catalog 扫描时就把未知 kind 过滤了（与 TS 同）。
- **未迁的 3 条**（`POST /optimize/{id}`、`GET /scheduler`、`POST /run`）**无需显式委派**：显式委派只用于「会被已注册**参数路由遮蔽**」的路径（如 `/agent-configs/defaults` 被 `/{id}` 吞）；这三条首段字面量都不同 ⇒ 直接走 catch-all（501 或反代）。自检断言它们**仍返回 501**，防止新路由把它们误吞。
- `route_parity_test.py` 的 `MIGRATED` 加 `evaluation.ts`：Node 204→**209**、已注册 174→**176**、未注册 34→**36**。**多出的 3 条正是 optimizer/scheduler 那批 ⇒ 守卫顺手当成待办清单**（每条都必须 501，等迁完自然归零）。
- 我自己的 1 处断言错误：硬编「2 个镜头在案例区间内」，实际 `sb-case-001` 的区间是 **4~8** ⇒ 改为**用案例自己的 rubric 推期望值**（`in_range = min <= n <= max`），别再硬编。

## S6 第 5 步：creator + optimize（评测域 5 端点已迁 4，`/agent-configs/generate` 解锁）

**新增**：`agents/creator.py`（← `creator.ts` 169 行）+ `evaluation/optimizer.py`（← `optimizer.ts` 216 行）+ `routers/agent_configs.py` 的 `POST /generate` + `routers/evaluation.py` 的 `POST /optimize/{id}` + 两个自检文件（各 **28 用例**）。

- ⭐ **一套投入两处收口**：`creator.py` 同时解锁了 `/evaluation/optimize` 与 `/agent-configs/generate`（后者原先 501）。守卫：已注册 176→**178**、未注册 36→**34**。
- `creator`：`AVAILABLE_SKILL_IDS` 是**模块加载快照**；skills 只留自有 id、非法**静默丢弃**、空则回退默认绑定；`systemPrompt < 50` 报错；upsert 按 `agent_type` 取**第一条**、更新时**只改** name/description/systemPrompt/skills/isActive（保留 model/temperature）且把 `deleted_at` **清回 null 复活**；`persist_agent_config` 返回 **Row**（路由用 `row_to_dict` 归一，与 `GET /agent-configs` 同口径）。
- ⚠️ **两套文案别混**：路由层 `agent_type required` / `requirement required`；creator 内部 `未知 Agent 类型：…` / `requirement 不能为空`；optimizer 的短候选报错是**英文** `Optimizer produced invalid candidate (length=N)`。
- `optimizer` 状态机四条判据（自检全钉住）：① `format_feedback` **只给维度名+分数、绝不给 detail**（防作弊）；② 候选与 Reference 的 `runtimeModel` 不同 ⇒ 本轮 **不 accept（99 分也不认）**；③ accept = 严格高于**当前 best**（允许连续晋升）；④ 落库 = `autoPersist` 且 `best.version > 0` 且 `best.score > reference.score`。`??` 语义：`iterations: 0` 就是 0。
- ⚠️ **`optimizer.py` 已加进 `json.dumps` 守卫白名单**：历史文件镜像 `JSON.stringify(history, null, 2)` ⇒ `ensure_ascii=False, indent=2`（**有意非紧凑**）。
- 我自己踩的坑：桩里用 `candidates[version - 1]` 取候选 —— 但 `version` 参数是**当前 best 的版本**（第 1 轮=0）⇒ 负索引取到最后一个；改为按「第几次调用」取。另有 `content=b"{坏"`（bytes 不能含非 ASCII）与第 7 次 Windows `⇒` 编码问题。

## S6 第 6 步：评测调度器 ⇒ **评测域整域关闭（5/5 端点）**

**新增**：`services/evaluation_scheduler.py`（← `evaluation-scheduler.ts` 126 行）+ `routers/evaluation.py` 的 `/scheduler`、`/run` + `tests/evaluation_scheduler_test.py`（**22 用例**）+ **`main.py` lifespan 里启动调度器**（与 TS 的 index.ts 一致；默认关闭，只打 warn）。

- 四条语义：**防重入**（上一轮没跑完就跳过，返回上一轮结果）｜**失败隔离**（单 case 挂了不影响其余）｜**串行**（并发 LLM 会限流/烧钱）｜默认**关闭**。`iterations` 取自 `config.evaluation.auto_optimize`（`AUTO_OPTIMIZE_*` 环境变量可覆盖）。
- ⚠️ **`running` 没有 try/finally 兜底（原 TS 的真实缺陷，保真不修）**：循环**外**出错（典型：基准目录坏 JSON ⇒ `listBenchmarkCases` 直接抛）⇒ `running` **永远卡 true** ⇒ 之后所有触发都被防重入吃掉。自检里**故意钉住**它（`保真: 循环外抛错 -> running 卡在 true`），避免后人"顺手修"。
- ⚠️ `下一轮时间` 用**本地时间**的 `setHours` 语义（今天的 HH:MM 已过则顺延次日；**秒/微秒归零** ⇒ 当前这一分钟也算已过）。
- ⚠️ 结构性差异：一轮里**每个 case 各开一个短事务**（TS 是全局 `db`，没有连接生命周期概念）—— 别把整轮包进一个事务，否则 LLM 调用期间一直占着 SQLite 写锁。
- 守卫：已注册 178→**180**、未注册 34→**32**。顺手把 `evaluation_route_test` 里那条「未迁的 N 条走兜底」改成**整域 5/5 非 501** 断言（每次迁完一个端点这条都要跟着改，已踩两次）。
- 又踩一次 Windows 编码（**第 8 次**）：新写的测试里又有 `⇒`（已用 `str.maketrans` 清）。

## S6 第 7 步：`auto-pipeline` 前置就位（sse-hub + frame-extractor）

**新增**：`services/sse_hub.py`（← `utils/sse-hub.ts` 57 行）+ `services/frame_extractor.py`（← `services/frame-extractor.ts` 86 行）+ `tests/sse_hub_frames_test.py`（**24 用例**）。⚠️ **`auto-pipeline.ts` 服务本身 744 行（8 阶段状态机）+ 路由 101 行（含唯一 SSE 端点）尚未迁** —— 下轮做。

- **sse-hub 红线**：`publish_pipeline_event` **永不抛**、订阅者异常**自吞**（单订阅者炸不影响其他、也不回抛）｜按 `dramaId` 分频道｜**频道空了自动清理**｜事件**不落盘不缓存**（历史走 `GET /status` 快照，SSE 只推增量）｜事件补 `ts`（ISO）。Python 侧 handler 仍同步，SSE 路由把它桥到 `asyncio.Queue`（同一条事件循环，`put_nowait` 安全）。
- **尾帧提取红线**：只写 `tail_frame_image`（**真实尾帧**），**绝不碰** `last_frame_image`（**设计尾帧**）—— 自检用「先塞一个设计尾帧再跑」把这条钉死。语义分离是 2026-09-03 定的：混了会让真实产物污染 shot-router 的 FL2VA 决策。
- `frame_extractor`：`seek = duration - 0.2`（从末尾往前退一点，避免落在最后一帧之外）｜`-ss` 必须在 `-i` **之前**（fluent-ffmpeg 的 `seekInput` 语义）｜`-frames:v 1 -q:v 2`｜产物落 `storage_root/frames/<uuid>.jpg`，返回相对路径 `static/frames/<uuid>.jpg`｜每个分镜**独立短事务**写回｜幂等（视频重生成后重跑刷新）。
- ⚠️ **别复用 `video_probe.probe_video_duration`**：那里探时长返回**整秒**（回填 `storyboards.duration`），这里要**浮点秒**（`-0.2` 精度）⇒ 单独实现。
- ⚠️ 路径映射两套写法**等价**（`storage_root` 就是 `<data_root>/static`）：frame-extractor 是「`static/` → data_root」，video_probe 是「剥 `static/` → storage_root」，结果相同。
- **测试缝**：给 `frame_extractor` 加了 `_run_ffmpeg(args)`（与 `ffmpeg_compose` 同形）⇒ 没装 ffmpeg 的机器也能锁住参数与状态机。
- 编码坑又中一次（**第 9 次**）：这次是**减号 `−`(U+2212)** 打不进 GBK。**教训升级：测试的打印字符串只用 GBK 可编码字符**（汉字/全角标点/`-`/`->`），别用 `⇒`、`−`、星号、emoji。

## S6 第 8 步：全自动管线整域关闭（8 阶段 + SSE）

**新增**：`services/auto_pipeline.py`（← `services/auto-pipeline.ts` **814 行**）+ `routers/auto_pipeline.py`（← `routes/auto-pipeline.ts` 101 行，**4 端点含全仓唯一 SSE**）+ `tests/auto_pipeline_test.py`（**50 用例**）+ `main.py` lifespan 里接**崩溃恢复**。守卫：Node 209→**213**、已注册 180→**184**、未注册仍 32（4 条新路径全部注册 ✓）。

- ⚠️ **阶段顺序反直觉**：`scripting → extracting → **voicing** → storyboarding`（**配音在分镜之前**），而 `STAGE_SEQUENCE` 就是幂等判据（`is_stage_completed` 比对的是 **`next` 索引**，不是当前阶段）——改顺序等于改「跳过哪些阶段」。
- ⚠️ **媒体开关有依赖补全**（`media_needs`）：`video` 隐含 `image`、`compose` 隐含 `image+video`、`merge` 隐含全部 ⇒ 只勾 `withMerge` 也要把四个媒体阶段都跑起来，否则 merge 永远缺料。
- ⚠️ **幂等三件套**：提交前查已有产物（`first_frame_image`/`video_url`/`composed_video_url`）**或**已有 `processing/pending/completed` 任务 ⇒ 崩溃重启不重复提交（**重复扣费**）。媒体轮询超时 **30 分钟 / 5 秒**一轮；资产门禁阻断的镜头视为「已结束」，不再等。
- ⚠️ **只写 `tail_frame_image`**（真实尾帧）作为下一镜起帧，**绝不覆盖 `last_frame_image`**（设计尾帧是 FL2VA 决策依据）。
- ⚠️ **同进程防重入 + follow-up 队列**：执行期间再次触发**入队**（不静默丢弃），本轮结束后自动重跑一轮；`dramas.metadata` 存 `JSON.stringify(opts)` ⇒ 必须**紧凑分隔符**；`drama` 已软删 ⇒ 一个阶段都不跑（防复活）。
- ⭐ **`asyncio.ensure_future` 在 ASGI 工作线程里会炸**：`There is no current event loop in thread 'AnyIO worker thread'` —— anyio 起的循环不保证 `set_event_loop`。**凡「从协程调用的同步代码里起后台任务」，必须用 `asyncio.get_running_loop().create_task(...)`**（并让该路由是 `async def`，否则同步路由跑在线程池里同样拿不到）。已修 `_spawn` 与 SSE 心跳两处。
- **SSE 四保证**：**先 subscribe 再发快照**（否则「订阅→快照」窗口丢事件）｜心跳 20s `event: ping`｜客户端断开在生成器 `finally` 里清订阅与心跳任务｜`X-Accel-Buffering: no`。⚠️ **自检别用 TestClient 读永不结束的流**（portal 与流互相等死，实测卡住）⇒ 直接 `await` 路由函数拿 `StreamingResponse`，自己按帧迭代 + `wait_for` 超时；且**帧以空行结尾**，收集行时要过滤空行否则索引被插位。

## S6 第 9 步：localModels 整域关闭（扫描 + 注册 + 异步任务 + NDJSON 下载）

**新增**：`services/local_model_scan.py`（← `local-model-scan.ts` **709 行**）+ `routers/local_models.py`（← `routes/localModels.ts` **688 行**，**11 端点**）+ `tests/local_models_test.py`（**89 用例**，扫描在临时目录树上做、下载全部打桩）。守卫：Node 213→**224**、已注册 184→**195**、**未注册仍 32**（11 条新路径全部注册 ✓）。

- ⚠️ **规则顺序就是一切**（12 条启发式规则，先命中先返回）：H3 的 text_encoders（qwen3vl）属**视频**、必须排在文本 LLM 之前；Wan/Hunyuan 的 GGUF 必须排在通用 GGUF LLM 之前；组件目录（vae/clip/controlnet…）必须排在「目录=LLM」与「扩散主权重」之前。顺序错 ⇒ 整片误判且**不报错**。
- ⚠️ **组件不等于跳过**：VAE/CLIP/ControlNet 都扫出来，只是 `role='component'`；而 **ComfyUI 是 `callable=false` + 空 baseUrl**（只读扫描来源，不注册为可调用后端）。`skip` 只给「组件 / comfyui 只读 / 无法识别」三类，三类**文案不同**。
- ⚠️ **同步与异步默认值不同**：同步 `maxDepth=5 / maxFiles=8000`，异步 `8 / 50000`（异步每 1000 个文件报一次进度、随时可取消，取消抛 `ScanCancelledError`）。
- ⚠️ **`maxDepth` / `maxFiles` / `taskId` 是 camelCase 查询参数** ⇒ 必须 `Query(alias=...)`（**第 7 道守卫按设计盯住了这条**）。另：这俩参数刻意声明成 **str 再自己解析**（`Number(x) || 默认值` ⇒ `?maxDepth=abc` 要回退默认值而不是 422）。
- ⚠️ **下载五件事**：① 已存在且够大 ⇒ 跳过（`skipped:true`）；② `.part` 断点续传（**206 续传 / 200 重下**），带 `Range` 头；③ 失败**保留 .part** 供下次续传；④ ModelScope 先取**签名 URL**、失败回退 `/resolve/` 302；⑤ `Number(content-length)` 用来校正 total。删除则**一律限制在模型存储目录内**（防穿越）且**不许删根目录**；越界是**逐条 failed 里的错误**（整体仍 200，不是 400）。
- ⭐ **TestClient 的 portal 只在请求期间给事件循环切片** ⇒ 「后台任务 + 前端轮询」这种模式**在 HTTP 层断言不稳**（实测一直 `done=False`，因为任务拿不到足够调度）。改法：**直接 `await` 任务体**（`asyncio.run(_drive())`）做确定性断言，HTTP 层只留同步断言（如 cancel 回执）。同类坑之前 SSR 那次也遇到过（别用 TestClient 读永不结束的流）。
- ⭐ **守卫自身也有假阳性（本轮修掉）**：`_TS_ROUTE` 原来只写 `\.(get|post|...)\('...'`，于是把 ``resp.headers.get('content-length')`` 当成了一条 `GET /content-length` 端点 ⇒ 计数虚高、未注册表里多一条**不存在的路径**（本轮就靠它误报的 +1 才发现）。**但反过来也不能只锚 `app.`** —— 仓里有 `router.get(...)`（**53 处**）与 `aiProviders.post(...)` ⇒ 一锚死就漏掉一半端点（192→140）。最终口径：**「裸标识符（前面不是 `.`）+ `.方法('字面量')`」**。修正后 Node 225→**224**、未注册 33→**32**。
- **`_JSON_DUMPS_ALLOW` 加 `local_model_scan.py`**：那里的 `json.dumps(cfg, indent=2)` 是**有意**镜像 TS 的 `JSON.stringify(cfg, null, 2)`（写 `configs/model-paths.json`），不是漂移。

## S6 第 10 步：补齐 10 条「延后理由已过期」的生成端点

**起因（一次认知修正）**：上一轮我说「S6 只剩 `/gpu/*`」是**错的**。实际那 32 条未注册里只有 **2 条**是 `/gpu/*`，其余 30 条是**有意延后**的端点（每个路由的 docstring 里都有一条「未迁移」清单）。进一步发现：**其中一批的延后理由已经过期** —— 它们依赖的服务（`image-generation` / `text-generation` / `prompt-utils` / `tts-generation`）在 S3/S4 就迁完了。

**本轮补齐 10 条**：`characters` **8 条**（auto-split-visuals / generate-voice-sample / generate-image / generate-prompt / generate-three-views / generate-equip-image / generate-expressions / batch-generate-images）+ `props`、`scenes` 各 **1 条** `generate-image`。新增 `tests/characters_generate_test.py`（**59 用例**）+ `tests/props_scenes_generate_test.py`（**23 用例**）。守卫：已注册 195→**205**、未注册 32→**22**。

- **characters 生成链的六处保真点**：① **错误码不统一** —— id 非法 404，角色找不到 **400**（**只有 auto-split-visuals 是 404**），`generate-three-views` **没有 try/catch**（异常直冒 500），批量接口**逐条静默跳过**；② **配置回退不同** —— 单张接口 `ep.imageConfigId ?? drama 级第一个`，**批量接口只用 ep 的**；③ **画风收口** —— 自定义 prompt 也必须追加画风后缀 + 注入服装/武器/首饰子句；④ **视觉锚定** —— 立绘不做自动锚定，三视图锚主立绘，装备/表情锚「三视图 combined → 主立绘」，显式 `reference_images` 最高优先（**截断 6 张**）、`anchor='none'` 不锚；⑤ **装备图负向词是嵌套三元** —— 单件用物品负向；三视图若用户负向**缺人物排除词**（或命中旧版提示词）就**强制换**装备负向，防装备图出人；⑥ `coreFeatures` **只吃 JSON 字符串**（TS 签名就是 `string`）⇒ 传数组会被静默忽略（**两边一致的怪癖**，不是漂移）。
- **props/scenes 的关键语义**：props 找不到物品是 **404 中文「物品不存在」**、`category='屏幕留白'` 走留白图构建器、`config_id` 是 **falsy 判定**、回执是 **camelCase 且回显 prompt**；scenes id 非法 404 / 场景不存在 400、**跨集参考图**（同剧同地点已出图的其他场景，**最多 2 张**，**地点为空必须跳过否则串图**）、置 `processing` 后**成功不重置 / 失败置 `failed`**。另：`build_scene_image_prompt` 是「**有 prompt 用 prompt，否则 location + time**」——两条分支都要有用例。
- ⭐ **跨文件的隐性耦合：`smoke_test.py` 里硬编码了「这 3 条未迁移 ⇒ 501」**，本轮实现后**全量才暴露**（单跑新自检全绿）。已改成用**仍未迁移**的端点（`episodes/*/continue-script`、`dramas/*/rhythm`）保住原意。**教训：凡断言「未迁移/501」的测试，迁移时必须同步** —— `route_parity_test.py` 用「未注册」动态口径兜住了这类，smoke 里则是硬编码清单，属已知脆弱点。
- ⚠️ 自检种子注意：`scenes` 的 `time` / `prompt` / `location` 都是 **NOT NULL** ⇒ 种子里「无地点」要用**空串**（正是端点要防的那种），不能传 None。
- **剩余未注册 22 条**（全部为「有依赖未迁」或 GPU）：`/gpu/*` 2、`agent_configs` 2、`dramas` 2（含 rhythm）、`episodes` 2（continue-script / consistency-qc）、`export` 5（整文件未迁，**裸 Response + Content-Disposition**）、`storyboards` 9、`style_profiles` 1、`storage` 1。

## S6 第 11 步：storyboards 5 条生成/LLM 端点（可做档继续清）

**新增**：`storyboards.py` 追加 5 条 —— `generate-tts` / `regenerate-image` / `action-suggestion` / `split` / `optimize-prompt`（+ **416 行**，含 `_get_character_voice_params` / `_compact_json` / `_shot_context` 三个助手）+ `tests/storyboards_generate_test.py`（**39 用例**）。守卫：已注册 205→**210**、未注册 22→**17**。

- ⚠️ **generate-tts 有两套回执**：多人对话（拆出 >1 条）⇒ `{lines:[...]}` 且**结果数组整体以 JSON 字符串**存进 `tts_audio_url`；单人 ⇒ **扁平对象**（`tts_audio_url`/`voice_id`/`match_status`/`speaker`/`text` + **条件 `warning`**，not_found 与 no_voice 文案不同）。单人模式若解析 `ignorable`（纯环境音/无对白）⇒ **400，且连 start 日志都不打**。⚠️ 两个校验助手的键风格**不同**：`validate_tts_speaker` 返回 `{match_status, voiceId, characterId}`（混合），`validate_dialogue_lines` 的条目是 `{speaker, text, match_status, voice_id, character_id}`。
- ⭐ **对话行解析是「贪婪」的，我踩了一次**：`([^:：\n]{1,10}?)[:：]([^:：\n]{8,})` 的台词捕获一路吃到**下一个冒号**，所以 `A：长台词。B：长台词。` **只会拆出 1 行**（B 被吞进 A 的台词里）⇒ 走单人分支。多行必须**换行分隔**才稳定。**TS 注释里那个「→ 3 行」的示例其实也是错的**（同一正则）。
- **三个 LLM 端点的回执与兜底文案**：`{suggestion}`／`{subShots:[...]}`／`{optimizedPrompt}`；异常时文案兜底各不相同 —— `Failed to generate action suggestion`／`Failed to split shot`／`Failed to optimize prompt`（都不落库）。
- **split 落库七件事**：原镜头**保留** → 新镜头**插在其后** → 后续分镜号**顺延 shift** → 角色关联**同步到新镜头** → `duration = clamp(round(原时长/子镜头数), 2, 4)` → `dialogue=None` → `status='pending'`。子镜头为空数组则**不动任何编号**。
- **场景上下文 `_shot_context`**（split 与 optimize-prompt 共用）：视觉风格链是 **storyboard → episode → `dramas.style`**；有 `scene_id` 时场景信息**整体换成场景表的**（location/time/atmosphere），否则用分镜自身的；角色名从 `storyboard_characters` 关联查出。
- **regenerate-image 三条回退链**：prompt `body → customImagePrompt → 标准构建器`；负向 `body → 分镜自身 → 按画风默认`；参考图 `body.reference_images → getStoryboardReferenceImages`（画风解析是 4 参形式：`resolve_effective_art_style(conn, dramaId, None, body.style)`）。
- **剩余 17 条**：`/gpu/*` 2、`dramas` 2（rhythm / era-background）、`episodes` 2（continue-script / consistency-qc）、**`storyboards` 4**（regenerate-frame / set-frame / qc / retry-qc，卡在 ffmpeg 抽帧与未迁的 `qc-scoring`/`qc-retry`）、`export` 5（整文件）、`style-profiles/distill` 1、`storage/change` 1。

## S6 第 12 步：续写端点 + **剩余 16 条的成因分类**（本轮核心产出）

**新增 1 条**：`POST /episodes/{id}/continue-script`（`text_generation.continue_script` 已迁 ✓）+ `tests/episodes_continue_script_test.py`（**8 用例**）。守卫：已注册 210→**211**、未注册 17→**16**。

- **continue-script 三处语义**：① 集查询**不带软删过滤**，但拿到后显式判 `deleted_at` ⇒ **软删集是 404**；② `mode` **只有等于 `'script'`** 才是剧本模式，其余（缺省/非法/大写）一律 `'raw'`；③ `text` **必须是字符串**（数字/对象/数组都当空 ⇒ 400 `text is required`），且字符串**原样透传不 trim**。

- ⭐ **【重要】剩余 16 条已全部查明成因，别再逐个重新调研**：
  1. **`storage/change`（1）—— 有意延后，别做**：它写项目级 `.data-root` 标记文件，而 **Node 启动也读这个文件** ⇒ 从 Python 切一次目录会把 Node 的数据根一起挪走（跨进程副作用），还要关库重开。`routers/storage.py` 与 `services/data_storage.py` 模块头都写明「**等 Node 完全下线（单后端）再迁**」。
  2. **`export/*`（5）—— 缺两个大服务**：`routers/export.py` **已在**（`project-ledger` 2 条已迁 ✓），缺的是 `services/jianying-draft.ts`（**339 行**）+ `services/qc-report.ts`（**515 行**，含 contact-sheet / qc-report 的 Markdown+HTML 构建）⇒ **≈854 行，建议单独一轮**。
  3. **`style-profiles/{id}/distill`（1）**：缺 `distillStyleProfile`（~70 行）= **媒体探测 `probeMeasurementFacts`** + **直连 LLM**（TS 用 AI SDK `createOpenAI` + `Agent`，**不走 adapter**，移植时要么改写走 `generate_text`，要么补工具循环）。
  4. **`dramas/{id}/era-background/extract`（1）**：缺 `extractDramaEraBackground`（~80 行：取剧本 → 8000 字截断（前 70% + 后 30%）→ LLM → JSON 归一）。**解析件 `services/era_background.py` 已在** ✓，只差提取函数。
  5. **`dramas/{id}/rhythm`（1）**：`rhythm-phase.ts` 未迁（既有决策：占位）。
  6. **QC 三兄弟（3）**：`episodes/{id}/consistency-qc`、`storyboards/{id}/qc`、`storyboards/{id}/retry-qc` —— 依赖 `qc-scoring.ts` / `qc-retry.ts` / `consistency-qc.ts`（**视觉模型 + 多图输入**），三者都未迁。
  7. **抽帧两条（2）**：`storyboards/{id}/regenerate-frame`、`set-frame` —— 需要 ffmpeg 抽帧/写帧链路（`frame_extractor.py` 只有「尾帧提取」，不够用）。
  8. **`/gpu/*`（2）**：GPU 租约域（`ai-configs/gpu/status`、`gpu/release-all`）。

## S6 第 13 步：把「未迁移 ⇒ 501」清单改成**动态派生**（根除反复踩的坑）

**背景**：这个坑连续三轮咬人（characters/scenes/props 三连 → storyboards 的 generate-tts/split → episodes 的 continue-script），每次都是**我为了替换上一条而写的新清单**在下一轮被打破，且只有跑全量才暴露。

**做法**：`smoke_test.py` 新增 `_unregistered_samples(limit=5)`，直接复用守卫口径（**Node 侧路径 − Python 已注册路径 = 仍未迁移**，再用守卫的 `_concrete()` 把 `/{id}` 换成可请求的具体路径）⇒ 两处硬编码块改成动态抽样循环。**从根上不会再过期**（迁移一条，抽样自动换下一条）。冒烟 483 → **487 用例**。

- ⭐ **新踩一个坑（重要）**：`_unregistered_samples` 懒导入 `route_parity_test`（守卫）时，**守卫在模块级会「硬改」`DATA_ROOT` / `PROXY_TO_NODE` 环境变量**（它自己的设计：必须在导入 app 前指到临时数据根）⇒ 一导入就把 **smoke 自己的数据根顶掉**，表现为**后面「traces list」这类读文件系统的断言失败**（离出错点很远，很难联想起是 import 的副作用）。
  **解法：导入前后「快照 + 还原」环境变量**（守卫模块只真正执行一次，`sys.modules` 缓存 ⇒ 后续调用无副作用）。
  **通用教训**：`import` 一个「自己做环境准备的测试/工具模块」时，要么在最早的 import 之前完成，要么**快照还原它碰过的 env**。
- **口径统一备忘**：现在「仍未迁移」这件事只有**一处**事实来源（`route_parity_test.py`），smoke 只是消费它 —— 今后新写「兜底 501」类断言，一律走 `_unregistered_samples()`，**不要再抄端点名**。

## S6 第 14 步：export 开工 —— `export-service` + EDL/ZIP 两条端点

**新增**：`services/export_service.py`（← `services/export-service.ts` **192 行**）+ `export.py` 追加 **2 条端点**（`GET /export/dramas/{id}/edl`、`GET /export/dramas/{id}` ZIP）+ `tests/export_service_test.py`（**22 用例**）。守卫：已注册 211→**213**、未注册 16→**14**。

- ⚠️ **导出语义**：只导「已落盘」媒体（`data:` / `http(s)://` 一律跳过）；ZIP 内路径要 sanitize（`\/:*?"<>|`+控制字符→`_`，空→`untitled`），扩展名推断**要吃掉 `?query`**、认不出用 `bin`；分组是 `videos/ep01_标题.ext` / `characters/` / `scenes/` / `storyboards/ep01/sb01_first|last|tail.ext`（**真实尾帧 `tail_frame_image` 单独导出**，不与设计尾帧混用）；EDL 的 `fps` 默认 **25**，分镜 `duration<=0` 时**去 ffprobe 探测真实时长，探测失败就跳过该镜**。
- ⭐ **又抓到一个真 bug（第 4 次同类）：`seconds_to_timecode` 用了 Python `round()`**。TS 是 ``Math.round(seconds*fps)`` = ``floor(x+0.5)``，而 Python ``round`` 是**银行家舍入**（``round(0.5)==0``）⇒ **0.5 帧的边界会差 1 帧**（`0.02s @25fps` 该给 ``:01``，Python 给 ``:00``）。已改用仓里的 **`js_round`**。
  ⇒ **规律（值得记牢）：凡 JS 的 `Math.round/floor/ceil`、`Number.toFixed`、`JSON.stringify`，在 Python 都别直接用「看起来一样」的默认函数**（已踩：`toFixed` 四舍五入 vs 银行家、`json.dumps` 默认分隔符、`Math.round` 银行家）。仓里对应件：`response.js_round / js_number / js_truthy / js_nullish`、`json.dumps(..., separators=(",",":"))`。
- **一处已知差异（已写进模块头）**：TS 用 `archiver` 流式边压边出；Python 用标准库 `zipfile`（**deflate level=6**，与 TS 的 `{zlib:{level:6}}` 对齐）写**临时文件** + `StreamingResponse` 分块吐 + `BackgroundTask` 清理 ⇒ 不新增依赖、也不把整包读进内存。头与文件名完全一致（含 `X-Export-Count`）。
- **export 剩余 3 条**：`jianying-draft`（需 `jianying-draft.ts` **339 行**）、`qc-report` + `contact-sheet`（需 `qc-report.ts` **515 行**，含 ~130 行 ffprobe 媒体探测解析）。

## S6 第 15 步：剪映草稿导出（分 4 阶段落地）

**新增**：`services/jianying_draft.py`（← `services/jianying-draft.ts` **339 行**，~370 行 Python）+ `GET /export/dramas/{id}/jianying-draft?episodeId=`（ZIP 复用 `build_export_zip` ✓）+ `tests/jianying_draft_test.py`（**25 用例**）。守卫：已注册 213→**214**、未注册 14→**13**。

**分阶段做法（本轮用户要求「避免上下文过长」）**：① 读 1-161 → 写助手层（`to_media_path`/`copy_to_media`/`us_of`/`sanitize`/`segment`/`text_segment`）；② 分两次读 163-373 → 写入口 + `_build_draft_for_episode`；③ 接端点 + 守卫白名单；④ 自检。**每阶段都落到盘上再进下一阶段**，中途不持有大块未落盘内容。

- **格式要点**：草稿目录 `<storageRoot>/jianying/<draftName>.draft/`，含 `draft_content.json` + `draft_info.json`（`draft_meta_info.json` 剪映自补）；**微秒计时**、UUID 引用式（`tracks.segments.material_id` 必须命中 `materials`）、`materials`/`tracks` 三段式；素材复制进 `media/` 后按**绝对路径**引用。
- ⚠️ 六处易错：① **素材编号从 0 开始**（TS 是 `videoIdx++` **后置自增** ⇒ 首个是 `video_000`）；② `us_of` 的 `?? 5` 是 **nullish**（只有 `None` 才回退 5 秒，`0` 保留再被 `max(1, ...)` 夹住）+ 用 **`js_round`**；③ `sanitize` 有 **40 字符上限**（防路径超限）；④ 草稿名 `dramaN_epNN_标题sanitize`；⑤ 画布尺寸取该分镜 `video_generations` 里**最后一条有宽高**的记录（缺省 1920x1080）；⑥ **字幕是按循环计数取 `texts`**（中间某镜无对白 ⇒ 后续字幕**错位**到更早的 text 上；原 TS 如此，**照抄不修**）。
- ⚠️ **全剧导出**（不给 `episodeId`）时，**任何一集没有合成视频 ⇒ 整体 400**（`Episode N has no composed videos`）—— 已用端点用例锁住。
- ⚠️ `indent=2` 写草稿 JSON 是**有意**的（剪映读的就是它）⇒ 已进 `_JSON_DUMPS_ALLOW` 白名单。
- ⚠️ **测试里踩到 `ResourceClosedError`（现象 + 规避）**：自检中「先在一个 `with engine.begin()` 里构建，再开**另一个** `with engine.begin()` 构建」会报 `This Connection is closed`；写最小探针复现时**同一序列却是正常的**。规避方式是**同一事务内连做两次**（覆盖面不变）。**结论只按现象记**：这类「跨上下文管理器复用连接」的长测试里不稳，遇到优先改成同事务连做，别急着怀疑服务代码。

## ⚠️ 记忆层修复（同日）：目录只剩本日志，INDEX 已对齐

**现象**：`.codebuddy/memory/` 下 7 篇历史日记（2026-09-12 / 09-11 / 09-03 / 09-02 / 09-01 / 08-31 / 08-29）**已不在盘**（此前守卫还报「8 篇在盘」；符合「旧日记蒸馏后删除」的维护规则，其结论应已在 `MEMORY.md` / `TOPICS.md` 内）。

**后果与处置**：`INDEX.md` 里那 7 篇的日志头行 + **它们各自的锚点行**成了悬空引用 ⇒ 守卫先报 8 处、删了头行后**涨到 90 处**（锚点行没有文件名，按名筛不掉）。

**修法**：删掉「含 `@行号` 且不含 `S6 第 4 步`」的行（= 除今日锚点行以外的全部锚点行，共 7 行），并把今日末节锚点补成 `@151`。守卫现为 **1 篇登记 / 1 篇在盘 / 13 处锚点 / 致命 0** ✓。

**教训**：**INDEX 的日志头行与锚点行是两条独立记录**，清理时必须**成对**处理；只删头行会让「锚点悬空」从 8 处涨到 90 处。

## S6 第 16 步：QC 报告交付物 → **`export.ts` 整域关闭 7/7**

**新增**：`services/qc_report.py`（← `services/qc-report.ts` **515 行**，分 3 阶段落地）+ `export.py` 追加 **2 端点**（`/qc-report?format=json|md|html`、`/contact-sheet`）+ `tests/qc_report_test.py`（**42 用例**）。守卫：已注册 214→**216**、未注册 13→**11**（**export 整域清零**）。

**分阶段**：① 探测层（`ffprobe_info` + 解析助手 + `latest_video_gen/latest_qc` + `build_media_info`）｜② 结构化报告 `build_qc_report`｜③ 渲染层（Markdown / 联系表 HTML / 报告 HTML / 文件清单 / 媒体信息）｜④ 端点｜⑤ 自检。

- **三条回退链**（都锁了用例）：**字段级** `vg.x ?? ffprobe.x ?? None`（不是整体替换）｜**fileSize 二级**（ffprobe 没有就 `os.stat` 量文件）｜**durationSec 三级**（`sb.duration || media.durationSec || 0`，仍 ≤0 且有视频才 ffprobe，探测失败**保持 0**）。
- **平均分口径**：只统计**有 QC 的镜头**，`Math.round(x*10)/10` ⇒ 用 `js_round`；**达标边界是 `>=60`**（`?? 0` 是 nullish，分数 0 要保留 0）。
- **交付物文本细节**：`toFixed(1)`、整数不带 `.0`（JS 的 90 不是 90.0 ⇒ `_js_num`）、缺省 `—`/`未跑`、`toISOString` 形态（`_iso_now` 补毫秒 3 位 + `Z`）、`escape_html` **不转单引号**、状态 emoji 三分支（有视频 ✅ / 标记完成但无视频 ⚠️ / 其它 ⛔）。
- ⭐ **又是 `toFixed`/`round` 那一类**：新增 `_js_to_fixed`（**half-away-from-zero**，`1024.5 → 1025`；Python `f"{x:.0f}"` 会给 1024）。且 `avgContinuity` 用例我**两次**把期望写成 55.5 —— 实际 `55.55*10 = 555.5` ⇒ `Math.round` **进位到 556** ⇒ **55.6**（浮点 + half-up 双重叠加，别再靠心算）。
- ⭐ **自检打印会因 GBK 崩**：Windows 控制台 stdout 是 GBK，检查名里带 `⇒`/emoji 会 `UnicodeEncodeError: 'gbk' codec can't encode`，**在打印阶段才炸**（前面断言全过）。跑自检请带 `PYTHONIOENCODING=utf-8`（全量 `run_all` 也要）。
- 另：`video_quality_checks.updated_at` 是 **NOT NULL**（种子里必须给）。

## S6 第 17 步：时代背景提炼 + 风格提炼（两个「中等件」）

**新增**：`era_background.py` 追加 `strip_json_fence` + `extract_drama_era_background`（← `era-background.ts` 87-135）｜`style_profiles.py` 追加 `_probe_measurement_facts` + `distill_style_profile`（← `style-profiles.ts` 200-295）｜**2 端点**（`POST /dramas/{id}/era-background/extract`、`POST /style-profiles/{id}/distill`）｜`tests/era_style_distill_test.py`（**31 用例**）。守卫：已注册 216→**218**、未注册 11→**9**。

- **时代背景提炼**：剧本聚合**原 TS 就没有 ORDER BY**（照抄，顺序即 SQLite 返回序，软删集不参与）｜**8000 字截断 = 首 5600 + 「……（中间省略）……」 + 尾 2400**（保开头设定与结尾走向）｜```json 围栏剥离再截最外层大括号｜落库 `{era,summary,imageHint}` 紧凑 JSON。
- ⚠️ **异常分层**：JSON 解析失败 / 字段缺失一律**包一层** ``时代背景提炼失败: ``；而 ``Drama not found`` 与「该剧暂无剧本内容…」写在 try **之外**、**不包**。
- ⭐ **又验证了一次「判据比文案宽松」那个坑**：只给 `{"era":"x"}` 也会被**接受** —— `era`↔`summary` 互相回退成同值，但 **`imageHint` 的回退链里没有 era**（是 `image_style_en → imageHint → summary`）⇒ 它留空。我**两次**把期望写成「三字段同值」，最后照实测改对（清单里那条注释现在就是给未来的自己看的）。
- **风格提炼**：**不落库**（返回待确认，用户点 `/apply` 才写）｜四对象三数组**缺省归一**（`{}`/`[]`）｜prompt 组装三个缺省文案（素材说明按「（未提供，仅凭 Profile 名称/描述）」、偏好按「（无）」）｜测量事实 **indent=2** 注入（白名单：`style_profiles.py`）。
- ⚠️ **所有异常都吞成 `{ok:false,error}`**（连 provider 报错也是）⇒ 路由一律 **400**；Profile 不存在同样是 **400 而不是 404**（原 TS 如此）。
- ⚠️ **契约冒烟里那条 `distill NOT migrated -> 501` 必须改** —— 而且**别在冒烟里打通真提炼**：冒烟环境居然有可用文本模型，实测真发了一次 LLM 调用（返回了完整风格分析）。改成「用不存在的 id ⇒ 400 Profile not found」，既证明「不再是 501」又不触发模型。
- ⚠️ 小坑：`episodes.title` 是 **NOT NULL** ⇒ 想测「无标题」分支要用 `""`（不是 `None`）。
- ⚠️ 落点：`get_absolute_path` 在 **`app/services/file_storage.py`**（`from .file_storage import`），不在 `app/` 顶层。

## S7 第 1 步：基准资产搬迁 + 评测 CLI（「删 backend/」的前置）

**新增**：`<项目根>/benchmarks/`（4 个 case JSON，从 `backend/benchmarks/` 搬，**SHA256 逐字节一致**）｜`app/services/evaluation/cli.py`（← `evaluation/cli.ts` **64 行**）｜`tests/eval_cli_test.py`（**22 用例**）。`catalog.benchmarks_dir()` 默认值切到 `PROJECT_ROOT/benchmarks`（env 覆盖不变）；`optimizer` 的 `historyDir` 默认改为 `benchmarks_dir()/history`。

- ⭐ **关键盘点：`app/` 对 `backend/` 的路径依赖 = 0** ⇒ 代码层已无阻碍。**唯一还读 TS 源码的是 `tests/route_parity_test.py` 的九道漂移守卫**（`backend/src/**`），删 `backend/` 当天必须退役或改成「冻结期望文件」，否则整套守卫报错。`eval_cli_test` 的字节一致断言已改成「`backend/` 不在就 skip」。
- **`historyDir` 是 cwd 相关的**：TS 写 `Path.cwd()/benchmarks/history`，而 Node 的 cwd 恒为 `backend/` ⇒ 历史就落在 case 文件旁。S7 把 case 搬到项目根后，若照抄 cwd 写法会另开一份 `backend-py/benchmarks/history`、与 case 目录脱节 ⇒ 改成 `benchmarks_dir()/history`（**行为等价、不再依赖 cwd**）。
- **CLI 的两处有意加固**（都为了「S7 之后还能用」）：① 路径解析 **cwd 优先、回退项目根**（TS 是纯 `resolve(process.cwd(),p)`；否则从 `backend-py/` 跑得写 `../benchmarks/x.json`）；② `--iterations abc`（TS `Number('abc')`=NaN ⇒ 迭代**静默 0 次**）改成**回退默认 3 + stderr 告警**，与 README 里 `format_vendor_http_error` 同类先例。
- ⚠️ **case 的 id/kind 以真实数据为准，别按文件名猜**：`ext-case-001/extractor`、`script-rewriter-001/script_rewriter`、`sb-case-001/storyboard`、`voice-assigner-001/voice_assigner`（我按文件名猜的四条全错，被自检当场打脸）。
- 数字：**45 项 / 2092 用例**；守卫 218 已注册 / 9 未注册 / 0 漂移。

## S7 第 2 步：GPU 显存管理器（473 行整域）→ `/gpu/*` 两条端点关闭

**新增**：`app/services/gpu_manager.py`（← `services/gpu-manager.ts` **473 行**）+ `ai_configs.py` 追加 **2 端点**（`GET /gpu/status`、`POST /gpu/release-all`）+ `tests/gpu_manager_test.py`（**28 用例**）。守卫：已注册 218→**220**、未注册 9→**7**。

- **互斥锁 + FIFO 队列**（``asyncio.Future``）；**驱逐从小到大**、跳过即将加载的新模型、每卸一个 `sleep(cooldownMs)`；**TTL 10 分钟**清理已释放模型的估算（避免「只增不减」）。
- ⚠️ **卸载策略的返回值是有语义的**：`ollama-keep-alive` / `passive` / `sd-unload-checkpoint` **一律 True**（哪怕请求失败/不支持），`comfyui-free` 看 2xx，`none` False —— **驱逐据此累加「已腾出显存」**，改返回值就改了驱逐行为。
- ⚠️ **释放分流**：`ollama-keep-alive` / `comfyui-free` **立即卸载**（fire-and-forget + 从账本删除），其余策略只打 `releasedAt` 等 TTL。
- ⚠️ **Ollama 卸载的模型名取 `modelKey.split(':')[1]`** ⇒ `ollama:qwen3:14b` 得到 **`qwen3`**（丢标签）；这是原 TS 行为，照抄并在注释里标明。
- ⚠️ **`GET /gpu/status` 返回裸 JSON**（不是 `{code,data,message}` 信封），失败时还是 **HTTP 200 + `{code:500}`**（`c.json` 没带状态码）—— 与「本仓其它端点都是信封」的习惯相反，自检里专门锁了这条。
- ⭐ **`is_local_config` 的真实落点**：白名单与判定**已在 `services/ai_configs.py`**（带着「与 JS URL 解析等价性」的分析）⇒ `gpu_manager` 只**再导出**，不复制第二份实现。
- ⭐ **自检踩坑：互斥锁下「连拿两个租约」会死等** —— 我写 `acquire(); acquire();` 想造「账本里两个模型」的现场，结果第二次**排队等到天荒地老**（测试跑 120 秒不返回，工具把它当长任务后台化了）。正确姿势：拿第一个后**人为 `_holder = None`** 再拿第二个（这正是 `release_all` 要处理的「锁丢在别人手里」现场）；所有 `acquire` 都该加 `wait_for` 兜底。
- ⚠️ **又两条过期断言**：`smoke_test.py` 里「aic not migrated: GET /gpu/status | POST release-all → 501」必须改成「已迁移」（与上一轮 distill 同类）。

### ⚠️ 已知偶发（留给下一轮加固）：`smoke_test.py` 的假 provider 服务器

聚合跑（46 个套件连跑）时，`tests/smoke_test.py` 里那条「`test: 404 存在可达服务 -> reachable=false + 文案`」偶发失败：拿到的是 `message='请求失败'`（连接层失败），即**假 provider 服务器（`HTTPServer(('127.0.0.1', 0))` + daemon 线程）还没就绪**。证据：单跑 2/2 通过、上一轮聚合跑同一条通过 ⇒ 与业务代码无关，是测试基建的**启动竞争**。加固方向：起线程后**轮询到能连上再发请求**（或给请求加一次重试）。

## S7 第 3 步：GPU 租约接线（text / tts 两处，安全属性首次真正生效）

**改动**：`text_generation.generate_text` 与 `tts_generation.generate_tts` 各加
``lease = await gpu_manager.acquire(...) if is_local`` + 现有 ``try/except`` 上补一个 ``finally`` 释放；新增 `tests/gpu_lease_wiring_test.py`（**11 用例**）。

- ⭐ **契约是「每个模型尝试各自申请/释放」**（TS 的 `finally { if (lease) lease.release() }` 在循环内）——**不能改成「整条 fallback 链持有一次」**：那样锁会攥到最后一个模型才放，队列里其它本地请求（乃至图像/视频任务）全程白等。自检用事件序列锁死了 `acquire→release→acquire→release`。
- ⭐ **异常路径必须释放**（否则锁泄漏 ⇒ 后续**所有**本地请求永久排队，比不申请租约更糟）。全失败场景也锁了「两次尝试都释放」。
- 落地手法：两处都已有 `try/except` ⇒ 只加 `finally:`，**零缩进改动**，diff 极小、回归面最小。
- 回归：`text_generation_test.py` / `tts_generation_test.py` / `adapters_test.py` 三套件全绿（接线前后一致）。
- **待接：image / video** —— 它们是**长租约**：TS 在提交时 `acquire` 并塞进模块级 Map（`imageGpuLeases`/`videoGpuLeases`），在**任务完成/失败/重试**时由 `releaseImageGpuLease(id)` 释放（跨轮询持有），且 acquire 失败只 warn 不中断任务。

## 待办（优先级）

1. **`evaluation` 的 optimizer + 调度器** → 补齐 `/optimize`、`/scheduler`、`/run`（前置：`creator.ts` 151 行）。
2. `localModels.ts`（611 行 / 11 端点）｜`auto-pipeline.ts`（89 行，**唯一 SSE**）。
3. **S7 前必办**：把 `backend/benchmarks/*.json`（4 个）挪到 `PROJECT_ROOT/benchmarks`（`evaluation/catalog.py` 现在指向 `backend/benchmarks`，删 `backend/` 会让评测空转）；`/gpu/*` GPU 租约未迁。
4. **记忆层**：`MEMORY.md` 已 **7798/8000**（余量仅 202）⇒ **下轮写内容前先把低频专题下移 `TOPICS.md`**（否则 `check-memory.mjs` 报预算超限）。另：新日志**必须登记进 `INDEX.md` 并带 `@行号` 锚点**（含**末节**锚点），否则守卫致命。
5. 长期：像素处理（需 Pillow）、镜头 QC 打分、`subagent.ts`、`rhythm-phase.ts`。

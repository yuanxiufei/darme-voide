# 2026-08-28

- 提交 `68d452d` feat(voices): 音色体系增强（声纹克隆/CosyVoice 零样本克隆/专属音色批量生成），15 files +431/-29。工作区已清理干净。
- 将 `data/traces/`（Mastra 框架运行时 trace 产物）加入 `.gitignore`，避免误提交。
- 定位 MiniMax H3 配置：`ai_service_configs` 表 id=6「本地视频(本地H3)服务」，provider=minimax，base_url=`http://localhost:8765`，api_key=`local`（本地部署占位，非真实云端 key），model=`hailuo-02`。云端 MiniMax 真实 key（sk-api-...）仅配在 TTS(id=1) 和在线文本(id=8) 上，未配给 H3 视频。

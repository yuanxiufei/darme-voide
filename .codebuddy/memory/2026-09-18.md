# 2026-09-18 工作日志

（承接 `2026-09-17.md` —— S7 自研引擎 + 参考项目移植 ✓）

## S7 第 83 步：**参考项目移植（第二批）** —— 资产清单 + 验收门（31/31 ✓ 零依赖）

同源 ✓（`reference/short-drama-agent/references/asset-to-video-pipeline.md` ✓），
守的是参考项目里那条**产品硬规则**：

> 「**不要从被拒或缺失的锚定资产提交付费视频任务**」✓

### 本项目原来的状态（为什么算真缺口 ✓）

`services/asset_versions.py` 有「记录版本 / 激活版本」✓ —— 那是 **存** ✓；
**没有任何东西阻止**"角色参考图还是 `to-generate` 就去跑付费视频" ✗（门是缺的 ✗）。

### 交付

| 文件 | 内容 | 实测 |
|---|---|---|
`services/asset_manifest.py` | ① **清单校验**（六种类型 ✓ / 状态取值 ✓ / **asset_id 唯一** ✓ / 依赖不悬空 ✓）；② ⭐ **依赖拓扑 + 成环检测**（Kahn ✓ 稳定排序 ✓ 环**逐个点名** ✓）；③ ⭐ **验收门**（逐镜算「被谁卡住」✓ + 唯一布尔 `canGenerateVideo` ✓ + `gateReasons` ✓） | — |
`routers/asset_gate.py` | `POST /api/v1/assets/manifest/validate` ✓ + `POST /api/v1/assets/gate/check` ✓（**执行器调用前**的最后一道 ✓） | 196 路由 ✓ |
`tests/asset_gate_test.py` | 31 项 ✓ | **31/31** ✓ |

### 三条刻意写死的语义（防"门形同虚设" ✓）

1. ⭐ **`generated` 不算验收通过** ✗ —— 参考项目把「图生成了」与「人验收了」当**两件事** ✓；
   若这条松了，门就等于没有 ✓（自检 ⑰ 专门钉它 ✓）。
2. **`approved` 只能由人给** ✓ —— 本模块**不会**自己把 `to-generate` 提成通过 ✗（机器只算门锁在哪 ✓）。
3. ⚠️ 六种类型里 **`ui_plate`（留给后期叠字的空屏 ✓）** 很关键 ✓ ——
   让模型去生成"手机屏幕上一整段中文短信"必然糊 ✗（这条已在代码注释里写明 ✓）。

### ⚠️ 自检抓到一个**真 bug** ✗✓（不是计数问题，是功能问题 ✓）

㉔ 当场红 ✓：`BLOCKING` / `STATUSES` 里写的是 **`needs_regeneration`（下划线 ✗）**，
而 `_norm_status()` 把输入归一化成 **`needs-regeneration`（连字符 ✓）** ⇒ **两者永不相等** ✗✗。后果两条：

1. `blocking` 计数少一个 ✗（小 ✓）；
2. ⭐ **「待重做」的资产被排除出 `order`** ✗✗ —— 而那个顺序**恰恰就是「接下来该重做什么」** ✓
   （大 ✓：等于"返工清单里漏掉了要返工的那一项" ✗）。

修：两个集合统一用**归一化形态** ✓；并补一条**回归断言**（㉕′ ✓）直接钉"待重做资产必须在生成顺序里" ✓
—— 钉**后果** ✓ 而不是只钉计数 ✓。

> **规则（通用）**：**归一化的产物与判断用的常量必须同源** ✗ —— 一处 `-` 一处 `_` 就是隐形失配 ✓；
> 断言要写在**后果**上 ✓（"它没进生成顺序" ✓）而不只是"计数不对" ✓。

另：引号自查 `[\p{Han}]"[\p{Han}]` 这次报了 5 处 ✓（3 处在注释/docstring ✓ 2 处在真字符串 ✓），
但我**只修了 2 处、漏了 1 处** ✗ ⇒ 又编译失败一次 ✓。
> **规则修正**：写完立刻 `compile()` **定位到行** ✓ —— 比手工比对 grep 命中**快且不会漏** ✓。

**实测**：`asset_gate_test` **31/31** ✓；层级 6/6 ✓；路径+常量 **245 已注册 / 未注册 0 / 漂移 0** ✓。

### 下一步（同批剩余 ✓）

1. **连续性表**（场景空间 / 角色 / 道具状态 / 线索揭示 / 动作因果 / 转场动机 ✓ + 检查器 ✓）——
   同源最后一块 ✓；
2. 其它项目：**Mini-Agent 的上下文压缩/摘要** ✓、**ollama 的流式工具调用解析**（增量 JSON ✓）、
   **ollama-python 的结构化输出 + 函数→工具 schema** ✓（先核对本项目 agent 工具集是否已有 ✗）。

## S7 第 84 步：**参考项目移植（第三批）** —— 连续性表（37/37 ✓ 零依赖）

同源最后一块 ✓（`production-plan-contract.md` **§6-§11** ✓）。本项目原来**只有事后**的
`consistency_qc`（对**已生成**的相邻画面算 dHash 相似度 ✗ ——「**拍完**才发现不对」✓）；
本步补的是**事前**的：**还没花钱生成，就说清「这一镜接不上上一镜」** ✓。

### 交付

| 文件 | 内容 | 实测 |
|---|---|---|
`services/continuity.py` | 六张表体检 ✓：§6 场景空间（**越轴** ✗ ✓）/ §7 角色连续性卡（必填 ✓）/ §8 **道具状态时间线** ✓✓ / §9 **线索不许提前暴露** ✓✓ / §10 **动作必须"做成点什么"** ✓✓ / §11 **转场必须给动机** ✓✓ | — |
`routers/continuity.py` | `POST /api/v1/continuity/check` ✓ + `GET /api/v1/continuity/rules` ✓（**把判据本身暴露给前端** ✓ —— 「凭什么说它不连续」的唯一诚实答法 ✓） | 198 路由 ✓ |
`tests/continuity_test.py` | 37 项 ✓ | **37/37** ✓ |

### 三条判据**逐字照抄契约**（不自己发明规则 ✓）

1. §8：*刀掉到地上，就不能再回到角色手里 —— 除非有镜头展示谁把它捡起来* ✓
   ⇒ 判据是「状态**变了**就必须有动作交代」✓（一直在地上**不**报 ✓ —— 那才是好镜头 ✓）；
2. §10：*不推进剧情/揭示信息/改变危险/建立空间/改变情绪的动作 ⇒ 标为可删或可并* ✓
   ⇒ 五类作用一个都给不出 ⇒ 进 `deletable` ✓（**只预警不阻断** ✓ —— 删戏是创作决定 ✓）；
3. §11：*永远不要只写「切到下一镜」，要解释剪辑理由* ✓ ⇒ 空动机/自创类别一律阻断 ✓。

### ⚠️ 我自己**先看出**一个坑，并在自检里钉住（这次不是被抓 ✓）

跨镜方向记忆是**模块级**的（要跨 shot 比 ✓）⇒ 初版**忘了每次清零** ✗ ⇒
**上一位调用者的方向会污染下一位** ✓（服务端长期运行 ⇒ 不同集/不同剧互相"越轴" ✗✓）。
已在 `check_continuity` 开头清零 ✓，并写成自检 ㉜（**两次调用互不影响** ✓）。

> **规则**：**凡"为了跨项比较"而引入的模块级状态，入口必须清零** ✗ ——
> 单次调用测不出来 ✓，只有连着调两次才现形 ✓。

### ⚠️ 自检 8 条红里 **7 条是我测试期望写错** ✓（1 条是实现该改 ✓）

- 夹具给多镜计划**默认补了合法转场动机** ✓ —— 改之前 7 条用例全被 §11 拦住 ✗，
  而那 7 条**根本不是要测转场** ✓（夹具便利 vs 判据真实，两边都要顾 ✓）；
- `actor` 既不在场也不在角色表时，**该报更强的那条**（不在场 ✓）而不是弱的 ✓
  ⇒ `elif` 链的**优先级**是对的 ✓，我用例写错了 ✓；
- 实现那条 ✓：线索声明的镜头**不在本次计划里**时，先后**无法比较** ✓
  ⇒ 本来直接跳过 ✗（静默漏掉 ✓）⇒ 改成降级**预警**（"判不了就说判不了" ✓）。

**实测**：`continuity_test` **37/37** ✓ + `asset_gate_test` 31/31 ✓ + `prompt_contract_test` 33/33 ✓；
层级 6/6 ✓；路径+常量 **247 已注册 / 未注册 0 / 漂移 0** ✓；套件 **87** ✓。

### 下一步

1. 参考项目**剩下的三件**（前两件要先核对本项目是否已有 ✗）：**Mini-Agent 的上下文压缩/摘要** ✓、
   **ollama 的流式工具调用解析**（增量 JSON ✓）、**ollama-python 的结构化输出 + 函数→工具 schema** ✓；
2. 或者把已做好的四块**接进前端/流程**（提示词契约 ✓、参数校验 ✓、资产门 ✓、连续性表 ✓ ——
   四个端点都在 ✓ 且都是毫秒级的 ✓）。

## S7 第 85 步：**参考项目移植（第四批）** —— Agent 上下文预算（25/25 ✓ 零依赖）+ 判掉两件不该搬的

### 先**判掉**两件（比搬更重要的判断 ✓）

| 参考项 | 本项目现状 | 结论 |
|---|---|---|
`ollama-python` 的**函数→工具 schema** | **已有**：`agent/tool.py` 的 `json_string`/`json_number`/`array_of`/`object_schema` ✓ + `as_openai_tools`/`as_gemini_tools` ✓ | **不搬** ✗（已有且更显式 ✓） |
`ollama` 的**流式工具调用解析**（增量 JSON ✓） | 本项目 agent 走**非流式**取 `tool_calls` ✓ ⇒ 搬来**没人调用** ✗ | **不搬** ✗（**"没人调用的库不算功能"** ✓ —— 等真接流式工具调用时再搬 ✓） |
**上下文预算/压缩** | ✗✗ **真缺**：主循环每步重发**整份 messages** ✓，而 skill 上限 60000 字符 ✓，**没有任何东西在管上下文** ✗ | ⭐ **搬这件** ✓ |

### 交付：`app/agent/context_budget.py` + **接进真循环**

| 项 | 内容 |
|---|---|
估算 | CJK 与 ASCII **分开估** ✓（纯按字符数会严重低估中文 ✗）；**不引 tokenizer 依赖** ✗ —— 为了估个数字装库不值 ✓，系数取保守侧 ✓（宁可早压 ✓ 不可爆 ✗） |
① **机械层**（`apply_plan` ✓） | 老的 **tool 输出**换成"保留开头 + 明说被压缩"✓ ⇒ **零 LLM 调用** ✓ |
② **摘要层**（`summarize_prompt` ✓） | 把一段交给 LLM 写成要点 ✓（要求**保结构** ✓、**不许编** ✗、标为中间过程摘要 ✓）—— **要不要花这次调用由调用方决定** ✓（本模块不擅自调模型 ✗） |
接线 | 主循环（`runtime.py` `default_generate` ✓）**每步开头**做机械层 ✓，压过就把 `contextBudget` 记进 `steps` ✓ |

### ⭐⭐ 本步最要紧的一条：**tool 往返必须成对**

OpenAI 兼容协议要求带 `tool_calls` 的 assistant 消息后**必须**跟对应的 `role=tool` 应答 ✓
（`tool_call_id` 一一对应 ✓）—— 压缩器只要"删消息"就会留下**没人回答的 tool_call** ✗
⇒ **下一次请求直接 400** ✗✗。所以本模块**只替换 `content`、从不删消息** ✓
（自检 ⑨✓⑩ 钉两条：**条数不变** ✓ + **成对仍成立** ✓；⑫⑬ 还钉"检查器本身能识破残缺形态" ✓
—— 检查器坏了的话，上面全是假绿 ✓）。

### ⚠️⚠️ 我踩到并记录的一个**新坑**：`compile()` 过了 ≠ 导入可用 ✗

我给 `runtime.py` 插了调用 ✓，但补 import 的那次编辑**锚点写错、没生效** ✗ ——
而 `compile()` **照样通过** ✓（未定义名是**运行时**才炸 ✗）⇒ 真机上第一步就 `NameError` ✗✗。
是我自己顺手 `import app.agent.runtime` 才当场抓到 ✓。

> **规则（补进"写完先 compile"那条）**：**改动了模块级引用 ⇒ 还要 `import` 一次那个模块** ✓。
> `compile()` 只能证明**语法**对 ✓，证明不了**名字**在 ✓（两者是两道不同的门 ✓）。

另：`route_parity` 抓到 **1 条镜像常量漂移** ✗（`context_budget.py` 的 `json.dumps(indent=1)` ✓）
—— 与 `dryrun.py` **同类判例** ✓（发给/给人看的正文 ✓、没有 Node 对应物 ✗）⇒ 加白名单并写明理由 ✓。

**实测**：新套件 **25/25** ✓；**10 个 agent 套件全绿** ✓（25+23+20+25+16+50+25+32+15+22 = 253 项 ✓，
含 `agent_runtime_test` **50/50** ✓ —— 接线没弄坏别的 ✓）；层级 6/6 ✓；
路径+常量 **247 已注册 / 未注册 0 / 漂移 0** ✓；套件 **88** ✓。

> ⚠️ `MEMORY.md` 已 7977/8000 字符（**只剩 23 字符** ✗）⇒ 本轮教训只能落在日志 ✓；
> 长期规则入库需要先**做一次提炼/精简**（按本仓惯例 ✓）。

### 下一步（你挑）

1. **把四块已交付的接进前端/流程**（提示词契约 → 资产生成 → 素材验收 → 连续性检查 ✓
   正好是一条**事前省钱链** ✓；再加上按需的**上下文预算** ✓）；
2. 参考项目还剩**两件**（都已判过"现阶段不搬" ✗，理由是**本项目还没有那个调用方** ✓）——
   真接流式工具调用时再回来搬 ✓；
3. 遗留待拍板：**装 torch/diffusers**（真出片 ✗）、上一轮「调用 ComfyUI」那套代码**删不删** ✗。

## S7 第 86 步：**开跑前体检** —— 把四块串成一次调用（25/25 ✓ 零依赖）

做的是"**接进流程**"那一半 ✓：前端要显示「本集生产准备度」时，**不必自己调四个端点再拼结论** ✗
（那样每个页面都会写一遍合并逻辑 ✗）。新增 `services/production_preflight.py` ✓ +
`POST /api/v1/production/preflight` ✓ + `GET .../preflight/schema` ✓（200 路由 ✓）。

**它不产生新判据** ✗ —— 只**调用**那四个模块 ✓（自检 ④⑤⑥⑦ 专门钉：
``sections`` 里每段必须与**直接调用**对应模块**逐字段一致** ✓✓ ⇒ 防止"又抄了一遍判据"✗
（那样以后改一处漏一处 ✗））。

### 两个设计判断（都写进了代码注释 ✓）

1. **质感层的问题只预警不阻断** ✓✓（约束不够硬 ⇒ **画得差点** ✓ 不是"接不上"✗）；
   同理 §10 的「可删或可并」也只预警 ✓（删戏是**创作决定** ✓ 由人拍板 ✓）。
   而占位符/连续性/验收门的问题**一律阻断** ✓。
2. ⭐ ``nextActions`` **按成本排序** ✓✓：先补映射表（零成本 ✓）→ 再改分镜/连续性表（零成本 ✓）
   → 最后才处理资产（**要人工 + 要花钱** ✓）✓。自检 ⑮ 用 `index()` 关系把它钉死 ✓。

### ⚠️⚠️ 抓到一个**跨全仓的约定**，而且我前面写的路由全都犯了 ✗

自检 ㉔ 红 ✓：我以为"非对象请求体 ⇒ 400" ✓ 实际拿到 200 ✗。一查 `core/request_utils.read_json` ✓：

> 它**有意**把「非法 JSON / 非对象」**归一成 ``{}``** ✓（注释写明：对齐 TS 的
> ``c.req.json()`` 会抛错并被 catch 成 400 ⇒ Python 侧走空 dict、**由业务校验给出 400** ✓，
> 且**不让"客户端发了坏 JSON"升级成 500** ✓）。

⇒ 结论 ✓：**路由里写 ``if not isinstance(body, dict): return bad_request(...)`` 是死代码** ✗✗
（永远进不去 ✓）。而我在 `engine.py` / `prompt_tools.py` / `asset_gate.py` / `continuity.py`
**每个**路由都写了这一行 ✗。

**判断**：那几处**无害** ✓（因为紧接着的业务校验——缺 `text` / 缺 `plan.shots` / 缺 `shots`——
照样会 400 ✓，行为一致 ✓），但**必须知道它是死的** ✓，否则以后会把"该报 400 的输入"
误以为已被拦住 ✗。本端点已按正解改写 ✓：**判据写在业务层**（"一段都没给 ⇒ 400" ✓）。

> **规则**：**先读会"归一/兜底"的工具函数，再决定校验写在哪一层** ✓ ——
> `read_json` 把异常吞成 `{}` ✓ ⇒ 校验只能靠**业务字段** ✓。（已记：这是**全仓约定** ✓。）
> **待办**：可考虑加一条守卫「路由里的 `isinstance(body, dict)` 为死代码」✓（本轮没做 ✗）。

**实测**：新套件 **25/25** ✓；层级 6/6 ✓；路径+常量 **249 已注册 / 未注册 0 / 漂移 0** ✓；套件 **89** ✓。

## S7 第 87 步：**立守卫并清掉 9 处死代码**（`read_json` 那条约定 ✓ 守卫 10/10 ✓）

接第 86 步的发现 ✓：全仓路由里对 ``read_json`` 返回值的 ``isinstance(body, dict)`` 是**死代码** ✓✗
⇒ 不能只是"知道" ✓，得**立守卫**防止再写 ✓，并把已有的清掉 ✓。

### 交付

* `tests/read_json_guard_test.py` ✓（**10/10** ✓）—— 三段：**合同锚点**（`read_json` 必须仍然
  保证 dict ✓，哪天被改会提醒"death-code 这个前提不成立了"✓）+ **正/负对照** ✓ +
  **全仓扫描** ✓（且扫描面非空 ✓ —— 扫 0 个文件也会"全绿"✗）。
* **清掉 9 处** ✓：我自己写的 8 处（engine 3 ✓ / prompt_tools 2 ✓ / asset_gate 2 ✓ /
  continuity 1 ✓）+ **`comfyui.py` 的历史遗留 1 处** ✓。
  这是**行为保持**的改动 ✓（那些判断**永远进不去** ✓）≠ 猜测 ✓ ——
  由九套复跑背书 ✓：**冒烟 476/476** ✓、engine_pipeline 98/98 ✓、comfyui_capability 25/25 ✓ …
  路径+常量 **249 已注册 / 漂移 0** ✓。

### ⚠️⚠️ 守卫**自己返工了一次**（这一步的教训最值钱 ✓）

第一版用**正则按行**匹配"固定两行"✓ —— 结果两头都错 ✗：

1. **误报** ✗：我在 `preflight.py` 的**文档串**里写了 ``isinstance(body, dict)`` 来说明
   "这里不做这个检查" ✓ ⇒ 被当成违规 ✓（**说明性文字被当成代码** ✗）；
2. **漏报** ✗：`prompt_tools.py` 还有一处**形状略有不同**的 ✓ ⇒ 正则匹配不到 ✓。

⇒ 改成 **AST** ✓（认语义：*对 ``read_json`` 绑定的名字做 ``isinstance(..., dict)``* ✓）：
注释/字符串天然不进 AST ✓、变量名与形状变体都跟得住 ✓；删除也改成**先 `ast.parse` 验证再写回** ✓
（语法不通就不写回 ✓）。自检里补了两条对照 ✓：**正对照**（合成死代码必须检出 ✓）+
**负对照**（`request.json()` 的返回值不算死代码 ✓ —— 它真可能不是 dict ✓）。

> **规则**：**判"代码里有没有某种用法"要用 AST，不要用正则** ✗ ——
> 正则既会把**说明文字**当代码（误报 ✓），又会漏掉**换了个写法**的同类（漏报 ✓）。
> 本例里两种错**同时**发生在同一版守卫上 ✓ —— 只做正对照是发现不了的 ✓
> （**负对照 + 全仓扫描面非空**同样必要 ✓）。

另：清理脚本第一版从**错的 cwd** 跑 ✓ ⇒ 相对路径 `app/routers` 解析到不存在的目录 ✓
⇒ 输出"删除 0 处" ✓（**脚本报 0 时要先怀疑扫描面** ✓，不要以为"本来就没有" ✓）。

**实测**：`read_json_guard_test` **10/10** ✓；九套受影响套件全绿 ✓（含冒烟 476/476 ✓）；套件 **90** ✓。

## S7 第 88 步：**去接前端**，结果挖出一条更值钱的结论 ✗✓（不撒谎的适配器；16/16 ✓）

目标是把 `/production/preflight` 接到前端 ✓。摸完前端（**Nuxt** ✓ `frontend/app/` ✓ API 层 `composables/useApi.ts` ✓
`api.post` 自动脱 `json.data` ✓；分镜页 `pages/drama/[id]/episode/[episodeNumber].vue` **2300+ 行** ✗
⇒ 选**只有 ~300 行**的剧集页 `pages/drama/[id]/index.vue` 作落点 ✓ 它本就是"本剧看板" ✓）后，
要写请求体时发现 ✗✗：

> **前端手里没有 `plan` / `manifest`** ✗ —— 那是**分镜表**与**资产表**里的东西 ✓；
> 而**后端也组装不出来** ✓✗：契约 §6-§11 要的字段，**当前的 `storyboards` / `scenes` 表里根本没有** ✗。

| 契约要素 | 出处 | 现状 |
|---|---|---|
道具**状态** | §8 | ✗ `props` 没有"每镜在谁手上/在哪"的列 |
线索**揭示顺序** | §9 | ✗ 没有"首次可见镜头"的列 |
**画面方向** | §6 | ✗ `scenes` 没有左/右/中后列 |
**转场动机** | §11 | ✗ `storyboards` 只有 `shot_type`/`angle`，没有"为什么切" |
动作的**效果** | §10 | ✗ 只有 `action` 文本，没有"推进了什么" |
每镜**起止状态** | §8/§11 推论 | ✗ 无对应列 |

### 交付：`services/storyboard_continuity.py` ✓（只做**不撒谎**的两件事）

1. **只映射表里真实存在的列** ✓（镜头号/场景/动作文本/时长 ✓），
   ⭐⭐ **绝不给缺的列补默认值** ✗✗ —— 补一句"状态没变"会让 §8 判定**假绿** ✓
   （"道具一直没动"看起来完全合法 ✓ 实际是**信息缺失** ✓，比不做还糟 ✓）；自检 ⑤⑥ 两条钉死它 ✓；
2. **把缺的东西逐条报出来** ✓（`schemaGaps` ✓ 带**契约出处** ✓）——
   这份清单就是"要让连续性真正可强制，需要给 schema 加什么"的**可执行答案** ✓。

适配器**不判任何东西** ✗（判据仍只有 `continuity.py` 一份 ✓）；自检 ⑭⑮ 钉"喂给判定器不崩、
且结果**如实反映信息缺失**（不是因为补了默认值而看起来通过 ✓）" ✓。

> **教训（接口面，新）**：**"接口该长什么样"往往在写调用方时才看清** ✓ ——
> 第 86 步我以为"调用方手里有 plan"✓，真去接前端才发现没有 ✓✗。
> **规则：能在服务端由 id 组装的，就别让调用方拼** ✓；而**组装不出来的**要**如实报缺** ✓
> （`schemaGaps` ✓），不要用默认值把缺口糊过去 ✗。

另：PowerShell 里含 `[` `]` 的路径是**通配符** × ⇒ 必须 `-LiteralPath` ✓（`[episodeNumber].vue` 处踩到 ✓）；
且**内联脚本里混引号**这事我第 N 次踩 ✓ ⇒ 这轮改用 `search_content` 结构化检索 ✓ 一次就拿到结构 ✓。

**实测**：`storyboard_continuity_test` **16/16** ✓；层级 6/6 ✓；套件 **91** ✓。

## S7 第 89 步：⚠️ **上一步的结论是错的** ✓✗ —— 纠错（22/22 ✓，**不加任何表** ✓）

第 88 步我断言「契约 §6-§11 的字段**当前 schema 里没地方放**」✗ ——
处理前顺手一查 `models.py` 的表清单 ✓ 就发现 ✗✗：**`continuity_states` 早就在那儿** ✓★

```text
continuity_states(episode_id, storyboard_id, scene_id,
                  state_type, entity_key, state_value, constraints, meta)
```

⇒ 它正是契约要的那种**通用状态表** ✓：道具状态 ✓ / 画面方向 ✓ / 转场动机 ✓ /
起止状态 ✓ / 线索可见 ✓ **全都装得下** ✓。**不需要加表** ✓（也**没加** ✓ ⇒ 冒烟的白名单不用动 ✓）。

> ⚠️⚠️ **为什么会错**：我只查了 `storyboards` / `scenes` 的**列** ✓，就下了
> "无处安放"的结论 ✗ —— **从没去找"有没有一张专用表"** ✗✗。
> **规则（已入日志，够格进 MEMORY）**：
> **凡是"schema 里没有"这类断言，必须查过"专用表"再说** ✗；
> **先找现成的家，再决定要不要盖房子** ✓ —— 否则会去**加一堆本不需要的表** ✓✗。

### 交付（重写 `services/storyboard_continuity.py` ✓ 22/22 ✓）

1. ⭐ **`STATE_TYPES` 词汇表**（7 条 ✓ 每条标注契约出处 ✓）—— 这才是**真正缺的东西** ✓：
   原表**没规定** `state_type` 该填什么 ✓；现在有了 ✓ 且**逐条对得上契约小节** ✓，不是拍脑袋 ✓：
   `prop`(§8) / `direction`(§6) / `transition`(§11) / `start_state`·`end_state`(接戏) /
   `clue`(§9) / `action`(§10)；
2. **`CONTRACT_MAP`** ✓：六个契约要素**全部**有承载表 ✓（`UNHOMED` 为空 ✓）；
3. ⭐⭐ **`coverage_report`** ✓✓ —— 报每个要素的**分母**（如 `§8 道具状态 3/12 镜` ✓）。
   **没有它，部分数据会得到"全绿"** ✓✗（看起来通过 ✓ 其实只判了四分之一 ✓）；
   且"一镜都没有数据"的要素会**明说「没判」** ✓（不是"通过"✗）；
4. `storyboard_props` 只用来统计覆盖率 ✓ —— **它没有状态** ✗ ⇒ 绝不当作"状态未变" ✗✓
   （覆盖率里专门注明"那些镜的状态是**未知**，不是「没变」"✓）。

### ⚠️ 自检又抓到两处**真问题**（都已修 ✓）

1. **⑲**：`state_type=action` **没法表达"这个动作碰了哪件道具"** ✗✗ ⇒
   §8「状态变化要有交代」**永远判不了** ✓（刀从 on_floor→hand ✓、同镜也有动作 ✓，
   但**没人说那动作碰的就是刀** ✗）⇒ 承载方式定为 `meta` 里的 `{"prop": "9"}` ✓
   （或 `{"props": [...]}` ✓）；
2. **⑳**：状态行的动作**与**分镜原文字段被当成**两个**动作 ✗ ⇒
   §10 会把这一镜判成"可删或可并"✗，而数据其实说了它推进剧情 ✓
   ⇒ 改成 **状态行是权威 ✓、分镜原文只作兜底** ✓（该镜没有状态行时才用 ✓）。

**实测**：`storyboard_continuity_test` **22/22** ✓；`continuity_test` 37/37 ✓；层级 6/6 ✓；套件 **91** ✓（未新增表 ⇒ 冒烟 schema 断言不受影响 ✓）。

## S7 第 90 步：**按 `episodeId` 组装** ✓ —— 前端只剩「挂个组件」（17/17 ✓）

`POST /production/preflight` 现在支持 `{"episodeId": 3}` ✓：后端**自己取数**
（分镜/场景/角色/道具 + `continuity_states` + `storyboard_props` ✓）→ 适配器 → 判定 ✓
⇒ 前端**一个调用**拿到结论 ✓（不必知道 plan/manifest 长什么样 ✓）。
新增 `services/preflight_source.py` ✓（**取数归取数、判据归判据** ✓ —— 取数错了只影响"喂进去什么"✓）。

### ⭐ 又找到两个**现成的家**（延续第 89 步的教训 ✓ 先找再盖 ✓）

* **验收状态**：`storyboards.asset_status`（默认 `missing` ✓）**早就有** ✓ —— 不必新造 ✓；
  但它是**逐镜**列 ✓ ⇒ 这里取「该资产**最早需要它的那一镜**」的状态 ✓ 并**在 notes 里写明取法** ✓
  （不做隐性换算 ✗）；
* **逐镜角色（含服装 ✓）**：`storyboard_characters.costume` ✓ —— §7 角色连续性要的正是它 ✓。

### ⚠️⚠️ 本步最要紧的一条：**没有分镜 ⇒ 绝不能给绿灯** ✗✗

纯函数那边"没给的内容就跳过" ✓ ⇒ 于是**不存在的集**会看起来「可开跑」✗✗
（**没有任何证据却给绿灯** ✓ —— 最坏的一类误导 ✓）。⇒ 在源层补一条**阻断** ✓：
`ready=False` + 「这一集**没有分镜行** ⇒ 无从体检 ✓（**不等于通过** ✗）」✓ +
行动项换成"先生成分镜"✓ 并**撤掉**"可以开跑"✓（自检 ⑩⑪⑫ 钉死 ✓）。

### 三条口径（与既有模块**保持一致** ✓）

1. ⭐ **`generated` 不算验收通过** ✗（与第 83 步验收门同一条语义 ✓ —— 同一仓库里
   对同一个词给两种口径就完了 ✓）；词汇表 `ASSET_STATUSES` 里**只有 `approved` 为真** ✓；
2. ⭐⭐ **认不出的 `asset_status` ⇒ 按最保守处理**（当 `missing` ✓）**并报出来** ✓✓
   —— **默认放行是更坏的选择** ✗；
3. ⭐ **`dependencies` 一律空要明说** ✓✓ —— 空数组会被 :func:`.asset_manifest.generation_order`
   当成"无依赖"✓，而真相是 **schema 没这一列** ✗（**信息缺失** ✓ 不是真无依赖 ✗）；
   仍无承载的 manifest 字段逐条列在 `MANIFEST_GAPS` ✓（依赖 / 验收标准 ✓）。

另：自检 ⑨ 一次红 ✓ 是**我夹具写错**（全给了 `approved` ⇒ "0 已验收"的提示当然不出现 ✓，
实现是对的 ✓）⇒ 换夹具 ✓。

**实测**：`preflight_source_test` **17/17** ✓；`preflight_test` 25/25 ✓；层级 6/6 ✓；
路径+常量 **249 已注册 / 未注册 0 / 漂移 0** ✓；**冒烟 476/476** ✓；套件 **93** ✓。

## S7 第 91 步：**前端面板落地** ✓（链子接完了；⚠️ 但**编译校验没跑成** ✗ 如实记）

### 交付

| 文件 | 内容 |
|---|---|
`frontend/app/composables/useApi.ts` | 新增 `productionAPI` ✓（`preflightForEpisode(id)` / `preflight(payload)` / `schema()` ✓）—— 按本仓约定（**具名 API 对象** ✓，`req` 自动脱 `json.data` ✓） |
`frontend/app/components/PreflightPanel.vue` | 体检面板 ✓：`ready` 灯 + **行动项**（按成本排序 ✓）+ 阻断/建议**带 stage 标签** ✓ + **数据与覆盖率**区 ✓；样式 `scoped` ✓（不依赖全局类名 ✓ ⇒ 不会因为主题改动而崩 ✗） |
`frontend/app/pages/drama/[id]/index.vue` | 挂上：`section-label` + 原生 `<select>` 选集 ✓ + `<PreflightPanel :episode-id=…>` ✓；**选一集才跑** ✓（不自动全跑 ✓ —— 免得一进页面就打一堆查询 ✓） |

★ 面板的四条设计全来自后端那几步踩出来的结论 ✓：
1. **只给 `episodeId`** ✓（后端自己取数组装 ✓ ⇒ 前端不认识 plan/manifest ✓）；
2. ⭐ **`ready` 之外必须显示 `nextActions`** ✓（那是**按成本排序**的清单 ✓ 比"红了几条"有用得多 ✓）；
3. ⭐⭐ **原样显示**后端那句「无从体检 ≠ 通过」✓ —— **不自己改写成"通过"** ✗；
4. ⭐ 界面上**必须能区分「没判」与「通过」** ✗ ⇒ 覆盖率/取数痕迹单独一区 ✓。

刻意**不猜**的地方 ✓：没用 `<BaseSelect>` ✗（我不知道它的 props ✓ —— 猜错就是坏掉的控件 ✗），
用原生 `<select>` ✓（不好看但**一定对** ✓）。

### ⚠️ 如实汇报：**编译校验没跑成** ✗（环境连着把命令判成"后台"✓）

本想用 `@vue/compiler-sfc` **真编译**这两个改动过的 SFC ✓（**肉眼读 .vue 是发现不了语法错的** ✗），
脚本写了两次（第二版放 `frontend/` 下 ✓ —— Node 按**脚本位置**解析依赖 ✓ 不是 cwd ✗），
但**三条命令连续被判"后台运行"** ✗✗ ⇒ **本次编译结果未知** ✓。

* 该跑的校验命令是（在 `frontend/` 下 ✓，需 `@vue/compiler-sfc` ✓）：
  真编译 `app/components/PreflightPanel.vue` 与 `app/pages/drama/[id]/index.vue` ✓；
* ⚠️ **可能留下一个临时脚本** `frontend/_sfc_check.mjs` ✓（清理命令与校验在同一条里 ✓，
  命令若没跑完就不会删 ✓）⇒ **下次会话先删它** ✓（它不是项目资产 ✗）。

> **教训（工具面，第 2 次）**：命令被判"后台"时，**只要有重定向就先去读产物** ✓
> （这条救过两次 ✓）；但**没有重定向的那些就是真的没结果** ✗ ⇒ **不要当它跑过了** ✓，
> 更不要据此声称"已验证" ✓。

## S7 第 92 步：补跑前端编译校验 ✓（**上一轮的欠账已还清** ✓）

第 91 步因命令连续被判"后台" ✗ ⇒ **前端编译结果未知** ✗ 并如实记了账 ✓。
本轮用**同一招**补上 ✓（`write_to_file` 落到 `frontend/_sfc_check.mjs` ✓ +
跑完在同一条命令里删掉 ✓ + **结果重定向到文件** ✓ ⇒ 命令即便被判后台，产物也在 ✓✓）：

```text
OK   app\components\PreflightPanel.vue          ← 真编译（script setup + template ✓）
OK   app\pages\drama\[id]\index.vue
全部通过 ✓
```

⇒ 前端两处改动**编译验证通过** ✓✓（不再是"未知" ✓）；临时脚本与校验在**同一条命令**里 ✓
且产物已生成 ⇒ 脚本**确已删除** ✓（下次会话仍可顺手 `Test-Path` 复核 ✓）。

同时跑了**前端调用点 ↔ 后端路由**守卫 ✓：**5/5** ✓ —— 调用点 **166 → 168** ✓（正是新增的两个 ✓）、
**0 未匹配** ✓、后端 249 条 `(method,path)` ✓ ⇒ 新加的 `productionAPI` 打的是**真实存在**的路由 ✓✓。

> **做法小结（值得复用）**：**凡是"必须跑一次才知道结果"的校验，都写成
> 「落盘脚本 + 重定向输出」** ✓ —— 这样命令被环境判成"后台"也不怕 ✓（产物流在盘上 ✓）；
> 反而**不重定向的校验，一跳过就等于没做** ✗，且很容易被当成做过了 ✓✗。

## S7 第 93 步：**记忆层维护** —— `MEMORY.md` 下移 `TOPICS.md`（8121 → **7685** ✓）+ 新规则入库

守卫连报两轮「`MEMORY.md` 余量仅 23 字符」✗ ⇒ 按本仓约定（`MEMORY.md` 须 ≤8k ✓ 超了会**在半句处被截断** ✓
且**尾部 `## 协作与提交` 最先丢** ✗）先腾位置 ✓，再把第 82→92 步那几条**真正会导致 bug** 的规则写进去 ✓。

### 下移（细节进 `TOPICS.md` ✓ 红线留在 `MEMORY.md` ✓）

新增 `TOPICS.md` §**自 MEMORY.md 下移（2026-09-18，第二次腾 8k 预算）** ✓，收：
* `backend-py/` **迁移明细**（覆盖率数字 / `frozen_ts_source.py` / `PROXY_TO_NODE` 已无对象 ✓）；
* **Skill 体系明细**（注入闸阈值与口径 / DB 配置优先细节 / 宿主工具判据 / 改名核对步骤 / 兜底库 ✓）；
* 另加一节**新引擎与"事前省钱链"速查** ✓（引擎 17 个模块 + 上下文预算 + 生产链四模块
  + 粘合/取数/适配 + 前端面板 ✓ —— 并记下**判据只有一份**这条 ✓）。

`MEMORY.md` 对应处改为**短红线 + 指针** ✓。两处最长条目（画风正负成对 / Skill 库识别）也压成红线 ✓。

### 入库的 4 条新规则（都是本轮**真踩出来的** ✓ 已写进 §代码约定 ✓）

1. **判"代码里有没有某种写法"用 AST，别用正则** ✗（正则把**文档串里的说明**当代码 ✓、又漏
   **换了写法**的同类 ✓）⇒ 守卫三段：合同锚点 + **正/负对照** + 扫描面非空 ✓；
2. **归一化产物与判断常量必须同源** ✗（`-` vs `_` ⇒ "待重做资产被排除出生成顺序" ✓✗）；
   断言钉**后果**，不只钉计数 ✓；
3. **"schema 里没有"先找专用表再说** ✗（`continuity_states` 早就在 ✓）⇒ 先找现成的家 ✓；
4. **"没数据/没读到" ≠ 通过** ✗（纯函数"没给就跳过" ⇒ **空集被读成绿灯** ✓✗）⇒ 外层补阻断 + 报覆盖率 ✓。

**实测**：`MEMORY.md` **7685/8000** ✓（原 8121 ✗）；记忆守卫 **致命 0** ✓（6 篇已登记 / 112 锚点 ✓）。
> 守卫仍会提示「余量仅 315 ⇒ 下轮进内容前先下移」✓ —— 这是**提示**不是致命 ✓；下次再进内容前继续下移 ✓。

## S7 第 94 步：**写入侧** —— 连续性状态落库（18/18 ✓ 链子通了）

前面 82→92 步把"事前省钱链"从判据一路接到**前端** ✓，但那条链一直**没有数据** ✗
（体检只能报"没判" ✓，这是诚实的 ✓ 但也就发挥不出价值 ✓）。本轮补**写入侧** ✓。

### 交付

| 件 | 内容 |
|---|---|
`services/continuity_store.py` | 校验 + **幂等替换** ✓：`normalize_states`（词汇校验 ✓）+ `write_shot_states`（单镜 ✓）+ `write_episode_states`（批量 ✓） |
`routers/preflight.py`（+2 端点） | `PUT /api/v1/production/continuity-states` ✓ + `GET …/continuity-states/vocabulary` ✓（回显词汇表 ✓） |
`tests/continuity_store_test.py` | **18/18** ✓（⚠️ **用临时数据根** ✓ —— 写入测试会真插行 ✓，**绝不能**写进 `data/drama.db` ✗） |

### ⭐⭐ 本步抓到并修掉一个**会毁数据**的 bug

初版 `write_shot_states` 的顺序是「校验 → 归属检查 → **先删旧行** → 写通过校验的行」✗
⇒ 当 `state_type` 填错时 ⇒ **把这一镜原本的状态整片抹掉** ✗✗（自检 ⑩ 当场抓到 ✓，
它断言"词汇错 ⇒ 不写 ✓"时发现旧行也没了 ✓）。

修法 ✓：**有校验问题 ⇒ 本镜一行都不动** ✓✓ —— 并写成明确语义：
> **「拒收」必须是「什么都不做」** ✓ —— **全有或全无（按镜）** ✓；
> **半套状态比没有更糟** ✗（体检会拿残缺的时间线当真 ✓）。

### 四条写入语义（都钉在自检里 ✓）

1. ⭐ **词汇拒收** ✓：`state_type` 不在 `STATE_TYPES` ⇒ **报错 + 回显可选值** ✓（**不是静默忽略** ✗
   —— 静默忽略会让"明明填了却没生效"变成悬案 ✓）；
2. ⭐ **幂等** ✓：同一镜重跑是"先删后写" ✓ ⇒ 行数不涨 ✓（涨了的话体检会把旧行当**另一个状态** ✗ ⇒ 假阳性 ✓）；
   多行也成立 ✓（A 原有 3 行 ⇒ 重写 1 行后剩 1 行 ✓）；
3. ⭐ **只动显式给出的镜** ✓（没提到的镜一行不碰 ✓ —— 不做"清空整集"✗）；
4. ⭐⭐ **归属校验** ✓：`storyboard_id` 必须**真属于**该集 ✓，否则**一条都不写** ✗
   （写坏别的集的状态极难查 ✓）。

### ⭐⭐ 闭环（⑫）

写完 → `run_episode_preflight` **真的看得见** ✓（覆盖率从 `0/N` 变成 `§6 1/`、`§8 2/` ✓）
且**结论仍由判据给** ✓（写数据不会让 `ready` 变假绿 ✗）⇒ **链子通了** ✓。

### ⚠️ 如实说清**边界**（免得以为它能自动补全 ✗）

能从现有列自动推出的只有一小部分 ✗：**`action` 部分可推** ✓（演员来自 `storyboard_characters` ✓、
文本来自 `storyboards.action` ✓），但 **`effects`（五类作用）推不出来** ✗；
**道具状态 / 画面方向 / 转场动机 / 起止状态 / 线索可见** 表里**没有任何列能推** ✗
⇒ **必须由生成阶段（LLM）给出** ✓。
⇒ **LLM 侧（让分镜生成同时输出这些状态）是下一步** ✓ —— 本轮**不假装**它已自动生成 ✗。

**实测**：`continuity_store_test` **18/18** ✓；层级 6/6 ✓（176 个 .py / 违规 0 ✓）；
路径+常量 **251 已注册 / 未注册 0 / 漂移 0** ✓；套件 **95** ✓。

## S7 第 95 步：**LLM 侧** —— 结果发现它早就存在 ✗，而且**带一个数据丢失 bug** ✗✗

### ⚠️ 先纠正前提：LLM 侧**不是不存在** ✗，而是**早就有**

`agent/tools/storyboard_tools.py` 里**已有 `save_continuity_states`** ✓（并被描述为
"*Replaces all previous states*" ✓）⇒ 本步该做的是**对齐 + 修 bug** ✓ 而不是新造 ✓。

### ⭐⭐ 真 bug：**整集 DELETE** ⇒ 分批调用会静默吞掉别的镜

原实现：`DELETE FROM continuity_states WHERE episode_id = ?` ✗ 再插入本次传来的 ✓。
分集一长，LLM **几乎必然分批调用** ✓✗ ⇒ 第二批发来时，**第一批那些镜的状态被整片删掉** ✗✗
（且**无任何提示** ✓）。改成 **按作用域替换** ✓✓：**镜 / 场景 / 集级**各自一桶 ✓，
**没提到的一行不碰** ✓；**显式空数组**仍然 = **明确要求清空本集** ✓（这个既有能力保留 ✓）。

### ⚠️⚠️ 我**两次过度纠正** ✗✓（都是测试把我拉回来的 ✓）

1. 初版把 **`scene_id`** 那档**全拒了** ✗ —— 而 `continuity_states` 本来就有它 ✓、
   工具描述里也写着 *scene space layout* ✓ ⇒ **场景级状态是既有能力** ✗；
2. 接着又把**"两个 id 都空"的集级状态**拒了 ✗ —— 老代码允许它 ✓（测试 64/65/66 钉着 ✓）。

> **规则（已入日志，够格进 MEMORY）**：
> **改一个既有工具的行为前，先读它的测试** ✓✓ —— 测试就是"既有能力"的清单 ✓；
> 我两次"顺手删掉一个档位"都是被测试拦住的 ✓。**要修的是那个 bug（部分列表静默清其余 ✗），
> 不是把整片能力删掉** ✗✓。

### ⭐ 又一处**必须兼容的既存事实**：老 `state_type` 名字

现网/老数据用的是 **TS 时代**的名字（`scene_space` / `prop_state` / `character_pose` ✓），
我的新词汇表会**拒收**它们 ✗✗（等于把既有数据判成非法 ✓）⇒ 加 `LEGACY_STATE_TYPES` ✓：
**照收不拒** ✓、能映射的映射（`prop_state` → §8 道具状态 ✓）、映射不了的**如实说明** ✓
（不静默吞 ✗）。**新写入仍请用 7 条新词** ✓。

### 其它交付与修正

* ⭐ 工具返回里**回灌词汇表** ✓（`allowedStateTypes` / `vocabulary` / `problems` ✓）——
  **工具返回就是模型的反馈通道** ✓ ⇒ 它下一轮能自己改对 ✓✓；
* `continuity_store` 增 **`write_episode_level_states`** ✓（删除**只针对"两个 id 都空"那一桶** ✓，
  **不是**按 `episode_id` 清空 ✗）+ `write_scene_states` ✓；
* ⚠️ 我两处**自己的失误**：① `{**json_string, ...}` ⇒ `TypeError: 'function' object is not a mapping`
  ✗（它是**函数** ✗；顺带发现同文件 `nullable_*` 被当**值**用 ✓✗ —— 既存形状 ✓ 没跟 ✓）；
  ② 集级写入写到了 `with engine.begin()` **外面** ✗ ⇒ `conn` 已关 ✓（缩进错位很隐蔽 ✓ 测试抓到 ✓）；
* ⚠️ 收据断言是**整字典相等** ✗ ⇒ 我**有意扩展**了收据（message/count **逐字不变** ✓ + 反馈字段 ✓），
  并在测试里写明"这是有意扩展" ✓（不是改测试变绿 ✓ —— 核心意图更强了 ✓）。

**实测**：`agent_storyboard_tools_test` **33/33** ✓（32 → 33 ✓）；`continuity_store` **18/18** ✓；
`storyboard_continuity` **22/22** ✓；`agent_context` **25/25** ✓；层级 6/6 ✓；
路径+常量 **251 已注册 / 未注册 0 / 漂移 0** ✓；套件 **95** ✓。

## S7 第 96 步：**真数据最后一公里验收** ✓ —— 一跑就抓到一个"永远红"的结构性问题

写了个**只读**诊断脚本（`tmp/diag_real.py` ✓ 不改一行数据 ✓）对真库跑了一遍 ✓。

### 真库现状（结论本身就有价值 ✓）

```text
episodes=7  storyboards=46  scenes=4  continuity_states=0
```

⇒ **这条链从没被喂过真数据** ✓（`continuity_states` 全空 ✓）——
所以前几步的"18/18/22/22"都是在合成数据上绿的 ✓，**这次才是第一次见真章** ✓。

### ⭐ 真机体检（episode 1 / 20 镜）跑通了，而且**报告是可信的** ✓✓

`ready=False` ✓；**覆盖率全部 `0/20`** ✓ 且**如实写「对应判定完全不成立 ⇒ 没判」** ✓✓
（**没有假装干净** ✓ —— 这正是当初设计覆盖率的目的 ✓）；manifest 给出 6 个资产 / **0 已验收** ✓
并明说「这是**数据问题**不是代码问题」✓。

### ⚠️⚠️ 但阻断项**全是 `table-field`** ⇒ 那是"**永远红**"的结构性问题 ✗✗

阻断全落在 `locations[*]` 缺 §6 的 `left/right/back/foreground/axis` ✓ 与
`characters[*]` 缺 §7 的 `current_state` ✗ ⇒ **只要不填就永远红** ✓✗
（一个"永远报错"的检查 = 噪声 ⇒ 迟早被无视 ✓）。

而这两个**恰恰就是** `scene_space` / `character_pose` 的**家** ✓✓ ⇒
我第 95 步加了**兼容（照收不拒 ✓）**，却**从没去读它们** ✗✗
⇒ **光"收"不"读" = 没兼容** ✗ —— 它们只是从"非法"变成了"**合法但无效**" ✓✗。

### 修法：把场景级/角色级老状态**回填**进 §6/§7 ✓

`build_plan_from_rows` 现在 ✓：
* **场景级**行（`scene_id` 有值、`storyboard_id` 空 ✓）里的 `scene_space` ⇒
  按 `entity_key` 回填 `locations[].{left,right,back,foreground,axis}` ✓；
* `character_pose` ⇒ 回填 `characters[].current_state` ✓；
* 回填**写进 notes** ✓（不做隐性换算 ✓）；
* 自检 ㉓㉔㉕ 钉住 ✓（**22 → 25/25** ✓）。

真库结论**不变** ✓（`ready=False` ✓）—— **这是对的** ✓：真库 0 条状态 ⇒ 没东西可回填 ✓；
但那些字段现在**有可写的家**了 ✓⇒ 链路闭合 ✓（写侧能写 ✓、读侧会读 ✓、体检说得清 ✓）。

> **规则（与"没数据 ≠ 通过"同族）**：**兼容一个老名字，必须同时"读"它** ✗ ——
> **只把它加进白名单 = 让它"合法但无效"** ✓✗（比直接拒收更难查 ✓：
> 数据写进去了 ✓、没报错 ✓、却对结果毫无影响 ✓）。

**实测**：`storyboard_continuity` **25/25** ✓；`preflight_source` 17/17 ✓；
`continuity_store` 18/18 ✓；`preflight` 25/25 ✓；真机体检（只读）跑通 ✓。

## S7 第 97 步：**端到端证明「填齐了就是绿的」** ✓ —— 又抓到两个真问题

新增 `tests/chain_e2e_test.py` ✓（**9/9** ✓ 临时数据根 ✓）：把整条链**串起来跑一次** ✓：

```text
分镜（DB）──▶ save_continuity_states（**真工具** ✓ = LLM 那条路径 ✓）
          ──▶ run_episode_preflight（取数 → 适配 → 判定 → 门 ✓）
```

终于回答了此前只能**推测**的问题 ✓：**把 `STATE_TYPES` 那六样填齐 ⇒ `ready` 真的变 True** ✓✓
（③ 覆盖率满格 ✓、⑤ 结构性误报一条不剩 ✓、⑥ 门开着 ✓、⑦ 判定真跑过（时间线 2 条 / 揭示顺序 1 条）✓）。

### ⭐⭐ 端到端抓到的**真 bug**：同一个数据、两侧写法不同 ⇒ 每镜都报错

`shots[].location` 我打了前缀 **`L5`** ✓，而 `locations[].id` 是**裸 `5`** ✗ ⇒
判定器 `shot.location in locations` **全不匹配** ✓✗ ⇒ **每一镜都报 `unknown-location`** ✓✗
⇒ **真机必然全红** ✓✗（合成数据那几套因为断言写的是同一侧，**测不出来** ✗）。
已修 ✓：两侧统一用**裸 id** ✓，并把 ② 断言改成"两侧同源" ✓。

> **规则（本会话第 2 次）**：**同一份数据的两侧，写法必须同源** ✗ ——
> 上一次是 `-` vs `_`（状态常量 ✓），这次是 `L5` vs `5`（场景 id ✓）。
> **判据两侧各自写死一个拼法 ⇒ 必然漂移** ✓；**且只有端到端能发现** ✓。

### ⭐⭐ 同时抓到**我自己设计里的缺口**（写测试时发现的 ✓）

六项覆盖率**全为 0**（判不了）时 ✓，`ready` **仍然是 True** ✗✗ ⇒ **拿"没判"冒充"通过"** ✓✗
—— 正是本仓反复立的那条规则 ✓。修 ✓：
* 新增**结构化** `coverage_counts()` ✓（`coverage_report` 与它**同源** ✓ —— 判据只有一份 ✓）；
* `run_episode_preflight` 里 **六项全 0 ⇒ 加 `continuity-coverage` 阻断** ✓✓
  并把"先写状态"这条**排到行动项第一位** ✓（真机报告立刻变得可执行 ✓）；
* 自检 ⑧⑨ 一正一反钉住 ✓（清空状态 ⇒ 覆盖率归零 ✓ **且** `ready=False` ✓）。

另：⑥ 一条是**我期望写错** ✓（只链了 1 个角色 ⇒ 资产是 **2** 个 ✓ 不是 3 ✓，实现是对的 ✓）。

**实测**：`chain_e2e` **9/9** ✓（6 → 9 ✓）；`storyboard_continuity` **25/25** ✓；
`preflight_source` **17/17** ✓；真机体检（只读）行动项已变可执行 ✓；套件 **96** ✓。

## S7 第 98 步：**全量复测（发起 ✓ 未跑完 ✗）** —— 已实测 **48 套 / 1971 项 / 0 失败**

一句背景 ✓：96 套的**断言总数**自第 76 步起就没实测过 ✗（文档里一直写"待实测"✓）；
而这一整天又改了 16 步 ✓ ⇒ 该做一次**全回归 + 补数字** ✓。

### 做法（照本仓踩过的坑来 ✓）

写 `tmp/batch_run.py` ✓：分批跑 ✓ + **每批结果落盘** `tmp/batch_*.txt` ✓ +
`--sum` 汇总时**按文件名去重** ✓（批次重叠/重跑会产生重复行 ✓）。
⇒ 命令即便被判"后台运行" ✗，证据也**在盘上** ✓（本会话靠这招救过三次 ✓）。

### 已实测（**分批完成的部分** ✓ 不是推算 ✓）

| 批 | 套 | 项 | 失败 |
|---|---|---|---|
b1（0-24） | 24 | **1411** | 0 ✓ |
b2（24-48） | 24 | **560** | 0 ✓ |
**合计** | **48** | **1971** | **0** ✓ |

★ 其中 **`smoke_test` 476/476** ✓✓ —— 今天 16 步改动**没有破坏冒烟** ✓
（含 schema 断言 / 路由断言 / 记忆-技能守卫 ✓）。

### ⚠️ 未完成的部分（如实记 ✓）

**b3（48-72）与 b4（72-96）仍在后台跑** ✗ ⇒ **96 套的总数仍未实测完整** ✓ ⇒
`run_all.py`/README 里继续维持"**总数待实测**"✗✓ —— **不推算** ✓（本会话因推算栽过两次 ✓）。

**收尾命令**（下次会话直接跑 ✓）::

    cd backend-py
    ..\\backend-py\\.venv\\Scripts\\python.exe ..\\tmp\\batch_run.py 48 72 b3
    ..\\backend-py\\.venv\\Scripts\\python.exe ..\\tmp\\batch_run.py 72 96 b4
    ..\\backend-py\\.venv\\Scripts\\python.exe ..\\tmp\\batch_run.py --sum

> ⚠️ 另：`tmp/batch_run.py` 是**可复用工具** ✓（分批 + 落盘 + 去重汇总 ✓）—— 别当临时垃圾删掉 ✓。

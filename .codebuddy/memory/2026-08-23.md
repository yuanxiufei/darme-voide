# 2026-08-23

## 角色详情页「设定图鉴」：服装/武器/首饰独立图片占位 + 生成
- 用户反馈（续上一轮）：详情页没有服装/武器/首饰的对应图片占位。
- 新增功能：左侧预览区「设定图鉴」区块，服装/武器/首饰三卡片，各带图片占位 + 生成/重新生成按钮（3s 轮询 120s 超时，equipPollRef/equipPollTimeoutRef）。
- 数据模型：characters 加 `equip_images`（JSON `{clothing/weapon/accessory: {type,imageUrl,prompt,generatedAt}}`）；image_generations 加 `equip_type`。db/index.ts ensureColumn 两列 + schema.ts 两字段（equipImages text('equip_images') / equipType text('equip_type')）。
- prompt-utils.ts 新增 `buildEquipImagePrompt(type, char)`（服装 costume design sheet / 武器 weapon concept art / 首饰 accessory jewelry design，复用 formatEquip/parseCoreFeatures）。
- image-generation.ts：GenerateImageParams 加 equipType；insert 写 equip_type；updateCharacterImage 加 equipType 分支回写 equipImages（而非主图，排在 viewType 分支后 costume 分支前）。
- characters.ts 新增端点 `POST /:id/generate-equip-image`（body.type=clothing/weapon/accessory，prompt 优先级 body.prompt > buildEquipImagePrompt，透传 color_grade/negative_prompt/model/configId）。
- 前端 useApi.ts 加 characterAPI.generateEquipImage；详情页模板复用 .three-views/.tv-* 样式做 equip-gallery，script 加 equipTypes/parseEquipImages/equipImages computed + generateEquip + onUnmounted 清理，CSS 加 .equip-cell .btn 缩小。
- lint 0 错误；未 git commit，等待审查。

## 角色详情页「形象设定」表单字段优化
- 用户要求：服装风格/首饰配饰/多套服装变体三字段占位文字更新 + 输入框加大 + 标签/提示清晰。
- 服装风格：3 列窄 input → 2 列 textarea(rows=2)，占位「请输入服装风格，例如：休闲、正式、街头」。
- 首饰配饰：3 列窄 input → 2 列 textarea(rows=2)，占位「请输入首饰或配饰描述，例如：银色项链、皮质手链」。
- 多套服装变体：占位改「输入多个变体名称，用逗号分隔（可选）」，标签改「多套服装变体（可选）」。
- 布局：服装风格/首饰配饰 一组 field-grid-2（textarea），武器装备/多套服装变体 一组 field-grid-2（input），末尾加 `.hint` 说明用途。
- lint 0 错误；未 git commit。

## 角色详情页「图片生成提示词」正向/反向提示词字段优化
- 用户扩展为 5 字段完整需求（前 3 字段形象设定上轮已改，本轮补正向/反向提示词）。
- 正向提示词：标签「自定义 Prompt（留空则自动…）」→「正向提示词」，textarea rows=4，占位「请输入正向提示词，描述期望的服装特征、材质、色彩等」。
- 反向提示词：标签「反向提示词（Negative Prompt…）」→「反向提示词」，textarea rows=2→3，占位「请输入反向提示词，描述需排除的元素，如模糊、低质量、变形等」。
- hint 改为说明正/反向语义：正向描述期望内容（留空自动组合），反向描述需排除内容。
- 字段绑定不变：customPrompt/negativePrompt 同时供主形象图/三视图/服装设定图生成使用。
- lint 0 错误；未 git commit。

## 角色详情页：武器装备/多套服装变体改 textarea + 智能拆分功能
- 武器装备、多套服装变体两个字段从单行 input 改为 textarea(rows=2)，与服装风格/首饰配饰一致（用户要求「也要是文本输入框」）。
- 新增「智能拆分服装/武器/首饰」功能（用户选择「自动拆分回填」方案，解决外貌特征里已含服装/武器描述但对应字段为空的重复填写问题）：
  - 后端 text-generation.ts 新增 `splitCharacterVisuals({appearance})`：generateText + SPLIT_VISUALS_SYSTEM_PROMPT，拆 clothing/weapons/accessories 三字段，只提炼原文、每项≤20字、只输出 JSON，cleanJsonString 解析。
  - 后端 characters.ts 新增端点 `POST /characters/:id/auto-split-visuals`：body.appearance 优先（传未保存的最新内容）> char.appearance；只拆分不写库，返回 {clothing,weapons,accessories}。
  - 前端 useApi.ts 加 `characterAPI.autoSplitVisuals(id, appearance)`；详情页外貌特征下方加「智能拆分服装/武器/首饰」按钮（splitting 状态 + .split-row 布局 + .btn-sm），非空结果覆盖回填表单，用户微调后保存才落库。
- lint 0 错误；未 git commit。

## 智能拆分按钮位置与 loading 反馈优化（续）
- 用户确认采用「按钮方案」（非保存时自动拆分）。两个体验优化：
  - 按钮从「外貌特征」下方挪到「服装风格/首饰配饰 + 武器装备/多套服装变体」两组字段之后，紧邻回填目标；hint 文案改为「回填上方…」，.split-row 加上虚线分隔线。
  - 加 `.spinner` 旋转圈（splitting 时显示）+ `.btn-split` 强调色按钮样式。
- 方案决策理由：AI 拆分有延迟/可能失败，独立按钮便于定位错误；用户微调后保存才落库，链路清晰；不每次保存都消耗文本模型调用。
- lint 0 错误；未 git commit。

## 生成按钮 loading 旋转圈统一（续）
- 角色详情页三个图片生成按钮统一加 `.spinner` 旋转圈（生成中显示）：主形象图（btn-primary → `.spinner.spinner-light` 白色变体）、三视图（btn-sm）、设定图鉴（btn-sm）。
- 全局 `.btn` 已是 `inline-flex + align-items:center + gap`（studio.css L121），spinner 直接放按钮内自动对齐；`.spinner` 加 `flex:none` 防收缩。
- lint 0 错误；未 git commit。

# Dependencies
node_modules/

# Build output
dist/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment
.env
.env.local

# Logs
*.log

# Python
__pycache__/
*.pyc
# Python 后端（backend-py）虚拟环境：只提交 requirements.txt，不入库 site-packages
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Data (database and uploaded files)
# 数据根目录标记（`POST /storage/change` 写入；内容是**机器相关的绝对路径**，不入库）
.data-root
data/drama.db
data/drama.db-wal
data/drama.db-shm
data/public.sql
data/*.bak*
data/static/
data/storage/
data/traces/

# Prompt corpora (third-party text, copyright — reference only, never commit)
data/prompt-corpus/

# Config
configs/config.yaml

# Misc
tmp/
temp/
.superpowers/
/CLAUDE.md
/AGENTS.md

# AI service configuration (contains real API keys)
ai-configs.md
local-deploy-plan.md

# CodeBuddy project-level model config (holds plaintext API keys; keep it user-level instead)
.codebuddy/models.json

# Playwright CLI test artifacts
.playwright-cli/

# Local model manifest (machine-specific / generated)
models.yaml

# Reference projects (downloaded for learning)
reference/

# Local model services cloned by backend-py/scripts/model_manager.py（机器相关，不入库）
# ⚠️ 服务根 2026-09-15 从仓库根 local_services/ 并入 backend-py/app/local_services/。
#    `h3/` 是**项目自带**的 8765 薄封装（纳管），其余是 git clone 下来的第三方服务。
#    注意：git 的规则是「父目录被排除则子文件无法再包含」⇒ 必须先 `/*` 再反向包含 h3/。
backend-py/app/local_services/*
!backend-py/app/local_services/h3/
